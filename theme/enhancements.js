/* NanoHive ABS - JS Enhancements  v6.245.0  (injected build) */

(function () {
  'use strict';

  let nhHomeCover = '';
  let isInjectingHero = false;

  const GOOGLE_FONTS = ["Spectral", "Inter", "Merriweather", "Montserrat", "Playfair Display", "Oswald", "Raleway", "Nunito", "Ubuntu", "Lora", "Work Sans", "Fira Sans", "Poppins", "Cinzel", "Bitter", "Quicksand"];
  let fontLink = document.getElementById('nh-custom-font-link');
  if (!fontLink) {
    fontLink = document.createElement('link');
    fontLink.id = 'nh-custom-font-link';
    fontLink.rel = 'stylesheet';
    const fontQuery = GOOGLE_FONTS.map(f => `family=${f.replace(/ /g, '+')}:wght@400;500;600;700`).join('&');
    fontLink.href = `https://fonts.googleapis.com/css2?${fontQuery}&display=swap`;
    document.head.appendChild(fontLink);
  }

  // ==========================================
  // 1. SETTINGS & CUSTOMIZATIONS
  // ==========================================
  // Built-in defaults are deliberately neutral: a stock Audiobookshelf look with the
  // theme's layout on top. Operators override them via env vars (nginx injects
  // window.NH_CONFIG); users override those from the in-app settings panel.
  // Precedence: saved user settings > operator NH_CONFIG > these.
  const defaultSettings = {
    appName: "",
    showLogoText: true,
    colorizeLogo: false,
    logoUrl: "",
    accentColor: "#e0c27a",
    baseTheme: "warm",
    mainFont: "Merriweather",
    fontScale: 1.0,
    carouselTiming: 15,
    hideRailSeries: false,
    hideRailCollections: false,
    hideRailAuthors: false,
    hideRailNarrators: false,
    hideRailStats: false,
    hideHomeRecentlyAdded: false,
    hideHomeRecentSeries: false,
    hideHomeContinueSeries: false,
    hideHomeListenAgain: false,
    hideHomeDiscover: false,
    hideHomeNewAuthors: false,
    showCustomRecentSeries: true,
    recentSeriesCount: 12,
    customSeriesCards: true,
    showHeroCarousel: true,
    usersSort: 'time', // Settings -> Users card grid: 'time' | 'name'
    familyStats: true,  // A5: share your listening TIME (totals + per-day) - the ranking
    shareReading: true,  // share WHICH books (titles, progress, finished); the
                         // ADMIN-level switches ship off, so nothing shows
                         // until an admin enables them (Pawel's split)
    shareRatingName: true, // show my username next to my ratings - same model:
                           // user ON, global admin switch OFF until enabled
    shareRatings: true,    // share my ratings with other users at all; OFF =
                           // only I (and admins, marked) see them
    hcSync: false,   // #26: push my ratings to Hardcover as I rate
    hcKey: '',       // the user's own Hardcover API token - personal, never a default
    narratorsCards: true,    // redesigned narrators grid (A9)
    collectionsPages: true,  // collections landing + detail redesign (A10)
    usersCards: true,        // Settings -> Users card grid
    statsRanking: true,      // Server Ranking / family board on the stats page
    communityRatings: true,  // Goodreads community score on book pages + sort/filter (#27)
    accountPhoto: true,      // profile photo in the appbar
    cinematicBg: true,       // blurred cover backdrop on home/series/author pages
    pageTransitions: true,   // masked page transitions + boot veil
    continueReadingMode: 'combine',
    showRateFinished: true,   // home row: finished books you have not rated
    showMarkFinished: true,   // home row + avatar badge: books to mark finished (#v2.5)
    // OFF by default, unlike the other two: this one changes what comes out of
    // your speakers without being asked. Display features can default on; a
    // playback behaviour should be chosen.
    autoplaySeries: false,   // when a book ends, start the next one in its series
    finishedTools: true,     // "recently finished" + "almost done" cards in your stats
    homeOrder: [],           // home section order; empty = ABS order
    showRatings: true,
    showCardRatings: true,
    // Per-library ratings override, { <libraryId>: true|false }. A library with
    // no entry follows its media type, so a podcast library is out of the box
    // free of book ratings and nobody has to configure anything to get there.
    ratingLibs: {},
    globalSearch: true,
    startPage: '',
    seriesProgressBadge: true, // green/amber tick on a series card (#13)
    modernFilters: true,       // new filter & sort panel; off = ABS's own dropdowns
    // Saved sort/filter per library and mode (#17), e.g. "<libId>:items".
    // Object, so it gets its own copy per user (see the deep-copy loop below).
    libViews: {},
    seriesCoverMode: 'grid' // series header fallback when no cover was uploaded (Pawel: grid is the default)
  };

  const serverSettings = (window.NH_CONFIG && typeof window.NH_CONFIG === 'object') ? window.NH_CONFIG : {};
  // UI-saved server defaults (see the Server Defaults card): sit between env vars
  // and the user's own saved settings.
  const uiServerSettings = (window.NH_SERVER_CONFIG && typeof window.NH_SERVER_CONFIG === 'object') ? { ...window.NH_SERVER_CONFIG } : {};
  // runtime-only key: pre-r19 saves wrote the whole config INCLUDING the then-
  // current themeVersion into server-config.json, and that fossil then shadowed
  // the REAL build stamp on every later deploy (staging reported the v1.9.1
  // stamp under an r18 container). Never accept it from the stored file.
  delete uiServerSettings.themeVersion;
  // familyStats and shareReading are personal CONSENT switches, not looks - an
  // admin's own choice must never become everyone's silent default (Pawel found
  // a fresh user with sharing off: an old full-dump save had familyStats:false
  // in it). Server-wide policy lives in the Social card / NH_SOCIAL instead;
  // the user switches always start from the built-in defaults (time ON, books
  // OFF) and only their owner moves them.
  delete uiServerSettings.familyStats;
  delete uiServerSettings.shareReading;
  delete uiServerSettings.shareRatingName;
  delete uiServerSettings.shareRatings;
  // #26: the Hardcover token and its switch are personal too
  delete uiServerSettings.hcSync;
  delete uiServerSettings.hcKey;
  // The Goodreads helper address is a server fact, not a look: keep it out of
  // the per-user merge (nhSettings) entirely.
  const nhGoodreadsUrl = uiServerSettings.goodreadsUrl || '';
  delete uiServerSettings.goodreadsUrl;

  // Redesigned pages are the ADMIN's call now (Pawel): whatever the server
  // config says for these keys WINS over a user's stored copy - the toggles
  // only exist on the Administration tab, so a personal value is a fossil.
  const NH_PAGES_KEYS = ['narratorsCards', 'collectionsPages', 'usersCards', 'statsRanking', 'accountPhoto', 'cinematicBg', 'pageTransitions', 'communityRatings'];
  function nhForcePages() {
    NH_PAGES_KEYS.forEach((k) => {
      if (Object.prototype.hasOwnProperty.call(uiServerSettings, k)) nhSettings[k] = uiServerSettings[k] !== false;
    });
  }

  // ===================== PER-USER SETTINGS (GitHub #12) =====================
  // A shared device (a tablet in the kitchen, one browser, three kids) used to
  // have ONE settings blob, so whoever changed the theme changed it for everyone
  // who signed in next. Settings are now namespaced by ABS user id.
  // The id is read STRAIGHT OUT OF localStorage, not the store: this runs at
  // parse time, before $nuxt exists, and settings have to be resolved
  // synchronously or the page paints with the wrong theme and then jumps.
  const NH_SET_BASE = 'nh-settings';
  const NH_SET_CLAIMED = 'nh-settings-claimed';
  // Declared here, next to the other storage keys, rather than beside the sync
  // code that uses them: saveSettings can run during boot, and a const only
  // initialised further down the file would still be in its dead zone.
  const NH_SET_TS = 'nh-settings-ts:';
  const nhPrefs = { dead: false, pulledFor: null, timer: 0, inflight: false };
  // The id comes out of the stored JWT. ABS 2.36 no longer persists its vuex
  // blob to localStorage, so the token is the only synchronous source left (and
  // it is the same thing the njs side reads to identify a caller). The payload is
  // matched with a regex rather than JSON.parse'd: atob returns Latin-1 bytes, so
  // a username with an accent in it would mangle the string and throw, while the
  // id itself is always plain ASCII.
  function nhSettingsUid() {
    try {
      const t = localStorage.getItem('token') || '';
      const p = t.split('.')[1];
      if (p) {
        const raw = atob(p.replace(/-/g, '+').replace(/_/g, '/') + '==='.slice((p.length + 3) % 4));
        const m = /"(?:userId|sub)"\s*:\s*"([^"]+)"/.exec(raw);
        if (m) return m[1];
      }
    } catch (e) {}
    // Pre-2.36 ABS kept the user in a persisted vuex blob.
    try {
      const u = JSON.parse(localStorage.getItem('vuex') || '{}');
      const id = u && u.user && u.user.user && u.user.user.id;
      return id ? String(id) : '';
    } catch (e) {}
    return '';
  }
  function nhSettingsKey(uid) { return uid ? NH_SET_BASE + ':' + uid : NH_SET_BASE; }
  // Reads one user's saved diff, migrating the pre-2.1 shared blob ONCE.
  // Exactly one account inherits it, in practice whoever set the device up.
  // Everyone else starts from the server/env defaults, which is the whole point
  // of the change; inheriting it for all of them would just reproduce the bug
  // with extra steps.
  function nhReadSaved(uid) {
    const key = nhSettingsKey(uid);
    let raw = null;
    try { raw = localStorage.getItem(key); } catch (e) {}
    if (raw === null && uid) {
      try {
        if (!localStorage.getItem(NH_SET_CLAIMED)) {
          const legacy = localStorage.getItem(NH_SET_BASE);
          if (legacy) {
            localStorage.setItem(key, legacy);
            localStorage.setItem(NH_SET_CLAIMED, uid);
            // Stamp it NOW, not epoch. These are the settings the person is
            // using at this moment, so if an older copy of theirs is sitting on
            // the server from another browser, the one in front of them wins.
            localStorage.setItem(NH_SET_TS + uid, String(Date.now()));
            raw = legacy;
          }
        }
      } catch (e) {}
    }
    try { return JSON.parse(raw || '{}') || {}; } catch (e) { return {}; }
  }
  let nhSetUid = nhSettingsUid();
  // book-details.js loads after this file and reads the same store.
  try { window.__nhSettingsKey = function () { return nhSettingsKey(nhSetUid); }; } catch (e) {}

  let nhLastCrMode;
  let nhSettings = { ...defaultSettings, ...serverSettings, ...uiServerSettings };
  try {
    let saved = nhReadSaved(nhSetUid);
    // Legacy full-dump migration. Pre-diff-era saves (<= v1.9.1) dumped EVERY setting
    // into the browser, which pinned the then-current defaults forever, an admin could
    // change the server defaults and a pinned browser would never follow.
    // This used to DISCARD the dump, which cost everyone upgrading from 1.9.x their whole
    // saved look on the first load after the update, with no warning (reported from the
    // field). Converting costs nothing and keeps what they actually chose: any key that
    // still differs from the effective default is kept verbatim; keys that merely restate
    // a default are dropped, which is what unpins them. The result is stamped `_v` and
    // written back, so this runs once per browser and never again.
    // Deliberately the same shape saveSettings writes, the two can never disagree.
    if (saved && !saved._v && Object.keys(saved).length >= 20) {
      const legacyBase = { ...defaultSettings, ...serverSettings, ...uiServerSettings };
      const kept = { _v: 2 };
      Object.keys(saved).forEach((k) => {
        if (k === '_v') return;
        // Never restore the fossil build stamp: pre-r19 dumps carried themeVersion, and
        // restoring it would shadow the real one again (see the delete above).
        if (k === 'themeVersion') return;
        if (!(k in legacyBase)) return; // a setting this build no longer has
        if (JSON.stringify(saved[k]) !== JSON.stringify(legacyBase[k])) kept[k] = saved[k];
      });
      try { localStorage.setItem(nhSettingsKey(nhSetUid), JSON.stringify(kept)); } catch (e2) {}
      saved = kept;
    }
    delete saved._v;
    nhSettings = { ...defaultSettings, ...serverSettings, ...uiServerSettings, ...saved };
  } catch (e) {}
  // The spreads above are shallow, so any object/array default (ratingLibs,
  // homeOrder) is the SAME object as the one in defaultSettings. Editing it in
  // place would also edit the default that saveSettings diffs against, and the
  // change would be dropped as "same as default", silently unsaveable. Give
  // nhSettings its own copies so a nested edit is always visible as a change.
  Object.keys(nhSettings).forEach((k) => {
    const v = nhSettings[k];
    if (v && typeof v === 'object') { try { nhSettings[k] = JSON.parse(JSON.stringify(v)); } catch (e) {} }
  });

  // Admin kill-switches (B6): lock* keys in the UI server config hard-disable a
  // feature for EVERY user, they win over personal settings unconditionally.
  const NH_LOCKS = { lockRatings: 'showRatings', lockCardRatings: 'showCardRatings', lockGlobalSearch: 'globalSearch', lockHeroCarousel: 'showHeroCarousel' };
  Object.keys(NH_LOCKS).forEach((lk) => { if (uiServerSettings[lk]) nhSettings[NH_LOCKS[lk]] = false; });
  nhForcePages();
  function nhIsLocked(settingKey) {
    return Object.keys(NH_LOCKS).some((lk) => NH_LOCKS[lk] === settingKey && uiServerSettings[lk]);
  }

  // ---- per-library ratings ---------------------------------------------------
  // Ratings are a book idea. A server with a podcast library alongside its books
  // was being asked to rate episodes it had finished, and the "rate what you
  // finished" row pooled both libraries into one list (github #2, reddit #5).
  // Media type decides the default; an explicit entry in ratingLibs overrides it
  // either way, so a podcast library can still opt in and a book library out.
  function nhCurLibId() {
    const m = location.pathname.match(/\/library\/([^/]+)/);
    if (m) return m[1];
    try { return window.$nuxt.$store.state.libraries.currentLibraryId || ''; } catch (e) { return ''; }
  }

  function nhLibMediaType(libId) {
    try {
      const st = window.$nuxt.$store;
      if (!libId || st.state.libraries.currentLibraryId === libId) {
        const mt = st.getters['libraries/getCurrentLibraryMediaType'];
        if (mt) return mt;
      }
      const f = (st.state.libraries.libraries || []).find((l) => l.id === libId);
      if (f) return f.mediaType || '';
    } catch (e) {}
    return '';
  }

  function nhRatingsLibOn(libId) {
    const m = (nhSettings.ratingLibs && typeof nhSettings.ratingLibs === 'object') ? nhSettings.ratingLibs : {};
    if (libId && Object.prototype.hasOwnProperty.call(m, libId)) return m[libId] !== false;
    return nhLibMediaType(libId) !== 'podcast';
  }
  // book-details.js resolves its own settings but should not re-derive this; on
  // an /item/ page nhCurLibId() falls back to the store's current library, which
  // ABS sets to the item's own library.
  window.__nhRatingsHere = function () { return nhRatingsLibOn(nhCurLibId()); };
  // "Anonymous" for masked rating names - book-details.js renders the rows but
  // this file owns the 40-language tables, so it borrows the label
  window.__nhAnonLabel = function () { const T = nhGsT(); return T.rtAnon || PANEL_T.en.rtAnon; };

  const baseThemes = {
    warm:  { name: 'Warm Dark', canvas: '#181512', rail: '#120f0d', raised: '#221e1a', rgb: '24, 21, 18', appbar: 'rgba(24, 21, 18, 0.70)' },
    slate: { name: 'Cool Slate', canvas: '#111625', rail: '#0d111c', raised: '#1a2238', rgb: '17, 22, 37', appbar: 'rgba(17, 22, 37, 0.70)' },
    black: { name: 'True Black', canvas: '#080808', rail: '#050505', raised: '#141414', rgb: '8, 8, 8', appbar: 'rgba(8, 8, 8, 0.70)' },
    navy:  { name: 'Deep Navy', canvas: '#0a111a', rail: '#070c12', raised: '#101b29', rgb: '10, 17, 26', appbar: 'rgba(10, 17, 26, 0.70)' },
    mocha: { name: 'Mocha', canvas: '#231c18', rail: '#1c1613', raised: '#2e2520', rgb: '35, 28, 24', appbar: 'rgba(35, 28, 24, 0.70)' },
    pine:  { name: 'Deep Pine', canvas: '#121a15', rail: '#0e1410', raised: '#19241d', rgb: '18, 26, 21', appbar: 'rgba(18, 26, 21, 0.70)' },
    plum:    { name: 'Plum',    canvas: '#1a1320', rail: '#140e19', raised: '#261b2e', rgb: '26, 19, 32', appbar: 'rgba(26, 19, 32, 0.70)' },
    crimson: { name: 'Crimson', canvas: '#1d1212', rail: '#160d0d', raised: '#2b1b1b', rgb: '29, 18, 18', appbar: 'rgba(29, 18, 18, 0.70)' },
    ocean:   { name: 'Ocean',   canvas: '#0b1618', rail: '#081011', raised: '#122124', rgb: '11, 22, 24', appbar: 'rgba(11, 22, 24, 0.70)' },
    sand:    { name: 'Sand',    canvas: '#1c1814', rail: '#15120f', raised: '#2a241d', rgb: '28, 24, 20', appbar: 'rgba(28, 24, 20, 0.70)' },
    steel:   { name: 'Steel',   canvas: '#13171c', rail: '#0e1114', raised: '#1e242b', rgb: '19, 23, 28', appbar: 'rgba(19, 23, 28, 0.70)' },
    wine:    { name: 'Wine',    canvas: '#1a1014', rail: '#140c0f', raised: '#281820', rgb: '26, 16, 20', appbar: 'rgba(26, 16, 20, 0.70)' }
  };

  const presetColorsRow1 = ['#c88d36', '#5b8a62', '#4f728c', '#836589', '#b85b49', '#b5767a'];
  const presetColorsRow2 = ['#ff9800', '#4caf50', '#2196f3', '#9c27b0', '#f44336', '#e91e63'];
  const presetColorsRow3 = ['#d4b383', '#8c9a83', '#798492', '#9b859d', '#c08779', '#a89f91'];
  const presetColorsRow4 = ['#e0c27a', '#7fa7c4', '#a88bbf', '#d98c7a', '#6fae8e', '#c77fa0'];
  const presetColorsRow5 = ['#ffc107', '#00bcd4', '#673ab7', '#8bc34a', '#ff5722', '#03a9f4'];
  const presetColors = [...presetColorsRow1, ...presetColorsRow2, ...presetColorsRow3, ...presetColorsRow4, ...presetColorsRow5];

  function hexToRgba(hex, alpha) {
    if (!hex || typeof hex !== 'string') hex = '#c88d36';
    const r = parseInt(hex.slice(1, 3), 16) || 200;
    const g = parseInt(hex.slice(3, 5), 16) || 141;
    const b = parseInt(hex.slice(5, 7), 16) || 54;
    return `rgba(${r}, ${g}, ${b}, ${alpha})`;
  }

  // Browser tab identity: title + favicon follow the customizations. Called
  // from applySettings AND from the tick - Nuxt re-renders <head> on route
  // changes, so a one-shot swap silently reverts to ABS's stock icon.
  // Stock icon links we removed, so they can be put back. nh-early.js removes
  // them before first paint and leaves them on window for us (#28), otherwise
  // clearing the logo left the page with no icon at all until a reload.
  const nhFavStock = (window.__nhFavStock || []).slice();
  // A custom logo has to be a usable image source: http(s), a data: image, or a
  // same-origin path (#28). A password manager's autofilled username or a
  // half-typed value must never be saved or applied, it removes the stock icon
  // for a broken one and Chrome paints a fallback letter 'A' instead.
  function nhLogoOk(v) {
    v = String(v || '').trim();
    return /^(https?:\/\/\S+|data:image\/[a-z0-9.+-]+[;,]\S*|\/[^/\s]\S*)$/i.test(v) ? v : '';
  }
  // Nuxt re-inserts its own <link rel=icon> when it re-renders <head>, and the
  // ~80ms reactive tick was slow enough that the browser sometimes latched onto
  // the stock icon first - which is why the custom one appeared only sometimes.
  // A head observer re-asserts in the SAME frame as the insertion. Guarded so a
  // storm of head writes cannot loop: the callback is coalesced per frame, and
  // our own link edits are ignored while we are inside the function.
  let nhFavBusy = false, nhFavQueued = false;
  function nhFavWatchHead() {
    if (!document.head || document.head.dataset.nhFavWatch) return;
    document.head.dataset.nhFavWatch = '1';
    try {
      new MutationObserver(() => {
        if (nhFavBusy || nhFavQueued) return;
        nhFavQueued = true;
        requestAnimationFrame(() => { nhFavQueued = false; try { nhApplyTabIdentity(); } catch (e) {} });
      }).observe(document.head, { childList: true });
    } catch (e) {}
  }

  function nhApplyTabIdentity() {
    nhFavBusy = true;
    try { nhApplyTabIdentityInner(); } finally { nhFavBusy = false; }
    nhFavWatchHead();
  }
  // Firefox restores tabs from the bfcache without re-running the tick fast
  // enough for the tab strip's first paint - re-assert the moment the page is
  // shown or refocused.
  window.addEventListener('pageshow', () => { try { nhApplyTabIdentity(); } catch (e) {} });
  window.addEventListener('focus', () => { try { nhApplyTabIdentity(); } catch (e) {} });

  function nhApplyTabIdentityInner() {
  // Tab identity follows the customizations (user request): the browser
  // title carries the custom app name, the favicon follows the custom logo.
  // Nuxt rewrites the title on route changes - the tick re-brands it within
  // ~80ms; the replace is a no-op once done.
  try {
    const appName = (nhSettings.appName || '').trim();
    if (appName && /audiobookshelf/i.test(document.title)) {
      document.title = document.title.replace(/audiobookshelf/ig, appName);
    }
    const fav = nhLogoOk(nhSettings.logoUrl);
    let favHref = fav;
    if (fav && nhSettings.colorizeLogo) {
      // match the colorized appbar logo: same accent tint, done on canvas
      const tinted = nhFavTint(fav, nhSettings.accentColor || '#e0c27a');
      if (tinted) favHref = tinted;
    }
    // Chrome won't reliably refresh the tab icon when an EXISTING link's
    // href changes (a refresh falls back to the cached default) - the
    // stock icon links are disabled and OUR link node is re-created fresh
    // whenever the target changes.
    let favLink = document.getElementById('nh-favicon');
    if (favHref) {
      // Firefox keeps painting a stock icon link whose rel we merely renamed
      // (Pawel: correct until a refresh, then the default is back), so remove
      // them outright - remembered so they can be restored if the custom logo
      // is cleared.
      document.querySelectorAll('link[rel*="icon"]:not(#nh-favicon)').forEach((l) => {
        if (l.rel !== 'nh-icon-disabled') {
          nhFavStock.push({ rel: l.dataset.nhOff || l.rel, href: l.getAttribute('href'), type: l.getAttribute('type'), sizes: l.getAttribute('sizes') });
        }
        l.remove();
      });
      if (!favLink || favLink.dataset.src !== favHref) {
        if (favLink) favLink.remove();
        favLink = document.createElement('link');
        favLink.id = 'nh-favicon';
        favLink.rel = 'icon';
        // Firefox repaints the tab icon far more reliably for a URL it has never
        // used for a favicon before - reusing one it already resolved (even via
        // a fresh <link>) is the remaining "sometimes it's the old icon" case.
        // A marker query makes ours distinct from any stock use of the file.
        // Never on data: URLs (the tinted canvas) - a query would corrupt them.
        favLink.href = /^data:/i.test(favHref) ? favHref
          : favHref + (favHref.indexOf('?') < 0 ? '?' : '&') + 'nhfav=1';
        favLink.dataset.src = favHref;
        document.head.appendChild(favLink);
      }
    } else {
      if (favLink) favLink.remove();
      document.querySelectorAll('link[data-nh-off]').forEach((l) => {
        l.rel = l.dataset.nhOff;
        delete l.dataset.nhOff;
      });
      while (nhFavStock.length) { // restore the ones we removed
        const o = nhFavStock.shift();
        // Nuxt may have re-rendered <head> with the same links meanwhile.
        if (o.href && Array.from(document.querySelectorAll('link[rel*="icon"]')).some((x) => x.rel === o.rel && x.getAttribute('href') === o.href)) continue;
        const l = document.createElement('link');
        l.rel = o.rel;
        if (o.href) l.href = o.href;
        if (o.type) l.type = o.type;
        if (o.sizes) l.setAttribute('sizes', o.sizes);
        document.head.appendChild(l);
      }
    }
  } catch (e) {}
  }

  function saveSettings() {
    // Store only the keys that actually DIFFER from the operator/server defaults.
    // The old full-object dump pinned EVERY value the moment a user touched ONE
    // toggle - after which admin changes to the server defaults never reached that
    // browser again (and the README's "only touched keys are stored" was a lie).
    // Equal-to-default keys are dropped, so they keep tracking future defaults.
    const base = { ...defaultSettings, ...serverSettings, ...uiServerSettings };
    const diff = { _v: 2 }; // marks post-diff-era saves for the legacy migration
    Object.keys(nhSettings).forEach((k) => {
      if (JSON.stringify(nhSettings[k]) !== JSON.stringify(base[k])) diff[k] = nhSettings[k];
    });
    localStorage.setItem(nhSettingsKey(nhSetUid), JSON.stringify(diff));
    try { localStorage.setItem(NH_SET_TS + nhSetUid, String(Date.now())); } catch (e) {}
    nhPrefsPush(diff);
  }

  // ---------- settings that follow a user between browsers (GitHub #12) ----------
  // localStorage stays the source of truth AT BOOT: it is synchronous, so the page
  // paints the right theme immediately with no fetch to wait on and no flash. The
  // server copy is a sync channel on top - pulled once shortly after load, pushed
  // (debounced) on every save. Whichever side was written last wins.
  // A dead or absent endpoint just disables the feature; nothing here can break
  // local settings, which is why the pull is allowed to be late and lossy.
  function nhPrefsLocalTs(uid) {
    try { return parseInt(localStorage.getItem(NH_SET_TS + uid), 10) || 0; } catch (e) { return 0; }
  }
  function nhPrefsPush(diff) {
    if (nhPrefs.dead || !nhSetUid) return;
    const tok = nhSrToken();
    if (!tok) return;
    // Debounced: dragging the font-scale slider fires saveSettings on every step.
    clearTimeout(nhPrefs.timer);
    nhPrefs.timer = setTimeout(() => {
      const ts = Date.now();
      fetch('/_nh/api/prefs', {
        method: 'POST',
        headers: { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/json' },
        body: JSON.stringify({ settings: diff, ts: ts }),
      }).then((r) => {
        if (r.status === 404) nhPrefs.dead = true; // older proxy without the endpoint
        else if (r.ok) { try { localStorage.setItem(NH_SET_TS + nhSetUid, String(ts)); } catch (e) {} }
      }).catch(() => {});
    }, 800);
  }
  function nhPrefsPull() {
    if (nhPrefs.dead || nhPrefs.inflight || !nhSetUid || nhPrefs.pulledFor === nhSetUid) return;
    const tok = nhSrToken();
    if (!tok) return;
    nhPrefs.inflight = true;
    nhPrefs.pulledFor = nhSetUid;
    const uid = nhSetUid;
    fetch('/_nh/api/prefs', { headers: { Authorization: 'Bearer ' + tok } })
      .then((r) => {
        if (r.status === 404) { nhPrefs.dead = true; return null; }
        return r.ok ? r.json() : null;
      })
      .then((j) => {
        nhPrefs.inflight = false;
        if (!j || uid !== nhSetUid) return; // user switched while in flight
        const srvTs = Number(j.ts) || 0;
        const localTs = nhPrefsLocalTs(uid);
        let localRaw = null;
        try { localRaw = localStorage.getItem(nhSettingsKey(uid)); } catch (e) {}
        // Nothing on the server yet, but something here worth keeping: seed it.
        if (!srvTs || !j.settings) { if (localRaw) nhPrefsPush(JSON.parse(localRaw)); return; }
        if (srvTs <= localTs) return; // ours is the same or newer
        nhAdoptSettings(j.settings, uid, srvTs);
      })
      .catch(() => { nhPrefs.inflight = false; nhPrefs.pulledFor = null; });
  }
  // Swap in a settings diff that came from somewhere else (the server copy, or a
  // user change in this same tab) and repaint. The panel is REMOVED rather than
  // patched: every control read nhSettings when it was built, so rebuilding it is
  // the only way to be sure nothing still shows the old value. The tick puts it
  // straight back.
  function nhAdoptSettings(diff, uid, ts) {
    const clean = { ...(diff || {}) };
    delete clean._v;
    delete clean.themeVersion;
    nhSettings = { ...defaultSettings, ...serverSettings, ...uiServerSettings, ...clean };
    Object.keys(nhSettings).forEach((k) => {
      const v = nhSettings[k];
      if (v && typeof v === 'object') nhSettings[k] = Array.isArray(v) ? v.slice() : { ...v };
    });
    Object.keys(NH_LOCKS).forEach((lk) => { if (uiServerSettings[lk]) nhSettings[NH_LOCKS[lk]] = false; });
    nhForcePages();
    try {
      localStorage.setItem(nhSettingsKey(uid), JSON.stringify({ _v: 2, ...clean }));
      if (ts) localStorage.setItem(NH_SET_TS + uid, String(ts));
    } catch (e) {}
    try { applySettings(); } catch (e) {}
    const p = document.getElementById('nh-settings-panel');
    if (p && !p.closest('#nh-settings-modal')) p.remove();
  }
  // ---------- ABS's OWN preferences, per user (#12 follow-up) ----------
  // hobesman asked whether playback speed follows a user too. Measured: ABS keeps
  // its user settings in a single device-wide `userSettings` localStorage entry,
  // and no account has a server-side copy - so on a shared browser one person's
  // playback rate, jump amounts, sort order and cover size really do become
  // everyone's. Same disease as the theme's own settings, so the same cure:
  // snapshot the entry per user while they are signed in, and put theirs back
  // when they return. ABS's own action does the writing, so the running app and
  // the stored value can never disagree.
  // First time we see an account there is no snapshot yet, so it keeps whatever
  // the device currently has; from that point on its changes stay its own.
  const NH_ABS_SET = 'userSettings';
  let nhAbsSetLast = '';
  function nhAbsSettingsSnapshot(uid) {
    if (!uid) return;
    try {
      const cur = localStorage.getItem(NH_ABS_SET);
      if (!cur || cur === nhAbsSetLast) return; // only on a real change
      nhAbsSetLast = cur;
      localStorage.setItem(NH_ABS_SET + ':' + uid, cur);
    } catch (e) {}
  }
  function nhAbsSettingsRestore(uid) {
    if (!uid) return;
    try {
      const mine = localStorage.getItem(NH_ABS_SET + ':' + uid);
      if (!mine) { nhAbsSetLast = ''; nhAbsSettingsSnapshot(uid); return; }
      nhAbsSetLast = mine;
      if (mine === localStorage.getItem(NH_ABS_SET)) return;
      const parsed = JSON.parse(mine);
      localStorage.setItem(NH_ABS_SET, mine);
      const st = window.$nuxt && window.$nuxt.$store;
      if (st) st.dispatch('user/updateUserSettings', parsed);
    } catch (e) {}
  }

  // Signing in does not reload the page in ABS, so the id we resolved at parse
  // time can go stale mid-session - on a shared device that is the exact moment
  // the wrong person's theme would otherwise stick.
  function nhSettingsUserWatch() {
    const uid = nhSettingsUid();
    if (uid === nhSetUid) {
      nhAbsSettingsSnapshot(uid);
      nhPrefsPull();
      return;
    }
    nhSetUid = uid;
    nhPrefs.pulledFor = null;
    nhAdoptSettings(nhReadSaved(uid), uid, 0);
    nhAbsSettingsRestore(uid);
    nhPrefsPull();
  }

  // Accent-tinted favicon (colorizeLogo): the logo's alpha is kept, every
  // opaque pixel becomes the accent colour - the canvas twin of the CSS
  // mask the appbar uses. Async; the tick swaps the favicon in once the
  // dataURL is cached. Remote logos that taint the canvas fall back to the
  // untinted file.
  const nhFav = { key: '', url: '', pending: false };
  function nhFavTint(src, accent) {
    const key = src + '|' + accent;
    if (nhFav.key === key) return nhFav.url || null;
    if (nhFav.pending) return null;
    nhFav.pending = true;
    const im = new Image();
    im.onload = () => {
      try {
        const cv = document.createElement('canvas');
        cv.width = im.naturalWidth || 64;
        cv.height = im.naturalHeight || 64;
        const ctx = cv.getContext('2d');
        ctx.drawImage(im, 0, 0);
        ctx.globalCompositeOperation = 'source-in';
        ctx.fillStyle = accent;
        ctx.fillRect(0, 0, cv.width, cv.height);
        nhFav.url = cv.toDataURL('image/png');
      } catch (e) { nhFav.url = ''; }
      nhFav.key = key;
      nhFav.pending = false;
    };
    im.onerror = () => { nhFav.url = ''; nhFav.key = key; nhFav.pending = false; };
    im.src = src;
    return null;
  }

  // ABS's home shelf ids -> the setting that hides that shelf. Read off a live server
  // (`/api/libraries/<id>/personalized`); ids are language-independent, which the
  // heading text this used to match on is not. continue-listening / continue-reading
  // are deliberately absent - that shelf is the hero carousel's business (crMode).
  const NH_HIDE_BY_SHELF_ID = {
    'recently-added': 'hideHomeRecentlyAdded',
    'recent-series': 'hideHomeRecentSeries',
    'continue-series': 'hideHomeContinueSeries',
    'listen-again': 'hideHomeListenAgain',
    'discover': 'hideHomeDiscover',
    'newest-authors': 'hideHomeNewAuthors'
  };

  function applySettings() {
    try {
      let style = document.getElementById('nh-dynamic-styles');
      if (!style) {
        style = document.createElement('style');
        style.id = 'nh-dynamic-styles';
        document.head.appendChild(style);
      }

      const accentHover = nhSettings.accentColor + 'ee';
      const accentShadow = hexToRgba(nhSettings.accentColor, 0.30);
      const theme = baseThemes[nhSettings.baseTheme] || baseThemes.warm;
      const fontScale = nhSettings.fontScale || 1.0;

      let css = `
        :root {
          --nh-canvas: ${theme.canvas} !important;
          --nh-rail: ${theme.rail} !important;
          --nh-raised: ${theme.raised} !important;
          --nh-raised-hover: ${theme.raisedHover || theme.raised} !important;
          --nh-appbar-bg: ${theme.appbar} !important;
          --nh-bg-rgb: ${theme.rgb} !important;
          --nh-amber: ${nhSettings.accentColor} !important;
          --nh-amber-hover: ${accentHover} !important;
          --nh-amber-shadow: ${accentShadow} !important;
          --nh-serif: "${nhSettings.mainFont}", "Spectral", Georgia, serif !important;
          --nh-font-scale: ${fontScale} !important;
        }
      `;

      if (nhLastCrMode === undefined) nhLastCrMode = nhSettings.continueReadingMode;
      if (nhLastCrMode !== nhSettings.continueReadingMode) {
        nhLastCrMode = nhSettings.continueReadingMode;
        const hc = document.getElementById('nh-hero-container');
        if (hc) hc.remove();
        document.querySelectorAll('[data-hero-injected="true"]').forEach(function (r) {
          r.style.display = '';
          Array.from(r.children).forEach(function (c) { if (c.id !== 'nh-hero-container') c.style.display = ''; });
          delete r.dataset.heroInjected;
        });
      }

      const nhStock = nhSettings.customSeriesCards === false;
      if (document.documentElement.classList.contains('nh-stock-series') !== nhStock) {
        document.documentElement.classList.toggle('nh-stock-series', nhStock);
        // ABS measured its dummy card under the other mode's CSS; re-measure now.
        const bs = document.getElementById('bookshelf');
        const bvm = bs && bs.__vue__;
        if (bvm && typeof bvm.setCardSize === 'function') {
          Promise.resolve(bvm.setCardSize()).then(function () { if (typeof bvm.executeRebuild === 'function') bvm.executeRebuild(); }).catch(function () {});
        }
      }

      // Card hover polish: softer dim, accent play control, stars fade in with
      // the hover overlay. This used to be a toggle; it is now unconditional
      // (Pawel: "it should be always on and that's it"). Deliberately NOT gated
      // on nhSettings.cardHoverPolish any more -- anyone who had switched it off
      // has that false persisted in their browser, and reading the key would pin
      // them to the stock look forever with no UI left to change it back.
      {
        css += `[id^="cover-area-"] div[class*="bg-black/40"] { background-color: rgba(0,0,0,0.22) !important; }
        [id^="cover-area-"] div[class*="hover:scale-110"] { color: var(--nh-amber, #e0c27a) !important; filter: drop-shadow(0 2px 5px rgba(0,0,0,0.4)); opacity: 0.72; transition: opacity 0.15s ease; }
        [id^="cover-area-"] div[class*="hover:scale-110"]:hover { opacity: 1; }
        [id^="cover-area-"] div[class*="hover:scale-110"] .material-symbols { font-size: 2.4rem !important; }
        `;
        /* The rating badge used to fade in with this hover overlay, with a
           (hover: none) escape hatch for touch. That was two designs -- Pawel
           asked for one -- and on a laptop with a touchscreen the escape hatch
           does not fire at all, so the badge stayed hover-only there. It now
           lives in the card caption and is always visible; see the A7 block in
           core.js. Nothing to gate here any more. */
      }
      if (!nhSettings.showLogoText) css += `#appbar h1 { display: none !important; } #page-wrapper img[alt="Audiobookshelf Logo"] + h1 { display: none !important; } `;
      if (nhSettings.hideRailSeries) css += `[aria-label="Library Sidebar"] a[href$="/bookshelf/series"] { display: none !important; } `;
      if (nhSettings.hideRailCollections) css += `[aria-label="Library Sidebar"] a[href$="/bookshelf/collections"] { display: none !important; } `;
      if (nhSettings.hideRailAuthors) css += `[aria-label="Library Sidebar"] a[href$="/bookshelf/authors"] { display: none !important; } `;
      if (nhSettings.hideRailNarrators) css += `[aria-label="Library Sidebar"] a[href$="/narrators"] { display: none !important; } `;
      if (nhSettings.hideRailStats) css += `[aria-label="Library Sidebar"] a[href$="/stats"] { display: none !important; } `;

      nhApplyTabIdentity();

      // CSS-only Logo Colorization via Safe DOM Insertion
      // Appbar logo + the login page header (no #appbar there; anchored on its alt text)
      const logoImgs = document.querySelectorAll('#appbar a[href$="/"] img, #page-wrapper img[alt="Audiobookshelf Logo"]');
      logoImgs.forEach(function (img) {
        if (!img.dataset.origSrc) img.dataset.origSrc = img.getAttribute('src');
        const targetSrc = nhLogoOk(nhSettings.logoUrl) || img.dataset.origSrc;
        document.documentElement.style.setProperty('--nh-logo-url', `url("${targetSrc}")`);

        const aTag = img.parentElement;
        if (nhSettings.colorizeLogo) {
          aTag.classList.add('nh-logo-colorized');
        } else {
          aTag.classList.remove('nh-logo-colorized');
          if (img.getAttribute('src') !== targetSrc) {
            img.setAttribute('src', targetSrc);
          }
        }
      });

      if (style.textContent !== css) style.textContent = css;

      if (nhSettings.mainFont && nhSettings.mainFont.toLowerCase() !== 'spectral') {
        let customFontLink = document.getElementById('nh-custom-font-link');
        if (!customFontLink) {
          customFontLink = document.createElement('link');
          customFontLink.id = 'nh-custom-font-link';
          customFontLink.rel = 'stylesheet';
          document.head.appendChild(customFontLink);
        }
        const fontUrl = `https://fonts.googleapis.com/css2?family=${nhSettings.mainFont.replace(/ /g, '+')}:wght@400;500;600;700&display=swap`;
        if (customFontLink.href !== fontUrl) customFontLink.href = fontUrl;
      }

      document.querySelectorAll('#appbar a[href$="/"] h1, #page-wrapper img[alt="Audiobookshelf Logo"] + h1').forEach(function (brand) {
        brand.textContent = nhSettings.appName || 'audiobookshelf';
      });

      const shelves = nhShelfRows();
      shelves.forEach(row => {
        const h2 = row.querySelector('h2');
        if (!h2) return;
        const title = h2.textContent.trim().toLowerCase();
        // ABS's own shelf id where the row exposes one - both home views do. The title
        // text below is a FALLBACK only: it is matched in English and Polish, so on a
        // German, French or Spanish server every one of these toggles silently did
        // nothing at all. The id is the same string in every language.
        const shelfKey = NH_HIDE_BY_SHELF_ID[nhShelfId(row)];

        let hide = false;
        if (shelfKey) {
          hide = !!nhSettings[shelfKey];
        } else {
          if (nhSettings.hideHomeRecentlyAdded && (title.includes('recently added') || title.includes('ostatnio dodane') || title.includes('niedawno dodane'))) hide = true;
          if (nhSettings.hideHomeRecentSeries && (title.includes('recent series') || title.includes('ostatnie serie') || title.includes('najnowsze serie'))) hide = true;
          if (nhSettings.hideHomeContinueSeries && (title.includes('continue series') || title.includes('kontynuuj seri'))) hide = true;
          if (nhSettings.hideHomeListenAgain && (title.includes('listen again') || title.includes('słuchaj ponownie'))) hide = true;
          if (nhSettings.hideHomeDiscover && (title.includes('discover') || title.includes('odkry'))) hide = true;
          if (nhSettings.hideHomeNewAuthors && (title.includes('newest authors') || title.includes('authors') || title.includes('autor'))) hide = true;
        }
        // Our expanded Recent Series row replaces the native one wholesale, so that
        // shelf goes whichever way it was identified.
        if ((shelfKey === 'hideHomeRecentSeries' || (!shelfKey && (title.includes('recent series') || title.includes('ostatnie serie') || title.includes('najnowsze serie'))))
          && nhSettings.showCustomRecentSeries && nhSettings.customSeriesCards !== false) hide = true;

        if (hide) {
          row.style.display = 'none';
        } else if (row.style.display === 'none' && !row.dataset.heroInjected) {
          row.style.display = '';
        }
      });
    } catch (err) {
      // Catch silently so Vue execution isn't halted
    }
  }

  // `invert` reads and writes the SETTING inverted, so a show* key can be shown as
  // a "Hide …" row. That is what the Hide Homepage Shelves group needs: every row
  // there must mean the same thing (on = hidden), and showRateFinished is the one
  // key stored the other way round. Inverting the stored value instead would flip
  // the row for every existing user and every saved server default.
  function createToggle(labelText, settingKey, invert) {
    const wrapper = document.createElement('div');
    // admin-locked feature: show a static note instead of a dead toggle
    if (nhIsLocked(settingKey)) {
      wrapper.className = 'flex items-center py-2';
      const T2 = nhGsT();
      wrapper.innerHTML = '<span class="nh-lock-chip">🔒</span><p class="pl-3 text-gray-500 text-sm">' + labelText + ' · ' + (T2.lockedNote || PANEL_T.en.lockedNote) + '</p>';
      return wrapper;
    }
    wrapper.className = 'flex items-center py-2 cursor-pointer group';

    const readTog = () => (invert ? nhSettings[settingKey] === false : !!nhSettings[settingKey]);
    const renderBtnClass = (checked) =>
      checked ? 'bg-success border-success justify-end' : 'bg-primary border-black-100 justify-start';

    wrapper.innerHTML = `
      <div>
        <button type="button" class="border rounded-full flex items-center ${renderBtnClass(readTog())}" style="width: 40px; transition: all 0.2s;">
          <span class="rounded-full border border-black-50 shadow-sm bg-white" style="width: 20px; height: 20px;"></span>
        </button>
      </div>
      <p class="pl-4 text-gray-200 group-hover:text-white transition-colors text-sm">${labelText}</p>
    `;

    wrapper.addEventListener('click', () => {
      const next = !readTog();
      nhSettings[settingKey] = invert ? !next : next;
      saveSettings();
      wrapper.querySelector('button').className = `border rounded-full flex items-center ${renderBtnClass(next)}`;
      applySettings();
    });

    return wrapper;
  }

  // Same shape as createToggle, but the state lives in a map keyed by library
  // rather than in a settings key of its own, and "unset" is a real third state:
  // a library with no entry follows its media type, which is how a podcast
  // library ends up off without anyone configuring it.
  function createLibRatingToggle(lib) {
    const wrapper = document.createElement('div');
    wrapper.className = 'flex items-center py-2 cursor-pointer group';
    wrapper.dataset.libId = lib.id;
    const renderBtnClass = (checked) =>
      checked ? 'bg-success border-success justify-end' : 'bg-primary border-black-100 justify-start';
    // The type tag explains why a library starts switched off. It is noise when
    // the name already says it ("Podcasts - podcasts"), so only add it when the
    // name carries neither the localised word nor the near-universal loanword.
    const tag = nhGsT().rlPodcast || PANEL_T.en.rlPodcast;
    const nm = lib.name.toLowerCase();
    const redundant = nm.indexOf(tag.toLowerCase()) >= 0 || nm.indexOf('podcast') >= 0;
    const label = lib.name + (lib.mt === 'podcast' && !redundant ? ' · ' + tag : '');
    wrapper.innerHTML = `
      <div>
        <button type="button" class="border rounded-full flex items-center ${renderBtnClass(nhRatingsLibOn(lib.id))}" style="width: 40px; transition: all 0.2s;">
          <span class="rounded-full border border-black-50 shadow-sm bg-white" style="width: 20px; height: 20px;"></span>
        </button>
      </div>
      <p class="pl-4 text-gray-200 group-hover:text-white transition-colors text-sm"></p>
    `;
    wrapper.querySelector('p').textContent = label;
    wrapper.addEventListener('click', () => {
      const next = !nhRatingsLibOn(lib.id);
      // Replace the map rather than mutating it - see the deep-copy note where
      // nhSettings is built.
      const map = Object.assign({}, (nhSettings.ratingLibs && typeof nhSettings.ratingLibs === 'object') ? nhSettings.ratingLibs : {});
      map[lib.id] = next;
      nhSettings.ratingLibs = map;
      saveSettings();
      wrapper.querySelector('button').className = `border rounded-full flex items-center ${renderBtnClass(next)}`;
      applySettings();
    });
    return wrapper;
  }

  function renderThemeButtons(container) {
     container.innerHTML = '';
     container.className = '';
     container.style.display = 'grid';
     container.style.gridTemplateColumns = 'repeat(3, minmax(0, 1fr))';
     container.style.gap = '10px';
     Object.entries(baseThemes).forEach(([k, v]) => {
        const btn = document.createElement('button');
        const isActive = nhSettings.baseTheme === k;
        btn.type = 'button';
        btn.textContent = v.name;
        btn.style.cssText = `padding: 11px 8px; border-radius: 10px; font-size: 0.72rem; font-weight: 600; text-align: center; cursor: pointer; color: #f4eee2; background: ${v.canvas}; border: 2px solid ${isActive ? 'var(--nh-amber)' : 'rgba(255,255,255,0.10)'}; box-shadow: ${isActive ? '0 0 12px var(--nh-amber-shadow)' : 'none'}; transition: border-color .15s, box-shadow .15s, transform .15s;`;
        btn.addEventListener('mouseenter', () => { if (!isActive) btn.style.transform = 'translateY(-2px)'; });
        btn.addEventListener('mouseleave', () => { btn.style.transform = 'none'; });
        btn.addEventListener('click', () => {
           nhSettings.baseTheme = k;
           saveSettings(); applySettings();
           renderThemeButtons(container);
        });
        container.appendChild(btn);
     });
  }

  function renderFontButtons(container) {
     container.innerHTML = '';
     container.className = 'flex flex-wrap gap-2 mt-2 mb-2';
     GOOGLE_FONTS.forEach(f => {
        const btn = document.createElement('button');
        const isActive = nhSettings.mainFont === f;
        btn.type = 'button';
        btn.className = `px-4 py-2 rounded-lg border-2 ${isActive ? 'border-accent bg-white/10 text-white shadow-lg scale-105 z-10' : 'border-white/10 bg-primary text-gray-300 hover:border-white/30 hover:scale-105'} transition-all text-sm`;
        btn.style.setProperty('font-family', `"${f}", sans-serif`, 'important');
        btn.textContent = f;
        btn.addEventListener('click', () => {
           nhSettings.mainFont = f;
           saveSettings(); applySettings();
           renderFontButtons(container);
        });
        container.appendChild(btn);
     });
  }

  function updateSwatchesUI() {
    const lowerAccent = (nhSettings.accentColor || '#c88d36').toLowerCase();

    document.querySelectorAll('.nh-color-preset').forEach(btn => {
      const isActive = btn.dataset.color === lowerAccent;
      btn.className = `nh-color-preset w-8 h-8 rounded-full border-2 ${isActive ? 'border-white' : 'border-transparent'} hover:scale-110 transition-transform shadow-md shrink-0`;
    });

    const isCustom = !presetColors.includes(lowerAccent);
    const customLabel = document.querySelector('#nh-custom-color-label');
    if (customLabel) {
       customLabel.className = `relative w-8 h-8 rounded-full flex items-center justify-center shrink-0 cursor-pointer border-2 ${isCustom ? 'border-white' : 'border-gray-500'} hover:scale-110 transition-transform shadow-md ml-4`;
    }
  }

  const NH_HASH = '#nh-customizations';

  // Per-language additions merged into PANEL_T after its definition (keeps the
  // giant one-line dicts untouched). Word-form arrays follow nhWordForm's
  // 2-or-3 form convention; Polish masculine-personal nouns take the genitive
  // for every count but 1 ("1162 Lektorów").
  const NH_T_EXTRA = {
    en: { cmStopped: "stopped", cmScanErrs: "{e} could not be checked (Goodreads errors)", cmScanBusy: "A scan is already running", cmGrErr: "Goodreads not reachable, will try again", cmNoMatchYet: "No Goodreads match found", cmLooking: "Looking on Goodreads…", cmToolHint: "What Goodreads readers think of this book. The ? shows the matched book and lets you change it.", cmSearchPh: "Search Goodreads…", cmNoVotes: "no ratings yet", cmRefresh: "Refresh", cmScanAll: "Scan everything again, not just the missing ones", cmScanDone: "Done: {m} matched · {x} not found · {s} already had one", cmStop: "Stop", cmScanLibs: "Libraries to scan", cmScan: "Scan for ratings", lsLongest: "Longest books", lsSize: "Size on disk", lsDuration: "Total length", lsGenresN: "Genres", lsSeriesN: "Series", lsAuthorsN: "Authors", lsBooks: "Books", lsGlance: "Library at a glance", cmOpen: "Open on Goodreads", cmDetect: "Find it", cmAddr: "Helper address", cmHint: "What Goodreads readers think of a book: the average score and how many people gave one, under the stars on the book page and as a sort and a filter. Book pages fill in by themselves as people browse.", cmGrOff: "Goodreads: not set up", cmSetup: "No helper yet? Run this next to the theme (compose lines below), then press Find it. Any other way of running the image works too: paste its address above.", cmGrOn: "Goodreads: connected", cmGrDown: "Goodreads: set up but not answering", cmGrNoNum: "Goodreads: answering, but this abs-tract build has no rating numbers (use the ratings build, see README)", cmTitle: "Goodreads ratings", cmWrong: "Wrong book?", cmFind: "Find a community rating…", cmNone: "No match", cmUse: "Use this", lfCommunity: "Goodreads rating", mfNatChange: "Change…", lsByTime: "by listening time", rpBadge: 'Open problem reports', wkTitle: 'Minutes listening, last 7 days', wkAvg: 'Daily average', wkRow: 'Days in a row', lsTitle: 'Server statistics', lsAll: 'All libraries', lsPlayed: 'Books played', lsBest: 'Best rated', lsGenres: 'Top genres', srvPick: 'What to include:', srvGRest: 'Features & behaviour', aeBtn: 'Tidy authors', aeTitle: 'Authors with no books', aeNone: 'Every author here has at least one book.', aeRemove: 'Remove', aeDone: 'Removed', aeWorking: 'Removing…', rpMenu: 'Report a problem', rpTitle: 'Report a problem', rpWhat: 'What is wrong?', rpNote: 'Anything else the admin should know? (optional)', rpSend: 'Send report', rpSent: 'Sent. Thanks.', rpFail: 'Could not send', rpMissing: 'Missing or incomplete content', rpQuality: 'Bad audio quality', rpPlay: 'Will not play', rpWrong: 'Wrong book, cover or metadata', rpChapters: 'Chapters are wrong', rpOther: 'Something else', rpCard: 'Reported problems', rpHint: 'Problems users reported from a book page. Resolving one removes it from this list.', rpEmpty: 'Nothing reported.', rpResolve: 'Resolve', rpOpen: 'Open book', apCard: 'Playback', apToggle: 'Autoplay next book in a series', apHint: 'When a book finishes, start the next one in the same series automatically. Only books already in your library, and only ones you have not finished.', apNext: 'Next in series', fdToggle: 'Finished-book tools in your stats', ysFinished: 'Recently finished', ysAlmost: 'Almost done', ysMarkDone: 'Mark as finished', ysEditDate: 'Change the finished date', ysNoDate: 'no date', hmShowing: 'chart shows', ysTitle: 'Your listening', ysStreak: 'Streak', ysCurrent: 'Current', ysLongest: 'Longest', ysDaysTotal: 'Days total', ysPace: 'Pace', ysThisWeek: 'This week', ysPerDay: 'Per listening day', ysBestDay: 'Best day', ysWhen: 'When you listen', ysTopBooks: 'Most listened books', ysTopAuthors: 'Most listened authors', ysTopNarrators: 'Most listened narrators', rfRate: 'Rate', rfSheet: 'Rate this book', rfOpen: 'Open book page', rfPickHint: 'Pick a rating', rfTitle: 'Rate what you finished', rfToggle: 'Rate finished row on home', hoTitle: 'Home section order', hoEmpty: 'Open the home page once, then come back here.', hoReset: 'Reset to default order', ctIcon: 'Icon', ctColour: 'Accent', ctNeedBook: 'Audiobookshelf cannot store an empty collection, so pick at least one book. You can add or remove books later.', colEditIcon: 'Change icon', ciTitle: 'Collection icon', ciReset: 'Match the name automatically', nrForms: ['Narrator', 'Narrators'], auForms: ['Author', 'Authors'], auSearch: 'Filter authors…', colEditDesc: 'Edit description', colBookForms: ['book', 'books'], colForms: ['Collection', 'Collections'], ctTitle: 'Start a collection', ctBlank: 'Blank collection', ctNew: 'New collection', ctNewHint: 'from a template', ctNamePh: 'Collection name', ctSearch: 'Search books or authors to add…', ctCreate: 'Create', ctBack: '‹ Back', ctAddAll: 'Add all books', abTitle: 'Add books', abAdd: 'Add', sbTitle: 'Server Ranking', sbWeek: 'Week', sbMonth: 'Month', sbYear: 'Year', sbAll: 'All time', sbBooks: 'Books', sbTopBooks: 'Most listened', sbTotal: 'Total', sbListeners: 'Listeners', sbAvg: 'Average', avRemove: 'Remove photo', avSet: 'Set photo…', ugListened: 'Listened', ugPhoto: 'Profile picture', ugNever: 'Never', ugSeen: 'Last seen', ugEdit: 'Edit user', ugDelete: 'Delete user', ugAdd: 'Add photo', ugChange: 'Change photo', ugSortName: 'A–Z', ugSortTime: 'Most listened', ugSortSeen: 'Recently active', fsTitle: 'Server ranking', fsJoin: 'Turn on “Share my listening stats” in the customization panel to appear in the server ranking.', fsWaiting: 'Sharing is on, your summary joins the server ranking shortly, along with everyone else on this server.', fsToggle: 'Share my listening stats', rdTitle: 'Who’s reading it', rdFin: 'Finished', lfWho: 'Whose ratings', lfEveryone: 'Everyone', amfHide: 'Hide Mark as finished', amfConfirm: 'Mark this book as finished?', srToggle: 'Share which books you read', srHint: 'Adds which books you are reading, have finished and listen to most to what others can see. Off by default, and the admin’s server switches must be on as well.', seshTitle: 'Recent sessions', seshNow: 'Listening now', socTitle: 'Social', socHint: 'What this server shares between users. Listening time powers the ranking; the other two reveal what people actually read, so they stay off until you switch them on. Applies to everyone at once.', socTime: 'Listening time (server ranking)', socContent: 'Top titles in shared stats', socWho: 'Who’s reading (book pages)', socUsers: 'Per user', socUsersHint: 'Switch someone off and nothing of theirs is shared regardless of their own setting; only you can switch them back. "Opted out" means the person said no themselves.', socOpted: 'opted out', fsCard: 'Server ranking', fsHint: 'Share your listening with the other people on this server: time totals and per-day minutes for the ranking, and, when the admin has switched them on, your top titles and which books you are reading or finished. On by default. Turn it off and everything you shared is deleted.', yirBtn: 'Year in review', yirTitle: 'Your year in listening', yirYear: 'Last 12 months', yirDays: 'Days with listening', yirBest: 'Best day', yirMonths: 'By month', yirNone: 'No listening data yet.', bsCard: 'Book links', bsHint: 'Buttons on the book page for looking a title up elsewhere. Goodreads and your language\u2019s biggest local site are on by default.', bsFind: 'Find on', bsGlobal: 'Everywhere', bsLocal: 'For your language', bsOther: 'Other countries', pgCard: 'Redesigned pages', pgHint: 'Each of these replaces an Audiobookshelf page with the NanoHive version. Turn one off to get the stock page back.', pgNarrators: 'Narrator cards', pgCollections: 'Collection pages', pgUsers: 'User cards (settings)', pgStats: 'Listening ranking', pgPhoto: 'Profile photo in the top bar', pgCinematic: 'Cinematic background', pgTransitions: 'Page transitions', pSearch: 'Search settings…', pSearchNone: 'Nothing matches that.' , erTypeface: "Typeface", erPage: "Page theme", erText: "Text colour", erBg: "Background", erDefault: "Default (ABS)", erAuto: "Auto" , lfByAuthor: "Author › series order", lfBySeries: "Series › order" , lfSecFilters: "More filters", lfSecSort: "Multi-sort", lfAuthor: "Author", lfSeries: "Series", lfTitle: "Title", lfYear: "Year", lfAdded: "Added", lfDuration: "Duration", lfNarrator: "Narrator", lfGenre: "Genre", lfLanguage: "Language", lfProgress: "Progress", lfPgFinished: "Finished", lfPgInProgress: "In progress", lfPgNot: "Not started" , fsHint: 'Share a summary of your listening (totals and per-day minutes) with the other people on this server, it powers the server ranking. On by default. Turn it off and you are removed from the ranking and its totals, and everything you shared is deleted.' , fsHint: 'Share your listening time (totals and per-day minutes) with the other people on this server; it powers the ranking. On by default. Turn it off and other users no longer see you; server admins still see server activity, marked as opted out.', srHint: 'Also lets others see which books you are reading, have finished and listen to most (the who’s-reading row, shared titles). Nothing shows until the admin turns on the matching server switches.' , hcHint: 'Send your ratings to your Hardcover account. Paste the API key from your Hardcover account settings; keys expire after a year.', hcKeyLbl: 'API key', hcToggle: 'Sync new ratings automatically', hcPushAll: 'Push all ratings', hcPushDiff: 'Push differences only', hcDone: 'Done: %d synced, %d unmatched', hcNoKey: 'Paste your Hardcover API key first' , hcMissHead: 'Not found on Hardcover:' , hcHint: 'Send your ratings to your Hardcover account. Paste the API key from your Hardcover account settings.' , tabStyle: 'Style', tabMain: 'Main Page', tabBooks: 'Books', tabFeat: 'Features' , hcImport: 'Import from Hardcover', impMarkQ: 'Mark the %d matched books as finished here too?', impMarkDone: 'Marked %d as finished.' , impHint: 'Bring your ratings over from Goodreads, StoryGraph or Hardcover. Pick the exported CSV here; Hardcover imports directly via the button on its card. Every match is shown to confirm before anything is saved.' , impErrUnknown: 'This does not look like a Goodreads, StoryGraph or Hardcover export: no title and rating columns found.' , fsCard: 'Social', rnToggle: 'Share my name with ratings', rsToggle: 'Share my ratings with other users', socNames: 'Names with ratings', rtAnon: 'Anonymous' },
    pl: { cmStopped: "zatrzymano", cmScanErrs: "{e} nie sprawdzono (błędy Goodreads)", cmScanBusy: "Skanowanie już trwa", cmGrErr: "Goodreads niedostępne, spróbuję ponownie", cmNoMatchYet: "Nie znaleziono na Goodreads", cmLooking: "Szukam na Goodreads…", cmToolHint: "Co czytelnicy Goodreads myślą o tej książce. Znak ? pokazuje dopasowaną książkę i pozwala ją zmienić.", cmSearchPh: "Szukaj w Goodreads…", cmNoVotes: "brak ocen", cmRefresh: "Odśwież", cmScanAll: "Skanuj wszystko od nowa, nie tylko brakujące", cmScanDone: "Gotowe: {m} dopasowanych · {x} nie znaleziono · {s} już miało", cmStop: "Zatrzymaj", cmScanLibs: "Biblioteki do przeskanowania", cmScan: "Skanuj w poszukiwaniu ocen", lsLongest: "Najdłuższe książki", lsSize: "Rozmiar na dysku", lsDuration: "Łączny czas", lsGenresN: "Gatunki", lsSeriesN: "Serie", lsAuthorsN: "Autorzy", lsBooks: "Książki", lsGlance: "Biblioteka w skrócie", cmOpen: "Otwórz na Goodreads", cmDetect: "Znajdź", cmAddr: "Adres pomocnika", cmHint: "Co czytelnicy Goodreads myślą o książce: średnia ocena i liczba głosów, pod gwiazdkami na stronie książki oraz jako sortowanie i filtr. Strony książek uzupełniają się same podczas przeglądania.", cmGrOff: "Goodreads: nie skonfigurowano", cmSetup: "Nie masz jeszcze pomocnika? Uruchom go obok motywu (linie compose poniżej), potem naciśnij Znajdź. Obraz można uruchomić dowolnie inaczej, wtedy wklej jego adres powyżej.", cmGrOn: "Goodreads: połączono", cmGrDown: "Goodreads: skonfigurowano, ale nie odpowiada", cmGrNoNum: "Goodreads: odpowiada, ale ta wersja abs-tract nie podaje ocen (użyj wersji z ocenami, patrz README)", cmTitle: "Oceny Goodreads", cmWrong: "Zła książka?", cmFind: "Znajdź ocenę społeczności…", cmNone: "Brak dopasowania", cmUse: "Użyj tej", lfCommunity: "Ocena Goodreads", mfNatChange: "Zmień…", lsByTime: "wg czasu słuchania", rpBadge: 'Otwarte zgłoszenia problemów', wkTitle: 'Minuty słuchania, ostatnie 7 dni', wkAvg: 'Średnia dzienna', wkRow: 'Dni z rzędu', lsTitle: 'Statystyki serwera', lsAll: 'Wszystkie biblioteki', lsPlayed: 'Odtworzone książki', lsBest: 'Najlepiej oceniane', lsGenres: 'Najczęstsze gatunki', srvPick: 'Co zapisać:', srvGRest: 'Funkcje i zachowanie', aeBtn: 'Uporządkuj autorów', aeTitle: 'Autorzy bez książek', aeNone: 'Każdy autor ma tu co najmniej jedną książkę.', aeRemove: 'Usuń', aeDone: 'Usunięto', aeWorking: 'Usuwanie…', rpMenu: 'Zgłoś problem', rpTitle: 'Zgłoś problem', rpWhat: 'Co jest nie tak?', rpNote: 'Coś jeszcze, co powinien wiedzieć administrator? (opcjonalnie)', rpSend: 'Wyślij zgłoszenie', rpSent: 'Wysłano. Dzięki.', rpFail: 'Nie udało się wysłać', rpMissing: 'Brakująca lub niepełna treść', rpQuality: 'Zła jakość dźwięku', rpPlay: 'Nie odtwarza się', rpWrong: 'Zła książka, okładka lub metadane', rpChapters: 'Błędne rozdziały', rpOther: 'Coś innego', rpCard: 'Zgłoszone problemy', rpHint: 'Problemy zgłoszone przez użytkowników ze strony książki. Rozwiązanie usuwa zgłoszenie z listy.', rpEmpty: 'Brak zgłoszeń.', rpResolve: 'Rozwiąż', rpOpen: 'Otwórz książkę', apCard: 'Odtwarzanie', apToggle: 'Automatycznie odtwarzaj kolejną książkę z serii', apHint: 'Gdy książka się skończy, automatycznie zacznij następną z tej samej serii. Tylko książki już w Twojej bibliotece i tylko nieukończone.', apNext: 'Następna w serii', fdToggle: 'Narzędzia ukończonych książek w statystykach', ysFinished: 'Ostatnio ukończone', ysAlmost: 'Prawie ukończone', ysMarkDone: 'Oznacz jako ukończone', ysEditDate: 'Zmień datę ukończenia', ysNoDate: 'brak daty', hmShowing: 'wykres pokazuje', ysTitle: 'Twoje słuchanie', ysStreak: 'Passa', ysCurrent: 'Obecna', ysLongest: 'Najdłuższa', ysDaysTotal: 'Dni łącznie', ysPace: 'Tempo', ysThisWeek: 'W tym tygodniu', ysPerDay: 'Na dzień słuchania', ysBestDay: 'Najlepszy dzień', ysWhen: 'Kiedy słuchasz', ysTopBooks: 'Najczęściej słuchane książki', ysTopAuthors: 'Najczęściej słuchani autorzy', ysTopNarrators: 'Najczęściej słuchani lektorzy', rfRate: 'Oceń', rfSheet: 'Oceń tę książkę', rfOpen: 'Otwórz stronę książki', rfPickHint: 'Wybierz ocenę', rfTitle: 'Oceń przeczytane', rfToggle: 'Sekcja „Oceń przeczytane” na stronie głównej', hoTitle: 'Kolejność sekcji na stronie głównej', hoEmpty: 'Otwórz stronę główną, a potem wróć tutaj.', hoReset: 'Przywróć domyślną kolejność', ctIcon: 'Ikona', ctColour: 'Kolor', ctNeedBook: 'Audiobookshelf nie potrafi zapisać pustej kolekcji, więc wybierz co najmniej jedną książkę. Później możesz dodawać i usuwać dowolne.', colEditIcon: 'Zmień ikonę', ciTitle: 'Ikona kolekcji', ciReset: 'Dopasuj automatycznie do nazwy', nrForms: ['Lektor', 'Lektorów', 'Lektorów'], auForms: ['Autor', 'Autorów', 'Autorów'], auSearch: 'Filtruj autorów…', colEditDesc: 'Edytuj opis', colBookForms: ['książka', 'książki', 'książek'], colForms: ['Kolekcja', 'Kolekcje', 'Kolekcji'], ctTitle: 'Utwórz kolekcję', ctBlank: 'Pusta kolekcja', ctNew: 'Nowa kolekcja', ctNewHint: 'z szablonu', ctNamePh: 'Nazwa kolekcji', ctSearch: 'Szukaj książek lub autorów…', ctCreate: 'Utwórz', ctBack: '‹ Wstecz', ctAddAll: 'Dodaj wszystkie', abTitle: 'Dodaj książki', abAdd: 'Dodaj', sbTitle: 'Ranking serwera', sbWeek: 'Tydzień', sbMonth: 'Miesiąc', sbYear: 'Rok', sbAll: 'Całość', sbBooks: 'Książki', sbTopBooks: 'Najczęściej słuchane', sbTotal: 'Razem', sbListeners: 'Słuchacze', sbAvg: 'Średnia', avRemove: 'Usuń zdjęcie', avSet: 'Ustaw zdjęcie…', ugListened: 'Odsłuchano', ugPhoto: 'Zdjęcie profilowe', ugNever: 'Nigdy', ugSeen: 'Ostatnio', ugEdit: 'Edytuj użytkownika', ugDelete: 'Usuń użytkownika', ugAdd: 'Dodaj zdjęcie', ugChange: 'Zmień zdjęcie', ugSortName: 'A–Z', ugSortTime: 'Najwięcej odsłuchane', ugSortSeen: 'Ostatnio aktywni', fsTitle: 'Ranking serwera', fsJoin: 'Włącz „Udostępniaj moje statystyki” w panelu personalizacji, aby pojawić się w rankingu serwera.', fsWaiting: 'Udostępnianie włączone, Twoje podsumowanie za chwilę dołączy do rankingu serwera, razem z pozostałymi osobami.', fsToggle: 'Udostępniaj moje statystyki', rdTitle: 'Kto czyta tę książkę', rdFin: 'Ukończone', lfWho: 'Czyje oceny', lfEveryone: 'Wszyscy', amfHide: 'Ukryj Oznacz jako ukończone', amfConfirm: 'Oznaczyć tę książkę jako ukończoną?', srToggle: 'Udostępniaj, jakie książki czytasz', srHint: 'Dodaje do tego, co widzą inni, jakie książki właśnie czytasz, jakie skończyłeś i jakich słuchasz najczęściej. Domyślnie wyłączone; dodatkowo muszą być włączone przełączniki serwera u administratora.', socTitle: 'Funkcje społecznościowe', socHint: 'Co ten serwer udostępnia między użytkownikami. Czas słuchania zasila ranking; pozostałe dwie opcje zdradzają, co kto czyta, więc pozostają wyłączone, dopóki ich nie włączysz. Dotyczy wszystkich naraz.', socTime: 'Czas słuchania (ranking serwera)', socContent: 'Najczęściej słuchane tytuły w statystykach', socWho: 'Kto czyta (strony książek)', socUsers: 'Dla poszczególnych osób', socUsersHint: 'Wyłącz kogoś, a nic z jego danych nie będzie udostępniane niezależnie od jego ustawień; tylko Ty możesz to cofnąć. "Wyłączone przez użytkownika" oznacza, że ta osoba sama odmówiła.', socOpted: 'wyłączone przez użytkownika', fsCard: 'Ranking serwera', fsHint: 'Udostępniaj swoje słuchanie pozostałym osobom na tym serwerze: sumy czasu i minuty dzienne do rankingu, a gdy administrator je włączy, także najczęściej słuchane tytuły oraz książki, które właśnie czytasz lub skończyłeś. Domyślnie włączone. Po wyłączeniu wszystko, co udostępniono, zostaje usunięte.', yirBtn: 'Rok w podsumowaniu', yirTitle: 'Twój rok ze słuchaniem', yirYear: 'Ostatnie 12 miesięcy', yirDays: 'Dni ze słuchaniem', yirBest: 'Najlepszy dzień', yirMonths: 'Wg miesięcy', yirNone: 'Brak danych o słuchaniu.', bsCard: 'Linki do książki', bsHint: 'Przyciski na stronie książki do wyszukania tytułu w innych serwisach. Domyślnie Goodreads i największy serwis w Twoim języku.', bsFind: 'Szukaj w', bsGlobal: 'Wszędzie', bsLocal: 'Dla Twojego języka', bsOther: 'Inne kraje', pgCard: 'Przeprojektowane strony', pgHint: 'Każda z tych opcji zastępuje stronę Audiobookshelf wersją NanoHive. Wyłącz, aby wrócić do oryginalnej.', pgNarrators: 'Karty lektorów', pgCollections: 'Strony kolekcji', pgUsers: 'Karty użytkowników (ustawienia)', pgStats: 'Ranking słuchania', pgPhoto: 'Zdjęcie profilowe na górnym pasku', pgCinematic: 'Tło kinowe', pgTransitions: 'Przejścia między stronami', pSearch: 'Szukaj ustawień…', pSearchNone: 'Brak pasujących ustawień.' , erTypeface: "Krój pisma", erPage: "Motyw strony", erText: "Kolor tekstu", erBg: "Tło", erDefault: "Domyślny (ABS)", erAuto: "Auto" , lfByAuthor: "Autor › kolejność serii", lfBySeries: "Seria › kolejność" , lfSecFilters: "Więcej filtrów", lfSecSort: "Sortowanie wielopoziomowe", lfAuthor: "Autor", lfSeries: "Seria", lfTitle: "Tytuł", lfYear: "Rok", lfAdded: "Dodano", lfDuration: "Długość", lfNarrator: "Lektor", lfGenre: "Gatunek", lfLanguage: "Język", lfProgress: "Postęp", lfPgFinished: "Ukończone", lfPgInProgress: "W trakcie", lfPgNot: "Nierozpoczęte" , fsHint: 'Udostępnij podsumowanie swojego słuchania (sumy i minuty dzienne) pozostałym osobom na tym serwerze, to one zasilają ranking serwera. Domyślnie włączone. Po wyłączeniu znikasz z rankingu serwera i nie liczysz się do jego sum, a udostępnione dane zostają usunięte.' , fsHint: 'Udostępniaj swój czas słuchania (sumy i minuty dzienne) pozostałym osobom na tym serwerze; to on zasila ranking. Domyślnie włączone. Po wyłączeniu inni użytkownicy przestają Cię widzieć; administratorzy serwera nadal widzą aktywność, z oznaczeniem rezygnacji.', srHint: 'Pozwala też innym zobaczyć, jakie książki właśnie czytasz, jakie skończyłeś i jakich słuchasz najczęściej (sekcja "kto czyta", wspólne tytuły). Nic nie będzie widoczne, dopóki administrator nie włączy odpowiednich przełączników serwera.', seshTitle: 'Ostatnie sesje', seshNow: 'Słuchają teraz' , hcHint: 'Wysyłaj swoje oceny na konto Hardcover. Wklej klucz API z ustawień konta Hardcover; klucze wygasają po roku.', hcKeyLbl: 'Klucz API', hcToggle: 'Synchronizuj nowe oceny automatycznie', hcPushAll: 'Wyślij wszystkie oceny', hcPushDiff: 'Wyślij tylko różnice', hcDone: 'Gotowe: %d zsynchronizowano, %d bez dopasowania', hcNoKey: 'Najpierw wklej klucz API Hardcover' , hcMissHead: 'Nie znaleziono w Hardcover:' , hcHint: 'Wysyłaj swoje oceny na konto Hardcover. Wklej klucz API z ustawień konta Hardcover.' , tabStyle: 'Styl', tabMain: 'Strona główna', tabBooks: 'Książki', tabFeat: 'Funkcje' , hcImport: 'Importuj z Hardcover', impMarkQ: 'Oznaczyć też %d dopasowanych książek jako ukończone?', impMarkDone: 'Oznaczono %d jako ukończone.' , impHint: 'Przenieś swoje oceny z Goodreads, StoryGraph lub Hardcover. Wybierz tutaj wyeksportowany plik CSV; Hardcover importuje bezpośrednio przyciskiem na swojej karcie. Każde dopasowanie zobaczysz do potwierdzenia, zanim cokolwiek się zapisze.' , impErrUnknown: 'To nie wygląda na eksport z Goodreads, StoryGraph ani Hardcover: brak kolumn tytułu i oceny.' , fsCard: 'Społeczność', rnToggle: 'Pokazuj moją nazwę przy ocenach', rsToggle: 'Udostępniaj moje oceny innym użytkownikom', socNames: 'Nazwy przy ocenach', rtAnon: 'Anonim' , rnToggle: 'Pokazuj moje imię przy ocenach', socNames: 'Imiona przy ocenach' },
    de: { cmStopped: "abgebrochen", cmScanErrs: "{e} nicht geprüft (Goodreads-Fehler)", cmScanBusy: "Es läuft bereits ein Durchlauf", cmGrErr: "Goodreads nicht erreichbar, neuer Versuch folgt", cmNoMatchYet: "Kein Goodreads-Treffer", cmLooking: "Suche auf Goodreads…", cmToolHint: "Was Goodreads-Leser von diesem Buch halten. Das ? zeigt das zugeordnete Buch und lässt es ändern.", cmSearchPh: "Goodreads durchsuchen…", cmNoVotes: "noch keine Bewertungen", cmRefresh: "Aktualisieren", cmScanAll: "Alles neu prüfen, nicht nur die fehlenden", cmScanDone: "Fertig: {m} zugeordnet · {x} nicht gefunden · {s} hatten schon eine", cmStop: "Stopp", cmScanLibs: "Zu durchsuchende Bibliotheken", cmScan: "Nach Bewertungen suchen", lsLongest: "Längste Bücher", lsSize: "Größe auf der Platte", lsDuration: "Gesamtlänge", lsGenresN: "Genres", lsSeriesN: "Serien", lsAuthorsN: "Autoren", lsBooks: "Bücher", lsGlance: "Bibliothek auf einen Blick", cmOpen: "Auf Goodreads öffnen", cmDetect: "Suchen", cmAddr: "Adresse des Helfers", scmTitle: "Serien-Seite", scmHint: "So sieht die erzeugte Titelgrafik einer Serie aus, wenn kein eigenes Cover hochgeladen wurde (nur dieser Browser).", scmDeck: "Gestapeltes Deck", cmHint: "Was Goodreads-Leser von einem Buch halten: Durchschnittsnote und Anzahl der Stimmen, unter den Sternen auf der Buchseite und als Sortierung und Filter. Buchseiten füllen sich beim Stöbern von selbst.", cmGrOff: "Goodreads: nicht eingerichtet", cmSetup: "Noch kein Helfer? Diesen neben dem Theme starten (Compose-Zeilen unten), dann auf Suchen drücken. Jede andere Art, das Image zu starten, geht auch: Adresse oben eintragen.", cmGrOn: "Goodreads: verbunden", cmGrDown: "Goodreads: eingerichtet, antwortet aber nicht", cmGrNoNum: "Goodreads: antwortet, aber diese abs-tract-Version liefert keine Bewertungszahlen (Ratings-Build nutzen, siehe README)", cmTitle: "Goodreads-Bewertungen", cmWrong: "Falsches Buch?", cmFind: "Community-Bewertung suchen…", cmNone: "Kein Treffer", cmUse: "Diese nehmen", lfCommunity: "Goodreads-Bewertung", mfNatChange: "Ändern…", lsByTime: "nach Hörzeit", sbTitle: 'Server-Rangliste', rpBadge: 'Offene Problemmeldungen', wkTitle: 'Hörminuten, letzte 7 Tage', wkAvg: 'Tagesdurchschnitt', wkRow: 'Tage in Folge', lsTitle: 'Server-Statistiken', lsAll: 'Alle Bibliotheken', lsPlayed: 'Gespielte Bücher', lsBest: 'Am besten bewertet', lsGenres: 'Top-Genres', srvPick: 'Was speichern:', srvGRest: 'Funktionen & Verhalten', aeBtn: 'Autoren aufräumen', aeTitle: 'Autoren ohne Bücher', aeNone: 'Jeder Autor hier hat mindestens ein Buch.', aeRemove: 'Entfernen', aeDone: 'Entfernt', aeWorking: 'Wird entfernt…', rpMenu: 'Problem melden', rpTitle: 'Problem melden', rpWhat: 'Was stimmt nicht?', rpNote: 'Sonst noch etwas für die Administration? (optional)', rpSend: 'Meldung senden', rpSent: 'Gesendet. Danke.', rpFail: 'Senden fehlgeschlagen', rpMissing: 'Fehlender oder unvollständiger Inhalt', rpQuality: 'Schlechte Tonqualität', rpPlay: 'Spielt nicht ab', rpWrong: 'Falsches Buch, Cover oder Metadaten', rpChapters: 'Kapitel stimmen nicht', rpOther: 'Etwas anderes', rpCard: 'Gemeldete Probleme', rpHint: 'Von Nutzern auf einer Buchseite gemeldete Probleme. Erledigen entfernt die Meldung.', rpEmpty: 'Nichts gemeldet.', rpResolve: 'Erledigt', rpOpen: 'Buch öffnen', apCard: 'Wiedergabe', apToggle: 'Nächstes Buch einer Reihe automatisch abspielen', apHint: 'Wenn ein Buch endet, startet automatisch das nächste derselben Reihe. Nur Bücher aus deiner Bibliothek, und nur noch nicht beendete.', apNext: 'Nächstes der Reihe', fdToggle: 'Werkzeuge für beendete Bücher in den Statistiken', ysFinished: 'Zuletzt beendet', ysAlmost: 'Fast fertig', ysMarkDone: 'Als beendet markieren', ysEditDate: 'Abschlussdatum ändern', ysNoDate: 'kein Datum', hmShowing: 'Diagramm zeigt', ysTitle: 'Dein Hören', ysStreak: 'Serie', ysCurrent: 'Aktuell', ysLongest: 'Längste', ysDaysTotal: 'Tage gesamt', ysPace: 'Tempo', ysThisWeek: 'Diese Woche', ysPerDay: 'Pro Hörtag', ysBestDay: 'Bester Tag', ysWhen: 'Wann du hörst', ysTopBooks: 'Meistgehörte Bücher', ysTopAuthors: 'Meistgehörte Autoren', ysTopNarrators: 'Meistgehörte Sprecher', rfRate: 'Bewerten', rfSheet: 'Dieses Buch bewerten', rfOpen: 'Buchseite öffnen', rfPickHint: 'Bewertung wählen', rfTitle: 'Bewerte, was du beendet hast', rfToggle: 'Bereich ‚Bewerten‘ auf der Startseite', hoTitle: 'Reihenfolge der Startseite', hoEmpty: 'Öffne einmal die Startseite und komm dann zurück.', hoReset: 'Standardreihenfolge', nrSearch: 'Sprecher filtern…', nrSortName: 'Name', nrSortBooks: 'Meiste Bücher', nrForms: ['Sprecher', 'Sprecher'], nrBooksForms: ['Buch', 'Bücher'], auForms: ['Autor', 'Autoren'], auSearch: 'Autoren filtern…' , lsTitle: "Server-Statistiken", lsAll: "Alle Bibliotheken", lsPlayed: "Gespielte Bücher", lsBest: "Am besten bewertet", lsGenres: "Top-Genres", rpCard: "Gemeldete Probleme", rpHint: "Von Nutzern auf einer Buchseite gemeldete Probleme. Erledigen entfernt die Meldung.", rpEmpty: "Nichts gemeldet.", rpResolve: "Erledigt", rpOpen: "Buch öffnen", ctIcon: "Symbol", ctColour: "Akzent", ctNeedBook: "Audiobookshelf kann keine leere Sammlung speichern, wähle mindestens ein Buch. Später kannst du beliebig hinzufügen oder entfernen.", colEditIcon: "Symbol ändern", ciTitle: "Sammlungs-Symbol", ciReset: "Automatisch am Namen ausrichten", colEditDesc: "Beschreibung bearbeiten", colBookForms: ["Buch","Bücher"], colForms: ["Sammlung","Sammlungen"], ctTitle: "Sammlung anlegen", ctBlank: "Leere Sammlung", ctNew: "Neue Sammlung", ctNewHint: "aus einer Vorlage", ctNamePh: "Name der Sammlung", ctSearch: "Bücher oder Autoren suchen…", ctCreate: "Anlegen", ctBack: "‹ Zurück", ctAddAll: "Alle Bücher hinzufügen", abTitle: "Bücher hinzufügen", abAdd: "Hinzufügen", sbWeek: "Woche", sbMonth: "Monat", sbYear: "Jahr", sbAll: "Gesamt", sbBooks: "Bücher", sbTopBooks: "Meistgehört", sbTotal: "Gesamt", sbListeners: "Hörer", sbAvg: "Durchschnitt", avRemove: "Foto entfernen", avSet: "Foto wählen…", ugListened: "Gehört", ugPhoto: "Profilbild", ugNever: "Nie", ugSeen: "Zuletzt aktiv", ugEdit: "Nutzer bearbeiten", ugDelete: "Nutzer löschen", ugAdd: "Foto hinzufügen", ugChange: "Foto ändern", ugSortName: "A–Z", ugSortTime: "Meistgehört", ugSortSeen: "Zuletzt aktiv", fsTitle: "Server-Rangliste", fsCard: "Server-Rangliste", fsToggle: "Meine Hörstatistiken teilen", fsJoin: "Aktiviere „Meine Hörstatistiken teilen“ im Anpassungs-Panel, um in der Server-Rangliste zu erscheinen.", fsWaiting: "Teilen ist aktiv, deine Zusammenfassung erscheint gleich in der Server-Rangliste, zusammen mit allen anderen.", fsHint: "Teile eine Zusammenfassung deines Hörens (Summen und Minuten pro Tag) mit den anderen auf diesem Server, sie speist die Server-Rangliste. Standardmäßig aktiv. Beim Ausschalten wirst du aus der Rangliste und ihren Summen entfernt, und alles Geteilte wird gelöscht.", yirBtn: "Jahresrückblick", yirTitle: "Dein Hörjahr", yirYear: "Letzte 12 Monate", yirDays: "Tage mit Hören", yirBest: "Bester Tag", yirMonths: "Nach Monat", yirNone: "Noch keine Hördaten.", bsCard: "Buch-Links", bsHint: "Buttons auf der Buchseite, um einen Titel anderswo nachzuschlagen. Goodreads und die größte Seite deiner Sprache sind vorab aktiv.", bsFind: "Suchen auf", bsGlobal: "Überall", bsLocal: "Für deine Sprache", bsOther: "Andere Länder", pgCard: "Neu gestaltete Seiten", pgHint: "Jede Option ersetzt eine Audiobookshelf-Seite durch die NanoHive-Version. Ausschalten bringt die Originalseite zurück.", pgNarrators: "Sprecher-Karten", pgCollections: "Sammlungs-Seiten", pgUsers: "Nutzer-Karten (Einstellungen)", pgStats: "Hör-Rangliste", pgPhoto: "Profilbild in der Kopfleiste", pgCinematic: "Kino-Hintergrund", pgTransitions: "Seitenübergänge", pSearch: "Einstellungen durchsuchen…", pSearchNone: "Nichts gefunden.", srvPick: "Was speichern:", srvGRest: "Funktionen & Verhalten", apCard: "Wiedergabe", fdToggle: "Werkzeuge für beendete Bücher in den Statistiken", ysNoDate: "kein Datum", hmShowing: "Diagramm zeigt", ysStreak: "Serie", ysCurrent: "Aktuell", ysLongest: "Längste", ysDaysTotal: "Tage gesamt", ysPace: "Tempo", ysPerDay: "Pro Hörtag", ysWhen: "Wann du hörst", ysTopBooks: "Meistgehörte Bücher", ysTopAuthors: "Meistgehörte Autoren", ysTopNarrators: "Meistgehörte Sprecher", rfToggle: "Bereich ‚Bewerten‘ auf der Startseite", hoTitle: "Reihenfolge der Startseite", hoEmpty: "Öffne einmal die Startseite und komm dann zurück.", hoReset: "Standardreihenfolge", ssscmDeck: "Gestapeltes Deck", scmFirst: "Cover des ersten Buchs", scmGrid: "Cover-Raster (Standard)", scmOff: "Ausgeblendet" , erTypeface: "Schriftart", erPage: "Seitenthema", erText: "Textfarbe", erBg: "Hintergrund", erDefault: "Standard (ABS)", erAuto: "Auto" , lfByAuthor: "Autor › Serienfolge", lfBySeries: "Serie › Reihenfolge" , lfSecFilters: "Mehr Filter", lfSecSort: "Mehrfach-Sortierung", lfAuthor: "Autor", lfSeries: "Serie", lfTitle: "Titel", lfYear: "Jahr", lfAdded: "Hinzugefügt", lfDuration: "Dauer", lfNarrator: "Sprecher", lfGenre: "Genre", lfLanguage: "Sprache", lfProgress: "Fortschritt", lfPgFinished: "Beendet", lfPgInProgress: "Laufend", lfPgNot: "Nicht begonnen" , logoUpload: 'Vom Gerät hochladen…', logoUploaded: 'Hochgeladen ✓', logoBadType: 'Nicht unterstützter Bildtyp', logoTooBig: 'Bild zu groß (max. 4 MB)', logoOr: 'oder', bookPage: 'Buchseite', ratingsToggle: 'Community-Bewertungen (Sterne & Rezensionen)', ratingsHint: 'Sternebewertungen mit kurzen Rezensionen auf jeder Buchseite, geteilt mit allen Nutzern dieses Servers.', searchCard: 'Suche', gsToggle: 'Alle Bibliotheken auf einmal durchsuchen', gsHint: 'Eine gemeinsame Ergebnisliste über alle zugänglichen Bibliotheken, mit einem Bibliotheks-Abzeichen an jedem Treffer. Ausschalten stellt das Dropdown pro Bibliothek wieder her.', gsNoResults: 'Keine Ergebnisse', gsSearching: 'Suche…', gsBooks: 'Bücher', gsSeries: 'Serien', gsAuthors: 'Autoren', spTitle: 'Startansicht', spHint: 'Die App immer auf einer gewählten Bibliotheksansicht öffnen (nur dieser Browser). Direkte Links werden nie umgeleitet.', spDefault: 'App-Standard', spViewHome: 'Startseite', spViewLibrary: 'Bibliothek', spViewSeries: 'Serien', spViewCollections: 'Sammlungen', spViewAuthors: 'Autoren', scSet: 'Serien-Cover festlegen', scRemove: 'Serien-Cover entfernen', scEditDesc: 'Serienbeschreibung bearbeiten', cardStars: 'Sterne-Abzeichen auf Buchcovern', hoverPolish: 'Feinschliff beim Karten-Hover (weicheres Abdunkeln, Akzent-Play, Sterne beim Überfahren)', sectUser: 'Deine Einstellungen', sectAdmin: 'Verwaltung', lockTitle: 'Funktionsschalter', lockHint: 'Eine Funktion für alle Nutzer dieses Servers zwangsweise abschalten. Gewinnt gegen persönliche Einstellungen; andere sehen es nach dem nächsten Neuladen.', lockRatings: 'Bewertungen überall deaktivieren', lockCardRatings: 'Sterne-Abzeichen auf Covern deaktivieren', lockGlobalSearch: 'Globale Suche deaktivieren', lockHoverPolish: 'Karten-Hover-Feinschliff deaktivieren', lockHeroCarousel: 'Startseiten-Karussell deaktivieren', lockedNote: 'vom Admin deaktiviert', lfSort: 'Bewertung', lfRated: 'Bewertet', lfUnrated: 'Unbewertet', lfClear: 'Zurücksetzen', lfNone: 'Keine passenden Bücher', lfSection: 'Bewertung', lfWho: 'Wessen Bewertungen', lfEveryone: 'Alle', sdSave: 'Speichern', sdClear: 'Buchbeschreibung wiederherstellen', sdMore: 'Mehr lesen', sdLess: 'Weniger zeigen', shSeries: 'Serie', shBy: 'von ', shMore: ' & mehr', shBookForms: ['Buch', 'Bücher'], shAvg: 'Buch-Schnitt', shRated: 'bewertet', impLabel: 'Bewertungen importieren', impHint: 'Übernimm Bewertungen aus StoryGraph oder Goodreads. Exportiere dort deine Bücher als CSV und wähle die Datei hier aus. Jeder Treffer wird dir zur Bestätigung gezeigt, gespeichert wird erst, wenn du bestätigst.', impBtn: 'CSV-Datei wählen…', impReading: 'Datei wird gelesen…', impMatching: 'Abgleich mit deinen Bibliotheken…', impErrUnknown: 'Das sieht nicht nach einem StoryGraph- oder Goodreads-Export aus. Es sollte deren CSV-Datei sein, mit einer Spalte "Star Rating" oder "My Rating".', impErrEmpty: 'Diese Datei enthält keine Zeilen.', impErrNoRated: 'In dieser Datei sind keine bewerteten Bücher.', impErrRead: 'Die Datei konnte nicht gelesen werden.', impErrLoad: 'Deine Bibliotheken konnten nicht geladen werden. Versuch es gleich noch einmal.', impErrNoLibs: 'Keine Bibliothek nimmt an Bewertungen teil, es gibt also nichts zu importieren.', impTitle: 'Bewertungen importieren', impFrom: '%s-Export: %d Bücher, davon %d bewertet', impLibs: 'Durchsucht: %s', impMatched: 'Zugeordnet', impCheck: 'Einen Blick wert', impCheckHint: 'Nah dran, aber nicht sicher. Prüfe die Zuordnung oder wähle eine andere.', impAlready: 'Bereits von dir bewertet', impAlreadyHint: 'Bleiben unangetastet, außer du wählst Überschreiben.', impNone: 'Nicht in deiner Bibliothek', impNoneHint: 'Auf diesem Server passt nichts zu diesen Einträgen.', impAll: 'Alle', impSkip: 'Diesen überspringen', impDup: 'Duplikat', impByTitle: 'Titel', impYours: 'deine: %s', impReviews: 'Auch geschriebene Rezensionen importieren (%d)', impOver: 'Eigene vorhandene Bewertungen überschreiben', impGo: '%d Bewertungen importieren', impCancel: 'Abbrechen', impCloseBtn: 'Schließen', impWorking: 'Speichern… %d von %d', impDone: '%d Bewertungen importiert.', impFail: '%d konnten nicht gespeichert werden.', rdTitle: 'Wer dieses Buch liest', rdFin: 'Beendet', socTitle: 'Soziale Funktionen', socHint: 'Was dieser Server zwischen Nutzern teilt. Die Hörzeit speist die Rangliste; die anderen beiden verraten, was jemand liest, und bleiben deshalb aus, bis du sie einschaltest. Gilt für alle auf einmal.', socTime: 'Hörzeit (Server-Rangliste)', socContent: 'Top-Titel in geteilten Statistiken', socWho: 'Wer liest gerade (Buchseiten)', socUsers: 'Pro Nutzer', socUsersHint: 'Schaltest du jemanden aus, wird nichts von dieser Person geteilt, egal was sie selbst eingestellt hat; nur du kannst das zurücknehmen. "Selbst abgemeldet" heißt, die Person hat selbst Nein gesagt.', socOpted: 'selbst abgemeldet', fsHint: 'Teile dein Hören mit den anderen auf diesem Server: Zeitsummen und Minuten pro Tag für die Rangliste, und, wenn die Verwaltung es eingeschaltet hat, auch deine Top-Titel und welche Bücher du gerade liest oder beendet hast. Standardmäßig an. Beim Ausschalten wird alles Geteilte gelöscht.' , amfHide: 'Als beendet markieren ausblenden' , amfConfirm: 'Dieses Buch als beendet markieren?' , fsHint: 'Teile eine Zusammenfassung deines Hörens (Summen und Minuten pro Tag) mit den anderen auf diesem Server, sie speist die Server-Rangliste. Standardmäßig aktiv. Beim Ausschalten wirst du aus der Rangliste und ihren Summen entfernt, und alles Geteilte wird gelöscht.' , srToggle: 'Teilen, welche Bücher du liest', srHint: 'Fügt dem, was andere sehen, hinzu, welche Bücher du gerade liest, beendet hast und am meisten hörst. Standardmäßig aus; zusätzlich müssen die Server-Schalter der Verwaltung an sein.' , fsHint: 'Teile deine Hörzeit (Summen und Minuten pro Tag) mit den anderen auf diesem Server; sie speist die Rangliste. Standardmäßig an. Beim Ausschalten sehen andere Nutzer dich nicht mehr; Server-Admins sehen die Aktivität weiterhin, als abgemeldet markiert.', srHint: 'Lässt andere außerdem sehen, welche Bücher du gerade liest, beendet hast und am meisten hörst. Sichtbar wird nichts, bevor die Verwaltung die passenden Server-Schalter einschaltet.', seshTitle: 'Letzte Sitzungen', seshNow: 'Hören gerade' , hcHint: 'Sende deine Bewertungen an dein Hardcover-Konto. Füge den API-Schlüssel aus den Hardcover-Kontoeinstellungen ein; Schlüssel laufen nach einem Jahr ab.', hcKeyLbl: 'API-Schlüssel', hcToggle: 'Neue Bewertungen automatisch synchronisieren', hcPushAll: 'Alle Bewertungen senden', hcPushDiff: 'Nur Unterschiede senden', hcDone: 'Fertig: %d synchronisiert, %d ohne Treffer', hcNoKey: 'Füge zuerst deinen Hardcover-API-Schlüssel ein' , hcMissHead: 'Auf Hardcover nicht gefunden:' , hcHint: 'Sende deine Bewertungen an dein Hardcover-Konto. Füge den API-Schlüssel aus den Hardcover-Kontoeinstellungen ein.' , tabStyle: 'Stil', tabMain: 'Startseite', tabBooks: 'Bücher', tabFeat: 'Funktionen' , hcImport: 'Aus Hardcover importieren', impMarkQ: 'Die %d zugeordneten Bücher auch hier als beendet markieren?', impMarkDone: '%d als beendet markiert.' , impHint: 'Übernimm deine Bewertungen aus Goodreads, StoryGraph oder Hardcover. Wähle hier die exportierte CSV-Datei; Hardcover importiert direkt über den Button auf seiner Karte. Jede Zuordnung wird vor dem Speichern zur Bestätigung angezeigt.' , impErrUnknown: 'Das sieht nicht nach einem Export aus Goodreads, StoryGraph oder Hardcover aus: keine Titel- und Bewertungsspalten gefunden.' , fsCard: 'Soziales', rnToggle: 'Meinen Namen bei Bewertungen zeigen', rsToggle: 'Meine Bewertungen mit anderen teilen', socNames: 'Namen bei Bewertungen', rtAnon: 'Anonym' },
    fr: { cmStopped: "arrêté", cmScanErrs: "{e} non vérifiés (erreurs Goodreads)", cmScanBusy: "Une analyse est déjà en cours", cmGrErr: "Goodreads injoignable, nouvel essai à venir", cmNoMatchYet: "Aucune correspondance sur Goodreads", cmLooking: "Recherche sur Goodreads…", cmToolHint: "Ce que les lecteurs Goodreads pensent de ce livre. Le ? montre le livre associé et permet de le changer.", cmSearchPh: "Chercher sur Goodreads…", cmNoVotes: "pas encore de notes", cmRefresh: "Actualiser", cmScanAll: "Tout réanalyser, pas seulement les manquants", cmScanDone: "Terminé : {m} trouvés · {x} introuvables · {s} en avaient déjà", cmStop: "Arrêter", cmScanLibs: "Bibliothèques à parcourir", cmScan: "Rechercher les notes", lsLongest: "Livres les plus longs", lsSize: "Taille sur le disque", lsDuration: "Durée totale", lsGenresN: "Genres", lsSeriesN: "Séries", lsAuthorsN: "Auteurs", lsBooks: "Livres", lsGlance: "La bibliothèque en un coup d’œil", cmOpen: "Ouvrir sur Goodreads", cmDetect: "Chercher", cmAddr: "Adresse de l’assistant", scmTitle: "Page de série", scmHint: "Apparence de la couverture générée au-dessus du titre d’une série quand aucune couverture n’a été téléversée (ce navigateur uniquement).", scmDeck: "Pile en éventail", cmHint: "Ce que les lecteurs Goodreads pensent d’un livre : note moyenne et nombre de votes, sous les étoiles de la page du livre et comme tri et filtre. Les pages de livres se remplissent d’elles-mêmes au fil de la navigation.", cmGrOff: "Goodreads : non configuré", cmSetup: "Pas encore d’assistant ? Lancez ceci à côté du thème (lignes compose ci-dessous), puis cliquez sur Chercher. Toute autre façon de lancer l’image convient aussi : collez son adresse ci-dessus.", cmGrOn: "Goodreads : connecté", cmGrDown: "Goodreads : configuré mais ne répond pas", cmGrNoNum: "Goodreads : répond, mais cette version d’abs-tract ne fournit pas de notes (utilisez la version « ratings », voir README)", cmTitle: "Notes Goodreads", cmWrong: "Mauvais livre ?", cmFind: "Chercher une note communautaire…", cmNone: "Aucune correspondance", cmUse: "Utiliser", lfCommunity: "Note Goodreads", mfNatChange: "Modifier…", lsByTime: "par temps d'écoute", sbTitle: 'Classement du serveur', rpBadge: 'Signalements ouverts', wkTitle: 'Minutes d\u2019écoute, 7 derniers jours', wkAvg: 'Moyenne quotidienne', wkRow: 'Jours d\u2019affilée', lsTitle: 'Statistiques du serveur', lsAll: 'Toutes les bibliothèques', lsPlayed: 'Livres écoutés', lsBest: 'Les mieux notés', lsGenres: 'Genres principaux', srvPick: 'Quoi inclure :', srvGRest: 'Fonctions et comportement', aeBtn: 'Ranger les auteurs', aeTitle: 'Auteurs sans livre', aeNone: 'Chaque auteur ici a au moins un livre.', aeRemove: 'Supprimer', aeDone: 'Supprimés', aeWorking: 'Suppression…', rpMenu: 'Signaler un problème', rpTitle: 'Signaler un problème', rpWhat: 'Quel est le problème ?', rpNote: 'Autre chose à signaler à l\u2019administrateur ? (facultatif)', rpSend: 'Envoyer', rpSent: 'Envoyé. Merci.', rpFail: 'Envoi impossible', rpMissing: 'Contenu manquant ou incomplet', rpQuality: 'Mauvaise qualité audio', rpPlay: 'Ne se lit pas', rpWrong: 'Mauvais livre, couverture ou métadonnées', rpChapters: 'Chapitres incorrects', rpOther: 'Autre chose', rpCard: 'Problèmes signalés', rpHint: 'Problèmes signalés par les utilisateurs depuis une page de livre. Résoudre retire le signalement.', rpEmpty: 'Aucun signalement.', rpResolve: 'Résoudre', rpOpen: 'Ouvrir le livre', apCard: 'Lecture', apToggle: 'Lire automatiquement le livre suivant d\u2019une série', apHint: 'À la fin d\u2019un livre, démarre automatiquement le suivant de la même série. Uniquement les livres déjà dans votre bibliothèque, et non terminés.', apNext: 'Suivant de la série', fdToggle: 'Outils des livres terminés dans vos statistiques', ysFinished: 'Terminés récemment', ysAlmost: 'Presque fini', ysMarkDone: 'Marquer comme terminé', ysEditDate: 'Modifier la date de fin', ysNoDate: 'sans date', hmShowing: 'le graphique affiche', ysTitle: 'Votre écoute', ysStreak: 'Série', ysCurrent: 'Actuelle', ysLongest: 'Plus longue', ysDaysTotal: 'Jours au total', ysPace: 'Rythme', ysThisWeek: 'Cette semaine', ysPerDay: 'Par jour d\u2019écoute', ysBestDay: 'Meilleur jour', ysWhen: 'Quand vous écoutez', ysTopBooks: 'Livres les plus écoutés', ysTopAuthors: 'Auteurs les plus écoutés', ysTopNarrators: 'Narrateurs les plus écoutés', rfRate: 'Noter', rfSheet: 'Noter ce livre', rfOpen: 'Ouvrir la page du livre', rfPickHint: 'Choisissez une note', rfTitle: 'Notez vos livres terminés', rfToggle: 'Section « À noter » sur l’accueil', hoTitle: 'Ordre des sections d’accueil', hoEmpty: 'Ouvrez la page d’accueil une fois, puis revenez ici.', hoReset: 'Rétablir l’ordre par défaut', nrSearch: 'Filtrer les narrateurs…', nrSortName: 'Nom', nrSortBooks: 'Plus de livres', nrForms: ['Narrateur', 'Narrateurs'], nrBooksForms: ['livre', 'livres'], auForms: ['Auteur', 'Auteurs'], auSearch: 'Filtrer les auteurs…' , lsTitle: "Statistiques du serveur", lsAll: "Toutes les bibliothèques", lsPlayed: "Livres écoutés", lsBest: "Les mieux notés", lsGenres: "Genres principaux", rpCard: "Problèmes signalés", rpHint: "Problèmes signalés par les utilisateurs depuis une page de livre. Résoudre retire le signalement.", rpEmpty: "Aucun signalement.", rpResolve: "Résoudre", rpOpen: "Ouvrir le livre", ctIcon: "Icône", ctColour: "Accent", ctNeedBook: "Audiobookshelf ne peut pas enregistrer une collection vide, choisissez au moins un livre. Vous pourrez en ajouter ou retirer ensuite.", colEditIcon: "Changer l’icône", ciTitle: "Icône de la collection", ciReset: "Faire correspondre au nom automatiquement", colEditDesc: "Modifier la description", colBookForms: ["livre","livres"], colForms: ["Collection","Collections"], ctTitle: "Créer une collection", ctBlank: "Collection vide", ctNew: "Nouvelle collection", ctNewHint: "depuis un modèle", ctNamePh: "Nom de la collection", ctSearch: "Chercher des livres ou des auteurs…", ctCreate: "Créer", ctBack: "‹ Retour", ctAddAll: "Ajouter tous les livres", abTitle: "Ajouter des livres", abAdd: "Ajouter", sbWeek: "Semaine", sbMonth: "Mois", sbYear: "Année", sbAll: "Depuis toujours", sbBooks: "Livres", sbTopBooks: "Les plus écoutés", sbTotal: "Total", sbListeners: "Auditeurs", sbAvg: "Moyenne", avRemove: "Retirer la photo", avSet: "Choisir une photo…", ugListened: "Écouté", ugPhoto: "Photo de profil", ugNever: "Jamais", ugSeen: "Vu récemment", ugEdit: "Modifier l’utilisateur", ugDelete: "Supprimer l’utilisateur", ugAdd: "Ajouter une photo", ugChange: "Changer la photo", ugSortName: "A–Z", ugSortTime: "Les plus écoutés", ugSortSeen: "Actifs récemment", fsTitle: "Classement du serveur", fsCard: "Classement du serveur", fsToggle: "Partager mes statistiques d’écoute", fsJoin: "Activez « Partager mes statistiques d’écoute » dans le panneau de personnalisation pour apparaître dans le classement du serveur.", fsWaiting: "Partage activé, votre résumé rejoint bientôt le classement du serveur, avec tous les autres.", fsHint: "Partagez un résumé de votre écoute (totaux et minutes par jour) avec les autres personnes de ce serveur, il alimente le classement. Activé par défaut. En le désactivant, vous quittez le classement et ses totaux, et tout ce qui a été partagé est supprimé.", yirBtn: "Rétrospective de l’année", yirTitle: "Votre année d’écoute", yirYear: "12 derniers mois", yirDays: "Jours avec écoute", yirBest: "Meilleur jour", yirMonths: "Par mois", yirNone: "Pas encore de données d’écoute.", bsCard: "Liens du livre", bsHint: "Boutons sur la page du livre pour chercher un titre ailleurs. Goodreads et le plus grand site de votre langue sont activés par défaut.", bsFind: "Chercher sur", bsGlobal: "Partout", bsLocal: "Pour votre langue", bsOther: "Autres pays", pgCard: "Pages repensées", pgHint: "Chaque option remplace une page Audiobookshelf par la version NanoHive. Désactivez pour retrouver la page d’origine.", pgNarrators: "Cartes des narrateurs", pgCollections: "Pages des collections", pgUsers: "Cartes des utilisateurs (réglages)", pgStats: "Classement d’écoute", pgPhoto: "Photo de profil dans la barre du haut", pgCinematic: "Arrière-plan cinéma", pgTransitions: "Transitions de pages", pSearch: "Rechercher un réglage…", pSearchNone: "Rien ne correspond.", srvPick: "Quoi inclure :", srvGRest: "Fonctions et comportement", apCard: "Lecture", fdToggle: "Outils des livres terminés dans vos statistiques", ysNoDate: "sans date", hmShowing: "le graphique affiche", ysStreak: "Série", ysCurrent: "Actuelle", ysLongest: "Plus longue", ysDaysTotal: "Jours au total", ysPace: "Rythme", ysPerDay: "Par jour d’écoute", ysWhen: "Quand vous écoutez", ysTopBooks: "Livres les plus écoutés", ysTopAuthors: "Auteurs les plus écoutés", ysTopNarrators: "Narrateurs les plus écoutés", rfToggle: "Section « À noter » sur l’accueil", hoTitle: "Ordre des sections d’accueil", hoEmpty: "Ouvrez la page d’accueil une fois, puis revenez ici.", hoReset: "Rétablir l’ordre par défaut", ssscmDeck: "Pile en éventail", scmFirst: "Couverture du premier livre", scmGrid: "Grille de couvertures (par défaut)", scmOff: "Masquée" , erTypeface: "Police", erPage: "Thème de page", erText: "Couleur du texte", erBg: "Arrière-plan", erDefault: "Par défaut (ABS)", erAuto: "Auto" , lfByAuthor: "Auteur › ordre de série", lfBySeries: "Série › ordre" , lfSecFilters: "Plus de filtres", lfSecSort: "Tri multiple", lfAuthor: "Auteur", lfSeries: "Série", lfTitle: "Titre", lfYear: "Année", lfAdded: "Ajouté", lfDuration: "Durée", lfNarrator: "Narrateur", lfGenre: "Genre", lfLanguage: "Langue", lfProgress: "Progression", lfPgFinished: "Terminés", lfPgInProgress: "En cours", lfPgNot: "Non commencés" , logoUpload: 'Téléverser depuis l’appareil…', logoUploaded: 'Téléversé ✓', logoBadType: 'Type d’image non pris en charge', logoTooBig: 'Image trop lourde (max. 4 Mo)', logoOr: 'ou', bookPage: 'Page du livre', ratingsToggle: 'Notes de la communauté (étoiles et avis)', ratingsHint: 'Des notes avec de courts avis sur chaque page de livre, partagées avec tous les utilisateurs de ce serveur.', searchCard: 'Recherche', gsToggle: 'Chercher dans toutes les bibliothèques à la fois', gsHint: 'Une liste de résultats fusionnée sur toutes vos bibliothèques accessibles, avec un badge de bibliothèque sur chaque résultat. Désactivez pour retrouver le menu par bibliothèque.', gsNoResults: 'Aucun résultat', gsSearching: 'Recherche…', gsBooks: 'Livres', gsSeries: 'Séries', gsAuthors: 'Auteurs', spTitle: 'Page de départ', spHint: 'Ouvrir l’application toujours sur la vue choisie d’une bibliothèque (ce navigateur uniquement). Les liens directs ne sont jamais redirigés.', spDefault: 'Défaut de l’application', spViewHome: 'Accueil', spViewLibrary: 'Bibliothèque', spViewSeries: 'Séries', spViewCollections: 'Collections', spViewAuthors: 'Auteurs', scSet: 'Définir la couverture de série', scRemove: 'Retirer la couverture de série', scEditDesc: 'Modifier la description de la série', cardStars: 'Étoiles sur les couvertures', hoverPolish: 'Finition au survol des cartes (assombrissement plus doux, lecture accentuée, étoiles au survol)', sectUser: 'Vos réglages', sectAdmin: 'Administration', lockTitle: 'Interrupteurs de fonctions', lockHint: 'Désactive de force une fonction pour tous les utilisateurs de ce serveur. Prime sur les réglages personnels ; visible pour les autres à leur prochain rechargement.', lockRatings: 'Désactiver les notes partout', lockCardRatings: 'Désactiver les étoiles sur les couvertures', lockGlobalSearch: 'Désactiver la recherche globale', lockHoverPolish: 'Désactiver la finition au survol', lockHeroCarousel: 'Désactiver le carrousel d’accueil', lockedNote: 'désactivé par l’admin', lfSort: 'Note', lfRated: 'Notés', lfUnrated: 'Non notés', lfClear: 'Effacer', lfNone: 'Aucun livre ne correspond', lfSection: 'Note', lfWho: 'Notes de qui', lfEveryone: 'Tout le monde', sdSave: 'Enregistrer', sdClear: 'Restaurer la description du livre', sdMore: 'Lire la suite', sdLess: 'Réduire', shSeries: 'Série', shBy: 'de ', shMore: ' et plus', shBookForms: ['livre', 'livres'], shAvg: 'moy. des livres', shRated: 'notés', impLabel: 'Importer des notes', impHint: 'Rapatriez vos notes depuis StoryGraph ou Goodreads. Exportez-y vos livres en CSV puis choisissez le fichier ici. Chaque correspondance vous est montrée pour confirmation et rien n’est enregistré avant.', impBtn: 'Choisir le fichier CSV…', impReading: 'Lecture du fichier…', impMatching: 'Correspondance avec vos bibliothèques…', impErrUnknown: 'Cela ne ressemble pas à un export StoryGraph ou Goodreads. Il faut leur fichier CSV, avec une colonne "Star Rating" ou "My Rating".', impErrEmpty: 'Ce fichier ne contient aucune ligne.', impErrNoRated: 'Aucun livre noté dans ce fichier.', impErrRead: 'Le fichier n’a pas pu être lu.', impErrLoad: 'Vos bibliothèques n’ont pas pu être chargées. Réessayez dans un instant.', impErrNoLibs: 'Aucune bibliothèque ne participe aux notes, il n’y a donc rien à importer.', impTitle: 'Importer des notes', impFrom: 'Export %s : %d livres, dont %d notés', impLibs: 'Recherche dans : %s', impMatched: 'Correspondants', impCheck: 'À vérifier', impCheckHint: 'Proche mais incertain. Vérifiez la correspondance ou choisissez-en une autre.', impAlready: 'Déjà notés par vous', impAlreadyHint: 'Laissés tels quels, sauf si vous demandez l’écrasement.', impNone: 'Pas dans votre bibliothèque', impNoneHint: 'Rien sur ce serveur ne correspond à ces entrées.', impAll: 'Tout', impSkip: 'Ignorer celui-ci', impDup: 'doublon', impByTitle: 'titre', impYours: 'la vôtre : %s', impReviews: 'Importer aussi les avis écrits (%d)', impOver: 'Écraser les notes que j’ai déjà', impGo: 'Importer %d notes', impCancel: 'Annuler', impCloseBtn: 'Fermer', impWorking: 'Enregistrement… %d sur %d', impDone: '%d notes importées.', impFail: '%d n’ont pas pu être enregistrées.', rdTitle: 'Qui lit ce livre', rdFin: 'Terminé', socTitle: 'Fonctions sociales', socHint: 'Ce que ce serveur partage entre utilisateurs. Le temps d’écoute alimente le classement ; les deux autres révèlent ce que chacun lit, elles restent donc désactivées tant que vous ne les activez pas. S’applique à tout le monde à la fois.', socTime: 'Temps d’écoute (classement du serveur)', socContent: 'Meilleurs titres dans les stats partagées', socWho: 'Qui lit (pages des livres)', socUsers: 'Par utilisateur', socUsersHint: 'Désactivez quelqu’un et rien de ses données n’est partagé, quel que soit son propre réglage ; vous seul pouvez le rétablir. "Retrait volontaire" signifie que la personne a dit non elle-même.', socOpted: 'retrait volontaire', srvTitle: 'Valeurs par défaut du serveur', srvHint: 'Enregistrez vos réglages actuels comme défauts pour tous les utilisateurs de ce serveur. Chacun peut ensuite personnaliser par-dessus. Montez un volume sur /data/nh dans le conteneur du thème pour les conserver entre les mises à jour.', srvSave: 'Enregistrer comme défauts du serveur', srvClear: 'Effacer les défauts du serveur', srvSaved: 'Enregistré', srvCleared: 'Effacé', srvErr: 'Échec : connexion admin requise', crMode: 'Étagère Continuer la lecture', crCombine: 'Fusionner dans le carrousel', crSeparate: 'Garder en étagère séparée', crHide: 'Masquée', fsHint: 'Partagez votre écoute avec les autres personnes de ce serveur : totaux et minutes par jour pour le classement, et, si l’admin les a activés, vos meilleurs titres et les livres que vous lisez ou avez terminés. Activé par défaut. En désactivant, tout ce qui a été partagé est supprimé.' , amfHide: 'Masquer Marquer comme terminé' , amfConfirm: 'Marquer ce livre comme terminé ?' , fsHint: 'Partagez un résumé de votre écoute (totaux et minutes par jour) avec les autres personnes de ce serveur, il alimente le classement. Activé par défaut. En le désactivant, vous quittez le classement et ses totaux, et tout ce qui a été partagé est supprimé.' , srToggle: 'Partager quels livres vous lisez', srHint: 'Ajoute à ce que voient les autres quels livres vous lisez, avez terminés et écoutez le plus. Désactivé par défaut ; les interrupteurs serveur de l’admin doivent aussi être activés.' , fsHint: 'Partagez votre temps d’écoute (totaux et minutes par jour) avec les autres personnes de ce serveur ; il alimente le classement. Activé par défaut. En le désactivant, les autres ne vous voient plus ; les admins du serveur voient toujours l’activité, marquée comme retirée.', srHint: 'Permet aussi aux autres de voir quels livres vous lisez, avez terminés et écoutez le plus. Rien ne s’affiche tant que l’admin n’a pas activé les interrupteurs serveur correspondants.', seshTitle: 'Sessions récentes', seshNow: 'En écoute' , hcHint: 'Envoyez vos notes vers votre compte Hardcover. Collez la clé API depuis les réglages de votre compte Hardcover ; les clés expirent après un an.', hcKeyLbl: 'Clé API', hcToggle: 'Synchroniser automatiquement les nouvelles notes', hcPushAll: 'Envoyer toutes les notes', hcPushDiff: 'Envoyer seulement les différences', hcDone: 'Terminé : %d synchronisées, %d sans correspondance', hcNoKey: 'Collez d’abord votre clé API Hardcover' , hcMissHead: 'Introuvable sur Hardcover :' , hcHint: 'Envoyez vos notes vers votre compte Hardcover. Collez la clé API depuis les réglages de votre compte Hardcover.' , tabStyle: 'Style', tabMain: 'Page d’accueil', tabBooks: 'Livres', tabFeat: 'Fonctions' , hcImport: 'Importer depuis Hardcover', impMarkQ: 'Marquer aussi les %d livres associés comme terminés ici ?', impMarkDone: '%d marqués comme terminés.' , impHint: 'Récupérez vos notes depuis Goodreads, StoryGraph ou Hardcover. Choisissez ici le fichier CSV exporté ; Hardcover s’importe directement via le bouton de sa carte. Chaque correspondance est affichée pour confirmation avant tout enregistrement.' , impErrUnknown: 'Cela ne ressemble pas à un export Goodreads, StoryGraph ou Hardcover : colonnes de titre et de note introuvables.' , fsCard: 'Social', rnToggle: 'Afficher mon nom avec mes notes', rsToggle: 'Partager mes notes avec les autres', socNames: 'Noms avec les notes', rtAnon: 'Anonyme' },
    es: { cmStopped: "detenido", cmScanErrs: "{e} sin comprobar (errores de Goodreads)", cmScanBusy: "Ya hay un escaneo en marcha", cmGrErr: "Goodreads no responde, se reintentará", cmNoMatchYet: "Sin coincidencia en Goodreads", cmLooking: "Buscando en Goodreads…", cmToolHint: "Lo que opinan los lectores de Goodreads de este libro. El ? muestra el libro asociado y permite cambiarlo.", cmSearchPh: "Buscar en Goodreads…", cmNoVotes: "sin valoraciones aún", cmRefresh: "Actualizar", cmScanAll: "Volver a revisar todo, no solo los que faltan", cmScanDone: "Listo: {m} encontrados · {x} sin resultado · {s} ya tenían", cmStop: "Detener", cmScanLibs: "Bibliotecas a recorrer", cmScan: "Buscar valoraciones", lsLongest: "Libros más largos", lsSize: "Tamaño en disco", lsDuration: "Duración total", lsGenresN: "Géneros", lsSeriesN: "Series", lsAuthorsN: "Autores", lsBooks: "Libros", lsGlance: "La biblioteca de un vistazo", cmOpen: "Abrir en Goodreads", cmDetect: "Buscar", cmAddr: "Dirección del ayudante", scmTitle: "Página de serie", scmHint: "Aspecto de la portada generada sobre el título de una serie cuando no se ha subido una propia (solo este navegador).", scmDeck: "Baraja apilada", cmHint: "Lo que opinan los lectores de Goodreads de un libro: nota media y número de votos, bajo las estrellas de la página del libro y como orden y filtro. Las páginas de libros se completan solas al navegar.", cmGrOff: "Goodreads: sin configurar", cmSetup: "¿Aún sin ayudante? Ejecuta esto junto al tema (líneas compose abajo) y pulsa Buscar. Cualquier otra forma de ejecutar la imagen también vale: pega su dirección arriba.", cmGrOn: "Goodreads: conectado", cmGrDown: "Goodreads: configurado pero no responde", cmGrNoNum: "Goodreads: responde, pero esta versión de abs-tract no da cifras de valoración (usa la versión con valoraciones, ver README)", cmTitle: "Valoraciones de Goodreads", cmWrong: "¿Libro equivocado?", cmFind: "Buscar valoración de la comunidad…", cmNone: "Sin coincidencia", cmUse: "Usar esta", lfCommunity: "Valoración de Goodreads", mfNatChange: "Cambiar…", lsByTime: "por tiempo de escucha", sbTitle: 'Clasificación del servidor', rpBadge: 'Informes de problemas abiertos', wkTitle: 'Minutos de escucha, últimos 7 días', wkAvg: 'Media diaria', wkRow: 'Días seguidos', lsTitle: 'Estadísticas del servidor', lsAll: 'Todas las bibliotecas', lsPlayed: 'Libros reproducidos', lsBest: 'Mejor valorados', lsGenres: 'Géneros principales', srvPick: 'Qué incluir:', srvGRest: 'Funciones y comportamiento', aeBtn: 'Ordenar autores', aeTitle: 'Autores sin libros', aeNone: 'Todos los autores aquí tienen al menos un libro.', aeRemove: 'Eliminar', aeDone: 'Eliminados', aeWorking: 'Eliminando…', rpMenu: 'Informar de un problema', rpTitle: 'Informar de un problema', rpWhat: '¿Qué ocurre?', rpNote: '¿Algo más que deba saber el administrador? (opcional)', rpSend: 'Enviar informe', rpSent: 'Enviado. Gracias.', rpFail: 'No se pudo enviar', rpMissing: 'Contenido ausente o incompleto', rpQuality: 'Mala calidad de audio', rpPlay: 'No se reproduce', rpWrong: 'Libro, portada o metadatos incorrectos', rpChapters: 'Capítulos incorrectos', rpOther: 'Otra cosa', rpCard: 'Problemas informados', rpHint: 'Problemas que los usuarios informaron desde una página de libro. Resolver lo quita de la lista.', rpEmpty: 'Nada informado.', rpResolve: 'Resolver', rpOpen: 'Abrir libro', apCard: 'Reproducción', apToggle: 'Reproducir automáticamente el siguiente libro de una serie', apHint: 'Cuando termina un libro, empieza automáticamente el siguiente de la misma serie. Solo libros que ya tienes y que no has terminado.', apNext: 'Siguiente de la serie', fdToggle: 'Herramientas de libros terminados en tus estadísticas', ysFinished: 'Terminados hace poco', ysAlmost: 'Casi terminado', ysMarkDone: 'Marcar como terminado', ysEditDate: 'Cambiar la fecha de fin', ysNoDate: 'sin fecha', hmShowing: 'el gráfico muestra', ysTitle: 'Tu escucha', ysStreak: 'Racha', ysCurrent: 'Actual', ysLongest: 'Más larga', ysDaysTotal: 'Días en total', ysPace: 'Ritmo', ysThisWeek: 'Esta semana', ysPerDay: 'Por día de escucha', ysBestDay: 'Mejor día', ysWhen: 'Cuándo escuchas', ysTopBooks: 'Libros más escuchados', ysTopAuthors: 'Autores más escuchados', ysTopNarrators: 'Narradores más escuchados', rfRate: 'Valorar', rfSheet: 'Valora este libro', rfOpen: 'Abrir la página del libro', rfPickHint: 'Elige una valoración', rfTitle: 'Valora lo que terminaste', rfToggle: 'Sección «Valorar» en el inicio', hoTitle: 'Orden de secciones del inicio', hoEmpty: 'Abre la página de inicio una vez y vuelve aquí.', hoReset: 'Restablecer el orden', nrSearch: 'Filtrar narradores…', nrSortName: 'Nombre', nrSortBooks: 'Más libros', nrForms: ['Narrador', 'Narradores'], nrBooksForms: ['libro', 'libros'], auForms: ['Autor', 'Autores'], auSearch: 'Filtrar autores…' , lsTitle: "Estadísticas del servidor", lsAll: "Todas las bibliotecas", lsPlayed: "Libros reproducidos", lsBest: "Mejor valorados", lsGenres: "Géneros principales", rpCard: "Problemas informados", rpHint: "Problemas que los usuarios informaron desde una página de libro. Resolver lo quita de la lista.", rpEmpty: "Nada informado.", rpResolve: "Resolver", rpOpen: "Abrir libro", ctIcon: "Icono", ctColour: "Acento", ctNeedBook: "Audiobookshelf no puede guardar una colección vacía: elige al menos un libro. Después podrás añadir o quitar los que quieras.", colEditIcon: "Cambiar icono", ciTitle: "Icono de la colección", ciReset: "Ajustar automáticamente al nombre", colEditDesc: "Editar descripción", colBookForms: ["libro","libros"], colForms: ["Colección","Colecciones"], ctTitle: "Crear una colección", ctBlank: "Colección vacía", ctNew: "Nueva colección", ctNewHint: "desde una plantilla", ctNamePh: "Nombre de la colección", ctSearch: "Buscar libros o autores…", ctCreate: "Crear", ctBack: "‹ Atrás", ctAddAll: "Añadir todos los libros", abTitle: "Añadir libros", abAdd: "Añadir", sbWeek: "Semana", sbMonth: "Mes", sbYear: "Año", sbAll: "Todo el tiempo", sbBooks: "Libros", sbTopBooks: "Más escuchados", sbTotal: "Total", sbListeners: "Oyentes", sbAvg: "Media", avRemove: "Quitar foto", avSet: "Elegir foto…", ugListened: "Escuchado", ugPhoto: "Foto de perfil", ugNever: "Nunca", ugSeen: "Última actividad", ugEdit: "Editar usuario", ugDelete: "Eliminar usuario", ugAdd: "Añadir foto", ugChange: "Cambiar foto", ugSortName: "A–Z", ugSortTime: "Más escuchados", ugSortSeen: "Activos recientemente", fsTitle: "Clasificación del servidor", fsCard: "Clasificación del servidor", fsToggle: "Compartir mis estadísticas de escucha", fsJoin: "Activa «Compartir mis estadísticas de escucha» en el panel de personalización para aparecer en la clasificación del servidor.", fsWaiting: "Compartir está activado: tu resumen se unirá en breve a la clasificación del servidor, junto con los demás.", fsHint: "Comparte un resumen de tu escucha (totales y minutos por día) con las demás personas de este servidor: alimenta la clasificación. Activado por defecto. Al desactivarlo desapareces de la clasificación y de sus totales, y todo lo compartido se elimina.", yirBtn: "Resumen del año", yirTitle: "Tu año de escucha", yirYear: "Últimos 12 meses", yirDays: "Días con escucha", yirBest: "Mejor día", yirMonths: "Por mes", yirNone: "Aún no hay datos de escucha.", bsCard: "Enlaces del libro", bsHint: "Botones en la página del libro para buscar un título en otros sitios. Goodreads y el mayor sitio de tu idioma vienen activados.", bsFind: "Buscar en", bsGlobal: "En todas partes", bsLocal: "Para tu idioma", bsOther: "Otros países", pgCard: "Páginas rediseñadas", pgHint: "Cada opción sustituye una página de Audiobookshelf por la versión NanoHive. Desactívala para volver a la original.", pgNarrators: "Tarjetas de narradores", pgCollections: "Páginas de colecciones", pgUsers: "Tarjetas de usuarios (ajustes)", pgStats: "Clasificación de escucha", pgPhoto: "Foto de perfil en la barra superior", pgCinematic: "Fondo cinematográfico", pgTransitions: "Transiciones de página", pSearch: "Buscar ajustes…", pSearchNone: "Nada coincide.", srvPick: "Qué incluir:", srvGRest: "Funciones y comportamiento", apCard: "Reproducción", fdToggle: "Herramientas de libros terminados en tus estadísticas", ysNoDate: "sin fecha", hmShowing: "el gráfico muestra", ysStreak: "Racha", ysCurrent: "Actual", ysLongest: "Más larga", ysDaysTotal: "Días en total", ysPace: "Ritmo", ysPerDay: "Por día de escucha", ysWhen: "Cuándo escuchas", ysTopBooks: "Libros más escuchados", ysTopAuthors: "Autores más escuchados", ysTopNarrators: "Narradores más escuchados", rfToggle: "Sección «Valorar» en el inicio", hoTitle: "Orden de secciones del inicio", hoEmpty: "Abre la página de inicio una vez y vuelve aquí.", hoReset: "Restablecer el orden", ssscmDeck: "Baraja apilada", scmFirst: "Portada del primer libro", scmGrid: "Cuadrícula de portadas (por defecto)", scmOff: "Oculta" , erTypeface: "Tipografía", erPage: "Tema de página", erText: "Color del texto", erBg: "Fondo", erDefault: "Predeterminado (ABS)", erAuto: "Auto" , lfByAuthor: "Autor › orden de serie", lfBySeries: "Serie › orden" , lfSecFilters: "Más filtros", lfSecSort: "Orden múltiple", lfAuthor: "Autor", lfSeries: "Serie", lfTitle: "Título", lfYear: "Año", lfAdded: "Añadido", lfDuration: "Duración", lfNarrator: "Narrador", lfGenre: "Género", lfLanguage: "Idioma", lfProgress: "Progreso", lfPgFinished: "Terminados", lfPgInProgress: "En curso", lfPgNot: "Sin empezar" , logoUpload: 'Subir desde el dispositivo…', logoUploaded: 'Subido ✓', logoBadType: 'Tipo de imagen no compatible', logoTooBig: 'Imagen demasiado grande (máx. 4 MB)', logoOr: 'o', bookPage: 'Página del libro', ratingsToggle: 'Valoraciones de la comunidad (estrellas y reseñas)', ratingsHint: 'Valoraciones con reseñas cortas en cada página de libro, compartidas con todos los usuarios de este servidor.', searchCard: 'Búsqueda', gsToggle: 'Buscar en todas las bibliotecas a la vez', gsHint: 'Una lista de resultados combinada de todas tus bibliotecas accesibles, con una insignia de biblioteca en cada resultado. Desactívalo para volver al menú por biblioteca.', gsNoResults: 'Sin resultados', gsSearching: 'Buscando…', gsBooks: 'Libros', gsSeries: 'Series', gsAuthors: 'Autores', spTitle: 'Página de inicio', spHint: 'Abrir la aplicación siempre en la vista elegida de una biblioteca (solo este navegador). Los enlaces directos nunca se redirigen.', spDefault: 'Predeterminada de la app', spViewHome: 'Inicio', spViewLibrary: 'Biblioteca', spViewSeries: 'Series', spViewCollections: 'Colecciones', spViewAuthors: 'Autores', scSet: 'Establecer portada de la serie', scRemove: 'Quitar portada de la serie', scEditDesc: 'Editar descripción de la serie', cardStars: 'Estrellas en las portadas', hoverPolish: 'Acabado al pasar el cursor (menos oscurecido, botón de acento, estrellas al pasar)', sectUser: 'Tus ajustes', sectAdmin: 'Administración', lockTitle: 'Interruptores de funciones', lockHint: 'Desactiva a la fuerza una función para todos los usuarios de este servidor. Gana a los ajustes personales; los demás lo verán tras recargar.', lockRatings: 'Desactivar valoraciones por completo', lockCardRatings: 'Desactivar estrellas en portadas', lockGlobalSearch: 'Desactivar búsqueda global', lockHoverPolish: 'Desactivar acabado al pasar el cursor', lockHeroCarousel: 'Desactivar carrusel de inicio', lockedNote: 'desactivado por el administrador', lfSort: 'Valoración', lfRated: 'Valorados', lfUnrated: 'Sin valorar', lfClear: 'Limpiar', lfNone: 'Ningún libro coincide', lfSection: 'Valoración', lfWho: 'Valoraciones de quién', lfEveryone: 'Todos', sdSave: 'Guardar', sdClear: 'Restaurar la descripción del libro', sdMore: 'Leer más', sdLess: 'Mostrar menos', shSeries: 'Serie', shBy: 'de ', shMore: ' y más', shBookForms: ['libro', 'libros'], shAvg: 'media de libros', shRated: 'valorados', impLabel: 'Importar valoraciones', impHint: 'Trae tus valoraciones de StoryGraph o Goodreads. Exporta allí tus libros como CSV y elige el archivo aquí. Cada coincidencia se muestra para que la confirmes y nada se guarda hasta que lo hagas.', impBtn: 'Elegir archivo CSV…', impReading: 'Leyendo el archivo…', impMatching: 'Comparando con tus bibliotecas…', impErrUnknown: 'Esto no parece un export de StoryGraph ni de Goodreads. Debe ser su CSV, con una columna "Star Rating" o "My Rating".', impErrEmpty: 'Ese archivo no tiene filas.', impErrNoRated: 'No hay libros valorados en ese archivo.', impErrRead: 'No se pudo leer el archivo.', impErrLoad: 'No se pudieron cargar tus bibliotecas. Inténtalo de nuevo en un momento.', impErrNoLibs: 'Ninguna biblioteca participa en las valoraciones, así que no hay dónde importar.', impTitle: 'Importar valoraciones', impFrom: 'Export de %s: %d libros, %d valorados', impLibs: 'Buscando en: %s', impMatched: 'Coincidencias', impCheck: 'Merece un vistazo', impCheckHint: 'Cerca pero sin certeza. Revisa la coincidencia o elige otra.', impAlready: 'Ya valorados por ti', impAlreadyHint: 'Se dejan como están, salvo que pidas sobrescribirlos.', impNone: 'No están en tu biblioteca', impNoneHint: 'Nada en este servidor coincide con estos.', impAll: 'Todos', impSkip: 'Omitir este', impDup: 'duplicado', impByTitle: 'título', impYours: 'la tuya: %s', impReviews: 'Importar también reseñas escritas (%d)', impOver: 'Sobrescribir valoraciones que ya tengo', impGo: 'Importar %d valoraciones', impCancel: 'Cancelar', impCloseBtn: 'Cerrar', impWorking: 'Guardando… %d de %d', impDone: '%d valoraciones importadas.', impFail: '%d no se pudieron guardar.', rdTitle: 'Quién lee este libro', rdFin: 'Terminado', socTitle: 'Funciones sociales', socHint: 'Lo que este servidor comparte entre usuarios. El tiempo de escucha alimenta el ranking; las otras dos revelan qué lee cada uno, así que quedan apagadas hasta que las actives. Se aplica a todos a la vez.', socTime: 'Tiempo de escucha (ranking del servidor)', socContent: 'Títulos destacados en estadísticas compartidas', socWho: 'Quién está leyendo (páginas de libros)', socUsers: 'Por usuario', socUsersHint: 'Apaga a alguien y nada suyo se comparte, elija lo que elija esa persona; solo tú puedes revertirlo. "Se dio de baja" significa que la persona dijo que no por sí misma.', socOpted: 'se dio de baja', srvTitle: 'Valores por defecto del servidor', srvHint: 'Guarda tus ajustes actuales como predeterminados para todos los usuarios de este servidor. Cada uno puede personalizar encima. Monta un volumen en /data/nh en el contenedor del tema para conservarlos entre actualizaciones.', srvSave: 'Guardar como predeterminados', srvClear: 'Borrar predeterminados', srvSaved: 'Guardado', srvCleared: 'Borrado', srvErr: 'Error: se requiere sesión de administrador', crMode: 'Estante Continuar leyendo', crCombine: 'Combinar con el carrusel', crSeparate: 'Mantener como estante aparte', crHide: 'Oculto', fsHint: 'Comparte tu escucha con las demás personas de este servidor: totales y minutos por día para el ranking y, si el administrador lo ha activado, también tus títulos destacados y qué libros estás leyendo o has terminado. Activado por defecto. Al desactivarlo se borra todo lo compartido.' , amfHide: 'Ocultar Marcar como terminado' , amfConfirm: '¿Marcar este libro como terminado?' , fsHint: 'Comparte un resumen de tu escucha (totales y minutos por día) con las demás personas de este servidor: alimenta la clasificación. Activado por defecto. Al desactivarlo desapareces de la clasificación y de sus totales, y todo lo compartido se elimina.' , srToggle: 'Compartir qué libros lees', srHint: 'Añade a lo que ven los demás qué libros estás leyendo, cuáles has terminado y cuáles escuchas más. Desactivado por defecto; además deben estar activados los interruptores del servidor del administrador.' , fsHint: 'Comparte tu tiempo de escucha (totales y minutos por día) con las demás personas de este servidor; alimenta el ranking. Activado por defecto. Al desactivarlo, los demás dejan de verte; los administradores siguen viendo la actividad, marcada como baja.', srHint: 'También permite a los demás ver qué libros lees, cuáles has terminado y cuáles escuchas más. No se muestra nada hasta que el administrador active los interruptores del servidor correspondientes.', seshTitle: 'Sesiones recientes', seshNow: 'Escuchando ahora' , hcHint: 'Envía tus valoraciones a tu cuenta de Hardcover. Pega la clave API desde los ajustes de tu cuenta de Hardcover; las claves caducan al año.', hcKeyLbl: 'Clave API', hcToggle: 'Sincronizar nuevas valoraciones automáticamente', hcPushAll: 'Enviar todas las valoraciones', hcPushDiff: 'Enviar solo las diferencias', hcDone: 'Listo: %d sincronizadas, %d sin coincidencia', hcNoKey: 'Pega primero tu clave API de Hardcover' , hcMissHead: 'No encontrado en Hardcover:' , hcHint: 'Envía tus valoraciones a tu cuenta de Hardcover. Pega la clave API desde los ajustes de tu cuenta de Hardcover.' , tabStyle: 'Estilo', tabMain: 'Página principal', tabBooks: 'Libros', tabFeat: 'Funciones' , hcImport: 'Importar desde Hardcover', impMarkQ: '¿Marcar también los %d libros emparejados como terminados aquí?', impMarkDone: '%d marcados como terminados.' , impHint: 'Trae tus valoraciones desde Goodreads, StoryGraph o Hardcover. Elige aquí el CSV exportado; Hardcover se importa directamente con el botón de su tarjeta. Cada coincidencia se muestra para confirmar antes de guardar nada.' , impErrUnknown: 'Esto no parece un export de Goodreads, StoryGraph ni Hardcover: no se encontraron columnas de título y valoración.' , fsCard: 'Social', rnToggle: 'Mostrar mi nombre con las valoraciones', rsToggle: 'Compartir mis valoraciones con los demás', socNames: 'Nombres con las valoraciones', rtAnon: 'Anónimo' },
    it: { cmStopped: "interrotto", cmScanErrs: "{e} non controllati (errori Goodreads)", cmScanBusy: "Una scansione è già in corso", cmGrErr: "Goodreads non raggiungibile, riproverò", cmNoMatchYet: "Nessuna corrispondenza su Goodreads", cmLooking: "Ricerca su Goodreads…", cmToolHint: "Cosa pensano di questo libro i lettori di Goodreads. Il ? mostra il libro abbinato e permette di cambiarlo.", cmSearchPh: "Cerca su Goodreads…", cmNoVotes: "nessuna valutazione", cmRefresh: "Aggiorna", cmScanAll: "Ricontrolla tutto, non solo i mancanti", cmScanDone: "Fatto: {m} trovati · {x} non trovati · {s} già presenti", cmStop: "Ferma", cmScanLibs: "Librerie da analizzare", cmScan: "Cerca valutazioni", lsLongest: "Libri più lunghi", lsSize: "Spazio su disco", lsDuration: "Durata totale", lsGenresN: "Generi", lsSeriesN: "Serie", lsAuthorsN: "Autori", lsBooks: "Libri", lsGlance: "La libreria a colpo d’occhio", cmOpen: "Apri su Goodreads", cmDetect: "Cerca", cmAddr: "Indirizzo dell’aiutante", scmTitle: "Pagina della serie", scmHint: "Aspetto della copertina generata sopra il titolo di una serie quando non ne è stata caricata una (solo questo browser).", scmDeck: "Mazzo sovrapposto", cmHint: "Cosa pensano di un libro i lettori di Goodreads: voto medio e numero di voti, sotto le stelle nella pagina del libro e come ordinamento e filtro. Le pagine dei libri si completano da sole navigando.", cmGrOff: "Goodreads: non configurato", cmSetup: "Nessun aiutante ancora? Avvia questo accanto al tema (righe compose sotto), poi premi Cerca. Va bene anche qualsiasi altro modo di avviare l’immagine: incolla il suo indirizzo sopra.", cmGrOn: "Goodreads: connesso", cmGrDown: "Goodreads: configurato ma non risponde", cmGrNoNum: "Goodreads: risponde, ma questa versione di abs-tract non fornisce i voti (usa la versione con valutazioni, vedi README)", cmTitle: "Valutazioni Goodreads", cmWrong: "Libro sbagliato?", cmFind: "Cerca una valutazione della community…", cmNone: "Nessuna corrispondenza", cmUse: "Usa questo", lfCommunity: "Valutazione Goodreads", mfNatChange: "Cambia…", lsByTime: "per tempo di ascolto", nrSearch: 'Filtra narratori…', nrSortName: 'Nome', nrSortBooks: 'Più libri', nrForms: ['Narratore', 'Narratori'], nrBooksForms: ['libro', 'libri'], auForms: ['Autore', 'Autori'], auSearch: 'Filtra autori…' , rpBadge: "Segnalazioni aperte", wkTitle: "Minuti di ascolto, ultimi 7 giorni", wkAvg: "Media giornaliera", wkRow: "Giorni di fila", lsTitle: "Statistiche del server", lsAll: "Tutte le librerie", lsPlayed: "Libri riprodotti", lsBest: "I più votati", lsGenres: "Generi principali", srvPick: "Cosa includere:", srvGRest: "Funzioni e comportamento", aeBtn: "Riordina autori", aeTitle: "Autori senza libri", aeNone: "Ogni autore qui ha almeno un libro.", aeRemove: "Rimuovi", aeDone: "Rimossi", aeWorking: "Rimozione…", rpMenu: "Segnala un problema", rpTitle: "Segnala un problema", rpWhat: "Cosa non va?", rpNote: "Altro che l’amministratore dovrebbe sapere? (facoltativo)", rpSend: "Invia segnalazione", rpSent: "Inviata. Grazie.", rpFail: "Invio non riuscito", rpMissing: "Contenuto mancante o incompleto", rpQuality: "Audio di scarsa qualità", rpPlay: "Non si riproduce", rpWrong: "Libro, copertina o metadati sbagliati", rpChapters: "Capitoli sbagliati", rpOther: "Altro", rpCard: "Problemi segnalati", rpHint: "Problemi segnalati dagli utenti da una pagina libro. Risolverne uno lo toglie dall’elenco.", rpEmpty: "Nessuna segnalazione.", rpResolve: "Risolvi", rpOpen: "Apri il libro", apCard: "Riproduzione", apToggle: "Riproduci automaticamente il libro successivo di una serie", apHint: "Quando un libro finisce, parte automaticamente il successivo della stessa serie. Solo libri già in libreria e non ancora finiti.", apNext: "Prossimo della serie", fdToggle: "Strumenti per i libri finiti nelle tue statistiche", ysFinished: "Finiti di recente", ysAlmost: "Quasi finiti", ysMarkDone: "Segna come finito", ysEditDate: "Cambia la data di fine", ysNoDate: "senza data", hmShowing: "il grafico mostra", ysTitle: "Il tuo ascolto", ysStreak: "Serie di giorni", ysCurrent: "Attuale", ysLongest: "Più lunga", ysDaysTotal: "Giorni in totale", ysPace: "Ritmo", ysThisWeek: "Questa settimana", ysPerDay: "Per giorno di ascolto", ysBestDay: "Giorno migliore", ysWhen: "Quando ascolti", ysTopBooks: "Libri più ascoltati", ysTopAuthors: "Autori più ascoltati", ysTopNarrators: "Narratori più ascoltati", rfRate: "Vota", rfSheet: "Vota questo libro", rfOpen: "Apri la pagina del libro", rfPickHint: "Scegli un voto", rfTitle: "Vota ciò che hai finito", rfToggle: "Sezione «Vota» nella home", hoTitle: "Ordine delle sezioni della home", hoEmpty: "Apri una volta la home e poi torna qui.", hoReset: "Ripristina l’ordine predefinito", ctIcon: "Icona", ctColour: "Accento", ctNeedBook: "Audiobookshelf non può salvare una raccolta vuota: scegli almeno un libro. Potrai aggiungerne o toglierne in seguito.", colEditIcon: "Cambia icona", ciTitle: "Icona della raccolta", ciReset: "Abbina automaticamente al nome", colEditDesc: "Modifica descrizione", colBookForms: ["libro","libri"], colForms: ["Raccolta","Raccolte"], ctTitle: "Crea una raccolta", ctBlank: "Raccolta vuota", ctNew: "Nuova raccolta", ctNewHint: "da un modello", ctNamePh: "Nome della raccolta", ctSearch: "Cerca libri o autori…", ctCreate: "Crea", ctBack: "‹ Indietro", ctAddAll: "Aggiungi tutti i libri", abTitle: "Aggiungi libri", abAdd: "Aggiungi", sbTitle: "Classifica del server", sbWeek: "Settimana", sbMonth: "Mese", sbYear: "Anno", sbAll: "Da sempre", sbBooks: "Libri", sbTopBooks: "I più ascoltati", sbTotal: "Totale", sbListeners: "Ascoltatori", sbAvg: "Media", avRemove: "Rimuovi foto", avSet: "Scegli foto…", ugListened: "Ascoltato", ugPhoto: "Foto profilo", ugNever: "Mai", ugSeen: "Ultima attività", ugEdit: "Modifica utente", ugDelete: "Elimina utente", ugAdd: "Aggiungi foto", ugChange: "Cambia foto", ugSortName: "A–Z", ugSortTime: "Più ascoltati", ugSortSeen: "Attivi di recente", fsTitle: "Classifica del server", fsCard: "Classifica del server", fsToggle: "Condividi le mie statistiche di ascolto", fsJoin: "Attiva «Condividi le mie statistiche di ascolto» nel pannello di personalizzazione per comparire nella classifica del server.", fsWaiting: "Condivisione attiva, il tuo riepilogo entrerà a breve nella classifica del server, insieme agli altri.", fsHint: "Condividi un riepilogo del tuo ascolto (totali e minuti al giorno) con le altre persone di questo server: alimenta la classifica. Attivo per impostazione predefinita. Disattivandolo esci dalla classifica e dai suoi totali, e tutto ciò che hai condiviso viene eliminato.", yirBtn: "Anno in rassegna", yirTitle: "Il tuo anno di ascolto", yirYear: "Ultimi 12 mesi", yirDays: "Giorni con ascolto", yirBest: "Giorno migliore", yirMonths: "Per mese", yirNone: "Ancora nessun dato di ascolto.", bsCard: "Collegamenti del libro", bsHint: "Pulsanti nella pagina del libro per cercare un titolo altrove. Goodreads e il sito più grande della tua lingua sono attivi in partenza.", bsFind: "Cerca su", bsGlobal: "Ovunque", bsLocal: "Per la tua lingua", bsOther: "Altri paesi", pgCard: "Pagine ridisegnate", pgHint: "Ogni opzione sostituisce una pagina di Audiobookshelf con la versione NanoHive. Disattivala per tornare all’originale.", pgNarrators: "Schede dei narratori", pgCollections: "Pagine delle raccolte", pgUsers: "Schede utenti (impostazioni)", pgStats: "Classifica di ascolto", pgPhoto: "Foto profilo nella barra in alto", pgCinematic: "Sfondo cinematografico", pgTransitions: "Transizioni di pagina", pSearch: "Cerca impostazioni…", pSearchNone: "Nessuna corrispondenza.", ssscmDeck: "Mazzo sovrapposto", scmFirst: "Copertina del primo libro", scmGrid: "Griglia di copertine (predefinita)", scmOff: "Nascosta" , erTypeface: "Carattere", erPage: "Tema pagina", erText: "Colore del testo", erBg: "Sfondo", erDefault: "Predefinito (ABS)", erAuto: "Auto" , lfByAuthor: "Autore › ordine della serie", lfBySeries: "Serie › ordine" , lfSecFilters: "Altri filtri", lfSecSort: "Ordinamento multiplo", lfAuthor: "Autore", lfSeries: "Serie", lfTitle: "Titolo", lfYear: "Anno", lfAdded: "Aggiunto", lfDuration: "Durata", lfNarrator: "Narratore", lfGenre: "Genere", lfLanguage: "Lingua", lfProgress: "Progresso", lfPgFinished: "Finiti", lfPgInProgress: "In corso", lfPgNot: "Non iniziati" , logoUpload: 'Carica dal dispositivo…', logoUploaded: 'Caricato ✓', logoBadType: 'Tipo di immagine non supportato', logoTooBig: 'Immagine troppo grande (max 4 MB)', logoOr: 'oppure', bookPage: 'Pagina del libro', ratingsToggle: 'Valutazioni della community (stelle e recensioni)', ratingsHint: 'Valutazioni a stelle con brevi recensioni su ogni pagina libro, condivise con tutti gli utenti di questo server.', searchCard: 'Ricerca', gsToggle: 'Cerca in tutte le librerie insieme', gsHint: 'Un unico elenco di risultati per tutte le librerie accessibili, con il badge della libreria su ogni risultato. Disattiva per tornare al menu per libreria.', gsNoResults: 'Nessun risultato', gsSearching: 'Ricerca…', gsBooks: 'Libri', gsSeries: 'Serie', gsAuthors: 'Autori', spTitle: 'Pagina iniziale', spHint: 'Apri l’app sempre sulla vista scelta di una libreria (solo questo browser). I link diretti non vengono mai reindirizzati.', spDefault: 'Predefinita dell’app', spViewHome: 'Home', spViewLibrary: 'Libreria', spViewSeries: 'Serie', spViewCollections: 'Raccolte', spViewAuthors: 'Autori', scSet: 'Imposta copertina della serie', scRemove: 'Rimuovi copertina della serie', scEditDesc: 'Modifica descrizione della serie', cardStars: 'Stelle sulle copertine', hoverPolish: 'Rifinitura al passaggio del mouse (oscuramento più morbido, play in accento, stelle al passaggio)', sectUser: 'Le tue impostazioni', sectAdmin: 'Amministrazione', lockTitle: 'Interruttori delle funzioni', lockHint: 'Disattiva forzatamente una funzione per tutti gli utenti di questo server. Vince sulle impostazioni personali; gli altri la vedono al prossimo ricaricamento.', lockRatings: 'Disattiva le valutazioni ovunque', lockCardRatings: 'Disattiva le stelle sulle copertine', lockGlobalSearch: 'Disattiva la ricerca globale', lockHoverPolish: 'Disattiva la rifinitura al passaggio', lockHeroCarousel: 'Disattiva il carosello della home', lockedNote: 'disattivato dall’amministratore', lfSort: 'Valutazione', lfRated: 'Valutati', lfUnrated: 'Non valutati', lfClear: 'Pulisci', lfNone: 'Nessun libro corrisponde', lfSection: 'Valutazione', lfWho: 'Valutazioni di chi', lfEveryone: 'Tutti', sdSave: 'Salva', sdClear: 'Ripristina la descrizione del libro', sdMore: 'Leggi tutto', sdLess: 'Mostra meno', shSeries: 'Serie', shBy: 'di ', shMore: ' e altri', shBookForms: ['libro', 'libri'], shAvg: 'media libri', shRated: 'valutati', impLabel: 'Importa valutazioni', impHint: 'Porta le tue valutazioni da StoryGraph o Goodreads. Esporta lì i tuoi libri in CSV e scegli il file qui. Ogni corrispondenza ti viene mostrata da confermare e nulla viene salvato finché non confermi.', impBtn: 'Scegli file CSV…', impReading: 'Lettura del file…', impMatching: 'Confronto con le tue librerie…', impErrUnknown: 'Non sembra un export di StoryGraph o Goodreads. Serve il loro CSV, con una colonna "Star Rating" o "My Rating".', impErrEmpty: 'Quel file non contiene righe.', impErrNoRated: 'In quel file non ci sono libri valutati.', impErrRead: 'Impossibile leggere il file.', impErrLoad: 'Impossibile caricare le tue librerie. Riprova tra un momento.', impErrNoLibs: 'Nessuna libreria partecipa alle valutazioni, quindi non c’è dove importare.', impTitle: 'Importa valutazioni', impFrom: 'Export %s: %d libri, di cui %d valutati', impLibs: 'Ricerca in: %s', impMatched: 'Abbinati', impCheck: 'Da controllare', impCheckHint: 'Vicino ma non certo. Controlla l’abbinamento o scegline un altro.', impAlready: 'Già valutati da te', impAlreadyHint: 'Restano invariati, a meno che tu non chieda di sovrascriverli.', impNone: 'Non nella tua libreria', impNoneHint: 'Nulla su questo server corrisponde a queste voci.', impAll: 'Tutti', impSkip: 'Salta questo', impDup: 'duplicato', impByTitle: 'titolo', impYours: 'la tua: %s', impReviews: 'Importa anche le recensioni scritte (%d)', impOver: 'Sovrascrivi le valutazioni che ho già', impGo: 'Importa %d valutazioni', impCancel: 'Annulla', impCloseBtn: 'Chiudi', impWorking: 'Salvataggio… %d di %d', impDone: '%d valutazioni importate.', impFail: '%d non salvate.', rdTitle: 'Chi sta leggendo questo libro', rdFin: 'Finito', socTitle: 'Funzioni social', socHint: 'Cosa condivide questo server tra gli utenti. Il tempo di ascolto alimenta la classifica; le altre due rivelano cosa legge ciascuno, quindi restano spente finché non le attivi. Vale per tutti insieme.', socTime: 'Tempo di ascolto (classifica del server)', socContent: 'Titoli più ascoltati nelle statistiche condivise', socWho: 'Chi sta leggendo (pagine dei libri)', socUsers: 'Per utente', socUsersHint: 'Spegni qualcuno e nulla di suo viene condiviso, qualunque cosa abbia scelto; solo tu puoi riattivarlo. "Ha rinunciato" significa che la persona ha detto no da sola.', socOpted: 'ha rinunciato', srvTitle: 'Predefiniti del server', srvHint: 'Salva le impostazioni attuali come predefinite per tutti gli utenti di questo server. Ognuno può comunque personalizzare le proprie. Monta un volume su /data/nh nel container del tema per conservarle tra gli aggiornamenti.', srvSave: 'Salva come predefiniti del server', srvClear: 'Cancella predefiniti del server', srvSaved: 'Salvato', srvCleared: 'Cancellato', srvErr: 'Errore: serve l’accesso amministratore', crMode: 'Scaffale Continua a leggere', crCombine: 'Unisci al carosello', crSeparate: 'Mantieni come scaffale separato', crHide: 'Nascosto', fsHint: 'Condividi il tuo ascolto con le altre persone di questo server: totali e minuti al giorno per la classifica e, se l’amministratore le ha attivate, anche i tuoi titoli più ascoltati e quali libri stai leggendo o hai finito. Attivo per impostazione predefinita. Disattivando, tutto ciò che è stato condiviso viene eliminato.' , amfHide: 'Nascondi Segna come finito' , amfConfirm: 'Segnare questo libro come finito?' , fsHint: 'Condividi un riepilogo del tuo ascolto (totali e minuti al giorno) con le altre persone di questo server: alimenta la classifica. Attivo per impostazione predefinita. Disattivandolo esci dalla classifica e dai suoi totali, e tutto ciò che hai condiviso viene eliminato.' , srToggle: 'Condividi quali libri leggi', srHint: 'Aggiunge a ciò che vedono gli altri quali libri stai leggendo, quali hai finito e quali ascolti di più. Disattivato per impostazione predefinita; servono anche gli interruttori server dell’amministratore.' , fsHint: 'Condividi il tuo tempo di ascolto (totali e minuti al giorno) con le altre persone di questo server; alimenta la classifica. Attivo per impostazione predefinita. Disattivandolo gli altri non ti vedono più; gli amministratori vedono comunque l’attività, contrassegnata come esclusa.', srHint: 'Consente anche agli altri di vedere quali libri stai leggendo, quali hai finito e quali ascolti di più. Non si vede nulla finché l’amministratore non attiva i relativi interruttori del server.', seshTitle: 'Sessioni recenti', seshNow: 'In ascolto ora' , hcHint: 'Invia le tue valutazioni al tuo account Hardcover. Incolla la chiave API dalle impostazioni del tuo account Hardcover; le chiavi scadono dopo un anno.', hcKeyLbl: 'Chiave API', hcToggle: 'Sincronizza automaticamente le nuove valutazioni', hcPushAll: 'Invia tutte le valutazioni', hcPushDiff: 'Invia solo le differenze', hcDone: 'Fatto: %d sincronizzate, %d senza corrispondenza', hcNoKey: 'Incolla prima la tua chiave API di Hardcover' , hcMissHead: 'Non trovato su Hardcover:' , hcHint: 'Invia le tue valutazioni al tuo account Hardcover. Incolla la chiave API dalle impostazioni del tuo account Hardcover.' , tabStyle: 'Stile', tabMain: 'Pagina principale', tabBooks: 'Libri', tabFeat: 'Funzioni' , hcImport: 'Importa da Hardcover', impMarkQ: 'Segnare anche qui come finiti i %d libri abbinati?', impMarkDone: '%d segnati come finiti.' , impHint: 'Porta le tue valutazioni da Goodreads, StoryGraph o Hardcover. Scegli qui il CSV esportato; Hardcover importa direttamente col pulsante sulla sua scheda. Ogni abbinamento viene mostrato per conferma prima di salvare.' , impErrUnknown: 'Non sembra un export di Goodreads, StoryGraph o Hardcover: colonne di titolo e valutazione non trovate.' , fsCard: 'Social', rnToggle: 'Mostra il mio nome con le valutazioni', rsToggle: 'Condividi le mie valutazioni con gli altri', socNames: 'Nomi con le valutazioni', rtAnon: 'Anonimo' },
    pt: { cmStopped: "parado", cmScanErrs: "{e} por verificar (erros do Goodreads)", cmScanBusy: "Já há uma verificação a correr", cmGrErr: "Goodreads indisponível, vai tentar de novo", cmNoMatchYet: "Sem correspondência no Goodreads", cmLooking: "A procurar no Goodreads…", cmToolHint: "O que os leitores do Goodreads acham deste livro. O ? mostra o livro associado e permite alterá-lo.", cmSearchPh: "Procurar no Goodreads…", cmNoVotes: "ainda sem avaliações", cmRefresh: "Atualizar", cmScanAll: "Voltar a verificar tudo, não só os em falta", cmScanDone: "Concluído: {m} encontrados · {x} não encontrados · {s} já tinham", cmStop: "Parar", cmScanLibs: "Bibliotecas a percorrer", cmScan: "Procurar avaliações", lsLongest: "Livros mais longos", lsSize: "Tamanho em disco", lsDuration: "Duração total", lsGenresN: "Géneros", lsSeriesN: "Séries", lsAuthorsN: "Autores", lsBooks: "Livros", lsGlance: "A biblioteca num relance", cmOpen: "Abrir no Goodreads", cmDetect: "Procurar", cmAddr: "Endereço do auxiliar", scmTitle: "Página da série", scmHint: "Aspeto da capa gerada sobre o título de uma série quando não foi carregada nenhuma (apenas este navegador).", scmDeck: "Baralho em camadas", cmHint: "O que os leitores do Goodreads acham de um livro: nota média e número de votos, sob as estrelas na página do livro e como ordenação e filtro. As páginas dos livros preenchem-se sozinhas ao navegar.", cmGrOff: "Goodreads: não configurado", cmSetup: "Ainda sem auxiliar? Corre isto ao lado do tema (linhas compose abaixo) e carrega em Procurar. Qualquer outra forma de correr a imagem também serve: cola o endereço acima.", cmGrOn: "Goodreads: ligado", cmGrDown: "Goodreads: configurado mas não responde", cmGrNoNum: "Goodreads: responde, mas esta versão do abs-tract não traz avaliações (use a versão com avaliações, ver README)", cmTitle: "Avaliações do Goodreads", cmWrong: "Livro errado?", cmFind: "Procurar avaliação da comunidade…", cmNone: "Sem correspondência", cmUse: "Usar este", lfCommunity: "Avaliação do Goodreads", mfNatChange: "Alterar…", lsByTime: "por tempo de escuta", nrSearch: 'Filtrar narradores…', nrSortName: 'Nome', nrSortBooks: 'Mais livros', nrForms: ['Narrador', 'Narradores'], nrBooksForms: ['livro', 'livros'], auForms: ['Autor', 'Autores'], auSearch: 'Filtrar autores…' , rpBadge: "Problemas por resolver", wkTitle: "Minutos de escuta, últimos 7 dias", wkAvg: "Média diária", wkRow: "Dias seguidos", lsTitle: "Estatísticas do servidor", lsAll: "Todas as bibliotecas", lsPlayed: "Livros reproduzidos", lsBest: "Mais bem avaliados", lsGenres: "Géneros principais", srvPick: "O que incluir:", srvGRest: "Funções e comportamento", aeBtn: "Arrumar autores", aeTitle: "Autores sem livros", aeNone: "Cada autor aqui tem pelo menos um livro.", aeRemove: "Remover", aeDone: "Removidos", aeWorking: "A remover…", rpMenu: "Comunicar um problema", rpTitle: "Comunicar um problema", rpWhat: "O que está errado?", rpNote: "Mais alguma coisa que o administrador deva saber? (opcional)", rpSend: "Enviar", rpSent: "Enviado. Obrigado.", rpFail: "Não foi possível enviar", rpMissing: "Conteúdo em falta ou incompleto", rpQuality: "Má qualidade de áudio", rpPlay: "Não reproduz", rpWrong: "Livro, capa ou metadados errados", rpChapters: "Capítulos errados", rpOther: "Outra coisa", rpCard: "Problemas comunicados", rpHint: "Problemas comunicados pelos utilizadores a partir de uma página de livro. Resolver remove-o da lista.", rpEmpty: "Nada comunicado.", rpResolve: "Resolver", rpOpen: "Abrir livro", apCard: "Reprodução", apToggle: "Reproduzir automaticamente o próximo livro de uma série", apHint: "Quando um livro termina, começa automaticamente o seguinte da mesma série. Apenas livros já na biblioteca e ainda não terminados.", apNext: "Próximo da série", fdToggle: "Ferramentas de livros terminados nas estatísticas", ysFinished: "Terminados recentemente", ysAlmost: "Quase terminados", ysMarkDone: "Marcar como terminado", ysEditDate: "Alterar a data de fim", ysNoDate: "sem data", hmShowing: "o gráfico mostra", ysTitle: "A tua escuta", ysStreak: "Sequência", ysCurrent: "Atual", ysLongest: "Mais longa", ysDaysTotal: "Dias no total", ysPace: "Ritmo", ysThisWeek: "Esta semana", ysPerDay: "Por dia de escuta", ysBestDay: "Melhor dia", ysWhen: "Quando escutas", ysTopBooks: "Livros mais escutados", ysTopAuthors: "Autores mais escutados", ysTopNarrators: "Narradores mais escutados", rfRate: "Avaliar", rfSheet: "Avaliar este livro", rfOpen: "Abrir a página do livro", rfPickHint: "Escolhe uma avaliação", rfTitle: "Avalia o que terminaste", rfToggle: "Secção «Avaliar» na página inicial", hoTitle: "Ordem das secções da página inicial", hoEmpty: "Abre a página inicial uma vez e volta aqui.", hoReset: "Repor a ordem", ctIcon: "Ícone", ctColour: "Realce", ctNeedBook: "O Audiobookshelf não consegue guardar uma coleção vazia, escolhe pelo menos um livro. Depois podes adicionar ou remover à vontade.", colEditIcon: "Mudar o ícone", ciTitle: "Ícone da coleção", ciReset: "Ajustar automaticamente ao nome", colEditDesc: "Editar descrição", colBookForms: ["livro","livros"], colForms: ["Coleção","Coleções"], ctTitle: "Criar uma coleção", ctBlank: "Coleção vazia", ctNew: "Nova coleção", ctNewHint: "a partir de um modelo", ctNamePh: "Nome da coleção", ctSearch: "Procurar livros ou autores…", ctCreate: "Criar", ctBack: "‹ Voltar", ctAddAll: "Adicionar todos", abTitle: "Adicionar livros", abAdd: "Adicionar", sbTitle: "Classificação do servidor", sbWeek: "Semana", sbMonth: "Mês", sbYear: "Ano", sbAll: "Desde sempre", sbBooks: "Livros", sbTopBooks: "Mais escutados", sbTotal: "Total", sbListeners: "Ouvintes", sbAvg: "Média", avRemove: "Remover foto", avSet: "Escolher foto…", ugListened: "Escutado", ugPhoto: "Foto de perfil", ugNever: "Nunca", ugSeen: "Última atividade", ugEdit: "Editar utilizador", ugDelete: "Eliminar utilizador", ugAdd: "Adicionar foto", ugChange: "Mudar foto", ugSortName: "A–Z", ugSortTime: "Mais escutados", ugSortSeen: "Ativos recentemente", fsTitle: "Classificação do servidor", fsCard: "Classificação do servidor", fsToggle: "Partilhar as minhas estatísticas de escuta", fsJoin: "Ativa «Partilhar as minhas estatísticas de escuta» no painel de personalização para apareceres na classificação do servidor.", fsWaiting: "Partilha ativada, o teu resumo junta-se em breve à classificação do servidor, com toda a gente.", fsHint: "Partilha um resumo da tua escuta (totais e minutos por dia) com as outras pessoas deste servidor, alimenta a classificação. Ativado por predefinição. Ao desligar sais da classificação e dos totais, e tudo o que partilhaste é apagado.", yirBtn: "Retrospetiva do ano", yirTitle: "O teu ano de escuta", yirYear: "Últimos 12 meses", yirDays: "Dias com escuta", yirBest: "Melhor dia", yirMonths: "Por mês", yirNone: "Ainda sem dados de escuta.", bsCard: "Ligações do livro", bsHint: "Botões na página do livro para procurar um título noutros sítios. O Goodreads e o maior site da tua língua vêm ativados.", bsFind: "Procurar em", bsGlobal: "Em todo o lado", bsLocal: "Para a tua língua", bsOther: "Outros países", pgCard: "Páginas redesenhadas", pgHint: "Cada opção substitui uma página do Audiobookshelf pela versão NanoHive. Desativa para voltar à original.", pgNarrators: "Cartões de narradores", pgCollections: "Páginas de coleções", pgUsers: "Cartões de utilizadores (definições)", pgStats: "Classificação de escuta", pgPhoto: "Foto de perfil na barra superior", pgCinematic: "Fundo cinematográfico", pgTransitions: "Transições de página", pSearch: "Procurar definições…", pSearchNone: "Nada corresponde.", ssscmDeck: "Baralho em camadas", scmFirst: "Capa do primeiro livro", scmGrid: "Grelha de capas (predefinida)", scmOff: "Oculta" , erTypeface: "Tipo de letra", erPage: "Tema da página", erText: "Cor do texto", erBg: "Fundo", erDefault: "Predefinição (ABS)", erAuto: "Auto" , lfByAuthor: "Autor › ordem da série", lfBySeries: "Série › ordem" , lfSecFilters: "Mais filtros", lfSecSort: "Ordenação múltipla", lfAuthor: "Autor", lfSeries: "Série", lfTitle: "Título", lfYear: "Ano", lfAdded: "Adicionado", lfDuration: "Duração", lfNarrator: "Narrador", lfGenre: "Género", lfLanguage: "Idioma", lfProgress: "Progresso", lfPgFinished: "Terminados", lfPgInProgress: "Em curso", lfPgNot: "Por começar" , logoUpload: 'Enviar do dispositivo…', logoUploaded: 'Enviado ✓', logoBadType: 'Tipo de imagem não suportado', logoTooBig: 'Imagem demasiado grande (máx. 4 MB)', logoOr: 'ou', bookPage: 'Página do livro', ratingsToggle: 'Avaliações da comunidade (estrelas e resenhas)', ratingsHint: 'Avaliações com resenhas curtas em cada página de livro, partilhadas com todos os utilizadores deste servidor.', searchCard: 'Pesquisa', gsToggle: 'Pesquisar em todas as bibliotecas de uma vez', gsHint: 'Uma lista de resultados combinada de todas as bibliotecas a que tem acesso, com um selo de biblioteca em cada resultado. Desligue para voltar ao menu por biblioteca.', gsNoResults: 'Sem resultados', gsSearching: 'A pesquisar…', gsBooks: 'Livros', gsSeries: 'Séries', gsAuthors: 'Autores', spTitle: 'Página inicial', spHint: 'Abrir a aplicação sempre na vista escolhida de uma biblioteca (apenas neste navegador). Links diretos nunca são redirecionados.', spDefault: 'Padrão da aplicação', spViewHome: 'Início', spViewLibrary: 'Biblioteca', spViewSeries: 'Séries', spViewCollections: 'Coleções', spViewAuthors: 'Autores', scSet: 'Definir capa da série', scRemove: 'Remover capa da série', scEditDesc: 'Editar descrição da série', cardStars: 'Estrelas nas capas dos livros', hoverPolish: 'Acabamento ao passar o rato (escurecimento suave, play em destaque, estrelas ao passar)', sectUser: 'As suas definições', sectAdmin: 'Administração', lockTitle: 'Interruptores de funções', lockHint: 'Desativa à força uma função para todos os utilizadores deste servidor. Vence as definições pessoais; os outros veem após recarregar.', lockRatings: 'Desativar avaliações em todo o lado', lockCardRatings: 'Desativar estrelas nas capas', lockGlobalSearch: 'Desativar pesquisa global', lockHoverPolish: 'Desativar acabamento ao passar o rato', lockHeroCarousel: 'Desativar carrossel inicial', lockedNote: 'desativado pelo administrador', lfSort: 'Avaliação', lfRated: 'Avaliados', lfUnrated: 'Sem avaliação', lfClear: 'Limpar', lfNone: 'Nenhum livro corresponde', lfSection: 'Avaliação', lfWho: 'Avaliações de quem', lfEveryone: 'Todos', sdSave: 'Guardar', sdClear: 'Restaurar a descrição do livro', sdMore: 'Ler mais', sdLess: 'Mostrar menos', shSeries: 'Série', shBy: 'de ', shMore: ' e mais', shBookForms: ['livro', 'livros'], shAvg: 'média dos livros', shRated: 'avaliados', impLabel: 'Importar avaliações', impHint: 'Traga as suas avaliações do StoryGraph ou Goodreads. Exporte lá os seus livros como CSV e escolha o ficheiro aqui. Cada correspondência é mostrada para confirmar e nada é guardado até confirmar.', impBtn: 'Escolher ficheiro CSV…', impReading: 'A ler o ficheiro…', impMatching: 'A comparar com as suas bibliotecas…', impErrUnknown: 'Isto não parece um export do StoryGraph nem do Goodreads. Deve ser o CSV deles, com uma coluna "Star Rating" ou "My Rating".', impErrEmpty: 'Esse ficheiro não tem linhas.', impErrNoRated: 'Não há livros avaliados nesse ficheiro.', impErrRead: 'Não foi possível ler o ficheiro.', impErrLoad: 'Não foi possível carregar as suas bibliotecas. Tente novamente daqui a pouco.', impErrNoLibs: 'Nenhuma biblioteca participa nas avaliações, por isso não há para onde importar.', impTitle: 'Importar avaliações', impFrom: 'Export do %s: %d livros, %d deles avaliados', impLibs: 'A pesquisar: %s', impMatched: 'Correspondidos', impCheck: 'Vale a pena ver', impCheckHint: 'Perto mas sem certeza. Verifique a correspondência ou escolha outra.', impAlready: 'Já avaliados por si', impAlreadyHint: 'Ficam como estão, a menos que peça para os sobrescrever.', impNone: 'Não estão na sua biblioteca', impNoneHint: 'Nada neste servidor corresponde a estes.', impAll: 'Todos', impSkip: 'Saltar este', impDup: 'duplicado', impByTitle: 'título', impYours: 'a sua: %s', impReviews: 'Importar também resenhas escritas (%d)', impOver: 'Sobrescrever avaliações que já tenho', impGo: 'Importar %d avaliações', impCancel: 'Cancelar', impCloseBtn: 'Fechar', impWorking: 'A guardar… %d de %d', impDone: '%d avaliações importadas.', impFail: '%d não puderam ser guardadas.', rdTitle: 'Quem está a ler este livro', rdFin: 'Terminado', socTitle: 'Funções sociais', socHint: 'O que este servidor partilha entre utilizadores. O tempo de escuta alimenta o ranking; as outras duas revelam o que cada um lê, por isso ficam desligadas até as ativar. Aplica-se a todos de uma vez.', socTime: 'Tempo de escuta (ranking do servidor)', socContent: 'Títulos principais nas estatísticas partilhadas', socWho: 'Quem está a ler (páginas dos livros)', socUsers: 'Por utilizador', socUsersHint: 'Desligue alguém e nada dessa pessoa é partilhado, independentemente da escolha dela; só você pode reverter. "Saiu por opção" significa que a própria pessoa disse não.', socOpted: 'saiu por opção', srvTitle: 'Predefinições do servidor', srvHint: 'Guarde as suas definições atuais como padrão para todos os utilizadores deste servidor. Cada um pode personalizar por cima. Monte um volume em /data/nh no contentor do tema para as manter entre atualizações.', srvSave: 'Guardar como padrão do servidor', srvClear: 'Limpar padrão do servidor', srvSaved: 'Guardado', srvCleared: 'Limpo', srvErr: 'Falhou: é preciso sessão de administrador', crMode: 'Estante Continuar a ler', crCombine: 'Juntar ao carrossel', crSeparate: 'Manter como estante separada', crHide: 'Oculta', fsHint: 'Partilhe a sua escuta com as outras pessoas deste servidor: totais e minutos por dia para o ranking e, se o administrador os tiver ativado, também os seus títulos principais e que livros está a ler ou terminou. Ligado por predefinição. Ao desligar, tudo o que foi partilhado é eliminado.' , amfHide: 'Ocultar Marcar como terminado' , amfConfirm: 'Marcar este livro como terminado?' , fsHint: 'Partilha um resumo da tua escuta (totais e minutos por dia) com as outras pessoas deste servidor, alimenta a classificação. Ativado por predefinição. Ao desligar sais da classificação e dos totais, e tudo o que partilhaste é apagado.' , srToggle: 'Partilhar que livros você lê', srHint: 'Adiciona ao que os outros veem que livros está a ler, quais terminou e quais ouve mais. Desligado por predefinição; os interruptores do servidor do administrador também têm de estar ligados.' , fsHint: 'Partilhe o seu tempo de escuta (totais e minutos por dia) com as outras pessoas deste servidor; alimenta o ranking. Ligado por predefinição. Ao desligar, os outros deixam de o ver; os administradores continuam a ver a atividade, marcada como desativada.', srHint: 'Também permite aos outros ver que livros está a ler, quais terminou e quais ouve mais. Nada aparece até o administrador ligar os interruptores do servidor correspondentes.', seshTitle: 'Sessões recentes', seshNow: 'A ouvir agora' , hcHint: 'Envia as tuas avaliações para a tua conta Hardcover. Cola a chave API das definições da tua conta Hardcover; as chaves expiram ao fim de um ano.', hcKeyLbl: 'Chave API', hcToggle: 'Sincronizar novas avaliações automaticamente', hcPushAll: 'Enviar todas as avaliações', hcPushDiff: 'Enviar apenas as diferenças', hcDone: 'Concluído: %d sincronizadas, %d sem correspondência', hcNoKey: 'Cola primeiro a tua chave API do Hardcover' , hcMissHead: 'Não encontrado no Hardcover:' , hcHint: 'Envia as tuas avaliações para a tua conta Hardcover. Cola a chave API das definições da tua conta Hardcover.' , tabStyle: 'Estilo', tabMain: 'Página inicial', tabBooks: 'Livros', tabFeat: 'Funções' , hcImport: 'Importar do Hardcover', impMarkQ: 'Marcar também aqui os %d livros correspondidos como terminados?', impMarkDone: '%d marcados como terminados.' , impHint: 'Traz as tuas avaliações do Goodreads, StoryGraph ou Hardcover. Escolhe aqui o CSV exportado; o Hardcover importa diretamente pelo botão no seu cartão. Cada correspondência é mostrada para confirmares antes de guardar.' , impErrUnknown: 'Isto não parece um export do Goodreads, StoryGraph ou Hardcover: não foram encontradas colunas de título e avaliação.' , fsCard: 'Social', rnToggle: 'Mostrar o meu nome nas avaliações', rsToggle: 'Partilhar as minhas avaliações com os outros', socNames: 'Nomes nas avaliações', rtAnon: 'Anónimo' },
    nl: { cmStopped: "gestopt", cmScanErrs: "{e} niet gecontroleerd (Goodreads-fouten)", cmScanBusy: "Er loopt al een scan", cmGrErr: "Goodreads onbereikbaar, probeert opnieuw", cmNoMatchYet: "Geen match op Goodreads", cmLooking: "Zoeken op Goodreads…", cmToolHint: "Wat Goodreads-lezers van dit boek vinden. Het ? toont het gekoppelde boek en laat je het wijzigen.", cmSearchPh: "Zoeken op Goodreads…", cmNoVotes: "nog geen beoordelingen", cmRefresh: "Vernieuwen", cmScanAll: "Alles opnieuw nakijken, niet alleen wat ontbreekt", cmScanDone: "Klaar: {m} gevonden · {x} niet gevonden · {s} hadden er al een", cmStop: "Stoppen", cmScanLibs: "Te doorzoeken bibliotheken", cmScan: "Beoordelingen zoeken", lsLongest: "Langste boeken", lsSize: "Grootte op schijf", lsDuration: "Totale duur", lsGenresN: "Genres", lsSeriesN: "Series", lsAuthorsN: "Auteurs", lsBooks: "Boeken", lsGlance: "Bibliotheek in één oogopslag", cmOpen: "Openen op Goodreads", cmDetect: "Zoeken", cmAddr: "Adres van de helper", scmTitle: "Seriepagina", scmHint: "Hoe de gegenereerde omslag boven een serietitel eruitziet als er geen eigen omslag is geüpload (alleen deze browser).", scmDeck: "Gestapelde waaier", cmHint: "Wat Goodreads-lezers van een boek vinden: gemiddelde score en aantal stemmen, onder de sterren op de boekpagina en als sortering en filter. Boekpagina’s vullen zichzelf tijdens het bladeren.", cmGrOff: "Goodreads: niet ingesteld", cmSetup: "Nog geen helper? Draai dit naast het thema (compose-regels hieronder) en druk op Zoeken. Elke andere manier om de image te draaien werkt ook: plak het adres hierboven.", cmGrOn: "Goodreads: verbonden", cmGrDown: "Goodreads: ingesteld maar antwoordt niet", cmGrNoNum: "Goodreads: antwoordt, maar deze abs-tract-versie geeft geen beoordelingscijfers (gebruik de ratings-versie, zie README)", cmTitle: "Goodreads-beoordelingen", cmWrong: "Verkeerd boek?", cmFind: "Communitybeoordeling zoeken…", cmNone: "Geen match", cmUse: "Deze gebruiken", lfCommunity: "Goodreads-beoordeling", mfNatChange: "Wijzigen…", lsByTime: "op luistertijd", nrSearch: 'Vertellers filteren…', nrSortName: 'Naam', nrSortBooks: 'Meeste boeken', nrForms: ['Verteller', 'Vertellers'], nrBooksForms: ['boek', 'boeken'], auForms: ['Auteur', 'Auteurs'], auSearch: 'Auteurs filteren…' , rpBadge: "Open probleemmeldingen", wkTitle: "Luisterminuten, laatste 7 dagen", wkAvg: "Daggemiddelde", wkRow: "Dagen op rij", lsTitle: "Serverstatistieken", lsAll: "Alle bibliotheken", lsPlayed: "Afgespeelde boeken", lsBest: "Best beoordeeld", lsGenres: "Topgenres", srvPick: "Wat opslaan:", srvGRest: "Functies & gedrag", aeBtn: "Auteurs opruimen", aeTitle: "Auteurs zonder boeken", aeNone: "Elke auteur hier heeft minstens één boek.", aeRemove: "Verwijderen", aeDone: "Verwijderd", aeWorking: "Bezig met verwijderen…", rpMenu: "Probleem melden", rpTitle: "Probleem melden", rpWhat: "Wat is er mis?", rpNote: "Nog iets dat de beheerder moet weten? (optioneel)", rpSend: "Melding versturen", rpSent: "Verstuurd. Bedankt.", rpFail: "Versturen mislukt", rpMissing: "Ontbrekende of onvolledige inhoud", rpQuality: "Slechte geluidskwaliteit", rpPlay: "Speelt niet af", rpWrong: "Verkeerd boek, omslag of metadata", rpChapters: "Hoofdstukken kloppen niet", rpOther: "Iets anders", rpCard: "Gemelde problemen", rpHint: "Problemen die gebruikers vanaf een boekpagina meldden. Oplossen haalt de melding uit de lijst.", rpEmpty: "Niets gemeld.", rpResolve: "Oplossen", rpOpen: "Boek openen", apCard: "Afspelen", apToggle: "Volgende boek in een serie automatisch afspelen", apHint: "Als een boek klaar is, start automatisch het volgende uit dezelfde serie. Alleen boeken die al in je bibliotheek staan en nog niet uit zijn.", apNext: "Volgende in de serie", fdToggle: "Hulpmiddelen voor uitgelezen boeken in je statistieken", ysFinished: "Onlangs uitgelezen", ysAlmost: "Bijna klaar", ysMarkDone: "Markeren als uitgelezen", ysEditDate: "Einddatum wijzigen", ysNoDate: "geen datum", hmShowing: "grafiek toont", ysTitle: "Jouw luisteren", ysStreak: "Reeks", ysCurrent: "Huidige", ysLongest: "Langste", ysDaysTotal: "Dagen totaal", ysPace: "Tempo", ysThisWeek: "Deze week", ysPerDay: "Per luisterdag", ysBestDay: "Beste dag", ysWhen: "Wanneer je luistert", ysTopBooks: "Meest beluisterde boeken", ysTopAuthors: "Meest beluisterde auteurs", ysTopNarrators: "Meest beluisterde vertellers", rfRate: "Beoordelen", rfSheet: "Beoordeel dit boek", rfOpen: "Boekpagina openen", rfPickHint: "Kies een beoordeling", rfTitle: "Beoordeel wat je uit hebt", rfToggle: "Sectie ‘Beoordelen’ op de startpagina", hoTitle: "Volgorde van de startpagina", hoEmpty: "Open de startpagina één keer en kom dan terug.", hoReset: "Standaardvolgorde herstellen", ctIcon: "Pictogram", ctColour: "Accent", ctNeedBook: "Audiobookshelf kan geen lege collectie opslaan, kies minstens één boek. Later kun je vrij toevoegen of verwijderen.", colEditIcon: "Pictogram wijzigen", ciTitle: "Collectie-pictogram", ciReset: "Automatisch op naam afstemmen", colEditDesc: "Beschrijving bewerken", colBookForms: ["boek","boeken"], colForms: ["Collectie","Collecties"], ctTitle: "Collectie aanmaken", ctBlank: "Lege collectie", ctNew: "Nieuwe collectie", ctNewHint: "vanuit een sjabloon", ctNamePh: "Naam van de collectie", ctSearch: "Zoek boeken of auteurs…", ctCreate: "Aanmaken", ctBack: "‹ Terug", ctAddAll: "Alle boeken toevoegen", abTitle: "Boeken toevoegen", abAdd: "Toevoegen", sbTitle: "Serverranglijst", sbWeek: "Week", sbMonth: "Maand", sbYear: "Jaar", sbAll: "Altijd", sbBooks: "Boeken", sbTopBooks: "Meest beluisterd", sbTotal: "Totaal", sbListeners: "Luisteraars", sbAvg: "Gemiddelde", avRemove: "Foto verwijderen", avSet: "Foto kiezen…", ugListened: "Beluisterd", ugPhoto: "Profielfoto", ugNever: "Nooit", ugSeen: "Laatst actief", ugEdit: "Gebruiker bewerken", ugDelete: "Gebruiker verwijderen", ugAdd: "Foto toevoegen", ugChange: "Foto wijzigen", ugSortName: "A–Z", ugSortTime: "Meest beluisterd", ugSortSeen: "Onlangs actief", fsTitle: "Serverranglijst", fsCard: "Serverranglijst", fsToggle: "Mijn luisterstatistieken delen", fsJoin: "Zet ‘Mijn luisterstatistieken delen’ aan in het aanpassingspaneel om in de serverranglijst te verschijnen.", fsWaiting: "Delen staat aan, je samenvatting verschijnt zo in de serverranglijst, samen met alle anderen.", fsHint: "Deel een samenvatting van je luisteren (totalen en minuten per dag) met de anderen op deze server, dit voedt de serverranglijst. Standaard aan. Zet je het uit, dan verdwijn je uit de ranglijst en de totalen, en wordt alles wat je deelde gewist.", yirBtn: "Jaaroverzicht", yirTitle: "Jouw luisterjaar", yirYear: "Laatste 12 maanden", yirDays: "Dagen met luisteren", yirBest: "Beste dag", yirMonths: "Per maand", yirNone: "Nog geen luistergegevens.", bsCard: "Boeklinks", bsHint: "Knoppen op de boekpagina om een titel elders op te zoeken. Goodreads en de grootste site in jouw taal staan standaard aan.", bsFind: "Zoeken op", bsGlobal: "Overal", bsLocal: "Voor jouw taal", bsOther: "Andere landen", pgCard: "Herontworpen pagina's", pgHint: "Elke optie vervangt een Audiobookshelf-pagina door de NanoHive-versie. Zet uit om de originele terug te krijgen.", pgNarrators: "Vertellerkaarten", pgCollections: "Collectiepagina's", pgUsers: "Gebruikerskaarten (instellingen)", pgStats: "Luisterranglijst", pgPhoto: "Profielfoto in de bovenbalk", pgCinematic: "Filmische achtergrond", pgTransitions: "Paginaovergangen", pSearch: "Instellingen zoeken…", pSearchNone: "Niets gevonden.", ssscmDeck: "Gestapelde waaier", scmFirst: "Omslag van het eerste boek", scmGrid: "Omslagraster (standaard)", scmOff: "Verborgen" , erTypeface: "Lettertype", erPage: "Paginathema", erText: "Tekstkleur", erBg: "Achtergrond", erDefault: "Standaard (ABS)", erAuto: "Auto" , lfByAuthor: "Auteur › serievolgorde", lfBySeries: "Serie › volgorde" , lfSecFilters: "Meer filters", lfSecSort: "Multi-sortering", lfAuthor: "Auteur", lfSeries: "Serie", lfTitle: "Titel", lfYear: "Jaar", lfAdded: "Toegevoegd", lfDuration: "Duur", lfNarrator: "Verteller", lfGenre: "Genre", lfLanguage: "Taal", lfProgress: "Voortgang", lfPgFinished: "Uitgelezen", lfPgInProgress: "Bezig", lfPgNot: "Niet begonnen" , logoUpload: 'Uploaden vanaf apparaat…', logoUploaded: 'Geüpload ✓', logoBadType: 'Niet-ondersteund afbeeldingstype', logoTooBig: 'Afbeelding te groot (max. 4 MB)', logoOr: 'of', bookPage: 'Boekpagina', ratingsToggle: 'Communitybeoordelingen (sterren & recensies)', ratingsHint: 'Sterrenbeoordelingen met korte recensies op elke boekpagina, gedeeld met alle gebruikers van deze server.', searchCard: 'Zoeken', gsToggle: 'In alle bibliotheken tegelijk zoeken', gsHint: 'Eén samengevoegde resultatenlijst over alle toegankelijke bibliotheken, met een bibliotheekbadge bij elke treffer. Zet uit om het menu per bibliotheek terug te krijgen.', gsNoResults: 'Geen resultaten', gsSearching: 'Zoeken…', gsBooks: 'Boeken', gsSeries: 'Series', gsAuthors: 'Auteurs', spTitle: 'Startpagina', spHint: 'De app altijd openen op een gekozen bibliotheekweergave (alleen deze browser). Directe links worden nooit omgeleid.', spDefault: 'App-standaard', spViewHome: 'Home', spViewLibrary: 'Bibliotheek', spViewSeries: 'Series', spViewCollections: 'Collecties', spViewAuthors: 'Auteurs', scSet: 'Seriecover instellen', scRemove: 'Seriecover verwijderen', scEditDesc: 'Seriebeschrijving bewerken', cardStars: 'Sterren op boekcovers', hoverPolish: 'Afwerking bij zweven over kaarten (zachter dimmen, accentknop, sterren bij zweven)', sectUser: 'Jouw instellingen', sectAdmin: 'Beheer', lockTitle: 'Functieschakelaars', lockHint: 'Schakel een functie geforceerd uit voor alle gebruikers van deze server. Wint van persoonlijke instellingen; anderen zien het na hun volgende herlaadactie.', lockRatings: 'Beoordelingen overal uitschakelen', lockCardRatings: 'Sterren op covers uitschakelen', lockGlobalSearch: 'Globaal zoeken uitschakelen', lockHoverPolish: 'Kaartafwerking uitschakelen', lockHeroCarousel: 'Homecarrousel uitschakelen', lockedNote: 'uitgeschakeld door beheerder', lfSort: 'Beoordeling', lfRated: 'Beoordeeld', lfUnrated: 'Onbeoordeeld', lfClear: 'Wissen', lfNone: 'Geen boeken gevonden', lfSection: 'Beoordeling', lfWho: 'Wiens beoordelingen', lfEveryone: 'Iedereen', sdSave: 'Opslaan', sdClear: 'Boekbeschrijving herstellen', sdMore: 'Lees meer', sdLess: 'Minder tonen', shSeries: 'Serie', shBy: 'door ', shMore: ' & meer', shBookForms: ['boek', 'boeken'], shAvg: 'boekgemiddelde', shRated: 'beoordeeld', impLabel: 'Beoordelingen importeren', impHint: 'Haal je beoordelingen op uit StoryGraph of Goodreads. Exporteer daar je boeken als CSV en kies het bestand hier. Elke match wordt getoond ter bevestiging en niets wordt opgeslagen tot je bevestigt.', impBtn: 'CSV-bestand kiezen…', impReading: 'Bestand wordt gelezen…', impMatching: 'Vergelijken met je bibliotheken…', impErrUnknown: 'Dit lijkt geen export van StoryGraph of Goodreads. Het moet hun CSV zijn, met een kolom "Star Rating" of "My Rating".', impErrEmpty: 'Dat bestand bevat geen rijen.', impErrNoRated: 'Er staan geen beoordeelde boeken in dat bestand.', impErrRead: 'Het bestand kon niet worden gelezen.', impErrLoad: 'Je bibliotheken konden niet worden geladen. Probeer het zo opnieuw.', impErrNoLibs: 'Geen enkele bibliotheek doet mee aan beoordelingen, dus er is niets om in te importeren.', impTitle: 'Beoordelingen importeren', impFrom: '%s-export: %d boeken, waarvan %d beoordeeld', impLibs: 'Zoeken in: %s', impMatched: 'Gematcht', impCheck: 'Even nakijken', impCheckHint: 'Dichtbij maar niet zeker. Controleer de match of kies een andere.', impAlready: 'Al door jou beoordeeld', impAlreadyHint: 'Blijven ongemoeid, tenzij je overschrijven aanvinkt.', impNone: 'Niet in je bibliotheek', impNoneHint: 'Niets op deze server komt hiermee overeen.', impAll: 'Alle', impSkip: 'Deze overslaan', impDup: 'duplicaat', impByTitle: 'titel', impYours: 'de jouwe: %s', impReviews: 'Ook geschreven recensies importeren (%d)', impOver: 'Bestaande eigen beoordelingen overschrijven', impGo: '%d beoordelingen importeren', impCancel: 'Annuleren', impCloseBtn: 'Sluiten', impWorking: 'Opslaan… %d van %d', impDone: '%d beoordelingen geïmporteerd.', impFail: '%d konden niet worden opgeslagen.', rdTitle: 'Wie dit boek leest', rdFin: 'Uitgelezen', socTitle: 'Sociale functies', socHint: 'Wat deze server tussen gebruikers deelt. Luistertijd voedt de ranglijst; de andere twee verraden wat iemand leest en blijven dus uit tot je ze aanzet. Geldt voor iedereen tegelijk.', socTime: 'Luistertijd (serverranglijst)', socContent: 'Toptitels in gedeelde statistieken', socWho: 'Wie leest er (boekpagina\'s)', socUsers: 'Per gebruiker', socUsersHint: 'Zet je iemand uit, dan wordt niets van die persoon gedeeld, wat die zelf ook koos; alleen jij kunt dat terugdraaien. "Zelf afgemeld" betekent dat die persoon zelf nee zei.', socOpted: 'zelf afgemeld', srvTitle: 'Serverstandaarden', srvHint: 'Sla je huidige instellingen op als standaard voor elke gebruiker van deze server. Iedereen kan er zelf overheen personaliseren. Koppel een volume aan /data/nh in de themacontainer om ze bij updates te bewaren.', srvSave: 'Opslaan als serverstandaard', srvClear: 'Serverstandaard wissen', srvSaved: 'Opgeslagen', srvCleared: 'Gewist', srvErr: 'Mislukt: beheerdersaanmelding vereist', crMode: 'Verder lezen-plank', crCombine: 'Samenvoegen met carrousel', crSeparate: 'Als aparte plank houden', crHide: 'Verborgen', fsHint: 'Deel je luisteren met de anderen op deze server: tijdstotalen en minuten per dag voor de ranglijst, en, als de beheerder het heeft aangezet, ook je toptitels en welke boeken je leest of uit hebt. Standaard aan. Zet je het uit, dan wordt alles wat je deelde verwijderd.' , amfHide: 'Verberg Markeer als uitgelezen' , amfConfirm: 'Dit boek als uitgelezen markeren?' , fsHint: 'Deel een samenvatting van je luisteren (totalen en minuten per dag) met de anderen op deze server, dit voedt de serverranglijst. Standaard aan. Zet je het uit, dan verdwijn je uit de ranglijst en de totalen, en wordt alles wat je deelde gewist.' , srToggle: 'Delen welke boeken je leest', srHint: 'Voegt aan wat anderen zien toe welke boeken je leest, uit hebt en het meest beluistert. Standaard uit; ook de serverschakelaars van de beheerder moeten aan staan.' , fsHint: 'Deel je luistertijd (totalen en minuten per dag) met de anderen op deze server; die voedt de ranglijst. Standaard aan. Zet je het uit, dan zien andere gebruikers je niet meer; serverbeheerders zien de activiteit nog wel, gemarkeerd als afgemeld.', srHint: 'Laat anderen ook zien welke boeken je leest, uit hebt en het meest beluistert. Er is niets zichtbaar totdat de beheerder de bijbehorende serverschakelaars aanzet.', seshTitle: 'Recente sessies', seshNow: 'Luistert nu' , hcHint: 'Stuur je beoordelingen naar je Hardcover-account. Plak de API-sleutel uit je Hardcover-accountinstellingen; sleutels verlopen na een jaar.', hcKeyLbl: 'API-sleutel', hcToggle: 'Nieuwe beoordelingen automatisch synchroniseren', hcPushAll: 'Alle beoordelingen versturen', hcPushDiff: 'Alleen verschillen versturen', hcDone: 'Klaar: %d gesynchroniseerd, %d zonder match', hcNoKey: 'Plak eerst je Hardcover-API-sleutel' , hcMissHead: 'Niet gevonden op Hardcover:' , hcHint: 'Stuur je beoordelingen naar je Hardcover-account. Plak de API-sleutel uit je Hardcover-accountinstellingen.' , tabStyle: 'Stijl', tabMain: 'Startpagina', tabBooks: 'Boeken', tabFeat: 'Functies' , hcImport: 'Importeren uit Hardcover', impMarkQ: 'De %d gematchte boeken hier ook als uitgelezen markeren?', impMarkDone: '%d als uitgelezen gemarkeerd.' , impHint: 'Haal je beoordelingen op uit Goodreads, StoryGraph of Hardcover. Kies hier het geëxporteerde CSV-bestand; Hardcover importeert direct via de knop op zijn kaart. Elke match wordt eerst ter bevestiging getoond.' , impErrUnknown: 'Dit lijkt geen export van Goodreads, StoryGraph of Hardcover: geen titel- en beoordelingskolommen gevonden.' , fsCard: 'Sociaal', rnToggle: 'Mijn naam bij beoordelingen tonen', rsToggle: 'Mijn beoordelingen met anderen delen', socNames: 'Namen bij beoordelingen', rtAnon: 'Anoniem' },
    cs: { cmStopped: "zastaveno", cmScanErrs: "{e} nezkontrolováno (chyby Goodreads)", cmScanBusy: "Skenování už běží", cmGrErr: "Goodreads nedostupné, zkusím znovu", cmNoMatchYet: "Na Goodreads nenalezeno", cmLooking: "Hledám na Goodreads…", cmToolHint: "Co si o této knize myslí čtenáři Goodreads. Otazník ukáže přiřazenou knihu a umožní ji změnit.", cmSearchPh: "Hledat na Goodreads…", cmNoVotes: "zatím bez hodnocení", cmRefresh: "Obnovit", cmScanAll: "Znovu projít vše, ne jen chybějící", cmScanDone: "Hotovo: {m} nalezeno · {x} nenalezeno · {s} už mělo", cmStop: "Zastavit", cmScanLibs: "Knihovny k prohledání", cmScan: "Vyhledat hodnocení", lsLongest: "Nejdelší knihy", lsSize: "Velikost na disku", lsDuration: "Celková délka", lsGenresN: "Žánry", lsSeriesN: "Série", lsAuthorsN: "Autoři", lsBooks: "Knihy", lsGlance: "Knihovna v kostce", cmOpen: "Otevřít na Goodreads", cmDetect: "Najít", cmAddr: "Adresa pomocníka", scmTitle: "Stránka série", scmHint: "Vzhled generované obálky nad názvem série, když nebyla nahrána vlastní (jen tento prohlížeč).", scmDeck: "Vrstvený balíček", cmHint: "Co si o knize myslí čtenáři Goodreads: průměrné hodnocení a počet hlasů, pod hvězdičkami na stránce knihy a jako řazení a filtr. Stránky knih se doplňují samy při procházení.", cmGrOff: "Goodreads: nenastaveno", cmSetup: "Ještě žádný pomocník? Spusťte tohle vedle motivu (řádky compose níže) a stiskněte Najít. Jde i jakýkoli jiný způsob spuštění obrazu: vložte jeho adresu výše.", cmGrOn: "Goodreads: připojeno", cmGrDown: "Goodreads: nastaveno, ale neodpovídá", cmGrNoNum: "Goodreads: odpovídá, ale tato verze abs-tract nedává hodnocení (použijte verzi s hodnocením, viz README)", cmTitle: "Hodnocení Goodreads", cmWrong: "Špatná kniha?", cmFind: "Najít hodnocení komunity…", cmNone: "Žádná shoda", cmUse: "Použít", lfCommunity: "Hodnocení Goodreads", mfNatChange: "Změnit…", lsByTime: "podle času poslechu", nrSearch: 'Filtrovat vypravěče…', nrSortName: 'Jméno', nrSortBooks: 'Nejvíce knih', nrForms: ['Vypravěč', 'Vypravěči', 'Vypravěčů'], nrBooksForms: ['kniha', 'knihy', 'knih'], auForms: ['Autor', 'Autoři', 'Autorů'], auSearch: 'Filtrovat autory…' , sbTitle: "Žebříček serveru", sbWeek: "Týden", sbMonth: "Měsíc", sbYear: "Rok", sbAll: "Celkem", sbBooks: "Knihy", sbTopBooks: "Nejposlouchanější", sbTotal: "Celkem", sbListeners: "Posluchači", sbAvg: "Průměr", wkTitle: "Minuty poslechu, posledních 7 dní", wkAvg: "Denní průměr", wkRow: "Dní v řadě", rpBadge: "Otevřená hlášení problémů", rpMenu: "Nahlásit problém", rpTitle: "Nahlásit problém", rpWhat: "Co je špatně?", rpNote: "Ještě něco pro správce? (nepovinné)", rpSend: "Odeslat hlášení", rpSent: "Odesláno. Díky.", rpFail: "Odeslání se nezdařilo", rpMissing: "Chybějící nebo neúplný obsah", rpQuality: "Špatná kvalita zvuku", rpPlay: "Nepřehrává se", rpWrong: "Špatná kniha, obálka nebo metadata", rpChapters: "Špatné kapitoly", rpOther: "Něco jiného", rfRate: "Ohodnotit", rfSheet: "Ohodnoťte tuto knihu", rfOpen: "Otevřít stránku knihy", rfPickHint: "Vyberte hodnocení", rfTitle: "Ohodnoťte dokončené", ysFinished: "Nedávno dokončené", ysAlmost: "Téměř hotové", ysMarkDone: "Označit jako dokončené", ysEditDate: "Změnit datum dokončení", ysThisWeek: "Tento týden", ysBestDay: "Nejlepší den", ysTitle: "Váš poslech", fsTitle: "Žebříček serveru", fsCard: "Žebříček serveru", fsToggle: "Sdílet mé statistiky poslechu", fsJoin: "Zapněte „Sdílet mé statistiky poslechu“ v panelu přizpůsobení a objevíte se v žebříčku serveru.", fsWaiting: "Sdílení je zapnuté, vaše shrnutí se brzy přidá do žebříčku serveru spolu s ostatními.", fsHint: "Sdílejte shrnutí svého poslechu (součty a minuty za den) s ostatními na tomto serveru, pohání žebříček serveru. Ve výchozím stavu zapnuto. Po vypnutí zmizíte ze žebříčku i jeho součtů a sdílená data se smažou.", yirBtn: "Rok v přehledu", yirTitle: "Váš rok poslechu", yirYear: "Posledních 12 měsíců", yirDays: "Dny s poslechem", yirBest: "Nejlepší den", yirMonths: "Po měsících", yirNone: "Zatím žádná data o poslechu.", ssscmDeck: "Vrstvený balíček", scmFirst: "Obálka první knihy", scmGrid: "Mřížka obálek (výchozí)", scmOff: "Skrytá" , lsTitle: "Statistiky serveru", lsAll: "Všechny knihovny", lsPlayed: "Přehrané knihy", lsBest: "Nejlépe hodnocené", lsGenres: "Nejčastější žánry", srvPick: "Co uložit:", srvGRest: "Funkce a chování", aeBtn: "Uklidit autory", aeTitle: "Autoři bez knih", aeNone: "Každý autor tu má aspoň jednu knihu.", aeRemove: "Odebrat", aeDone: "Odebráno", aeWorking: "Odebírání…", rpCard: "Nahlášené problémy", rpHint: "Problémy nahlášené uživateli ze stránky knihy. Vyřešení je odstraní ze seznamu.", rpEmpty: "Nic nenahlášeno.", rpResolve: "Vyřešit", rpOpen: "Otevřít knihu", apCard: "Přehrávání", apToggle: "Automaticky přehrát další knihu ze série", apHint: "Po skončení knihy se automaticky spustí další ze stejné série. Jen knihy, které už máš v knihovně a nedokončil jsi je.", apNext: "Další ze série", fdToggle: "Nástroje dokončených knih ve statistikách", ysNoDate: "bez data", hmShowing: "graf ukazuje", ysStreak: "Šňůra", ysCurrent: "Aktuální", ysLongest: "Nejdelší", ysDaysTotal: "Dní celkem", ysPace: "Tempo", ysPerDay: "Na den poslechu", ysWhen: "Kdy posloucháš", ysTopBooks: "Nejposlouchanější knihy", ysTopAuthors: "Nejposlouchanější autoři", ysTopNarrators: "Nejposlouchanější vypravěči", rfToggle: "Sekce „Ohodnoťte“ na hlavní stránce", hoTitle: "Pořadí sekcí hlavní stránky", hoEmpty: "Otevři jednou hlavní stránku a vrať se sem.", hoReset: "Obnovit výchozí pořadí", ctIcon: "Ikona", ctColour: "Akcent", ctNeedBook: "Audiobookshelf neumí uložit prázdnou kolekci, vyber aspoň jednu knihu. Později můžeš přidávat i odebírat.", colEditIcon: "Změnit ikonu", ciTitle: "Ikona kolekce", ciReset: "Přiřadit automaticky podle názvu", colEditDesc: "Upravit popis", colBookForms: ["kniha","knihy","knih"], colForms: ["Kolekce","Kolekce","Kolekcí"], ctTitle: "Založit kolekci", ctBlank: "Prázdná kolekce", ctNew: "Nová kolekce", ctNewHint: "ze šablony", ctNamePh: "Název kolekce", ctSearch: "Hledat knihy nebo autory…", ctCreate: "Vytvořit", ctBack: "‹ Zpět", ctAddAll: "Přidat všechny knihy", abTitle: "Přidat knihy", abAdd: "Přidat", avRemove: "Odebrat fotku", avSet: "Vybrat fotku…", ugListened: "Poslechnuto", ugPhoto: "Profilová fotka", ugNever: "Nikdy", ugSeen: "Naposledy aktivní", ugEdit: "Upravit uživatele", ugDelete: "Smazat uživatele", ugAdd: "Přidat fotku", ugChange: "Změnit fotku", ugSortName: "A–Z", ugSortTime: "Nejvíce poslechnuté", ugSortSeen: "Nedávno aktivní", bsCard: "Odkazy knihy", bsHint: "Tlačítka na stránce knihy pro vyhledání titulu jinde. Goodreads a největší web tvého jazyka jsou zapnuté předem.", bsFind: "Hledat na", bsGlobal: "Všude", bsLocal: "Pro tvůj jazyk", bsOther: "Jiné země", pgCard: "Přepracované stránky", pgHint: "Každá volba nahradí stránku Audiobookshelf verzí NanoHive. Vypni ji a vrátí se původní.", pgNarrators: "Karty vypravěčů", pgCollections: "Stránky kolekcí", pgUsers: "Karty uživatelů (nastavení)", pgStats: "Žebříček poslechu", pgPhoto: "Profilová fotka v horní liště", pgCinematic: "Filmové pozadí", pgTransitions: "Přechody stránek", pSearch: "Hledat nastavení…", pSearchNone: "Nic neodpovídá.", erTypeface: "Písmo", erPage: "Motiv stránky", erText: "Barva textu", erBg: "Pozadí", erDefault: "Výchozí (ABS)", erAuto: "Auto" , lfByAuthor: "Autor › pořadí série", lfBySeries: "Série › pořadí" , lfSecFilters: "Další filtry", lfSecSort: "Vícenásobné řazení", lfAuthor: "Autor", lfSeries: "Série", lfTitle: "Název", lfYear: "Rok", lfAdded: "Přidáno", lfDuration: "Délka", lfNarrator: "Vypravěč", lfGenre: "Žánr", lfLanguage: "Jazyk", lfProgress: "Průběh", lfPgFinished: "Dokončené", lfPgInProgress: "Rozposlouchané", lfPgNot: "Nezačaté" , logoUpload: 'Nahrát ze zařízení…', logoUploaded: 'Nahráno ✓', logoBadType: 'Nepodporovaný typ obrázku', logoTooBig: 'Obrázek je příliš velký (max. 4 MB)', logoOr: 'nebo', bookPage: 'Stránka knihy', ratingsToggle: 'Hodnocení komunity (hvězdičky a recenze)', ratingsHint: 'Hvězdičková hodnocení s krátkými recenzemi na každé stránce knihy, sdílená se všemi uživateli tohoto serveru.', searchCard: 'Hledání', gsToggle: 'Hledat ve všech knihovnách najednou', gsHint: 'Jeden sloučený seznam výsledků ze všech dostupných knihoven, s odznakem knihovny u každého výsledku. Vypnutím se vrátí nabídka po knihovnách.', gsNoResults: 'Žádné výsledky', gsSearching: 'Hledám…', gsBooks: 'Knihy', gsSeries: 'Série', gsAuthors: 'Autoři', spTitle: 'Úvodní stránka', spHint: 'Otevírat aplikaci vždy na zvoleném pohledu knihovny (jen tento prohlížeč). Přímé odkazy se nikdy nepřesměrují.', spDefault: 'Výchozí aplikace', spViewHome: 'Domů', spViewLibrary: 'Knihovna', spViewSeries: 'Série', spViewCollections: 'Sbírky', spViewAuthors: 'Autoři', scSet: 'Nastavit obálku série', scRemove: 'Odebrat obálku série', scEditDesc: 'Upravit popis série', cardStars: 'Hvězdičky na obálkách knih', hoverPolish: 'Doladění karet při najetí (jemnější ztmavení, akcentní tlačítko, hvězdičky při najetí)', sectUser: 'Tvoje nastavení', sectAdmin: 'Správa', lockTitle: 'Přepínače funkcí', lockHint: 'Vynuceně vypne funkci pro všechny uživatele tohoto serveru. Má přednost před osobním nastavením; ostatní to uvidí po dalším načtení.', lockRatings: 'Vypnout hodnocení všude', lockCardRatings: 'Vypnout hvězdičky na obálkách', lockGlobalSearch: 'Vypnout globální hledání', lockHoverPolish: 'Vypnout doladění karet', lockHeroCarousel: 'Vypnout úvodní karusel', lockedNote: 'vypnuto správcem', lfSort: 'Hodnocení', lfRated: 'Ohodnocené', lfUnrated: 'Neohodnocené', lfClear: 'Vymazat', lfNone: 'Žádná kniha neodpovídá', lfSection: 'Hodnocení', lfWho: 'Čí hodnocení', lfEveryone: 'Všichni', sdSave: 'Uložit', sdClear: 'Obnovit popis knihy', sdMore: 'Číst dál', sdLess: 'Zobrazit méně', shSeries: 'Série', shBy: 'od ', shMore: ' a další', shBookForms: ['kniha', 'knihy', 'knih'], shAvg: 'průměr knih', shRated: 'hodnoceno', impLabel: 'Import hodnocení', impHint: 'Přenes si hodnocení ze StoryGraph nebo Goodreads. Exportuj tam své knihy do CSV a vyber soubor tady. Každou shodu uvidíš k potvrzení a nic se neuloží, dokud nepotvrdíš.', impBtn: 'Vybrat soubor CSV…', impReading: 'Čtu soubor…', impMatching: 'Porovnávám s tvými knihovnami…', impErrUnknown: 'Tohle nevypadá jako export ze StoryGraph ani Goodreads. Měl by to být jejich CSV soubor se sloupcem "Star Rating" nebo "My Rating".', impErrEmpty: 'Soubor neobsahuje žádné řádky.', impErrNoRated: 'V souboru nejsou žádné ohodnocené knihy.', impErrRead: 'Soubor se nepodařilo přečíst.', impErrLoad: 'Knihovny se nepodařilo načíst. Zkus to za chvíli znovu.', impErrNoLibs: 'Žádná knihovna se neúčastní hodnocení, takže není kam importovat.', impTitle: 'Import hodnocení', impFrom: 'Export %s: %d knih, z toho %d hodnocených', impLibs: 'Prohledávám: %s', impMatched: 'Spárováno', impCheck: 'Stojí za kontrolu', impCheckHint: 'Blízko, ale bez jistoty. Zkontroluj shodu, nebo vyber jinou.', impAlready: 'Už jsi hodnotil(a)', impAlreadyHint: 'Zůstanou beze změny, pokud nezvolíš přepsání.', impNone: 'Nejsou v tvé knihovně', impNoneHint: 'Nic na tomto serveru těmto položkám neodpovídá.', impAll: 'Vše', impSkip: 'Přeskočit', impDup: 'duplikát', impByTitle: 'název', impYours: 'tvoje: %s', impReviews: 'Importovat i psané recenze (%d)', impOver: 'Přepsat hodnocení, která už mám', impGo: 'Importovat %d hodnocení', impCancel: 'Zrušit', impCloseBtn: 'Zavřít', impWorking: 'Ukládám… %d z %d', impDone: 'Importováno %d hodnocení.', impFail: '%d se nepodařilo uložit.', rdTitle: 'Kdo tuhle knihu čte', rdFin: 'Dočteno', socTitle: 'Sociální funkce', socHint: 'Co tento server sdílí mezi uživateli. Čas poslechu pohání žebříček; zbylé dvě prozrazují, co kdo čte, takže zůstávají vypnuté, dokud je nezapneš. Platí pro všechny najednou.', socTime: 'Čas poslechu (žebříček serveru)', socContent: 'Nejposlouchanější tituly ve sdílených statistikách', socWho: 'Kdo čte (stránky knih)', socUsers: 'Po uživatelích', socUsersHint: 'Když někoho vypneš, nic z jeho dat se nesdílí bez ohledu na jeho volbu; vrátit to můžeš jen ty. "Odhlásil se sám" znamená, že dotyčný řekl ne sám.', socOpted: 'odhlásil se sám', srvTitle: 'Výchozí nastavení serveru', srvHint: 'Ulož svá aktuální nastavení jako výchozí pro všechny uživatele tohoto serveru. Každý si je může dál přizpůsobit. Připoj svazek na /data/nh v kontejneru motivu, aby vydržela mezi aktualizacemi.', srvSave: 'Uložit jako výchozí serveru', srvClear: 'Smazat výchozí serveru', srvSaved: 'Uloženo', srvCleared: 'Smazáno', srvErr: 'Selhalo: je potřeba přihlášení správce', crMode: 'Police Pokračovat ve čtení', crCombine: 'Sloučit do karuselu', crSeparate: 'Nechat jako samostatnou polici', crHide: 'Skrytá', fsHint: 'Sdílej svůj poslech s ostatními na tomto serveru: součty času a minuty po dnech pro žebříček, a pokud je správce zapnul, i tvoje nejposlouchanější tituly a knihy, které právě čteš nebo jsi dočetl(a). Ve výchozím stavu zapnuto. Po vypnutí se vše sdílené smaže.' , amfHide: 'Skrýt Označit jako dočtené' , amfConfirm: 'Označit tuto knihu jako dočtenou?' , fsHint: 'Sdílejte shrnutí svého poslechu (součty a minuty za den) s ostatními na tomto serveru, pohání žebříček serveru. Ve výchozím stavu zapnuto. Po vypnutí zmizíte ze žebříčku i jeho součtů a sdílená data se smažou.' , srToggle: 'Sdílet, jaké knihy čteš', srHint: 'Přidá k tomu, co ostatní vidí, jaké knihy právě čteš, které jsi dočetl(a) a které posloucháš nejvíc. Ve výchozím stavu vypnuto; navíc musí být zapnuté serverové přepínače správce.' , fsHint: 'Sdílej svůj čas poslechu (součty a minuty po dnech) s ostatními na tomto serveru; pohání žebříček. Ve výchozím stavu zapnuto. Po vypnutí tě ostatní uživatelé přestanou vidět; správci serveru aktivitu vidí dál, označenou jako odhlášenou.', srHint: 'Umožní ostatním vidět i to, jaké knihy právě čteš, které jsi dočetl(a) a které posloucháš nejvíc. Nic se nezobrazí, dokud správce nezapne příslušné serverové přepínače.', seshTitle: 'Poslední relace', seshNow: 'Právě poslouchají' , hcHint: 'Odesílejte svá hodnocení na účet Hardcover. Vložte klíč API z nastavení účtu Hardcover; klíče vyprší po roce.', hcKeyLbl: 'Klíč API', hcToggle: 'Synchronizovat nová hodnocení automaticky', hcPushAll: 'Odeslat všechna hodnocení', hcPushDiff: 'Odeslat jen rozdíly', hcDone: 'Hotovo: %d synchronizováno, %d bez shody', hcNoKey: 'Nejdřív vložte svůj klíč API Hardcover' , hcMissHead: 'Nenalezeno na Hardcover:' , hcHint: 'Odesílejte svá hodnocení na účet Hardcover. Vložte klíč API z nastavení účtu Hardcover.' , tabStyle: 'Styl', tabMain: 'Hlavní strana', tabBooks: 'Knihy', tabFeat: 'Funkce' , hcImport: 'Importovat z Hardcover', impMarkQ: 'Označit i zde %d spárovaných knih jako dokončené?', impMarkDone: '%d označeno jako dokončené.' , impHint: 'Přeneste svá hodnocení z Goodreads, StoryGraph nebo Hardcover. Vyberte zde exportované CSV; Hardcover se importuje přímo tlačítkem na své kartě. Každá shoda se zobrazí k potvrzení, než se cokoli uloží.' , impErrUnknown: 'Nevypadá to jako export z Goodreads, StoryGraph ani Hardcover: chybí sloupce s názvem a hodnocením.' , fsCard: 'Sociální', rnToggle: 'Zobrazovat mé jméno u hodnocení', rsToggle: 'Sdílet má hodnocení s ostatními', socNames: 'Jména u hodnocení', rtAnon: 'Anonym' },
    sk: { cmStopped: "zastavené", cmScanErrs: "{e} neskontrolované (chyby Goodreads)", cmScanBusy: "Skenovanie už beží", cmGrErr: "Goodreads nedostupné, skúsim znova", cmNoMatchYet: "Na Goodreads nenájdené", cmLooking: "Hľadám na Goodreads…", cmToolHint: "Čo si o tejto knihe myslia čitatelia Goodreads. Otáznik ukáže priradenú knihu a umožní ju zmeniť.", cmSearchPh: "Hľadať na Goodreads…", cmNoVotes: "zatiaľ bez hodnotení", cmRefresh: "Obnoviť", cmScanAll: "Znova prejsť všetko, nie len chýbajúce", cmScanDone: "Hotovo: {m} nájdených · {x} nenájdených · {s} už malo", cmStop: "Zastaviť", cmScanLibs: "Knižnice na prehľadanie", cmScan: "Vyhľadať hodnotenia", lsLongest: "Najdlhšie knihy", lsSize: "Veľkosť na disku", lsDuration: "Celková dĺžka", lsGenresN: "Žánre", lsSeriesN: "Série", lsAuthorsN: "Autori", lsBooks: "Knihy", lsGlance: "Knižnica v skratke", cmOpen: "Otvoriť na Goodreads", cmDetect: "Nájsť", cmAddr: "Adresa pomocníka", scmTitle: "Stránka série", scmHint: "Vzhľad generovanej obálky nad názvom série, keď nebola nahraná vlastná (len tento prehliadač).", scmDeck: "Vrstvený balíček", cmHint: "Čo si o knihe myslia čitatelia Goodreads: priemerné hodnotenie a počet hlasov, pod hviezdičkami na stránke knihy a ako triedenie a filter. Stránky kníh sa dopĺňajú samy pri prezeraní.", cmGrOff: "Goodreads: nenastavené", cmSetup: "Ešte žiadny pomocník? Spustite toto vedľa motívu (riadky compose nižšie) a stlačte Nájsť. Funguje aj akýkoľvek iný spôsob spustenia obrazu: vložte jeho adresu vyššie.", cmGrOn: "Goodreads: pripojené", cmGrDown: "Goodreads: nastavené, ale neodpovedá", cmGrNoNum: "Goodreads: odpovedá, ale táto verzia abs-tract nedáva hodnotenia (použite verziu s hodnoteniami, pozri README)", cmTitle: "Hodnotenia Goodreads", cmWrong: "Nesprávna kniha?", cmFind: "Nájsť hodnotenie komunity…", cmNone: "Žiadna zhoda", cmUse: "Použiť", lfCommunity: "Hodnotenie Goodreads", mfNatChange: "Zmeniť…", lsByTime: "podľa času počúvania", nrSearch: 'Filtrovať rozprávačov…', nrSortName: 'Meno', nrSortBooks: 'Najviac kníh', nrForms: ['Rozprávač', 'Rozprávači', 'Rozprávačov'], nrBooksForms: ['kniha', 'knihy', 'kníh'], auForms: ['Autor', 'Autori', 'Autorov'], auSearch: 'Filtrovať autorov…' , sbTitle: "Rebríček servera", sbWeek: "Týždeň", sbMonth: "Mesiac", sbYear: "Rok", sbAll: "Celkovo", sbBooks: "Knihy", sbTopBooks: "Najpočúvanejšie", sbTotal: "Spolu", sbListeners: "Poslucháči", sbAvg: "Priemer", wkTitle: "Minúty počúvania, posledných 7 dní", wkAvg: "Denný priemer", wkRow: "Dní po sebe", rpBadge: "Otvorené hlásenia problémov", rpMenu: "Nahlásiť problém", rpTitle: "Nahlásiť problém", rpWhat: "Čo je zle?", rpNote: "Ešte niečo pre správcu? (voliteľné)", rpSend: "Odoslať hlásenie", rpSent: "Odoslané. Vďaka.", rpFail: "Odoslanie zlyhalo", rpMissing: "Chýbajúci alebo neúplný obsah", rpQuality: "Zlá kvalita zvuku", rpPlay: "Neprehráva sa", rpWrong: "Zlá kniha, obálka alebo metadáta", rpChapters: "Zlé kapitoly", rpOther: "Niečo iné", rfRate: "Ohodnotiť", rfSheet: "Ohodnoťte túto knihu", rfOpen: "Otvoriť stránku knihy", rfPickHint: "Vyberte hodnotenie", rfTitle: "Ohodnoťte dokončené", ysFinished: "Nedávno dokončené", ysAlmost: "Takmer hotové", ysMarkDone: "Označiť ako dokončené", ysEditDate: "Zmeniť dátum dokončenia", ysThisWeek: "Tento týždeň", ysBestDay: "Najlepší deň", ysTitle: "Vaše počúvanie", fsTitle: "Rebríček servera", fsCard: "Rebríček servera", fsToggle: "Zdieľať moje štatistiky počúvania", fsJoin: "Zapnite „Zdieľať moje štatistiky počúvania“ v paneli prispôsobenia a objavíte sa v rebríčku servera.", fsWaiting: "Zdieľanie je zapnuté, vaše zhrnutie sa čoskoro pridá do rebríčka servera spolu s ostatnými.", fsHint: "Zdieľajte zhrnutie svojho počúvania (súčty a minúty za deň) s ostatnými na tomto serveri, poháňa rebríček servera. Predvolene zapnuté. Po vypnutí zmiznete z rebríčka aj jeho súčtov a zdieľané dáta sa vymažú.", yirBtn: "Rok v prehľade", yirTitle: "Váš rok počúvania", yirYear: "Posledných 12 mesiacov", yirDays: "Dni s počúvaním", yirBest: "Najlepší deň", yirMonths: "Po mesiacoch", yirNone: "Zatiaľ žiadne dáta o počúvaní.", ssscmDeck: "Vrstvený balíček", scmFirst: "Obálka prvej knihy", scmGrid: "Mriežka obálok (predvolené)", scmOff: "Skrytá" , lsTitle: "Štatistiky servera", lsAll: "Všetky knižnice", lsPlayed: "Prehrané knihy", lsBest: "Najlepšie hodnotené", lsGenres: "Najčastejšie žánre", srvPick: "Čo uložiť:", srvGRest: "Funkcie a správanie", aeBtn: "Upratať autorov", aeTitle: "Autori bez kníh", aeNone: "Každý autor tu má aspoň jednu knihu.", aeRemove: "Odstrániť", aeDone: "Odstránené", aeWorking: "Odstraňovanie…", rpCard: "Nahlásené problémy", rpHint: "Problémy nahlásené používateľmi zo stránky knihy. Vyriešenie ich odstráni zo zoznamu.", rpEmpty: "Nič nenahlásené.", rpResolve: "Vyriešiť", rpOpen: "Otvoriť knihu", apCard: "Prehrávanie", apToggle: "Automaticky prehrať ďalšiu knihu zo série", apHint: "Po skončení knihy sa automaticky spustí ďalšia z tej istej série. Iba knihy, ktoré už máš v knižnici a nedokončil si ich.", apNext: "Ďalšia zo série", fdToggle: "Nástroje dokončených kníh v štatistikách", ysNoDate: "bez dátumu", hmShowing: "graf ukazuje", ysStreak: "Séria dní", ysCurrent: "Aktuálna", ysLongest: "Najdlhšia", ysDaysTotal: "Dní spolu", ysPace: "Tempo", ysPerDay: "Na deň počúvania", ysWhen: "Kedy počúvaš", ysTopBooks: "Najpočúvanejšie knihy", ysTopAuthors: "Najpočúvanejší autori", ysTopNarrators: "Najpočúvanejší rozprávači", rfToggle: "Sekcia „Ohodnoť“ na hlavnej stránke", hoTitle: "Poradie sekcií hlavnej stránky", hoEmpty: "Otvor raz hlavnú stránku a vráť sa sem.", hoReset: "Obnoviť predvolené poradie", ctIcon: "Ikona", ctColour: "Akcent", ctNeedBook: "Audiobookshelf nevie uložiť prázdnu kolekciu, vyber aspoň jednu knihu. Neskôr môžeš pridávať aj odoberať.", colEditIcon: "Zmeniť ikonu", ciTitle: "Ikona kolekcie", ciReset: "Priradiť automaticky podľa názvu", colEditDesc: "Upraviť popis", colBookForms: ["kniha","knihy","kníh"], colForms: ["Kolekcia","Kolekcie","Kolekcií"], ctTitle: "Založiť kolekciu", ctBlank: "Prázdna kolekcia", ctNew: "Nová kolekcia", ctNewHint: "zo šablóny", ctNamePh: "Názov kolekcie", ctSearch: "Hľadať knihy alebo autorov…", ctCreate: "Vytvoriť", ctBack: "‹ Späť", ctAddAll: "Pridať všetky knihy", abTitle: "Pridať knihy", abAdd: "Pridať", avRemove: "Odstrániť fotku", avSet: "Vybrať fotku…", ugListened: "Vypočuté", ugPhoto: "Profilová fotka", ugNever: "Nikdy", ugSeen: "Naposledy aktívny", ugEdit: "Upraviť používateľa", ugDelete: "Zmazať používateľa", ugAdd: "Pridať fotku", ugChange: "Zmeniť fotku", ugSortName: "A–Z", ugSortTime: "Najviac vypočuté", ugSortSeen: "Nedávno aktívni", bsCard: "Odkazy knihy", bsHint: "Tlačidlá na stránke knihy na vyhľadanie titulu inde. Goodreads a najväčší web tvojho jazyka sú zapnuté vopred.", bsFind: "Hľadať na", bsGlobal: "Všade", bsLocal: "Pre tvoj jazyk", bsOther: "Iné krajiny", pgCard: "Prerobené stránky", pgHint: "Každá voľba nahradí stránku Audiobookshelf verziou NanoHive. Vypni ju a vráti sa pôvodná.", pgNarrators: "Karty rozprávačov", pgCollections: "Stránky kolekcií", pgUsers: "Karty používateľov (nastavenia)", pgStats: "Rebríček počúvania", pgPhoto: "Profilová fotka v hornej lište", pgCinematic: "Filmové pozadie", pgTransitions: "Prechody stránok", pSearch: "Hľadať nastavenia…", pSearchNone: "Nič sa nezhoduje.", erTypeface: "Písmo", erPage: "Motív stránky", erText: "Farba textu", erBg: "Pozadie", erDefault: "Predvolené (ABS)", erAuto: "Auto" , lfByAuthor: "Autor › poradie série", lfBySeries: "Séria › poradie" , lfSecFilters: "Ďalšie filtre", lfSecSort: "Viacúrovňové radenie", lfAuthor: "Autor", lfSeries: "Séria", lfTitle: "Názov", lfYear: "Rok", lfAdded: "Pridané", lfDuration: "Dĺžka", lfNarrator: "Rozprávač", lfGenre: "Žáner", lfLanguage: "Jazyk", lfProgress: "Priebeh", lfPgFinished: "Dokončené", lfPgInProgress: "Rozpočúvané", lfPgNot: "Nezačaté" , logoUpload: 'Nahrať zo zariadenia…', logoUploaded: 'Nahrané ✓', logoBadType: 'Nepodporovaný typ obrázka', logoTooBig: 'Obrázok je príliš veľký (max. 4 MB)', logoOr: 'alebo', bookPage: 'Stránka knihy', ratingsToggle: 'Hodnotenia komunity (hviezdičky a recenzie)', ratingsHint: 'Hviezdičkové hodnotenia s krátkymi recenziami na každej stránke knihy, zdieľané so všetkými používateľmi tohto servera.', searchCard: 'Hľadanie', gsToggle: 'Hľadať vo všetkých knižniciach naraz', gsHint: 'Jeden zlúčený zoznam výsledkov zo všetkých dostupných knižníc, s odznakom knižnice pri každom výsledku. Vypnutím sa vráti ponuka po knižniciach.', gsNoResults: 'Žiadne výsledky', gsSearching: 'Hľadám…', gsBooks: 'Knihy', gsSeries: 'Série', gsAuthors: 'Autori', spTitle: 'Úvodná stránka', spHint: 'Otvárať aplikáciu vždy na zvolenom pohľade knižnice (len tento prehliadač). Priame odkazy sa nikdy nepresmerujú.', spDefault: 'Predvolené aplikácie', spViewHome: 'Domov', spViewLibrary: 'Knižnica', spViewSeries: 'Série', spViewCollections: 'Zbierky', spViewAuthors: 'Autori', scSet: 'Nastaviť obálku série', scRemove: 'Odstrániť obálku série', scEditDesc: 'Upraviť popis série', cardStars: 'Hviezdičky na obálkach kníh', hoverPolish: 'Doladenie kariet pri prejdení (jemnejšie stmavenie, akcentné tlačidlo, hviezdičky pri prejdení)', sectUser: 'Tvoje nastavenia', sectAdmin: 'Správa', lockTitle: 'Prepínače funkcií', lockHint: 'Vynútene vypne funkciu pre všetkých používateľov tohto servera. Má prednosť pred osobnými nastaveniami; ostatní to uvidia po ďalšom načítaní.', lockRatings: 'Vypnúť hodnotenia všade', lockCardRatings: 'Vypnúť hviezdičky na obálkach', lockGlobalSearch: 'Vypnúť globálne hľadanie', lockHoverPolish: 'Vypnúť doladenie kariet', lockHeroCarousel: 'Vypnúť úvodný karusel', lockedNote: 'vypnuté správcom', lfSort: 'Hodnotenie', lfRated: 'Ohodnotené', lfUnrated: 'Neohodnotené', lfClear: 'Vymazať', lfNone: 'Žiadna kniha nezodpovedá', lfSection: 'Hodnotenie', lfWho: 'Čie hodnotenia', lfEveryone: 'Všetci', sdSave: 'Uložiť', sdClear: 'Obnoviť popis knihy', sdMore: 'Čítať ďalej', sdLess: 'Zobraziť menej', shSeries: 'Séria', shBy: 'od ', shMore: ' a ďalší', shBookForms: ['kniha', 'knihy', 'kníh'], shAvg: 'priemer kníh', shRated: 'hodnotených', impLabel: 'Import hodnotení', impHint: 'Prenes si hodnotenia zo StoryGraph alebo Goodreads. Exportuj tam svoje knihy do CSV a vyber súbor tu. Každú zhodu uvidíš na potvrdenie a nič sa neuloží, kým nepotvrdíš.', impBtn: 'Vybrať súbor CSV…', impReading: 'Čítam súbor…', impMatching: 'Porovnávam s tvojimi knižnicami…', impErrUnknown: 'Toto nevyzerá ako export zo StoryGraph ani Goodreads. Má to byť ich CSV so stĺpcom "Star Rating" alebo "My Rating".', impErrEmpty: 'Súbor neobsahuje žiadne riadky.', impErrNoRated: 'V súbore nie sú žiadne ohodnotené knihy.', impErrRead: 'Súbor sa nepodarilo prečítať.', impErrLoad: 'Knižnice sa nepodarilo načítať. Skús to o chvíľu znova.', impErrNoLibs: 'Žiadna knižnica sa nezúčastňuje hodnotení, takže niet kam importovať.', impTitle: 'Import hodnotení', impFrom: 'Export %s: %d kníh, z toho %d hodnotených', impLibs: 'Prehľadávam: %s', impMatched: 'Spárované', impCheck: 'Stojí za kontrolu', impCheckHint: 'Blízko, ale bez istoty. Skontroluj zhodu alebo vyber inú.', impAlready: 'Už si hodnotil(a)', impAlreadyHint: 'Zostanú bez zmeny, pokiaľ nezvolíš prepísanie.', impNone: 'Nie sú v tvojej knižnici', impNoneHint: 'Nič na tomto serveri týmto položkám nezodpovedá.', impAll: 'Všetko', impSkip: 'Preskočiť', impDup: 'duplikát', impByTitle: 'názov', impYours: 'tvoje: %s', impReviews: 'Importovať aj písané recenzie (%d)', impOver: 'Prepísať hodnotenia, ktoré už mám', impGo: 'Importovať %d hodnotení', impCancel: 'Zrušiť', impCloseBtn: 'Zavrieť', impWorking: 'Ukladám… %d z %d', impDone: 'Importovaných %d hodnotení.', impFail: '%d sa nepodarilo uložiť.', rdTitle: 'Kto túto knihu číta', rdFin: 'Dočítané', socTitle: 'Sociálne funkcie', socHint: 'Čo tento server zdieľa medzi používateľmi. Čas počúvania poháňa rebríček; zvyšné dve prezrádzajú, čo kto číta, takže zostávajú vypnuté, kým ich nezapneš. Platí pre všetkých naraz.', socTime: 'Čas počúvania (rebríček servera)', socContent: 'Najpočúvanejšie tituly v zdieľaných štatistikách', socWho: 'Kto číta (stránky kníh)', socUsers: 'Po používateľoch', socUsersHint: 'Keď niekoho vypneš, nič z jeho dát sa nezdieľa bez ohľadu na jeho voľbu; vrátiť to môžeš len ty. "Odhlásil sa sám" znamená, že dotyčný povedal nie sám.', socOpted: 'odhlásil sa sám', srvTitle: 'Predvolené nastavenia servera', srvHint: 'Ulož svoje aktuálne nastavenia ako predvolené pre všetkých používateľov tohto servera. Každý si ich môže ďalej prispôsobiť. Pripoj zväzok na /data/nh v kontajneri témy, aby vydržali medzi aktualizáciami.', srvSave: 'Uložiť ako predvolené servera', srvClear: 'Zmazať predvolené servera', srvSaved: 'Uložené', srvCleared: 'Zmazané', srvErr: 'Zlyhalo: vyžaduje sa prihlásenie správcu', crMode: 'Polica Pokračovať v čítaní', crCombine: 'Zlúčiť do karuselu', crSeparate: 'Nechať ako samostatnú policu', crHide: 'Skrytá', fsHint: 'Zdieľaj svoje počúvanie s ostatnými na tomto serveri: súčty času a minúty po dňoch pre rebríček, a ak ich správca zapol, aj tvoje najpočúvanejšie tituly a knihy, ktoré práve čítaš alebo si dočítal(a). Predvolene zapnuté. Po vypnutí sa všetko zdieľané zmaže.' , amfHide: 'Skryť Označiť ako dočítané' , amfConfirm: 'Označiť túto knihu ako dočítanú?' , fsHint: 'Zdieľajte zhrnutie svojho počúvania (súčty a minúty za deň) s ostatnými na tomto serveri, poháňa rebríček servera. Predvolene zapnuté. Po vypnutí zmiznete z rebríčka aj jeho súčtov a zdieľané dáta sa vymažú.' , srToggle: 'Zdieľať, aké knihy čítaš', srHint: 'Pridá k tomu, čo ostatní vidia, aké knihy práve čítaš, ktoré si dočítal(a) a ktoré počúvaš najviac. Predvolene vypnuté; navyše musia byť zapnuté serverové prepínače správcu.' , fsHint: 'Zdieľaj svoj čas počúvania (súčty a minúty po dňoch) s ostatnými na tomto serveri; poháňa rebríček. Predvolene zapnuté. Po vypnutí ťa ostatní používatelia prestanú vidieť; správcovia servera aktivitu vidia ďalej, označenú ako odhlásenú.', srHint: 'Umožní ostatným vidieť aj to, aké knihy práve čítaš, ktoré si dočítal(a) a ktoré počúvaš najviac. Nič sa nezobrazí, kým správca nezapne príslušné serverové prepínače.', seshTitle: 'Posledné relácie', seshNow: 'Práve počúvajú' , hcHint: 'Odosielajte svoje hodnotenia na účet Hardcover. Vložte kľúč API z nastavení účtu Hardcover; kľúče vypršia po roku.', hcKeyLbl: 'Kľúč API', hcToggle: 'Synchronizovať nové hodnotenia automaticky', hcPushAll: 'Odoslať všetky hodnotenia', hcPushDiff: 'Odoslať len rozdiely', hcDone: 'Hotovo: %d synchronizovaných, %d bez zhody', hcNoKey: 'Najprv vložte svoj kľúč API Hardcover' , hcMissHead: 'Nenájdené na Hardcover:' , hcHint: 'Odosielajte svoje hodnotenia na účet Hardcover. Vložte kľúč API z nastavení účtu Hardcover.' , tabStyle: 'Štýl', tabMain: 'Hlavná stránka', tabBooks: 'Knihy', tabFeat: 'Funkcie' , hcImport: 'Importovať z Hardcover', impMarkQ: 'Označiť aj tu %d spárovaných kníh ako dokončené?', impMarkDone: '%d označených ako dokončené.' , impHint: 'Preneste svoje hodnotenia z Goodreads, StoryGraph alebo Hardcover. Vyberte tu exportované CSV; Hardcover sa importuje priamo tlačidlom na svojej karte. Každá zhoda sa zobrazí na potvrdenie, kým sa čokoľvek uloží.' , impErrUnknown: 'Nevyzerá to ako export z Goodreads, StoryGraph ani Hardcover: chýbajú stĺpce s názvom a hodnotením.' , fsCard: 'Sociálne', rnToggle: 'Zobrazovať moje meno pri hodnoteniach', rsToggle: 'Zdieľať moje hodnotenia s ostatnými', socNames: 'Mená pri hodnoteniach', rtAnon: 'Anonym' },
    da: { cmStopped: "stoppet", cmScanErrs: "{e} ikke tjekket (Goodreads-fejl)", cmScanBusy: "En scanning kører allerede", cmGrErr: "Goodreads utilgængelig, prøver igen", cmNoMatchYet: "Intet match på Goodreads", cmLooking: "Søger på Goodreads…", cmToolHint: "Hvad Goodreads-læsere mener om denne bog. ? viser den matchede bog og lader dig ændre den.", cmSearchPh: "Søg på Goodreads…", cmNoVotes: "ingen bedømmelser endnu", cmRefresh: "Opdater", cmScanAll: "Gennemgå alt igen, ikke kun de manglende", cmScanDone: "Færdig: {m} fundet · {x} ikke fundet · {s} havde allerede", cmStop: "Stop", cmScanLibs: "Biblioteker at gennemgå", cmScan: "Søg bedømmelser", lsLongest: "Længste bøger", lsSize: "Størrelse på disk", lsDuration: "Samlet varighed", lsGenresN: "Genrer", lsSeriesN: "Serier", lsAuthorsN: "Forfattere", lsBooks: "Bøger", lsGlance: "Biblioteket i overblik", cmOpen: "Åbn på Goodreads", cmDetect: "Find", cmAddr: "Hjælperens adresse", scmTitle: "Serieside", scmHint: "Sådan ser det genererede omslag over en serietitel ud, når der ikke er uploadet et eget (kun denne browser).", scmDeck: "Lagdelt stak", cmHint: "Hvad Goodreads-læsere mener om en bog: gennemsnit og antal stemmer, under stjernerne på bogsiden og som sortering og filter. Bogsider udfylder sig selv, mens man kigger rundt.", cmGrOff: "Goodreads: ikke sat op", cmSetup: "Ingen hjælper endnu? Kør dette ved siden af temaet (compose-linjer herunder), og tryk på Find. Enhver anden måde at køre imaget på virker også: indsæt adressen ovenfor.", cmGrOn: "Goodreads: forbundet", cmGrDown: "Goodreads: sat op, men svarer ikke", cmGrNoNum: "Goodreads: svarer, men denne abs-tract-udgave giver ingen bedømmelsestal (brug ratings-udgaven, se README)", cmTitle: "Goodreads-bedømmelser", cmWrong: "Forkert bog?", cmFind: "Find fællesskabets bedømmelse…", cmNone: "Intet match", cmUse: "Brug denne", lfCommunity: "Goodreads-bedømmelse", mfNatChange: "Skift…", lsByTime: "efter lyttetid", nrSearch: 'Filtrér fortællere…', nrSortName: 'Navn', nrSortBooks: 'Flest bøger', nrForms: ['Fortæller', 'Fortællere'], nrBooksForms: ['bog', 'bøger'], auForms: ['Forfatter', 'Forfattere'], auSearch: 'Filtrér forfattere…' , sbTitle: "Serverrangliste", sbWeek: "Uge", sbMonth: "Måned", sbYear: "År", sbAll: "Altid", sbBooks: "Bøger", sbTopBooks: "Mest aflyttet", sbTotal: "I alt", sbListeners: "Lyttere", sbAvg: "Gennemsnit", wkTitle: "Lytteminutter, seneste 7 dage", wkAvg: "Dagligt gennemsnit", wkRow: "Dage i træk", rpBadge: "Åbne problemmeldinger", rpMenu: "Meld et problem", rpTitle: "Meld et problem", rpWhat: "Hvad er galt?", rpNote: "Andet administratoren bør vide? (valgfrit)", rpSend: "Send melding", rpSent: "Sendt. Tak.", rpFail: "Kunne ikke sende", rpMissing: "Manglende eller ufuldstændigt indhold", rpQuality: "Dårlig lydkvalitet", rpPlay: "Afspiller ikke", rpWrong: "Forkert bog, omslag eller metadata", rpChapters: "Forkerte kapitler", rpOther: "Noget andet", rfRate: "Bedøm", rfSheet: "Bedøm denne bog", rfOpen: "Åbn bogsiden", rfPickHint: "Vælg en bedømmelse", rfTitle: "Bedøm det du har lyttet færdigt", ysFinished: "Færdige for nylig", ysAlmost: "Næsten færdig", ysMarkDone: "Markér som færdig", ysEditDate: "Ret slutdatoen", ysThisWeek: "Denne uge", ysBestDay: "Bedste dag", ysTitle: "Din lytning", fsTitle: "Serverrangliste", fsCard: "Serverrangliste", fsToggle: "Del mine lyttestatistikker", fsJoin: "Slå “Del mine lyttestatistikker” til i tilpasningspanelet for at optræde i serverranglisten.", fsWaiting: "Deling er slået til, dit resumé kommer snart med i serverranglisten sammen med alle andre.", fsHint: "Del et resumé af din lytning (totaler og minutter pr. dag) med de andre på denne server, det driver serverranglisten. Slået til som standard. Slår du det fra, forsvinder du fra ranglisten og dens totaler, og alt delt slettes.", yirBtn: "Året i tilbageblik", yirTitle: "Dit lytteår", yirYear: "Seneste 12 måneder", yirDays: "Dage med lytning", yirBest: "Bedste dag", yirMonths: "Pr. måned", yirNone: "Ingen lyttedata endnu.", ssscmDeck: "Lagdelt stak", scmFirst: "Første bogs omslag", scmGrid: "Omslagsgitter (standard)", scmOff: "Skjult" , lsTitle: "Serverstatistik", lsAll: "Alle biblioteker", lsPlayed: "Afspillede bøger", lsBest: "Bedst bedømt", lsGenres: "Topgenrer", srvPick: "Hvad skal gemmes:", srvGRest: "Funktioner & adfærd", aeBtn: "Ryd op i forfattere", aeTitle: "Forfattere uden bøger", aeNone: "Alle forfattere her har mindst én bog.", aeRemove: "Fjern", aeDone: "Fjernet", aeWorking: "Fjerner…", rpCard: "Meldte problemer", rpHint: "Problemer som brugere meldte fra en bogside. Løses et, forsvinder det fra listen.", rpEmpty: "Intet meldt.", rpResolve: "Løs", rpOpen: "Åbn bog", apCard: "Afspilning", apToggle: "Afspil automatisk næste bog i en serie", apHint: "Når en bog slutter, starter den næste i samme serie automatisk. Kun bøger du allerede har, og som ikke er færdige.", apNext: "Næste i serien", fdToggle: "Værktøjer til færdige bøger i din statistik", ysNoDate: "ingen dato", hmShowing: "grafen viser", ysStreak: "Stime", ysCurrent: "Nuværende", ysLongest: "Længste", ysDaysTotal: "Dage i alt", ysPace: "Tempo", ysPerDay: "Pr. lyttedag", ysWhen: "Hvornår du lytter", ysTopBooks: "Mest aflyttede bøger", ysTopAuthors: "Mest aflyttede forfattere", ysTopNarrators: "Mest aflyttede oplæsere", rfToggle: "Sektionen “Bedøm” på forsiden", hoTitle: "Rækkefølge på forsiden", hoEmpty: "Åbn forsiden én gang og kom tilbage.", hoReset: "Gendan standardrækkefølge", ctIcon: "Ikon", ctColour: "Accent", ctNeedBook: "Audiobookshelf kan ikke gemme en tom samling, vælg mindst én bog. Du kan tilføje og fjerne frit senere.", colEditIcon: "Skift ikon", ciTitle: "Samlingens ikon", ciReset: "Tilpas automatisk efter navnet", colEditDesc: "Redigér beskrivelse", colBookForms: ["bog","bøger"], colForms: ["Samling","Samlinger"], ctTitle: "Opret en samling", ctBlank: "Tom samling", ctNew: "Ny samling", ctNewHint: "fra en skabelon", ctNamePh: "Samlingens navn", ctSearch: "Søg bøger eller forfattere…", ctCreate: "Opret", ctBack: "‹ Tilbage", ctAddAll: "Tilføj alle bøger", abTitle: "Tilføj bøger", abAdd: "Tilføj", avRemove: "Fjern foto", avSet: "Vælg foto…", ugListened: "Lyttet", ugPhoto: "Profilfoto", ugNever: "Aldrig", ugSeen: "Sidst aktiv", ugEdit: "Redigér bruger", ugDelete: "Slet bruger", ugAdd: "Tilføj foto", ugChange: "Skift foto", ugSortName: "A–Z", ugSortTime: "Mest aflyttet", ugSortSeen: "Senest aktive", bsCard: "Boglinks", bsHint: "Knapper på bogsiden til at slå en titel op andre steder. Goodreads og dit sprogs største side er slået til fra start.", bsFind: "Søg på", bsGlobal: "Overalt", bsLocal: "Til dit sprog", bsOther: "Andre lande", pgCard: "Redesignede sider", pgHint: "Hver af disse erstatter en Audiobookshelf-side med NanoHive-versionen. Slå fra for at få originalen tilbage.", pgNarrators: "Oplæserkort", pgCollections: "Samlingssider", pgUsers: "Brugerkort (indstillinger)", pgStats: "Lytterangliste", pgPhoto: "Profilfoto i topbjælken", pgCinematic: "Biografbaggrund", pgTransitions: "Sideovergange", pSearch: "Søg indstillinger…", pSearchNone: "Intet matcher.", erTypeface: "Skrifttype", erPage: "Sidetema", erText: "Tekstfarve", erBg: "Baggrund", erDefault: "Standard (ABS)", erAuto: "Auto" , lfByAuthor: "Forfatter › serierækkefølge", lfBySeries: "Serie › rækkefølge" , lfSecFilters: "Flere filtre", lfSecSort: "Multi-sortering", lfAuthor: "Forfatter", lfSeries: "Serie", lfTitle: "Titel", lfYear: "År", lfAdded: "Tilføjet", lfDuration: "Varighed", lfNarrator: "Oplæser", lfGenre: "Genre", lfLanguage: "Sprog", lfProgress: "Fremskridt", lfPgFinished: "Færdige", lfPgInProgress: "I gang", lfPgNot: "Ikke begyndt" , logoUpload: 'Upload fra enheden…', logoUploaded: 'Uploadet ✓', logoBadType: 'Billedtypen understøttes ikke', logoTooBig: 'Billedet er for stort (maks. 4 MB)', logoOr: 'eller', bookPage: 'Bogside', ratingsToggle: 'Fællesskabets bedømmelser (stjerner & anmeldelser)', ratingsHint: 'Stjernebedømmelser med korte anmeldelser på hver bogside, delt med alle brugere af denne server.', searchCard: 'Søgning', gsToggle: 'Søg i alle biblioteker på én gang', gsHint: 'Én samlet resultatliste på tværs af alle tilgængelige biblioteker, med et biblioteksmærke på hvert hit. Slå fra for at få menuen pr. bibliotek tilbage.', gsNoResults: 'Ingen resultater', gsSearching: 'Søger…', gsBooks: 'Bøger', gsSeries: 'Serier', gsAuthors: 'Forfattere', spTitle: 'Startside', spHint: 'Åbn altid appen på en valgt biblioteksvisning (kun denne browser). Direkte links omdirigeres aldrig.', spDefault: 'Appens standard', spViewHome: 'Hjem', spViewLibrary: 'Bibliotek', spViewSeries: 'Serier', spViewCollections: 'Samlinger', spViewAuthors: 'Forfattere', scSet: 'Vælg seriecover', scRemove: 'Fjern seriecover', scEditDesc: 'Rediger seriebeskrivelse', cardStars: 'Stjerner på bogcovers', hoverPolish: 'Finpudsning ved hover (blødere dæmpning, accentknap, stjerner ved hover)', sectUser: 'Dine indstillinger', sectAdmin: 'Administration', lockTitle: 'Funktionskontakter', lockHint: 'Tvangsdeaktiver en funktion for alle brugere af denne server. Vinder over personlige indstillinger; andre ser det efter næste genindlæsning.', lockRatings: 'Slå bedømmelser fra overalt', lockCardRatings: 'Slå stjerner på covers fra', lockGlobalSearch: 'Slå global søgning fra', lockHoverPolish: 'Slå hover-finpudsning fra', lockHeroCarousel: 'Slå forsidekarrusel fra', lockedNote: 'slået fra af administrator', lfSort: 'Bedømmelse', lfRated: 'Bedømt', lfUnrated: 'Ikke bedømt', lfClear: 'Ryd', lfNone: 'Ingen bøger matcher', lfSection: 'Bedømmelse', lfWho: 'Hvis bedømmelser', lfEveryone: 'Alle', sdSave: 'Gem', sdClear: 'Gendan bogbeskrivelsen', sdMore: 'Læs mere', sdLess: 'Vis mindre', shSeries: 'Serie', shBy: 'af ', shMore: ' & flere', shBookForms: ['bog', 'bøger'], shAvg: 'boggennemsnit', shRated: 'bedømt', impLabel: 'Importér bedømmelser', impHint: 'Hent dine bedømmelser fra StoryGraph eller Goodreads. Eksportér dine bøger dér som CSV og vælg filen her. Hvert match vises til bekræftelse, og intet gemmes, før du bekræfter.', impBtn: 'Vælg CSV-fil…', impReading: 'Læser filen…', impMatching: 'Matcher mod dine biblioteker…', impErrUnknown: 'Det ligner ikke en eksport fra StoryGraph eller Goodreads. Det skal være deres CSV med en kolonne "Star Rating" eller "My Rating".', impErrEmpty: 'Filen har ingen rækker.', impErrNoRated: 'Der er ingen bedømte bøger i filen.', impErrRead: 'Filen kunne ikke læses.', impErrLoad: 'Dine biblioteker kunne ikke indlæses. Prøv igen om lidt.', impErrNoLibs: 'Intet bibliotek deltager i bedømmelser, så der er intet at importere til.', impTitle: 'Importér bedømmelser', impFrom: '%s-eksport: %d bøger, heraf %d bedømt', impLibs: 'Søger i: %s', impMatched: 'Matchet', impCheck: 'Værd at tjekke', impCheckHint: 'Tæt på, men ikke sikkert. Tjek matchet, eller vælg et andet.', impAlready: 'Allerede bedømt af dig', impAlreadyHint: 'Røres ikke, medmindre du vælger at overskrive.', impNone: 'Ikke i dit bibliotek', impNoneHint: 'Intet på denne server matcher disse.', impAll: 'Alle', impSkip: 'Spring denne over', impDup: 'dublet', impByTitle: 'titel', impYours: 'din: %s', impReviews: 'Importér også skrevne anmeldelser (%d)', impOver: 'Overskriv bedømmelser, jeg allerede har', impGo: 'Importér %d bedømmelser', impCancel: 'Annuller', impCloseBtn: 'Luk', impWorking: 'Gemmer… %d af %d', impDone: '%d bedømmelser importeret.', impFail: '%d kunne ikke gemmes.', rdTitle: 'Hvem læser denne bog', rdFin: 'Færdig', socTitle: 'Sociale funktioner', socHint: 'Hvad denne server deler mellem brugere. Lyttetiden driver ranglisten; de to andre afslører, hvad folk læser, så de er slået fra, indtil du slår dem til. Gælder for alle på én gang.', socTime: 'Lyttetid (serverrangliste)', socContent: 'Toptitler i delte statistikker', socWho: 'Hvem læser (bogsider)', socUsers: 'Pr. bruger', socUsersHint: 'Slår du nogen fra, deles intet af deres, uanset hvad de selv har valgt; kun du kan slå det til igen. "Frameldt selv" betyder, at personen selv sagde nej.', socOpted: 'frameldt selv', srvTitle: 'Serverstandarder', srvHint: 'Gem dine nuværende indstillinger som standard for alle brugere af denne server. Alle kan stadig tilpasse ovenpå. Montér et volume på /data/nh i tema-containeren for at beholde dem gennem opdateringer.', srvSave: 'Gem som serverstandard', srvClear: 'Ryd serverstandard', srvSaved: 'Gemt', srvCleared: 'Ryddet', srvErr: 'Mislykkedes: kræver administrator-login', crMode: 'Læs videre-hylde', crCombine: 'Kombinér med karrusellen', crSeparate: 'Behold som separat hylde', crHide: 'Skjult', fsHint: 'Del din lytning med de andre på denne server: tidssummer og minutter pr. dag til ranglisten, og, hvis administratoren har slået det til, også dine toptitler og hvilke bøger du læser eller har gjort færdige. Slået til som standard. Slår du det fra, slettes alt det delte.' , amfHide: 'Skjul Markér som færdig' , amfConfirm: 'Markér denne bog som færdig?' , fsHint: 'Del et resumé af din lytning (totaler og minutter pr. dag) med de andre på denne server, det driver serverranglisten. Slået til som standard. Slår du det fra, forsvinder du fra ranglisten og dens totaler, og alt delt slettes.' , srToggle: 'Del hvilke bøger du læser', srHint: 'Tilføjer til det, andre ser, hvilke bøger du læser, har gjort færdige og lytter mest til. Slået fra som standard; administratorens serverkontakter skal også være slået til.' , fsHint: 'Del din lyttetid (summer og minutter pr. dag) med de andre på denne server; den driver ranglisten. Slået til som standard. Slår du fra, ser andre brugere dig ikke længere; serveradministratorer ser stadig aktiviteten, markeret som frameldt.', srHint: 'Lader også andre se, hvilke bøger du læser, har gjort færdige og lytter mest til. Intet vises, før administratoren slår de tilhørende serverkontakter til.', seshTitle: 'Seneste sessioner', seshNow: 'Lytter nu' , hcHint: 'Send dine bedømmelser til din Hardcover-konto. Indsæt API-nøglen fra dine Hardcover-kontoindstillinger; nøgler udløber efter et år.', hcKeyLbl: 'API-nøgle', hcToggle: 'Synkroniser nye bedømmelser automatisk', hcPushAll: 'Send alle bedømmelser', hcPushDiff: 'Send kun forskelle', hcDone: 'Færdig: %d synkroniseret, %d uden match', hcNoKey: 'Indsæt først din Hardcover-API-nøgle' , hcMissHead: 'Ikke fundet på Hardcover:' , hcHint: 'Send dine bedømmelser til din Hardcover-konto. Indsæt API-nøglen fra dine Hardcover-kontoindstillinger.' , tabStyle: 'Stil', tabMain: 'Forside', tabBooks: 'Bøger', tabFeat: 'Funktioner' , hcImport: 'Importér fra Hardcover', impMarkQ: 'Markér også de %d matchede bøger som færdige her?', impMarkDone: '%d markeret som færdige.' , impHint: 'Hent dine bedømmelser fra Goodreads, StoryGraph eller Hardcover. Vælg den eksporterede CSV her; Hardcover importeres direkte via knappen på sit kort. Hvert match vises til bekræftelse, før noget gemmes.' , impErrUnknown: 'Det ligner ikke et export fra Goodreads, StoryGraph eller Hardcover: ingen titel- og bedømmelseskolonner fundet.' , fsCard: 'Socialt', rnToggle: 'Vis mit navn ved bedømmelser', rsToggle: 'Del mine bedømmelser med andre', socNames: 'Navne ved bedømmelser', rtAnon: 'Anonym' },
    sv: { cmStopped: "stoppad", cmScanErrs: "{e} inte kontrollerade (Goodreads-fel)", cmScanBusy: "En sökning pågår redan", cmGrErr: "Goodreads nås inte, försöker igen", cmNoMatchYet: "Ingen träff på Goodreads", cmLooking: "Söker på Goodreads…", cmToolHint: "Vad Goodreads-läsare tycker om den här boken. ? visar den matchade boken och låter dig ändra den.", cmSearchPh: "Sök på Goodreads…", cmNoVotes: "inga betyg än", cmRefresh: "Uppdatera", cmScanAll: "Gå igenom allt igen, inte bara de som saknas", cmScanDone: "Klart: {m} hittade · {x} hittades inte · {s} hade redan", cmStop: "Stoppa", cmScanLibs: "Bibliotek att söka igenom", cmScan: "Sök betyg", lsLongest: "Längsta böckerna", lsSize: "Storlek på disk", lsDuration: "Total längd", lsGenresN: "Genrer", lsSeriesN: "Serier", lsAuthorsN: "Författare", lsBooks: "Böcker", lsGlance: "Biblioteket i korthet", cmOpen: "Öppna på Goodreads", cmDetect: "Hitta", cmAddr: "Hjälpens adress", scmTitle: "Seriesida", scmHint: "Hur det genererade omslaget ovanför en serietitel ser ut när inget eget laddats upp (endast denna webbläsare).", scmDeck: "Lagrad kortlek", cmHint: "Vad Goodreads-läsare tycker om en bok: medelbetyg och antal röster, under stjärnorna på boksidan och som sortering och filter. Boksidor fylls i av sig själva när man bläddrar.", cmGrOff: "Goodreads: inte inställt", cmSetup: "Ingen hjälp än? Kör detta bredvid temat (compose-rader nedan) och tryck på Hitta. Alla andra sätt att köra imagen fungerar också: klistra in adressen ovan.", cmGrOn: "Goodreads: ansluten", cmGrDown: "Goodreads: inställt men svarar inte", cmGrNoNum: "Goodreads: svarar, men denna abs-tract-version ger inga betygssiffror (använd ratings-versionen, se README)", cmTitle: "Goodreads-betyg", cmWrong: "Fel bok?", cmFind: "Hitta communitybetyg…", cmNone: "Ingen träff", cmUse: "Använd denna", lfCommunity: "Goodreads-betyg", mfNatChange: "Ändra…", lsByTime: "efter lyssningstid", nrSearch: 'Filtrera uppläsare…', nrSortName: 'Namn', nrSortBooks: 'Flest böcker', nrForms: ['Uppläsare', 'Uppläsare'], nrBooksForms: ['bok', 'böcker'], auForms: ['Författare', 'Författare'], auSearch: 'Filtrera författare…' , sbTitle: "Serverranking", sbWeek: "Vecka", sbMonth: "Månad", sbYear: "År", sbAll: "Totalt", sbBooks: "Böcker", sbTopBooks: "Mest lyssnade", sbTotal: "Totalt", sbListeners: "Lyssnare", sbAvg: "Genomsnitt", wkTitle: "Lyssningsminuter, senaste 7 dagarna", wkAvg: "Dagligt snitt", wkRow: "Dagar i rad", rpBadge: "Öppna problemrapporter", rpMenu: "Rapportera ett problem", rpTitle: "Rapportera ett problem", rpWhat: "Vad är fel?", rpNote: "Något mer administratören bör veta? (valfritt)", rpSend: "Skicka rapport", rpSent: "Skickat. Tack.", rpFail: "Kunde inte skicka", rpMissing: "Saknat eller ofullständigt innehåll", rpQuality: "Dålig ljudkvalitet", rpPlay: "Spelar inte upp", rpWrong: "Fel bok, omslag eller metadata", rpChapters: "Fel kapitel", rpOther: "Något annat", rfRate: "Betygsätt", rfSheet: "Betygsätt den här boken", rfOpen: "Öppna boksidan", rfPickHint: "Välj ett betyg", rfTitle: "Betygsätt det du lyssnat klart", ysFinished: "Nyligen avslutade", ysAlmost: "Nästan klara", ysMarkDone: "Markera som avslutad", ysEditDate: "Ändra slutdatum", ysThisWeek: "Denna vecka", ysBestDay: "Bästa dagen", ysTitle: "Ditt lyssnande", fsTitle: "Serverranking", fsCard: "Serverranking", fsToggle: "Dela min lyssningsstatistik", fsJoin: "Slå på ”Dela min lyssningsstatistik” i anpassningspanelen för att synas i serverrankingen.", fsWaiting: "Delning är på, din sammanfattning läggs snart till i serverrankingen tillsammans med alla andra.", fsHint: "Dela en sammanfattning av ditt lyssnande (totaler och minuter per dag) med de andra på servern, den driver serverrankingen. På som standard. Stänger du av försvinner du ur rankingen och dess totaler, och allt delat raderas.", yirBtn: "Året i återblick", yirTitle: "Ditt lyssningsår", yirYear: "Senaste 12 månaderna", yirDays: "Dagar med lyssning", yirBest: "Bästa dagen", yirMonths: "Per månad", yirNone: "Inga lyssningsdata ännu.", ssscmDeck: "Lagrad kortlek", scmFirst: "Första bokens omslag", scmGrid: "Omslagsrutnät (standard)", scmOff: "Dold" , lsTitle: "Serverstatistik", lsAll: "Alla bibliotek", lsPlayed: "Spelade böcker", lsBest: "Bäst betygsatta", lsGenres: "Toppgenrer", srvPick: "Vad ska sparas:", srvGRest: "Funktioner & beteende", aeBtn: "Städa författare", aeTitle: "Författare utan böcker", aeNone: "Alla författare här har minst en bok.", aeRemove: "Ta bort", aeDone: "Borttagna", aeWorking: "Tar bort…", rpCard: "Rapporterade problem", rpHint: "Problem som användare rapporterat från en boksida. Att lösa ett tar bort det ur listan.", rpEmpty: "Inget rapporterat.", rpResolve: "Lös", rpOpen: "Öppna boken", apCard: "Uppspelning", apToggle: "Spela automatiskt nästa bok i en serie", apHint: "När en bok tar slut startar nästa i samma serie automatiskt. Bara böcker du redan har och inte lyssnat klart.", apNext: "Nästa i serien", fdToggle: "Verktyg för avslutade böcker i din statistik", ysNoDate: "inget datum", hmShowing: "grafen visar", ysStreak: "Svit", ysCurrent: "Nuvarande", ysLongest: "Längsta", ysDaysTotal: "Dagar totalt", ysPace: "Tempo", ysPerDay: "Per lyssningsdag", ysWhen: "När du lyssnar", ysTopBooks: "Mest lyssnade böcker", ysTopAuthors: "Mest lyssnade författare", ysTopNarrators: "Mest lyssnade uppläsare", rfToggle: "Sektionen ”Betygsätt” på startsidan", hoTitle: "Ordning på startsidans sektioner", hoEmpty: "Öppna startsidan en gång och kom tillbaka.", hoReset: "Återställ standardordning", ctIcon: "Ikon", ctColour: "Accent", ctNeedBook: "Audiobookshelf kan inte spara en tom samling, välj minst en bok. Du kan lägga till och ta bort fritt senare.", colEditIcon: "Byt ikon", ciTitle: "Samlingens ikon", ciReset: "Matcha namnet automatiskt", colEditDesc: "Redigera beskrivning", colBookForms: ["bok","böcker"], colForms: ["Samling","Samlingar"], ctTitle: "Skapa en samling", ctBlank: "Tom samling", ctNew: "Ny samling", ctNewHint: "från en mall", ctNamePh: "Samlingens namn", ctSearch: "Sök böcker eller författare…", ctCreate: "Skapa", ctBack: "‹ Tillbaka", ctAddAll: "Lägg till alla böcker", abTitle: "Lägg till böcker", abAdd: "Lägg till", avRemove: "Ta bort foto", avSet: "Välj foto…", ugListened: "Lyssnat", ugPhoto: "Profilfoto", ugNever: "Aldrig", ugSeen: "Senast aktiv", ugEdit: "Redigera användare", ugDelete: "Ta bort användare", ugAdd: "Lägg till foto", ugChange: "Byt foto", ugSortName: "A–Z", ugSortTime: "Mest lyssnat", ugSortSeen: "Nyligen aktiva", bsCard: "Boklänkar", bsHint: "Knappar på boksidan för att slå upp en titel någon annanstans. Goodreads och ditt språks största sajt är på från början.", bsFind: "Sök på", bsGlobal: "Överallt", bsLocal: "För ditt språk", bsOther: "Andra länder", pgCard: "Omgjorda sidor", pgHint: "Var och en ersätter en Audiobookshelf-sida med NanoHive-versionen. Stäng av för att få tillbaka originalet.", pgNarrators: "Uppläsarkort", pgCollections: "Samlingssidor", pgUsers: "Användarkort (inställningar)", pgStats: "Lyssningsranking", pgPhoto: "Profilfoto i toppfältet", pgCinematic: "Biografbakgrund", pgTransitions: "Sidövergångar", pSearch: "Sök inställningar…", pSearchNone: "Inget matchar.", erTypeface: "Typsnitt", erPage: "Sidtema", erText: "Textfärg", erBg: "Bakgrund", erDefault: "Standard (ABS)", erAuto: "Auto" , lfByAuthor: "Författare › serieordning", lfBySeries: "Serie › ordning" , lfSecFilters: "Fler filter", lfSecSort: "Multisortering", lfAuthor: "Författare", lfSeries: "Serie", lfTitle: "Titel", lfYear: "År", lfAdded: "Tillagd", lfDuration: "Längd", lfNarrator: "Uppläsare", lfGenre: "Genre", lfLanguage: "Språk", lfProgress: "Förlopp", lfPgFinished: "Klara", lfPgInProgress: "Pågående", lfPgNot: "Inte påbörjade" , logoUpload: 'Ladda upp från enheten…', logoUploaded: 'Uppladdad ✓', logoBadType: 'Bildtypen stöds inte', logoTooBig: 'Bilden är för stor (max 4 MB)', logoOr: 'eller', bookPage: 'Boksida', ratingsToggle: 'Gemenskapens betyg (stjärnor & recensioner)', ratingsHint: 'Stjärnbetyg med korta recensioner på varje boksida, delade med alla användare av denna server.', searchCard: 'Sök', gsToggle: 'Sök i alla bibliotek samtidigt', gsHint: 'En sammanslagen resultatlista över alla bibliotek du har åtkomst till, med en biblioteksbricka på varje träff. Stäng av för att få tillbaka menyn per bibliotek.', gsNoResults: 'Inga resultat', gsSearching: 'Söker…', gsBooks: 'Böcker', gsSeries: 'Serier', gsAuthors: 'Författare', spTitle: 'Startsida', spHint: 'Öppna alltid appen på en vald biblioteksvy (endast denna webbläsare). Direktlänkar omdirigeras aldrig.', spDefault: 'Appens standard', spViewHome: 'Hem', spViewLibrary: 'Bibliotek', spViewSeries: 'Serier', spViewCollections: 'Samlingar', spViewAuthors: 'Författare', scSet: 'Ange serieomslag', scRemove: 'Ta bort serieomslag', scEditDesc: 'Redigera seriebeskrivning', cardStars: 'Stjärnor på bokomslag', hoverPolish: 'Finputs vid hovring (mjukare dämpning, accentknapp, stjärnor vid hovring)', sectUser: 'Dina inställningar', sectAdmin: 'Administration', lockTitle: 'Funktionsbrytare', lockHint: 'Tvångsavaktivera en funktion för alla användare av denna server. Vinner över personliga inställningar; andra ser det efter nästa omläsning.', lockRatings: 'Stäng av betyg överallt', lockCardRatings: 'Stäng av stjärnor på omslag', lockGlobalSearch: 'Stäng av global sökning', lockHoverPolish: 'Stäng av hovringsfinputs', lockHeroCarousel: 'Stäng av startsidans karusell', lockedNote: 'avstängd av administratören', lfSort: 'Betyg', lfRated: 'Betygsatta', lfUnrated: 'Utan betyg', lfClear: 'Rensa', lfNone: 'Inga böcker matchar', lfSection: 'Betyg', lfWho: 'Vems betyg', lfEveryone: 'Alla', sdSave: 'Spara', sdClear: 'Återställ bokbeskrivningen', sdMore: 'Läs mer', sdLess: 'Visa mindre', shSeries: 'Serie', shBy: 'av ', shMore: ' & fler', shBookForms: ['bok', 'böcker'], shAvg: 'boksnitt', shRated: 'betygsatta', impLabel: 'Importera betyg', impHint: 'Hämta dina betyg från StoryGraph eller Goodreads. Exportera dina böcker där som CSV och välj filen här. Varje matchning visas för bekräftelse och inget sparas förrän du bekräftar.', impBtn: 'Välj CSV-fil…', impReading: 'Läser filen…', impMatching: 'Matchar mot dina bibliotek…', impErrUnknown: 'Det här ser inte ut som en export från StoryGraph eller Goodreads. Det ska vara deras CSV med en kolumn "Star Rating" eller "My Rating".', impErrEmpty: 'Filen har inga rader.', impErrNoRated: 'Det finns inga betygsatta böcker i filen.', impErrRead: 'Filen kunde inte läsas.', impErrLoad: 'Dina bibliotek kunde inte läsas in. Försök igen om en stund.', impErrNoLibs: 'Inget bibliotek deltar i betygen, så det finns inget att importera till.', impTitle: 'Importera betyg', impFrom: '%s-export: %d böcker, varav %d betygsatta', impLibs: 'Söker i: %s', impMatched: 'Matchade', impCheck: 'Värda en titt', impCheckHint: 'Nära men inte säkert. Kontrollera matchningen eller välj en annan.', impAlready: 'Redan betygsatta av dig', impAlreadyHint: 'Lämnas orörda om du inte väljer att skriva över.', impNone: 'Inte i ditt bibliotek', impNoneHint: 'Inget på denna server matchar dessa.', impAll: 'Alla', impSkip: 'Hoppa över denna', impDup: 'dubblett', impByTitle: 'titel', impYours: 'ditt: %s', impReviews: 'Importera även skrivna recensioner (%d)', impOver: 'Skriv över betyg jag redan har', impGo: 'Importera %d betyg', impCancel: 'Avbryt', impCloseBtn: 'Stäng', impWorking: 'Sparar… %d av %d', impDone: '%d betyg importerade.', impFail: '%d kunde inte sparas.', rdTitle: 'Vem läser den här boken', rdFin: 'Klar', socTitle: 'Sociala funktioner', socHint: 'Vad denna server delar mellan användare. Lyssningstiden driver rankningen; de andra två avslöjar vad folk läser, så de är avstängda tills du slår på dem. Gäller alla på en gång.', socTime: 'Lyssningstid (serverrankning)', socContent: 'Topptitlar i delad statistik', socWho: 'Vem läser (boksidor)', socUsers: 'Per användare', socUsersHint: 'Stänger du av någon delas inget av deras, oavsett vad de själva valt; bara du kan slå på det igen. "Avanmäld själv" betyder att personen själv sa nej.', socOpted: 'avanmäld själv', srvTitle: 'Serverstandarder', srvHint: 'Spara dina nuvarande inställningar som standard för alla användare av denna server. Alla kan ändå anpassa ovanpå. Montera en volym på /data/nh i temacontainern för att behålla dem mellan uppdateringar.', srvSave: 'Spara som serverstandard', srvClear: 'Rensa serverstandard', srvSaved: 'Sparat', srvCleared: 'Rensat', srvErr: 'Misslyckades: kräver administratörsinloggning', crMode: 'Fortsätt läsa-hylla', crCombine: 'Slå ihop med karusellen', crSeparate: 'Behåll som egen hylla', crHide: 'Dold', fsHint: 'Dela ditt lyssnande med de andra på denna server: tidssummor och minuter per dag för rankningen, och, om administratören slagit på det, även dina topptitlar och vilka böcker du läser eller har läst klart. På som standard. Stänger du av raderas allt du delat.' , amfHide: 'Dölj Markera som klar' , amfConfirm: 'Markera den här boken som klar?' , fsHint: 'Dela en sammanfattning av ditt lyssnande (totaler och minuter per dag) med de andra på servern, den driver serverrankingen. På som standard. Stänger du av försvinner du ur rankingen och dess totaler, och allt delat raderas.' , srToggle: 'Dela vilka böcker du läser', srHint: 'Lägger till vad andra ser: vilka böcker du läser, har läst klart och lyssnar mest på. Av som standard; administratörens serverbrytare måste också vara på.' , fsHint: 'Dela din lyssningstid (summor och minuter per dag) med de andra på denna server; den driver rankningen. På som standard. Stänger du av ser andra användare dig inte längre; serveradministratörer ser fortfarande aktiviteten, markerad som avanmäld.', srHint: 'Låter även andra se vilka böcker du läser, har läst klart och lyssnar mest på. Inget visas förrän administratören slår på motsvarande serverbrytare.', seshTitle: 'Senaste sessioner', seshNow: 'Lyssnar nu' , hcHint: 'Skicka dina betyg till ditt Hardcover-konto. Klistra in API-nyckeln från dina Hardcover-kontoinställningar; nycklar går ut efter ett år.', hcKeyLbl: 'API-nyckel', hcToggle: 'Synkronisera nya betyg automatiskt', hcPushAll: 'Skicka alla betyg', hcPushDiff: 'Skicka bara skillnader', hcDone: 'Klart: %d synkade, %d utan träff', hcNoKey: 'Klistra först in din Hardcover-API-nyckel' , hcMissHead: 'Hittades inte på Hardcover:' , hcHint: 'Skicka dina betyg till ditt Hardcover-konto. Klistra in API-nyckeln från dina Hardcover-kontoinställningar.' , tabStyle: 'Stil', tabMain: 'Startsida', tabBooks: 'Böcker', tabFeat: 'Funktioner' , hcImport: 'Importera från Hardcover', impMarkQ: 'Markera också de %d matchade böckerna som klara här?', impMarkDone: '%d markerade som klara.' , impHint: 'Hämta dina betyg från Goodreads, StoryGraph eller Hardcover. Välj den exporterade CSV-filen här; Hardcover importeras direkt via knappen på sitt kort. Varje träff visas för bekräftelse innan något sparas.' , impErrUnknown: 'Det ser inte ut som ett export från Goodreads, StoryGraph eller Hardcover: inga titel- och betygskolumner hittades.' , fsCard: 'Socialt', rnToggle: 'Visa mitt namn vid betyg', rsToggle: 'Dela mina betyg med andra', socNames: 'Namn vid betyg', rtAnon: 'Anonym' },
    no: { cmStopped: "stoppet", cmScanErrs: "{e} ikke sjekket (Goodreads-feil)", cmScanBusy: "En skanning kjører allerede", cmGrErr: "Goodreads utilgjengelig, prøver igjen", cmNoMatchYet: "Ingen treff på Goodreads", cmLooking: "Søker på Goodreads…", cmToolHint: "Hva Goodreads-lesere mener om denne boken. ? viser den matchede boken og lar deg endre den.", cmSearchPh: "Søk på Goodreads…", cmNoVotes: "ingen vurderinger ennå", cmRefresh: "Oppdater", cmScanAll: "Gå gjennom alt på nytt, ikke bare de som mangler", cmScanDone: "Ferdig: {m} funnet · {x} ikke funnet · {s} hadde allerede", cmStop: "Stopp", cmScanLibs: "Biblioteker å gå gjennom", cmScan: "Søk vurderinger", lsLongest: "Lengste bøker", lsSize: "Størrelse på disk", lsDuration: "Total lengde", lsGenresN: "Sjangre", lsSeriesN: "Serier", lsAuthorsN: "Forfattere", lsBooks: "Bøker", lsGlance: "Biblioteket i korte trekk", cmOpen: "Åpne på Goodreads", cmDetect: "Finn", cmAddr: "Hjelperens adresse", scmTitle: "Serieside", scmHint: "Slik ser det genererte omslaget over en serietittel ut når ingen egen er lastet opp (bare denne nettleseren).", scmDeck: "Lagdelt stabel", cmHint: "Hva Goodreads-lesere mener om en bok: snitt og antall stemmer, under stjernene på boksiden og som sortering og filter. Boksider fyller seg selv mens man blar.", cmGrOff: "Goodreads: ikke satt opp", cmSetup: "Ingen hjelper ennå? Kjør dette ved siden av temaet (compose-linjer under), og trykk Finn. Enhver annen måte å kjøre imaget på virker også: lim inn adressen over.", cmGrOn: "Goodreads: tilkoblet", cmGrDown: "Goodreads: satt opp, men svarer ikke", cmGrNoNum: "Goodreads: svarer, men denne abs-tract-utgaven gir ingen vurderingstall (bruk ratings-utgaven, se README)", cmTitle: "Goodreads-vurderinger", cmWrong: "Feil bok?", cmFind: "Finn fellesskapets vurdering…", cmNone: "Ingen treff", cmUse: "Bruk denne", lfCommunity: "Goodreads-vurdering", mfNatChange: "Endre…", lsByTime: "etter lyttetid", nrSearch: 'Filtrer fortellere…', nrSortName: 'Navn', nrSortBooks: 'Flest bøker', nrForms: ['Forteller', 'Fortellere'], nrBooksForms: ['bok', 'bøker'], auForms: ['Forfatter', 'Forfattere'], auSearch: 'Filtrer forfattere…' , sbTitle: "Serverrangering", sbWeek: "Uke", sbMonth: "Måned", sbYear: "År", sbAll: "Totalt", sbBooks: "Bøker", sbTopBooks: "Mest lyttet", sbTotal: "Totalt", sbListeners: "Lyttere", sbAvg: "Gjennomsnitt", wkTitle: "Lytteminutter, siste 7 dager", wkAvg: "Daglig snitt", wkRow: "Dager på rad", rpBadge: "Åpne problemrapporter", rpMenu: "Rapporter et problem", rpTitle: "Rapporter et problem", rpWhat: "Hva er galt?", rpNote: "Noe mer administratoren bør vite? (valgfritt)", rpSend: "Send rapport", rpSent: "Sendt. Takk.", rpFail: "Kunne ikke sende", rpMissing: "Manglende eller ufullstendig innhold", rpQuality: "Dårlig lydkvalitet", rpPlay: "Spiller ikke av", rpWrong: "Feil bok, omslag eller metadata", rpChapters: "Feil kapitler", rpOther: "Noe annet", rfRate: "Vurder", rfSheet: "Vurder denne boken", rfOpen: "Åpne boksiden", rfPickHint: "Velg en vurdering", rfTitle: "Vurder det du har hørt ferdig", ysFinished: "Nylig fullført", ysAlmost: "Nesten ferdig", ysMarkDone: "Merk som fullført", ysEditDate: "Endre sluttdato", ysThisWeek: "Denne uken", ysBestDay: "Beste dag", ysTitle: "Din lytting", fsTitle: "Serverrangering", fsCard: "Serverrangering", fsToggle: "Del lyttestatistikken min", fsJoin: "Slå på «Del lyttestatistikken min» i tilpasningspanelet for å vises i serverrangeringen.", fsWaiting: "Deling er på, sammendraget ditt blir snart med i serverrangeringen sammen med alle andre.", fsHint: "Del et sammendrag av lyttingen din (totaler og minutter per dag) med de andre på serveren, det driver serverrangeringen. På som standard. Slår du det av, forsvinner du fra rangeringen og totalene, og alt delt slettes.", yirBtn: "Året i tilbakeblikk", yirTitle: "Ditt lytteår", yirYear: "Siste 12 måneder", yirDays: "Dager med lytting", yirBest: "Beste dag", yirMonths: "Per måned", yirNone: "Ingen lyttedata ennå.", ssscmDeck: "Lagdelt stabel", scmFirst: "Første bokas omslag", scmGrid: "Omslagsrutenett (standard)", scmOff: "Skjult" , lsTitle: "Serverstatistikk", lsAll: "Alle bibliotek", lsPlayed: "Avspilte bøker", lsBest: "Best vurdert", lsGenres: "Toppsjangre", srvPick: "Hva skal lagres:", srvGRest: "Funksjoner og oppførsel", aeBtn: "Rydd i forfattere", aeTitle: "Forfattere uten bøker", aeNone: "Alle forfattere her har minst én bok.", aeRemove: "Fjern", aeDone: "Fjernet", aeWorking: "Fjerner…", rpCard: "Rapporterte problemer", rpHint: "Problemer brukere har rapportert fra en bokside. Løser du ett, forsvinner det fra listen.", rpEmpty: "Ingenting rapportert.", rpResolve: "Løs", rpOpen: "Åpne boka", apCard: "Avspilling", apToggle: "Spill automatisk neste bok i en serie", apHint: "Når en bok er ferdig, starter neste i samme serie automatisk. Bare bøker du alt har, og som ikke er fullført.", apNext: "Neste i serien", fdToggle: "Verktøy for fullførte bøker i statistikken", ysNoDate: "ingen dato", hmShowing: "grafen viser", ysStreak: "Rekke", ysCurrent: "Nåværende", ysLongest: "Lengste", ysDaysTotal: "Dager totalt", ysPace: "Tempo", ysPerDay: "Per lyttedag", ysWhen: "Når du lytter", ysTopBooks: "Mest lyttede bøker", ysTopAuthors: "Mest lyttede forfattere", ysTopNarrators: "Mest lyttede opplesere", rfToggle: "Seksjonen «Vurder» på forsiden", hoTitle: "Rekkefølge på forsiden", hoEmpty: "Åpne forsiden én gang og kom tilbake.", hoReset: "Tilbakestill rekkefølgen", ctIcon: "Ikon", ctColour: "Aksent", ctNeedBook: "Audiobookshelf kan ikke lagre en tom samling, velg minst én bok. Du kan legge til og fjerne fritt senere.", colEditIcon: "Bytt ikon", ciTitle: "Samlingens ikon", ciReset: "Tilpass navnet automatisk", colEditDesc: "Rediger beskrivelse", colBookForms: ["bok","bøker"], colForms: ["Samling","Samlinger"], ctTitle: "Opprett en samling", ctBlank: "Tom samling", ctNew: "Ny samling", ctNewHint: "fra en mal", ctNamePh: "Samlingens navn", ctSearch: "Søk bøker eller forfattere…", ctCreate: "Opprett", ctBack: "‹ Tilbake", ctAddAll: "Legg til alle bøker", abTitle: "Legg til bøker", abAdd: "Legg til", avRemove: "Fjern bilde", avSet: "Velg bilde…", ugListened: "Lyttet", ugPhoto: "Profilbilde", ugNever: "Aldri", ugSeen: "Sist aktiv", ugEdit: "Rediger bruker", ugDelete: "Slett bruker", ugAdd: "Legg til bilde", ugChange: "Bytt bilde", ugSortName: "A–Å", ugSortTime: "Mest lyttet", ugSortSeen: "Nylig aktive", bsCard: "Boklenker", bsHint: "Knapper på boksiden for å slå opp en tittel andre steder. Goodreads og språkets største side er på fra start.", bsFind: "Søk på", bsGlobal: "Overalt", bsLocal: "For språket ditt", bsOther: "Andre land", pgCard: "Omarbeidede sider", pgHint: "Hver av disse erstatter en Audiobookshelf-side med NanoHive-versjonen. Slå av for å få originalen tilbake.", pgNarrators: "Oppleserkort", pgCollections: "Samlingssider", pgUsers: "Brukerkort (innstillinger)", pgStats: "Lytterangering", pgPhoto: "Profilbilde i topplinja", pgCinematic: "Kinobakgrunn", pgTransitions: "Sideoverganger", pSearch: "Søk i innstillinger…", pSearchNone: "Ingen treff.", erTypeface: "Skrifttype", erPage: "Sidetema", erText: "Tekstfarge", erBg: "Bakgrunn", erDefault: "Standard (ABS)", erAuto: "Auto" , lfByAuthor: "Forfatter › serierekkefølge", lfBySeries: "Serie › rekkefølge" , lfSecFilters: "Flere filtre", lfSecSort: "Multisortering", lfAuthor: "Forfatter", lfSeries: "Serie", lfTitle: "Tittel", lfYear: "År", lfAdded: "Lagt til", lfDuration: "Varighet", lfNarrator: "Oppleser", lfGenre: "Sjanger", lfLanguage: "Språk", lfProgress: "Fremdrift", lfPgFinished: "Fullførte", lfPgInProgress: "Pågående", lfPgNot: "Ikke påbegynt" , logoUpload: 'Last opp fra enheten…', logoUploaded: 'Lastet opp ✓', logoBadType: 'Bildetypen støttes ikke', logoTooBig: 'Bildet er for stort (maks 4 MB)', logoOr: 'eller', bookPage: 'Bokside', ratingsToggle: 'Fellesskapets vurderinger (stjerner & anmeldelser)', ratingsHint: 'Stjernevurderinger med korte anmeldelser på hver bokside, delt med alle brukere av denne serveren.', searchCard: 'Søk', gsToggle: 'Søk i alle bibliotek samtidig', gsHint: 'Én samlet resultatliste på tvers av alle tilgjengelige bibliotek, med et biblioteksmerke på hvert treff. Slå av for å få tilbake menyen per bibliotek.', gsNoResults: 'Ingen resultater', gsSearching: 'Søker…', gsBooks: 'Bøker', gsSeries: 'Serier', gsAuthors: 'Forfattere', spTitle: 'Startside', spHint: 'Åpne alltid appen på en valgt biblioteksvisning (bare denne nettleseren). Direktelenker omdirigeres aldri.', spDefault: 'Appens standard', spViewHome: 'Hjem', spViewLibrary: 'Bibliotek', spViewSeries: 'Serier', spViewCollections: 'Samlinger', spViewAuthors: 'Forfattere', scSet: 'Angi serieomslag', scRemove: 'Fjern serieomslag', scEditDesc: 'Rediger seriebeskrivelse', cardStars: 'Stjerner på bokomslag', hoverPolish: 'Finpuss ved hover (mykere demping, aksentknapp, stjerner ved hover)', sectUser: 'Dine innstillinger', sectAdmin: 'Administrasjon', lockTitle: 'Funksjonsbrytere', lockHint: 'Tvangsdeaktiver en funksjon for alle brukere av denne serveren. Vinner over personlige innstillinger; andre ser det etter neste innlasting.', lockRatings: 'Slå av vurderinger overalt', lockCardRatings: 'Slå av stjerner på omslag', lockGlobalSearch: 'Slå av globalt søk', lockHoverPolish: 'Slå av hover-finpuss', lockHeroCarousel: 'Slå av forsidekarusell', lockedNote: 'slått av av administrator', lfSort: 'Vurdering', lfRated: 'Vurdert', lfUnrated: 'Ikke vurdert', lfClear: 'Tøm', lfNone: 'Ingen bøker passer', lfSection: 'Vurdering', lfWho: 'Hvem sine vurderinger', lfEveryone: 'Alle', sdSave: 'Lagre', sdClear: 'Gjenopprett bokbeskrivelsen', sdMore: 'Les mer', sdLess: 'Vis mindre', shSeries: 'Serie', shBy: 'av ', shMore: ' & flere', shBookForms: ['bok', 'bøker'], shAvg: 'boksnitt', shRated: 'vurdert', impLabel: 'Importer vurderinger', impHint: 'Hent vurderingene dine fra StoryGraph eller Goodreads. Eksporter bøkene dine der som CSV og velg filen her. Hvert treff vises for bekreftelse, og ingenting lagres før du bekrefter.', impBtn: 'Velg CSV-fil…', impReading: 'Leser filen…', impMatching: 'Matcher mot bibliotekene dine…', impErrUnknown: 'Dette ser ikke ut som en eksport fra StoryGraph eller Goodreads. Det skal være deres CSV med en kolonne "Star Rating" eller "My Rating".', impErrEmpty: 'Filen har ingen rader.', impErrNoRated: 'Det er ingen vurderte bøker i filen.', impErrRead: 'Filen kunne ikke leses.', impErrLoad: 'Bibliotekene dine kunne ikke lastes. Prøv igjen om litt.', impErrNoLibs: 'Ingen bibliotek deltar i vurderinger, så det er ingenting å importere til.', impTitle: 'Importer vurderinger', impFrom: '%s-eksport: %d bøker, %d av dem vurdert', impLibs: 'Søker i: %s', impMatched: 'Matchet', impCheck: 'Verdt en titt', impCheckHint: 'Nært, men ikke sikkert. Sjekk treffet, eller velg et annet.', impAlready: 'Allerede vurdert av deg', impAlreadyHint: 'Røres ikke, med mindre du velger å overskrive.', impNone: 'Ikke i biblioteket ditt', impNoneHint: 'Ingenting på denne serveren matcher disse.', impAll: 'Alle', impSkip: 'Hopp over denne', impDup: 'duplikat', impByTitle: 'tittel', impYours: 'din: %s', impReviews: 'Importer også skrevne anmeldelser (%d)', impOver: 'Overskriv vurderinger jeg allerede har', impGo: 'Importer %d vurderinger', impCancel: 'Avbryt', impCloseBtn: 'Lukk', impWorking: 'Lagrer… %d av %d', impDone: '%d vurderinger importert.', impFail: '%d kunne ikke lagres.', rdTitle: 'Hvem leser denne boka', rdFin: 'Ferdig', socTitle: 'Sosiale funksjoner', socHint: 'Hva denne serveren deler mellom brukere. Lyttetiden driver rangeringen; de to andre røper hva folk leser, så de er av til du slår dem på. Gjelder alle samtidig.', socTime: 'Lyttetid (serverrangering)', socContent: 'Topptitler i delt statistikk', socWho: 'Hvem leser (boksider)', socUsers: 'Per bruker', socUsersHint: 'Slår du av noen, deles ingenting av deres, uansett hva de selv valgte; bare du kan slå det på igjen. "Meldte seg av selv" betyr at personen selv sa nei.', socOpted: 'meldte seg av selv', srvTitle: 'Serverstandarder', srvHint: 'Lagre gjeldende innstillinger som standard for alle brukere av denne serveren. Alle kan fortsatt tilpasse oppå. Monter et volum på /data/nh i tema-containeren for å beholde dem gjennom oppdateringer.', srvSave: 'Lagre som serverstandard', srvClear: 'Tøm serverstandard', srvSaved: 'Lagret', srvCleared: 'Tømt', srvErr: 'Mislyktes: krever administratorpålogging', crMode: 'Les videre-hylle', crCombine: 'Slå sammen med karusellen', crSeparate: 'Behold som egen hylle', crHide: 'Skjult', fsHint: 'Del lyttingen din med de andre på denne serveren: tidssummer og minutter per dag til rangeringen, og, hvis administratoren har slått det på, også topptitlene dine og hvilke bøker du leser eller har fullført. På som standard. Slår du av, slettes alt du har delt.' , amfHide: 'Skjul Merk som ferdig' , amfConfirm: 'Merke denne boka som ferdig?' , fsHint: 'Del et sammendrag av lyttingen din (totaler og minutter per dag) med de andre på serveren, det driver serverrangeringen. På som standard. Slår du det av, forsvinner du fra rangeringen og totalene, og alt delt slettes.' , srToggle: 'Del hvilke bøker du leser', srHint: 'Legger til det andre ser hvilke bøker du leser, har fullført og lytter mest til. Av som standard; administratorens serverbrytere må også være på.' , fsHint: 'Del lyttetiden din (summer og minutter per dag) med de andre på denne serveren; den driver rangeringen. På som standard. Slår du av, ser ikke andre brukere deg lenger; serveradministratorer ser fortsatt aktiviteten, merket som avmeldt.', srHint: 'Lar også andre se hvilke bøker du leser, har fullført og lytter mest til. Ingenting vises før administratoren slår på de tilhørende serverbryterne.', seshTitle: 'Siste økter', seshNow: 'Lytter nå' , hcHint: 'Send vurderingene dine til Hardcover-kontoen din. Lim inn API-nøkkelen fra kontoinnstillingene på Hardcover; nøkler utløper etter ett år.', hcKeyLbl: 'API-nøkkel', hcToggle: 'Synkroniser nye vurderinger automatisk', hcPushAll: 'Send alle vurderinger', hcPushDiff: 'Send bare forskjeller', hcDone: 'Ferdig: %d synkronisert, %d uten treff', hcNoKey: 'Lim først inn Hardcover-API-nøkkelen din' , hcMissHead: 'Ikke funnet på Hardcover:' , hcHint: 'Send vurderingene dine til Hardcover-kontoen din. Lim inn API-nøkkelen fra kontoinnstillingene på Hardcover.' , tabStyle: 'Stil', tabMain: 'Forside', tabBooks: 'Bøker', tabFeat: 'Funksjoner' , hcImport: 'Importer fra Hardcover', impMarkQ: 'Merke de %d matchede bøkene som fullført her også?', impMarkDone: '%d merket som fullført.' , impHint: 'Hent vurderingene dine fra Goodreads, StoryGraph eller Hardcover. Velg den eksporterte CSV-filen her; Hardcover importeres direkte via knappen på kortet sitt. Hvert treff vises til bekreftelse før noe lagres.' , impErrUnknown: 'Dette ser ikke ut som en eksport fra Goodreads, StoryGraph eller Hardcover: fant ingen tittel- og vurderingskolonner.' , fsCard: 'Sosialt', rnToggle: 'Vis navnet mitt ved vurderinger', rsToggle: 'Del vurderingene mine med andre', socNames: 'Navn ved vurderinger', rtAnon: 'Anonym' },
    fi: { cmStopped: "pysäytetty", cmScanErrs: "{e} tarkistamatta (Goodreads-virheitä)", cmScanBusy: "Läpikäynti on jo käynnissä", cmGrErr: "Goodreads ei vastaa, yritetään uudelleen", cmNoMatchYet: "Ei osumaa Goodreadsissa", cmLooking: "Haetaan Goodreadsista…", cmToolHint: "Mitä Goodreads-lukijat ajattelevat tästä kirjasta. ? näyttää yhdistetyn kirjan ja antaa vaihtaa sen.", cmSearchPh: "Hae Goodreadsista…", cmNoVotes: "ei vielä arvioita", cmRefresh: "Päivitä", cmScanAll: "Käy kaikki läpi uudelleen, ei vain puuttuvat", cmScanDone: "Valmis: {m} löytyi · {x} ei löytynyt · {s} oli jo", cmStop: "Pysäytä", cmScanLibs: "Läpikäytävät kirjastot", cmScan: "Hae arviot", lsLongest: "Pisimmät kirjat", lsSize: "Koko levyllä", lsDuration: "Kokonaiskesto", lsGenresN: "Lajityypit", lsSeriesN: "Sarjat", lsAuthorsN: "Kirjailijat", lsBooks: "Kirjat", lsGlance: "Kirjasto yhdellä silmäyksellä", cmOpen: "Avaa Goodreadsissa", cmDetect: "Etsi", cmAddr: "Apurin osoite", scmTitle: "Sarjan sivu", scmHint: "Miltä sarjan otsikon yllä oleva generoitu kansi näyttää, kun omaa ei ole ladattu (vain tämä selain).", scmDeck: "Kerrostettu pakka", cmHint: "Mitä Goodreads-lukijat ajattelevat kirjasta: keskiarvo ja äänten määrä, kirjan sivun tähtien alla sekä lajitteluna ja suodattimena. Kirjojen sivut täydentyvät itsestään selatessa.", cmGrOff: "Goodreads: ei käytössä", cmSetup: "Ei vielä apuria? Aja tämä teeman rinnalla (compose-rivit alla) ja paina Etsi. Mikä tahansa muu tapa ajaa image käy myös: liitä sen osoite yllä.", cmGrOn: "Goodreads: yhdistetty", cmGrDown: "Goodreads: käytössä, mutta ei vastaa", cmGrNoNum: "Goodreads: vastaa, mutta tämä abs-tract-versio ei anna arviolukuja (käytä ratings-versiota, ks. README)", cmTitle: "Goodreads-arviot", cmWrong: "Väärä kirja?", cmFind: "Etsi yhteisön arvio…", cmNone: "Ei osumaa", cmUse: "Käytä tätä", lfCommunity: "Goodreads-arvio", mfNatChange: "Vaihda…", lsByTime: "kuunteluajan mukaan", nrSearch: 'Suodata lukijoita…', nrSortName: 'Nimi', nrSortBooks: 'Eniten kirjoja', nrForms: ['Lukija', 'Lukijaa'], nrBooksForms: ['kirja', 'kirjaa'], auForms: ['Kirjailija', 'Kirjailijaa'], auSearch: 'Suodata kirjailijoita…' , sbTitle: "Palvelimen sijoitukset", sbWeek: "Viikko", sbMonth: "Kuukausi", sbYear: "Vuosi", sbAll: "Kaikki aika", sbBooks: "Kirjat", sbTopBooks: "Kuunnelluimmat", sbTotal: "Yhteensä", sbListeners: "Kuuntelijat", sbAvg: "Keskiarvo", wkTitle: "Kuunteluminuutit, viimeiset 7 päivää", wkAvg: "Päiväkeskiarvo", wkRow: "Päivää putkeen", rpBadge: "Avoimia ongelmailmoituksia", rpMenu: "Ilmoita ongelmasta", rpTitle: "Ilmoita ongelmasta", rpWhat: "Mikä on vialla?", rpNote: "Muuta kerrottavaa ylläpitäjälle? (valinnainen)", rpSend: "Lähetä ilmoitus", rpSent: "Lähetetty. Kiitos.", rpFail: "Lähetys epäonnistui", rpMissing: "Puuttuva tai keskeneräinen sisältö", rpQuality: "Huono äänenlaatu", rpPlay: "Ei toistu", rpWrong: "Väärä kirja, kansi tai metatiedot", rpChapters: "Väärät luvut", rpOther: "Jotain muuta", rfRate: "Arvioi", rfSheet: "Arvioi tämä kirja", rfOpen: "Avaa kirjan sivu", rfPickHint: "Valitse arvio", rfTitle: "Arvioi loppuun kuuntelemasi", ysFinished: "Äskettäin valmistuneet", ysAlmost: "Melkein valmiit", ysMarkDone: "Merkitse valmiiksi", ysEditDate: "Muuta valmistumispäivää", ysThisWeek: "Tällä viikolla", ysBestDay: "Paras päivä", ysTitle: "Kuuntelusi", fsTitle: "Palvelimen sijoitukset", fsCard: "Palvelimen sijoitukset", fsToggle: "Jaa kuuntelutilastoni", fsJoin: "Ota ”Jaa kuuntelutilastoni” käyttöön mukautuspaneelissa, niin näyt palvelimen sijoituksissa.", fsWaiting: "Jakaminen on päällä, yhteenvetosi liittyy pian palvelimen sijoituksiin muiden mukana.", fsHint: "Jaa yhteenveto kuuntelustasi (summat ja minuutit päivässä) muiden tämän palvelimen käyttäjien kanssa, se tuottaa palvelimen sijoitukset. Oletuksena päällä. Pois kytkettäessä poistut sijoituksista ja summista, ja jaetut tiedot poistetaan.", yirBtn: "Vuosi tiivistettynä", yirTitle: "Kuunteluvuotesi", yirYear: "Viimeiset 12 kuukautta", yirDays: "Kuuntelupäivät", yirBest: "Paras päivä", yirMonths: "Kuukausittain", yirNone: "Ei vielä kuuntelutietoja.", ssscmDeck: "Kerrostettu pakka", scmFirst: "Ensimmäisen kirjan kansi", scmGrid: "Kansiruudukko (oletus)", scmOff: "Piilotettu" , lsTitle: "Palvelimen tilastot", lsAll: "Kaikki kirjastot", lsPlayed: "Toistetut kirjat", lsBest: "Parhaiten arvioidut", lsGenres: "Suosituimmat lajityypit", srvPick: "Mitä tallennetaan:", srvGRest: "Toiminnot ja käyttäytyminen", aeBtn: "Siivoa kirjailijat", aeTitle: "Kirjailijat ilman kirjoja", aeNone: "Jokaisella kirjailijalla on täällä vähintään yksi kirja.", aeRemove: "Poista", aeDone: "Poistettu", aeWorking: "Poistetaan…", rpCard: "Ilmoitetut ongelmat", rpHint: "Käyttäjien kirjasivulta ilmoittamat ongelmat. Ratkaiseminen poistaa ilmoituksen listalta.", rpEmpty: "Ei ilmoituksia.", rpResolve: "Ratkaise", rpOpen: "Avaa kirja", apCard: "Toisto", apToggle: "Toista sarjan seuraava kirja automaattisesti", apHint: "Kun kirja loppuu, saman sarjan seuraava alkaa automaattisesti. Vain kirjastossasi jo olevat, kesken olevat kirjat.", apNext: "Sarjan seuraava", fdToggle: "Valmiiden kirjojen työkalut tilastoissasi", ysNoDate: "ei päivämäärää", hmShowing: "kaavio näyttää", ysStreak: "Putki", ysCurrent: "Nykyinen", ysLongest: "Pisin", ysDaysTotal: "Päiviä yhteensä", ysPace: "Tahti", ysPerDay: "Kuuntelupäivää kohden", ysWhen: "Milloin kuuntelet", ysTopBooks: "Kuunnelluimmat kirjat", ysTopAuthors: "Kuunnelluimmat kirjailijat", ysTopNarrators: "Kuunnelluimmat lukijat", rfToggle: "Arvioi-osio etusivulla", hoTitle: "Etusivun osioiden järjestys", hoEmpty: "Avaa etusivu kerran ja palaa tänne.", hoReset: "Palauta oletusjärjestys", ctIcon: "Kuvake", ctColour: "Korostus", ctNeedBook: "Audiobookshelf ei voi tallentaa tyhjää kokoelmaa, valitse vähintään yksi kirja. Voit lisätä ja poistaa vapaasti myöhemmin.", colEditIcon: "Vaihda kuvake", ciTitle: "Kokoelman kuvake", ciReset: "Sovita nimeen automaattisesti", colEditDesc: "Muokkaa kuvausta", colBookForms: ["kirja","kirjaa"], colForms: ["Kokoelma","Kokoelmat"], ctTitle: "Luo kokoelma", ctBlank: "Tyhjä kokoelma", ctNew: "Uusi kokoelma", ctNewHint: "mallista", ctNamePh: "Kokoelman nimi", ctSearch: "Hae kirjoja tai kirjailijoita…", ctCreate: "Luo", ctBack: "‹ Takaisin", ctAddAll: "Lisää kaikki kirjat", abTitle: "Lisää kirjoja", abAdd: "Lisää", avRemove: "Poista kuva", avSet: "Valitse kuva…", ugListened: "Kuunneltu", ugPhoto: "Profiilikuva", ugNever: "Ei koskaan", ugSeen: "Viimeksi aktiivinen", ugEdit: "Muokkaa käyttäjää", ugDelete: "Poista käyttäjä", ugAdd: "Lisää kuva", ugChange: "Vaihda kuva", ugSortName: "A–Ö", ugSortTime: "Kuunnelluimmat", ugSortSeen: "Äskettäin aktiiviset", bsCard: "Kirjalinkit", bsHint: "Kirjasivun painikkeet nimikkeen hakemiseen muualta. Goodreads ja kielesi suurin sivusto ovat valmiiksi päällä.", bsFind: "Hae palvelusta", bsGlobal: "Kaikkialla", bsLocal: "Kielellesi", bsOther: "Muut maat", pgCard: "Uudistetut sivut", pgHint: "Jokainen näistä korvaa Audiobookshelf-sivun NanoHive-versiolla. Poista käytöstä palataksesi alkuperäiseen.", pgNarrators: "Lukijakortit", pgCollections: "Kokoelmasivut", pgUsers: "Käyttäjäkortit (asetukset)", pgStats: "Kuuntelusijoitukset", pgPhoto: "Profiilikuva yläpalkissa", pgCinematic: "Elokuvamainen tausta", pgTransitions: "Sivusiirtymät", pSearch: "Hae asetuksia…", pSearchNone: "Ei osumia.", erTypeface: "Kirjasin", erPage: "Sivun teema", erText: "Tekstin väri", erBg: "Tausta", erDefault: "Oletus (ABS)", erAuto: "Automaattinen" , lfByAuthor: "Kirjailija › sarjajärjestys", lfBySeries: "Sarja › järjestys" , lfSecFilters: "Lisää suodattimia", lfSecSort: "Monitasolajittelu", lfAuthor: "Kirjailija", lfSeries: "Sarja", lfTitle: "Nimi", lfYear: "Vuosi", lfAdded: "Lisätty", lfDuration: "Kesto", lfNarrator: "Lukija", lfGenre: "Lajityyppi", lfLanguage: "Kieli", lfProgress: "Edistyminen", lfPgFinished: "Valmiit", lfPgInProgress: "Kesken", lfPgNot: "Aloittamattomat" , logoUpload: 'Lataa laitteelta…', logoUploaded: 'Ladattu ✓', logoBadType: 'Kuvatyyppiä ei tueta', logoTooBig: 'Kuva on liian suuri (enint. 4 Mt)', logoOr: 'tai', bookPage: 'Kirjan sivu', ratingsToggle: 'Yhteisön arviot (tähdet ja arvostelut)', ratingsHint: 'Tähtiarviot lyhyine arvosteluineen jokaisella kirjan sivulla, jaettuna kaikille tämän palvelimen käyttäjille.', searchCard: 'Haku', gsToggle: 'Hae kaikista kirjastoista kerralla', gsHint: 'Yksi yhdistetty tuloslista kaikista käytettävissä olevista kirjastoista, kirjastomerkki jokaisessa osumassa. Poista käytöstä palataksesi kirjastokohtaiseen valikkoon.', gsNoResults: 'Ei tuloksia', gsSearching: 'Haetaan…', gsBooks: 'Kirjat', gsSeries: 'Sarjat', gsAuthors: 'Kirjailijat', spTitle: 'Aloitussivu', spHint: 'Avaa sovellus aina valittuun kirjastonäkymään (vain tämä selain). Suoria linkkejä ei koskaan uudelleenohjata.', spDefault: 'Sovelluksen oletus', spViewHome: 'Koti', spViewLibrary: 'Kirjasto', spViewSeries: 'Sarjat', spViewCollections: 'Kokoelmat', spViewAuthors: 'Kirjailijat', scSet: 'Aseta sarjan kansi', scRemove: 'Poista sarjan kansi', scEditDesc: 'Muokkaa sarjan kuvausta', cardStars: 'Tähdet kirjojen kansissa', hoverPolish: 'Korttien viimeistely osoitettaessa (pehmeämpi himmennys, korostettu toisto, tähdet osoitettaessa)', sectUser: 'Omat asetukset', sectAdmin: 'Hallinta', lockTitle: 'Ominaisuuskytkimet', lockHint: 'Poistaa ominaisuuden pakolla käytöstä kaikilta tämän palvelimen käyttäjiltä. Voittaa henkilökohtaiset asetukset; muut näkevät sen seuraavan latauksen jälkeen.', lockRatings: 'Poista arviot käytöstä kaikkialta', lockCardRatings: 'Poista tähdet kansista', lockGlobalSearch: 'Poista yleishaku käytöstä', lockHoverPolish: 'Poista korttien viimeistely', lockHeroCarousel: 'Poista etusivun karuselli', lockedNote: 'ylläpitäjän poistama', lfSort: 'Arvio', lfRated: 'Arvioidut', lfUnrated: 'Arvioimattomat', lfClear: 'Tyhjennä', lfNone: 'Yksikään kirja ei täsmää', lfSection: 'Arvio', lfWho: 'Kenen arviot', lfEveryone: 'Kaikki', sdSave: 'Tallenna', sdClear: 'Palauta kirjan kuvaus', sdMore: 'Lue lisää', sdLess: 'Näytä vähemmän', shSeries: 'Sarja', shBy: '', shMore: ' & muita', shBookForms: ['kirja', 'kirjaa'], shAvg: 'kirjojen keskiarvo', shRated: 'arvioitu', impLabel: 'Tuo arviot', impHint: 'Tuo arviosi StoryGraphista tai Goodreadsista. Vie kirjasi siellä CSV-muotoon ja valitse tiedosto tästä. Jokainen osuma näytetään vahvistettavaksi, eikä mitään tallenneta ennen vahvistusta.', impBtn: 'Valitse CSV-tiedosto…', impReading: 'Luetaan tiedostoa…', impMatching: 'Verrataan kirjastoihisi…', impErrUnknown: 'Tämä ei näytä StoryGraphin tai Goodreadsin viennistä. Sen pitäisi olla niiden CSV, jossa on sarake "Star Rating" tai "My Rating".', impErrEmpty: 'Tiedostossa ei ole rivejä.', impErrNoRated: 'Tiedostossa ei ole arvioituja kirjoja.', impErrRead: 'Tiedostoa ei voitu lukea.', impErrLoad: 'Kirjastojasi ei voitu ladata. Yritä hetken kuluttua uudelleen.', impErrNoLibs: 'Mikään kirjasto ei osallistu arvioihin, joten tuotavaa ei ole.', impTitle: 'Tuo arviot', impFrom: '%s-vienti: %d kirjaa, joista %d arvioitu', impLibs: 'Haetaan: %s', impMatched: 'Täsmätyt', impCheck: 'Tarkistamisen arvoiset', impCheckHint: 'Lähellä mutta epävarma. Tarkista osuma tai valitse toinen.', impAlready: 'Jo arvioimasi', impAlreadyHint: 'Jätetään ennalleen, ellet valitse ylikirjoitusta.', impNone: 'Ei kirjastossasi', impNoneHint: 'Mikään tällä palvelimella ei vastaa näitä.', impAll: 'Kaikki', impSkip: 'Ohita tämä', impDup: 'kaksoiskappale', impByTitle: 'nimi', impYours: 'omasi: %s', impReviews: 'Tuo myös kirjoitetut arvostelut (%d)', impOver: 'Ylikirjoita jo olemassa olevat arvioni', impGo: 'Tuo %d arviota', impCancel: 'Peruuta', impCloseBtn: 'Sulje', impWorking: 'Tallennetaan… %d/%d', impDone: '%d arviota tuotu.', impFail: '%d ei voitu tallentaa.', rdTitle: 'Kuka lukee tätä kirjaa', rdFin: 'Valmis', socTitle: 'Sosiaaliset toiminnot', socHint: 'Mitä tämä palvelin jakaa käyttäjien kesken. Kuunteluaika pyörittää sijoituslistaa; kaksi muuta paljastavat, mitä kukin lukee, joten ne ovat pois päältä, kunnes kytket ne. Koskee kaikkia kerralla.', socTime: 'Kuunteluaika (palvelimen sijoituslista)', socContent: 'Kuunnelluimmat nimikkeet jaetuissa tilastoissa', socWho: 'Kuka lukee (kirjojen sivut)', socUsers: 'Käyttäjittäin', socUsersHint: 'Kun kytket jonkun pois, mitään hänen tiedoistaan ei jaeta riippumatta hänen omasta valinnastaan; vain sinä voit palauttaa sen. "Kieltäytyi itse" tarkoittaa, että henkilö sanoi itse ei.', socOpted: 'kieltäytyi itse', srvTitle: 'Palvelimen oletukset', srvHint: 'Tallenna nykyiset asetuksesi oletuksiksi kaikille tämän palvelimen käyttäjille. Jokainen voi silti muokata omiaan päälle. Liitä taltio polkuun /data/nh teemakontissa, jotta ne säilyvät päivityksissä.', srvSave: 'Tallenna palvelimen oletuksiksi', srvClear: 'Tyhjennä palvelimen oletukset', srvSaved: 'Tallennettu', srvCleared: 'Tyhjennetty', srvErr: 'Epäonnistui: vaatii ylläpitäjän kirjautumisen', crMode: 'Jatka lukemista -hylly', crCombine: 'Yhdistä karuselliin', crSeparate: 'Pidä erillisenä hyllynä', crHide: 'Piilotettu', fsHint: 'Jaa kuuntelusi muiden tämän palvelimen käyttäjien kanssa: aikasummat ja minuutit päivittäin sijoituslistaa varten, ja jos ylläpitäjä on kytkenyt ne, myös kuunnelluimmat nimikkeesi ja mitä kirjoja luet tai olet saanut valmiiksi. Oletuksena päällä. Kun kytket pois, kaikki jakamasi poistetaan.' , amfHide: 'Piilota Merkitse valmiiksi' , amfConfirm: 'Merkitäänkö tämä kirja valmiiksi?' , fsHint: 'Jaa yhteenveto kuuntelustasi (summat ja minuutit päivässä) muiden tämän palvelimen käyttäjien kanssa, se tuottaa palvelimen sijoitukset. Oletuksena päällä. Pois kytkettäessä poistut sijoituksista ja summista, ja jaetut tiedot poistetaan.' , srToggle: 'Jaa, mitä kirjoja luet', srHint: 'Lisää muiden näkemään, mitä kirjoja parhaillaan luet, mitkä olet saanut valmiiksi ja mitä kuuntelet eniten. Oletuksena pois; lisäksi ylläpitäjän palvelinkytkinten on oltava päällä.' , fsHint: 'Jaa kuunteluaikasi (summat ja minuutit päivittäin) muiden tämän palvelimen käyttäjien kanssa; se pyörittää sijoituslistaa. Oletuksena päällä. Kun kytket pois, muut käyttäjät eivät enää näe sinua; ylläpitäjät näkevät toiminnan yhä, merkittynä kieltäytyneeksi.', srHint: 'Antaa muiden nähdä myös, mitä kirjoja luet, mitkä olet saanut valmiiksi ja mitä kuuntelet eniten. Mitään ei näy, ennen kuin ylläpitäjä kytkee vastaavat palvelinkytkimet päälle.', seshTitle: 'Viimeisimmät istunnot', seshNow: 'Kuuntelevat nyt' , hcHint: 'Lähetä arviosi Hardcover-tilillesi. Liitä API-avain Hardcover-tilisi asetuksista; avaimet vanhenevat vuoden kuluttua.', hcKeyLbl: 'API-avain', hcToggle: 'Synkronoi uudet arviot automaattisesti', hcPushAll: 'Lähetä kaikki arviot', hcPushDiff: 'Lähetä vain erot', hcDone: 'Valmis: %d synkronoitu, %d ilman osumaa', hcNoKey: 'Liitä ensin Hardcover-API-avaimesi' , hcMissHead: 'Ei löytynyt Hardcoverista:' , hcHint: 'Lähetä arviosi Hardcover-tilillesi. Liitä API-avain Hardcover-tilisi asetuksista.' , tabStyle: 'Tyyli', tabMain: 'Etusivu', tabBooks: 'Kirjat', tabFeat: 'Toiminnot' , hcImport: 'Tuo Hardcoverista', impMarkQ: 'Merkitäänkö myös %d täsmännyttä kirjaa valmiiksi täällä?', impMarkDone: '%d merkitty valmiiksi.' , impHint: 'Tuo arviosi Goodreadsista, StoryGraphista tai Hardcoverista. Valitse viety CSV tässä; Hardcover tuodaan suoraan sen kortin painikkeella. Jokainen osuma näytetään vahvistettavaksi ennen tallennusta.' , impErrUnknown: 'Tämä ei näytä Goodreadsin, StoryGraphin tai Hardcoverin viennistä: otsikko- ja arviosarakkeita ei löytynyt.' , fsCard: 'Sosiaalinen', rnToggle: 'Näytä nimeni arvioiden yhteydessä', rsToggle: 'Jaa arvioni muiden kanssa', socNames: 'Nimet arvioiden yhteydessä', rtAnon: 'Anonyymi' },
    ru: { cmStopped: "остановлено", cmScanErrs: "{e} не проверено (ошибки Goodreads)", cmScanBusy: "Сканирование уже идёт", cmGrErr: "Goodreads недоступен, попробую снова", cmNoMatchYet: "На Goodreads не найдено", cmLooking: "Ищу на Goodreads…", cmToolHint: "Что думают об этой книге читатели Goodreads. Знак ? показывает сопоставленную книгу и позволяет её изменить.", cmSearchPh: "Искать на Goodreads…", cmNoVotes: "пока нет оценок", cmRefresh: "Обновить", cmScanAll: "Проверить всё заново, а не только недостающие", cmScanDone: "Готово: {m} найдено · {x} не найдено · {s} уже было", cmStop: "Остановить", cmScanLibs: "Библиотеки для сканирования", cmScan: "Найти оценки", lsLongest: "Самые длинные книги", lsSize: "Размер на диске", lsDuration: "Общая длительность", lsGenresN: "Жанры", lsSeriesN: "Серии", lsAuthorsN: "Авторы", lsBooks: "Книги", lsGlance: "Библиотека в цифрах", cmOpen: "Открыть на Goodreads", cmDetect: "Найти", cmAddr: "Адрес помощника", scmTitle: "Страница серии", scmHint: "Как выглядит сгенерированная обложка над названием серии, если своя не загружена (только этот браузер).", scmDeck: "Слоёная колода", cmHint: "Что думают о книге читатели Goodreads: средняя оценка и число голосов, под звёздами на странице книги, а также сортировка и фильтр. Страницы книг заполняются сами при просмотре.", cmGrOff: "Goodreads: не настроено", cmSetup: "Помощника ещё нет? Запустите это рядом с темой (строки compose ниже) и нажмите Найти. Подойдёт и любой другой способ запуска образа: вставьте его адрес выше.", cmGrOn: "Goodreads: подключено", cmGrDown: "Goodreads: настроено, но не отвечает", cmGrNoNum: "Goodreads: отвечает, но эта сборка abs-tract не даёт оценок (используйте сборку с оценками, см. README)", cmTitle: "Оценки Goodreads", cmWrong: "Не та книга?", cmFind: "Найти оценку сообщества…", cmNone: "Нет совпадения", cmUse: "Использовать", lfCommunity: "Оценка Goodreads", mfNatChange: "Изменить…", lsByTime: "по времени прослушивания", nrSearch: 'Фильтр чтецов…', nrSortName: 'Имя', nrSortBooks: 'Больше книг', nrForms: ['Чтец', 'Чтеца', 'Чтецов'], nrBooksForms: ['книга', 'книги', 'книг'], auForms: ['Автор', 'Автора', 'Авторов'], auSearch: 'Фильтр авторов…' , sbTitle: "Рейтинг сервера", sbWeek: "Неделя", sbMonth: "Месяц", sbYear: "Год", sbAll: "За всё время", sbBooks: "Книги", sbTopBooks: "Самые прослушиваемые", sbTotal: "Всего", sbListeners: "Слушатели", sbAvg: "В среднем", wkTitle: "Минуты прослушивания, последние 7 дней", wkAvg: "В среднем за день", wkRow: "Дней подряд", rpBadge: "Открытые сообщения о проблемах", rpMenu: "Сообщить о проблеме", rpTitle: "Сообщить о проблеме", rpWhat: "Что не так?", rpNote: "Что-то ещё для администратора? (необязательно)", rpSend: "Отправить", rpSent: "Отправлено. Спасибо.", rpFail: "Не удалось отправить", rpMissing: "Отсутствует или неполное содержимое", rpQuality: "Плохое качество звука", rpPlay: "Не воспроизводится", rpWrong: "Не та книга, обложка или метаданные", rpChapters: "Неверные главы", rpOther: "Другое", rfRate: "Оценить", rfSheet: "Оцените эту книгу", rfOpen: "Открыть страницу книги", rfPickHint: "Выберите оценку", rfTitle: "Оцените прослушанное", ysFinished: "Недавно закончены", ysAlmost: "Почти закончены", ysMarkDone: "Отметить как законченную", ysEditDate: "Изменить дату окончания", ysThisWeek: "На этой неделе", ysBestDay: "Лучший день", ysTitle: "Ваше прослушивание", fsTitle: "Рейтинг сервера", fsCard: "Рейтинг сервера", fsToggle: "Делиться моей статистикой прослушивания", fsJoin: "Включите «Делиться моей статистикой прослушивания» в панели настройки, чтобы появиться в рейтинге сервера.", fsWaiting: "Обмен включён, ваша сводка скоро появится в рейтинге сервера вместе с остальными.", fsHint: "Делитесь сводкой прослушивания (итоги и минуты за день) с другими на этом сервере, она питает рейтинг сервера. Включено по умолчанию. При отключении вы исчезаете из рейтинга и его итогов, а всё переданное удаляется.", yirBtn: "Итоги года", yirTitle: "Ваш год прослушивания", yirYear: "Последние 12 месяцев", yirDays: "Дни с прослушиванием", yirBest: "Лучший день", yirMonths: "По месяцам", yirNone: "Данных о прослушивании пока нет.", ssscmDeck: "Слоёная колода", scmFirst: "Обложка первой книги", scmGrid: "Сетка обложек (по умолчанию)", scmOff: "Скрыта" , lsTitle: "Статистика сервера", lsAll: "Все библиотеки", lsPlayed: "Прослушанные книги", lsBest: "С лучшими оценками", lsGenres: "Популярные жанры", srvPick: "Что сохранить:", srvGRest: "Функции и поведение", aeBtn: "Прибрать авторов", aeTitle: "Авторы без книг", aeNone: "У каждого автора здесь есть хотя бы одна книга.", aeRemove: "Удалить", aeDone: "Удалено", aeWorking: "Удаление…", rpCard: "Сообщённые проблемы", rpHint: "Проблемы, о которых пользователи сообщили со страницы книги. Решение убирает запись из списка.", rpEmpty: "Сообщений нет.", rpResolve: "Решить", rpOpen: "Открыть книгу", apCard: "Воспроизведение", apToggle: "Автоматически включать следующую книгу серии", apHint: "Когда книга заканчивается, автоматически начинается следующая из той же серии. Только книги из вашей библиотеки, ещё не законченные.", apNext: "Следующая в серии", fdToggle: "Инструменты законченных книг в статистике", ysNoDate: "без даты", hmShowing: "график показывает", ysStreak: "Серия дней", ysCurrent: "Текущая", ysLongest: "Самая длинная", ysDaysTotal: "Всего дней", ysPace: "Темп", ysPerDay: "За день прослушивания", ysWhen: "Когда вы слушаете", ysTopBooks: "Самые прослушиваемые книги", ysTopAuthors: "Самые прослушиваемые авторы", ysTopNarrators: "Самые прослушиваемые чтецы", rfToggle: "Раздел «Оцените» на главной", hoTitle: "Порядок разделов главной", hoEmpty: "Откройте главную один раз и вернитесь сюда.", hoReset: "Восстановить порядок по умолчанию", ctIcon: "Значок", ctColour: "Акцент", ctNeedBook: "Audiobookshelf не может сохранить пустую коллекцию, выберите хотя бы одну книгу. Позже можно свободно добавлять и убирать.", colEditIcon: "Сменить значок", ciTitle: "Значок коллекции", ciReset: "Подобрать по названию автоматически", colEditDesc: "Изменить описание", colBookForms: ["книга","книги","книг"], colForms: ["Коллекция","Коллекции","Коллекций"], ctTitle: "Создать коллекцию", ctBlank: "Пустая коллекция", ctNew: "Новая коллекция", ctNewHint: "из шаблона", ctNamePh: "Название коллекции", ctSearch: "Искать книги или авторов…", ctCreate: "Создать", ctBack: "‹ Назад", ctAddAll: "Добавить все книги", abTitle: "Добавить книги", abAdd: "Добавить", avRemove: "Убрать фото", avSet: "Выбрать фото…", ugListened: "Прослушано", ugPhoto: "Фото профиля", ugNever: "Никогда", ugSeen: "Был(а) недавно", ugEdit: "Изменить пользователя", ugDelete: "Удалить пользователя", ugAdd: "Добавить фото", ugChange: "Сменить фото", ugSortName: "А–Я", ugSortTime: "Больше всего слушали", ugSortSeen: "Недавно активные", bsCard: "Ссылки книги", bsHint: "Кнопки на странице книги для поиска названия на других сайтах. Goodreads и крупнейший сайт вашего языка включены заранее.", bsFind: "Искать на", bsGlobal: "Везде", bsLocal: "Для вашего языка", bsOther: "Другие страны", pgCard: "Переработанные страницы", pgHint: "Каждая опция заменяет страницу Audiobookshelf версией NanoHive. Выключите, чтобы вернуть оригинал.", pgNarrators: "Карточки чтецов", pgCollections: "Страницы коллекций", pgUsers: "Карточки пользователей (настройки)", pgStats: "Рейтинг прослушивания", pgPhoto: "Фото профиля в верхней панели", pgCinematic: "Киношный фон", pgTransitions: "Переходы страниц", pSearch: "Поиск настроек…", pSearchNone: "Ничего не найдено.", erTypeface: "Шрифт", erPage: "Тема страницы", erText: "Цвет текста", erBg: "Фон", erDefault: "По умолчанию (ABS)", erAuto: "Авто" , lfByAuthor: "Автор › порядок серии", lfBySeries: "Серия › порядок" , lfSecFilters: "Ещё фильтры", lfSecSort: "Многоуровневая сортировка", lfAuthor: "Автор", lfSeries: "Серия", lfTitle: "Название", lfYear: "Год", lfAdded: "Добавлено", lfDuration: "Длительность", lfNarrator: "Чтец", lfGenre: "Жанр", lfLanguage: "Язык", lfProgress: "Прогресс", lfPgFinished: "Законченные", lfPgInProgress: "Слушаются", lfPgNot: "Не начатые" , logoUpload: 'Загрузить с устройства…', logoUploaded: 'Загружено ✓', logoBadType: 'Неподдерживаемый тип изображения', logoTooBig: 'Изображение слишком большое (макс. 4 МБ)', logoOr: 'или', bookPage: 'Страница книги', ratingsToggle: 'Оценки сообщества (звёзды и отзывы)', ratingsHint: 'Оценки со звёздами и короткими отзывами на каждой странице книги, общие для всех пользователей этого сервера.', searchCard: 'Поиск', gsToggle: 'Искать сразу во всех библиотеках', gsHint: 'Один общий список результатов по всем доступным библиотекам, с меткой библиотеки у каждого совпадения. Выключите, чтобы вернуть меню по библиотекам.', gsNoResults: 'Ничего не найдено', gsSearching: 'Поиск…', gsBooks: 'Книги', gsSeries: 'Серии', gsAuthors: 'Авторы', spTitle: 'Стартовая страница', spHint: 'Всегда открывать приложение на выбранном виде библиотеки (только этот браузер). Прямые ссылки никогда не перенаправляются.', spDefault: 'По умолчанию', spViewHome: 'Главная', spViewLibrary: 'Библиотека', spViewSeries: 'Серии', spViewCollections: 'Коллекции', spViewAuthors: 'Авторы', scSet: 'Задать обложку серии', scRemove: 'Убрать обложку серии', scEditDesc: 'Изменить описание серии', cardStars: 'Звёзды на обложках книг', hoverPolish: 'Доводка карточек при наведении (мягче затемнение, акцентная кнопка, звёзды при наведении)', sectUser: 'Ваши настройки', sectAdmin: 'Администрирование', lockTitle: 'Переключатели функций', lockHint: 'Принудительно отключает функцию для всех пользователей этого сервера. Сильнее личных настроек; остальные увидят после следующей перезагрузки.', lockRatings: 'Отключить оценки везде', lockCardRatings: 'Отключить звёзды на обложках', lockGlobalSearch: 'Отключить глобальный поиск', lockHoverPolish: 'Отключить доводку карточек', lockHeroCarousel: 'Отключить карусель на главной', lockedNote: 'отключено администратором', lfSort: 'Оценка', lfRated: 'С оценкой', lfUnrated: 'Без оценки', lfClear: 'Сбросить', lfNone: 'Нет подходящих книг', lfSection: 'Оценка', lfWho: 'Чьи оценки', lfEveryone: 'Все', sdSave: 'Сохранить', sdClear: 'Вернуть описание книги', sdMore: 'Читать дальше', sdLess: 'Свернуть', shSeries: 'Серия', shBy: '', shMore: ' и другие', shBookForms: ['книга', 'книги', 'книг'], shAvg: 'среднее по книгам', shRated: 'с оценкой', impLabel: 'Импорт оценок', impHint: 'Перенесите оценки из StoryGraph или Goodreads. Выгрузите там свои книги в CSV и выберите файл здесь. Каждое совпадение показывается на подтверждение, и ничего не сохраняется, пока вы не подтвердите.', impBtn: 'Выбрать файл CSV…', impReading: 'Чтение файла…', impMatching: 'Сопоставление с вашими библиотеками…', impErrUnknown: 'Это не похоже на экспорт из StoryGraph или Goodreads. Нужен их CSV со столбцом "Star Rating" или "My Rating".', impErrEmpty: 'В файле нет строк.', impErrNoRated: 'В файле нет книг с оценками.', impErrRead: 'Не удалось прочитать файл.', impErrLoad: 'Не удалось загрузить библиотеки. Попробуйте ещё раз через минуту.', impErrNoLibs: 'Ни одна библиотека не участвует в оценках, импортировать некуда.', impTitle: 'Импорт оценок', impFrom: 'Экспорт %s: %d книг, из них %d с оценкой', impLibs: 'Поиск в: %s', impMatched: 'Сопоставлено', impCheck: 'Стоит проверить', impCheckHint: 'Похоже, но не наверняка. Проверьте совпадение или выберите другое.', impAlready: 'Уже оценено вами', impAlreadyHint: 'Останутся как есть, если не включить перезапись.', impNone: 'Нет в вашей библиотеке', impNoneHint: 'На этом сервере ничего не соответствует этим записям.', impAll: 'Все', impSkip: 'Пропустить', impDup: 'дубликат', impByTitle: 'название', impYours: 'ваша: %s', impReviews: 'Импортировать и написанные отзывы (%d)', impOver: 'Перезаписать мои существующие оценки', impGo: 'Импортировать %d оценок', impCancel: 'Отмена', impCloseBtn: 'Закрыть', impWorking: 'Сохранение… %d из %d', impDone: 'Импортировано оценок: %d.', impFail: 'Не удалось сохранить: %d.', rdTitle: 'Кто читает эту книгу', rdFin: 'Прочитано', socTitle: 'Социальные функции', socHint: 'Что этот сервер делит между пользователями. Время прослушивания питает рейтинг; две другие функции раскрывают, кто что читает, поэтому выключены, пока вы их не включите. Действует сразу для всех.', socTime: 'Время прослушивания (рейтинг сервера)', socContent: 'Топ-названия в общей статистике', socWho: 'Кто читает (страницы книг)', socUsers: 'По пользователям', socUsersHint: 'Выключите кого-то, и его данные не будут делиться независимо от его выбора; вернуть можете только вы. "Отказался сам" значит, что человек сам сказал нет.', socOpted: 'отказался сам', srvTitle: 'Настройки сервера по умолчанию', srvHint: 'Сохраните текущие настройки как значения по умолчанию для всех пользователей этого сервера. Каждый сможет настроить поверх. Смонтируйте том в /data/nh контейнера темы, чтобы они пережили обновления.', srvSave: 'Сохранить как значения сервера', srvClear: 'Сбросить значения сервера', srvSaved: 'Сохранено', srvCleared: 'Сброшено', srvErr: 'Не удалось: нужен вход администратора', crMode: 'Полка Продолжить чтение', crCombine: 'Объединить с каруселью', crSeparate: 'Оставить отдельной полкой', crHide: 'Скрыта', fsHint: 'Делитесь своим прослушиванием с другими на этом сервере: суммы времени и минуты по дням для рейтинга, а если администратор включил, также ваши топ-названия и какие книги вы читаете или закончили. Включено по умолчанию. При выключении всё опубликованное удаляется.' , amfHide: 'Скрыть Отметить как прочитанное' , amfConfirm: 'Отметить эту книгу как прочитанную?' , fsHint: 'Делитесь сводкой прослушивания (итоги и минуты за день) с другими на этом сервере, она питает рейтинг сервера. Включено по умолчанию. При отключении вы исчезаете из рейтинга и его итогов, а всё переданное удаляется.' , srToggle: 'Делиться тем, какие книги вы читаете', srHint: 'Добавляет к тому, что видят другие, какие книги вы сейчас читаете, какие закончили и какие слушаете чаще всего. По умолчанию выключено; также должны быть включены серверные переключатели администратора.' , fsHint: 'Делитесь временем прослушивания (суммы и минуты по дням) с другими на этом сервере; оно питает рейтинг. Включено по умолчанию. При выключении другие пользователи вас больше не видят; администраторы сервера по-прежнему видят активность с пометкой об отказе.', srHint: 'Также позволяет другим видеть, какие книги вы читаете, какие закончили и какие слушаете чаще всего. Ничего не отображается, пока администратор не включит соответствующие серверные переключатели.', seshTitle: 'Последние сессии', seshNow: 'Слушают сейчас' , hcHint: 'Отправляйте свои оценки в аккаунт Hardcover. Вставьте API-ключ из настроек аккаунта Hardcover; ключи истекают через год.', hcKeyLbl: 'API-ключ', hcToggle: 'Синхронизировать новые оценки автоматически', hcPushAll: 'Отправить все оценки', hcPushDiff: 'Отправить только различия', hcDone: 'Готово: %d синхронизировано, %d без совпадения', hcNoKey: 'Сначала вставьте свой API-ключ Hardcover' , hcMissHead: 'Не найдено на Hardcover:' , hcHint: 'Отправляйте свои оценки в аккаунт Hardcover. Вставьте API-ключ из настроек аккаунта Hardcover.' , tabStyle: 'Стиль', tabMain: 'Главная', tabBooks: 'Книги', tabFeat: 'Функции' , hcImport: 'Импорт из Hardcover', impMarkQ: 'Отметить также %d совпавших книг как завершённые здесь?', impMarkDone: '%d отмечено как завершённые.' , impHint: 'Перенесите свои оценки из Goodreads, StoryGraph или Hardcover. Выберите здесь экспортированный CSV; Hardcover импортируется напрямую кнопкой на своей карточке. Каждое совпадение показывается для подтверждения перед сохранением.' , impErrUnknown: 'Это не похоже на экспорт из Goodreads, StoryGraph или Hardcover: не найдены столбцы названия и оценки.' , fsCard: 'Социальное', rnToggle: 'Показывать моё имя рядом с оценками', rsToggle: 'Делиться моими оценками с другими', socNames: 'Имена рядом с оценками', rtAnon: 'Аноним' },
    uk: { cmStopped: "зупинено", cmScanErrs: "{e} не перевірено (помилки Goodreads)", cmScanBusy: "Сканування вже триває", cmGrErr: "Goodreads недоступний, спробую знову", cmNoMatchYet: "На Goodreads не знайдено", cmLooking: "Шукаю на Goodreads…", cmToolHint: "Що думають про цю книгу читачі Goodreads. Знак ? показує зіставлену книгу і дозволяє її змінити.", cmSearchPh: "Шукати на Goodreads…", cmNoVotes: "ще немає оцінок", cmRefresh: "Оновити", cmScanAll: "Перевірити все заново, а не лише відсутні", cmScanDone: "Готово: {m} знайдено · {x} не знайдено · {s} уже було", cmStop: "Зупинити", cmScanLibs: "Бібліотеки для сканування", cmScan: "Знайти оцінки", lsLongest: "Найдовші книги", lsSize: "Розмір на диску", lsDuration: "Загальна тривалість", lsGenresN: "Жанри", lsSeriesN: "Серії", lsAuthorsN: "Автори", lsBooks: "Книги", lsGlance: "Бібліотека в цифрах", cmOpen: "Відкрити на Goodreads", cmDetect: "Знайти", cmAddr: "Адреса помічника", scmTitle: "Сторінка серії", scmHint: "Як виглядає згенерована обкладинка над назвою серії, коли власну не завантажено (лише цей браузер).", scmDeck: "Шарувата колода", cmHint: "Що думають про книгу читачі Goodreads: середня оцінка і кількість голосів, під зірками на сторінці книги, а також сортування та фільтр. Сторінки книг заповнюються самі під час перегляду.", cmGrOff: "Goodreads: не налаштовано", cmSetup: "Помічника ще немає? Запустіть це поруч із темою (рядки compose нижче) і натисніть Знайти. Підійде і будь-який інший спосіб запуску образу: вставте його адресу вище.", cmGrOn: "Goodreads: підключено", cmGrDown: "Goodreads: налаштовано, але не відповідає", cmGrNoNum: "Goodreads: відповідає, але ця збірка abs-tract не дає оцінок (використайте збірку з оцінками, див. README)", cmTitle: "Оцінки Goodreads", cmWrong: "Не та книга?", cmFind: "Знайти оцінку спільноти…", cmNone: "Немає збігу", cmUse: "Використати", lfCommunity: "Оцінка Goodreads", mfNatChange: "Змінити…", lsByTime: "за часом прослуховування", nrSearch: 'Фільтр читців…', nrSortName: 'Імʼя', nrSortBooks: 'Більше книг', nrForms: ['Читець', 'Читці', 'Читців'], nrBooksForms: ['книга', 'книги', 'книг'], auForms: ['Автор', 'Автори', 'Авторів'], auSearch: 'Фільтр авторів…' , sbTitle: "Рейтинг сервера", sbWeek: "Тиждень", sbMonth: "Місяць", sbYear: "Рік", sbAll: "За весь час", sbBooks: "Книги", sbTopBooks: "Найбільше слухані", sbTotal: "Разом", sbListeners: "Слухачі", sbAvg: "У середньому", wkTitle: "Хвилини слухання, останні 7 днів", wkAvg: "Середнє за день", wkRow: "Днів поспіль", rpBadge: "Відкриті повідомлення про проблеми", rpMenu: "Повідомити про проблему", rpTitle: "Повідомити про проблему", rpWhat: "Що не так?", rpNote: "Щось іще для адміністратора? (необов’язково)", rpSend: "Надіслати", rpSent: "Надіслано. Дякуємо.", rpFail: "Не вдалося надіслати", rpMissing: "Відсутній або неповний вміст", rpQuality: "Погана якість звуку", rpPlay: "Не відтворюється", rpWrong: "Не та книга, обкладинка чи метадані", rpChapters: "Неправильні розділи", rpOther: "Інше", rfRate: "Оцінити", rfSheet: "Оцініть цю книгу", rfOpen: "Відкрити сторінку книги", rfPickHint: "Виберіть оцінку", rfTitle: "Оцініть прослухане", ysFinished: "Нещодавно завершені", ysAlmost: "Майже завершені", ysMarkDone: "Позначити як завершену", ysEditDate: "Змінити дату завершення", ysThisWeek: "Цього тижня", ysBestDay: "Найкращий день", ysTitle: "Ваше слухання", fsTitle: "Рейтинг сервера", fsCard: "Рейтинг сервера", fsToggle: "Ділитися моєю статистикою слухання", fsJoin: "Увімкніть «Ділитися моєю статистикою слухання» в панелі налаштувань, щоб з’явитися в рейтингу сервера.", fsWaiting: "Обмін увімкнено, ваш підсумок невдовзі долучиться до рейтингу сервера разом з іншими.", fsHint: "Діліться підсумком свого слухання (загальні суми та хвилини за день) з іншими на цьому сервері, він живить рейтинг сервера. Типово ввімкнено. Після вимкнення ви зникаєте з рейтингу та його сум, а всі передані дані видаляються.", yirBtn: "Підсумки року", yirTitle: "Ваш рік слухання", yirYear: "Останні 12 місяців", yirDays: "Дні зі слуханням", yirBest: "Найкращий день", yirMonths: "За місяцями", yirNone: "Даних про слухання ще немає.", ssscmDeck: "Шарувата колода", scmFirst: "Обкладинка першої книги", scmGrid: "Сітка обкладинок (типово)", scmOff: "Прихована" , lsTitle: "Статистика сервера", lsAll: "Усі бібліотеки", lsPlayed: "Прослухані книги", lsBest: "Найкраще оцінені", lsGenres: "Найпопулярніші жанри", srvPick: "Що зберегти:", srvGRest: "Функції та поведінка", aeBtn: "Прибрати авторів", aeTitle: "Автори без книг", aeNone: "Кожен автор тут має принаймні одну книгу.", aeRemove: "Видалити", aeDone: "Видалено", aeWorking: "Видалення…", rpCard: "Повідомлені проблеми", rpHint: "Проблеми, про які користувачі повідомили зі сторінки книги. Розвʼязання прибирає запис зі списку.", rpEmpty: "Нічого не повідомлено.", rpResolve: "Розвʼязати", rpOpen: "Відкрити книгу", apCard: "Відтворення", apToggle: "Автоматично вмикати наступну книгу серії", apHint: "Коли книга закінчується, автоматично починається наступна з тієї ж серії. Лише книги з вашої бібліотеки, ще не завершені.", apNext: "Наступна в серії", fdToggle: "Інструменти завершених книг у статистиці", ysNoDate: "без дати", hmShowing: "графік показує", ysStreak: "Серія днів", ysCurrent: "Поточна", ysLongest: "Найдовша", ysDaysTotal: "Днів разом", ysPace: "Темп", ysPerDay: "За день слухання", ysWhen: "Коли ви слухаєте", ysTopBooks: "Найбільш слухані книги", ysTopAuthors: "Найбільш слухані автори", ysTopNarrators: "Найбільш слухані оповідачі", rfToggle: "Розділ «Оцініть» на головній", hoTitle: "Порядок розділів головної", hoEmpty: "Відкрийте головну один раз і поверніться сюди.", hoReset: "Відновити типовий порядок", ctIcon: "Значок", ctColour: "Акцент", ctNeedBook: "Audiobookshelf не може зберегти порожню колекцію, виберіть хоча б одну книгу. Пізніше можна вільно додавати й прибирати.", colEditIcon: "Змінити значок", ciTitle: "Значок колекції", ciReset: "Підібрати за назвою автоматично", colEditDesc: "Редагувати опис", colBookForms: ["книга","книги","книг"], colForms: ["Колекція","Колекції","Колекцій"], ctTitle: "Створити колекцію", ctBlank: "Порожня колекція", ctNew: "Нова колекція", ctNewHint: "із шаблону", ctNamePh: "Назва колекції", ctSearch: "Шукати книги або авторів…", ctCreate: "Створити", ctBack: "‹ Назад", ctAddAll: "Додати всі книги", abTitle: "Додати книги", abAdd: "Додати", avRemove: "Прибрати фото", avSet: "Вибрати фото…", ugListened: "Прослухано", ugPhoto: "Фото профілю", ugNever: "Ніколи", ugSeen: "Востаннє активний", ugEdit: "Редагувати користувача", ugDelete: "Видалити користувача", ugAdd: "Додати фото", ugChange: "Змінити фото", ugSortName: "А–Я", ugSortTime: "Найбільше слухали", ugSortSeen: "Нещодавно активні", bsCard: "Посилання книги", bsHint: "Кнопки на сторінці книги для пошуку назви деінде. Goodreads і найбільший сайт вашої мови ввімкнені наперед.", bsFind: "Шукати на", bsGlobal: "Всюди", bsLocal: "Для вашої мови", bsOther: "Інші країни", pgCard: "Перероблені сторінки", pgHint: "Кожна опція замінює сторінку Audiobookshelf версією NanoHive. Вимкніть, щоб повернути оригінал.", pgNarrators: "Картки оповідачів", pgCollections: "Сторінки колекцій", pgUsers: "Картки користувачів (налаштування)", pgStats: "Рейтинг слухання", pgPhoto: "Фото профілю у верхній панелі", pgCinematic: "Кінематографічне тло", pgTransitions: "Переходи сторінок", pSearch: "Шукати налаштування…", pSearchNone: "Нічого не знайдено.", erTypeface: "Шрифт", erPage: "Тема сторінки", erText: "Колір тексту", erBg: "Тло", erDefault: "Типово (ABS)", erAuto: "Авто" , lfByAuthor: "Автор › порядок серії", lfBySeries: "Серія › порядок" , lfSecFilters: "Ще фільтри", lfSecSort: "Багаторівневе сортування", lfAuthor: "Автор", lfSeries: "Серія", lfTitle: "Назва", lfYear: "Рік", lfAdded: "Додано", lfDuration: "Тривалість", lfNarrator: "Оповідач", lfGenre: "Жанр", lfLanguage: "Мова", lfProgress: "Прогрес", lfPgFinished: "Завершені", lfPgInProgress: "Слухаються", lfPgNot: "Не розпочаті" , logoUpload: 'Завантажити з пристрою…', logoUploaded: 'Завантажено ✓', logoBadType: 'Непідтримуваний тип зображення', logoTooBig: 'Зображення завелике (макс. 4 МБ)', logoOr: 'або', bookPage: 'Сторінка книги', ratingsToggle: 'Оцінки спільноти (зірки та відгуки)', ratingsHint: 'Зіркові оцінки з короткими відгуками на кожній сторінці книги, спільні для всіх користувачів цього сервера.', searchCard: 'Пошук', gsToggle: 'Шукати одразу в усіх бібліотеках', gsHint: 'Один об\'єднаний список результатів з усіх доступних бібліотек, з позначкою бібліотеки біля кожного збігу. Вимкніть, щоб повернути меню по бібліотеках.', gsNoResults: 'Нічого не знайдено', gsSearching: 'Пошук…', gsBooks: 'Книги', gsSeries: 'Серії', gsAuthors: 'Автори', spTitle: 'Початкова сторінка', spHint: 'Завжди відкривати застосунок на вибраному вигляді бібліотеки (лише цей браузер). Прямі посилання ніколи не перенаправляються.', spDefault: 'Типово для застосунку', spViewHome: 'Головна', spViewLibrary: 'Бібліотека', spViewSeries: 'Серії', spViewCollections: 'Колекції', spViewAuthors: 'Автори', scSet: 'Встановити обкладинку серії', scRemove: 'Прибрати обкладинку серії', scEditDesc: 'Редагувати опис серії', cardStars: 'Зірки на обкладинках книг', hoverPolish: 'Доопрацювання карток при наведенні (м\'якше затемнення, акцентна кнопка, зірки при наведенні)', sectUser: 'Ваші налаштування', sectAdmin: 'Адміністрування', lockTitle: 'Перемикачі функцій', lockHint: 'Примусово вимикає функцію для всіх користувачів цього сервера. Сильніше за особисті налаштування; інші побачать після наступного перезавантаження.', lockRatings: 'Вимкнути оцінки всюди', lockCardRatings: 'Вимкнути зірки на обкладинках', lockGlobalSearch: 'Вимкнути глобальний пошук', lockHoverPolish: 'Вимкнути доопрацювання карток', lockHeroCarousel: 'Вимкнути карусель на головній', lockedNote: 'вимкнено адміністратором', lfSort: 'Оцінка', lfRated: 'З оцінкою', lfUnrated: 'Без оцінки', lfClear: 'Скинути', lfNone: 'Немає відповідних книг', lfSection: 'Оцінка', lfWho: 'Чиї оцінки', lfEveryone: 'Усі', sdSave: 'Зберегти', sdClear: 'Повернути опис книги', sdMore: 'Читати далі', sdLess: 'Згорнути', shSeries: 'Серія', shBy: '', shMore: ' та інші', shBookForms: ['книга', 'книги', 'книг'], shAvg: 'середнє по книгах', shRated: 'з оцінкою', impLabel: 'Імпорт оцінок', impHint: 'Перенесіть оцінки зі StoryGraph або Goodreads. Експортуйте там свої книги у CSV і виберіть файл тут. Кожен збіг показується на підтвердження, і нічого не зберігається, доки ви не підтвердите.', impBtn: 'Вибрати файл CSV…', impReading: 'Читання файлу…', impMatching: 'Зіставлення з вашими бібліотеками…', impErrUnknown: 'Це не схоже на експорт зі StoryGraph чи Goodreads. Потрібен їхній CSV зі стовпцем "Star Rating" або "My Rating".', impErrEmpty: 'У файлі немає рядків.', impErrNoRated: 'У файлі немає книг з оцінками.', impErrRead: 'Не вдалося прочитати файл.', impErrLoad: 'Не вдалося завантажити бібліотеки. Спробуйте ще раз за мить.', impErrNoLibs: 'Жодна бібліотека не бере участі в оцінках, тож нікуди імпортувати.', impTitle: 'Імпорт оцінок', impFrom: 'Експорт %s: %d книг, з них %d з оцінкою', impLibs: 'Пошук у: %s', impMatched: 'Зіставлено', impCheck: 'Варто перевірити', impCheckHint: 'Близько, але не напевно. Перевірте збіг або виберіть інший.', impAlready: 'Уже оцінено вами', impAlreadyHint: 'Залишаться як є, якщо не увімкнути перезапис.', impNone: 'Немає у вашій бібліотеці', impNoneHint: 'На цьому сервері ніщо не відповідає цим записам.', impAll: 'Усі', impSkip: 'Пропустити', impDup: 'дублікат', impByTitle: 'назва', impYours: 'ваша: %s', impReviews: 'Імпортувати й написані відгуки (%d)', impOver: 'Перезаписати мої наявні оцінки', impGo: 'Імпортувати %d оцінок', impCancel: 'Скасувати', impCloseBtn: 'Закрити', impWorking: 'Збереження… %d з %d', impDone: 'Імпортовано оцінок: %d.', impFail: 'Не вдалося зберегти: %d.', rdTitle: 'Хто читає цю книгу', rdFin: 'Прочитано', socTitle: 'Соціальні функції', socHint: 'Що цей сервер ділить між користувачами. Час прослуховування живить рейтинг; дві інші функції розкривають, хто що читає, тому вимкнені, доки ви їх не увімкнете. Діє одразу для всіх.', socTime: 'Час прослуховування (рейтинг сервера)', socContent: 'Топ-назви у спільній статистиці', socWho: 'Хто читає (сторінки книг)', socUsers: 'По користувачах', socUsersHint: 'Вимкніть когось, і його дані не ділитимуться незалежно від його вибору; повернути можете лише ви. "Відмовився сам" означає, що людина сама сказала ні.', socOpted: 'відмовився сам', srvTitle: 'Типові налаштування сервера', srvHint: 'Збережіть поточні налаштування як типові для всіх користувачів цього сервера. Кожен зможе налаштувати поверх. Змонтуйте том у /data/nh контейнера теми, щоб вони пережили оновлення.', srvSave: 'Зберегти як типові сервера', srvClear: 'Скинути типові сервера', srvSaved: 'Збережено', srvCleared: 'Скинуто', srvErr: 'Не вдалося: потрібен вхід адміністратора', crMode: 'Полиця Продовжити читання', crCombine: 'Об\'єднати з каруселлю', crSeparate: 'Лишити окремою полицею', crHide: 'Прихована', fsHint: 'Діліться своїм прослуховуванням з іншими на цьому сервері: суми часу та хвилини по днях для рейтингу, а якщо адміністратор увімкнув, також ваші топ-назви й які книги ви читаєте чи закінчили. Типово увімкнено. При вимкненні все опубліковане видаляється.' , amfHide: 'Приховати Позначити як прочитане' , amfConfirm: 'Позначити цю книгу як прочитану?' , fsHint: 'Діліться підсумком свого слухання (загальні суми та хвилини за день) з іншими на цьому сервері, він живить рейтинг сервера. Типово ввімкнено. Після вимкнення ви зникаєте з рейтингу та його сум, а всі передані дані видаляються.' , srToggle: 'Ділитися тим, які книги ви читаєте', srHint: 'Додає до того, що бачать інші, які книги ви саме читаєте, які закінчили та які слухаєте найчастіше. Типово вимкнено; також мають бути увімкнені серверні перемикачі адміністратора.' , fsHint: 'Діліться часом прослуховування (суми та хвилини по днях) з іншими на цьому сервері; він живить рейтинг. Типово увімкнено. Після вимкнення інші користувачі вас більше не бачать; адміністратори сервера все одно бачать активність із позначкою відмови.', srHint: 'Також дозволяє іншим бачити, які книги ви читаєте, які закінчили та які слухаєте найчастіше. Нічого не видно, доки адміністратор не увімкне відповідні серверні перемикачі.', seshTitle: 'Останні сесії', seshNow: 'Слухають зараз' , hcHint: 'Надсилайте свої оцінки до акаунта Hardcover. Вставте API-ключ із налаштувань акаунта Hardcover; ключі спливають через рік.', hcKeyLbl: 'API-ключ', hcToggle: 'Синхронізувати нові оцінки автоматично', hcPushAll: 'Надіслати всі оцінки', hcPushDiff: 'Надіслати лише відмінності', hcDone: 'Готово: %d синхронізовано, %d без збігу', hcNoKey: 'Спершу вставте свій API-ключ Hardcover' , hcMissHead: 'Не знайдено на Hardcover:' , hcHint: 'Надсилайте свої оцінки до акаунта Hardcover. Вставте API-ключ із налаштувань акаунта Hardcover.' , tabStyle: 'Стиль', tabMain: 'Головна', tabBooks: 'Книги', tabFeat: 'Функції' , hcImport: 'Імпорт із Hardcover', impMarkQ: 'Позначити також %d зіставлених книг як завершені тут?', impMarkDone: '%d позначено як завершені.' , impHint: 'Перенесіть свої оцінки з Goodreads, StoryGraph або Hardcover. Виберіть тут експортований CSV; Hardcover імпортується напряму кнопкою на своїй картці. Кожен збіг показується для підтвердження перед збереженням.' , impErrUnknown: 'Це не схоже на експорт із Goodreads, StoryGraph або Hardcover: не знайдено стовпців назви та оцінки.' , fsCard: 'Соціальне', rnToggle: 'Показувати моє ім’я поруч з оцінками', rsToggle: 'Ділитися моїми оцінками з іншими', socNames: 'Імена поруч з оцінками', rtAnon: 'Анонім' },
    be: { cmStopped: "спынена", cmScanErrs: "{e} не праверана (памылкі Goodreads)", cmScanBusy: "Сканаванне ўжо ідзе", cmGrErr: "Goodreads недаступны, паспрабую зноў", cmNoMatchYet: "На Goodreads не знойдзена", cmLooking: "Шукаю на Goodreads…", cmToolHint: "Што думаюць пра гэтую кнігу чытачы Goodreads. Знак ? паказвае супастаўленую кнігу і дазваляе яе змяніць.", cmSearchPh: "Шукаць на Goodreads…", cmNoVotes: "пакуль няма ацэнак", cmRefresh: "Абнавіць", cmScanAll: "Праверыць усё нанова, а не толькі адсутныя", cmScanDone: "Гатова: {m} знойдзена · {x} не знойдзена · {s} ужо было", cmStop: "Спыніць", cmScanLibs: "Бібліятэкі для сканавання", cmScan: "Знайсці ацэнкі", lsLongest: "Найдаўжэйшыя кнігі", lsSize: "Памер на дыску", lsDuration: "Агульная працягласць", lsGenresN: "Жанры", lsSeriesN: "Серыі", lsAuthorsN: "Аўтары", lsBooks: "Кнігі", lsGlance: "Бібліятэка ў лічбах", cmOpen: "Адкрыць на Goodreads", cmDetect: "Знайсці", cmAddr: "Адрас памочніка", scmTitle: "Старонка серыі", scmHint: "Як выглядае згенераваная вокладка над назвай серыі, калі ўласную не загружана (толькі гэты браўзер).", scmDeck: "Слаістая калода", cmHint: "Што думаюць пра кнігу чытачы Goodreads: сярэдняя ацэнка і колькасць галасоў, пад зоркамі на старонцы кнігі, а таксама сартаванне і фільтр. Старонкі кніг запаўняюцца самі пры праглядзе.", cmGrOff: "Goodreads: не наладжана", cmSetup: "Памочніка яшчэ няма? Запусціце гэта побач з тэмай (радкі compose ніжэй) і націсніце Знайсці. Падыдзе і любы іншы спосаб запуску вобраза: устаўце яго адрас вышэй.", cmGrOn: "Goodreads: падключана", cmGrDown: "Goodreads: наладжана, але не адказвае", cmGrNoNum: "Goodreads: адказвае, але гэтая зборка abs-tract не дае ацэнак (выкарыстайце зборку з ацэнкамі, гл. README)", cmTitle: "Ацэнкі Goodreads", cmWrong: "Не тая кніга?", cmFind: "Знайсці ацэнку супольнасці…", cmNone: "Няма супадзення", cmUse: "Выкарыстаць", lfCommunity: "Ацэнка Goodreads", mfNatChange: "Змяніць…", lsByTime: "па часе праслухоўвання", nrSearch: 'Фільтр чытальнікаў…', nrSortName: 'Імя', nrSortBooks: 'Больш кніг', nrForms: ['Чытальнік', 'Чытальнікі', 'Чытальнікаў'], nrBooksForms: ['кніга', 'кнігі', 'кніг'], auForms: ['Аўтар', 'Аўтары', 'Аўтараў'], auSearch: 'Фільтр аўтараў…' , sbTitle: "Рэйтынг сервера", sbWeek: "Тыдзень", sbMonth: "Месяц", sbYear: "Год", sbAll: "За ўвесь час", sbBooks: "Кнігі", sbTopBooks: "Самыя праслухоўваныя", sbTotal: "Разам", sbListeners: "Слухачы", sbAvg: "У сярэднім", wkTitle: "Хвіліны праслухоўвання, апошнія 7 дзён", wkAvg: "Сярэдняе за дзень", wkRow: "Дзён запар", rpBadge: "Адкрытыя паведамленні пра праблемы", rpMenu: "Паведаміць пра праблему", rpTitle: "Паведаміць пра праблему", rpWhat: "Што не так?", rpNote: "Нешта яшчэ для адміністратара? (неабавязкова)", rpSend: "Даслаць", rpSent: "Даслана. Дзякуй.", rpFail: "Не ўдалося даслаць", rpMissing: "Адсутны або няпоўны змест", rpQuality: "Дрэнная якасць гуку", rpPlay: "Не прайграецца", rpWrong: "Не тая кніга, вокладка ці метаданыя", rpChapters: "Няправільныя раздзелы", rpOther: "Іншае", rfRate: "Ацаніць", rfSheet: "Ацаніце гэтую кнігу", rfOpen: "Адкрыць старонку кнігі", rfPickHint: "Выберыце ацэнку", rfTitle: "Ацаніце праслуханае", ysFinished: "Нядаўна скончаныя", ysAlmost: "Амаль скончаныя", ysMarkDone: "Пазначыць як скончаную", ysEditDate: "Змяніць дату заканчэння", ysThisWeek: "На гэтым тыдні", ysBestDay: "Лепшы дзень", ysTitle: "Ваша праслухоўванне", fsTitle: "Рэйтынг сервера", fsCard: "Рэйтынг сервера", fsToggle: "Дзяліцца маёй статыстыкай праслухоўвання", fsJoin: "Уключыце «Дзяліцца маёй статыстыкай праслухоўвання» ў панэлі налад, каб з’явіцца ў рэйтынгу сервера.", fsWaiting: "Абмен уключаны, ваша зводка неўзабаве далучыцца да рэйтынгу сервера разам з іншымі.", fsHint: "Дзяліцеся зводкай свайго праслухоўвання (сумы і хвіліны за дзень) з іншымі на гэтым серверы, яна жывіць рэйтынг сервера. Тыпова ўключана. Пасля выключэння вы знікаеце з рэйтынгу і яго сум, а перададзеныя даныя выдаляюцца.", yirBtn: "Вынікі года", yirTitle: "Ваш год праслухоўвання", yirYear: "Апошнія 12 месяцаў", yirDays: "Дні з праслухоўваннем", yirBest: "Лепшы дзень", yirMonths: "Па месяцах", yirNone: "Даных пра праслухоўванне пакуль няма.", ssscmDeck: "Слаістая калода", scmFirst: "Вокладка першай кнігі", scmGrid: "Сетка вокладак (тыпова)", scmOff: "Схаваная" , lsTitle: "Статыстыка сервера", lsAll: "Усе бібліятэкі", lsPlayed: "Праслуханыя кнігі", lsBest: "Найлепш ацэненыя", lsGenres: "Папулярныя жанры", srvPick: "Што захаваць:", srvGRest: "Функцыі і паводзіны", aeBtn: "Прыбраць аўтараў", aeTitle: "Аўтары без кніг", aeNone: "У кожнага аўтара тут ёсць прынамсі адна кніга.", aeRemove: "Выдаліць", aeDone: "Выдалена", aeWorking: "Выдаленне…", rpCard: "Паведамленыя праблемы", rpHint: "Праблемы, пра якія карыстальнікі паведамілі са старонкі кнігі. Рашэнне прыбірае запіс са спіса.", rpEmpty: "Нічога не паведамлена.", rpResolve: "Вырашыць", rpOpen: "Адкрыць кнігу", apCard: "Прайграванне", apToggle: "Аўтаматычна ўключаць наступную кнігу серыі", apHint: "Калі кніга заканчваецца, аўтаматычна пачынаецца наступная з той жа серыі. Толькі кнігі з вашай бібліятэкі, яшчэ не скончаныя.", apNext: "Наступная ў серыі", fdToggle: "Інструменты скончаных кніг у статыстыцы", ysNoDate: "без даты", hmShowing: "графік паказвае", ysStreak: "Серыя дзён", ysCurrent: "Бягучая", ysLongest: "Найдаўжэйшая", ysDaysTotal: "Дзён разам", ysPace: "Тэмп", ysPerDay: "За дзень праслухоўвання", ysWhen: "Калі вы слухаеце", ysTopBooks: "Самыя праслухоўваныя кнігі", ysTopAuthors: "Самыя праслухоўваныя аўтары", ysTopNarrators: "Самыя праслухоўваныя чытальнікі", rfToggle: "Раздзел «Ацаніце» на галоўнай", hoTitle: "Парадак раздзелаў галоўнай", hoEmpty: "Адкрыйце галоўную адзін раз і вярніцеся сюды.", hoReset: "Аднавіць прадвызначаны парадак", ctIcon: "Значок", ctColour: "Акцэнт", ctNeedBook: "Audiobookshelf не можа захаваць пустую калекцыю, выберыце прынамсі адну кнігу. Пазней можна вольна дадаваць і прыбіраць.", colEditIcon: "Змяніць значок", ciTitle: "Значок калекцыі", ciReset: "Падабраць па назве аўтаматычна", colEditDesc: "Рэдагаваць апісанне", colBookForms: ["кніга","кнігі","кніг"], colForms: ["Калекцыя","Калекцыі","Калекцый"], ctTitle: "Стварыць калекцыю", ctBlank: "Пустая калекцыя", ctNew: "Новая калекцыя", ctNewHint: "з шаблону", ctNamePh: "Назва калекцыі", ctSearch: "Шукаць кнігі або аўтараў…", ctCreate: "Стварыць", ctBack: "‹ Назад", ctAddAll: "Дадаць усе кнігі", abTitle: "Дадаць кнігі", abAdd: "Дадаць", avRemove: "Прыбраць фота", avSet: "Выбраць фота…", ugListened: "Праслухана", ugPhoto: "Фота профілю", ugNever: "Ніколі", ugSeen: "Апошні раз актыўны", ugEdit: "Рэдагаваць карыстальніка", ugDelete: "Выдаліць карыстальніка", ugAdd: "Дадаць фота", ugChange: "Змяніць фота", ugSortName: "А–Я", ugSortTime: "Найбольш слухалі", ugSortSeen: "Нядаўна актыўныя", bsCard: "Спасылкі кнігі", bsHint: "Кнопкі на старонцы кнігі для пошуку назвы ў іншых сэрвісах. Goodreads і найбуйнейшы сайт вашай мовы ўключаныя загадзя.", bsFind: "Шукаць на", bsGlobal: "Усюды", bsLocal: "Для вашай мовы", bsOther: "Іншыя краіны", pgCard: "Перапрацаваныя старонкі", pgHint: "Кожная опцыя замяняе старонку Audiobookshelf версіяй NanoHive. Выключыце, каб вярнуць арыгінал.", pgNarrators: "Карткі чытальнікаў", pgCollections: "Старонкі калекцый", pgUsers: "Карткі карыстальнікаў (налады)", pgStats: "Рэйтынг праслухоўвання", pgPhoto: "Фота профілю ў верхняй панэлі", pgCinematic: "Кінематаграфічны фон", pgTransitions: "Пераходы старонак", pSearch: "Шукаць налады…", pSearchNone: "Нічога не знойдзена.", erTypeface: "Шрыфт", erPage: "Тэма старонкі", erText: "Колер тэксту", erBg: "Фон", erDefault: "Прадвызначана (ABS)", erAuto: "Аўта" , lfByAuthor: "Аўтар › парадак серыі", lfBySeries: "Серыя › парадак" , lfSecFilters: "Яшчэ фільтры", lfSecSort: "Шматузроўневае сартаванне", lfAuthor: "Аўтар", lfSeries: "Серыя", lfTitle: "Назва", lfYear: "Год", lfAdded: "Дададзена", lfDuration: "Працягласць", lfNarrator: "Чытальнік", lfGenre: "Жанр", lfLanguage: "Мова", lfProgress: "Прагрэс", lfPgFinished: "Скончаныя", lfPgInProgress: "Слухаюцца", lfPgNot: "Не пачатыя" , logoUpload: 'Загрузіць з прылады…', logoUploaded: 'Загружана ✓', logoBadType: 'Тып выявы не падтрымліваецца', logoTooBig: 'Выява завялікая (макс. 4 МБ)', logoOr: 'або', bookPage: 'Старонка кнігі', ratingsToggle: 'Ацэнкі супольнасці (зоркі і водгукі)', ratingsHint: 'Зоркавыя ацэнкі з кароткімі водгукамі на кожнай старонцы кнігі, агульныя для ўсіх карыстальнікаў гэтага сервера.', searchCard: 'Пошук', gsToggle: 'Шукаць адразу ва ўсіх бібліятэках', gsHint: 'Адзін аб\'яднаны спіс вынікаў па ўсіх даступных бібліятэках, з пазнакай бібліятэкі каля кожнага супадзення. Выключыце, каб вярнуць меню па бібліятэках.', gsNoResults: 'Нічога не знойдзена', gsSearching: 'Пошук…', gsBooks: 'Кнігі', gsSeries: 'Серыі', gsAuthors: 'Аўтары', spTitle: 'Пачатковая старонка', spHint: 'Заўсёды адкрываць праграму на выбраным выглядзе бібліятэкі (толькі гэты браўзер). Прамыя спасылкі ніколі не перанакіроўваюцца.', spDefault: 'Тыпова для праграмы', spViewHome: 'Галоўная', spViewLibrary: 'Бібліятэка', spViewSeries: 'Серыі', spViewCollections: 'Калекцыі', spViewAuthors: 'Аўтары', scSet: 'Усталяваць вокладку серыі', scRemove: 'Прыбраць вокладку серыі', scEditDesc: 'Рэдагаваць апісанне серыі', cardStars: 'Зоркі на вокладках кніг', hoverPolish: 'Дапрацоўка картак пры навядзенні (мякчэйшае зацямненне, акцэнтная кнопка, зоркі пры навядзенні)', sectUser: 'Вашы налады', sectAdmin: 'Адміністраванне', lockTitle: 'Пераключальнікі функцый', lockHint: 'Прымусова выключае функцыю для ўсіх карыстальнікаў гэтага сервера. Мацней за асабістыя налады; астатнія ўбачаць пасля наступнай перазагрузкі.', lockRatings: 'Выключыць ацэнкі ўсюды', lockCardRatings: 'Выключыць зоркі на вокладках', lockGlobalSearch: 'Выключыць глабальны пошук', lockHoverPolish: 'Выключыць дапрацоўку картак', lockHeroCarousel: 'Выключыць карусель на галоўнай', lockedNote: 'выключана адміністратарам', lfSort: 'Ацэнка', lfRated: 'З ацэнкай', lfUnrated: 'Без ацэнкі', lfClear: 'Скінуць', lfNone: 'Няма адпаведных кніг', lfSection: 'Ацэнка', lfWho: 'Чые ацэнкі', lfEveryone: 'Усе', sdSave: 'Захаваць', sdClear: 'Вярнуць апісанне кнігі', sdMore: 'Чытаць далей', sdLess: 'Згарнуць', shSeries: 'Серыя', shBy: '', shMore: ' і іншыя', shBookForms: ['кніга', 'кнігі', 'кніг'], shAvg: 'сярэдняе па кнігах', shRated: 'з ацэнкай', impLabel: 'Імпарт ацэнак', impHint: 'Перанясіце ацэнкі са StoryGraph або Goodreads. Экспартуйце там свае кнігі ў CSV і выберыце файл тут. Кожнае супадзенне паказваецца на пацвярджэнне, і нічога не захоўваецца, пакуль вы не пацвердзіце.', impBtn: 'Выбраць файл CSV…', impReading: 'Чытанне файла…', impMatching: 'Супастаўленне з вашымі бібліятэкамі…', impErrUnknown: 'Гэта не падобна на экспарт са StoryGraph ці Goodreads. Патрэбны іх CSV са слупком "Star Rating" або "My Rating".', impErrEmpty: 'У файле няма радкоў.', impErrNoRated: 'У файле няма кніг з ацэнкамі.', impErrRead: 'Не ўдалося прачытаць файл.', impErrLoad: 'Не ўдалося загрузіць бібліятэкі. Паспрабуйце яшчэ раз праз хвіліну.', impErrNoLibs: 'Ніводная бібліятэка не ўдзельнічае ў ацэнках, імпартаваць няма куды.', impTitle: 'Імпарт ацэнак', impFrom: 'Экспарт %s: %d кніг, з іх %d з ацэнкай', impLibs: 'Пошук у: %s', impMatched: 'Супастаўлена', impCheck: 'Варта праверыць', impCheckHint: 'Блізка, але не напэўна. Праверце супадзенне або выберыце іншае.', impAlready: 'Ужо ацэнена вамі', impAlreadyHint: 'Застануцца як ёсць, калі не ўключыць перазапіс.', impNone: 'Няма ў вашай бібліятэцы', impNoneHint: 'На гэтым серверы нішто не адпавядае гэтым запісам.', impAll: 'Усе', impSkip: 'Прапусціць', impDup: 'дублікат', impByTitle: 'назва', impYours: 'ваша: %s', impReviews: 'Імпартаваць і напісаныя водгукі (%d)', impOver: 'Перазапісаць мае існуючыя ацэнкі', impGo: 'Імпартаваць %d ацэнак', impCancel: 'Скасаваць', impCloseBtn: 'Закрыць', impWorking: 'Захаванне… %d з %d', impDone: 'Імпартавана ацэнак: %d.', impFail: 'Не ўдалося захаваць: %d.', rdTitle: 'Хто чытае гэтую кнігу', rdFin: 'Прачытана', socTitle: 'Сацыяльныя функцыі', socHint: 'Што гэты сервер дзеліць паміж карыстальнікамі. Час праслухоўвання жывіць рэйтынг; дзве іншыя раскрываюць, хто што чытае, таму выключаны, пакуль вы іх не ўключыце. Дзейнічае адразу для ўсіх.', socTime: 'Час праслухоўвання (рэйтынг сервера)', socContent: 'Топ-назвы ў агульнай статыстыцы', socWho: 'Хто чытае (старонкі кніг)', socUsers: 'Па карыстальніках', socUsersHint: 'Выключыце кагосьці, і яго даныя не будуць дзяліцца незалежна ад яго выбару; вярнуць можаце толькі вы. "Адмовіўся сам" значыць, што чалавек сам сказаў не.', socOpted: 'адмовіўся сам', srvTitle: 'Тыповыя налады сервера', srvHint: 'Захавайце бягучыя налады як тыповыя для ўсіх карыстальнікаў гэтага сервера. Кожны зможа наладзіць паверх. Змантуйце том у /data/nh кантэйнера тэмы, каб яны перажылі абнаўленні.', srvSave: 'Захаваць як тыповыя сервера', srvClear: 'Скінуць тыповыя сервера', srvSaved: 'Захавана', srvCleared: 'Скінута', srvErr: 'Не ўдалося: патрэбны ўваход адміністратара', crMode: 'Паліца Працягнуць чытанне', crCombine: 'Аб\'яднаць з каруселлю', crSeparate: 'Пакінуць асобнай паліцай', crHide: 'Схаваная', fsHint: 'Дзяліцеся сваім праслухоўваннем з іншымі на гэтым серверы: сумы часу і хвіліны па днях для рэйтынгу, а калі адміністратар уключыў, таксама вашы топ-назвы і якія кнігі вы чытаеце ці скончылі. Тыпова ўключана. Пры выключэнні ўсё апублікаванае выдаляецца.' , amfHide: 'Схаваць Пазначыць як прачытанае' , amfConfirm: 'Пазначыць гэтую кнігу як прачытаную?' , fsHint: 'Дзяліцеся зводкай свайго праслухоўвання (сумы і хвіліны за дзень) з іншымі на гэтым серверы, яна жывіць рэйтынг сервера. Тыпова ўключана. Пасля выключэння вы знікаеце з рэйтынгу і яго сум, а перададзеныя даныя выдаляюцца.' , srToggle: 'Дзяліцца тым, якія кнігі вы чытаеце', srHint: 'Дадае да таго, што бачаць іншыя, якія кнігі вы зараз чытаеце, якія скончылі і якія слухаеце часцей за ўсё. Тыпова выключана; таксама павінны быць уключаны серверныя пераключальнікі адміністратара.' , fsHint: 'Дзяліцеся часам праслухоўвання (сумы і хвіліны па днях) з іншымі на гэтым серверы; ён жывіць рэйтынг. Тыпова ўключана. Пасля выключэння іншыя карыстальнікі вас больш не бачаць; адміністратары сервера ўсё роўна бачаць актыўнасць з пазнакай адмовы.', srHint: 'Таксама дазваляе іншым бачыць, якія кнігі вы чытаеце, якія скончылі і якія слухаеце часцей за ўсё. Нічога не відаць, пакуль адміністратар не ўключыць адпаведныя серверныя пераключальнікі.', seshTitle: 'Апошнія сесіі', seshNow: 'Слухаюць зараз' , hcHint: 'Дасылайце свае адзнакі ў акаўнт Hardcover. Устаўце API-ключ з налад акаўнта Hardcover; ключы сканчаюцца праз год.', hcKeyLbl: 'API-ключ', hcToggle: 'Сінхранізаваць новыя адзнакі аўтаматычна', hcPushAll: 'Даслаць усе адзнакі', hcPushDiff: 'Даслаць толькі адрозненні', hcDone: 'Гатова: %d сінхранізавана, %d без супадзення', hcNoKey: 'Спачатку ўстаўце свой API-ключ Hardcover' , hcMissHead: 'Не знойдзена на Hardcover:' , hcHint: 'Дасылайце свае адзнакі ў акаўнт Hardcover. Устаўце API-ключ з налад акаўнта Hardcover.' , tabStyle: 'Стыль', tabMain: 'Галоўная', tabBooks: 'Кнігі', tabFeat: 'Функцыі' , hcImport: 'Імпарт з Hardcover', impMarkQ: 'Пазначыць таксама %d супастаўленых кніг як завершаныя тут?', impMarkDone: '%d пазначана як завершаныя.' , impHint: 'Перанясіце свае адзнакі з Goodreads, StoryGraph або Hardcover. Абярыце тут экспартаваны CSV; Hardcover імпартуецца наўпрост кнопкай на сваёй картцы. Кожнае супадзенне паказваецца для пацверджання перад захаваннем.' , impErrUnknown: 'Гэта не падобна на экспарт з Goodreads, StoryGraph або Hardcover: не знойдзены слупкі назвы і адзнакі.' , fsCard: 'Сацыяльнае', rnToggle: 'Паказваць маё імя побач з адзнакамі', rsToggle: 'Дзяліцца маімі адзнакамі з іншымі', socNames: 'Імёны побач з адзнакамі', rtAnon: 'Ананім' },
    bg: { cmStopped: "спряно", cmScanErrs: "{e} непроверени (грешки в Goodreads)", cmScanBusy: "Вече тече сканиране", cmGrErr: "Goodreads не отговаря, ще опитам пак", cmNoMatchYet: "Няма съвпадение в Goodreads", cmLooking: "Търся в Goodreads…", cmToolHint: "Какво мислят за тази книга читателите в Goodreads. Знакът ? показва съпоставената книга и позволява да я смените.", cmSearchPh: "Търси в Goodreads…", cmNoVotes: "още няма оценки", cmRefresh: "Обнови", cmScanAll: "Провери всичко отново, не само липсващите", cmScanDone: "Готово: {m} намерени · {x} ненамерени · {s} вече имаха", cmStop: "Спри", cmScanLibs: "Библиотеки за сканиране", cmScan: "Търси оценки", lsLongest: "Най-дългите книги", lsSize: "Размер на диска", lsDuration: "Обща продължителност", lsGenresN: "Жанрове", lsSeriesN: "Поредици", lsAuthorsN: "Автори", lsBooks: "Книги", lsGlance: "Библиотеката накратко", cmOpen: "Отвори в Goodreads", cmDetect: "Намери", cmAddr: "Адрес на помощника", scmTitle: "Страница на поредицата", scmHint: "Как изглежда генерираната корица над заглавието на поредица, когато не е качена собствена (само този браузър).", scmDeck: "Пластова колода", cmHint: "Какво мислят за книгата читателите в Goodreads: средна оценка и брой гласове, под звездите на страницата на книгата и като сортиране и филтър. Страниците на книгите се попълват сами при разглеждане.", cmGrOff: "Goodreads: не е настроено", cmSetup: "Все още няма помощник? Стартирайте това до темата (compose редове по-долу) и натиснете Намери. Всеки друг начин за стартиране на образа също става: поставете адреса му по-горе.", cmGrOn: "Goodreads: свързано", cmGrDown: "Goodreads: настроено, но не отговаря", cmGrNoNum: "Goodreads: отговаря, но тази версия на abs-tract не дава оценки (използвайте версията с оценки, вж. README)", cmTitle: "Оценки от Goodreads", cmWrong: "Грешна книга?", cmFind: "Намери оценка на общността…", cmNone: "Няма съвпадение", cmUse: "Използвай", lfCommunity: "Оценка от Goodreads", mfNatChange: "Промяна…", lsByTime: "по време на слушане", nrSearch: 'Филтър разказвачи…', nrSortName: 'Име', nrSortBooks: 'Най-много книги', nrForms: ['Разказвач', 'Разказвачи'], nrBooksForms: ['книга', 'книги'], auForms: ['Автор', 'Автори'], auSearch: 'Филтър автори…' , sbTitle: "Класация на сървъра", sbWeek: "Седмица", sbMonth: "Месец", sbYear: "Година", sbAll: "За цялото време", sbBooks: "Книги", sbTopBooks: "Най-слушани", sbTotal: "Общо", sbListeners: "Слушатели", sbAvg: "Средно", wkTitle: "Минути слушане, последните 7 дни", wkAvg: "Дневно средно", wkRow: "Дни подред", rpBadge: "Отворени сигнали за проблеми", rpMenu: "Съобщи за проблем", rpTitle: "Съобщи за проблем", rpWhat: "Какво не е наред?", rpNote: "Още нещо за администратора? (по избор)", rpSend: "Изпрати сигнал", rpSent: "Изпратено. Благодарим.", rpFail: "Неуспешно изпращане", rpMissing: "Липсващо или непълно съдържание", rpQuality: "Лошо качество на звука", rpPlay: "Не се възпроизвежда", rpWrong: "Грешна книга, корица или метаданни", rpChapters: "Грешни глави", rpOther: "Друго", rfRate: "Оцени", rfSheet: "Оцени тази книга", rfOpen: "Отвори страницата на книгата", rfPickHint: "Избери оценка", rfTitle: "Оцени завършеното", ysFinished: "Наскоро завършени", ysAlmost: "Почти готови", ysMarkDone: "Отбележи като завършена", ysEditDate: "Промени датата на завършване", ysThisWeek: "Тази седмица", ysBestDay: "Най-добър ден", ysTitle: "Твоето слушане", fsTitle: "Класация на сървъра", fsCard: "Класация на сървъра", fsToggle: "Споделяй статистиката ми за слушане", fsJoin: "Включи „Споделяй статистиката ми за слушане“ в панела за персонализация, за да се появиш в класацията на сървъра.", fsWaiting: "Споделянето е включено, твоето резюме скоро ще се присъедини към класацията на сървъра заедно с всички останали.", fsHint: "Сподели резюме на слушането си (общи суми и минути на ден) с останалите на този сървър, то захранва класацията. Включено по подразбиране. При изключване изчезваш от класацията и сумите ѝ, а споделеното се изтрива.", yirBtn: "Годината в обзор", yirTitle: "Твоята година на слушане", yirYear: "Последните 12 месеца", yirDays: "Дни със слушане", yirBest: "Най-добър ден", yirMonths: "По месеци", yirNone: "Още няма данни за слушане.", ssscmDeck: "Пластова колода", scmFirst: "Корицата на първата книга", scmGrid: "Мрежа от корици (по подразбиране)", scmOff: "Скрита" , lsTitle: "Статистика на сървъра", lsAll: "Всички библиотеки", lsPlayed: "Пуснати книги", lsBest: "Най-добре оценени", lsGenres: "Топ жанрове", srvPick: "Какво да се запази:", srvGRest: "Функции и поведение", aeBtn: "Подреди авторите", aeTitle: "Автори без книги", aeNone: "Всеки автор тук има поне една книга.", aeRemove: "Премахни", aeDone: "Премахнати", aeWorking: "Премахване…", rpCard: "Съобщени проблеми", rpHint: "Проблеми, съобщени от потребители от страница на книга. Решаването премахва записа от списъка.", rpEmpty: "Нищо не е съобщено.", rpResolve: "Реши", rpOpen: "Отвори книгата", apCard: "Възпроизвеждане", apToggle: "Автоматично пускай следващата книга от поредица", apHint: "Когато книга свърши, автоматично започва следващата от същата поредица. Само книги, които вече имаш и не си завършил.", apNext: "Следваща от поредицата", fdToggle: "Инструменти за завършени книги в статистиката", ysNoDate: "без дата", hmShowing: "графиката показва", ysStreak: "Поредица дни", ysCurrent: "Текуща", ysLongest: "Най-дълга", ysDaysTotal: "Дни общо", ysPace: "Темпо", ysPerDay: "На ден слушане", ysWhen: "Кога слушаш", ysTopBooks: "Най-слушани книги", ysTopAuthors: "Най-слушани автори", ysTopNarrators: "Най-слушани разказвачи", rfToggle: "Секция „Оцени“ на началната страница", hoTitle: "Ред на секциите на началната", hoEmpty: "Отвори началната страница веднъж и се върни тук.", hoReset: "Възстанови реда по подразбиране", ctIcon: "Икона", ctColour: "Акцент", ctNeedBook: "Audiobookshelf не може да запази празна колекция, избери поне една книга. По-късно можеш свободно да добавяш и махаш.", colEditIcon: "Смени иконата", ciTitle: "Икона на колекцията", ciReset: "Автоматично според името", colEditDesc: "Редактирай описанието", colBookForms: ["книга","книги"], colForms: ["Колекция","Колекции"], ctTitle: "Създай колекция", ctBlank: "Празна колекция", ctNew: "Нова колекция", ctNewHint: "от шаблон", ctNamePh: "Име на колекцията", ctSearch: "Търси книги или автори…", ctCreate: "Създай", ctBack: "‹ Назад", ctAddAll: "Добави всички книги", abTitle: "Добави книги", abAdd: "Добави", avRemove: "Премахни снимката", avSet: "Избери снимка…", ugListened: "Прослушано", ugPhoto: "Профилна снимка", ugNever: "Никога", ugSeen: "Последно активен", ugEdit: "Редактирай потребителя", ugDelete: "Изтрий потребителя", ugAdd: "Добави снимка", ugChange: "Смени снимката", ugSortName: "А–Я", ugSortTime: "Най-слушани", ugSortSeen: "Скоро активни", bsCard: "Връзки за книгата", bsHint: "Бутони на страницата на книгата за търсене на заглавието другаде. Goodreads и най-големият сайт на твоя език са включени предварително.", bsFind: "Търси в", bsGlobal: "Навсякъде", bsLocal: "За твоя език", bsOther: "Други страни", pgCard: "Преработени страници", pgHint: "Всяка опция заменя страница на Audiobookshelf с версията на NanoHive. Изключи, за да върнеш оригинала.", pgNarrators: "Карти на разказвачи", pgCollections: "Страници на колекции", pgUsers: "Карти на потребители (настройки)", pgStats: "Класация на слушането", pgPhoto: "Профилна снимка в горната лента", pgCinematic: "Кино фон", pgTransitions: "Преходи между страници", pSearch: "Търси настройки…", pSearchNone: "Няма съвпадения.", erTypeface: "Шрифт", erPage: "Тема на страницата", erText: "Цвят на текста", erBg: "Фон", erDefault: "По подразбиране (ABS)", erAuto: "Авто" , lfByAuthor: "Автор › ред на поредицата", lfBySeries: "Поредица › ред" , lfSecFilters: "Още филтри", lfSecSort: "Многостепенно подреждане", lfAuthor: "Автор", lfSeries: "Поредица", lfTitle: "Заглавие", lfYear: "Година", lfAdded: "Добавено", lfDuration: "Времетраене", lfNarrator: "Разказвач", lfGenre: "Жанр", lfLanguage: "Език", lfProgress: "Напредък", lfPgFinished: "Завършени", lfPgInProgress: "В процес", lfPgNot: "Незапочнати" , logoUpload: 'Качване от устройството…', logoUploaded: 'Качено ✓', logoBadType: 'Неподдържан тип изображение', logoTooBig: 'Изображението е твърде голямо (макс. 4 МБ)', logoOr: 'или', bookPage: 'Страница на книгата', ratingsToggle: 'Оценки на общността (звезди и ревюта)', ratingsHint: 'Звездни оценки с кратки ревюта на всяка страница на книга, споделени с всички потребители на този сървър.', searchCard: 'Търсене', gsToggle: 'Търсене във всички библиотеки наведнъж', gsHint: 'Един обединен списък с резултати от всички достъпни библиотеки, със значка на библиотеката при всяко съвпадение. Изключете, за да върнете менюто по библиотеки.', gsNoResults: 'Няма резултати', gsSearching: 'Търсене…', gsBooks: 'Книги', gsSeries: 'Поредици', gsAuthors: 'Автори', spTitle: 'Начална страница', spHint: 'Отваряйте приложението винаги на избран изглед на библиотека (само този браузър). Директните връзки никога не се пренасочват.', spDefault: 'По подразбиране', spViewHome: 'Начало', spViewLibrary: 'Библиотека', spViewSeries: 'Поредици', spViewCollections: 'Колекции', spViewAuthors: 'Автори', scSet: 'Задай корица на поредицата', scRemove: 'Премахни корицата на поредицата', scEditDesc: 'Редактирай описанието на поредицата', cardStars: 'Звезди върху кориците', hoverPolish: 'Финес при посочване на картите (по-меко затъмняване, акцентен бутон, звезди при посочване)', sectUser: 'Вашите настройки', sectAdmin: 'Администрация', lockTitle: 'Превключватели на функции', lockHint: 'Принудително изключва функция за всички потребители на този сървър. По-силно от личните настройки; останалите ще видят след следващото презареждане.', lockRatings: 'Изключи оценките навсякъде', lockCardRatings: 'Изключи звездите върху кориците', lockGlobalSearch: 'Изключи глобалното търсене', lockHoverPolish: 'Изключи финеса на картите', lockHeroCarousel: 'Изключи въртележката на началната', lockedNote: 'изключено от администратора', lfSort: 'Оценка', lfRated: 'С оценка', lfUnrated: 'Без оценка', lfClear: 'Изчисти', lfNone: 'Няма съвпадащи книги', lfSection: 'Оценка', lfWho: 'Чии оценки', lfEveryone: 'Всички', sdSave: 'Запази', sdClear: 'Върни описанието на книгата', sdMore: 'Прочети още', sdLess: 'Покажи по-малко', shSeries: 'Поредица', shBy: 'от ', shMore: ' и още', shBookForms: ['книга', 'книги'], shAvg: 'средно за книгите', shRated: 'оценени', impLabel: 'Импорт на оценки', impHint: 'Пренесете оценките си от StoryGraph или Goodreads. Експортирайте там книгите си като CSV и изберете файла тук. Всяко съвпадение се показва за потвърждение и нищо не се записва, докато не потвърдите.', impBtn: 'Избери CSV файл…', impReading: 'Четене на файла…', impMatching: 'Сравняване с вашите библиотеки…', impErrUnknown: 'Това не прилича на експорт от StoryGraph или Goodreads. Трябва да е техният CSV с колона "Star Rating" или "My Rating".', impErrEmpty: 'Файлът няма редове.', impErrNoRated: 'Във файла няма оценени книги.', impErrRead: 'Файлът не можа да бъде прочетен.', impErrLoad: 'Библиотеките не можаха да се заредят. Опитайте пак след малко.', impErrNoLibs: 'Никоя библиотека не участва в оценките, така че няма къде да се импортира.', impTitle: 'Импорт на оценки', impFrom: 'Експорт от %s: %d книги, %d от тях оценени', impLibs: 'Търсене в: %s', impMatched: 'Съвпаднали', impCheck: 'Заслужава проверка', impCheckHint: 'Близко, но несигурно. Проверете съвпадението или изберете друго.', impAlready: 'Вече оценени от вас', impAlreadyHint: 'Остават непокътнати, освен ако не изберете презаписване.', impNone: 'Не са във вашата библиотека', impNoneHint: 'Нищо на този сървър не съвпада с тези.', impAll: 'Всички', impSkip: 'Пропусни този', impDup: 'дубликат', impByTitle: 'заглавие', impYours: 'вашата: %s', impReviews: 'Импортирай и написаните ревюта (%d)', impOver: 'Презапиши оценките, които вече имам', impGo: 'Импортирай %d оценки', impCancel: 'Отказ', impCloseBtn: 'Затвори', impWorking: 'Записване… %d от %d', impDone: 'Импортирани %d оценки.', impFail: '%d не можаха да се запишат.', rdTitle: 'Кой чете тази книга', rdFin: 'Завършена', socTitle: 'Социални функции', socHint: 'Какво споделя този сървър между потребителите. Времето на слушане захранва класацията; другите две разкриват какво чете всеки, затова остават изключени, докато не ги включите. Важи за всички наведнъж.', socTime: 'Време на слушане (класация на сървъра)', socContent: 'Топ заглавия в споделената статистика', socWho: 'Кой чете (страници на книгите)', socUsers: 'По потребители', socUsersHint: 'Изключите ли някого, нищо негово не се споделя независимо от неговия избор; само вие можете да го върнете. "Отказал се сам" значи, че човекът сам е казал не.', socOpted: 'отказал се сам', srvTitle: 'Настройки по подразбиране на сървъра', srvHint: 'Запазете текущите си настройки като подразбиране за всички потребители на този сървър. Всеки може да персонализира отгоре. Монтирайте том на /data/nh в контейнера на темата, за да оцелеят при обновявания.', srvSave: 'Запази като подразбиране', srvClear: 'Изчисти подразбирането', srvSaved: 'Запазено', srvCleared: 'Изчистено', srvErr: 'Неуспех: изисква се вход като администратор', crMode: 'Рафт Продължи четенето', crCombine: 'Обедини с въртележката', crSeparate: 'Запази като отделен рафт', crHide: 'Скрит', fsHint: 'Споделяйте слушането си с останалите на този сървър: общо време и минути по дни за класацията, а ако администраторът ги е включил, и вашите топ заглавия и кои книги четете или сте завършили. Включено по подразбиране. При изключване всичко споделено се изтрива.' , amfHide: 'Скрий Отбележи като завършена' , amfConfirm: 'Да се отбележи ли тази книга като завършена?' , fsHint: 'Сподели резюме на слушането си (общи суми и минути на ден) с останалите на този сървър, то захранва класацията. Включено по подразбиране. При изключване изчезваш от класацията и сумите ѝ, а споделеното се изтрива.' , srToggle: 'Споделяне кои книги четете', srHint: 'Добавя към това, което другите виждат, кои книги четете в момента, кои сте завършили и кои слушате най-често. Изключено по подразбиране; трябва да са включени и сървърните превключватели на администратора.' , fsHint: 'Споделяйте времето си на слушане (общо и минути по дни) с останалите на този сървър; то захранва класацията. Включено по подразбиране. При изключване другите потребители вече не ви виждат; администраторите на сървъра продължават да виждат активността, отбелязана като отказана.', srHint: 'Позволява също другите да виждат кои книги четете, кои сте завършили и кои слушате най-често. Нищо не се показва, докато администраторът не включи съответните сървърни превключватели.', seshTitle: 'Последни сесии', seshNow: 'Слушат сега' , hcHint: 'Изпращайте оценките си към акаунта в Hardcover. Поставете API ключа от настройките на акаунта в Hardcover; ключовете изтичат след година.', hcKeyLbl: 'API ключ', hcToggle: 'Синхронизирай новите оценки автоматично', hcPushAll: 'Изпрати всички оценки', hcPushDiff: 'Изпрати само разликите', hcDone: 'Готово: %d синхронизирани, %d без съвпадение', hcNoKey: 'Първо поставете своя API ключ за Hardcover' , hcMissHead: 'Не е намерено в Hardcover:' , hcHint: 'Изпращайте оценките си към акаунта в Hardcover. Поставете API ключа от настройките на акаунта в Hardcover.' , tabStyle: 'Стил', tabMain: 'Начало', tabBooks: 'Книги', tabFeat: 'Функции' , hcImport: 'Импортиране от Hardcover', impMarkQ: 'Да се отбележат ли и тук %d съвпаднали книги като завършени?', impMarkDone: '%d отбелязани като завършени.' , impHint: 'Пренесете оценките си от Goodreads, StoryGraph или Hardcover. Изберете тук експортирания CSV; Hardcover се импортира директно с бутона на своята карта. Всяко съвпадение се показва за потвърждение, преди нещо да се запази.' , impErrUnknown: 'Това не прилича на експорт от Goodreads, StoryGraph или Hardcover: не са намерени колони за заглавие и оценка.' , fsCard: 'Социално', rnToggle: 'Показвай името ми при оценките', rsToggle: 'Споделяй оценките ми с другите', socNames: 'Имена при оценките', rtAnon: 'Анонимен' },
    hr: { cmStopped: "zaustavljeno", cmScanErrs: "{e} neprovjereno (Goodreads greške)", cmScanBusy: "Pretraga je već u tijeku", cmGrErr: "Goodreads nedostupan, pokušat ću ponovno", cmNoMatchYet: "Nema podudaranja na Goodreadsu", cmLooking: "Tražim na Goodreadsu…", cmToolHint: "Što čitatelji Goodreadsa misle o ovoj knjizi. Znak ? prikazuje uparenu knjigu i omogućuje promjenu.", cmSearchPh: "Pretraži Goodreads…", cmNoVotes: "još nema ocjena", cmRefresh: "Osvježi", cmScanAll: "Ponovno provjeri sve, ne samo ono što nedostaje", cmScanDone: "Gotovo: {m} pronađeno · {x} nije pronađeno · {s} već imalo", cmStop: "Zaustavi", cmScanLibs: "Knjižnice za pretragu", cmScan: "Traži ocjene", lsLongest: "Najduže knjige", lsSize: "Veličina na disku", lsDuration: "Ukupno trajanje", lsGenresN: "Žanrovi", lsSeriesN: "Serijali", lsAuthorsN: "Autori", lsBooks: "Knjige", lsGlance: "Knjižnica na prvi pogled", cmOpen: "Otvori na Goodreadsu", cmDetect: "Pronađi", cmAddr: "Adresa pomoćnika", scmTitle: "Stranica serijala", scmHint: "Kako izgleda generirana naslovnica iznad naslova serijala kad vlastita nije učitana (samo ovaj preglednik).", scmDeck: "Slojeviti špil", cmHint: "Što čitatelji Goodreadsa misle o knjizi: prosječna ocjena i broj glasova, ispod zvjezdica na stranici knjige te kao sortiranje i filtar. Stranice knjiga popunjavaju se same tijekom pregledavanja.", cmGrOff: "Goodreads: nije postavljeno", cmSetup: "Još nema pomoćnika? Pokrenite ovo uz temu (compose retci ispod) pa pritisnite Pronađi. Radi i bilo koji drugi način pokretanja slike: zalijepite njezinu adresu iznad.", cmGrOn: "Goodreads: povezano", cmGrDown: "Goodreads: postavljeno, ali ne odgovara", cmGrNoNum: "Goodreads: odgovara, ali ova verzija abs-tracta ne daje ocjene (koristite verziju s ocjenama, vidi README)", cmTitle: "Goodreads ocjene", cmWrong: "Pogrešna knjiga?", cmFind: "Pronađi ocjenu zajednice…", cmNone: "Nema podudaranja", cmUse: "Koristi ovo", lfCommunity: "Goodreads ocjena", mfNatChange: "Promijeni…", lsByTime: "po vremenu slušanja", nrSearch: 'Filtriraj pripovjedače…', nrSortName: 'Ime', nrSortBooks: 'Najviše knjiga', nrForms: ['Pripovjedač', 'Pripovjedača', 'Pripovjedača'], nrBooksForms: ['knjiga', 'knjige', 'knjiga'], auForms: ['Autor', 'Autora', 'Autora'], auSearch: 'Filtriraj autore…' , sbTitle: "Poredak poslužitelja", sbWeek: "Tjedan", sbMonth: "Mjesec", sbYear: "Godina", sbAll: "Sve vrijeme", sbBooks: "Knjige", sbTopBooks: "Najslušanije", sbTotal: "Ukupno", sbListeners: "Slušatelji", sbAvg: "Prosjek", wkTitle: "Minute slušanja, zadnjih 7 dana", wkAvg: "Dnevni prosjek", wkRow: "Dana zaredom", rpBadge: "Otvorene prijave problema", rpMenu: "Prijavi problem", rpTitle: "Prijavi problem", rpWhat: "Što ne valja?", rpNote: "Još nešto za administratora? (neobavezno)", rpSend: "Pošalji prijavu", rpSent: "Poslano. Hvala.", rpFail: "Slanje nije uspjelo", rpMissing: "Nedostaje ili je nepotpun sadržaj", rpQuality: "Loša kvaliteta zvuka", rpPlay: "Ne reproducira se", rpWrong: "Kriva knjiga, naslovnica ili metapodaci", rpChapters: "Kriva poglavlja", rpOther: "Nešto drugo", rfRate: "Ocijeni", rfSheet: "Ocijeni ovu knjigu", rfOpen: "Otvori stranicu knjige", rfPickHint: "Odaberi ocjenu", rfTitle: "Ocijeni što si završio", ysFinished: "Nedavno završeno", ysAlmost: "Gotovo završeno", ysMarkDone: "Označi kao završeno", ysEditDate: "Promijeni datum završetka", ysThisWeek: "Ovaj tjedan", ysBestDay: "Najbolji dan", ysTitle: "Tvoje slušanje", fsTitle: "Poredak poslužitelja", fsCard: "Poredak poslužitelja", fsToggle: "Dijeli moju statistiku slušanja", fsJoin: "Uključi „Dijeli moju statistiku slušanja“ u ploči prilagodbe da se pojaviš u poretku poslužitelja.", fsWaiting: "Dijeljenje je uključeno, tvoj sažetak uskoro ulazi u poredak poslužitelja zajedno sa svima ostalima.", fsHint: "Podijeli sažetak svog slušanja (ukupno i minute po danu) s ostalima na ovom poslužitelju, on pokreće poredak. Uključeno prema zadanome. Isključivanjem nestaješ iz poretka i njegovih zbrojeva, a podijeljeno se briše.", yirBtn: "Godina u pregledu", yirTitle: "Tvoja godina slušanja", yirYear: "Zadnjih 12 mjeseci", yirDays: "Dani sa slušanjem", yirBest: "Najbolji dan", yirMonths: "Po mjesecima", yirNone: "Još nema podataka o slušanju.", ssscmDeck: "Slojeviti špil", scmFirst: "Naslovnica prve knjige", scmGrid: "Mreža naslovnica (zadano)", scmOff: "Skrivena" , lsTitle: "Statistika poslužitelja", lsAll: "Sve knjižnice", lsPlayed: "Reproducirane knjige", lsBest: "Najbolje ocijenjene", lsGenres: "Top žanrovi", srvPick: "Što spremiti:", srvGRest: "Funkcije i ponašanje", aeBtn: "Uredi autore", aeTitle: "Autori bez knjiga", aeNone: "Svaki autor ovdje ima barem jednu knjigu.", aeRemove: "Ukloni", aeDone: "Uklonjeno", aeWorking: "Uklanjanje…", rpCard: "Prijavljeni problemi", rpHint: "Problemi koje su korisnici prijavili sa stranice knjige. Rješavanje uklanja prijavu s popisa.", rpEmpty: "Ništa nije prijavljeno.", rpResolve: "Riješi", rpOpen: "Otvori knjigu", apCard: "Reprodukcija", apToggle: "Automatski reproduciraj sljedeću knjigu serijala", apHint: "Kad knjiga završi, automatski počinje sljedeća iz istog serijala. Samo knjige koje već imaš i nisi ih dovršio.", apNext: "Sljedeća u serijalu", fdToggle: "Alati za dovršene knjige u statistici", ysNoDate: "bez datuma", hmShowing: "grafikon prikazuje", ysStreak: "Niz", ysCurrent: "Trenutni", ysLongest: "Najduži", ysDaysTotal: "Dana ukupno", ysPace: "Tempo", ysPerDay: "Po danu slušanja", ysWhen: "Kada slušaš", ysTopBooks: "Najslušanije knjige", ysTopAuthors: "Najslušaniji autori", ysTopNarrators: "Najslušaniji pripovjedači", rfToggle: "Odjeljak „Ocijeni“ na početnoj", hoTitle: "Redoslijed odjeljaka početne", hoEmpty: "Otvori početnu jednom pa se vrati ovamo.", hoReset: "Vrati zadani redoslijed", ctIcon: "Ikona", ctColour: "Naglasak", ctNeedBook: "Audiobookshelf ne može spremiti praznu zbirku, odaberi barem jednu knjigu. Poslije možeš slobodno dodavati i uklanjati.", colEditIcon: "Promijeni ikonu", ciTitle: "Ikona zbirke", ciReset: "Automatski prema nazivu", colEditDesc: "Uredi opis", colBookForms: ["knjiga","knjige","knjiga"], colForms: ["Zbirka","Zbirke","Zbirki"], ctTitle: "Stvori zbirku", ctBlank: "Prazna zbirka", ctNew: "Nova zbirka", ctNewHint: "iz predloška", ctNamePh: "Naziv zbirke", ctSearch: "Traži knjige ili autore…", ctCreate: "Stvori", ctBack: "‹ Natrag", ctAddAll: "Dodaj sve knjige", abTitle: "Dodaj knjige", abAdd: "Dodaj", avRemove: "Ukloni fotografiju", avSet: "Odaberi fotografiju…", ugListened: "Preslušano", ugPhoto: "Profilna fotografija", ugNever: "Nikad", ugSeen: "Zadnje aktivan", ugEdit: "Uredi korisnika", ugDelete: "Izbriši korisnika", ugAdd: "Dodaj fotografiju", ugChange: "Promijeni fotografiju", ugSortName: "A–Ž", ugSortTime: "Najviše preslušano", ugSortSeen: "Nedavno aktivni", bsCard: "Poveznice knjige", bsHint: "Gumbi na stranici knjige za traženje naslova drugdje. Goodreads i najveća stranica tvog jezika uključeni su unaprijed.", bsFind: "Traži na", bsGlobal: "Svugdje", bsLocal: "Za tvoj jezik", bsOther: "Druge zemlje", pgCard: "Redizajnirane stranice", pgHint: "Svaka opcija zamjenjuje stranicu Audiobookshelfa NanoHive verzijom. Isključi za povratak izvorne.", pgNarrators: "Kartice pripovjedača", pgCollections: "Stranice zbirki", pgUsers: "Kartice korisnika (postavke)", pgStats: "Poredak slušanja", pgPhoto: "Profilna fotografija u gornjoj traci", pgCinematic: "Filmska pozadina", pgTransitions: "Prijelazi stranica", pSearch: "Traži postavke…", pSearchNone: "Nema podudaranja.", erTypeface: "Pismo", erPage: "Tema stranice", erText: "Boja teksta", erBg: "Pozadina", erDefault: "Zadano (ABS)", erAuto: "Automatski" , lfByAuthor: "Autor › redoslijed serijala", lfBySeries: "Serijal › redoslijed" , lfSecFilters: "Više filtara", lfSecSort: "Višerazinsko sortiranje", lfAuthor: "Autor", lfSeries: "Serijal", lfTitle: "Naslov", lfYear: "Godina", lfAdded: "Dodano", lfDuration: "Trajanje", lfNarrator: "Pripovjedač", lfGenre: "Žanr", lfLanguage: "Jezik", lfProgress: "Napredak", lfPgFinished: "Dovršene", lfPgInProgress: "U tijeku", lfPgNot: "Nezapočete" , logoUpload: 'Prenesi s uređaja…', logoUploaded: 'Preneseno ✓', logoBadType: 'Nepodržana vrsta slike', logoTooBig: 'Slika je prevelika (maks. 4 MB)', logoOr: 'ili', bookPage: 'Stranica knjige', ratingsToggle: 'Ocjene zajednice (zvjezdice i recenzije)', ratingsHint: 'Zvjezdane ocjene s kratkim recenzijama na svakoj stranici knjige, zajedničke svim korisnicima ovog poslužitelja.', searchCard: 'Pretraga', gsToggle: 'Pretraži sve knjižnice odjednom', gsHint: 'Jedan objedinjeni popis rezultata iz svih dostupnih knjižnica, sa značkom knjižnice na svakom pogotku. Isključi za povratak izbornika po knjižnicama.', gsNoResults: 'Nema rezultata', gsSearching: 'Pretraživanje…', gsBooks: 'Knjige', gsSeries: 'Serijali', gsAuthors: 'Autori', spTitle: 'Početna stranica', spHint: 'Uvijek otvaraj aplikaciju na odabranom prikazu knjižnice (samo ovaj preglednik). Izravne poveznice nikad se ne preusmjeravaju.', spDefault: 'Zadano aplikacije', spViewHome: 'Početna', spViewLibrary: 'Knjižnica', spViewSeries: 'Serijali', spViewCollections: 'Zbirke', spViewAuthors: 'Autori', scSet: 'Postavi naslovnicu serijala', scRemove: 'Ukloni naslovnicu serijala', scEditDesc: 'Uredi opis serijala', cardStars: 'Zvjezdice na naslovnicama', hoverPolish: 'Dorada kartica pri prelasku (blaže zatamnjenje, naglašena tipka, zvjezdice pri prelasku)', sectUser: 'Tvoje postavke', sectAdmin: 'Administracija', lockTitle: 'Prekidači funkcija', lockHint: 'Prisilno isključuje funkciju za sve korisnike ovog poslužitelja. Jače od osobnih postavki; ostali vide nakon sljedećeg učitavanja.', lockRatings: 'Isključi ocjene posvuda', lockCardRatings: 'Isključi zvjezdice na naslovnicama', lockGlobalSearch: 'Isključi globalnu pretragu', lockHoverPolish: 'Isključi doradu kartica', lockHeroCarousel: 'Isključi vrtuljak početne', lockedNote: 'isključio administrator', lfSort: 'Ocjena', lfRated: 'Ocijenjene', lfUnrated: 'Neocijenjene', lfClear: 'Očisti', lfNone: 'Nijedna knjiga ne odgovara', lfSection: 'Ocjena', lfWho: 'Čije ocjene', lfEveryone: 'Svi', sdSave: 'Spremi', sdClear: 'Vrati opis knjige', sdMore: 'Pročitaj više', sdLess: 'Prikaži manje', shSeries: 'Serijal', shBy: 'od ', shMore: ' i više', shBookForms: ['knjiga', 'knjige', 'knjiga'], shAvg: 'prosjek knjiga', shRated: 'ocijenjeno', impLabel: 'Uvoz ocjena', impHint: 'Prenesi ocjene sa StoryGrapha ili Goodreadsa. Izvezi ondje svoje knjige kao CSV i odaberi datoteku ovdje. Svako podudaranje prikazuje se na potvrdu i ništa se ne sprema dok ne potvrdiš.', impBtn: 'Odaberi CSV datoteku…', impReading: 'Čitanje datoteke…', impMatching: 'Uspoređivanje s tvojim knjižnicama…', impErrUnknown: 'Ovo ne izgleda kao izvoz iz StoryGrapha ili Goodreadsa. Treba biti njihov CSV sa stupcem "Star Rating" ili "My Rating".', impErrEmpty: 'Datoteka nema redaka.', impErrNoRated: 'U datoteci nema ocijenjenih knjiga.', impErrRead: 'Datoteku nije moguće pročitati.', impErrLoad: 'Knjižnice nije moguće učitati. Pokušaj ponovno za trenutak.', impErrNoLibs: 'Nijedna knjižnica ne sudjeluje u ocjenama, pa nema kamo uvesti.', impTitle: 'Uvoz ocjena', impFrom: '%s izvoz: %d knjiga, od toga %d ocijenjenih', impLibs: 'Pretražujem: %s', impMatched: 'Uparene', impCheck: 'Vrijedi provjeriti', impCheckHint: 'Blizu, ali nesigurno. Provjeri podudaranje ili odaberi drugo.', impAlready: 'Već si ih ocijenio/la', impAlreadyHint: 'Ostaju netaknute, osim ako ne zatražiš prepisivanje.', impNone: 'Nisu u tvojoj knjižnici', impNoneHint: 'Ništa na ovom poslužitelju ne odgovara ovima.', impAll: 'Sve', impSkip: 'Preskoči ovu', impDup: 'duplikat', impByTitle: 'naslov', impYours: 'tvoja: %s', impReviews: 'Uvezi i pisane recenzije (%d)', impOver: 'Prepiši ocjene koje već imam', impGo: 'Uvezi %d ocjena', impCancel: 'Odustani', impCloseBtn: 'Zatvori', impWorking: 'Spremanje… %d od %d', impDone: 'Uvezeno %d ocjena.', impFail: '%d nije moguće spremiti.', rdTitle: 'Tko čita ovu knjigu', rdFin: 'Završeno', socTitle: 'Društvene funkcije', socHint: 'Što ovaj poslužitelj dijeli među korisnicima. Vrijeme slušanja pokreće ljestvicu; druge dvije otkrivaju što tko čita, pa ostaju isključene dok ih ne uključiš. Vrijedi za sve odjednom.', socTime: 'Vrijeme slušanja (ljestvica poslužitelja)', socContent: 'Najslušaniji naslovi u dijeljenoj statistici', socWho: 'Tko čita (stranice knjiga)', socUsers: 'Po korisniku', socUsersHint: 'Isključiš li nekoga, ništa se njegovo ne dijeli bez obzira na njegov izbor; samo ti to možeš vratiti. "Sam se odjavio" znači da je osoba sama rekla ne.', socOpted: 'sam se odjavio', srvTitle: 'Zadane postavke poslužitelja', srvHint: 'Spremi svoje trenutačne postavke kao zadane za sve korisnike ovog poslužitelja. Svatko i dalje može prilagoditi svoje. Montiraj volumen na /data/nh u kontejneru teme da prežive ažuriranja.', srvSave: 'Spremi kao zadano poslužitelja', srvClear: 'Obriši zadano poslužitelja', srvSaved: 'Spremljeno', srvCleared: 'Obrisano', srvErr: 'Neuspjeh: potrebna administratorska prijava', crMode: 'Polica Nastavi čitati', crCombine: 'Spoji s vrtuljkom', crSeparate: 'Zadrži kao zasebnu policu', crHide: 'Skrivena', fsHint: 'Dijeli svoje slušanje s ostalima na ovom poslužitelju: zbrojevi vremena i minute po danima za ljestvicu, a ako ih je administrator uključio, i tvoji najslušaniji naslovi te koje knjige čitaš ili si završio/la. Uključeno prema zadanom. Isključivanjem se sve podijeljeno briše.' , amfHide: 'Sakrij Označi kao završeno' , amfConfirm: 'Označiti ovu knjigu kao završenu?' , fsHint: 'Podijeli sažetak svog slušanja (ukupno i minute po danu) s ostalima na ovom poslužitelju, on pokreće poredak. Uključeno prema zadanome. Isključivanjem nestaješ iz poretka i njegovih zbrojeva, a podijeljeno se briše.' , srToggle: 'Dijeli koje knjige čitaš', srHint: 'Dodaje onome što drugi vide koje knjige upravo čitaš, koje si završio/la i koje najviše slušaš. Isključeno prema zadanom; moraju biti uključeni i poslužiteljski prekidači administratora.' , fsHint: 'Dijeli svoje vrijeme slušanja (zbrojevi i minute po danima) s ostalima na ovom poslužitelju; ono pokreće ljestvicu. Uključeno prema zadanom. Isključiš li, drugi korisnici te više ne vide; administratori poslužitelja i dalje vide aktivnost, označenu kao odjavljenu.', srHint: 'Omogućuje drugima i da vide koje knjige upravo čitaš, koje si završio/la i koje najviše slušaš. Ništa se ne prikazuje dok administrator ne uključi odgovarajuće poslužiteljske prekidače.', seshTitle: 'Nedavne sesije', seshNow: 'Upravo slušaju' , hcHint: 'Šaljite svoje ocjene na Hardcover račun. Zalijepite API ključ iz postavki Hardcover računa; ključevi istječu nakon godinu dana.', hcKeyLbl: 'API ključ', hcToggle: 'Automatski sinkroniziraj nove ocjene', hcPushAll: 'Pošalji sve ocjene', hcPushDiff: 'Pošalji samo razlike', hcDone: 'Gotovo: %d sinkronizirano, %d bez podudaranja', hcNoKey: 'Najprije zalijepite svoj Hardcover API ključ' , hcMissHead: 'Nije pronađeno na Hardcoveru:' , hcHint: 'Šaljite svoje ocjene na Hardcover račun. Zalijepite API ključ iz postavki Hardcover računa.' , tabStyle: 'Stil', tabMain: 'Početna', tabBooks: 'Knjige', tabFeat: 'Funkcije' , hcImport: 'Uvezi iz Hardcovera', impMarkQ: 'Označiti i ovdje %d podudarenih knjiga kao završene?', impMarkDone: '%d označeno kao završeno.' , impHint: 'Prenesite svoje ocjene iz Goodreadsa, StoryGrapha ili Hardcovera. Ovdje odaberite izvezeni CSV; Hardcover se uvozi izravno gumbom na svojoj kartici. Svako podudaranje prikazuje se na potvrdu prije spremanja.' , impErrUnknown: 'Ovo ne izgleda kao izvoz iz Goodreadsa, StoryGrapha ili Hardcovera: nisu pronađeni stupci naslova i ocjene.' , fsCard: 'Društveno', rnToggle: 'Prikaži moje ime uz ocjene', rsToggle: 'Dijeli moje ocjene s drugima', socNames: 'Imena uz ocjene', rtAnon: 'Anonimno' },
    sl: { cmStopped: "ustavljeno", cmScanErrs: "{e} nepreverjeno (napake Goodreads)", cmScanBusy: "Pregled že poteka", cmGrErr: "Goodreads ni dosegljiv, poskusim znova", cmNoMatchYet: "Ni ujemanja na Goodreads", cmLooking: "Iščem na Goodreads…", cmToolHint: "Kaj o tej knjigi menijo bralci Goodreadsa. Vprašaj prikaže ujemajočo knjigo in omogoča spremembo.", cmSearchPh: "Išči na Goodreads…", cmNoVotes: "še ni ocen", cmRefresh: "Osveži", cmScanAll: "Znova preveri vse, ne le manjkajočih", cmScanDone: "Končano: {m} najdenih · {x} ni najdenih · {s} že imelo", cmStop: "Ustavi", cmScanLibs: "Knjižnice za pregled", cmScan: "Poišči ocene", lsLongest: "Najdaljše knjige", lsSize: "Velikost na disku", lsDuration: "Skupno trajanje", lsGenresN: "Žanri", lsSeriesN: "Serije", lsAuthorsN: "Avtorji", lsBooks: "Knjige", lsGlance: "Knjižnica na prvi pogled", cmOpen: "Odpri na Goodreads", cmDetect: "Poišči", cmAddr: "Naslov pomočnika", scmTitle: "Stran serije", scmHint: "Kako je videti ustvarjena naslovnica nad naslovom serije, ko lastna ni naložena (samo ta brskalnik).", scmDeck: "Plastnat komplet", cmHint: "Kaj o knjigi menijo bralci Goodreadsa: povprečna ocena in število glasov, pod zvezdicami na strani knjige ter kot razvrščanje in filter. Strani knjig se izpolnijo same med brskanjem.", cmGrOff: "Goodreads: ni nastavljeno", cmSetup: "Še ni pomočnika? Zaženite to ob temi (vrstice compose spodaj) in pritisnite Poišči. Deluje tudi vsak drug način zagona slike: prilepite njen naslov zgoraj.", cmGrOn: "Goodreads: povezano", cmGrDown: "Goodreads: nastavljeno, a se ne odziva", cmGrNoNum: "Goodreads: se odziva, a ta različica abs-tract ne daje ocen (uporabite različico z ocenami, glej README)", cmTitle: "Ocene Goodreads", cmWrong: "Napačna knjiga?", cmFind: "Poišči oceno skupnosti…", cmNone: "Ni ujemanja", cmUse: "Uporabi to", lfCommunity: "Ocena Goodreads", mfNatChange: "Spremeni…", lsByTime: "po času poslušanja", nrSearch: 'Filtriraj pripovedovalce…', nrSortName: 'Ime', nrSortBooks: 'Največ knjig', nrForms: ['Pripovedovalec', 'Pripovedovalca', 'Pripovedovalcev'], nrBooksForms: ['knjiga', 'knjigi', 'knjig'], auForms: ['Avtor', 'Avtorja', 'Avtorjev'], auSearch: 'Filtriraj avtorje…' , sbTitle: "Lestvica strežnika", sbWeek: "Teden", sbMonth: "Mesec", sbYear: "Leto", sbAll: "Ves čas", sbBooks: "Knjige", sbTopBooks: "Najbolj poslušane", sbTotal: "Skupaj", sbListeners: "Poslušalci", sbAvg: "Povprečje", wkTitle: "Minute poslušanja, zadnjih 7 dni", wkAvg: "Dnevno povprečje", wkRow: "Dni zapored", rpBadge: "Odprte prijave težav", rpMenu: "Prijavi težavo", rpTitle: "Prijavi težavo", rpWhat: "Kaj je narobe?", rpNote: "Še kaj za skrbnika? (neobvezno)", rpSend: "Pošlji prijavo", rpSent: "Poslano. Hvala.", rpFail: "Pošiljanje ni uspelo", rpMissing: "Manjkajoča ali nepopolna vsebina", rpQuality: "Slaba kakovost zvoka", rpPlay: "Se ne predvaja", rpWrong: "Napačna knjiga, naslovnica ali metapodatki", rpChapters: "Napačna poglavja", rpOther: "Nekaj drugega", rfRate: "Oceni", rfSheet: "Oceni to knjigo", rfOpen: "Odpri stran knjige", rfPickHint: "Izberi oceno", rfTitle: "Oceni, kar si končal", ysFinished: "Nedavno končane", ysAlmost: "Skoraj končane", ysMarkDone: "Označi kot končano", ysEditDate: "Spremeni datum konca", ysThisWeek: "Ta teden", ysBestDay: "Najboljši dan", ysTitle: "Tvoje poslušanje", fsTitle: "Lestvica strežnika", fsCard: "Lestvica strežnika", fsToggle: "Deli mojo statistiko poslušanja", fsJoin: "Vklopi »Deli mojo statistiko poslušanja« v plošči prilagoditev, da se pojaviš na lestvici strežnika.", fsWaiting: "Deljenje je vklopljeno, tvoj povzetek se kmalu pridruži lestvici strežnika skupaj z ostalimi.", fsHint: "Deli povzetek svojega poslušanja (skupno in minute na dan) z drugimi na tem strežniku, poganja lestvico strežnika. Privzeto vklopljeno. Ob izklopu izgineš z lestvice in njenih seštevkov, deljeno pa se izbriše.", yirBtn: "Leto v pregledu", yirTitle: "Tvoje leto poslušanja", yirYear: "Zadnjih 12 mesecev", yirDays: "Dnevi s poslušanjem", yirBest: "Najboljši dan", yirMonths: "Po mesecih", yirNone: "Podatkov o poslušanju še ni.", ssscmDeck: "Plastnat komplet", scmFirst: "Naslovnica prve knjige", scmGrid: "Mreža naslovnic (privzeto)", scmOff: "Skrita" , lsTitle: "Statistika strežnika", lsAll: "Vse knjižnice", lsPlayed: "Predvajane knjige", lsBest: "Najbolje ocenjene", lsGenres: "Top žanri", srvPick: "Kaj shraniti:", srvGRest: "Funkcije in vedenje", aeBtn: "Pospravi avtorje", aeTitle: "Avtorji brez knjig", aeNone: "Vsak avtor tukaj ima vsaj eno knjigo.", aeRemove: "Odstrani", aeDone: "Odstranjeno", aeWorking: "Odstranjevanje…", rpCard: "Prijavljene težave", rpHint: "Težave, ki so jih uporabniki prijavili s strani knjige. Rešitev odstrani prijavo s seznama.", rpEmpty: "Nič ni prijavljeno.", rpResolve: "Reši", rpOpen: "Odpri knjigo", apCard: "Predvajanje", apToggle: "Samodejno predvajaj naslednjo knjigo serije", apHint: "Ko se knjiga konča, se samodejno začne naslednja iz iste serije. Samo knjige, ki jih že imaš in jih nisi dokončal.", apNext: "Naslednja v seriji", fdToggle: "Orodja za dokončane knjige v statistiki", ysNoDate: "brez datuma", hmShowing: "graf prikazuje", ysStreak: "Niz", ysCurrent: "Trenutni", ysLongest: "Najdaljši", ysDaysTotal: "Skupaj dni", ysPace: "Tempo", ysPerDay: "Na dan poslušanja", ysWhen: "Kdaj poslušaš", ysTopBooks: "Najbolj poslušane knjige", ysTopAuthors: "Najbolj poslušani avtorji", ysTopNarrators: "Najbolj poslušani pripovedovalci", rfToggle: "Razdelek »Oceni« na začetni strani", hoTitle: "Vrstni red razdelkov začetne strani", hoEmpty: "Enkrat odpri začetno stran in se vrni sem.", hoReset: "Ponastavi privzeti vrstni red", ctIcon: "Ikona", ctColour: "Poudarek", ctNeedBook: "Audiobookshelf ne more shraniti prazne zbirke, izberi vsaj eno knjigo. Pozneje lahko prosto dodajaš in odstranjuješ.", colEditIcon: "Zamenjaj ikono", ciTitle: "Ikona zbirke", ciReset: "Samodejno uskladi z imenom", colEditDesc: "Uredi opis", colBookForms: ["knjiga","knjigi","knjig"], colForms: ["Zbirka","Zbirki","Zbirk"], ctTitle: "Ustvari zbirko", ctBlank: "Prazna zbirka", ctNew: "Nova zbirka", ctNewHint: "iz predloge", ctNamePh: "Ime zbirke", ctSearch: "Išči knjige ali avtorje…", ctCreate: "Ustvari", ctBack: "‹ Nazaj", ctAddAll: "Dodaj vse knjige", abTitle: "Dodaj knjige", abAdd: "Dodaj", avRemove: "Odstrani fotografijo", avSet: "Izberi fotografijo…", ugListened: "Poslušano", ugPhoto: "Profilna fotografija", ugNever: "Nikoli", ugSeen: "Nazadnje aktiven", ugEdit: "Uredi uporabnika", ugDelete: "Izbriši uporabnika", ugAdd: "Dodaj fotografijo", ugChange: "Zamenjaj fotografijo", ugSortName: "A–Ž", ugSortTime: "Največ poslušano", ugSortSeen: "Nedavno aktivni", bsCard: "Povezave knjige", bsHint: "Gumbi na strani knjige za iskanje naslova drugod. Goodreads in največja stran tvojega jezika sta vklopljena vnaprej.", bsFind: "Išči na", bsGlobal: "Povsod", bsLocal: "Za tvoj jezik", bsOther: "Druge države", pgCard: "Preoblikovane strani", pgHint: "Vsaka možnost zamenja stran Audiobookshelfa z različico NanoHive. Izklopi za vrnitev izvirnika.", pgNarrators: "Kartice pripovedovalcev", pgCollections: "Strani zbirk", pgUsers: "Kartice uporabnikov (nastavitve)", pgStats: "Lestvica poslušanja", pgPhoto: "Profilna fotografija v zgornji vrstici", pgCinematic: "Filmsko ozadje", pgTransitions: "Prehodi strani", pSearch: "Išči nastavitve…", pSearchNone: "Ni zadetkov.", erTypeface: "Pisava", erPage: "Tema strani", erText: "Barva besedila", erBg: "Ozadje", erDefault: "Privzeto (ABS)", erAuto: "Samodejno" , lfByAuthor: "Avtor › vrstni red serije", lfBySeries: "Serija › vrstni red" , lfSecFilters: "Več filtrov", lfSecSort: "Večnivojsko razvrščanje", lfAuthor: "Avtor", lfSeries: "Serija", lfTitle: "Naslov", lfYear: "Leto", lfAdded: "Dodano", lfDuration: "Trajanje", lfNarrator: "Pripovedovalec", lfGenre: "Žanr", lfLanguage: "Jezik", lfProgress: "Napredek", lfPgFinished: "Končane", lfPgInProgress: "V teku", lfPgNot: "Nezačete" , logoUpload: 'Naloži z naprave…', logoUploaded: 'Naloženo ✓', logoBadType: 'Nepodprta vrsta slike', logoTooBig: 'Slika je prevelika (najv. 4 MB)', logoOr: 'ali', bookPage: 'Stran knjige', ratingsToggle: 'Ocene skupnosti (zvezdice in recenzije)', ratingsHint: 'Zvezdne ocene s kratkimi recenzijami na vsaki strani knjige, skupne vsem uporabnikom tega strežnika.', searchCard: 'Iskanje', gsToggle: 'Išči po vseh knjižnicah hkrati', gsHint: 'En združen seznam rezultatov iz vseh dostopnih knjižnic, z značko knjižnice pri vsakem zadetku. Izklopi za vrnitev menija po knjižnicah.', gsNoResults: 'Ni rezultatov', gsSearching: 'Iskanje…', gsBooks: 'Knjige', gsSeries: 'Serije', gsAuthors: 'Avtorji', spTitle: 'Začetna stran', spHint: 'Aplikacijo vedno odpri na izbranem pogledu knjižnice (samo ta brskalnik). Neposredne povezave se nikoli ne preusmerijo.', spDefault: 'Privzeto aplikacije', spViewHome: 'Domov', spViewLibrary: 'Knjižnica', spViewSeries: 'Serije', spViewCollections: 'Zbirke', spViewAuthors: 'Avtorji', scSet: 'Nastavi naslovnico serije', scRemove: 'Odstrani naslovnico serije', scEditDesc: 'Uredi opis serije', cardStars: 'Zvezdice na naslovnicah', hoverPolish: 'Izpopolnitev kartic ob prehodu (mehkejša zatemnitev, poudarjen gumb, zvezdice ob prehodu)', sectUser: 'Tvoje nastavitve', sectAdmin: 'Upravljanje', lockTitle: 'Stikala funkcij', lockHint: 'Prisilno izklopi funkcijo za vse uporabnike tega strežnika. Premaga osebne nastavitve; drugi vidijo po naslednjem nalaganju.', lockRatings: 'Izklopi ocene povsod', lockCardRatings: 'Izklopi zvezdice na naslovnicah', lockGlobalSearch: 'Izklopi globalno iskanje', lockHoverPolish: 'Izklopi izpopolnitev kartic', lockHeroCarousel: 'Izklopi vrtiljak na domači strani', lockedNote: 'izklopil skrbnik', lfSort: 'Ocena', lfRated: 'Ocenjene', lfUnrated: 'Neocenjene', lfClear: 'Počisti', lfNone: 'Nobena knjiga ne ustreza', lfSection: 'Ocena', lfWho: 'Čigave ocene', lfEveryone: 'Vsi', sdSave: 'Shrani', sdClear: 'Obnovi opis knjige', sdMore: 'Preberi več', sdLess: 'Pokaži manj', shSeries: 'Serija', shBy: 'od ', shMore: ' in več', shBookForms: ['knjiga', 'knjigi', 'knjig'], shAvg: 'povprečje knjig', shRated: 'ocenjenih', impLabel: 'Uvoz ocen', impHint: 'Prenesi ocene iz StoryGrapha ali Goodreadsa. Tam izvozi svoje knjige kot CSV in tu izberi datoteko. Vsako ujemanje se pokaže v potrditev in nič se ne shrani, dokler ne potrdiš.', impBtn: 'Izberi datoteko CSV…', impReading: 'Branje datoteke…', impMatching: 'Primerjava s tvojimi knjižnicami…', impErrUnknown: 'To ni videti kot izvoz iz StoryGrapha ali Goodreadsa. Biti mora njihov CSV s stolpcem "Star Rating" ali "My Rating".', impErrEmpty: 'Datoteka nima vrstic.', impErrNoRated: 'V datoteki ni ocenjenih knjig.', impErrRead: 'Datoteke ni bilo mogoče prebrati.', impErrLoad: 'Knjižnic ni bilo mogoče naložiti. Poskusi znova čez trenutek.', impErrNoLibs: 'Nobena knjižnica ne sodeluje pri ocenah, zato ni kam uvoziti.', impTitle: 'Uvoz ocen', impFrom: 'Izvoz %s: %d knjig, od tega %d ocenjenih', impLibs: 'Iščem po: %s', impMatched: 'Ujemanja', impCheck: 'Vredno ogleda', impCheckHint: 'Blizu, a negotovo. Preveri ujemanje ali izberi drugo.', impAlready: 'Že ocenjene s tvoje strani', impAlreadyHint: 'Ostanejo nespremenjene, razen če izbereš prepis.', impNone: 'Niso v tvoji knjižnici', impNoneHint: 'Nič na tem strežniku ne ustreza tem vnosom.', impAll: 'Vse', impSkip: 'Preskoči to', impDup: 'dvojnik', impByTitle: 'naslov', impYours: 'tvoja: %s', impReviews: 'Uvozi tudi napisane recenzije (%d)', impOver: 'Prepiši ocene, ki jih že imam', impGo: 'Uvozi %d ocen', impCancel: 'Prekliči', impCloseBtn: 'Zapri', impWorking: 'Shranjevanje… %d od %d', impDone: 'Uvoženih %d ocen.', impFail: '%d ni bilo mogoče shraniti.', rdTitle: 'Kdo bere to knjigo', rdFin: 'Končano', socTitle: 'Družabne funkcije', socHint: 'Kaj ta strežnik deli med uporabniki. Čas poslušanja poganja lestvico; drugi dve razkrivata, kaj kdo bere, zato ostaneta izklopljeni, dokler ju ne vklopiš. Velja za vse hkrati.', socTime: 'Čas poslušanja (lestvica strežnika)', socContent: 'Najbolj poslušani naslovi v deljeni statistiki', socWho: 'Kdo bere (strani knjig)', socUsers: 'Po uporabnikih', socUsersHint: 'Če koga izklopiš, se nič njegovega ne deli ne glede na njegovo izbiro; le ti lahko to povrneš. "Sam se je odjavil" pomeni, da je oseba sama rekla ne.', socOpted: 'sam se je odjavil', srvTitle: 'Privzete nastavitve strežnika', srvHint: 'Shrani svoje trenutne nastavitve kot privzete za vse uporabnike tega strežnika. Vsak lahko še vedno prilagodi svoje. Priklopi nosilec na /data/nh v vsebniku teme, da preživijo posodobitve.', srvSave: 'Shrani kot privzeto strežnika', srvClear: 'Počisti privzeto strežnika', srvSaved: 'Shranjeno', srvCleared: 'Počiščeno', srvErr: 'Ni uspelo: potrebna je skrbniška prijava', crMode: 'Polica Nadaljuj branje', crCombine: 'Združi z vrtiljakom', crSeparate: 'Ohrani kot ločeno polico', crHide: 'Skrita', fsHint: 'Deli svoje poslušanje z drugimi na tem strežniku: vsote časa in minute po dnevih za lestvico, in če jih je skrbnik vklopil, tudi tvoje najbolj poslušane naslove ter katere knjige bereš ali si jih končal/a. Privzeto vklopljeno. Ob izklopu se vse deljeno izbriše.' , amfHide: 'Skrij Označi kot končano' , amfConfirm: 'Označim to knjigo kot končano?' , fsHint: 'Deli povzetek svojega poslušanja (skupno in minute na dan) z drugimi na tem strežniku, poganja lestvico strežnika. Privzeto vklopljeno. Ob izklopu izgineš z lestvice in njenih seštevkov, deljeno pa se izbriše.' , srToggle: 'Deli, katere knjige bereš', srHint: 'Doda k temu, kar vidijo drugi, katere knjige ravno bereš, katere si končal/a in katere največ poslušaš. Privzeto izklopljeno; vklopljena morajo biti tudi strežniška stikala skrbnika.' , fsHint: 'Deli svoj čas poslušanja (vsote in minute po dnevih) z drugimi na tem strežniku; poganja lestvico. Privzeto vklopljeno. Ob izklopu te drugi uporabniki ne vidijo več; skrbniki strežnika aktivnost še vedno vidijo, označeno kot odjavljeno.', srHint: 'Drugim omogoči tudi videti, katere knjige ravno bereš, katere si končal/a in katere največ poslušaš. Nič se ne prikaže, dokler skrbnik ne vklopi ustreznih strežniških stikal.', seshTitle: 'Nedavne seje', seshNow: 'Zdaj poslušajo' , hcHint: 'Pošiljajte svoje ocene na račun Hardcover. Prilepite ključ API iz nastavitev računa Hardcover; ključi potečejo po enem letu.', hcKeyLbl: 'Ključ API', hcToggle: 'Samodejno sinhroniziraj nove ocene', hcPushAll: 'Pošlji vse ocene', hcPushDiff: 'Pošlji samo razlike', hcDone: 'Končano: %d sinhroniziranih, %d brez ujemanja', hcNoKey: 'Najprej prilepite svoj ključ API za Hardcover' , hcMissHead: 'Ni najdeno na Hardcoverju:' , hcHint: 'Pošiljajte svoje ocene na račun Hardcover. Prilepite ključ API iz nastavitev računa Hardcover.' , tabStyle: 'Slog', tabMain: 'Domača stran', tabBooks: 'Knjige', tabFeat: 'Funkcije' , hcImport: 'Uvozi iz Hardcoverja', impMarkQ: 'Naj %d ujemajočih knjig tudi tukaj označim kot dokončane?', impMarkDone: '%d označenih kot dokončane.' , impHint: 'Prenesite svoje ocene iz Goodreadsa, StoryGrapha ali Hardcoverja. Tukaj izberite izvoženi CSV; Hardcover se uvozi neposredno z gumbom na svoji kartici. Vsako ujemanje se pokaže v potrditev, preden se kar koli shrani.' , impErrUnknown: 'To ni videti kot izvoz iz Goodreadsa, StoryGrapha ali Hardcoverja: stolpcev naslova in ocene ni mogoče najti.' , fsCard: 'Družabno', rnToggle: 'Prikaži moje ime ob ocenah', rsToggle: 'Deli moje ocene z drugimi', socNames: 'Imena ob ocenah', rtAnon: 'Anonimno' },
    hu: { cmStopped: "leállítva", cmScanErrs: "{e} nem ellenőrizve (Goodreads-hibák)", cmScanBusy: "Már fut egy átvizsgálás", cmGrErr: "A Goodreads nem elérhető, újra próbálom", cmNoMatchYet: "Nincs Goodreads-találat", cmLooking: "Keresés a Goodreadsen…", cmToolHint: "Mit gondolnak erről a könyvről a Goodreads olvasói. A ? mutatja a párosított könyvet, és lehet cserélni.", cmSearchPh: "Keresés a Goodreadsen…", cmNoVotes: "még nincs értékelés", cmRefresh: "Frissítés", cmScanAll: "Mindent újra átnézni, nem csak a hiányzókat", cmScanDone: "Kész: {m} találat · {x} nem található · {s} már volt", cmStop: "Leállítás", cmScanLibs: "Átvizsgálandó könyvtárak", cmScan: "Értékelések keresése", lsLongest: "Leghosszabb könyvek", lsSize: "Méret a lemezen", lsDuration: "Teljes hossz", lsGenresN: "Műfajok", lsSeriesN: "Sorozatok", lsAuthorsN: "Szerzők", lsBooks: "Könyvek", lsGlance: "A könyvtár egy pillantásra", cmOpen: "Megnyitás a Goodreadsen", cmDetect: "Keresés", cmAddr: "A segéd címe", scmTitle: "Sorozat oldala", scmHint: "Így néz ki a sorozatcím fölött generált borító, ha nincs saját feltöltve (csak ez a böngésző).", scmDeck: "Rétegzett pakli", cmHint: "Mit gondolnak a könyvről a Goodreads olvasói: átlagos pontszám és szavazatok száma, a könyv oldalán a csillagok alatt, valamint rendezésként és szűrőként. A könyvoldalak böngészés közben maguktól kitöltődnek.", cmGrOff: "Goodreads: nincs beállítva", cmSetup: "Még nincs segéd? Futtasd ezt a téma mellett (compose sorok lent), majd nyomd meg a Keresés gombot. Bármilyen más módon futtatott kép is jó: illeszd be a címét fent.", cmGrOn: "Goodreads: csatlakoztatva", cmGrDown: "Goodreads: beállítva, de nem válaszol", cmGrNoNum: "Goodreads: válaszol, de ez az abs-tract kiadás nem ad értékelésszámokat (használd a ratings kiadást, lásd README)", cmTitle: "Goodreads-értékelések", cmWrong: "Rossz könyv?", cmFind: "Közösségi értékelés keresése…", cmNone: "Nincs találat", cmUse: "Ezt használom", lfCommunity: "Goodreads-értékelés", mfNatChange: "Módosítás…", lsByTime: "hallgatási idő szerint", sbTitle: "Szerver ranglista", sbWeek: "Hét", sbMonth: "Hónap", sbYear: "Év", sbAll: "Összesen", sbBooks: "Könyvek", sbTopBooks: "Leghallgatottabbak", sbTotal: "Összesen", sbListeners: "Hallgatók", sbAvg: "Átlag", wkTitle: "Hallgatott percek, elmúlt 7 nap", wkAvg: "Napi átlag", wkRow: "Nap egymás után", rpBadge: "Nyitott hibabejelentések", rpMenu: "Hiba bejelentése", rpTitle: "Hiba bejelentése", rpWhat: "Mi a gond?", rpNote: "Bármi más az adminnak? (nem kötelező)", rpSend: "Bejelentés küldése", rpSent: "Elküldve. Köszönjük.", rpFail: "A küldés nem sikerült", rpMissing: "Hiányzó vagy hiányos tartalom", rpQuality: "Rossz hangminőség", rpPlay: "Nem játszódik le", rpWrong: "Rossz könyv, borító vagy metaadat", rpChapters: "Rossz fejezetek", rpOther: "Valami más", rfRate: "Értékelés", rfSheet: "Értékeld ezt a könyvet", rfOpen: "Könyv oldalának megnyitása", rfPickHint: "Válassz értékelést", rfTitle: "Értékeld, amit befejeztél", ysFinished: "Nemrég befejezett", ysAlmost: "Majdnem kész", ysMarkDone: "Megjelölés befejezettként", ysEditDate: "Befejezés dátumának módosítása", ysThisWeek: "Ezen a héten", ysBestDay: "Legjobb nap", ysTitle: "A hallgatásod", fsTitle: "Szerver ranglista", fsCard: "Szerver ranglista", fsToggle: "Hallgatási statisztikáim megosztása", fsJoin: "Kapcsold be a „Hallgatási statisztikáim megosztása” opciót a testreszabási panelen, hogy megjelenj a szerver ranglistáján.", fsWaiting: "A megosztás be van kapcsolva, összegzésed hamarosan csatlakozik a szerver ranglistájához a többiekével együtt.", fsHint: "Oszd meg hallgatásod összegzését (összesítések és napi percek) a szerver többi tagjával, ez táplálja a ranglistát. Alapból bekapcsolva. Kikapcsoláskor eltűnsz a ranglistáról és az összegekből, a megosztott adatok törlődnek.", yirBtn: "Év összefoglaló", yirTitle: "A hallgatási éved", yirYear: "Elmúlt 12 hónap", yirDays: "Hallgatással töltött napok", yirBest: "Legjobb nap", yirMonths: "Havonta", yirNone: "Még nincsenek hallgatási adatok.", ssscmDeck: "Rétegzett pakli", scmFirst: "Az első könyv borítója", scmGrid: "Borítórács (alapértelmezett)", scmOff: "Rejtett" , lsTitle: "Szerverstatisztika", lsAll: "Minden könyvtár", lsPlayed: "Lejátszott könyvek", lsBest: "Legjobbra értékelt", lsGenres: "Top műfajok", srvPick: "Mit mentsen:", srvGRest: "Funkciók és viselkedés", aeBtn: "Szerzők rendbetétele", aeTitle: "Szerzők könyv nélkül", aeNone: "Itt minden szerzőnek van legalább egy könyve.", aeRemove: "Eltávolítás", aeDone: "Eltávolítva", aeWorking: "Eltávolítás…", rpCard: "Bejelentett hibák", rpHint: "A felhasználók által könyvoldalról bejelentett hibák. A megoldás leveszi a listáról.", rpEmpty: "Nincs bejelentés.", rpResolve: "Megold", rpOpen: "Könyv megnyitása", apCard: "Lejátszás", apToggle: "Sorozat következő könyvének automatikus lejátszása", apHint: "Ha egy könyv véget ér, automatikusan indul a sorozat következő része. Csak már meglévő, be nem fejezett könyvek.", apNext: "A sorozat következője", fdToggle: "Befejezett könyvek eszközei a statisztikában", ysNoDate: "nincs dátum", hmShowing: "a grafikon ezt mutatja", ysStreak: "Sorozat", ysCurrent: "Jelenlegi", ysLongest: "Leghosszabb", ysDaysTotal: "Napok összesen", ysPace: "Tempó", ysPerDay: "Hallgatási naponként", ysWhen: "Mikor hallgatsz", ysTopBooks: "Leghallgatottabb könyvek", ysTopAuthors: "Leghallgatottabb szerzők", ysTopNarrators: "Leghallgatottabb felolvasók", rfToggle: "„Értékeld” szakasz a kezdőlapon", hoTitle: "Kezdőlap szakaszainak sorrendje", hoEmpty: "Nyisd meg egyszer a kezdőlapot, majd gyere vissza.", hoReset: "Alapértelmezett sorrend visszaállítása", ctIcon: "Ikon", ctColour: "Kiemelés", ctNeedBook: "Az Audiobookshelf nem tud üres gyűjteményt menteni, válassz legalább egy könyvet. Később szabadon bővítheted.", colEditIcon: "Ikon cseréje", ciTitle: "Gyűjtemény ikonja", ciReset: "Automatikus illesztés a névhez", colEditDesc: "Leírás szerkesztése", colBookForms: ["könyv","könyv"], colForms: ["Gyűjtemény","Gyűjtemények"], ctTitle: "Gyűjtemény létrehozása", ctBlank: "Üres gyűjtemény", ctNew: "Új gyűjtemény", ctNewHint: "sablonból", ctNamePh: "Gyűjtemény neve", ctSearch: "Könyvek vagy szerzők keresése…", ctCreate: "Létrehozás", ctBack: "‹ Vissza", ctAddAll: "Minden könyv hozzáadása", abTitle: "Könyvek hozzáadása", abAdd: "Hozzáadás", avRemove: "Fotó eltávolítása", avSet: "Fotó kiválasztása…", ugListened: "Meghallgatva", ugPhoto: "Profilkép", ugNever: "Soha", ugSeen: "Utoljára aktív", ugEdit: "Felhasználó szerkesztése", ugDelete: "Felhasználó törlése", ugAdd: "Fotó hozzáadása", ugChange: "Fotó cseréje", ugSortName: "A–Z", ugSortTime: "Leghallgatottabb", ugSortSeen: "Nemrég aktívak", bsCard: "Könyvhivatkozások", bsHint: "Gombok a könyvoldalon a cím máshol való kereséséhez. A Goodreads és a nyelved legnagyobb oldala előre be van kapcsolva.", bsFind: "Keresés itt:", bsGlobal: "Mindenhol", bsLocal: "A nyelvedhez", bsOther: "Más országok", pgCard: "Újratervezett oldalak", pgHint: "Mindegyik egy Audiobookshelf-oldalt cserél NanoHive-verzióra. Kikapcsolva visszatér az eredeti.", pgNarrators: "Felolvasókártyák", pgCollections: "Gyűjteményoldalak", pgUsers: "Felhasználókártyák (beállítások)", pgStats: "Hallgatási ranglista", pgPhoto: "Profilkép a felső sávban", pgCinematic: "Filmes háttér", pgTransitions: "Oldalátmenetek", pSearch: "Beállítások keresése…", pSearchNone: "Nincs találat.", erTypeface: "Betűtípus", erPage: "Oldaltéma", erText: "Szöveg színe", erBg: "Háttér", erDefault: "Alapértelmezett (ABS)", erAuto: "Automatikus", nrForms: ["Felolvasó","Felolvasók"], nrBooksForms: ["könyv","könyv"], nrSearch: "Felolvasók szűrése…", nrSortName: "Név", nrSortBooks: "Legtöbb könyv", auForms: ["Szerző","Szerzők"], auSearch: "Szerzők szűrése…" , lfByAuthor: "Szerző › sorozatrend", lfBySeries: "Sorozat › sorrend" , lfSecFilters: "További szűrők", lfSecSort: "Többszintű rendezés", lfAuthor: "Szerző", lfSeries: "Sorozat", lfTitle: "Cím", lfYear: "Év", lfAdded: "Hozzáadva", lfDuration: "Hossz", lfNarrator: "Felolvasó", lfGenre: "Műfaj", lfLanguage: "Nyelv", lfProgress: "Haladás", lfPgFinished: "Befejezett", lfPgInProgress: "Folyamatban", lfPgNot: "El nem kezdett" , logoUpload: 'Feltöltés eszközről…', logoUploaded: 'Feltöltve ✓', logoBadType: 'Nem támogatott képtípus', logoTooBig: 'A kép túl nagy (max. 4 MB)', logoOr: 'vagy', bookPage: 'Könyvoldal', ratingsToggle: 'Közösségi értékelések (csillagok és vélemények)', ratingsHint: 'Csillagos értékelések rövid véleményekkel minden könyvoldalon, megosztva a szerver összes felhasználójával.', searchCard: 'Keresés', gsToggle: 'Keresés az összes könyvtárban egyszerre', gsHint: 'Egyetlen egyesített találati lista az összes elérhető könyvtárból, könyvtárjelvénnyel minden találaton. Kikapcsolva visszatér a könyvtáranként menü.', gsNoResults: 'Nincs találat', gsSearching: 'Keresés…', gsBooks: 'Könyvek', gsSeries: 'Sorozatok', gsAuthors: 'Szerzők', spTitle: 'Kezdőoldal', spHint: 'Az alkalmazás mindig a kiválasztott könyvtárnézeten nyíljon (csak ez a böngésző). A közvetlen linkek sosem kerülnek átirányításra.', spDefault: 'Alkalmazás alapértelmezése', spViewHome: 'Kezdőlap', spViewLibrary: 'Könyvtár', spViewSeries: 'Sorozatok', spViewCollections: 'Gyűjtemények', spViewAuthors: 'Szerzők', scSet: 'Sorozatborító beállítása', scRemove: 'Sorozatborító eltávolítása', scEditDesc: 'Sorozatleírás szerkesztése', cardStars: 'Csillagok a borítókon', hoverPolish: 'Kártya-finomítás rámutatáskor (lágyabb sötétítés, kiemelt gomb, csillagok rámutatáskor)', sectUser: 'Saját beállítások', sectAdmin: 'Adminisztráció', lockTitle: 'Funkciókapcsolók', lockHint: 'Kényszerűen kikapcsol egy funkciót a szerver összes felhasználójánál. Erősebb a személyes beállításoknál; a többiek a következő újratöltés után látják.', lockRatings: 'Értékelések kikapcsolása mindenhol', lockCardRatings: 'Csillagok kikapcsolása a borítókon', lockGlobalSearch: 'Globális keresés kikapcsolása', lockHoverPolish: 'Kártya-finomítás kikapcsolása', lockHeroCarousel: 'Kezdőlapi körhinta kikapcsolása', lockedNote: 'az admin kikapcsolta', lfSort: 'Értékelés', lfRated: 'Értékelt', lfUnrated: 'Értékeletlen', lfClear: 'Törlés', lfNone: 'Nincs egyező könyv', lfSection: 'Értékelés', lfWho: 'Kinek az értékelései', lfEveryone: 'Mindenki', sdSave: 'Mentés', sdClear: 'Könyvleírás visszaállítása', sdMore: 'Tovább', sdLess: 'Kevesebb', shSeries: 'Sorozat', shBy: '', shMore: ' és mások', shBookForms: ['könyv', 'könyv'], shAvg: 'könyvek átlaga', shRated: 'értékelve', impLabel: 'Értékelések importálása', impHint: 'Hozd át az értékeléseidet a StoryGraphból vagy a Goodreadsből. Exportáld ott a könyveidet CSV-be, és válaszd ki a fájlt itt. Minden egyezést megerősítésre mutatunk, és semmi sem mentődik, amíg nem hagyod jóvá.', impBtn: 'CSV-fájl kiválasztása…', impReading: 'Fájl olvasása…', impMatching: 'Egyeztetés a könyvtáraiddal…', impErrUnknown: 'Ez nem tűnik StoryGraph- vagy Goodreads-exportnak. Az ő CSV-jük kell legyen, "Star Rating" vagy "My Rating" oszloppal.', impErrEmpty: 'A fájlban nincsenek sorok.', impErrNoRated: 'A fájlban nincsenek értékelt könyvek.', impErrRead: 'A fájlt nem sikerült beolvasni.', impErrLoad: 'A könyvtáraidat nem sikerült betölteni. Próbáld újra kicsit később.', impErrNoLibs: 'Egy könyvtár sem vesz részt az értékelésekben, így nincs hová importálni.', impTitle: 'Értékelések importálása', impFrom: '%s-export: %d könyv, ebből %d értékelt', impLibs: 'Keresés itt: %s', impMatched: 'Párosítva', impCheck: 'Érdemes megnézni', impCheckHint: 'Közeli, de bizonytalan. Ellenőrizd a párosítást, vagy válassz másikat.', impAlready: 'Már értékelted', impAlreadyHint: 'Érintetlenül maradnak, hacsak nem kéred a felülírást.', impNone: 'Nincs a könyvtáradban', impNoneHint: 'Ezen a szerveren semmi sem egyezik ezekkel.', impAll: 'Összes', impSkip: 'Ennek kihagyása', impDup: 'duplikátum', impByTitle: 'cím', impYours: 'a tiéd: %s', impReviews: 'Írott vélemények importálása is (%d)', impOver: 'Meglévő értékeléseim felülírása', impGo: '%d értékelés importálása', impCancel: 'Mégse', impCloseBtn: 'Bezárás', impWorking: 'Mentés… %d / %d', impDone: '%d értékelés importálva.', impFail: '%d nem menthető.', rdTitle: 'Ki olvassa ezt a könyvet', rdFin: 'Befejezve', socTitle: 'Közösségi funkciók', socHint: 'Amit ez a szerver megoszt a felhasználók között. A hallgatási idő hajtja a ranglistát; a másik kettő elárulja, ki mit olvas, ezért kikapcsolva marad, amíg be nem kapcsolod. Mindenkire egyszerre érvényes.', socTime: 'Hallgatási idő (szerver-ranglista)', socContent: 'Top címek a megosztott statisztikában', socWho: 'Ki olvas (könyvoldalak)', socUsers: 'Felhasználónként', socUsersHint: 'Ha valakit kikapcsolsz, semmije sem lesz megosztva, bármit is választott; csak te kapcsolhatod vissza. A "maga mondott le" azt jelenti, hogy az illető maga mondott nemet.', socOpted: 'maga mondott le', srvTitle: 'Szerver alapértelmezések', srvHint: 'Mentsd el a jelenlegi beállításaidat alapértelmezésként a szerver minden felhasználójának. Mindenki tovább személyre szabhatja. Csatolj kötetet a /data/nh útvonalra a téma konténerében, hogy megmaradjanak frissítéskor.', srvSave: 'Mentés szerver-alapértelmezésként', srvClear: 'Szerver-alapértelmezés törlése', srvSaved: 'Mentve', srvCleared: 'Törölve', srvErr: 'Sikertelen: admin bejelentkezés szükséges', crMode: 'Olvasás folytatása polc', crCombine: 'Összevonás a körhintával', crSeparate: 'Külön polcként megtartás', crHide: 'Rejtett', fsHint: 'Oszd meg a hallgatásodat a szerver többi tagjával: időösszegek és napi percek a ranglistához, és ha az admin bekapcsolta, a top címeid és hogy mely könyveket olvasod vagy fejezted be. Alapból bekapcsolva. Kikapcsoláskor minden megosztott adat törlődik.' , amfHide: 'A Megjelölés befejezettként elrejtése' , amfConfirm: 'Megjelölöd ezt a könyvet befejezettként?' , fsHint: 'Oszd meg hallgatásod összegzését (összesítések és napi percek) a szerver többi tagjával, ez táplálja a ranglistát. Alapból bekapcsolva. Kikapcsoláskor eltűnsz a ranglistáról és az összegekből, a megosztott adatok törlődnek.' , srToggle: 'Oszd meg, milyen könyveket olvasol', srHint: 'Hozzáadja ahhoz, amit mások látnak, hogy éppen milyen könyveket olvasol, melyeket fejezted be és melyeket hallgatod a legtöbbet. Alapból kikapcsolva; az admin szerverkapcsolóinak is bekapcsolva kell lenniük.' , fsHint: 'Oszd meg a hallgatási idődet (összegek és napi percek) a szerver többi tagjával; ez hajtja a ranglistát. Alapból bekapcsolva. Kikapcsolás után a többi felhasználó nem lát téged; a szerver adminjai továbbra is látják az aktivitást, lemondottként jelölve.', srHint: 'Azt is láthatóvá teszi mások számára, milyen könyveket olvasol, melyeket fejezted be és melyeket hallgatod a legtöbbet. Semmi sem jelenik meg, amíg az admin be nem kapcsolja a megfelelő szerverkapcsolókat.', seshTitle: 'Legutóbbi munkamenetek', seshNow: 'Most hallgatják' , hcHint: 'Küldd az értékeléseidet a Hardcover-fiókodba. Illeszd be az API-kulcsot a Hardcover-fiók beállításaiból; a kulcsok egy év után lejárnak.', hcKeyLbl: 'API-kulcs', hcToggle: 'Új értékelések automatikus szinkronizálása', hcPushAll: 'Összes értékelés küldése', hcPushDiff: 'Csak az eltérések küldése', hcDone: 'Kész: %d szinkronizálva, %d találat nélkül', hcNoKey: 'Előbb illeszd be a Hardcover API-kulcsodat' , hcMissHead: 'Nem található a Hardcoveren:' , hcHint: 'Küldd az értékeléseidet a Hardcover-fiókodba. Illeszd be az API-kulcsot a Hardcover-fiók beállításaiból.' , tabStyle: 'Stílus', tabMain: 'Főoldal', tabBooks: 'Könyvek', tabFeat: 'Funkciók' , hcImport: 'Importálás a Hardcoverről', impMarkQ: 'A(z) %d párosított könyvet itt is jelöljük befejezettnek?', impMarkDone: '%d befejezettnek jelölve.' , impHint: 'Hozd át az értékeléseidet a Goodreadsről, StoryGraphról vagy Hardcoverről. Válaszd ki itt az exportált CSV-t; a Hardcover közvetlenül importál a kártyáján lévő gombbal. Minden találat megerősítésre vár, mielőtt bármi mentésre kerülne.' , impErrUnknown: 'Ez nem tűnik Goodreads, StoryGraph vagy Hardcover exportnak: nem található cím- és értékelésoszlop.' , fsCard: 'Közösségi', rnToggle: 'A nevem megjelenítése az értékeléseknél', rsToggle: 'Értékeléseim megosztása másokkal', socNames: 'Nevek az értékeléseknél', rtAnon: 'Névtelen' },
    ro: { cmStopped: "oprit", cmScanErrs: "{e} neverificate (erori Goodreads)", cmScanBusy: "O scanare este deja în curs", cmGrErr: "Goodreads nu răspunde, voi reîncerca", cmNoMatchYet: "Nicio potrivire pe Goodreads", cmLooking: "Caut pe Goodreads…", cmToolHint: "Ce cred cititorii Goodreads despre această carte. Semnul ? arată cartea potrivită și permite schimbarea ei.", cmSearchPh: "Caută pe Goodreads…", cmNoVotes: "fără evaluări încă", cmRefresh: "Reîmprospătează", cmScanAll: "Reverifică tot, nu doar ce lipsește", cmScanDone: "Gata: {m} găsite · {x} negăsite · {s} aveau deja", cmStop: "Oprește", cmScanLibs: "Biblioteci de scanat", cmScan: "Caută evaluări", lsLongest: "Cele mai lungi cărți", lsSize: "Mărime pe disc", lsDuration: "Durată totală", lsGenresN: "Genuri", lsSeriesN: "Serii", lsAuthorsN: "Autori", lsBooks: "Cărți", lsGlance: "Biblioteca pe scurt", cmOpen: "Deschide pe Goodreads", cmDetect: "Caută", cmAddr: "Adresa ajutorului", scmTitle: "Pagina seriei", scmHint: "Cum arată coperta generată deasupra titlului unei serii când nu s-a încărcat una proprie (doar acest browser).", scmDeck: "Pachet stratificat", cmHint: "Ce cred cititorii Goodreads despre o carte: nota medie și numărul de voturi, sub stelele de pe pagina cărții și ca sortare și filtru. Paginile cărților se completează singure la răsfoire.", cmGrOff: "Goodreads: neconfigurat", cmSetup: "Încă fără ajutor? Rulează asta lângă temă (liniile compose de mai jos), apoi apasă Caută. Merge și orice alt mod de a rula imaginea: lipește adresa ei mai sus.", cmGrOn: "Goodreads: conectat", cmGrDown: "Goodreads: configurat, dar nu răspunde", cmGrNoNum: "Goodreads: răspunde, dar această versiune abs-tract nu oferă note (folosiți versiunea cu evaluări, vezi README)", cmTitle: "Evaluări Goodreads", cmWrong: "Carte greșită?", cmFind: "Caută evaluarea comunității…", cmNone: "Nicio potrivire", cmUse: "Folosește", lfCommunity: "Evaluare Goodreads", mfNatChange: "Schimbă…", lsByTime: "după timpul de ascultare", sbTitle: "Clasamentul serverului", sbWeek: "Săptămână", sbMonth: "Lună", sbYear: "An", sbAll: "Tot timpul", sbBooks: "Cărți", sbTopBooks: "Cele mai ascultate", sbTotal: "Total", sbListeners: "Ascultători", sbAvg: "Medie", wkTitle: "Minute de ascultare, ultimele 7 zile", wkAvg: "Media zilnică", wkRow: "Zile la rând", rpBadge: "Probleme raportate deschise", rpMenu: "Raportează o problemă", rpTitle: "Raportează o problemă", rpWhat: "Ce nu e în regulă?", rpNote: "Altceva pentru administrator? (opțional)", rpSend: "Trimite raportul", rpSent: "Trimis. Mulțumim.", rpFail: "Trimiterea a eșuat", rpMissing: "Conținut lipsă sau incomplet", rpQuality: "Calitate audio slabă", rpPlay: "Nu se redă", rpWrong: "Carte, copertă sau metadate greșite", rpChapters: "Capitole greșite", rpOther: "Altceva", rfRate: "Evaluează", rfSheet: "Evaluează această carte", rfOpen: "Deschide pagina cărții", rfPickHint: "Alege o notă", rfTitle: "Evaluează ce ai terminat", ysFinished: "Terminate recent", ysAlmost: "Aproape gata", ysMarkDone: "Marchează ca terminată", ysEditDate: "Schimbă data terminării", ysThisWeek: "Săptămâna aceasta", ysBestDay: "Cea mai bună zi", ysTitle: "Ascultarea ta", fsTitle: "Clasamentul serverului", fsCard: "Clasamentul serverului", fsToggle: "Împărtășește statisticile mele de ascultare", fsJoin: "Activează „Împărtășește statisticile mele de ascultare” din panoul de personalizare ca să apari în clasamentul serverului.", fsWaiting: "Partajarea e activă, rezumatul tău se alătură în curând clasamentului serverului, împreună cu toți ceilalți.", fsHint: "Împărtășește un rezumat al ascultării tale (totaluri și minute pe zi) cu ceilalți de pe acest server, alimentează clasamentul. Activ implicit. La dezactivare dispari din clasament și din totaluri, iar datele partajate se șterg.", yirBtn: "Anul în retrospectivă", yirTitle: "Anul tău de ascultare", yirYear: "Ultimele 12 luni", yirDays: "Zile cu ascultare", yirBest: "Cea mai bună zi", yirMonths: "Pe luni", yirNone: "Încă nu există date de ascultare.", ssscmDeck: "Pachet stratificat", scmFirst: "Coperta primei cărți", scmGrid: "Grilă de coperți (implicit)", scmOff: "Ascunsă" , lsTitle: "Statisticile serverului", lsAll: "Toate bibliotecile", lsPlayed: "Cărți redate", lsBest: "Cele mai bine cotate", lsGenres: "Genuri de top", srvPick: "Ce se salvează:", srvGRest: "Funcții și comportament", aeBtn: "Ordonează autorii", aeTitle: "Autori fără cărți", aeNone: "Fiecare autor de aici are cel puțin o carte.", aeRemove: "Elimină", aeDone: "Eliminați", aeWorking: "Se elimină…", rpCard: "Probleme raportate", rpHint: "Probleme raportate de utilizatori de pe pagina unei cărți. Rezolvarea o scoate din listă.", rpEmpty: "Nimic raportat.", rpResolve: "Rezolvă", rpOpen: "Deschide cartea", apCard: "Redare", apToggle: "Redă automat următoarea carte dintr-o serie", apHint: "Când o carte se termină, începe automat următoarea din aceeași serie. Doar cărți pe care le ai deja și neterminate.", apNext: "Următoarea din serie", fdToggle: "Instrumente pentru cărți terminate în statistici", ysNoDate: "fără dată", hmShowing: "graficul arată", ysStreak: "Serie de zile", ysCurrent: "Curentă", ysLongest: "Cea mai lungă", ysDaysTotal: "Zile în total", ysPace: "Ritm", ysPerDay: "Pe zi de ascultare", ysWhen: "Când asculți", ysTopBooks: "Cele mai ascultate cărți", ysTopAuthors: "Cei mai ascultați autori", ysTopNarrators: "Cei mai ascultați naratori", rfToggle: "Secțiunea „Evaluează” pe pagina principală", hoTitle: "Ordinea secțiunilor paginii principale", hoEmpty: "Deschide pagina principală o dată și revino aici.", hoReset: "Restabilește ordinea implicită", ctIcon: "Pictogramă", ctColour: "Accent", ctNeedBook: "Audiobookshelf nu poate salva o colecție goală, alege cel puțin o carte. Poți adăuga și elimina liber mai târziu.", colEditIcon: "Schimbă pictograma", ciTitle: "Pictograma colecției", ciReset: "Potrivește automat după nume", colEditDesc: "Editează descrierea", colBookForms: ["carte","cărți"], colForms: ["Colecție","Colecții"], ctTitle: "Creează o colecție", ctBlank: "Colecție goală", ctNew: "Colecție nouă", ctNewHint: "dintr-un șablon", ctNamePh: "Numele colecției", ctSearch: "Caută cărți sau autori…", ctCreate: "Creează", ctBack: "‹ Înapoi", ctAddAll: "Adaugă toate cărțile", abTitle: "Adaugă cărți", abAdd: "Adaugă", avRemove: "Elimină fotografia", avSet: "Alege fotografia…", ugListened: "Ascultat", ugPhoto: "Fotografie de profil", ugNever: "Niciodată", ugSeen: "Ultima activitate", ugEdit: "Editează utilizatorul", ugDelete: "Șterge utilizatorul", ugAdd: "Adaugă fotografie", ugChange: "Schimbă fotografia", ugSortName: "A–Z", ugSortTime: "Cele mai ascultate", ugSortSeen: "Activi recent", bsCard: "Linkurile cărții", bsHint: "Butoane pe pagina cărții pentru a căuta titlul în altă parte. Goodreads și cel mai mare site al limbii tale sunt activate din start.", bsFind: "Caută pe", bsGlobal: "Peste tot", bsLocal: "Pentru limba ta", bsOther: "Alte țări", pgCard: "Pagini regândite", pgHint: "Fiecare opțiune înlocuiește o pagină Audiobookshelf cu versiunea NanoHive. Dezactiveaz-o pentru a reveni la original.", pgNarrators: "Carduri de naratori", pgCollections: "Pagini de colecții", pgUsers: "Carduri de utilizatori (setări)", pgStats: "Clasament de ascultare", pgPhoto: "Fotografie de profil în bara de sus", pgCinematic: "Fundal cinematografic", pgTransitions: "Tranziții de pagină", pSearch: "Caută setări…", pSearchNone: "Nimic nu se potrivește.", erTypeface: "Font", erPage: "Tema paginii", erText: "Culoarea textului", erBg: "Fundal", erDefault: "Implicit (ABS)", erAuto: "Auto", nrForms: ["Narator","Naratori"], nrBooksForms: ["carte","cărți"], nrSearch: "Filtrează naratorii…", nrSortName: "Nume", nrSortBooks: "Cele mai multe cărți", auForms: ["Autor","Autori"], auSearch: "Filtrează autorii…" , lfByAuthor: "Autor › ordinea seriei", lfBySeries: "Serie › ordine" , lfSecFilters: "Mai multe filtre", lfSecSort: "Sortare multiplă", lfAuthor: "Autor", lfSeries: "Serie", lfTitle: "Titlu", lfYear: "An", lfAdded: "Adăugat", lfDuration: "Durată", lfNarrator: "Narator", lfGenre: "Gen", lfLanguage: "Limbă", lfProgress: "Progres", lfPgFinished: "Terminate", lfPgInProgress: "În curs", lfPgNot: "Neîncepute" , logoUpload: 'Încarcă de pe dispozitiv…', logoUploaded: 'Încărcat ✓', logoBadType: 'Tip de imagine neacceptat', logoTooBig: 'Imagine prea mare (max. 4 MB)', logoOr: 'sau', bookPage: 'Pagina cărții', ratingsToggle: 'Evaluările comunității (stele și recenzii)', ratingsHint: 'Evaluări cu stele și recenzii scurte pe fiecare pagină de carte, comune tuturor utilizatorilor acestui server.', searchCard: 'Căutare', gsToggle: 'Caută în toate bibliotecile deodată', gsHint: 'O listă de rezultate unificată din toate bibliotecile accesibile, cu insigna bibliotecii pe fiecare rezultat. Dezactivează pentru a reveni la meniul per bibliotecă.', gsNoResults: 'Niciun rezultat', gsSearching: 'Se caută…', gsBooks: 'Cărți', gsSeries: 'Serii', gsAuthors: 'Autori', spTitle: 'Pagina de pornire', spHint: 'Deschide aplicația mereu pe o vedere aleasă a unei biblioteci (doar acest browser). Linkurile directe nu sunt niciodată redirecționate.', spDefault: 'Implicit al aplicației', spViewHome: 'Acasă', spViewLibrary: 'Bibliotecă', spViewSeries: 'Serii', spViewCollections: 'Colecții', spViewAuthors: 'Autori', scSet: 'Setează coperta seriei', scRemove: 'Elimină coperta seriei', scEditDesc: 'Editează descrierea seriei', cardStars: 'Stele pe coperți', hoverPolish: 'Finisaj la trecerea cu mouse-ul (întunecare mai blândă, buton accentuat, stele la hover)', sectUser: 'Setările tale', sectAdmin: 'Administrare', lockTitle: 'Comutatoare de funcții', lockHint: 'Dezactivează forțat o funcție pentru toți utilizatorii acestui server. Bate setările personale; ceilalți văd după următoarea reîncărcare.', lockRatings: 'Dezactivează evaluările peste tot', lockCardRatings: 'Dezactivează stelele pe coperți', lockGlobalSearch: 'Dezactivează căutarea globală', lockHoverPolish: 'Dezactivează finisajul cardurilor', lockHeroCarousel: 'Dezactivează caruselul de pe acasă', lockedNote: 'dezactivat de administrator', lfSort: 'Evaluare', lfRated: 'Evaluate', lfUnrated: 'Neevaluate', lfClear: 'Golește', lfNone: 'Nicio carte nu corespunde', lfSection: 'Evaluare', lfWho: 'Ale cui evaluări', lfEveryone: 'Toți', sdSave: 'Salvează', sdClear: 'Restaurează descrierea cărții', sdMore: 'Citește mai mult', sdLess: 'Arată mai puțin', shSeries: 'Serie', shBy: 'de ', shMore: ' și alții', shBookForms: ['carte', 'cărți'], shAvg: 'media cărților', shRated: 'evaluate', impLabel: 'Importă evaluări', impHint: 'Adu-ți evaluările din StoryGraph sau Goodreads. Exportă acolo cărțile ca CSV și alege fișierul aici. Fiecare potrivire îți este arătată spre confirmare și nimic nu se salvează până nu confirmi.', impBtn: 'Alege fișierul CSV…', impReading: 'Se citește fișierul…', impMatching: 'Se compară cu bibliotecile tale…', impErrUnknown: 'Nu pare un export StoryGraph sau Goodreads. Trebuie să fie CSV-ul lor, cu o coloană "Star Rating" sau "My Rating".', impErrEmpty: 'Fișierul nu are rânduri.', impErrNoRated: 'Nu există cărți evaluate în fișier.', impErrRead: 'Fișierul nu a putut fi citit.', impErrLoad: 'Bibliotecile nu au putut fi încărcate. Încearcă din nou imediat.', impErrNoLibs: 'Nicio bibliotecă nu participă la evaluări, deci nu există unde importa.', impTitle: 'Importă evaluări', impFrom: 'Export %s: %d cărți, dintre care %d evaluate', impLibs: 'Se caută în: %s', impMatched: 'Potrivite', impCheck: 'Merită verificat', impCheckHint: 'Aproape, dar nesigur. Verifică potrivirea sau alege alta.', impAlready: 'Deja evaluate de tine', impAlreadyHint: 'Rămân neatinse, dacă nu ceri suprascrierea.', impNone: 'Nu sunt în biblioteca ta', impNoneHint: 'Nimic de pe acest server nu corespunde acestora.', impAll: 'Toate', impSkip: 'Sari peste aceasta', impDup: 'duplicat', impByTitle: 'titlu', impYours: 'a ta: %s', impReviews: 'Importă și recenziile scrise (%d)', impOver: 'Suprascrie evaluările pe care le am deja', impGo: 'Importă %d evaluări', impCancel: 'Anulează', impCloseBtn: 'Închide', impWorking: 'Se salvează… %d din %d', impDone: '%d evaluări importate.', impFail: '%d nu au putut fi salvate.', rdTitle: 'Cine citește această carte', rdFin: 'Terminată', socTitle: 'Funcții sociale', socHint: 'Ce împarte acest server între utilizatori. Timpul de ascultare alimentează clasamentul; celelalte două dezvăluie ce citește fiecare, așa că rămân oprite până le pornești. Se aplică tuturor deodată.', socTime: 'Timp de ascultare (clasamentul serverului)', socContent: 'Titluri de top în statisticile partajate', socWho: 'Cine citește (paginile cărților)', socUsers: 'Per utilizator', socUsersHint: 'Dacă oprești pe cineva, nimic al lui nu se mai partajează, indiferent de alegerea lui; doar tu poți reveni. "S-a retras singur" înseamnă că persoana a spus nu ea însăși.', socOpted: 's-a retras singur', srvTitle: 'Setări implicite ale serverului', srvHint: 'Salvează setările tale actuale ca implicite pentru toți utilizatorii acestui server. Fiecare poate personaliza peste. Montează un volum la /data/nh în containerul temei ca să reziste actualizărilor.', srvSave: 'Salvează ca implicite ale serverului', srvClear: 'Șterge implicitele serverului', srvSaved: 'Salvat', srvCleared: 'Șters', srvErr: 'Eșuat: necesită autentificare de administrator', crMode: 'Raftul Continuă lectura', crCombine: 'Combină cu caruselul', crSeparate: 'Păstrează ca raft separat', crHide: 'Ascuns', fsHint: 'Împarte-ți ascultarea cu ceilalți de pe acest server: totaluri de timp și minute pe zi pentru clasament, iar dacă administratorul le-a pornit, și titlurile tale de top și ce cărți citești sau ai terminat. Pornit implicit. La oprire, tot ce ai partajat se șterge.' , amfHide: 'Ascunde Marchează ca terminată' , amfConfirm: 'Marchezi această carte ca terminată?' , fsHint: 'Împărtășește un rezumat al ascultării tale (totaluri și minute pe zi) cu ceilalți de pe acest server, alimentează clasamentul. Activ implicit. La dezactivare dispari din clasament și din totaluri, iar datele partajate se șterg.' , srToggle: 'Împarte ce cărți citești', srHint: 'Adaugă la ceea ce văd ceilalți ce cărți citești acum, pe care le-ai terminat și pe care le asculți cel mai mult. Oprit implicit; trebuie pornite și comutatoarele de server ale administratorului.' , fsHint: 'Împarte timpul tău de ascultare (totaluri și minute pe zi) cu ceilalți de pe acest server; el alimentează clasamentul. Pornit implicit. Dacă îl oprești, ceilalți utilizatori nu te mai văd; administratorii serverului văd în continuare activitatea, marcată ca retrasă.', srHint: 'Le permite celorlalți să vadă și ce cărți citești, pe care le-ai terminat și pe care le asculți cel mai mult. Nu apare nimic până când administratorul nu pornește comutatoarele de server corespunzătoare.', seshTitle: 'Sesiuni recente', seshNow: 'Ascultă acum' , hcHint: 'Trimite evaluările tale în contul Hardcover. Lipește cheia API din setările contului Hardcover; cheile expiră după un an.', hcKeyLbl: 'Cheie API', hcToggle: 'Sincronizează automat evaluările noi', hcPushAll: 'Trimite toate evaluările', hcPushDiff: 'Trimite doar diferențele', hcDone: 'Gata: %d sincronizate, %d fără potrivire', hcNoKey: 'Lipește mai întâi cheia API Hardcover' , hcMissHead: 'Nu s-a găsit pe Hardcover:' , hcHint: 'Trimite evaluările tale în contul Hardcover. Lipește cheia API din setările contului Hardcover.' , tabStyle: 'Stil', tabMain: 'Pagina principală', tabBooks: 'Cărți', tabFeat: 'Funcții' , hcImport: 'Importă din Hardcover', impMarkQ: 'Marchezi și aici cele %d cărți potrivite ca terminate?', impMarkDone: '%d marcate ca terminate.' , impHint: 'Adu-ți evaluările din Goodreads, StoryGraph sau Hardcover. Alege aici CSV-ul exportat; Hardcover se importă direct cu butonul de pe cardul său. Fiecare potrivire este afișată spre confirmare înainte de salvare.' , impErrUnknown: 'Nu pare un export din Goodreads, StoryGraph sau Hardcover: nu s-au găsit coloane de titlu și evaluare.' , fsCard: 'Social', rnToggle: 'Afișează numele meu la evaluări', rsToggle: 'Partajează evaluările mele cu ceilalți', socNames: 'Nume la evaluări', rtAnon: 'Anonim' },
    lt: { cmStopped: "sustabdyta", cmScanErrs: "{e} nepatikrinta (Goodreads klaidos)", cmScanBusy: "Tikrinimas jau vyksta", cmGrErr: "Goodreads nepasiekiamas, bandysiu vėl", cmNoMatchYet: "Goodreads atitikmens nerasta", cmLooking: "Ieškoma Goodreads…", cmToolHint: "Ką apie šią knygą mano Goodreads skaitytojai. Klaustukas rodo priskirtą knygą ir leidžia ją pakeisti.", cmSearchPh: "Ieškoti Goodreads…", cmNoVotes: "dar nėra įvertinimų", cmRefresh: "Atnaujinti", cmScanAll: "Tikrinti viską iš naujo, ne tik trūkstamus", cmScanDone: "Baigta: {m} rasta · {x} nerasta · {s} jau turėjo", cmStop: "Sustabdyti", cmScanLibs: "Bibliotekos, kurias tikrinti", cmScan: "Ieškoti įvertinimų", lsLongest: "Ilgiausios knygos", lsSize: "Dydis diske", lsDuration: "Bendra trukmė", lsGenresN: "Žanrai", lsSeriesN: "Serijos", lsAuthorsN: "Autoriai", lsBooks: "Knygos", lsGlance: "Biblioteka trumpai", cmOpen: "Atidaryti Goodreads", cmDetect: "Rasti", cmAddr: "Pagalbininko adresas", scmTitle: "Serijos puslapis", scmHint: "Kaip atrodo sugeneruotas viršelis virš serijos pavadinimo, kai savas neįkeltas (tik ši naršyklė).", scmDeck: "Sluoksniuota kaladė", cmHint: "Ką apie knygą mano Goodreads skaitytojai: vidutinis įvertinimas ir balsų skaičius, po žvaigždutėmis knygos puslapyje bei kaip rikiavimas ir filtras. Knygų puslapiai užsipildo patys naršant.", cmGrOff: "Goodreads: nenustatyta", cmSetup: "Dar nėra pagalbininko? Paleiskite tai šalia temos (compose eilutės žemiau) ir spauskite Rasti. Tinka ir bet koks kitas atvaizdo paleidimo būdas: įklijuokite jo adresą viršuje.", cmGrOn: "Goodreads: prijungta", cmGrDown: "Goodreads: nustatyta, bet neatsako", cmGrNoNum: "Goodreads: atsako, bet ši abs-tract versija neduoda įvertinimų (naudokite versiją su įvertinimais, žr. README)", cmTitle: "Goodreads įvertinimai", cmWrong: "Ne ta knyga?", cmFind: "Rasti bendruomenės įvertinimą…", cmNone: "Nėra atitikmens", cmUse: "Naudoti šį", lfCommunity: "Goodreads įvertinimas", mfNatChange: "Keisti…", lsByTime: "pagal klausymo laiką", sbTitle: "Serverio reitingas", sbWeek: "Savaitė", sbMonth: "Mėnuo", sbYear: "Metai", sbAll: "Visą laiką", sbBooks: "Knygos", sbTopBooks: "Daugiausiai klausytos", sbTotal: "Iš viso", sbListeners: "Klausytojai", sbAvg: "Vidurkis", wkTitle: "Klausymo minutės, paskutinės 7 dienos", wkAvg: "Dienos vidurkis", wkRow: "Dienų iš eilės", rpBadge: "Atviri pranešimai apie problemas", rpMenu: "Pranešti apie problemą", rpTitle: "Pranešti apie problemą", rpWhat: "Kas negerai?", rpNote: "Dar kas nors administratoriui? (nebūtina)", rpSend: "Siųsti pranešimą", rpSent: "Išsiųsta. Ačiū.", rpFail: "Nepavyko išsiųsti", rpMissing: "Trūkstamas ar nepilnas turinys", rpQuality: "Prasta garso kokybė", rpPlay: "Negroja", rpWrong: "Ne ta knyga, viršelis ar metaduomenys", rpChapters: "Neteisingi skyriai", rpOther: "Kita", rfRate: "Įvertinti", rfSheet: "Įvertink šią knygą", rfOpen: "Atidaryti knygos puslapį", rfPickHint: "Pasirink įvertinimą", rfTitle: "Įvertink, ką baigei", ysFinished: "Neseniai baigtos", ysAlmost: "Beveik baigtos", ysMarkDone: "Žymėti kaip baigtą", ysEditDate: "Keisti pabaigos datą", ysThisWeek: "Šią savaitę", ysBestDay: "Geriausia diena", ysTitle: "Tavo klausymas", fsTitle: "Serverio reitingas", fsCard: "Serverio reitingas", fsToggle: "Dalintis mano klausymo statistika", fsJoin: "Įjunk „Dalintis mano klausymo statistika“ personalizavimo skydelyje, kad atsirastum serverio reitinge.", fsWaiting: "Dalijimasis įjungtas, tavo santrauka netrukus prisijungs prie serverio reitingo kartu su kitais.", fsHint: "Dalinkis savo klausymo santrauka (sumos ir minutės per dieną) su kitais šiame serveryje, ji maitina serverio reitingą. Įjungta pagal numatymą. Išjungus dingsti iš reitingo ir jo sumų, o pasidalinti duomenys ištrinami.", yirBtn: "Metų apžvalga", yirTitle: "Tavo klausymo metai", yirYear: "Paskutiniai 12 mėnesių", yirDays: "Dienos su klausymu", yirBest: "Geriausia diena", yirMonths: "Pagal mėnesius", yirNone: "Klausymo duomenų dar nėra.", ssscmDeck: "Sluoksniuota kaladė", scmFirst: "Pirmos knygos viršelis", scmGrid: "Viršelių tinklelis (numatytasis)", scmOff: "Paslėpta" , lsTitle: "Serverio statistika", lsAll: "Visos bibliotekos", lsPlayed: "Grotos knygos", lsBest: "Geriausiai įvertintos", lsGenres: "Populiariausi žanrai", srvPick: "Ką išsaugoti:", srvGRest: "Funkcijos ir elgsena", aeBtn: "Sutvarkyti autorius", aeTitle: "Autoriai be knygų", aeNone: "Kiekvienas autorius čia turi bent vieną knygą.", aeRemove: "Pašalinti", aeDone: "Pašalinta", aeWorking: "Šalinama…", rpCard: "Pranešti nesklandumai", rpHint: "Nesklandumai, apie kuriuos naudotojai pranešė iš knygos puslapio. Išsprendus, įrašas dingsta iš sąrašo.", rpEmpty: "Nieko nepranešta.", rpResolve: "Išspręsti", rpOpen: "Atidaryti knygą", apCard: "Grojimas", apToggle: "Automatiškai groti kitą serijos knygą", apHint: "Kai knyga baigiasi, automatiškai pradedama kita tos pačios serijos knyga. Tik jau turimos ir nebaigtos knygos.", apNext: "Kita serijoje", fdToggle: "Baigtų knygų įrankiai statistikoje", ysNoDate: "be datos", hmShowing: "grafikas rodo", ysStreak: "Dienų serija", ysCurrent: "Dabartinė", ysLongest: "Ilgiausia", ysDaysTotal: "Iš viso dienų", ysPace: "Tempas", ysPerDay: "Per klausymo dieną", ysWhen: "Kada klausai", ysTopBooks: "Daugiausiai klausytos knygos", ysTopAuthors: "Daugiausiai klausyti autoriai", ysTopNarrators: "Daugiausiai klausyti skaitovai", rfToggle: "Skiltis „Įvertink“ pradžios puslapyje", hoTitle: "Pradžios puslapio skilčių tvarka", hoEmpty: "Kartą atidaryk pradžios puslapį ir grįžk čia.", hoReset: "Atkurti numatytą tvarką", ctIcon: "Piktograma", ctColour: "Akcentas", ctNeedBook: "Audiobookshelf negali išsaugoti tuščios kolekcijos, pasirink bent vieną knygą. Vėliau galėsi laisvai pridėti ir šalinti.", colEditIcon: "Keisti piktogramą", ciTitle: "Kolekcijos piktograma", ciReset: "Automatiškai pagal pavadinimą", colEditDesc: "Redaguoti aprašymą", colBookForms: ["knyga","knygos","knygų"], colForms: ["Kolekcija","Kolekcijos","Kolekcijų"], ctTitle: "Sukurti kolekciją", ctBlank: "Tuščia kolekcija", ctNew: "Nauja kolekcija", ctNewHint: "iš šablono", ctNamePh: "Kolekcijos pavadinimas", ctSearch: "Ieškoti knygų ar autorių…", ctCreate: "Sukurti", ctBack: "‹ Atgal", ctAddAll: "Pridėti visas knygas", abTitle: "Pridėti knygų", abAdd: "Pridėti", avRemove: "Pašalinti nuotrauką", avSet: "Pasirinkti nuotrauką…", ugListened: "Išklausyta", ugPhoto: "Profilio nuotrauka", ugNever: "Niekada", ugSeen: "Paskutinį kartą aktyvus", ugEdit: "Redaguoti naudotoją", ugDelete: "Ištrinti naudotoją", ugAdd: "Pridėti nuotrauką", ugChange: "Keisti nuotrauką", ugSortName: "A–Ž", ugSortTime: "Daugiausiai klausyta", ugSortSeen: "Neseniai aktyvūs", bsCard: "Knygos nuorodos", bsHint: "Mygtukai knygos puslapyje pavadinimui ieškoti kitur. Goodreads ir didžiausia tavo kalbos svetainė įjungtos iš anksto.", bsFind: "Ieškoti", bsGlobal: "Visur", bsLocal: "Tavo kalbai", bsOther: "Kitos šalys", pgCard: "Perdarytos puslapiai", pgHint: "Kiekviena parinktis pakeičia Audiobookshelf puslapį NanoHive versija. Išjunk, kad grąžintum originalą.", pgNarrators: "Skaitovų kortelės", pgCollections: "Kolekcijų puslapiai", pgUsers: "Naudotojų kortelės (nustatymai)", pgStats: "Klausymo reitingas", pgPhoto: "Profilio nuotrauka viršutinėje juostoje", pgCinematic: "Kinematografinis fonas", pgTransitions: "Puslapių perėjimai", pSearch: "Ieškoti nustatymų…", pSearchNone: "Atitikmenų nėra.", erTypeface: "Šriftas", erPage: "Puslapio tema", erText: "Teksto spalva", erBg: "Fonas", erDefault: "Numatytasis (ABS)", erAuto: "Automatinis", nrForms: ["Skaitovas","Skaitovai","Skaitovų"], nrBooksForms: ["knyga","knygos","knygų"], nrSearch: "Filtruoti skaitovus…", nrSortName: "Vardas", nrSortBooks: "Daugiausiai knygų", auForms: ["Autorius","Autoriai","Autorių"], auSearch: "Filtruoti autorius…" , lfByAuthor: "Autorius › serijos tvarka", lfBySeries: "Serija › tvarka" , lfSecFilters: "Daugiau filtrų", lfSecSort: "Kelių lygių rikiavimas", lfAuthor: "Autorius", lfSeries: "Serija", lfTitle: "Pavadinimas", lfYear: "Metai", lfAdded: "Pridėta", lfDuration: "Trukmė", lfNarrator: "Skaitovas", lfGenre: "Žanras", lfLanguage: "Kalba", lfProgress: "Eiga", lfPgFinished: "Baigtos", lfPgInProgress: "Klausomos", lfPgNot: "Nepradėtos" , logoUpload: 'Įkelti iš įrenginio…', logoUploaded: 'Įkelta ✓', logoBadType: 'Nepalaikomas paveikslėlio tipas', logoTooBig: 'Paveikslėlis per didelis (maks. 4 MB)', logoOr: 'arba', bookPage: 'Knygos puslapis', ratingsToggle: 'Bendruomenės įvertinimai (žvaigždutės ir atsiliepimai)', ratingsHint: 'Žvaigždučių įvertinimai su trumpais atsiliepimais kiekviename knygos puslapyje, bendri visiems šio serverio naudotojams.', searchCard: 'Paieška', gsToggle: 'Ieškoti visose bibliotekose iš karto', gsHint: 'Vienas sujungtas rezultatų sąrašas iš visų pasiekiamų bibliotekų, su bibliotekos ženkleliu prie kiekvieno rezultato. Išjunkite, kad grįžtų meniu pagal biblioteką.', gsNoResults: 'Rezultatų nėra', gsSearching: 'Ieškoma…', gsBooks: 'Knygos', gsSeries: 'Serijos', gsAuthors: 'Autoriai', spTitle: 'Pradžios puslapis', spHint: 'Visada atidaryti programą pasirinktame bibliotekos rodinyje (tik ši naršyklė). Tiesioginės nuorodos niekada neperadresuojamos.', spDefault: 'Programos numatytasis', spViewHome: 'Pradžia', spViewLibrary: 'Biblioteka', spViewSeries: 'Serijos', spViewCollections: 'Kolekcijos', spViewAuthors: 'Autoriai', scSet: 'Nustatyti serijos viršelį', scRemove: 'Pašalinti serijos viršelį', scEditDesc: 'Redaguoti serijos aprašymą', cardStars: 'Žvaigždutės ant viršelių', hoverPolish: 'Kortelių šlifas užvedus (švelnesnis tamsinimas, akcentinis mygtukas, žvaigždutės užvedus)', sectUser: 'Jūsų nustatymai', sectAdmin: 'Administravimas', lockTitle: 'Funkcijų jungikliai', lockHint: 'Priverstinai išjungia funkciją visiems šio serverio naudotojams. Nugali asmeninius nustatymus; kiti pamatys po kito perkrovimo.', lockRatings: 'Išjungti įvertinimus visur', lockCardRatings: 'Išjungti žvaigždutes ant viršelių', lockGlobalSearch: 'Išjungti visuotinę paiešką', lockHoverPolish: 'Išjungti kortelių šlifą', lockHeroCarousel: 'Išjungti pradžios karuselę', lockedNote: 'išjungė administratorius', lfSort: 'Įvertinimas', lfRated: 'Įvertintos', lfUnrated: 'Neįvertintos', lfClear: 'Išvalyti', lfNone: 'Nė viena knyga neatitinka', lfSection: 'Įvertinimas', lfWho: 'Kieno įvertinimai', lfEveryone: 'Visi', sdSave: 'Išsaugoti', sdClear: 'Atkurti knygos aprašymą', sdMore: 'Skaityti daugiau', sdLess: 'Rodyti mažiau', shSeries: 'Serija', shBy: '', shMore: ' ir kiti', shBookForms: ['knyga', 'knygos', 'knygų'], shAvg: 'knygų vidurkis', shRated: 'įvertinta', impLabel: 'Importuoti įvertinimus', impHint: 'Perkelkite įvertinimus iš StoryGraph ar Goodreads. Ten eksportuokite knygas į CSV ir pasirinkite failą čia. Kiekvienas atitikmuo rodomas patvirtinti, ir niekas neišsaugoma, kol nepatvirtinsite.', impBtn: 'Pasirinkti CSV failą…', impReading: 'Skaitomas failas…', impMatching: 'Lyginama su jūsų bibliotekomis…', impErrUnknown: 'Tai nepanašu į StoryGraph ar Goodreads eksportą. Tai turi būti jų CSV su stulpeliu "Star Rating" arba "My Rating".', impErrEmpty: 'Faile nėra eilučių.', impErrNoRated: 'Faile nėra įvertintų knygų.', impErrRead: 'Nepavyko perskaityti failo.', impErrLoad: 'Nepavyko įkelti bibliotekų. Po akimirkos bandykite dar kartą.', impErrNoLibs: 'Nė viena biblioteka nedalyvauja įvertinimuose, todėl nėra kur importuoti.', impTitle: 'Importuoti įvertinimus', impFrom: '%s eksportas: %d knygų, iš jų %d įvertintų', impLibs: 'Ieškoma: %s', impMatched: 'Sutapo', impCheck: 'Verta patikrinti', impCheckHint: 'Arti, bet nesu tikras. Patikrinkite atitikmenį arba pasirinkite kitą.', impAlready: 'Jau įvertinta jūsų', impAlreadyHint: 'Lieka nepaliestos, nebent paprašysite perrašyti.', impNone: 'Nėra jūsų bibliotekoje', impNoneHint: 'Šiame serveryje niekas jų neatitinka.', impAll: 'Visos', impSkip: 'Praleisti šią', impDup: 'dublikatas', impByTitle: 'pavadinimas', impYours: 'jūsų: %s', impReviews: 'Importuoti ir parašytus atsiliepimus (%d)', impOver: 'Perrašyti jau turimus mano įvertinimus', impGo: 'Importuoti %d įvertinimų', impCancel: 'Atšaukti', impCloseBtn: 'Uždaryti', impWorking: 'Saugoma… %d iš %d', impDone: 'Importuota %d įvertinimų.', impFail: '%d nepavyko išsaugoti.', rdTitle: 'Kas skaito šią knygą', rdFin: 'Baigta', socTitle: 'Socialinės funkcijos', socHint: 'Ką šis serveris dalija tarp naudotojų. Klausymo laikas maitina reitingą; kitos dvi atskleidžia, ką kas skaito, todėl lieka išjungtos, kol jų neįjungsite. Galioja visiems iš karto.', socTime: 'Klausymo laikas (serverio reitingas)', socContent: 'Populiariausi pavadinimai bendroje statistikoje', socWho: 'Kas skaito (knygų puslapiai)', socUsers: 'Pagal naudotoją', socUsersHint: 'Išjungus ką nors, niekas iš jo duomenų nebedalijama, kad ir ką jis pasirinko; grąžinti galite tik jūs. "Atsisakė pats" reiškia, kad žmogus pats pasakė ne.', socOpted: 'atsisakė pats', srvTitle: 'Serverio numatytieji', srvHint: 'Išsaugokite dabartinius nustatymus kaip numatytuosius visiems šio serverio naudotojams. Kiekvienas vis tiek galės prisitaikyti savus. Prijunkite tomą prie /data/nh temos konteineryje, kad jie išliktų per atnaujinimus.', srvSave: 'Išsaugoti kaip serverio numatytuosius', srvClear: 'Išvalyti serverio numatytuosius', srvSaved: 'Išsaugota', srvCleared: 'Išvalyta', srvErr: 'Nepavyko: reikia administratoriaus prisijungimo', crMode: 'Lentyna Tęsti skaitymą', crCombine: 'Sujungti su karusele', crSeparate: 'Palikti atskira lentyna', crHide: 'Paslėpta', fsHint: 'Dalinkitės savo klausymu su kitais šio serverio žmonėmis: laiko sumos ir minutės pagal dienas reitingui, o jei administratorius įjungė, ir jūsų populiariausi pavadinimai bei kokias knygas skaitote ar baigėte. Įjungta pagal numatymą. Išjungus viskas, kuo dalinotės, ištrinama.' , amfHide: 'Slėpti Pažymėti kaip baigtą' , amfConfirm: 'Pažymėti šią knygą kaip baigtą?' , fsHint: 'Dalinkis savo klausymo santrauka (sumos ir minutės per dieną) su kitais šiame serveryje, ji maitina serverio reitingą. Įjungta pagal numatymą. Išjungus dingsti iš reitingo ir jo sumų, o pasidalinti duomenys ištrinami.' , srToggle: 'Dalytis, kokias knygas skaitote', srHint: 'Prideda prie to, ką mato kiti, kokias knygas dabar skaitote, kokias baigėte ir kokių klausotės daugiausia. Pagal numatymą išjungta; taip pat turi būti įjungti administratoriaus serverio jungikliai.' , fsHint: 'Dalinkitės klausymo laiku (sumos ir minutės pagal dienas) su kitais šiame serveryje; jis maitina reitingą. Įjungta pagal numatymą. Išjungus kiti naudotojai jūsų nebemato; serverio administratoriai veiklą mato toliau, pažymėtą kaip atsisakytą.', srHint: 'Taip pat leidžia kitiems matyti, kokias knygas skaitote, kokias baigėte ir kokių klausotės daugiausia. Niekas nerodoma, kol administratorius neįjungia atitinkamų serverio jungiklių.', seshTitle: 'Naujausios sesijos', seshNow: 'Klausosi dabar' , hcHint: 'Siųskite savo įvertinimus į Hardcover paskyrą. Įklijuokite API raktą iš Hardcover paskyros nustatymų; raktai baigia galioti po metų.', hcKeyLbl: 'API raktas', hcToggle: 'Automatiškai sinchronizuoti naujus įvertinimus', hcPushAll: 'Siųsti visus įvertinimus', hcPushDiff: 'Siųsti tik skirtumus', hcDone: 'Atlikta: %d sinchronizuota, %d be atitikmens', hcNoKey: 'Pirmiausia įklijuokite savo Hardcover API raktą' , hcMissHead: 'Nerasta Hardcover:' , hcHint: 'Siųskite savo įvertinimus į Hardcover paskyrą. Įklijuokite API raktą iš Hardcover paskyros nustatymų.' , tabStyle: 'Stilius', tabMain: 'Pagrindinis', tabBooks: 'Knygos', tabFeat: 'Funkcijos' , hcImport: 'Importuoti iš Hardcover', impMarkQ: 'Pažymėti ir čia %d atitikusias knygas kaip baigtas?', impMarkDone: '%d pažymėta kaip baigtos.' , impHint: 'Perkelkite savo įvertinimus iš Goodreads, StoryGraph ar Hardcover. Čia pasirinkite eksportuotą CSV; Hardcover importuojamas tiesiogiai mygtuku jo kortelėje. Kiekvienas atitikmuo rodomas patvirtinti prieš ką nors išsaugant.' , impErrUnknown: 'Tai nepanašu į Goodreads, StoryGraph ar Hardcover eksportą: nerasta pavadinimo ir įvertinimo stulpelių.' , fsCard: 'Socialu', rnToggle: 'Rodyti mano vardą prie įvertinimų', rsToggle: 'Dalytis mano įvertinimais su kitais', socNames: 'Vardai prie įvertinimų', rtAnon: 'Anonimas' },
    lv: { cmStopped: "apturēts", cmScanErrs: "{e} nav pārbaudīts (Goodreads kļūdas)", cmScanBusy: "Pārbaude jau notiek", cmGrErr: "Goodreads nav sasniedzams, mēģināšu vēlreiz", cmNoMatchYet: "Goodreads atbilstība nav atrasta", cmLooking: "Meklē Goodreads…", cmToolHint: "Ko par šo grāmatu domā Goodreads lasītāji. Jautājuma zīme rāda piemeklēto grāmatu un ļauj to mainīt.", cmSearchPh: "Meklēt Goodreads…", cmNoVotes: "vēl nav vērtējumu", cmRefresh: "Atjaunot", cmScanAll: "Pārbaudīt visu no jauna, ne tikai trūkstošos", cmScanDone: "Gatavs: {m} atrasti · {x} nav atrasti · {s} jau bija", cmStop: "Apturēt", cmScanLibs: "Pārbaudāmās bibliotēkas", cmScan: "Meklēt vērtējumus", lsLongest: "Garākās grāmatas", lsSize: "Lielums diskā", lsDuration: "Kopējais ilgums", lsGenresN: "Žanri", lsSeriesN: "Sērijas", lsAuthorsN: "Autori", lsBooks: "Grāmatas", lsGlance: "Bibliotēka īsumā", cmOpen: "Atvērt Goodreads", cmDetect: "Atrast", cmAddr: "Palīga adrese", scmTitle: "Sērijas lapa", scmHint: "Kā izskatās ģenerētais vāks virs sērijas nosaukuma, ja savs nav augšupielādēts (tikai šī pārlūkprogramma).", scmDeck: "Slāņaina kava", cmHint: "Ko par grāmatu domā Goodreads lasītāji: vidējais vērtējums un balsu skaits, zem zvaigznēm grāmatas lapā un kā kārtošana un filtrs. Grāmatu lapas aizpildās pašas pārlūkojot.", cmGrOff: "Goodreads: nav iestatīts", cmSetup: "Vēl nav palīga? Palaidiet šo blakus tēmai (compose rindas zemāk) un nospiediet Atrast. Der arī jebkurš cits veids, kā palaist attēlu: ielīmējiet tā adresi augstāk.", cmGrOn: "Goodreads: savienots", cmGrDown: "Goodreads: iestatīts, bet neatbild", cmGrNoNum: "Goodreads: atbild, bet šī abs-tract versija nedod vērtējumus (izmantojiet versiju ar vērtējumiem, sk. README)", cmTitle: "Goodreads vērtējumi", cmWrong: "Nepareizā grāmata?", cmFind: "Atrast kopienas vērtējumu…", cmNone: "Nav atbilstības", cmUse: "Izmantot šo", lfCommunity: "Goodreads vērtējums", mfNatChange: "Mainīt…", lsByTime: "pēc klausīšanās laika", sbTitle: "Servera reitings", sbWeek: "Nedēļa", sbMonth: "Mēnesis", sbYear: "Gads", sbAll: "Viss laiks", sbBooks: "Grāmatas", sbTopBooks: "Visvairāk klausītās", sbTotal: "Kopā", sbListeners: "Klausītāji", sbAvg: "Vidēji", wkTitle: "Klausīšanās minūtes, pēdējās 7 dienas", wkAvg: "Dienas vidējais", wkRow: "Dienas pēc kārtas", rpBadge: "Atvērti problēmu ziņojumi", rpMenu: "Ziņot par problēmu", rpTitle: "Ziņot par problēmu", rpWhat: "Kas nav kārtībā?", rpNote: "Vēl kas administratoram? (neobligāti)", rpSend: "Sūtīt ziņojumu", rpSent: "Nosūtīts. Paldies.", rpFail: "Neizdevās nosūtīt", rpMissing: "Trūkstošs vai nepilnīgs saturs", rpQuality: "Slikta skaņas kvalitāte", rpPlay: "Neatskaņojas", rpWrong: "Nepareizā grāmata, vāks vai metadati", rpChapters: "Nepareizas nodaļas", rpOther: "Kas cits", rfRate: "Vērtēt", rfSheet: "Novērtē šo grāmatu", rfOpen: "Atvērt grāmatas lapu", rfPickHint: "Izvēlies vērtējumu", rfTitle: "Novērtē pabeigto", ysFinished: "Nesen pabeigtās", ysAlmost: "Gandrīz pabeigtas", ysMarkDone: "Atzīmēt kā pabeigtu", ysEditDate: "Mainīt pabeigšanas datumu", ysThisWeek: "Šonedēļ", ysBestDay: "Labākā diena", ysTitle: "Tava klausīšanās", fsTitle: "Servera reitings", fsCard: "Servera reitings", fsToggle: "Kopīgot manu klausīšanās statistiku", fsJoin: "Ieslēdz “Kopīgot manu klausīšanās statistiku” personalizācijas panelī, lai parādītos servera reitingā.", fsWaiting: "Kopīgošana ieslēgta, tavs kopsavilkums drīz pievienosies servera reitingam kopā ar pārējiem.", fsHint: "Kopīgo savas klausīšanās kopsavilkumu (kopsummas un minūtes dienā) ar pārējiem šajā serverī, tas darbina servera reitingu. Ieslēgts pēc noklusējuma. Izslēdzot tu pazūdi no reitinga un tā kopsummām, un kopīgotie dati tiek dzēsti.", yirBtn: "Gads pārskatā", yirTitle: "Tavs klausīšanās gads", yirYear: "Pēdējie 12 mēneši", yirDays: "Dienas ar klausīšanos", yirBest: "Labākā diena", yirMonths: "Pa mēnešiem", yirNone: "Klausīšanās datu vēl nav.", ssscmDeck: "Slāņaina kava", scmFirst: "Pirmās grāmatas vāks", scmGrid: "Vāku režģis (noklusējums)", scmOff: "Paslēpts" , lsTitle: "Servera statistika", lsAll: "Visas bibliotēkas", lsPlayed: "Atskaņotās grāmatas", lsBest: "Vislabāk novērtētās", lsGenres: "Top žanri", srvPick: "Ko saglabāt:", srvGRest: "Funkcijas un uzvedība", aeBtn: "Sakārtot autorus", aeTitle: "Autori bez grāmatām", aeNone: "Katram autoram šeit ir vismaz viena grāmata.", aeRemove: "Noņemt", aeDone: "Noņemts", aeWorking: "Noņem…", rpCard: "Ziņotās problēmas", rpHint: "Problēmas, par kurām lietotāji ziņoja no grāmatas lapas. Atrisināšana noņem ierakstu no saraksta.", rpEmpty: "Nekas nav ziņots.", rpResolve: "Atrisināt", rpOpen: "Atvērt grāmatu", apCard: "Atskaņošana", apToggle: "Automātiski atskaņot nākamo sērijas grāmatu", apHint: "Kad grāmata beidzas, automātiski sākas nākamā no tās pašas sērijas. Tikai grāmatas, kas jau ir tavā bibliotēkā un nav pabeigtas.", apNext: "Nākamā sērijā", fdToggle: "Pabeigto grāmatu rīki statistikā", ysNoDate: "bez datuma", hmShowing: "grafiks rāda", ysStreak: "Dienu virkne", ysCurrent: "Pašreizējā", ysLongest: "Garākā", ysDaysTotal: "Dienas kopā", ysPace: "Temps", ysPerDay: "Klausīšanās dienā", ysWhen: "Kad tu klausies", ysTopBooks: "Visvairāk klausītās grāmatas", ysTopAuthors: "Visvairāk klausītie autori", ysTopNarrators: "Visvairāk klausītie lasītāji", rfToggle: "Sadaļa “Novērtē” sākumlapā", hoTitle: "Sākumlapas sadaļu secība", hoEmpty: "Atver sākumlapu vienreiz un atgriezies šeit.", hoReset: "Atjaunot noklusējuma secību", ctIcon: "Ikona", ctColour: "Akcents", ctNeedBook: "Audiobookshelf nevar saglabāt tukšu kolekciju, izvēlies vismaz vienu grāmatu. Vēlāk vari brīvi pievienot un noņemt.", colEditIcon: "Mainīt ikonu", ciTitle: "Kolekcijas ikona", ciReset: "Automātiski pēc nosaukuma", colEditDesc: "Rediģēt aprakstu", colBookForms: ["grāmata","grāmatas"], colForms: ["Kolekcija","Kolekcijas"], ctTitle: "Izveidot kolekciju", ctBlank: "Tukša kolekcija", ctNew: "Jauna kolekcija", ctNewHint: "no veidnes", ctNamePh: "Kolekcijas nosaukums", ctSearch: "Meklēt grāmatas vai autorus…", ctCreate: "Izveidot", ctBack: "‹ Atpakaļ", ctAddAll: "Pievienot visas grāmatas", abTitle: "Pievienot grāmatas", abAdd: "Pievienot", avRemove: "Noņemt foto", avSet: "Izvēlēties foto…", ugListened: "Noklausīts", ugPhoto: "Profila foto", ugNever: "Nekad", ugSeen: "Pēdējoreiz aktīvs", ugEdit: "Rediģēt lietotāju", ugDelete: "Dzēst lietotāju", ugAdd: "Pievienot foto", ugChange: "Mainīt foto", ugSortName: "A–Ž", ugSortTime: "Visvairāk klausīts", ugSortSeen: "Nesen aktīvie", bsCard: "Grāmatas saites", bsHint: "Pogas grāmatas lapā nosaukuma meklēšanai citur. Goodreads un tavas valodas lielākā vietne ir ieslēgtas jau sākumā.", bsFind: "Meklēt vietnē", bsGlobal: "Visur", bsLocal: "Tavai valodai", bsOther: "Citas valstis", pgCard: "Pārveidotās lapas", pgHint: "Katra opcija aizstāj Audiobookshelf lapu ar NanoHive versiju. Izslēdz, lai atgrieztu oriģinālu.", pgNarrators: "Lasītāju kartītes", pgCollections: "Kolekciju lapas", pgUsers: "Lietotāju kartītes (iestatījumi)", pgStats: "Klausīšanās reitings", pgPhoto: "Profila foto augšējā joslā", pgCinematic: "Kino fons", pgTransitions: "Lapu pārejas", pSearch: "Meklēt iestatījumus…", pSearchNone: "Nekas neatbilst.", erTypeface: "Fonts", erPage: "Lapas motīvs", erText: "Teksta krāsa", erBg: "Fons", erDefault: "Noklusējums (ABS)", erAuto: "Auto", nrForms: ["Lasītājs","Lasītāji"], nrBooksForms: ["grāmata","grāmatas"], nrSearch: "Filtrēt lasītājus…", nrSortName: "Vārds", nrSortBooks: "Visvairāk grāmatu", auForms: ["Autors","Autori"], auSearch: "Filtrēt autorus…" , lfByAuthor: "Autors › sērijas secība", lfBySeries: "Sērija › secība" , lfSecFilters: "Vairāk filtru", lfSecSort: "Daudzlīmeņu kārtošana", lfAuthor: "Autors", lfSeries: "Sērija", lfTitle: "Nosaukums", lfYear: "Gads", lfAdded: "Pievienots", lfDuration: "Ilgums", lfNarrator: "Lasītājs", lfGenre: "Žanrs", lfLanguage: "Valoda", lfProgress: "Progress", lfPgFinished: "Pabeigtās", lfPgInProgress: "Procesā", lfPgNot: "Nesāktās" , logoUpload: 'Augšupielādēt no ierīces…', logoUploaded: 'Augšupielādēts ✓', logoBadType: 'Neatbalstīts attēla tips', logoTooBig: 'Attēls par lielu (maks. 4 MB)', logoOr: 'vai', bookPage: 'Grāmatas lapa', ratingsToggle: 'Kopienas vērtējumi (zvaigznes un atsauksmes)', ratingsHint: 'Zvaigžņu vērtējumi ar īsām atsauksmēm katrā grāmatas lapā, kopīgi visiem šī servera lietotājiem.', searchCard: 'Meklēšana', gsToggle: 'Meklēt visās bibliotēkās uzreiz', gsHint: 'Viens apvienots rezultātu saraksts no visām pieejamajām bibliotēkām, ar bibliotēkas nozīmīti pie katra rezultāta. Izslēdziet, lai atgrieztu izvēlni pa bibliotēkām.', gsNoResults: 'Nav rezultātu', gsSearching: 'Meklē…', gsBooks: 'Grāmatas', gsSeries: 'Sērijas', gsAuthors: 'Autori', spTitle: 'Sākuma lapa', spHint: 'Vienmēr atvērt lietotni izvēlētajā bibliotēkas skatā (tikai šī pārlūkprogramma). Tiešās saites nekad netiek pāradresētas.', spDefault: 'Lietotnes noklusējums', spViewHome: 'Sākums', spViewLibrary: 'Bibliotēka', spViewSeries: 'Sērijas', spViewCollections: 'Kolekcijas', spViewAuthors: 'Autori', scSet: 'Iestatīt sērijas vāku', scRemove: 'Noņemt sērijas vāku', scEditDesc: 'Rediģēt sērijas aprakstu', cardStars: 'Zvaigznes uz vākiem', hoverPolish: 'Kartīšu piestrāde uzejot (maigāka aptumšošana, akcenta poga, zvaigznes uzejot)', sectUser: 'Jūsu iestatījumi', sectAdmin: 'Administrēšana', lockTitle: 'Funkciju slēdži', lockHint: 'Piespiedu kārtā izslēdz funkciju visiem šī servera lietotājiem. Uzvar personīgos iestatījumus; citi to redzēs pēc nākamās pārlādes.', lockRatings: 'Izslēgt vērtējumus visur', lockCardRatings: 'Izslēgt zvaigznes uz vākiem', lockGlobalSearch: 'Izslēgt globālo meklēšanu', lockHoverPolish: 'Izslēgt kartīšu piestrādi', lockHeroCarousel: 'Izslēgt sākuma karuseli', lockedNote: 'izslēdzis administrators', lfSort: 'Vērtējums', lfRated: 'Novērtētās', lfUnrated: 'Nenovērtētās', lfClear: 'Notīrīt', lfNone: 'Neviena grāmata neatbilst', lfSection: 'Vērtējums', lfWho: 'Kā vērtējumi', lfEveryone: 'Visi', sdSave: 'Saglabāt', sdClear: 'Atjaunot grāmatas aprakstu', sdMore: 'Lasīt vairāk', sdLess: 'Rādīt mazāk', shSeries: 'Sērija', shBy: '', shMore: ' un citi', shBookForms: ['grāmata', 'grāmatas', 'grāmatu'], shAvg: 'grāmatu vidējais', shRated: 'novērtētas', impLabel: 'Importēt vērtējumus', impHint: 'Pārnesiet vērtējumus no StoryGraph vai Goodreads. Tur eksportējiet grāmatas CSV formātā un izvēlieties failu šeit. Katra atbilsme tiek parādīta apstiprināšanai, un nekas netiek saglabāts, kamēr neapstiprināt.', impBtn: 'Izvēlēties CSV failu…', impReading: 'Lasa failu…', impMatching: 'Salīdzina ar jūsu bibliotēkām…', impErrUnknown: 'Tas neizskatās pēc StoryGraph vai Goodreads eksporta. Tam jābūt viņu CSV ar kolonnu "Star Rating" vai "My Rating".', impErrEmpty: 'Failā nav rindu.', impErrNoRated: 'Failā nav novērtētu grāmatu.', impErrRead: 'Failu neizdevās nolasīt.', impErrLoad: 'Bibliotēkas neizdevās ielādēt. Pēc brīža mēģiniet vēlreiz.', impErrNoLibs: 'Neviena bibliotēka nepiedalās vērtējumos, tāpēc nav kur importēt.', impTitle: 'Importēt vērtējumus', impFrom: '%s eksports: %d grāmatas, no tām %d novērtētas', impLibs: 'Meklē: %s', impMatched: 'Atbilsmes', impCheck: 'Vērts pārbaudīt', impCheckHint: 'Tuvu, bet nav droši. Pārbaudiet atbilsmi vai izvēlieties citu.', impAlready: 'Jau jūsu novērtētās', impAlreadyHint: 'Paliek neskartas, ja vien nepieprasāt pārrakstīšanu.', impNone: 'Nav jūsu bibliotēkā', impNoneHint: 'Nekas šajā serverī tām neatbilst.', impAll: 'Visas', impSkip: 'Izlaist šo', impDup: 'dublikāts', impByTitle: 'nosaukums', impYours: 'jūsu: %s', impReviews: 'Importēt arī rakstītās atsauksmes (%d)', impOver: 'Pārrakstīt manus esošos vērtējumus', impGo: 'Importēt %d vērtējumus', impCancel: 'Atcelt', impCloseBtn: 'Aizvērt', impWorking: 'Saglabā… %d no %d', impDone: 'Importēti %d vērtējumi.', impFail: '%d neizdevās saglabāt.', rdTitle: 'Kas lasa šo grāmatu', rdFin: 'Pabeigta', socTitle: 'Sociālās funkcijas', socHint: 'Ko šis serveris koplieto starp lietotājiem. Klausīšanās laiks darbina rangu tabulu; pārējās divas atklāj, ko katrs lasa, tāpēc tās paliek izslēgtas, kamēr tās neieslēdzat. Attiecas uz visiem uzreiz.', socTime: 'Klausīšanās laiks (servera rangs)', socContent: 'Populārākie nosaukumi koplietotajā statistikā', socWho: 'Kas lasa (grāmatu lapas)', socUsers: 'Pa lietotājiem', socUsersHint: 'Izslēdzot kādu, nekas no viņa datiem netiek koplietots neatkarīgi no viņa izvēles; atjaunot varat tikai jūs. "Atteicās pats" nozīmē, ka cilvēks pats pateica nē.', socOpted: 'atteicās pats', srvTitle: 'Servera noklusējumi', srvHint: 'Saglabājiet pašreizējos iestatījumus kā noklusējumu visiem šī servera lietotājiem. Katrs joprojām var pielāgot savus. Pievienojiet sējumu /data/nh tēmas konteinerā, lai tie saglabātos atjauninājumos.', srvSave: 'Saglabāt kā servera noklusējumu', srvClear: 'Notīrīt servera noklusējumu', srvSaved: 'Saglabāts', srvCleared: 'Notīrīts', srvErr: 'Neizdevās: nepieciešama administratora pieteikšanās', crMode: 'Plaukts Turpināt lasīt', crCombine: 'Apvienot ar karuseli', crSeparate: 'Paturēt kā atsevišķu plauktu', crHide: 'Paslēpts', fsHint: 'Kopīgojiet savu klausīšanos ar citiem šajā serverī: laika summas un minūtes pa dienām rangam, un, ja administrators tās ieslēdzis, arī jūsu populārākos nosaukumus un kuras grāmatas lasāt vai esat pabeidzis. Ieslēgts pēc noklusējuma. Izslēdzot viss kopīgotais tiek dzēsts.' , amfHide: 'Slēpt Atzīmēt kā pabeigtu' , amfConfirm: 'Atzīmēt šo grāmatu kā pabeigtu?' , fsHint: 'Kopīgo savas klausīšanās kopsavilkumu (kopsummas un minūtes dienā) ar pārējiem šajā serverī, tas darbina servera reitingu. Ieslēgts pēc noklusējuma. Izslēdzot tu pazūdi no reitinga un tā kopsummām, un kopīgotie dati tiek dzēsti.' , srToggle: 'Dalīties ar to, kādas grāmatas lasāt', srHint: 'Pievieno tam, ko redz citi, kādas grāmatas pašlaik lasāt, kuras esat pabeidzis un kuras klausāties visvairāk. Pēc noklusējuma izslēgts; jābūt ieslēgtiem arī administratora servera slēdžiem.' , fsHint: 'Kopīgojiet savu klausīšanās laiku (summas un minūtes pa dienām) ar citiem šajā serverī; tas darbina rangu. Ieslēgts pēc noklusējuma. Izslēdzot citi lietotāji jūs vairs neredz; servera administratori aktivitāti joprojām redz, atzīmētu kā atteiktu.', srHint: 'Ļauj citiem redzēt arī to, kādas grāmatas lasāt, kuras pabeigtas un kuras klausāties visvairāk. Nekas neparādās, kamēr administrators neieslēdz atbilstošos servera slēdžus.', seshTitle: 'Nesenās sesijas', seshNow: 'Klausās tagad' , hcHint: 'Sūtiet savus vērtējumus uz Hardcover kontu. Ielīmējiet API atslēgu no Hardcover konta iestatījumiem; atslēgas beidzas pēc gada.', hcKeyLbl: 'API atslēga', hcToggle: 'Automātiski sinhronizēt jaunos vērtējumus', hcPushAll: 'Sūtīt visus vērtējumus', hcPushDiff: 'Sūtīt tikai atšķirības', hcDone: 'Gatavs: %d sinhronizēti, %d bez atbilsmes', hcNoKey: 'Vispirms ielīmējiet savu Hardcover API atslēgu' , hcMissHead: 'Nav atrasts Hardcover:' , hcHint: 'Sūtiet savus vērtējumus uz Hardcover kontu. Ielīmējiet API atslēgu no Hardcover konta iestatījumiem.' , tabStyle: 'Stils', tabMain: 'Sākumlapa', tabBooks: 'Grāmatas', tabFeat: 'Funkcijas' , hcImport: 'Importēt no Hardcover', impMarkQ: 'Vai atzīmēt arī šeit %d atbilstošās grāmatas kā pabeigtas?', impMarkDone: '%d atzīmētas kā pabeigtas.' , impHint: 'Pārnesiet savus vērtējumus no Goodreads, StoryGraph vai Hardcover. Šeit izvēlieties eksportēto CSV; Hardcover importē tieši ar pogu savā kartītē. Katra atbilsme tiek parādīta apstiprināšanai, pirms kaut kas tiek saglabāts.' , impErrUnknown: 'Tas neizskatās pēc Goodreads, StoryGraph vai Hardcover eksporta: nav atrastas nosaukuma un vērtējuma kolonnas.' , fsCard: 'Sociālais', rnToggle: 'Rādīt manu vārdu pie vērtējumiem', rsToggle: 'Kopīgot manus vērtējumus ar citiem', socNames: 'Vārdi pie vērtējumiem', rtAnon: 'Anonīms' },
    et: { cmStopped: "peatatud", cmScanErrs: "{e} kontrollimata (Goodreadsi vead)", cmScanBusy: "Läbivaatus juba käib", cmGrErr: "Goodreads pole kättesaadav, proovin uuesti", cmNoMatchYet: "Goodreadsist vastet ei leitud", cmLooking: "Otsin Goodreadsist…", cmToolHint: "Mida arvavad sellest raamatust Goodreadsi lugejad. Küsimärk näitab sobitatud raamatut ja laseb seda muuta.", cmSearchPh: "Otsi Goodreadsist…", cmNoVotes: "hinnanguid veel pole", cmRefresh: "Värskenda", cmScanAll: "Vaata kõik uuesti üle, mitte ainult puuduvad", cmScanDone: "Valmis: {m} leitud · {x} ei leitud · {s} oli juba", cmStop: "Peata", cmScanLibs: "Läbivaadatavad kogud", cmScan: "Otsi hinnanguid", lsLongest: "Pikimad raamatud", lsSize: "Maht kettal", lsDuration: "Kogupikkus", lsGenresN: "Žanrid", lsSeriesN: "Sarjad", lsAuthorsN: "Autorid", lsBooks: "Raamatud", lsGlance: "Kogu ühe pilguga", cmOpen: "Ava Goodreadsis", cmDetect: "Otsi", cmAddr: "Abilise aadress", scmTitle: "Sarja leht", scmHint: "Kuidas näeb välja sarja pealkirja kohal genereeritud kaas, kui oma pole üles laaditud (ainult see brauser).", scmDeck: "Kihiline pakk", cmHint: "Mida arvavad raamatust Goodreadsi lugejad: keskmine hinne ja häälte arv, raamatu lehel tähtede all ning sortimise ja filtrina. Raamatulehed täituvad sirvides iseenesest.", cmGrOff: "Goodreads: seadistamata", cmSetup: "Abilist veel pole? Käivita see teema kõrval (compose read all) ja vajuta Otsi. Sobib ka iga muu viis tõmmise käivitamiseks: kleebi selle aadress üles.", cmGrOn: "Goodreads: ühendatud", cmGrDown: "Goodreads: seadistatud, aga ei vasta", cmGrNoNum: "Goodreads: vastab, aga see abs-tracti versioon ei anna hinnanguid (kasuta hinnangutega versiooni, vt README)", cmTitle: "Goodreadsi hinnangud", cmWrong: "Vale raamat?", cmFind: "Otsi kogukonna hinnangut…", cmNone: "Vastet pole", cmUse: "Kasuta seda", lfCommunity: "Goodreadsi hinnang", mfNatChange: "Muuda…", lsByTime: "kuulamisaja järgi", sbTitle: "Serveri edetabel", sbWeek: "Nädal", sbMonth: "Kuu", sbYear: "Aasta", sbAll: "Kogu aeg", sbBooks: "Raamatud", sbTopBooks: "Enim kuulatud", sbTotal: "Kokku", sbListeners: "Kuulajad", sbAvg: "Keskmine", wkTitle: "Kuulamisminutid, viimased 7 päeva", wkAvg: "Päeva keskmine", wkRow: "Päeva järjest", rpBadge: "Avatud probleemiteated", rpMenu: "Teata probleemist", rpTitle: "Teata probleemist", rpWhat: "Mis on valesti?", rpNote: "Veel midagi administraatorile? (valikuline)", rpSend: "Saada teade", rpSent: "Saadetud. Aitäh.", rpFail: "Saatmine ebaõnnestus", rpMissing: "Puuduv või poolik sisu", rpQuality: "Halb helikvaliteet", rpPlay: "Ei mängi", rpWrong: "Vale raamat, kaas või metaandmed", rpChapters: "Valed peatükid", rpOther: "Midagi muud", rfRate: "Hinda", rfSheet: "Hinda seda raamatut", rfOpen: "Ava raamatu leht", rfPickHint: "Vali hinnang", rfTitle: "Hinda lõpetatut", ysFinished: "Hiljuti lõpetatud", ysAlmost: "Peaaegu valmis", ysMarkDone: "Märgi lõpetatuks", ysEditDate: "Muuda lõpetamise kuupäeva", ysThisWeek: "Sel nädalal", ysBestDay: "Parim päev", ysTitle: "Sinu kuulamine", fsTitle: "Serveri edetabel", fsCard: "Serveri edetabel", fsToggle: "Jaga minu kuulamisstatistikat", fsJoin: "Lülita kohandamispaneelis sisse „Jaga minu kuulamisstatistikat”, et ilmuda serveri edetabelisse.", fsWaiting: "Jagamine on sees, sinu kokkuvõte liitub peagi serveri edetabeliga koos teistega.", fsHint: "Jaga oma kuulamise kokkuvõtet (summad ja minutid päevas) teistega selles serveris, see toidab serveri edetabelit. Vaikimisi sees. Välja lülitades kaod edetabelist ja selle summadest ning jagatu kustutatakse.", yirBtn: "Aasta kokkuvõte", yirTitle: "Sinu kuulamisaasta", yirYear: "Viimased 12 kuud", yirDays: "Kuulamisega päevi", yirBest: "Parim päev", yirMonths: "Kuude kaupa", yirNone: "Kuulamisandmeid veel pole.", ssscmDeck: "Kihiline pakk", scmFirst: "Esimese raamatu kaas", scmGrid: "Kaante ruudustik (vaikimisi)", scmOff: "Peidetud" , lsTitle: "Serveri statistika", lsAll: "Kõik kogud", lsPlayed: "Esitatud raamatud", lsBest: "Parima hinnanguga", lsGenres: "Populaarseimad žanrid", srvPick: "Mida salvestada:", srvGRest: "Funktsioonid ja käitumine", aeBtn: "Korrasta autorid", aeTitle: "Autorid ilma raamatuteta", aeNone: "Igal autoril siin on vähemalt üks raamat.", aeRemove: "Eemalda", aeDone: "Eemaldatud", aeWorking: "Eemaldan…", rpCard: "Teatatud probleemid", rpHint: "Probleemid, millest kasutajad raamatu lehelt teatasid. Lahendamine eemaldab kirje loendist.", rpEmpty: "Midagi pole teatatud.", rpResolve: "Lahenda", rpOpen: "Ava raamat", apCard: "Esitus", apToggle: "Esita sarja järgmine raamat automaatselt", apHint: "Kui raamat lõpeb, algab sama sarja järgmine automaatselt. Ainult raamatud, mis sul juba on ja mis pole lõpetatud.", apNext: "Sarja järgmine", fdToggle: "Lõpetatud raamatute tööriistad sinu statistikas", ysNoDate: "kuupäevata", hmShowing: "graafik näitab", ysStreak: "Jada", ysCurrent: "Praegune", ysLongest: "Pikim", ysDaysTotal: "Päevi kokku", ysPace: "Tempo", ysPerDay: "Kuulamispäeva kohta", ysWhen: "Millal sa kuulad", ysTopBooks: "Enim kuulatud raamatud", ysTopAuthors: "Enim kuulatud autorid", ysTopNarrators: "Enim kuulatud lugejad", rfToggle: "Jaotis „Hinda” avalehel", hoTitle: "Avalehe jaotiste järjekord", hoEmpty: "Ava avaleht üks kord ja tule siia tagasi.", hoReset: "Taasta vaikimisi järjekord", ctIcon: "Ikoon", ctColour: "Aktsent", ctNeedBook: "Audiobookshelf ei saa tühja kogumikku salvestada, vali vähemalt üks raamat. Hiljem saad vabalt lisada ja eemaldada.", colEditIcon: "Vaheta ikooni", ciTitle: "Kogumiku ikoon", ciReset: "Sobita nimega automaatselt", colEditDesc: "Muuda kirjeldust", colBookForms: ["raamat","raamatut"], colForms: ["Kogumik","Kogumikud"], ctTitle: "Loo kogumik", ctBlank: "Tühi kogumik", ctNew: "Uus kogumik", ctNewHint: "mallist", ctNamePh: "Kogumiku nimi", ctSearch: "Otsi raamatuid või autoreid…", ctCreate: "Loo", ctBack: "‹ Tagasi", ctAddAll: "Lisa kõik raamatud", abTitle: "Lisa raamatuid", abAdd: "Lisa", avRemove: "Eemalda foto", avSet: "Vali foto…", ugListened: "Kuulatud", ugPhoto: "Profiilifoto", ugNever: "Mitte kunagi", ugSeen: "Viimati aktiivne", ugEdit: "Muuda kasutajat", ugDelete: "Kustuta kasutaja", ugAdd: "Lisa foto", ugChange: "Vaheta fotot", ugSortName: "A–Ü", ugSortTime: "Enim kuulatud", ugSortSeen: "Hiljuti aktiivsed", bsCard: "Raamatu lingid", bsHint: "Nupud raamatu lehel pealkirja otsimiseks mujalt. Goodreads ja sinu keele suurim sait on ette sisse lülitatud.", bsFind: "Otsi saidilt", bsGlobal: "Kõikjal", bsLocal: "Sinu keelele", bsOther: "Teised riigid", pgCard: "Ümberkujundatud lehed", pgHint: "Iga valik asendab Audiobookshelfi lehe NanoHive'i versiooniga. Lülita välja, et originaal tagasi saada.", pgNarrators: "Lugejakaardid", pgCollections: "Kogumike lehed", pgUsers: "Kasutajakaardid (seaded)", pgStats: "Kuulamise edetabel", pgPhoto: "Profiilifoto ülaribal", pgCinematic: "Kinolik taust", pgTransitions: "Lehe üleminekud", pSearch: "Otsi seadeid…", pSearchNone: "Vasteid pole.", erTypeface: "Kirjatüüp", erPage: "Lehe teema", erText: "Teksti värv", erBg: "Taust", erDefault: "Vaikimisi (ABS)", erAuto: "Automaatne", nrForms: ["Lugeja","Lugejad"], nrBooksForms: ["raamat","raamatut"], nrSearch: "Filtreeri lugejaid…", nrSortName: "Nimi", nrSortBooks: "Enim raamatuid", auForms: ["Autor","Autorid"], auSearch: "Filtreeri autoreid…" , lfByAuthor: "Autor › sarja järjekord", lfBySeries: "Sari › järjekord" , lfSecFilters: "Rohkem filtreid", lfSecSort: "Mitmetasandiline sortimine", lfAuthor: "Autor", lfSeries: "Sari", lfTitle: "Pealkiri", lfYear: "Aasta", lfAdded: "Lisatud", lfDuration: "Kestus", lfNarrator: "Lugeja", lfGenre: "Žanr", lfLanguage: "Keel", lfProgress: "Edenemine", lfPgFinished: "Lõpetatud", lfPgInProgress: "Pooleli", lfPgNot: "Alustamata" , logoUpload: 'Laadi seadmest üles…', logoUploaded: 'Üles laaditud ✓', logoBadType: 'Toetamata pilditüüp', logoTooBig: 'Pilt on liiga suur (maks. 4 MB)', logoOr: 'või', bookPage: 'Raamatu leht', ratingsToggle: 'Kogukonna hinnangud (tärnid ja arvustused)', ratingsHint: 'Tärnihinnangud lühikeste arvustustega igal raamatu lehel, jagatud kõigi selle serveri kasutajatega.', searchCard: 'Otsing', gsToggle: 'Otsi kõigist kogudest korraga', gsHint: 'Üks ühendatud tulemuste loend kõigist ligipääsetavatest kogudest, iga vaste juures kogu märgis. Lülita välja, et taastada menüü kogude kaupa.', gsNoResults: 'Tulemusi pole', gsSearching: 'Otsin…', gsBooks: 'Raamatud', gsSeries: 'Sarjad', gsAuthors: 'Autorid', spTitle: 'Avaleht', spHint: 'Ava rakendus alati valitud kogu vaates (ainult see brauser). Otselinke ei suunata kunagi ümber.', spDefault: 'Rakenduse vaikimisi', spViewHome: 'Avaleht', spViewLibrary: 'Kogu', spViewSeries: 'Sarjad', spViewCollections: 'Kollektsioonid', spViewAuthors: 'Autorid', scSet: 'Määra sarja kaas', scRemove: 'Eemalda sarja kaas', scEditDesc: 'Muuda sarja kirjeldust', cardStars: 'Tärnid raamatukaantel', hoverPolish: 'Kaartide lihv kursoriga (pehmem tumendus, aktsentnupp, tärnid kursoriga)', sectUser: 'Sinu seaded', sectAdmin: 'Haldus', lockTitle: 'Funktsioonilülitid', lockHint: 'Lülitab funktsiooni sunniviisiliselt välja kõigil selle serveri kasutajatel. Võidab isiklikud seaded; teised näevad pärast järgmist laadimist.', lockRatings: 'Lülita hinnangud kõikjal välja', lockCardRatings: 'Lülita tärnid kaantel välja', lockGlobalSearch: 'Lülita üldotsing välja', lockHoverPolish: 'Lülita kaartide lihv välja', lockHeroCarousel: 'Lülita avalehe karussell välja', lockedNote: 'administraatori keelatud', lfSort: 'Hinnang', lfRated: 'Hinnatud', lfUnrated: 'Hindamata', lfClear: 'Tühjenda', lfNone: 'Ükski raamat ei sobi', lfSection: 'Hinnang', lfWho: 'Kelle hinnangud', lfEveryone: 'Kõik', sdSave: 'Salvesta', sdClear: 'Taasta raamatu kirjeldus', sdMore: 'Loe edasi', sdLess: 'Näita vähem', shSeries: 'Sari', shBy: '', shMore: ' ja teised', shBookForms: ['raamat', 'raamatut'], shAvg: 'raamatute keskmine', shRated: 'hinnatud', impLabel: 'Impordi hinnangud', impHint: 'Too oma hinnangud StoryGraphist või Goodreadsist. Ekspordi seal raamatud CSV-na ja vali fail siin. Iga vaste näidatakse kinnitamiseks ja midagi ei salvestata enne kinnitust.', impBtn: 'Vali CSV-fail…', impReading: 'Loen faili…', impMatching: 'Võrdlen sinu kogudega…', impErrUnknown: 'See ei näe välja nagu StoryGraphi või Goodreadsi eksport. See peab olema nende CSV veeruga "Star Rating" või "My Rating".', impErrEmpty: 'Failis pole ridu.', impErrNoRated: 'Failis pole hinnatud raamatuid.', impErrRead: 'Faili ei õnnestunud lugeda.', impErrLoad: 'Kogusid ei õnnestunud laadida. Proovi hetke pärast uuesti.', impErrNoLibs: 'Ükski kogu ei osale hinnangutes, seega pole kuhugi importida.', impTitle: 'Impordi hinnangud', impFrom: '%s eksport: %d raamatut, neist %d hinnatud', impLibs: 'Otsin: %s', impMatched: 'Vastavused', impCheck: 'Tasub üle vaadata', impCheckHint: 'Lähedal, aga pole kindel. Kontrolli vastet või vali teine.', impAlready: 'Sinu poolt juba hinnatud', impAlreadyHint: 'Jäävad puutumata, kui sa ei vali ülekirjutamist.', impNone: 'Pole sinu kogus', impNoneHint: 'Miski selles serveris ei vasta neile.', impAll: 'Kõik', impSkip: 'Jäta see vahele', impDup: 'duplikaat', impByTitle: 'pealkiri', impYours: 'sinu: %s', impReviews: 'Impordi ka kirjutatud arvustused (%d)', impOver: 'Kirjuta mu olemasolevad hinnangud üle', impGo: 'Impordi %d hinnangut', impCancel: 'Tühista', impCloseBtn: 'Sulge', impWorking: 'Salvestan… %d / %d', impDone: '%d hinnangut imporditud.', impFail: '%d ei õnnestunud salvestada.', rdTitle: 'Kes seda raamatut loeb', rdFin: 'Lõpetatud', socTitle: 'Sotsiaalsed funktsioonid', socHint: 'Mida see server kasutajate vahel jagab. Kuulamisaeg toidab edetabelit; ülejäänud kaks paljastavad, mida keegi loeb, seega on need väljas, kuni sa need sisse lülitad. Kehtib kõigile korraga.', socTime: 'Kuulamisaeg (serveri edetabel)', socContent: 'Populaarseimad pealkirjad jagatud statistikas', socWho: 'Kes loeb (raamatute lehed)', socUsers: 'Kasutajate kaupa', socUsersHint: 'Kui lülitad kellegi välja, ei jagata midagi tema andmetest, olenemata tema valikust; tagasi saad lülitada ainult sina. "Loobus ise" tähendab, et inimene ütles ise ei.', socOpted: 'loobus ise', srvTitle: 'Serveri vaikeseaded', srvHint: 'Salvesta oma praegused seaded vaikimisi kõigile selle serveri kasutajatele. Igaüks saab siiski enda omi kohandada. Ühenda köide /data/nh külge teema konteineris, et need uuendustes säiliksid.', srvSave: 'Salvesta serveri vaikeseadeteks', srvClear: 'Tühjenda serveri vaikeseaded', srvSaved: 'Salvestatud', srvCleared: 'Tühjendatud', srvErr: 'Ebaõnnestus: vajalik administraatori sisselogimine', crMode: 'Jätka lugemist riiul', crCombine: 'Ühenda karusselliga', crSeparate: 'Hoia eraldi riiulina', crHide: 'Peidetud', fsHint: 'Jaga oma kuulamist teistega selles serveris: ajasummad ja minutid päevade kaupa edetabeli jaoks, ja kui administraator on need sisse lülitanud, ka sinu populaarseimad pealkirjad ning millised raamatud sul pooleli või loetud on. Vaikimisi sees. Välja lülitades kustutatakse kõik jagatu.' , amfHide: 'Peida Märgi lõpetatuks' , amfConfirm: 'Kas märkida see raamat lõpetatuks?' , fsHint: 'Jaga oma kuulamise kokkuvõtet (summad ja minutid päevas) teistega selles serveris, see toidab serveri edetabelit. Vaikimisi sees. Välja lülitades kaod edetabelist ja selle summadest ning jagatu kustutatakse.' , srToggle: 'Jaga, milliseid raamatuid loed', srHint: 'Lisab teiste nähtavale, milliseid raamatuid parasjagu loed, millised on loetud ja mida kõige rohkem kuulad. Vaikimisi väljas; sisse peavad olema lülitatud ka administraatori serverilülitid.' , fsHint: 'Jaga oma kuulamisaega (summad ja minutid päevade kaupa) teistega selles serveris; see toidab edetabelit. Vaikimisi sees. Välja lülitades ei näe teised kasutajad sind enam; serveri administraatorid näevad tegevust endiselt, märgitud loobunuks.', srHint: 'Laseb teistel näha ka seda, milliseid raamatuid loed, millised on loetud ja mida enim kuulad. Midagi ei kuvata enne, kui administraator lülitab sisse vastavad serverilülitid.', seshTitle: 'Hiljutised seansid', seshNow: 'Kuulavad praegu' , hcHint: 'Saada oma hinnangud Hardcoveri kontole. Kleebi API võti Hardcoveri konto seadetest; võtmed aeguvad aasta pärast.', hcKeyLbl: 'API võti', hcToggle: 'Sünkrooni uued hinnangud automaatselt', hcPushAll: 'Saada kõik hinnangud', hcPushDiff: 'Saada ainult erinevused', hcDone: 'Valmis: %d sünkroonitud, %d vasteta', hcNoKey: 'Kleebi kõigepealt oma Hardcoveri API võti' , hcMissHead: 'Hardcoverist ei leitud:' , hcHint: 'Saada oma hinnangud Hardcoveri kontole. Kleebi API võti Hardcoveri konto seadetest.' , tabStyle: 'Stiil', tabMain: 'Avaleht', tabBooks: 'Raamatud', tabFeat: 'Funktsioonid' , hcImport: 'Impordi Hardcoverist', impMarkQ: 'Kas märkida ka siin %d sobitatud raamatut loetuks?', impMarkDone: '%d märgitud loetuks.' , impHint: 'Too oma hinnangud Goodreadsist, StoryGraphist või Hardcoverist. Vali siin eksporditud CSV; Hardcover impordib otse oma kaardi nupuga. Iga vaste näidatakse kinnitamiseks enne salvestamist.' , impErrUnknown: 'See ei näi olevat Goodreadsi, StoryGraphi ega Hardcoveri eksport: pealkirja- ja hinnanguveerge ei leitud.' , fsCard: 'Sotsiaalne', rnToggle: 'Näita mu nime hinnangute juures', rsToggle: 'Jaga mu hinnanguid teistega', socNames: 'Nimed hinnangute juures', rtAnon: 'Anonüümne' },
    el: { cmStopped: "διακόπηκε", cmScanErrs: "{e} δεν ελέγχθηκαν (σφάλματα Goodreads)", cmScanBusy: "Μια σάρωση εκτελείται ήδη", cmGrErr: "Το Goodreads δεν απαντά, θα ξαναδοκιμάσω", cmNoMatchYet: "Καμία αντιστοιχία στο Goodreads", cmLooking: "Αναζήτηση στο Goodreads…", cmToolHint: "Τι πιστεύουν οι αναγνώστες του Goodreads για αυτό το βιβλίο. Το ? δείχνει το αντιστοιχισμένο βιβλίο και επιτρέπει την αλλαγή του.", cmSearchPh: "Αναζήτηση στο Goodreads…", cmNoVotes: "χωρίς βαθμολογίες ακόμη", cmRefresh: "Ανανέωση", cmScanAll: "Επανέλεγχος όλων, όχι μόνο όσων λείπουν", cmScanDone: "Ολοκληρώθηκε: {m} βρέθηκαν · {x} δεν βρέθηκαν · {s} είχαν ήδη", cmStop: "Διακοπή", cmScanLibs: "Βιβλιοθήκες προς σάρωση", cmScan: "Αναζήτηση βαθμολογιών", lsLongest: "Τα μεγαλύτερα βιβλία", lsSize: "Μέγεθος στον δίσκο", lsDuration: "Συνολική διάρκεια", lsGenresN: "Είδη", lsSeriesN: "Σειρές", lsAuthorsN: "Συγγραφείς", lsBooks: "Βιβλία", lsGlance: "Η βιβλιοθήκη με μια ματιά", cmOpen: "Άνοιγμα στο Goodreads", cmDetect: "Εύρεση", cmAddr: "Διεύθυνση βοηθού", scmTitle: "Σελίδα σειράς", scmHint: "Πώς φαίνεται το παραγόμενο εξώφυλλο πάνω από τον τίτλο μιας σειράς όταν δεν έχει ανέβει δικό σου (μόνο αυτό το πρόγραμμα περιήγησης).", scmDeck: "Στρωματοποιημένη τράπουλα", cmHint: "Τι πιστεύουν οι αναγνώστες του Goodreads για ένα βιβλίο: μέση βαθμολογία και αριθμός ψήφων, κάτω από τα αστέρια στη σελίδα του βιβλίου και ως ταξινόμηση και φίλτρο. Οι σελίδες βιβλίων συμπληρώνονται μόνες τους κατά την περιήγηση.", cmGrOff: "Goodreads: δεν έχει ρυθμιστεί", cmSetup: "Δεν έχετε βοηθό ακόμη; Τρέξτε αυτό δίπλα στο θέμα (γραμμές compose παρακάτω) και πατήστε Εύρεση. Κάθε άλλος τρόπος εκτέλεσης της εικόνας είναι επίσης εντάξει: επικολλήστε τη διεύθυνσή του παραπάνω.", cmGrOn: "Goodreads: συνδεδεμένο", cmGrDown: "Goodreads: ρυθμισμένο αλλά δεν απαντά", cmGrNoNum: "Goodreads: απαντά, αλλά αυτή η έκδοση abs-tract δεν δίνει βαθμολογίες (χρησιμοποιήστε την έκδοση ratings, βλ. README)", cmTitle: "Βαθμολογίες Goodreads", cmWrong: "Λάθος βιβλίο;", cmFind: "Εύρεση βαθμολογίας κοινότητας…", cmNone: "Καμία αντιστοιχία", cmUse: "Χρήση αυτού", lfCommunity: "Βαθμολογία Goodreads", mfNatChange: "Αλλαγή…", lsByTime: "κατά χρόνο ακρόασης", sbTitle: "Κατάταξη διακομιστή", sbWeek: "Εβδομάδα", sbMonth: "Μήνας", sbYear: "Έτος", sbAll: "Συνολικά", sbBooks: "Βιβλία", sbTopBooks: "Πιο ακουσμένα", sbTotal: "Σύνολο", sbListeners: "Ακροατές", sbAvg: "Μέσος όρος", wkTitle: "Λεπτά ακρόασης, τελευταίες 7 ημέρες", wkAvg: "Ημερήσιος μέσος", wkRow: "Συνεχόμενες ημέρες", rpBadge: "Ανοιχτές αναφορές προβλημάτων", rpMenu: "Αναφορά προβλήματος", rpTitle: "Αναφορά προβλήματος", rpWhat: "Τι πάει στραβά;", rpNote: "Κάτι άλλο για τον διαχειριστή; (προαιρετικό)", rpSend: "Αποστολή αναφοράς", rpSent: "Στάλθηκε. Ευχαριστούμε.", rpFail: "Αποτυχία αποστολής", rpMissing: "Λείπει ή είναι ελλιπές περιεχόμενο", rpQuality: "Κακή ποιότητα ήχου", rpPlay: "Δεν αναπαράγεται", rpWrong: "Λάθος βιβλίο, εξώφυλλο ή μεταδεδομένα", rpChapters: "Λάθος κεφάλαια", rpOther: "Κάτι άλλο", rfRate: "Βαθμολόγησε", rfSheet: "Βαθμολόγησε αυτό το βιβλίο", rfOpen: "Άνοιγμα σελίδας βιβλίου", rfPickHint: "Διάλεξε βαθμολογία", rfTitle: "Βαθμολόγησε όσα τελείωσες", ysFinished: "Πρόσφατα ολοκληρωμένα", ysAlmost: "Σχεδόν έτοιμα", ysMarkDone: "Σήμανση ως ολοκληρωμένο", ysEditDate: "Αλλαγή ημερομηνίας ολοκλήρωσης", ysThisWeek: "Αυτή την εβδομάδα", ysBestDay: "Καλύτερη μέρα", ysTitle: "Η ακρόασή σου", fsTitle: "Κατάταξη διακομιστή", fsCard: "Κατάταξη διακομιστή", fsToggle: "Κοινοποίηση των στατιστικών ακρόασής μου", fsJoin: "Ενεργοποίησε την «Κοινοποίηση των στατιστικών ακρόασής μου» στον πίνακα εξατομίκευσης για να εμφανιστείς στην κατάταξη του διακομιστή.", fsWaiting: "Η κοινοποίηση είναι ενεργή, η σύνοψή σου θα μπει σύντομα στην κατάταξη του διακομιστή μαζί με όλους τους άλλους.", fsHint: "Κοινοποίησε μια σύνοψη της ακρόασής σου (σύνολα και λεπτά ανά ημέρα) στους υπόλοιπους του διακομιστή, τροφοδοτεί την κατάταξη. Ενεργό από προεπιλογή. Αν το απενεργοποιήσεις, βγαίνεις από την κατάταξη και τα σύνολά της, και ό,τι κοινοποιήθηκε διαγράφεται.", yirBtn: "Ανασκόπηση έτους", yirTitle: "Η χρονιά ακρόασής σου", yirYear: "Τελευταίοι 12 μήνες", yirDays: "Ημέρες με ακρόαση", yirBest: "Καλύτερη μέρα", yirMonths: "Ανά μήνα", yirNone: "Δεν υπάρχουν ακόμη δεδομένα ακρόασης.", ssscmDeck: "Στρωματοποιημένη τράπουλα", scmFirst: "Εξώφυλλο πρώτου βιβλίου", scmGrid: "Πλέγμα εξωφύλλων (προεπιλογή)", scmOff: "Κρυφό" , lsTitle: "Στατιστικά διακομιστή", lsAll: "Όλες οι βιβλιοθήκες", lsPlayed: "Βιβλία που παίχτηκαν", lsBest: "Με την καλύτερη βαθμολογία", lsGenres: "Κορυφαία είδη", srvPick: "Τι να αποθηκευτεί:", srvGRest: "Λειτουργίες και συμπεριφορά", aeBtn: "Τακτοποίηση συγγραφέων", aeTitle: "Συγγραφείς χωρίς βιβλία", aeNone: "Κάθε συγγραφέας εδώ έχει τουλάχιστον ένα βιβλίο.", aeRemove: "Αφαίρεση", aeDone: "Αφαιρέθηκαν", aeWorking: "Αφαίρεση…", rpCard: "Αναφερμένα προβλήματα", rpHint: "Προβλήματα που ανέφεραν χρήστες από σελίδα βιβλίου. Η επίλυση το αφαιρεί από τη λίστα.", rpEmpty: "Καμία αναφορά.", rpResolve: "Επίλυση", rpOpen: "Άνοιγμα βιβλίου", apCard: "Αναπαραγωγή", apToggle: "Αυτόματη αναπαραγωγή του επόμενου βιβλίου σειράς", apHint: "Όταν τελειώνει ένα βιβλίο, ξεκινά αυτόματα το επόμενο της ίδιας σειράς. Μόνο βιβλία που έχεις ήδη και δεν έχεις τελειώσει.", apNext: "Επόμενο της σειράς", fdToggle: "Εργαλεία ολοκληρωμένων βιβλίων στα στατιστικά", ysNoDate: "χωρίς ημερομηνία", hmShowing: "το γράφημα δείχνει", ysStreak: "Σερί", ysCurrent: "Τρέχον", ysLongest: "Μεγαλύτερο", ysDaysTotal: "Σύνολο ημερών", ysPace: "Ρυθμός", ysPerDay: "Ανά ημέρα ακρόασης", ysWhen: "Πότε ακούς", ysTopBooks: "Πιο ακουσμένα βιβλία", ysTopAuthors: "Πιο ακουσμένοι συγγραφείς", ysTopNarrators: "Πιο ακουσμένοι αφηγητές", rfToggle: "Ενότητα «Βαθμολόγησε» στην αρχική", hoTitle: "Σειρά ενοτήτων αρχικής", hoEmpty: "Άνοιξε μία φορά την αρχική και γύρνα εδώ.", hoReset: "Επαναφορά προεπιλεγμένης σειράς", ctIcon: "Εικονίδιο", ctColour: "Απόχρωση", ctNeedBook: "Το Audiobookshelf δεν μπορεί να αποθηκεύσει κενή συλλογή, διάλεξε τουλάχιστον ένα βιβλίο. Αργότερα προσθέτεις και αφαιρείς ελεύθερα.", colEditIcon: "Αλλαγή εικονιδίου", ciTitle: "Εικονίδιο συλλογής", ciReset: "Αυτόματο ταίριασμα με το όνομα", colEditDesc: "Επεξεργασία περιγραφής", colBookForms: ["βιβλίο","βιβλία"], colForms: ["Συλλογή","Συλλογές"], ctTitle: "Δημιουργία συλλογής", ctBlank: "Κενή συλλογή", ctNew: "Νέα συλλογή", ctNewHint: "από πρότυπο", ctNamePh: "Όνομα συλλογής", ctSearch: "Αναζήτηση βιβλίων ή συγγραφέων…", ctCreate: "Δημιουργία", ctBack: "‹ Πίσω", ctAddAll: "Προσθήκη όλων των βιβλίων", abTitle: "Προσθήκη βιβλίων", abAdd: "Προσθήκη", avRemove: "Αφαίρεση φωτογραφίας", avSet: "Επιλογή φωτογραφίας…", ugListened: "Ακούστηκε", ugPhoto: "Φωτογραφία προφίλ", ugNever: "Ποτέ", ugSeen: "Τελευταία δραστηριότητα", ugEdit: "Επεξεργασία χρήστη", ugDelete: "Διαγραφή χρήστη", ugAdd: "Προσθήκη φωτογραφίας", ugChange: "Αλλαγή φωτογραφίας", ugSortName: "Α–Ω", ugSortTime: "Πιο ακουσμένα", ugSortSeen: "Πρόσφατα ενεργοί", bsCard: "Σύνδεσμοι βιβλίου", bsHint: "Κουμπιά στη σελίδα του βιβλίου για αναζήτηση του τίτλου αλλού. Το Goodreads και ο μεγαλύτερος ιστότοπος της γλώσσας σου είναι ενεργά από πριν.", bsFind: "Αναζήτηση στο", bsGlobal: "Παντού", bsLocal: "Για τη γλώσσα σου", bsOther: "Άλλες χώρες", pgCard: "Επανασχεδιασμένες σελίδες", pgHint: "Καθεμία αντικαθιστά μια σελίδα του Audiobookshelf με την έκδοση NanoHive. Απενεργοποίησέ τη για να επανέλθει η αρχική.", pgNarrators: "Κάρτες αφηγητών", pgCollections: "Σελίδες συλλογών", pgUsers: "Κάρτες χρηστών (ρυθμίσεις)", pgStats: "Κατάταξη ακρόασης", pgPhoto: "Φωτογραφία προφίλ στην πάνω μπάρα", pgCinematic: "Κινηματογραφικό φόντο", pgTransitions: "Μεταβάσεις σελίδων", pSearch: "Αναζήτηση ρυθμίσεων…", pSearchNone: "Καμία αντιστοιχία.", erTypeface: "Γραμματοσειρά", erPage: "Θέμα σελίδας", erText: "Χρώμα κειμένου", erBg: "Φόντο", erDefault: "Προεπιλογή (ABS)", erAuto: "Αυτόματο", nrForms: ["Αφηγητής","Αφηγητές"], nrBooksForms: ["βιβλίο","βιβλία"], nrSearch: "Φιλτράρισμα αφηγητών…", nrSortName: "Όνομα", nrSortBooks: "Περισσότερα βιβλία", auForms: ["Συγγραφέας","Συγγραφείς"], auSearch: "Φιλτράρισμα συγγραφέων…" , lfByAuthor: "Συγγραφέας › αρίθμηση σειράς", lfBySeries: "Σειρά › αρίθμηση" , lfSecFilters: "Περισσότερα φίλτρα", lfSecSort: "Πολλαπλή ταξινόμηση", lfAuthor: "Συγγραφέας", lfSeries: "Σειρά", lfTitle: "Τίτλος", lfYear: "Έτος", lfAdded: "Προστέθηκε", lfDuration: "Διάρκεια", lfNarrator: "Αφηγητής", lfGenre: "Είδος", lfLanguage: "Γλώσσα", lfProgress: "Πρόοδος", lfPgFinished: "Ολοκληρωμένα", lfPgInProgress: "Σε εξέλιξη", lfPgNot: "Μη αρχισμένα" , logoUpload: 'Μεταφόρτωση από τη συσκευή…', logoUploaded: 'Μεταφορτώθηκε ✓', logoBadType: 'Μη υποστηριζόμενος τύπος εικόνας', logoTooBig: 'Η εικόνα είναι πολύ μεγάλη (έως 4 MB)', logoOr: 'ή', bookPage: 'Σελίδα βιβλίου', ratingsToggle: 'Βαθμολογίες κοινότητας (αστέρια και κριτικές)', ratingsHint: 'Βαθμολογίες με αστέρια και σύντομες κριτικές σε κάθε σελίδα βιβλίου, κοινές για όλους τους χρήστες αυτού του διακομιστή.', searchCard: 'Αναζήτηση', gsToggle: 'Αναζήτηση σε όλες τις βιβλιοθήκες ταυτόχρονα', gsHint: 'Μία ενιαία λίστα αποτελεσμάτων από όλες τις προσβάσιμες βιβλιοθήκες, με σήμα βιβλιοθήκης σε κάθε αποτέλεσμα. Απενεργοποιήστε για να επανέλθει το μενού ανά βιβλιοθήκη.', gsNoResults: 'Κανένα αποτέλεσμα', gsSearching: 'Αναζήτηση…', gsBooks: 'Βιβλία', gsSeries: 'Σειρές', gsAuthors: 'Συγγραφείς', spTitle: 'Αρχική σελίδα', spHint: 'Άνοιγμα της εφαρμογής πάντα σε επιλεγμένη προβολή βιβλιοθήκης (μόνο αυτό το πρόγραμμα περιήγησης). Οι άμεσοι σύνδεσμοι δεν ανακατευθύνονται ποτέ.', spDefault: 'Προεπιλογή εφαρμογής', spViewHome: 'Αρχική', spViewLibrary: 'Βιβλιοθήκη', spViewSeries: 'Σειρές', spViewCollections: 'Συλλογές', spViewAuthors: 'Συγγραφείς', scSet: 'Ορισμός εξωφύλλου σειράς', scRemove: 'Αφαίρεση εξωφύλλου σειράς', scEditDesc: 'Επεξεργασία περιγραφής σειράς', cardStars: 'Αστέρια στα εξώφυλλα', hoverPolish: 'Φινίρισμα καρτών στο πέρασμα (απαλότερη σκίαση, τονισμένο κουμπί, αστέρια στο πέρασμα)', sectUser: 'Οι ρυθμίσεις σας', sectAdmin: 'Διαχείριση', lockTitle: 'Διακόπτες λειτουργιών', lockHint: 'Απενεργοποιεί υποχρεωτικά μια λειτουργία για όλους τους χρήστες αυτού του διακομιστή. Υπερισχύει των προσωπικών ρυθμίσεων· οι άλλοι το βλέπουν μετά την επόμενη φόρτωση.', lockRatings: 'Απενεργοποίηση βαθμολογιών παντού', lockCardRatings: 'Απενεργοποίηση αστεριών στα εξώφυλλα', lockGlobalSearch: 'Απενεργοποίηση καθολικής αναζήτησης', lockHoverPolish: 'Απενεργοποίηση φινιρίσματος καρτών', lockHeroCarousel: 'Απενεργοποίηση καρουζέλ αρχικής', lockedNote: 'απενεργοποιήθηκε από τον διαχειριστή', lfSort: 'Βαθμολογία', lfRated: 'Βαθμολογημένα', lfUnrated: 'Αβαθμολόγητα', lfClear: 'Καθαρισμός', lfNone: 'Κανένα βιβλίο δεν ταιριάζει', lfSection: 'Βαθμολογία', lfWho: 'Ποιανού βαθμολογίες', lfEveryone: 'Όλοι', sdSave: 'Αποθήκευση', sdClear: 'Επαναφορά περιγραφής βιβλίου', sdMore: 'Διαβάστε περισσότερα', sdLess: 'Λιγότερα', shSeries: 'Σειρά', shBy: 'από ', shMore: ' και άλλοι', shBookForms: ['βιβλίο', 'βιβλία'], shAvg: 'μέσος όρος βιβλίων', shRated: 'βαθμολογημένα', impLabel: 'Εισαγωγή βαθμολογιών', impHint: 'Φέρτε τις βαθμολογίες σας από το StoryGraph ή το Goodreads. Εξαγάγετε εκεί τα βιβλία σας ως CSV και επιλέξτε το αρχείο εδώ. Κάθε αντιστοιχία εμφανίζεται για επιβεβαίωση και τίποτα δεν αποθηκεύεται μέχρι να επιβεβαιώσετε.', impBtn: 'Επιλογή αρχείου CSV…', impReading: 'Ανάγνωση αρχείου…', impMatching: 'Αντιστοίχιση με τις βιβλιοθήκες σας…', impErrUnknown: 'Δεν μοιάζει με εξαγωγή StoryGraph ή Goodreads. Πρέπει να είναι το CSV τους, με στήλη "Star Rating" ή "My Rating".', impErrEmpty: 'Το αρχείο δεν έχει γραμμές.', impErrNoRated: 'Δεν υπάρχουν βαθμολογημένα βιβλία στο αρχείο.', impErrRead: 'Το αρχείο δεν ήταν δυνατό να διαβαστεί.', impErrLoad: 'Οι βιβλιοθήκες δεν φορτώθηκαν. Δοκιμάστε ξανά σε λίγο.', impErrNoLibs: 'Καμία βιβλιοθήκη δεν συμμετέχει στις βαθμολογίες, οπότε δεν υπάρχει πού να γίνει εισαγωγή.', impTitle: 'Εισαγωγή βαθμολογιών', impFrom: 'Εξαγωγή %s: %d βιβλία, εκ των οποίων %d βαθμολογημένα', impLibs: 'Αναζήτηση σε: %s', impMatched: 'Αντιστοιχισμένα', impCheck: 'Αξίζει έλεγχο', impCheckHint: 'Κοντά αλλά όχι σίγουρο. Ελέγξτε την αντιστοιχία ή επιλέξτε άλλη.', impAlready: 'Ήδη βαθμολογημένα από εσάς', impAlreadyHint: 'Μένουν ανέγγιχτα, εκτός αν ζητήσετε αντικατάσταση.', impNone: 'Δεν είναι στη βιβλιοθήκη σας', impNoneHint: 'Τίποτα σε αυτόν τον διακομιστή δεν ταιριάζει με αυτά.', impAll: 'Όλα', impSkip: 'Παράλειψη αυτού', impDup: 'διπλότυπο', impByTitle: 'τίτλος', impYours: 'δικό σας: %s', impReviews: 'Εισαγωγή και γραπτών κριτικών (%d)', impOver: 'Αντικατάσταση βαθμολογιών που ήδη έχω', impGo: 'Εισαγωγή %d βαθμολογιών', impCancel: 'Ακύρωση', impCloseBtn: 'Κλείσιμο', impWorking: 'Αποθήκευση… %d από %d', impDone: 'Εισήχθησαν %d βαθμολογίες.', impFail: '%d δεν αποθηκεύτηκαν.', rdTitle: 'Ποιος διαβάζει αυτό το βιβλίο', rdFin: 'Ολοκληρώθηκε', socTitle: 'Κοινωνικές λειτουργίες', socHint: 'Τι μοιράζεται αυτός ο διακομιστής μεταξύ χρηστών. Ο χρόνος ακρόασης τροφοδοτεί την κατάταξη· οι άλλες δύο αποκαλύπτουν τι διαβάζει ο καθένας, οπότε μένουν κλειστές μέχρι να τις ανοίξετε. Ισχύει για όλους ταυτόχρονα.', socTime: 'Χρόνος ακρόασης (κατάταξη διακομιστή)', socContent: 'Κορυφαίοι τίτλοι στα κοινά στατιστικά', socWho: 'Ποιος διαβάζει (σελίδες βιβλίων)', socUsers: 'Ανά χρήστη', socUsersHint: 'Αν απενεργοποιήσετε κάποιον, τίποτα δικό του δεν μοιράζεται ό,τι κι αν επέλεξε ο ίδιος· μόνο εσείς μπορείτε να το επαναφέρετε. "Αποχώρησε μόνος του" σημαίνει ότι το άτομο είπε όχι μόνο του.', socOpted: 'αποχώρησε μόνος του', srvTitle: 'Προεπιλογές διακομιστή', srvHint: 'Αποθηκεύστε τις τρέχουσες ρυθμίσεις σας ως προεπιλογές για όλους τους χρήστες αυτού του διακομιστή. Ο καθένας μπορεί να προσαρμόσει από πάνω. Προσαρτήστε έναν τόμο στο /data/nh στο container του θέματος για να διατηρούνται στις ενημερώσεις.', srvSave: 'Αποθήκευση ως προεπιλογές διακομιστή', srvClear: 'Καθαρισμός προεπιλογών διακομιστή', srvSaved: 'Αποθηκεύτηκε', srvCleared: 'Καθαρίστηκε', srvErr: 'Απέτυχε: απαιτείται σύνδεση διαχειριστή', crMode: 'Ράφι Συνέχισε την ανάγνωση', crCombine: 'Συγχώνευση στο καρουζέλ', crSeparate: 'Διατήρηση ως ξεχωριστό ράφι', crHide: 'Κρυφό', fsHint: 'Μοιραστείτε την ακρόασή σας με τους άλλους σε αυτόν τον διακομιστή: σύνολα χρόνου και λεπτά ανά ημέρα για την κατάταξη, και, αν ο διαχειριστής τα έχει ενεργοποιήσει, τους κορυφαίους τίτλους σας και ποια βιβλία διαβάζετε ή ολοκληρώσατε. Ενεργό από προεπιλογή. Με την απενεργοποίηση διαγράφεται ό,τι μοιραστήκατε.' , amfHide: 'Απόκρυψη Σήμανση ως ολοκληρωμένου' , amfConfirm: 'Να σημανθεί αυτό το βιβλίο ως ολοκληρωμένο;' , fsHint: 'Κοινοποίησε μια σύνοψη της ακρόασής σου (σύνολα και λεπτά ανά ημέρα) στους υπόλοιπους του διακομιστή, τροφοδοτεί την κατάταξη. Ενεργό από προεπιλογή. Αν το απενεργοποιήσεις, βγαίνεις από την κατάταξη και τα σύνολά της, και ό,τι κοινοποιήθηκε διαγράφεται.' , srToggle: 'Κοινοποίηση των βιβλίων που διαβάζετε', srHint: 'Προσθέτει σε όσα βλέπουν οι άλλοι ποια βιβλία διαβάζετε τώρα, ποια ολοκληρώσατε και ποια ακούτε περισσότερο. Απενεργοποιημένο από προεπιλογή· πρέπει επίσης να είναι ενεργοί οι διακόπτες διακομιστή του διαχειριστή.' , fsHint: 'Μοιραστείτε τον χρόνο ακρόασής σας (σύνολα και λεπτά ανά ημέρα) με τους άλλους σε αυτόν τον διακομιστή· τροφοδοτεί την κατάταξη. Ενεργό από προεπιλογή. Αν το απενεργοποιήσετε, οι άλλοι χρήστες δεν σας βλέπουν πια· οι διαχειριστές εξακολουθούν να βλέπουν τη δραστηριότητα, με σήμανση αποχώρησης.', srHint: 'Επιτρέπει επίσης στους άλλους να βλέπουν ποια βιβλία διαβάζετε, ποια ολοκληρώσατε και ποια ακούτε περισσότερο. Τίποτα δεν εμφανίζεται μέχρι ο διαχειριστής να ενεργοποιήσει τους αντίστοιχους διακόπτες διακομιστή.', seshTitle: 'Πρόσφατες συνεδρίες', seshNow: 'Ακούν τώρα' , hcHint: 'Στείλτε τις βαθμολογίες σας στον λογαριασμό Hardcover. Επικολλήστε το κλειδί API από τις ρυθμίσεις του λογαριασμού Hardcover· τα κλειδιά λήγουν μετά από έναν χρόνο.', hcKeyLbl: 'Κλειδί API', hcToggle: 'Αυτόματος συγχρονισμός νέων βαθμολογιών', hcPushAll: 'Αποστολή όλων των βαθμολογιών', hcPushDiff: 'Αποστολή μόνο των διαφορών', hcDone: 'Έτοιμο: %d συγχρονίστηκαν, %d χωρίς αντιστοιχία', hcNoKey: 'Επικολλήστε πρώτα το κλειδί API του Hardcover' , hcMissHead: 'Δεν βρέθηκε στο Hardcover:' , hcHint: 'Στείλτε τις βαθμολογίες σας στον λογαριασμό Hardcover. Επικολλήστε το κλειδί API από τις ρυθμίσεις του λογαριασμού Hardcover.' , tabStyle: 'Στυλ', tabMain: 'Αρχική', tabBooks: 'Βιβλία', tabFeat: 'Λειτουργίες' , hcImport: 'Εισαγωγή από το Hardcover', impMarkQ: 'Να σημειωθούν και εδώ τα %d αντιστοιχισμένα βιβλία ως ολοκληρωμένα;', impMarkDone: '%d σημειώθηκαν ως ολοκληρωμένα.' , impHint: 'Φέρτε τις βαθμολογίες σας από Goodreads, StoryGraph ή Hardcover. Επιλέξτε εδώ το εξαγόμενο CSV· το Hardcover εισάγεται απευθείας με το κουμπί στην κάρτα του. Κάθε αντιστοίχιση εμφανίζεται για επιβεβαίωση πριν αποθηκευτεί οτιδήποτε.' , impErrUnknown: 'Δεν μοιάζει με εξαγωγή από Goodreads, StoryGraph ή Hardcover: δεν βρέθηκαν στήλες τίτλου και βαθμολογίας.' , fsCard: 'Κοινωνικά', rnToggle: 'Εμφάνιση του ονόματός μου στις βαθμολογίες', rsToggle: 'Κοινοποίηση των βαθμολογιών μου στους άλλους', socNames: 'Ονόματα στις βαθμολογίες', rtAnon: 'Ανώνυμος' },
    tr: { cmStopped: "durduruldu", cmScanErrs: "{e} kontrol edilemedi (Goodreads hataları)", cmScanBusy: "Zaten bir tarama çalışıyor", cmGrErr: "Goodreads’e ulaşılamıyor, tekrar denenecek", cmNoMatchYet: "Goodreads’te eşleşme yok", cmLooking: "Goodreads’te aranıyor…", cmToolHint: "Goodreads okurları bu kitap hakkında ne düşünüyor. ? eşleşen kitabı gösterir ve değiştirmenizi sağlar.", cmSearchPh: "Goodreads’te ara…", cmNoVotes: "henüz puan yok", cmRefresh: "Yenile", cmScanAll: "Sadece eksikleri değil, hepsini yeniden tara", cmScanDone: "Bitti: {m} eşleşti · {x} bulunamadı · {s} zaten vardı", cmStop: "Durdur", cmScanLibs: "Taranacak kütüphaneler", cmScan: "Puanları tara", lsLongest: "En uzun kitaplar", lsSize: "Diskteki boyut", lsDuration: "Toplam süre", lsGenresN: "Türler", lsSeriesN: "Seriler", lsAuthorsN: "Yazarlar", lsBooks: "Kitaplar", lsGlance: "Kütüphaneye bir bakış", cmOpen: "Goodreads’te aç", cmDetect: "Bul", cmAddr: "Yardımcı adresi", scmTitle: "Seri sayfası", scmHint: "Kendi kapağın yüklenmemişse seri başlığının üstündeki üretilen kapak böyle görünür (yalnızca bu tarayıcı).", scmDeck: "Katmanlı deste", cmHint: "Goodreads okurlarının bir kitap hakkında düşündükleri: ortalama puan ve oy sayısı, kitap sayfasındaki yıldızların altında ve sıralama ile filtre olarak. Kitap sayfaları gezerken kendiliğinden dolar.", cmGrOff: "Goodreads: kurulu değil", cmSetup: "Henüz yardımcı yok mu? Bunu temanın yanında çalıştırın (aşağıdaki compose satırları), sonra Bul’a basın. Görüntüyü başka bir şekilde çalıştırmak da olur: adresini yukarıya yapıştırın.", cmGrOn: "Goodreads: bağlı", cmGrDown: "Goodreads: kurulu ama yanıt vermiyor", cmGrNoNum: "Goodreads: yanıt veriyor ama bu abs-tract sürümü puan sayıları vermiyor (ratings sürümünü kullanın, bkz. README)", cmTitle: "Goodreads puanları", cmWrong: "Yanlış kitap mı?", cmFind: "Topluluk puanı bul…", cmNone: "Eşleşme yok", cmUse: "Bunu kullan", lfCommunity: "Goodreads puanı", mfNatChange: "Değiştir…", lsByTime: "dinleme süresine göre", sbTitle: "Sunucu sıralaması", sbWeek: "Hafta", sbMonth: "Ay", sbYear: "Yıl", sbAll: "Tüm zamanlar", sbBooks: "Kitaplar", sbTopBooks: "En çok dinlenen", sbTotal: "Toplam", sbListeners: "Dinleyiciler", sbAvg: "Ortalama", wkTitle: "Dinleme dakikaları, son 7 gün", wkAvg: "Günlük ortalama", wkRow: "Üst üste gün", rpBadge: "Açık sorun bildirimleri", rpMenu: "Sorun bildir", rpTitle: "Sorun bildir", rpWhat: "Sorun ne?", rpNote: "Yöneticinin bilmesi gereken başka bir şey? (isteğe bağlı)", rpSend: "Bildirimi gönder", rpSent: "Gönderildi. Teşekkürler.", rpFail: "Gönderilemedi", rpMissing: "Eksik veya yarım içerik", rpQuality: "Kötü ses kalitesi", rpPlay: "Oynatılmıyor", rpWrong: "Yanlış kitap, kapak veya üstveri", rpChapters: "Bölümler yanlış", rpOther: "Başka bir şey", rfRate: "Puanla", rfSheet: "Bu kitabı puanla", rfOpen: "Kitap sayfasını aç", rfPickHint: "Bir puan seç", rfTitle: "Bitirdiklerini puanla", ysFinished: "Yakınlarda bitenler", ysAlmost: "Neredeyse bitti", ysMarkDone: "Bitti olarak işaretle", ysEditDate: "Bitiş tarihini değiştir", ysThisWeek: "Bu hafta", ysBestDay: "En iyi gün", ysTitle: "Dinlemen", fsTitle: "Sunucu sıralaması", fsCard: "Sunucu sıralaması", fsToggle: "Dinleme istatistiklerimi paylaş", fsJoin: "Sunucu sıralamasında görünmek için kişiselleştirme panelinde “Dinleme istatistiklerimi paylaş”ı aç.", fsWaiting: "Paylaşım açık, özetin yakında herkesle birlikte sunucu sıralamasına katılacak.", fsHint: "Dinlemenin özetini (toplamlar ve günlük dakikalar) bu sunucudaki diğer kişilerle paylaş, sıralamayı bu besler. Varsayılan olarak açık. Kapatırsan sıralamadan ve toplamlarından çıkarsın, paylaşılanlar silinir.", yirBtn: "Yılın özeti", yirTitle: "Dinleme yılın", yirYear: "Son 12 ay", yirDays: "Dinleme olan günler", yirBest: "En iyi gün", yirMonths: "Aylara göre", yirNone: "Henüz dinleme verisi yok.", ssscmDeck: "Katmanlı deste", scmFirst: "İlk kitabın kapağı", scmGrid: "Kapak ızgarası (varsayılan)", scmOff: "Gizli" , lsTitle: "Sunucu istatistikleri", lsAll: "Tüm kitaplıklar", lsPlayed: "Çalınan kitaplar", lsBest: "En iyi puanlanan", lsGenres: "Popüler türler", srvPick: "Neler kaydedilsin:", srvGRest: "Özellikler ve davranış", aeBtn: "Yazarları düzenle", aeTitle: "Kitapsız yazarlar", aeNone: "Buradaki her yazarın en az bir kitabı var.", aeRemove: "Kaldır", aeDone: "Kaldırıldı", aeWorking: "Kaldırılıyor…", rpCard: "Bildirilen sorunlar", rpHint: "Kullanıcıların kitap sayfasından bildirdiği sorunlar. Çözmek, kaydı listeden kaldırır.", rpEmpty: "Bildirim yok.", rpResolve: "Çöz", rpOpen: "Kitabı aç", apCard: "Oynatma", apToggle: "Serinin sonraki kitabını otomatik oynat", apHint: "Bir kitap bitince aynı serinin sonraki kitabı otomatik başlar. Yalnızca zaten sahip olduğun ve bitirmediğin kitaplar.", apNext: "Serinin sonraki", fdToggle: "İstatistiklerinde biten kitap araçları", ysNoDate: "tarih yok", hmShowing: "grafik şunu gösteriyor", ysStreak: "Seri", ysCurrent: "Mevcut", ysLongest: "En uzun", ysDaysTotal: "Toplam gün", ysPace: "Tempo", ysPerDay: "Dinleme günü başına", ysWhen: "Ne zaman dinliyorsun", ysTopBooks: "En çok dinlenen kitaplar", ysTopAuthors: "En çok dinlenen yazarlar", ysTopNarrators: "En çok dinlenen seslendirenler", rfToggle: "Ana sayfadaki “Puanla” bölümü", hoTitle: "Ana sayfa bölümlerinin sırası", hoEmpty: "Ana sayfayı bir kez aç ve buraya dön.", hoReset: "Varsayılan sırayı geri yükle", ctIcon: "Simge", ctColour: "Vurgu", ctNeedBook: "Audiobookshelf boş bir koleksiyonu kaydedemez, en az bir kitap seç. Sonradan serbestçe ekleyip çıkarabilirsin.", colEditIcon: "Simgeyi değiştir", ciTitle: "Koleksiyon simgesi", ciReset: "Ada göre otomatik eşleştir", colEditDesc: "Açıklamayı düzenle", colBookForms: ["kitap","kitap"], colForms: ["Koleksiyon","Koleksiyonlar"], ctTitle: "Koleksiyon oluştur", ctBlank: "Boş koleksiyon", ctNew: "Yeni koleksiyon", ctNewHint: "şablondan", ctNamePh: "Koleksiyon adı", ctSearch: "Kitap veya yazar ara…", ctCreate: "Oluştur", ctBack: "‹ Geri", ctAddAll: "Tüm kitapları ekle", abTitle: "Kitap ekle", abAdd: "Ekle", avRemove: "Fotoğrafı kaldır", avSet: "Fotoğraf seç…", ugListened: "Dinlendi", ugPhoto: "Profil fotoğrafı", ugNever: "Hiç", ugSeen: "Son aktif", ugEdit: "Kullanıcıyı düzenle", ugDelete: "Kullanıcıyı sil", ugAdd: "Fotoğraf ekle", ugChange: "Fotoğrafı değiştir", ugSortName: "A–Z", ugSortTime: "En çok dinlenen", ugSortSeen: "Son zamanlarda aktif", bsCard: "Kitap bağlantıları", bsHint: "Kitap sayfasında başlığı başka yerde aramak için düğmeler. Goodreads ve dilinin en büyük sitesi baştan açık.", bsFind: "Şurada ara", bsGlobal: "Her yerde", bsLocal: "Dilin için", bsOther: "Diğer ülkeler", pgCard: "Yeniden tasarlanan sayfalar", pgHint: "Her seçenek bir Audiobookshelf sayfasını NanoHive sürümüyle değiştirir. Kapatınca özgün sayfa geri gelir.", pgNarrators: "Seslendiren kartları", pgCollections: "Koleksiyon sayfaları", pgUsers: "Kullanıcı kartları (ayarlar)", pgStats: "Dinleme sıralaması", pgPhoto: "Üst çubukta profil fotoğrafı", pgCinematic: "Sinematik arka plan", pgTransitions: "Sayfa geçişleri", pSearch: "Ayarlarda ara…", pSearchNone: "Eşleşme yok.", erTypeface: "Yazı tipi", erPage: "Sayfa teması", erText: "Metin rengi", erBg: "Arka plan", erDefault: "Varsayılan (ABS)", erAuto: "Otomatik", nrForms: ["Seslendiren","Seslendirenler"], nrBooksForms: ["kitap","kitap"], nrSearch: "Seslendirenleri filtrele…", nrSortName: "Ad", nrSortBooks: "En çok kitap", auForms: ["Yazar","Yazarlar"], auSearch: "Yazarları filtrele…" , lfByAuthor: "Yazar › seri sırası", lfBySeries: "Seri › sıra" , lfSecFilters: "Daha çok filtre", lfSecSort: "Çoklu sıralama", lfAuthor: "Yazar", lfSeries: "Seri", lfTitle: "Başlık", lfYear: "Yıl", lfAdded: "Eklendi", lfDuration: "Süre", lfNarrator: "Seslendiren", lfGenre: "Tür", lfLanguage: "Dil", lfProgress: "İlerleme", lfPgFinished: "Bitenler", lfPgInProgress: "Devam edenler", lfPgNot: "Başlanmayanlar" , logoUpload: 'Cihazdan yükle…', logoUploaded: 'Yüklendi ✓', logoBadType: 'Desteklenmeyen görsel türü', logoTooBig: 'Görsel çok büyük (en fazla 4 MB)', logoOr: 'veya', bookPage: 'Kitap sayfası', ratingsToggle: 'Topluluk puanları (yıldızlar ve incelemeler)', ratingsHint: 'Her kitap sayfasında kısa incelemeli yıldız puanları, bu sunucunun tüm kullanıcılarıyla paylaşılır.', searchCard: 'Arama', gsToggle: 'Tüm kütüphanelerde birden ara', gsHint: 'Erişebildiğin tüm kütüphanelerden tek birleşik sonuç listesi, her sonuçta kütüphane rozeti. Kapatınca kütüphane başına menü geri gelir.', gsNoResults: 'Sonuç yok', gsSearching: 'Aranıyor…', gsBooks: 'Kitaplar', gsSeries: 'Seriler', gsAuthors: 'Yazarlar', spTitle: 'Başlangıç sayfası', spHint: 'Uygulamayı her zaman seçilen kütüphane görünümünde aç (yalnızca bu tarayıcı). Doğrudan bağlantılar asla yönlendirilmez.', spDefault: 'Uygulama varsayılanı', spViewHome: 'Ana sayfa', spViewLibrary: 'Kütüphane', spViewSeries: 'Seriler', spViewCollections: 'Koleksiyonlar', spViewAuthors: 'Yazarlar', scSet: 'Seri kapağı ayarla', scRemove: 'Seri kapağını kaldır', scEditDesc: 'Seri açıklamasını düzenle', cardStars: 'Kapaklarda yıldız rozetleri', hoverPolish: 'Kart üzerine gelme rötuşu (daha yumuşak karartma, vurgulu oynat, üzerine gelince yıldızlar)', sectUser: 'Ayarların', sectAdmin: 'Yönetim', lockTitle: 'Özellik anahtarları', lockHint: 'Bir özelliği bu sunucunun tüm kullanıcıları için zorla kapatır. Kişisel ayarları geçersiz kılar; diğerleri bir sonraki yenilemede görür.', lockRatings: 'Puanlamayı her yerde kapat', lockCardRatings: 'Kapaklardaki yıldızları kapat', lockGlobalSearch: 'Genel aramayı kapat', lockHoverPolish: 'Kart rötuşunu kapat', lockHeroCarousel: 'Ana sayfa dönencesini kapat', lockedNote: 'yönetici tarafından kapatıldı', lfSort: 'Puan', lfRated: 'Puanlı', lfUnrated: 'Puansız', lfClear: 'Temizle', lfNone: 'Eşleşen kitap yok', lfSection: 'Puan', lfWho: 'Kimin puanları', lfEveryone: 'Herkes', sdSave: 'Kaydet', sdClear: 'Kitap açıklamasını geri yükle', sdMore: 'Devamını oku', sdLess: 'Daha az göster', shSeries: 'Seri', shBy: '', shMore: ' ve daha fazlası', shBookForms: ['kitap', 'kitap'], shAvg: 'kitap ortalaması', shRated: 'puanlı', impLabel: 'Puanları içe aktar', impHint: 'Puanlarını StoryGraph veya Goodreads\'ten getir. Oradaki kitaplarını CSV olarak dışa aktar ve dosyayı burada seç. Her eşleşme onayına sunulur ve sen onaylayana kadar hiçbir şey kaydedilmez.', impBtn: 'CSV dosyası seç…', impReading: 'Dosya okunuyor…', impMatching: 'Kütüphanelerinle eşleştiriliyor…', impErrUnknown: 'Bu bir StoryGraph veya Goodreads dışa aktarımına benzemiyor. Onların CSV dosyası olmalı, "Star Rating" veya "My Rating" sütunuyla.', impErrEmpty: 'Dosyada satır yok.', impErrNoRated: 'Dosyada puanlanmış kitap yok.', impErrRead: 'Dosya okunamadı.', impErrLoad: 'Kütüphanelerin yüklenemedi. Birazdan tekrar dene.', impErrNoLibs: 'Hiçbir kütüphane puanlamaya katılmıyor, içe aktarılacak yer yok.', impTitle: 'Puanları içe aktar', impFrom: '%s dışa aktarımı: %d kitap, %d tanesi puanlı', impLibs: 'Aranıyor: %s', impMatched: 'Eşleşti', impCheck: 'Bakmaya değer', impCheckHint: 'Yakın ama emin değil. Eşleşmeyi kontrol et veya başka birini seç.', impAlready: 'Zaten senin puanladıkların', impAlreadyHint: 'Üzerine yazmayı seçmedikçe dokunulmaz.', impNone: 'Kütüphanende yok', impNoneHint: 'Bu sunucuda bunlarla eşleşen bir şey yok.', impAll: 'Tümü', impSkip: 'Bunu atla', impDup: 'kopya', impByTitle: 'başlık', impYours: 'seninki: %s', impReviews: 'Yazılı incelemeleri de içe aktar (%d)', impOver: 'Zaten sahip olduğum puanların üzerine yaz', impGo: '%d puanı içe aktar', impCancel: 'İptal', impCloseBtn: 'Kapat', impWorking: 'Kaydediliyor… %d / %d', impDone: '%d puan içe aktarıldı.', impFail: '%d kaydedilemedi.', rdTitle: 'Bu kitabı kim okuyor', rdFin: 'Bitti', socTitle: 'Sosyal özellikler', socHint: 'Bu sunucunun kullanıcılar arasında neyi paylaştığı. Dinleme süresi sıralamayı besler; diğer ikisi kimin ne okuduğunu açık eder, bu yüzden sen açana kadar kapalı kalır. Herkes için birden geçerli.', socTime: 'Dinleme süresi (sunucu sıralaması)', socContent: 'Paylaşılan istatistiklerde en çok dinlenenler', socWho: 'Kim okuyor (kitap sayfaları)', socUsers: 'Kullanıcı başına', socUsersHint: 'Birini kapatırsan, kendi seçimi ne olursa olsun hiçbir verisi paylaşılmaz; geri açabilecek tek kişi sensin. "Kendi çıktı" o kişinin hayır dediği anlamına gelir.', socOpted: 'kendi çıktı', srvTitle: 'Sunucu varsayılanları', srvHint: 'Mevcut ayarlarını bu sunucunun tüm kullanıcıları için varsayılan olarak kaydet. Herkes yine de üstüne kişiselleştirebilir. Güncellemelerde korunmaları için tema kapsayıcısında /data/nh yoluna bir birim bağla.', srvSave: 'Sunucu varsayılanı olarak kaydet', srvClear: 'Sunucu varsayılanını temizle', srvSaved: 'Kaydedildi', srvCleared: 'Temizlendi', srvErr: 'Başarısız: yönetici girişi gerekli', crMode: 'Okumaya devam rafı', crCombine: 'Dönenceyle birleştir', crSeparate: 'Ayrı raf olarak tut', crHide: 'Gizli', fsHint: 'Dinlemeni bu sunucudaki diğer kişilerle paylaş: sıralama için toplam süre ve günlük dakikalar, yönetici açtıysa en çok dinlediğin başlıklar ve hangi kitapları okuyup bitirdiğin de dahil. Varsayılan olarak açık. Kapatırsan paylaştığın her şey silinir.' , amfHide: 'Bitti olarak işaretle bölümünü gizle' , amfConfirm: 'Bu kitap bitti olarak işaretlensin mi?' , fsHint: 'Dinlemenin özetini (toplamlar ve günlük dakikalar) bu sunucudaki diğer kişilerle paylaş, sıralamayı bu besler. Varsayılan olarak açık. Kapatırsan sıralamadan ve toplamlarından çıkarsın, paylaşılanlar silinir.' , srToggle: 'Hangi kitapları okuduğunu paylaş', srHint: 'Başkalarının gördüklerine şu an hangi kitapları okuduğunu, hangilerini bitirdiğini ve en çok hangilerini dinlediğini ekler. Varsayılan olarak kapalı; yöneticinin sunucu anahtarlarının da açık olması gerekir.' , fsHint: 'Dinleme süreni (toplamlar ve günlük dakikalar) bu sunucudaki diğer kişilerle paylaş; sıralamayı o besler. Varsayılan olarak açık. Kapatırsan diğer kullanıcılar seni artık görmez; sunucu yöneticileri etkinliği yine görür, çıkış işaretiyle.', srHint: 'Ayrıca başkalarının hangi kitapları okuduğunu, bitirdiğini ve en çok dinlediğini görmesini sağlar. Yönetici ilgili sunucu anahtarlarını açana kadar hiçbir şey görünmez.', seshTitle: 'Son oturumlar', seshNow: 'Şu an dinliyor' , hcHint: 'Puanlarını Hardcover hesabına gönder. API anahtarını Hardcover hesap ayarlarından yapıştır; anahtarlar bir yıl sonra sona erer.', hcKeyLbl: 'API anahtarı', hcToggle: 'Yeni puanları otomatik eşitle', hcPushAll: 'Tüm puanları gönder', hcPushDiff: 'Yalnızca farkları gönder', hcDone: 'Bitti: %d eşitlendi, %d eşleşmedi', hcNoKey: 'Önce Hardcover API anahtarını yapıştır' , hcMissHead: 'Hardcover üzerinde bulunamadı:' , hcHint: 'Puanlarını Hardcover hesabına gönder. API anahtarını Hardcover hesap ayarlarından yapıştır.' , tabStyle: 'Stil', tabMain: 'Ana sayfa', tabBooks: 'Kitaplar', tabFeat: 'Özellikler' , hcImport: 'Hardcover\'dan içe aktar', impMarkQ: 'Eşleşen %d kitap burada da bitmiş olarak işaretlensin mi?', impMarkDone: '%d bitmiş olarak işaretlendi.' , impHint: 'Puanlarını Goodreads, StoryGraph veya Hardcover\'dan getir. Dışa aktarılan CSV\'yi buradan seç; Hardcover kendi kartındaki düğmeyle doğrudan içe aktarır. Her eşleşme kaydedilmeden önce onay için gösterilir.' , impErrUnknown: 'Bu bir Goodreads, StoryGraph veya Hardcover dışa aktarımına benzemiyor: başlık ve puan sütunları bulunamadı.' , fsCard: 'Sosyal', rnToggle: 'Adımı puanlarla birlikte göster', rsToggle: 'Puanlarımı başkalarıyla paylaş', socNames: 'Puanlarda adlar', rtAnon: 'Anonim' },
    ca: { cmStopped: "aturat", cmScanErrs: "{e} sense comprovar (errors de Goodreads)", cmScanBusy: "Ja hi ha una revisió en marxa", cmGrErr: "Goodreads no respon, ho tornaré a provar", cmNoMatchYet: "Cap coincidència a Goodreads", cmLooking: "Cercant a Goodreads…", cmToolHint: "El que pensen d’aquest llibre els lectors de Goodreads. El ? mostra el llibre associat i permet canviar-lo.", cmSearchPh: "Cerca a Goodreads…", cmNoVotes: "encara sense valoracions", cmRefresh: "Actualitza", cmScanAll: "Torna a revisar-ho tot, no només els que falten", cmScanDone: "Fet: {m} trobats · {x} no trobats · {s} ja en tenien", cmStop: "Atura", cmScanLibs: "Biblioteques a revisar", cmScan: "Cerca valoracions", lsLongest: "Llibres més llargs", lsSize: "Mida al disc", lsDuration: "Durada total", lsGenresN: "Gèneres", lsSeriesN: "Sèries", lsAuthorsN: "Autors", lsBooks: "Llibres", lsGlance: "La biblioteca d’un cop d’ull", cmOpen: "Obre a Goodreads", cmDetect: "Cerca", cmAddr: "Adreça de l’ajudant", scmTitle: "Pàgina de sèrie", scmHint: "Aspecte de la coberta generada sobre el títol d’una sèrie quan no se n’ha pujat cap (només aquest navegador).", scmDeck: "Baralla apilada", cmHint: "El que pensen d’un llibre els lectors de Goodreads: nota mitjana i nombre de vots, sota les estrelles de la pàgina del llibre i com a ordenació i filtre. Les pàgines dels llibres s’omplen soles en navegar.", cmGrOff: "Goodreads: no configurat", cmSetup: "Encara sense ajudant? Executa això al costat del tema (línies compose a sota) i prem Cerca. Qualsevol altra manera d’executar la imatge també val: enganxa’n l’adreça a dalt.", cmGrOn: "Goodreads: connectat", cmGrDown: "Goodreads: configurat però no respon", cmGrNoNum: "Goodreads: respon, però aquesta versió d’abs-tract no dona valoracions (feu servir la versió amb valoracions, vegeu README)", cmTitle: "Valoracions de Goodreads", cmWrong: "Llibre equivocat?", cmFind: "Cerca una valoració de la comunitat…", cmNone: "Cap coincidència", cmUse: "Fes servir", lfCommunity: "Valoració de Goodreads", mfNatChange: "Canvia…", lsByTime: "per temps d'escolta", sbTitle: "Classificació del servidor", sbWeek: "Setmana", sbMonth: "Mes", sbYear: "Any", sbAll: "Tot el temps", sbBooks: "Llibres", sbTopBooks: "Més escoltats", sbTotal: "Total", sbListeners: "Oients", sbAvg: "Mitjana", wkTitle: "Minuts d’escolta, últims 7 dies", wkAvg: "Mitjana diària", wkRow: "Dies seguits", rpBadge: "Informes de problemes oberts", rpMenu: "Informa d’un problema", rpTitle: "Informa d’un problema", rpWhat: "Què passa?", rpNote: "Res més que hagi de saber l’administrador? (opcional)", rpSend: "Envia l’informe", rpSent: "Enviat. Gràcies.", rpFail: "No s’ha pogut enviar", rpMissing: "Contingut absent o incomplet", rpQuality: "Mala qualitat d’àudio", rpPlay: "No es reprodueix", rpWrong: "Llibre, coberta o metadades equivocats", rpChapters: "Capítols equivocats", rpOther: "Una altra cosa", rfRate: "Valora", rfSheet: "Valora aquest llibre", rfOpen: "Obre la pàgina del llibre", rfPickHint: "Tria una valoració", rfTitle: "Valora el que has acabat", ysFinished: "Acabats fa poc", ysAlmost: "Gairebé acabats", ysMarkDone: "Marca com a acabat", ysEditDate: "Canvia la data d’acabament", ysThisWeek: "Aquesta setmana", ysBestDay: "Millor dia", ysTitle: "La teva escolta", fsTitle: "Classificació del servidor", fsCard: "Classificació del servidor", fsToggle: "Comparteix les meves estadístiques d’escolta", fsJoin: "Activa «Comparteix les meves estadístiques d’escolta» al panell de personalització per aparèixer a la classificació del servidor.", fsWaiting: "La compartició és activa, el teu resum s’afegirà aviat a la classificació del servidor, amb tothom.", fsHint: "Comparteix un resum de la teva escolta (totals i minuts per dia) amb la resta d’aquest servidor, alimenta la classificació. Activat per defecte. En desactivar-ho desapareixes de la classificació i dels totals, i el que has compartit s’esborra.", yirBtn: "Resum de l’any", yirTitle: "El teu any d’escolta", yirYear: "Últims 12 mesos", yirDays: "Dies amb escolta", yirBest: "Millor dia", yirMonths: "Per mesos", yirNone: "Encara no hi ha dades d’escolta.", ssscmDeck: "Baralla apilada", scmFirst: "Coberta del primer llibre", scmGrid: "Graella de cobertes (per defecte)", scmOff: "Amagada" , lsTitle: "Estadístiques del servidor", lsAll: "Totes les biblioteques", lsPlayed: "Llibres reproduïts", lsBest: "Més ben valorats", lsGenres: "Gèneres principals", srvPick: "Què incloure:", srvGRest: "Funcions i comportament", aeBtn: "Endreça els autors", aeTitle: "Autors sense llibres", aeNone: "Cada autor d'aquí té almenys un llibre.", aeRemove: "Elimina", aeDone: "Eliminats", aeWorking: "Eliminant…", rpCard: "Problemes informats", rpHint: "Problemes que els usuaris han informat des d'una pàgina de llibre. Resoldre'l el treu de la llista.", rpEmpty: "Res informat.", rpResolve: "Resol", rpOpen: "Obre el llibre", apCard: "Reproducció", apToggle: "Reprodueix automàticament el següent llibre d'una sèrie", apHint: "Quan un llibre s'acaba, comença automàticament el següent de la mateixa sèrie. Només llibres que ja tens i no has acabat.", apNext: "Següent de la sèrie", fdToggle: "Eines de llibres acabats a les teves estadístiques", ysNoDate: "sense data", hmShowing: "el gràfic mostra", ysStreak: "Ratxa", ysCurrent: "Actual", ysLongest: "Més llarga", ysDaysTotal: "Dies en total", ysPace: "Ritme", ysPerDay: "Per dia d'escolta", ysWhen: "Quan escoltes", ysTopBooks: "Llibres més escoltats", ysTopAuthors: "Autors més escoltats", ysTopNarrators: "Narradors més escoltats", rfToggle: "Secció «Valora» a l'inici", hoTitle: "Ordre de les seccions de l'inici", hoEmpty: "Obre la pàgina d'inici un cop i torna aquí.", hoReset: "Restableix l'ordre", ctIcon: "Icona", ctColour: "Accent", ctNeedBook: "L'Audiobookshelf no pot desar una col·lecció buida, tria almenys un llibre. Després pots afegir i treure lliurement.", colEditIcon: "Canvia la icona", ciTitle: "Icona de la col·lecció", ciReset: "Ajusta automàticament al nom", colEditDesc: "Edita la descripció", colBookForms: ["llibre","llibres"], colForms: ["Col·lecció","Col·leccions"], ctTitle: "Crea una col·lecció", ctBlank: "Col·lecció buida", ctNew: "Col·lecció nova", ctNewHint: "des d'una plantilla", ctNamePh: "Nom de la col·lecció", ctSearch: "Cerca llibres o autors…", ctCreate: "Crea", ctBack: "‹ Enrere", ctAddAll: "Afegeix tots els llibres", abTitle: "Afegeix llibres", abAdd: "Afegeix", avRemove: "Treu la foto", avSet: "Tria una foto…", ugListened: "Escoltat", ugPhoto: "Foto de perfil", ugNever: "Mai", ugSeen: "Darrera activitat", ugEdit: "Edita l'usuari", ugDelete: "Suprimeix l'usuari", ugAdd: "Afegeix foto", ugChange: "Canvia la foto", ugSortName: "A–Z", ugSortTime: "Més escoltats", ugSortSeen: "Actius recentment", bsCard: "Enllaços del llibre", bsHint: "Botons a la pàgina del llibre per cercar el títol en altres llocs. Goodreads i el lloc més gran de la teva llengua venen activats.", bsFind: "Cerca a", bsGlobal: "Arreu", bsLocal: "Per a la teva llengua", bsOther: "Altres països", pgCard: "Pàgines redissenyades", pgHint: "Cada opció substitueix una pàgina d'Audiobookshelf per la versió NanoHive. Desactiva-la per recuperar l'original.", pgNarrators: "Targetes de narradors", pgCollections: "Pàgines de col·leccions", pgUsers: "Targetes d'usuaris (configuració)", pgStats: "Classificació d'escolta", pgPhoto: "Foto de perfil a la barra superior", pgCinematic: "Fons cinematogràfic", pgTransitions: "Transicions de pàgina", pSearch: "Cerca configuració…", pSearchNone: "Res no coincideix.", erTypeface: "Tipografia", erPage: "Tema de pàgina", erText: "Color del text", erBg: "Fons", erDefault: "Per defecte (ABS)", erAuto: "Auto", nrForms: ["Narrador","Narradors"], nrBooksForms: ["llibre","llibres"], nrSearch: "Filtra narradors…", nrSortName: "Nom", nrSortBooks: "Més llibres", auForms: ["Autor","Autors"], auSearch: "Filtra autors…" , lfByAuthor: "Autor › ordre de sèrie", lfBySeries: "Sèrie › ordre" , lfSecFilters: "Més filtres", lfSecSort: "Ordenació múltiple", lfAuthor: "Autor", lfSeries: "Sèrie", lfTitle: "Títol", lfYear: "Any", lfAdded: "Afegit", lfDuration: "Durada", lfNarrator: "Narrador", lfGenre: "Gènere", lfLanguage: "Llengua", lfProgress: "Progrés", lfPgFinished: "Acabats", lfPgInProgress: "En curs", lfPgNot: "No començats" , logoUpload: 'Puja des del dispositiu…', logoUploaded: 'Pujat ✓', logoBadType: 'Tipus d\'imatge no compatible', logoTooBig: 'Imatge massa gran (màx. 4 MB)', logoOr: 'o', bookPage: 'Pàgina del llibre', ratingsToggle: 'Valoracions de la comunitat (estrelles i ressenyes)', ratingsHint: 'Valoracions amb estrelles i ressenyes curtes a cada pàgina de llibre, compartides amb tots els usuaris d\'aquest servidor.', searchCard: 'Cerca', gsToggle: 'Cerca a totes les biblioteques alhora', gsHint: 'Una llista de resultats combinada de totes les biblioteques accessibles, amb una insígnia de biblioteca a cada resultat. Desactiva-ho per recuperar el menú per biblioteca.', gsNoResults: 'Cap resultat', gsSearching: 'Cercant…', gsBooks: 'Llibres', gsSeries: 'Sèries', gsAuthors: 'Autors', spTitle: 'Pàgina d\'inici', spHint: 'Obre l\'aplicació sempre a la vista triada d\'una biblioteca (només aquest navegador). Els enllaços directes mai no es redirigeixen.', spDefault: 'Per defecte de l\'aplicació', spViewHome: 'Inici', spViewLibrary: 'Biblioteca', spViewSeries: 'Sèries', spViewCollections: 'Col·leccions', spViewAuthors: 'Autors', scSet: 'Estableix la coberta de la sèrie', scRemove: 'Treu la coberta de la sèrie', scEditDesc: 'Edita la descripció de la sèrie', cardStars: 'Estrelles a les cobertes', hoverPolish: 'Acabat en passar el cursor (enfosquiment més suau, botó accentuat, estrelles en passar)', sectUser: 'Els teus ajustos', sectAdmin: 'Administració', lockTitle: 'Interruptors de funcions', lockHint: 'Desactiva a la força una funció per a tots els usuaris d\'aquest servidor. Guanya als ajustos personals; els altres ho veuran després de recarregar.', lockRatings: 'Desactiva les valoracions arreu', lockCardRatings: 'Desactiva les estrelles a les cobertes', lockGlobalSearch: 'Desactiva la cerca global', lockHoverPolish: 'Desactiva l\'acabat de les targetes', lockHeroCarousel: 'Desactiva el carrusel d\'inici', lockedNote: 'desactivat per l\'administrador', lfSort: 'Valoració', lfRated: 'Valorats', lfUnrated: 'Sense valorar', lfClear: 'Neteja', lfNone: 'Cap llibre no coincideix', lfSection: 'Valoració', lfWho: 'Valoracions de qui', lfEveryone: 'Tothom', sdSave: 'Desa', sdClear: 'Restaura la descripció del llibre', sdMore: 'Llegeix més', sdLess: 'Mostra\'n menys', shSeries: 'Sèrie', shBy: 'de ', shMore: ' i més', shBookForms: ['llibre', 'llibres'], shAvg: 'mitjana dels llibres', shRated: 'valorats', impLabel: 'Importa valoracions', impHint: 'Porta les teves valoracions de StoryGraph o Goodreads. Exporta-hi els teus llibres com a CSV i tria el fitxer aquí. Cada coincidència es mostra perquè la confirmis i res no es desa fins que ho facis.', impBtn: 'Tria el fitxer CSV…', impReading: 'Llegint el fitxer…', impMatching: 'Comparant amb les teves biblioteques…', impErrUnknown: 'Això no sembla una exportació de StoryGraph ni de Goodreads. Ha de ser el seu CSV, amb una columna "Star Rating" o "My Rating".', impErrEmpty: 'El fitxer no té files.', impErrNoRated: 'No hi ha llibres valorats al fitxer.', impErrRead: 'No s\'ha pogut llegir el fitxer.', impErrLoad: 'No s\'han pogut carregar les biblioteques. Torna-ho a provar d\'aquí a un moment.', impErrNoLibs: 'Cap biblioteca no participa en les valoracions, així que no hi ha on importar.', impTitle: 'Importa valoracions', impFrom: 'Exportació de %s: %d llibres, %d de valorats', impLibs: 'Cercant a: %s', impMatched: 'Coincidents', impCheck: 'Val la pena revisar', impCheckHint: 'A prop però sense certesa. Revisa la coincidència o tria\'n una altra.', impAlready: 'Ja valorats per tu', impAlreadyHint: 'Es deixen com estan, llevat que demanis sobreescriure\'ls.', impNone: 'No són a la teva biblioteca', impNoneHint: 'Res en aquest servidor no coincideix amb aquests.', impAll: 'Tots', impSkip: 'Omet aquest', impDup: 'duplicat', impByTitle: 'títol', impYours: 'la teva: %s', impReviews: 'Importa també les ressenyes escrites (%d)', impOver: 'Sobreescriu les valoracions que ja tinc', impGo: 'Importa %d valoracions', impCancel: 'Cancel·la', impCloseBtn: 'Tanca', impWorking: 'Desant… %d de %d', impDone: '%d valoracions importades.', impFail: '%d no s\'han pogut desar.', rdTitle: 'Qui llegeix aquest llibre', rdFin: 'Acabat', socTitle: 'Funcions socials', socHint: 'El que aquest servidor comparteix entre usuaris. El temps d\'escolta alimenta el rànquing; les altres dues revelen què llegeix cadascú, així que resten apagades fins que les actives. S\'aplica a tothom alhora.', socTime: 'Temps d\'escolta (rànquing del servidor)', socContent: 'Títols destacats a les estadístiques compartides', socWho: 'Qui llegeix (pàgines dels llibres)', socUsers: 'Per usuari', socUsersHint: 'Si apagues algú, res del que és seu no es comparteix, triï el que triï; només tu pots revertir-ho. "S\'ha donat de baixa" vol dir que la persona ha dit que no per si mateixa.', socOpted: 's\'ha donat de baixa', srvTitle: 'Valors per defecte del servidor', srvHint: 'Desa els teus ajustos actuals com a predeterminats per a tots els usuaris d\'aquest servidor. Cadascú pot personalitzar-hi a sobre. Munta un volum a /data/nh al contenidor del tema per conservar-los entre actualitzacions.', srvSave: 'Desa com a predeterminats del servidor', srvClear: 'Neteja els predeterminats del servidor', srvSaved: 'Desat', srvCleared: 'Netejat', srvErr: 'Ha fallat: cal iniciar sessió com a administrador', crMode: 'Prestatge Continua llegint', crCombine: 'Combina amb el carrusel', crSeparate: 'Mantén com a prestatge separat', crHide: 'Amagat', fsHint: 'Comparteix la teva escolta amb la resta de persones d\'aquest servidor: totals i minuts per dia per al rànquing i, si l\'administrador ho ha activat, també els teus títols destacats i quins llibres llegeixes o has acabat. Activat per defecte. En desactivar-ho, tot el que has compartit s\'esborra.' , amfHide: 'Amaga Marca com a acabat' , amfConfirm: 'Marcar aquest llibre com a acabat?' , fsHint: 'Comparteix un resum de la teva escolta (totals i minuts per dia) amb la resta d’aquest servidor, alimenta la classificació. Activat per defecte. En desactivar-ho desapareixes de la classificació i dels totals, i el que has compartit s’esborra.' , srToggle: 'Comparteix quins llibres llegeixes', srHint: 'Afegeix al que veuen els altres quins llibres estàs llegint, quins has acabat i quins escoltes més. Desactivat per defecte; també cal que els interruptors de servidor de l’administrador estiguin activats.' , fsHint: 'Comparteix el teu temps d’escolta (totals i minuts per dia) amb la resta de persones d’aquest servidor; alimenta el rànquing. Activat per defecte. Si el desactives, els altres usuaris ja no et veuen; els administradors del servidor continuen veient l’activitat, marcada com a donada de baixa.', srHint: 'També permet als altres veure quins llibres llegeixes, quins has acabat i quins escoltes més. No es mostra res fins que l’administrador activa els interruptors de servidor corresponents.', seshTitle: 'Sessions recents', seshNow: 'Escoltant ara' , hcHint: 'Envia les teves valoracions al teu compte de Hardcover. Enganxa la clau API des de la configuració del compte de Hardcover; les claus caduquen al cap d’un any.', hcKeyLbl: 'Clau API', hcToggle: 'Sincronitza automàticament les valoracions noves', hcPushAll: 'Envia totes les valoracions', hcPushDiff: 'Envia només les diferències', hcDone: 'Fet: %d sincronitzades, %d sense coincidència', hcNoKey: 'Primer enganxa la teva clau API de Hardcover' , hcMissHead: 'No trobat a Hardcover:' , hcHint: 'Envia les teves valoracions al teu compte de Hardcover. Enganxa la clau API des de la configuració del compte de Hardcover.' , tabStyle: 'Estil', tabMain: 'Pàgina principal', tabBooks: 'Llibres', tabFeat: 'Funcions' , hcImport: 'Importa de Hardcover', impMarkQ: 'Marcar també aquí els %d llibres coincidents com a acabats?', impMarkDone: '%d marcats com a acabats.' , impHint: 'Porta les teves valoracions de Goodreads, StoryGraph o Hardcover. Tria aquí el CSV exportat; Hardcover s’importa directament amb el botó de la seva targeta. Cada coincidència es mostra per confirmar abans de desar res.' , impErrUnknown: 'Això no sembla un export de Goodreads, StoryGraph ni Hardcover: no s\'han trobat columnes de títol i valoració.' , fsCard: 'Social', rnToggle: 'Mostra el meu nom amb les valoracions', rsToggle: 'Comparteix les meves valoracions amb els altres', socNames: 'Noms amb les valoracions', rtAnon: 'Anònim' },
    eu: { cmStopped: "geldituta", cmScanErrs: "{e} egiaztatu gabe (Goodreads erroreak)", cmScanBusy: "Azterketa bat dabil jada", cmGrErr: "Goodreads ez dago eskuragarri, berriro saiatuko naiz", cmNoMatchYet: "Ez da aurkitu Goodreads-en", cmLooking: "Goodreads-en bilatzen…", cmToolHint: "Goodreads-eko irakurleek liburu honi buruz pentsatzen dutena. ? ikurrak lotutako liburua erakusten du eta aldatzeko aukera ematen du.", cmSearchPh: "Bilatu Goodreads-en…", cmNoVotes: "oraindik baloraziorik ez", cmRefresh: "Freskatu", cmScanAll: "Berrikusi dena, ez soilik falta direnak", cmScanDone: "Eginda: {m} aurkituta · {x} ez da aurkitu · {s} bazuten", cmStop: "Gelditu", cmScanLibs: "Aztertu beharreko liburutegiak", cmScan: "Bilatu balorazioak", lsLongest: "Liburu luzeenak", lsSize: "Diskoko tamaina", lsDuration: "Iraupen osoa", lsGenresN: "Generoak", lsSeriesN: "Sailak", lsAuthorsN: "Egileak", lsBooks: "Liburuak", lsGlance: "Liburutegia begirada batean", cmOpen: "Ireki Goodreads-en", cmDetect: "Bilatu", cmAddr: "Laguntzailearen helbidea", scmTitle: "Serie-orria", scmHint: "Serie baten izenburuaren gainean sortutako azalaren itxura, berezkorik igo ez denean (nabigatzaile hau bakarrik).", scmDeck: "Geruzatutako sorta", cmHint: "Goodreads-eko irakurleek liburu bati buruz pentsatzen dutena: batez besteko puntuazioa eta boto kopurua, liburuaren orriko izarren azpian eta ordenatzeko eta iragazteko. Liburuen orriak beren kabuz betetzen dira nabigatzean.", cmGrOff: "Goodreads: konfiguratu gabe", cmSetup: "Oraindik laguntzailerik ez? Exekutatu hau gaiaren ondoan (compose lerroak behean) eta sakatu Bilatu. Irudia beste edozein modutan exekutatzea ere balio du: itsatsi bere helbidea goian.", cmGrOn: "Goodreads: konektatuta", cmGrDown: "Goodreads: konfiguratuta baina ez du erantzuten", cmGrNoNum: "Goodreads: erantzuten du, baina abs-tract bertsio honek ez du baloraziorik ematen (erabili balorazioen bertsioa, ikus README)", cmTitle: "Goodreads balorazioak", cmWrong: "Liburu okerra?", cmFind: "Bilatu komunitatearen balorazioa…", cmNone: "Bat ere ez", cmUse: "Hau erabili", lfCommunity: "Goodreads balorazioa", mfNatChange: "Aldatu…", lsByTime: "entzute-denboraren arabera", sbTitle: "Zerbitzariaren sailkapena", sbWeek: "Astea", sbMonth: "Hilabetea", sbYear: "Urtea", sbAll: "Denbora guztia", sbBooks: "Liburuak", sbTopBooks: "Entzunenak", sbTotal: "Guztira", sbListeners: "Entzuleak", sbAvg: "Batezbestekoa", wkTitle: "Entzute-minutuak, azken 7 egunak", wkAvg: "Eguneko batezbestekoa", wkRow: "Egun jarraian", rpBadge: "Arazo-txosten irekiak", rpMenu: "Arazo baten berri eman", rpTitle: "Arazo baten berri eman", rpWhat: "Zer dago gaizki?", rpNote: "Beste zerbait administratzailearentzat? (aukerakoa)", rpSend: "Bidali txostena", rpSent: "Bidalita. Eskerrik asko.", rpFail: "Ezin izan da bidali", rpMissing: "Eduki falta edo osatugabea", rpQuality: "Audio-kalitate txarra", rpPlay: "Ez da erreproduzitzen", rpWrong: "Liburu, azal edo metadatu okerrak", rpChapters: "Kapitulu okerrak", rpOther: "Beste zerbait", rfRate: "Baloratu", rfSheet: "Baloratu liburu hau", rfOpen: "Ireki liburuaren orria", rfPickHint: "Aukeratu balorazioa", rfTitle: "Baloratu amaitutakoa", ysFinished: "Duela gutxi amaituak", ysAlmost: "Ia amaituta", ysMarkDone: "Markatu amaitutzat", ysEditDate: "Aldatu amaiera-data", ysThisWeek: "Aste honetan", ysBestDay: "Egunik onena", ysTitle: "Zure entzutea", fsTitle: "Zerbitzariaren sailkapena", fsCard: "Zerbitzariaren sailkapena", fsToggle: "Partekatu nire entzute-estatistikak", fsJoin: "Aktibatu «Partekatu nire entzute-estatistikak» pertsonalizazio-panelean zerbitzariaren sailkapenean agertzeko.", fsWaiting: "Partekatzea aktibo dago, zure laburpena laster sartuko da zerbitzariaren sailkapenean, besteekin batera.", fsHint: "Partekatu zure entzutearen laburpena (guztizkoak eta eguneko minutuak) zerbitzari honetako gainerakoekin, sailkapena elikatzen du. Lehenespenez aktibatuta. Desaktibatzean sailkapenetik eta bere guztizkoetatik desagertzen zara, eta partekatutakoa ezabatzen da.", yirBtn: "Urtearen laburpena", yirTitle: "Zure entzute-urtea", yirYear: "Azken 12 hilabeteak", yirDays: "Entzutedun egunak", yirBest: "Egunik onena", yirMonths: "Hilabeteka", yirNone: "Oraindik ez dago entzute-daturik.", ssscmDeck: "Geruzatutako sorta", scmFirst: "Lehen liburuaren azala", scmGrid: "Azalen sareta (lehenetsia)", scmOff: "Ezkutatuta" , lsTitle: "Zerbitzariaren estatistikak", lsAll: "Liburutegi guztiak", lsPlayed: "Erreproduzitutako liburuak", lsBest: "Baloraziorik onenak", lsGenres: "Genero nagusiak", srvPick: "Zer gorde:", srvGRest: "Funtzioak eta portaera", aeBtn: "Egileak txukundu", aeTitle: "Libururik gabeko egileak", aeNone: "Hemengo egile bakoitzak gutxienez liburu bat du.", aeRemove: "Kendu", aeDone: "Kenduta", aeWorking: "Kentzen…", rpCard: "Jakinarazitako arazoak", rpHint: "Erabiltzaileek liburu-orritik jakinarazitako arazoak. Konpontzean zerrendatik kentzen da.", rpEmpty: "Ezer ez da jakinarazi.", rpResolve: "Konpondu", rpOpen: "Ireki liburua", apCard: "Erreprodukzioa", apToggle: "Serie bateko hurrengo liburua automatikoki erreproduzitu", apHint: "Liburu bat amaitzean, serie bereko hurrengoa automatikoki hasten da. Dagoeneko dituzun eta amaitu gabeko liburuak soilik.", apNext: "Seriearen hurrengoa", fdToggle: "Amaitutako liburuen tresnak estatistiketan", ysNoDate: "datarik gabe", hmShowing: "grafikoak erakusten du", ysStreak: "Bolada", ysCurrent: "Unekoa", ysLongest: "Luzeena", ysDaysTotal: "Egunak guztira", ysPace: "Erritmoa", ysPerDay: "Entzute-eguneko", ysWhen: "Noiz entzuten duzun", ysTopBooks: "Gehien entzundako liburuak", ysTopAuthors: "Gehien entzundako egileak", ysTopNarrators: "Gehien entzundako narratzaileak", rfToggle: "«Baloratu» atala hasieran", hoTitle: "Hasierako atalen ordena", hoEmpty: "Ireki hasiera behin eta itzuli hona.", hoReset: "Berrezarri ordena lehenetsia", ctIcon: "Ikonoa", ctColour: "Azentua", ctNeedBook: "Audiobookshelf-ek ezin du bilduma huts bat gorde, aukeratu gutxienez liburu bat. Gero libre gehitu eta kendu ditzakezu.", colEditIcon: "Aldatu ikonoa", ciTitle: "Bildumaren ikonoa", ciReset: "Automatikoki izenarekin bat etorri", colEditDesc: "Editatu deskribapena", colBookForms: ["liburu","liburu"], colForms: ["Bilduma","Bildumak"], ctTitle: "Sortu bilduma", ctBlank: "Bilduma hutsa", ctNew: "Bilduma berria", ctNewHint: "txantiloi batetik", ctNamePh: "Bildumaren izena", ctSearch: "Bilatu liburuak edo egileak…", ctCreate: "Sortu", ctBack: "‹ Atzera", ctAddAll: "Gehitu liburu guztiak", abTitle: "Gehitu liburuak", abAdd: "Gehitu", avRemove: "Kendu argazkia", avSet: "Aukeratu argazkia…", ugListened: "Entzunda", ugPhoto: "Profileko argazkia", ugNever: "Inoiz ez", ugSeen: "Azken jarduera", ugEdit: "Editatu erabiltzailea", ugDelete: "Ezabatu erabiltzailea", ugAdd: "Gehitu argazkia", ugChange: "Aldatu argazkia", ugSortName: "A–Z", ugSortTime: "Gehien entzundakoak", ugSortSeen: "Duela gutxi aktibo", bsCard: "Liburuaren estekak", bsHint: "Liburu-orriko botoiak izenburua beste nonbait bilatzeko. Goodreads eta zure hizkuntzako gunerik handiena aktibatuta datoz.", bsFind: "Bilatu hemen:", bsGlobal: "Edonon", bsLocal: "Zure hizkuntzarako", bsOther: "Beste herrialde batzuk", pgCard: "Birdiseinatutako orriak", pgHint: "Aukera bakoitzak Audiobookshelf orri bat NanoHive bertsioarekin ordezkatzen du. Desaktibatu jatorrizkoa berreskuratzeko.", pgNarrators: "Narratzaileen txartelak", pgCollections: "Bildumen orriak", pgUsers: "Erabiltzaileen txartelak (ezarpenak)", pgStats: "Entzute-sailkapena", pgPhoto: "Profileko argazkia goiko barran", pgCinematic: "Zinema-atzealdea", pgTransitions: "Orri-trantsizioak", pSearch: "Bilatu ezarpenak…", pSearchNone: "Ez dago bat datorrenik.", erTypeface: "Letra-tipoa", erPage: "Orriaren gaia", erText: "Testuaren kolorea", erBg: "Atzealdea", erDefault: "Lehenetsia (ABS)", erAuto: "Auto", nrForms: ["Narratzailea","Narratzaileak"], nrBooksForms: ["liburu","liburu"], nrSearch: "Iragazi narratzaileak…", nrSortName: "Izena", nrSortBooks: "Liburu gehien", auForms: ["Egilea","Egileak"], auSearch: "Iragazi egileak…" , lfByAuthor: "Egilea › serieko ordena", lfBySeries: "Seriea › ordena" , lfSecFilters: "Iragazki gehiago", lfSecSort: "Ordenatze anizkoitza", lfAuthor: "Egilea", lfSeries: "Seriea", lfTitle: "Izenburua", lfYear: "Urtea", lfAdded: "Gehituta", lfDuration: "Iraupena", lfNarrator: "Narratzailea", lfGenre: "Generoa", lfLanguage: "Hizkuntza", lfProgress: "Aurrerapena", lfPgFinished: "Amaituak", lfPgInProgress: "Abian", lfPgNot: "Hasi gabeak" , logoUpload: 'Igo gailutik…', logoUploaded: 'Igota ✓', logoBadType: 'Irudi mota ez da onartzen', logoTooBig: 'Irudia handiegia da (geh. 4 MB)', logoOr: 'edo', bookPage: 'Liburuaren orria', ratingsToggle: 'Komunitatearen balorazioak (izarrak eta iruzkinak)', ratingsHint: 'Izar-balorazioak iruzkin laburrekin liburu-orri bakoitzean, zerbitzari honetako erabiltzaile guztiekin partekatuta.', searchCard: 'Bilaketa', gsToggle: 'Bilatu liburutegi guztietan batera', gsHint: 'Emaitza-zerrenda bateratu bakarra eskura dituzun liburutegi guztietan, liburutegi-bereizgarri batekin emaitza bakoitzean. Itzali liburutegi bakoitzeko menua berreskuratzeko.', gsNoResults: 'Emaitzarik ez', gsSearching: 'Bilatzen…', gsBooks: 'Liburuak', gsSeries: 'Sailak', gsAuthors: 'Egileak', spTitle: 'Hasierako orria', spHint: 'Ireki aplikazioa beti aukeratutako liburutegi-ikuspegian (nabigatzaile hau bakarrik). Esteka zuzenak ez dira inoiz birbideratzen.', spDefault: 'Aplikazioaren lehenetsia', spViewHome: 'Hasiera', spViewLibrary: 'Liburutegia', spViewSeries: 'Sailak', spViewCollections: 'Bildumak', spViewAuthors: 'Egileak', scSet: 'Ezarri sailaren azala', scRemove: 'Kendu sailaren azala', scEditDesc: 'Editatu sailaren deskribapena', cardStars: 'Izarrak liburu-azaletan', hoverPolish: 'Txartelen fintzea gainetik pasatzean (ilunago leunagoa, botoi nabarmendua, izarrak gainetik pasatzean)', sectUser: 'Zure ezarpenak', sectAdmin: 'Administrazioa', lockTitle: 'Funtzio-etengailuak', lockHint: 'Funtzio bat derrigor desgaitzen du zerbitzari honetako erabiltzaile guztientzat. Ezarpen pertsonalen gainetik dago; besteek hurrengo kargatzean ikusiko dute.', lockRatings: 'Desgaitu balorazioak edonon', lockCardRatings: 'Desgaitu izarrak azaletan', lockGlobalSearch: 'Desgaitu bilaketa globala', lockHoverPolish: 'Desgaitu txartelen fintzea', lockHeroCarousel: 'Desgaitu hasierako karrusela', lockedNote: 'administratzaileak desgaitua', lfSort: 'Balorazioa', lfRated: 'Baloratuak', lfUnrated: 'Baloratu gabeak', lfClear: 'Garbitu', lfNone: 'Ez dago bat datorren libururik', lfSection: 'Balorazioa', lfWho: 'Noren balorazioak', lfEveryone: 'Denak', sdSave: 'Gorde', sdClear: 'Berrezarri liburuaren deskribapena', sdMore: 'Irakurri gehiago', sdLess: 'Erakutsi gutxiago', shSeries: 'Saila', shBy: '', shMore: ' eta gehiago', shBookForms: ['liburu', 'liburu'], shAvg: 'liburuen batezbestekoa', shRated: 'baloratuta', impLabel: 'Inportatu balorazioak', impHint: 'Ekarri zure balorazioak StoryGraph edo Goodreads-etik. Esportatu han zure liburuak CSV gisa eta aukeratu fitxategia hemen. Bat-etortze bakoitza baieztatzeko erakusten da eta ez da ezer gordetzen zuk baieztatu arte.', impBtn: 'Aukeratu CSV fitxategia…', impReading: 'Fitxategia irakurtzen…', impMatching: 'Zure liburutegiekin alderatzen…', impErrUnknown: 'Honek ez du StoryGraph edo Goodreads esportazio baten itxurarik. Haien CSVa izan behar du, "Star Rating" edo "My Rating" zutabearekin.', impErrEmpty: 'Fitxategiak ez du errenkadarik.', impErrNoRated: 'Fitxategian ez dago baloratutako libururik.', impErrRead: 'Ezin izan da fitxategia irakurri.', impErrLoad: 'Ezin izan dira zure liburutegiak kargatu. Saiatu berriro laster.', impErrNoLibs: 'Liburutegi batek ere ez du balorazioetan parte hartzen, beraz ez dago nora inportatu.', impTitle: 'Inportatu balorazioak', impFrom: '%s esportazioa: %d liburu, horietatik %d baloratuta', impLibs: 'Bilatzen: %s', impMatched: 'Bat etorriak', impCheck: 'Begiratzea merezi du', impCheckHint: 'Gertu baina ziurtasunik gabe. Egiaztatu bat-etortzea edo aukeratu beste bat.', impAlready: 'Dagoeneko zuk baloratuak', impAlreadyHint: 'Bere horretan uzten dira, gainidaztea eskatzen ez baduzu.', impNone: 'Ez daude zure liburutegian', impNoneHint: 'Zerbitzari honetan ezerk ez du hauekin bat egiten.', impAll: 'Denak', impSkip: 'Saltatu hau', impDup: 'bikoiztua', impByTitle: 'izenburua', impYours: 'zurea: %s', impReviews: 'Inportatu idatzitako iruzkinak ere (%d)', impOver: 'Gainidatzi dagoeneko ditudan balorazioak', impGo: 'Inportatu %d balorazio', impCancel: 'Utzi', impCloseBtn: 'Itxi', impWorking: 'Gordetzen… %d / %d', impDone: '%d balorazio inportatuta.', impFail: '%d ezin izan dira gorde.', rdTitle: 'Nork irakurtzen du liburu hau', rdFin: 'Amaituta', socTitle: 'Funtzio sozialak', socHint: 'Zerbitzari honek erabiltzaileen artean zer partekatzen duen. Entzute-denborak sailkapena elikatzen du; beste biek nork zer irakurtzen duen agerian uzten dute, beraz itzalita daude zuk piztu arte. Denei batera aplikatzen zaie.', socTime: 'Entzute-denbora (zerbitzariaren sailkapena)', socContent: 'Izenburu nagusiak estatistika partekatuetan', socWho: 'Nor ari da irakurtzen (liburu-orriak)', socUsers: 'Erabiltzaileko', socUsersHint: 'Norbait itzaltzen baduzu, bere ezer ez da partekatzen, berak edozer aukeratuta ere; zuk bakarrik itzul dezakezu. "Berak utzi zuen" esan nahi du pertsonak berak ezetz esan zuela.', socOpted: 'berak utzi zuen', srvTitle: 'Zerbitzariaren lehenetsiak', srvHint: 'Gorde zure uneko ezarpenak zerbitzari honetako erabiltzaile guztien lehenetsi gisa. Bakoitzak gainean pertsonaliza dezake. Muntatu bolumen bat /data/nh-n gaiaren edukiontzian eguneratzeen artean gordetzeko.', srvSave: 'Gorde zerbitzariaren lehenetsi gisa', srvClear: 'Garbitu zerbitzariaren lehenetsiak', srvSaved: 'Gordeta', srvCleared: 'Garbituta', srvErr: 'Huts egin du: administratzaile-saioa behar da', crMode: 'Jarraitu irakurtzen apala', crCombine: 'Bateratu karruselarekin', crSeparate: 'Mantendu apal bereizi gisa', crHide: 'Ezkutatuta', fsHint: 'Partekatu zure entzutea zerbitzari honetako besteekin: denbora-guztizkoak eta eguneko minutuak sailkapenerako, eta administratzaileak piztu baditu, baita zure izenburu nagusiak eta zein liburu irakurtzen edo amaitu dituzun ere. Lehenespenez piztuta. Itzaltzean, partekatutako guztia ezabatzen da.' , amfHide: 'Ezkutatu Markatu amaitutzat' , amfConfirm: 'Liburu hau amaitutzat markatu?' , fsHint: 'Partekatu zure entzutearen laburpena (guztizkoak eta eguneko minutuak) zerbitzari honetako gainerakoekin, sailkapena elikatzen du. Lehenespenez aktibatuta. Desaktibatzean sailkapenetik eta bere guztizkoetatik desagertzen zara, eta partekatutakoa ezabatzen da.' , srToggle: 'Partekatu zein liburu irakurtzen dituzun', srHint: 'Besteek ikusten dutenari gehitzen dio zein liburu irakurtzen ari zaren, zeintzuk amaitu dituzun eta zeintzuk entzuten dituzun gehien. Lehenespenez itzalita; administratzailearen zerbitzari-etengailuek ere piztuta egon behar dute.' , fsHint: 'Partekatu zure entzute-denbora (guztizkoak eta eguneko minutuak) zerbitzari honetako besteekin; horrek elikatzen du sailkapena. Lehenespenez piztuta. Itzaltzen baduzu, beste erabiltzaileek ez zaituzte gehiago ikusten; zerbitzariko administratzaileek jarduera ikusten jarraitzen dute, uko-marka batekin.', srHint: 'Besteei ere erakusten die zein liburu irakurtzen ari zaren, zeintzuk amaitu dituzun eta zeintzuk entzuten dituzun gehien. Ezer ez da agertzen administratzaileak dagozkion zerbitzari-etengailuak piztu arte.', seshTitle: 'Azken saioak', seshNow: 'Orain entzuten' , hcHint: 'Bidali zure balorazioak Hardcover kontura. Itsatsi API gakoa Hardcover kontuaren ezarpenetatik; gakoak urtebetera iraungitzen dira.', hcKeyLbl: 'API gakoa', hcToggle: 'Sinkronizatu balorazio berriak automatikoki', hcPushAll: 'Bidali balorazio guztiak', hcPushDiff: 'Bidali desberdintasunak bakarrik', hcDone: 'Eginda: %d sinkronizatuta, %d parekatu gabe', hcNoKey: 'Lehenik itsatsi zure Hardcover API gakoa' , hcMissHead: 'Ez da aurkitu Hardcover-en:' , hcHint: 'Bidali zure balorazioak Hardcover kontura. Itsatsi API gakoa Hardcover kontuaren ezarpenetatik.' , tabStyle: 'Estiloa', tabMain: 'Orri nagusia', tabBooks: 'Liburuak', tabFeat: 'Funtzioak' , hcImport: 'Inportatu Hardcover-etik', impMarkQ: 'Bat datozen %d liburuak hemen ere amaitutzat markatu?', impMarkDone: '%d amaitutzat markatuta.' , impHint: 'Ekarri zure balorazioak Goodreads, StoryGraph edo Hardcover-etik. Aukeratu hemen esportatutako CSVa; Hardcover zuzenean inportatzen da bere txarteleko botoiarekin. Bat-etortze bakoitza berresteko erakusten da ezer gorde aurretik.' , impErrUnknown: 'Honek ez du Goodreads, StoryGraph edo Hardcover esportazio baten itxurarik: ez dira aurkitu izenburu eta balorazio zutabeak.' , fsCard: 'Soziala', rnToggle: 'Erakutsi nire izena balorazioekin', rsToggle: 'Partekatu nire balorazioak besteekin', socNames: 'Izenak balorazioekin', rtAnon: 'Anonimoa' },
    is: { cmStopped: "stöðvað", cmScanErrs: "{e} ekki athugað (Goodreads-villur)", cmScanBusy: "Leit er þegar í gangi", cmGrErr: "Goodreads næst ekki, reyni aftur", cmNoMatchYet: "Engin samsvörun á Goodreads", cmLooking: "Leita á Goodreads…", cmToolHint: "Hvað Goodreads-lesendum finnst um þessa bók. ? sýnir tengdu bókina og leyfir að breyta henni.", cmSearchPh: "Leita á Goodreads…", cmNoVotes: "engar einkunnir enn", cmRefresh: "Endurnýja", cmScanAll: "Fara yfir allt aftur, ekki bara það sem vantar", cmScanDone: "Lokið: {m} fundust · {x} fundust ekki · {s} höfðu þegar", cmStop: "Stöðva", cmScanLibs: "Söfn til að leita í", cmScan: "Leita að einkunnum", lsLongest: "Lengstu bækurnar", lsSize: "Stærð á diski", lsDuration: "Heildarlengd", lsGenresN: "Bókmenntagreinar", lsSeriesN: "Raðir", lsAuthorsN: "Höfundar", lsBooks: "Bækur", lsGlance: "Safnið í hnotskurn", cmOpen: "Opna á Goodreads", cmDetect: "Finna", cmAddr: "Vistfang hjálpara", scmTitle: "Síða bókaraðar", scmHint: "Svona lítur mynduð kápa fyrir ofan titil bókaraðar út þegar engin eigin hefur verið hlaðið upp (aðeins þessi vafri).", scmDeck: "Lagskiptur bunki", cmHint: "Hvað Goodreads-lesendum finnst um bók: meðaleinkunn og fjöldi atkvæða, undir stjörnunum á bókarsíðunni og sem röðun og sía. Bókarsíður fyllast sjálfar við skoðun.", cmGrOff: "Goodreads: ekki uppsett", cmSetup: "Enginn hjálpari enn? Keyrðu þetta við hlið þemans (compose línur hér að neðan) og ýttu á Finna. Hvaða önnur leið til að keyra myndina virkar líka: límdu vistfangið hér að ofan.", cmGrOn: "Goodreads: tengt", cmGrDown: "Goodreads: uppsett en svarar ekki", cmGrNoNum: "Goodreads: svarar, en þessi abs-tract útgáfa gefur engar einkunnir (notið ratings útgáfuna, sjá README)", cmTitle: "Goodreads-einkunnir", cmWrong: "Röng bók?", cmFind: "Finna einkunn samfélagsins…", cmNone: "Engin samsvörun", cmUse: "Nota þessa", lfCommunity: "Goodreads-einkunn", mfNatChange: "Breyta…", lsByTime: "eftir hlustunartíma", sbTitle: "Röðun netþjóns", sbWeek: "Vika", sbMonth: "Mánuður", sbYear: "Ár", sbAll: "Allur tíminn", sbBooks: "Bækur", sbTopBooks: "Mest hlustað", sbTotal: "Samtals", sbListeners: "Hlustendur", sbAvg: "Meðaltal", wkTitle: "Hlustunarmínútur, síðustu 7 dagar", wkAvg: "Dagsmeðaltal", wkRow: "Dagar í röð", rpBadge: "Opnar vandamálatilkynningar", rpMenu: "Tilkynna vandamál", rpTitle: "Tilkynna vandamál", rpWhat: "Hvað er að?", rpNote: "Eitthvað fleira fyrir stjórnandann? (valfrjálst)", rpSend: "Senda tilkynningu", rpSent: "Sent. Takk.", rpFail: "Tókst ekki að senda", rpMissing: "Vantar eða ófullkomið efni", rpQuality: "Léleg hljóðgæði", rpPlay: "Spilast ekki", rpWrong: "Röng bók, kápa eða lýsigögn", rpChapters: "Rangir kaflar", rpOther: "Eitthvað annað", rfRate: "Gefa einkunn", rfSheet: "Gefðu þessari bók einkunn", rfOpen: "Opna bókarsíðu", rfPickHint: "Veldu einkunn", rfTitle: "Gefðu því sem þú kláraðir einkunn", ysFinished: "Nýlega kláraðar", ysAlmost: "Næstum búnar", ysMarkDone: "Merkja sem kláraða", ysEditDate: "Breyta lokadagsetningu", ysThisWeek: "Í þessari viku", ysBestDay: "Besti dagurinn", ysTitle: "Hlustunin þín", fsTitle: "Röðun netþjóns", fsCard: "Röðun netþjóns", fsToggle: "Deila hlustunartölfræðinni minni", fsJoin: "Kveiktu á „Deila hlustunartölfræðinni minni“ í sérstillingaspjaldinu til að birtast í röðun netþjónsins.", fsWaiting: "Deiling er á, samantektin þín bætist bráðum í röðun netþjónsins ásamt öllum hinum.", fsHint: "Deildu samantekt á hlustuninni þinni (heildartölur og mínútur á dag) með öðrum á þessum netþjóni, hún knýr röðunina. Sjálfgefið á. Ef þú slekkur hverfurðu úr röðuninni og tölum hennar og öllu deildu er eytt.", yirBtn: "Árið í hnotskurn", yirTitle: "Hlustunarárið þitt", yirYear: "Síðustu 12 mánuðir", yirDays: "Dagar með hlustun", yirBest: "Besti dagurinn", yirMonths: "Eftir mánuðum", yirNone: "Engin hlustunargögn ennþá.", ssscmDeck: "Lagskiptur bunki", scmFirst: "Kápa fyrstu bókar", scmGrid: "Kápunet (sjálfgefið)", scmOff: "Falin" , lsTitle: "Tölfræði netþjóns", lsAll: "Öll bókasöfn", lsPlayed: "Spilaðar bækur", lsBest: "Best metnar", lsGenres: "Efstu flokkar", srvPick: "Hvað á að vista:", srvGRest: "Eiginleikar og hegðun", aeBtn: "Taka til í höfundum", aeTitle: "Höfundar án bóka", aeNone: "Allir höfundar hér eiga að minnsta kosti eina bók.", aeRemove: "Fjarlægja", aeDone: "Fjarlægt", aeWorking: "Fjarlægi…", rpCard: "Tilkynnt vandamál", rpHint: "Vandamál sem notendur tilkynntu af bókarsíðu. Að leysa eitt fjarlægir það af listanum.", rpEmpty: "Ekkert tilkynnt.", rpResolve: "Leysa", rpOpen: "Opna bók", apCard: "Spilun", apToggle: "Spila sjálfkrafa næstu bók í bókaröð", apHint: "Þegar bók klárast byrjar sú næsta í sömu röð sjálfkrafa. Aðeins bækur sem þú átt nú þegar og hefur ekki klárað.", apNext: "Næsta í röðinni", fdToggle: "Verkfæri kláraðra bóka í tölfræðinni", ysNoDate: "engin dagsetning", hmShowing: "grafið sýnir", ysStreak: "Runa", ysCurrent: "Núverandi", ysLongest: "Lengsta", ysDaysTotal: "Dagar alls", ysPace: "Taktur", ysPerDay: "Á hlustunardag", ysWhen: "Hvenær þú hlustar", ysTopBooks: "Mest hlustuðu bækurnar", ysTopAuthors: "Mest hlustuðu höfundarnir", ysTopNarrators: "Mest hlustuðu lesararnir", rfToggle: "„Gefa einkunn“ hluti á forsíðu", hoTitle: "Röð forsíðuhluta", hoEmpty: "Opnaðu forsíðuna einu sinni og komdu svo aftur.", hoReset: "Endurstilla sjálfgefna röð", ctIcon: "Táknmynd", ctColour: "Áherslulitur", ctNeedBook: "Audiobookshelf getur ekki vistað tómt safn, veldu að minnsta kosti eina bók. Seinna geturðu bætt við og fjarlægt að vild.", colEditIcon: "Skipta um táknmynd", ciTitle: "Táknmynd safnsins", ciReset: "Passa sjálfkrafa við nafnið", colEditDesc: "Breyta lýsingu", colBookForms: ["bók","bækur"], colForms: ["Safn","Söfn"], ctTitle: "Búa til safn", ctBlank: "Tómt safn", ctNew: "Nýtt safn", ctNewHint: "úr sniðmáti", ctNamePh: "Nafn safnsins", ctSearch: "Leita að bókum eða höfundum…", ctCreate: "Búa til", ctBack: "‹ Til baka", ctAddAll: "Bæta öllum bókum við", abTitle: "Bæta við bókum", abAdd: "Bæta við", avRemove: "Fjarlægja mynd", avSet: "Velja mynd…", ugListened: "Hlustað", ugPhoto: "Prófílmynd", ugNever: "Aldrei", ugSeen: "Síðast virkur", ugEdit: "Breyta notanda", ugDelete: "Eyða notanda", ugAdd: "Bæta við mynd", ugChange: "Skipta um mynd", ugSortName: "A–Ö", ugSortTime: "Mest hlustað", ugSortSeen: "Nýlega virkir", bsCard: "Bókartenglar", bsHint: "Hnappar á bókarsíðunni til að fletta titlinum upp annars staðar. Goodreads og stærsta síða tungumálsins þíns eru virk frá byrjun.", bsFind: "Leita á", bsGlobal: "Alls staðar", bsLocal: "Fyrir þitt tungumál", bsOther: "Önnur lönd", pgCard: "Endurhannaðar síður", pgHint: "Hver valkostur skiptir Audiobookshelf-síðu út fyrir NanoHive-útgáfuna. Slökktu til að fá upprunalegu síðuna aftur.", pgNarrators: "Lesaraspjöld", pgCollections: "Safnasíður", pgUsers: "Notendaspjöld (stillingar)", pgStats: "Hlustunarröðun", pgPhoto: "Prófílmynd í efstu stiku", pgCinematic: "Kvikmyndalegur bakgrunnur", pgTransitions: "Síðuskipti", pSearch: "Leita í stillingum…", pSearchNone: "Ekkert passar.", erTypeface: "Leturgerð", erPage: "Síðuþema", erText: "Litur texta", erBg: "Bakgrunnur", erDefault: "Sjálfgefið (ABS)", erAuto: "Sjálfvirkt", nrForms: ["Lesari","Lesarar"], nrBooksForms: ["bók","bækur"], nrSearch: "Sía lesara…", nrSortName: "Nafn", nrSortBooks: "Flestar bækur", auForms: ["Höfundur","Höfundar"], auSearch: "Sía höfunda…" , lfByAuthor: "Höfundur › röð bókaraðar", lfBySeries: "Bókaröð › röð" , lfSecFilters: "Fleiri síur", lfSecSort: "Fjölþrepa röðun", lfAuthor: "Höfundur", lfSeries: "Bókaröð", lfTitle: "Titill", lfYear: "Ár", lfAdded: "Bætt við", lfDuration: "Lengd", lfNarrator: "Lesari", lfGenre: "Tegund", lfLanguage: "Tungumál", lfProgress: "Framvinda", lfPgFinished: "Kláraðar", lfPgInProgress: "Í gangi", lfPgNot: "Óbyrjaðar" , logoUpload: 'Hlaða upp úr tæki…', logoUploaded: 'Hlaðið upp ✓', logoBadType: 'Óstudd myndgerð', logoTooBig: 'Myndin er of stór (hám. 4 MB)', logoOr: 'eða', bookPage: 'Bókarsíða', ratingsToggle: 'Einkunnir samfélagsins (stjörnur og umsagnir)', ratingsHint: 'Stjörnueinkunnir með stuttum umsögnum á hverri bókarsíðu, deilt með öllum notendum þessa þjóns.', searchCard: 'Leit', gsToggle: 'Leita í öllum söfnum í einu', gsHint: 'Einn sameinaður niðurstöðulisti yfir öll aðgengileg söfn, með safnmerki á hverri niðurstöðu. Slökktu til að fá aftur valmynd fyrir hvert safn.', gsNoResults: 'Engar niðurstöður', gsSearching: 'Leita…', gsBooks: 'Bækur', gsSeries: 'Seríur', gsAuthors: 'Höfundar', spTitle: 'Upphafssíða', spHint: 'Opna forritið alltaf á völdu safnyfirliti (aðeins þessi vafri). Beinum tenglum er aldrei vísað annað.', spDefault: 'Sjálfgefið forrits', spViewHome: 'Heim', spViewLibrary: 'Safn', spViewSeries: 'Seríur', spViewCollections: 'Söfn', spViewAuthors: 'Höfundar', scSet: 'Velja seríukápu', scRemove: 'Fjarlægja seríukápu', scEditDesc: 'Breyta seríulýsingu', cardStars: 'Stjörnur á bókarkápum', hoverPolish: 'Fínpússun korta við yfirferð (mýkri deyfing, áherslutakki, stjörnur við yfirferð)', sectUser: 'Þínar stillingar', sectAdmin: 'Stjórnun', lockTitle: 'Eiginleikarofar', lockHint: 'Slekkur á eiginleika fyrir alla notendur þessa þjóns. Vinnur yfir persónulegar stillingar; aðrir sjá það eftir næstu endurhleðslu.', lockRatings: 'Slökkva á einkunnum alls staðar', lockCardRatings: 'Slökkva á stjörnum á kápum', lockGlobalSearch: 'Slökkva á víðtækri leit', lockHoverPolish: 'Slökkva á fínpússun korta', lockHeroCarousel: 'Slökkva á hringekju forsíðu', lockedNote: 'slökkt af stjórnanda', lfSort: 'Einkunn', lfRated: 'Með einkunn', lfUnrated: 'Án einkunnar', lfClear: 'Hreinsa', lfNone: 'Engin bók passar', lfSection: 'Einkunn', lfWho: 'Hverra einkunnir', lfEveryone: 'Allir', sdSave: 'Vista', sdClear: 'Endurheimta bókarlýsingu', sdMore: 'Lesa meira', sdLess: 'Sýna minna', shSeries: 'Sería', shBy: 'eftir ', shMore: ' og fleiri', shBookForms: ['bók', 'bækur'], shAvg: 'meðaltal bóka', shRated: 'með einkunn', impLabel: 'Flytja inn einkunnir', impHint: 'Sæktu einkunnirnar þínar frá StoryGraph eða Goodreads. Fluttu bækurnar þínar þar út sem CSV og veldu skrána hér. Hver samsvörun er sýnd til staðfestingar og ekkert er vistað fyrr en þú staðfestir.', impBtn: 'Velja CSV-skrá…', impReading: 'Les skrána…', impMatching: 'Ber saman við söfnin þín…', impErrUnknown: 'Þetta lítur ekki út eins og útflutningur frá StoryGraph eða Goodreads. Það á að vera CSV-skráin þeirra, með dálki "Star Rating" eða "My Rating".', impErrEmpty: 'Skráin hefur engar línur.', impErrNoRated: 'Engar bækur með einkunn í skránni.', impErrRead: 'Ekki tókst að lesa skrána.', impErrLoad: 'Ekki tókst að hlaða söfnunum þínum. Reyndu aftur eftir smástund.', impErrNoLibs: 'Ekkert safn tekur þátt í einkunnum, svo það er hvergi að flytja inn.', impTitle: 'Flytja inn einkunnir', impFrom: '%s-útflutningur: %d bækur, þar af %d með einkunn', impLibs: 'Leita í: %s', impMatched: 'Samsvaraði', impCheck: 'Vert að skoða', impCheckHint: 'Nálægt en ekki víst. Athugaðu samsvörunina eða veldu aðra.', impAlready: 'Þegar með einkunn frá þér', impAlreadyHint: 'Látnar óhreyfðar nema þú veljir yfirskrift.', impNone: 'Ekki í safninu þínu', impNoneHint: 'Ekkert á þessum þjóni passar við þetta.', impAll: 'Allar', impSkip: 'Sleppa þessari', impDup: 'tvítak', impByTitle: 'titill', impYours: 'þín: %s', impReviews: 'Flytja líka inn skrifaðar umsagnir (%d)', impOver: 'Yfirskrifa einkunnir sem ég á nú þegar', impGo: 'Flytja inn %d einkunnir', impCancel: 'Hætta við', impCloseBtn: 'Loka', impWorking: 'Vista… %d af %d', impDone: '%d einkunnir fluttar inn.', impFail: '%d tókst ekki að vista.', rdTitle: 'Hver er að lesa þessa bók', rdFin: 'Lokið', socTitle: 'Félagslegir eiginleikar', socHint: 'Það sem þessi þjónn deilir milli notenda. Hlustunartími knýr stigalistann; hinir tveir sýna hvað hver les, svo þeir eru slökktir þar til þú kveikir á þeim. Gildir fyrir alla í einu.', socTime: 'Hlustunartími (stigalisti þjóns)', socContent: 'Efstu titlar í deildri tölfræði', socWho: 'Hver er að lesa (bókarsíður)', socUsers: 'Eftir notanda', socUsersHint: 'Ef þú slekkur á einhverjum er engu af hans deilt, sama hvað hann valdi sjálfur; aðeins þú getur kveikt aftur. "Sagði sig úr" þýðir að viðkomandi sagði sjálfur nei.', socOpted: 'sagði sig úr', srvTitle: 'Sjálfgefnar stillingar þjóns', srvHint: 'Vistaðu núverandi stillingar sem sjálfgefnar fyrir alla notendur þessa þjóns. Hver og einn getur samt sérsniðið ofan á. Tengdu gagnamagn við /data/nh í þemugámnum svo þær haldist milli uppfærslna.', srvSave: 'Vista sem sjálfgefið þjóns', srvClear: 'Hreinsa sjálfgefið þjóns', srvSaved: 'Vistað', srvCleared: 'Hreinsað', srvErr: 'Mistókst: þarf stjórnandainnskráningu', crMode: 'Halda áfram að lesa hilla', crCombine: 'Sameina við hringekjuna', crSeparate: 'Halda sem sérhillu', crHide: 'Falin', fsHint: 'Deildu hlustuninni þinni með öðrum á þessum þjóni: tímasamtölur og mínútur á dag fyrir stigalistann, og ef stjórnandinn hefur kveikt á því, líka efstu titlarnir þínir og hvaða bækur þú ert að lesa eða hefur klárað. Kveikt sjálfgefið. Ef þú slekkur er öllu deildu eytt.' , amfHide: 'Fela Merkja sem lokið' , amfConfirm: 'Merkja þessa bók sem lokið?' , fsHint: 'Deildu samantekt á hlustuninni þinni (heildartölur og mínútur á dag) með öðrum á þessum netþjóni, hún knýr röðunina. Sjálfgefið á. Ef þú slekkur hverfurðu úr röðuninni og tölum hennar og öllu deildu er eytt.' , srToggle: 'Deila hvaða bækur þú lest', srHint: 'Bætir við það sem aðrir sjá hvaða bækur þú ert að lesa, hverjum þú hefur lokið og hverjar þú hlustar mest á. Slökkt sjálfgefið; rofar stjórnandans á þjóninum þurfa líka að vera á.' , fsHint: 'Deildu hlustunartíma þínum (samtölum og mínútum á dag) með öðrum á þessum þjóni; hann knýr stigalistann. Kveikt sjálfgefið. Ef þú slekkur sjá aðrir notendur þig ekki lengur; stjórnendur þjónsins sjá virknina áfram, merkta sem úrsögn.', srHint: 'Leyfir öðrum líka að sjá hvaða bækur þú ert að lesa, hverjum þú hefur lokið og hverjar þú hlustar mest á. Ekkert birtist fyrr en stjórnandinn kveikir á viðeigandi rofum þjónsins.', seshTitle: 'Nýlegar lotur', seshNow: 'Hlusta núna' , hcHint: 'Sendu einkunnirnar þínar á Hardcover-reikninginn þinn. Límdu API-lykilinn úr stillingum Hardcover-reikningsins; lyklar renna út eftir eitt ár.', hcKeyLbl: 'API-lykill', hcToggle: 'Samstilla nýjar einkunnir sjálfkrafa', hcPushAll: 'Senda allar einkunnir', hcPushDiff: 'Senda aðeins mismun', hcDone: 'Búið: %d samstillt, %d án samsvörunar', hcNoKey: 'Límdu fyrst Hardcover-API-lykilinn þinn' , hcMissHead: 'Fannst ekki á Hardcover:' , hcHint: 'Sendu einkunnirnar þínar á Hardcover-reikninginn þinn. Límdu API-lykilinn úr stillingum Hardcover-reikningsins.' , tabStyle: 'Stíll', tabMain: 'Forsíða', tabBooks: 'Bækur', tabFeat: 'Eiginleikar' , hcImport: 'Flytja inn frá Hardcover', impMarkQ: 'Merkja líka %d samsvöruðu bækurnar sem kláraðar hér?', impMarkDone: '%d merktar sem kláraðar.' , impHint: 'Sæktu einkunnirnar þínar frá Goodreads, StoryGraph eða Hardcover. Veldu útfluttu CSV-skrána hér; Hardcover flyst inn beint með hnappinum á sínu korti. Hver samsvörun er sýnd til staðfestingar áður en nokkuð er vistað.' , impErrUnknown: 'Þetta lítur ekki út eins og útflutningur frá Goodreads, StoryGraph eða Hardcover: engir titil- og einkunnadálkar fundust.' , fsCard: 'Félagslegt', rnToggle: 'Sýna nafnið mitt með einkunnum', rsToggle: 'Deila einkunnum mínum með öðrum', socNames: 'Nöfn með einkunnum', rtAnon: 'Nafnlaus' },
    ja: { cmStopped: "停止しました", cmScanErrs: "{e} 件を確認できませんでした (Goodreads エラー)", cmScanBusy: "スキャンはすでに実行中です", cmGrErr: "Goodreads に接続できません。再試行します", cmNoMatchYet: "Goodreads に該当なし", cmLooking: "Goodreads で検索中…", cmToolHint: "Goodreads の読者がこの本をどう思っているか。? で一致した本を確認・変更できます。", cmSearchPh: "Goodreads で検索…", cmNoVotes: "まだ評価なし", cmRefresh: "更新", cmScanAll: "未取得分だけでなく、すべてを再取得する", cmScanDone: "完了: {m} 件一致 · {x} 件未発見 · {s} 件取得済み", cmStop: "停止", cmScanLibs: "対象ライブラリ", cmScan: "評価を取得", lsLongest: "最も長い本", lsSize: "ディスク使用量", lsDuration: "合計時間", lsGenresN: "ジャンル", lsSeriesN: "シリーズ", lsAuthorsN: "著者", lsBooks: "本", lsGlance: "ライブラリの概要", cmOpen: "Goodreads で開く", cmDetect: "探す", cmAddr: "ヘルパーのアドレス", scmTitle: "シリーズページ", scmHint: "カバー未アップロード時にシリーズ名の上に生成される画像の見た目（このブラウザのみ）。", scmDeck: "重ねたデッキ", cmHint: "Goodreads の読者がその本をどう思っているか。平均点と投票数を本のページの星の下に表示し、並べ替えと絞り込みにも使えます。本のページは閲覧中に自動で埋まります。", cmGrOff: "Goodreads: 未設定", cmSetup: "ヘルパーがまだない場合は、テーマの隣でこれを動かし（下の compose 行）、「探す」を押してください。他の方法でイメージを動かしても構いません。その場合は上にアドレスを貼り付けてください。", cmGrOn: "Goodreads: 接続済み", cmGrDown: "Goodreads: 設定済みですが応答がありません", cmGrNoNum: "Goodreads: 応答はありますが、この abs-tract には評価数値がありません（ratings 版を使用、README 参照）", cmTitle: "Goodreads の評価", cmWrong: "別の本？", cmFind: "コミュニティの評価を探す…", cmNone: "該当なし", cmUse: "これを使う", lfCommunity: "Goodreads 評価", mfNatChange: "変更…", lsByTime: "再生時間順", sbTitle: "サーバーランキング", sbWeek: "週", sbMonth: "月", sbYear: "年", sbAll: "全期間", sbBooks: "書籍", sbTopBooks: "よく聴いた本", sbTotal: "合計", sbListeners: "リスナー", sbAvg: "平均", wkTitle: "再生時間（分）- 過去7日間", wkAvg: "1日平均", wkRow: "連続日数", rpBadge: "未対応の問題報告", rpMenu: "問題を報告", rpTitle: "問題を報告", rpWhat: "何が問題ですか？", rpNote: "管理者に伝えたいことがあれば（任意）", rpSend: "報告を送信", rpSent: "送信しました。ありがとうございます。", rpFail: "送信できませんでした", rpMissing: "内容の欠落・不完全", rpQuality: "音質が悪い", rpPlay: "再生できない", rpWrong: "本・カバー・メタデータが違う", rpChapters: "チャプターが違う", rpOther: "その他", rfRate: "評価", rfSheet: "この本を評価", rfOpen: "本のページを開く", rfPickHint: "評価を選択", rfTitle: "聴き終えた本を評価", ysFinished: "最近読み終えた本", ysAlmost: "もうすぐ読了", ysMarkDone: "読了にする", ysEditDate: "読了日を変更", ysThisWeek: "今週", ysBestDay: "ベストな日", ysTitle: "あなたのリスニング", fsTitle: "サーバーランキング", fsCard: "サーバーランキング", fsToggle: "リスニング統計を共有する", fsJoin: "カスタマイズパネルで「リスニング統計を共有する」をオンにすると、サーバーランキングに表示されます。", fsWaiting: "共有はオンです, まもなくあなたのサマリーが皆と一緒にサーバーランキングに加わります。", fsHint: "リスニングのサマリー（合計と1日あたりの分数）をこのサーバーのメンバーと共有します, これがサーバーランキングの元になります。既定でオン。オフにするとランキングと集計から外れ、共有データは削除されます。", yirBtn: "1年の振り返り", yirTitle: "あなたのリスニングの1年", yirYear: "過去12か月", yirDays: "聴いた日数", yirBest: "ベストな日", yirMonths: "月別", yirNone: "リスニングデータはまだありません。", ssscmDeck: "重ねたデッキ", scmFirst: "1冊目のカバー", scmGrid: "カバーグリッド（既定）", scmOff: "非表示" , lsTitle: "サーバー統計", lsAll: "すべてのライブラリ", lsPlayed: "再生された本", lsBest: "高評価の本", lsGenres: "人気ジャンル", srvPick: "保存する内容:", srvGRest: "機能と動作", aeBtn: "著者を整理", aeTitle: "本のない著者", aeNone: "ここの著者は全員少なくとも1冊あります。", aeRemove: "削除", aeDone: "削除しました", aeWorking: "削除中…", rpCard: "報告された問題", rpHint: "ユーザーが本のページから報告した問題。解決するとリストから消えます。", rpEmpty: "報告はありません。", rpResolve: "解決", rpOpen: "本を開く", apCard: "再生", apToggle: "シリーズの次の本を自動再生", apHint: "本が終わると同じシリーズの次の本が自動的に始まります。ライブラリにある未読了の本のみが対象です。", apNext: "シリーズの次", fdToggle: "統計に読了ツールを表示", ysNoDate: "日付なし", hmShowing: "グラフの表示範囲", ysStreak: "連続記録", ysCurrent: "現在", ysLongest: "最長", ysDaysTotal: "合計日数", ysPace: "ペース", ysPerDay: "聴いた日あたり", ysWhen: "聴く曜日", ysTopBooks: "よく聴いた本", ysTopAuthors: "よく聴いた著者", ysTopNarrators: "よく聴いたナレーター", rfToggle: "ホームの「評価」セクション", hoTitle: "ホームのセクション順", hoEmpty: "一度ホームを開いてから戻ってください。", hoReset: "既定の順序に戻す", ctIcon: "アイコン", ctColour: "アクセント", ctNeedBook: "Audiobookshelfは空のコレクションを保存できません, 少なくとも1冊選んでください。後から自由に追加・削除できます。", colEditIcon: "アイコンを変更", ciTitle: "コレクションのアイコン", ciReset: "名前に自動で合わせる", colEditDesc: "説明を編集", colBookForms: ["冊","冊"], colForms: ["コレクション","コレクション"], ctTitle: "コレクションを作成", ctBlank: "空のコレクション", ctNew: "新しいコレクション", ctNewHint: "テンプレートから", ctNamePh: "コレクション名", ctSearch: "本や著者を検索…", ctCreate: "作成", ctBack: "‹ 戻る", ctAddAll: "すべての本を追加", abTitle: "本を追加", abAdd: "追加", avRemove: "写真を削除", avSet: "写真を選択…", ugListened: "再生時間", ugPhoto: "プロフィール写真", ugNever: "なし", ugSeen: "最終アクティブ", ugEdit: "ユーザーを編集", ugDelete: "ユーザーを削除", ugAdd: "写真を追加", ugChange: "写真を変更", ugSortName: "名前順", ugSortTime: "再生時間順", ugSortSeen: "最近アクティブ", bsCard: "ブックリンク", bsHint: "本のページから他サイトでタイトルを調べるボタン。Goodreadsとあなたの言語で最大のサイトが最初から有効です。", bsFind: "検索:", bsGlobal: "どこでも", bsLocal: "あなたの言語向け", bsOther: "他の国", pgCard: "再設計されたページ", pgHint: "それぞれAudiobookshelfのページをNanoHive版に置き換えます。オフにすると元のページに戻ります。", pgNarrators: "ナレーターカード", pgCollections: "コレクションページ", pgUsers: "ユーザーカード(設定)", pgStats: "リスニングランキング", pgPhoto: "トップバーのプロフィール写真", pgCinematic: "シネマ背景", pgTransitions: "ページ遷移", pSearch: "設定を検索…", pSearchNone: "一致なし。", erTypeface: "書体", erPage: "ページテーマ", erText: "文字色", erBg: "背景", erDefault: "既定 (ABS)", erAuto: "自動", nrForms: ["ナレーター","ナレーター"], nrBooksForms: ["冊","冊"], nrSearch: "ナレーターを絞り込む…", nrSortName: "名前", nrSortBooks: "冊数順", auForms: ["著者","著者"], auSearch: "著者を絞り込む…" , lfByAuthor: "著者 › シリーズ順", lfBySeries: "シリーズ › 順番" , lfSecFilters: "追加フィルター", lfSecSort: "マルチソート", lfAuthor: "著者", lfSeries: "シリーズ", lfTitle: "タイトル", lfYear: "年", lfAdded: "追加日", lfDuration: "再生時間", lfNarrator: "ナレーター", lfGenre: "ジャンル", lfLanguage: "言語", lfProgress: "進捗", lfPgFinished: "読了", lfPgInProgress: "再生中", lfPgNot: "未再生" , logoUpload: 'デバイスからアップロード…', logoUploaded: 'アップロード済み ✓', logoBadType: '対応していない画像形式です', logoTooBig: '画像が大きすぎます（最大4MB）', logoOr: 'または', bookPage: '書籍ページ', ratingsToggle: 'コミュニティ評価（星とレビュー）', ratingsHint: '各書籍ページに星評価と短いレビューを表示し、このサーバーの全ユーザーと共有します。', searchCard: '検索', gsToggle: 'すべてのライブラリを一度に検索', gsHint: 'アクセスできる全ライブラリを横断した統合結果リスト。各結果にライブラリバッジが付きます。オフにするとライブラリごとのメニューに戻ります。', gsNoResults: '結果なし', gsSearching: '検索中…', gsBooks: '書籍', gsSeries: 'シリーズ', gsAuthors: '著者', spTitle: '開始ページ', spHint: 'アプリを常に選択したライブラリ表示で開きます（このブラウザのみ）。直接リンクはリダイレクトされません。', spDefault: 'アプリの既定', spViewHome: 'ホーム', spViewLibrary: 'ライブラリ', spViewSeries: 'シリーズ', spViewCollections: 'コレクション', spViewAuthors: '著者', scSet: 'シリーズカバーを設定', scRemove: 'シリーズカバーを削除', scEditDesc: 'シリーズの説明を編集', cardStars: 'カバーに星バッジを表示', hoverPolish: 'カードホバーの仕上げ（やわらかい暗転、アクセント再生、ホバー時の星）', sectUser: 'あなたの設定', sectAdmin: '管理', lockTitle: '機能スイッチ', lockHint: 'このサーバーの全ユーザーに対して機能を強制的に無効化します。個人設定より優先され、他のユーザーには次回の再読み込みで反映されます。', lockRatings: '評価を全面的に無効化', lockCardRatings: 'カバーの星バッジを無効化', lockGlobalSearch: '全体検索を無効化', lockHoverPolish: 'カードホバー仕上げを無効化', lockHeroCarousel: 'ホームカルーセルを無効化', lockedNote: '管理者により無効', lfSort: '評価', lfRated: '評価あり', lfUnrated: '評価なし', lfClear: 'クリア', lfNone: '該当する本がありません', lfSection: '評価', lfWho: '誰の評価', lfEveryone: '全員', sdSave: '保存', sdClear: '書籍の説明を元に戻す', sdMore: '続きを読む', sdLess: '折りたたむ', shSeries: 'シリーズ', shBy: '著者: ', shMore: ' ほか', shBookForms: ['冊', '冊'], shAvg: '書籍平均', shRated: '評価済み', impLabel: '評価のインポート', impHint: 'StoryGraphやGoodreadsから評価を取り込めます。そちらで本をCSVにエクスポートし、ここでファイルを選択してください。一致候補はすべて確認画面に表示され、確定するまで何も保存されません。', impBtn: 'CSVファイルを選択…', impReading: 'ファイルを読み込み中…', impMatching: 'ライブラリと照合中…', impErrUnknown: 'StoryGraphまたはGoodreadsのエクスポートではないようです。"Star Rating"または"My Rating"列を含むCSVが必要です。', impErrEmpty: 'このファイルには行がありません。', impErrNoRated: 'このファイルに評価済みの本がありません。', impErrRead: 'ファイルを読み込めませんでした。', impErrLoad: 'ライブラリを読み込めませんでした。しばらくして再試行してください。', impErrNoLibs: '評価に参加しているライブラリがないため、インポート先がありません。', impTitle: '評価のインポート', impFrom: '%sのエクスポート: %d冊、うち%d冊が評価済み', impLibs: '検索対象: %s', impMatched: '一致', impCheck: '要確認', impCheckHint: '近いものの確実ではありません。一致を確認するか、別の候補を選んでください。', impAlready: 'すでに評価済み', impAlreadyHint: '上書きを選ばない限りそのままです。', impNone: 'ライブラリにありません', impNoneHint: 'このサーバーに一致するものはありません。', impAll: 'すべて', impSkip: 'これをスキップ', impDup: '重複', impByTitle: 'タイトル', impYours: 'あなた: %s', impReviews: '書いたレビューもインポート（%d）', impOver: '既存の評価を上書き', impGo: '%d件の評価をインポート', impCancel: 'キャンセル', impCloseBtn: '閉じる', impWorking: '保存中… %d / %d', impDone: '%d件の評価をインポートしました。', impFail: '%d件を保存できませんでした。', rdTitle: 'この本を読んでいる人', rdFin: '読了', socTitle: 'ソーシャル機能', socHint: 'このサーバーがユーザー間で共有するもの。聴取時間はランキングに使われます。他の2つは誰が何を読んでいるかを明かすため、オンにするまで無効のままです。全員に一括で適用されます。', socTime: '聴取時間（サーバーランキング）', socContent: '共有統計のトップタイトル', socWho: '読んでいる人（書籍ページ）', socUsers: 'ユーザーごと', socUsersHint: '誰かをオフにすると、本人の選択に関係なくその人のデータは一切共有されません。戻せるのはあなただけです。「本人が拒否」はその人自身がノーと言ったことを意味します。', socOpted: '本人が拒否', srvTitle: 'サーバー既定値', srvHint: '現在の設定をこのサーバーの全ユーザーの既定値として保存します。各ユーザーはその上で自分好みに調整できます。更新後も保持するには、テーマコンテナの/data/nhにボリュームをマウントしてください。', srvSave: 'サーバー既定値として保存', srvClear: 'サーバー既定値をクリア', srvSaved: '保存しました', srvCleared: 'クリアしました', srvErr: '失敗: 管理者ログインが必要です', crMode: '読書を続ける棚', crCombine: 'カルーセルに統合', crSeparate: '別の棚として維持', crHide: '非表示', fsHint: 'このサーバーの他のメンバーと聴取状況を共有します: ランキング用の合計時間と日別の分数、管理者が有効にした場合はトップタイトルと読書中・読了の本も含まれます。既定でオン。オフにすると共有した内容はすべて削除されます。' , amfHide: '「読了にする」を非表示' , amfConfirm: 'この本を読了にしますか？' , fsHint: 'リスニングのサマリー（合計と1日あたりの分数）をこのサーバーのメンバーと共有します, これがサーバーランキングの元になります。既定でオン。オフにするとランキングと集計から外れ、共有データは削除されます。' , srToggle: '読んでいる本を共有', srHint: '今読んでいる本、読了した本、よく聴く本を他のユーザーに見えるようにします。既定ではオフで、管理者のサーバースイッチもオンである必要があります。' , fsHint: '聴取時間（合計と日別の分数）をこのサーバーの他のメンバーと共有します。ランキングの元データです。既定でオン。オフにすると他のユーザーからは見えなくなりますが、サーバー管理者には引き続き表示され、オプトアウト済みと示されます。', srHint: 'さらに、今読んでいる本・読了した本・よく聴く本を他のユーザーに公開します。管理者が対応するサーバースイッチをオンにするまでは何も表示されません。', seshTitle: '最近のセッション', seshNow: '再生中' , hcHint: 'あなたの評価をHardcoverアカウントに送信します。Hardcoverアカウント設定のAPIキーを貼り付けてください。キーは1年で期限切れになります。', hcKeyLbl: 'APIキー', hcToggle: '新しい評価を自動で同期', hcPushAll: 'すべての評価を送信', hcPushDiff: '差分のみ送信', hcDone: '完了: %d件を同期、%d件は一致なし', hcNoKey: 'まずHardcoverのAPIキーを貼り付けてください' , hcMissHead: 'Hardcoverで見つかりませんでした:' , hcHint: 'あなたの評価をHardcoverアカウントに送信します。Hardcoverアカウント設定のAPIキーを貼り付けてください。' , tabStyle: 'スタイル', tabMain: 'メインページ', tabBooks: '本', tabFeat: '機能' , hcImport: 'Hardcoverからインポート', impMarkQ: '一致した%d冊をこちらでも読了にしますか?', impMarkDone: '%d冊を読了にしました。' , impHint: 'Goodreads、StoryGraph、Hardcoverから評価を取り込めます。エクスポートしたCSVをここで選択してください。HardcoverはそのカードのボタンからAPIで直接インポートします。保存前にすべての一致を確認できます。' , impErrUnknown: 'Goodreads、StoryGraph、Hardcoverのエクスポートではないようです。タイトルと評価の列が見つかりません。' , fsCard: 'ソーシャル', rnToggle: '評価に自分の名前を表示', rsToggle: '自分の評価を他のユーザーと共有', socNames: '評価に名前を表示', rtAnon: '匿名' },
    ko: { cmStopped: "중지됨", cmScanErrs: "{e}개 확인 불가 (Goodreads 오류)", cmScanBusy: "이미 검색이 실행 중입니다", cmGrErr: "Goodreads에 연결할 수 없음, 다시 시도", cmNoMatchYet: "Goodreads 일치 없음", cmLooking: "Goodreads 검색 중…", cmToolHint: "Goodreads 독자들이 이 책을 어떻게 생각하는지. ? 로 일치한 책을 확인하고 바꿀 수 있습니다.", cmSearchPh: "Goodreads 검색…", cmNoVotes: "아직 평점 없음", cmRefresh: "새로고침", cmScanAll: "누락분만이 아니라 전부 다시 검색", cmScanDone: "완료: {m}개 일치 · {x}개 없음 · {s}개 이미 있음", cmStop: "중지", cmScanLibs: "검색할 라이브러리", cmScan: "평점 검색", lsLongest: "가장 긴 책", lsSize: "디스크 용량", lsDuration: "총 길이", lsGenresN: "장르", lsSeriesN: "시리즈", lsAuthorsN: "저자", lsBooks: "책", lsGlance: "라이브러리 한눈에 보기", cmOpen: "Goodreads에서 열기", cmDetect: "찾기", cmAddr: "도우미 주소", scmTitle: "시리즈 페이지", scmHint: "직접 올린 표지가 없을 때 시리즈 제목 위에 생성되는 표지의 모양(이 브라우저에만 적용).", scmDeck: "겹친 덱", cmHint: "Goodreads 독자들이 이 책을 어떻게 생각하는지: 평균 점수와 투표 수를 책 페이지의 별 아래에 표시하고 정렬과 필터로도 씁니다. 책 페이지는 둘러보는 동안 저절로 채워집니다.", cmGrOff: "Goodreads: 설정 안 됨", cmSetup: "아직 도우미가 없나요? 테마 옆에서 이것을 실행하고(아래 compose 줄) 찾기를 누르세요. 이미지를 다른 방식으로 실행해도 됩니다. 그 주소를 위에 붙여넣으세요.", cmGrOn: "Goodreads: 연결됨", cmGrDown: "Goodreads: 설정됐지만 응답 없음", cmGrNoNum: "Goodreads: 응답하지만 이 abs-tract 빌드에는 평점 숫자가 없습니다 (ratings 빌드 사용, README 참고)", cmTitle: "Goodreads 평점", cmWrong: "다른 책인가요?", cmFind: "커뮤니티 평점 찾기…", cmNone: "일치 없음", cmUse: "이것 사용", lfCommunity: "Goodreads 평점", mfNatChange: "변경…", lsByTime: "청취 시간 기준", sbTitle: "서버 랭킹", sbWeek: "주", sbMonth: "월", sbYear: "년", sbAll: "전체 기간", sbBooks: "책", sbTopBooks: "가장 많이 들은 책", sbTotal: "합계", sbListeners: "청취자", sbAvg: "평균", wkTitle: "청취 시간(분), 최근 7일", wkAvg: "일일 평균", wkRow: "연속 일수", rpBadge: "미해결 문제 신고", rpMenu: "문제 신고", rpTitle: "문제 신고", rpWhat: "무엇이 잘못되었나요?", rpNote: "관리자에게 더 알릴 내용이 있나요? (선택)", rpSend: "신고 보내기", rpSent: "보냈습니다. 감사합니다.", rpFail: "보내지 못했습니다", rpMissing: "내용 누락 또는 불완전", rpQuality: "음질 불량", rpPlay: "재생되지 않음", rpWrong: "잘못된 책·표지·메타데이터", rpChapters: "챕터가 잘못됨", rpOther: "기타", rfRate: "평가", rfSheet: "이 책 평가하기", rfOpen: "책 페이지 열기", rfPickHint: "평점을 선택하세요", rfTitle: "다 들은 책 평가하기", ysFinished: "최근에 끝낸 책", ysAlmost: "거의 다 들음", ysMarkDone: "완료로 표시", ysEditDate: "완료 날짜 변경", ysThisWeek: "이번 주", ysBestDay: "최고의 날", ysTitle: "나의 청취", fsTitle: "서버 랭킹", fsCard: "서버 랭킹", fsToggle: "내 청취 통계 공유", fsJoin: "사용자 지정 패널에서 ‘내 청취 통계 공유’를 켜면 서버 랭킹에 표시됩니다.", fsWaiting: "공유가 켜져 있습니다, 곧 여러분의 요약이 다른 사람들과 함께 서버 랭킹에 추가됩니다.", fsHint: "청취 요약(합계와 일일 분)을 이 서버의 다른 사람들과 공유합니다, 서버 랭킹의 원천입니다. 기본적으로 켜져 있습니다. 끄면 랭킹과 합계에서 제외되고 공유된 데이터는 삭제됩니다.", yirBtn: "한 해 돌아보기", yirTitle: "나의 청취 연말결산", yirYear: "최근 12개월", yirDays: "청취한 날", yirBest: "최고의 날", yirMonths: "월별", yirNone: "아직 청취 데이터가 없습니다.", ssscmDeck: "겹친 덱", scmFirst: "첫 책의 표지", scmGrid: "표지 그리드(기본)", scmOff: "숨김" , lsTitle: "서버 통계", lsAll: "모든 라이브러리", lsPlayed: "재생된 책", lsBest: "평점 높은 책", lsGenres: "인기 장르", srvPick: "저장할 항목:", srvGRest: "기능 및 동작", aeBtn: "작가 정리", aeTitle: "책이 없는 작가", aeNone: "여기 있는 모든 작가는 최소 한 권의 책이 있습니다.", aeRemove: "삭제", aeDone: "삭제됨", aeWorking: "삭제 중…", rpCard: "신고된 문제", rpHint: "사용자가 책 페이지에서 신고한 문제입니다. 해결하면 목록에서 사라집니다.", rpEmpty: "신고 없음.", rpResolve: "해결", rpOpen: "책 열기", apCard: "재생", apToggle: "시리즈의 다음 책 자동 재생", apHint: "책이 끝나면 같은 시리즈의 다음 책이 자동으로 시작됩니다. 이미 보유 중이고 끝내지 않은 책만 해당합니다.", apNext: "시리즈의 다음", fdToggle: "통계에 완독 도구 표시", ysNoDate: "날짜 없음", hmShowing: "차트 표시 범위", ysStreak: "연속 기록", ysCurrent: "현재", ysLongest: "최장", ysDaysTotal: "총 일수", ysPace: "페이스", ysPerDay: "청취일당", ysWhen: "듣는 요일", ysTopBooks: "가장 많이 들은 책", ysTopAuthors: "가장 많이 들은 작가", ysTopNarrators: "가장 많이 들은 낭독자", rfToggle: "홈의 '평가' 섹션", hoTitle: "홈 섹션 순서", hoEmpty: "홈을 한 번 연 다음 돌아오세요.", hoReset: "기본 순서 복원", ctIcon: "아이콘", ctColour: "강조색", ctNeedBook: "Audiobookshelf는 빈 컬렉션을 저장할 수 없습니다, 최소 한 권을 선택하세요. 나중에 자유롭게 추가·삭제할 수 있습니다.", colEditIcon: "아이콘 변경", ciTitle: "컬렉션 아이콘", ciReset: "이름에 자동으로 맞추기", colEditDesc: "설명 편집", colBookForms: ["권","권"], colForms: ["컬렉션","컬렉션"], ctTitle: "컬렉션 만들기", ctBlank: "빈 컬렉션", ctNew: "새 컬렉션", ctNewHint: "템플릿에서", ctNamePh: "컬렉션 이름", ctSearch: "책이나 작가 검색…", ctCreate: "만들기", ctBack: "‹ 뒤로", ctAddAll: "모든 책 추가", abTitle: "책 추가", abAdd: "추가", avRemove: "사진 삭제", avSet: "사진 선택…", ugListened: "청취 시간", ugPhoto: "프로필 사진", ugNever: "없음", ugSeen: "마지막 활동", ugEdit: "사용자 편집", ugDelete: "사용자 삭제", ugAdd: "사진 추가", ugChange: "사진 변경", ugSortName: "이름순", ugSortTime: "청취 시간순", ugSortSeen: "최근 활동순", bsCard: "책 링크", bsHint: "책 페이지에서 제목을 다른 곳에서 찾아보는 버튼입니다. Goodreads와 해당 언어 최대 사이트가 기본으로 켜져 있습니다.", bsFind: "검색:", bsGlobal: "어디서나", bsLocal: "당신의 언어", bsOther: "다른 나라", pgCard: "재설계된 페이지", pgHint: "각 옵션은 Audiobookshelf 페이지를 NanoHive 버전으로 바꿉니다. 끄면 원래 페이지로 돌아갑니다.", pgNarrators: "낭독자 카드", pgCollections: "컬렉션 페이지", pgUsers: "사용자 카드(설정)", pgStats: "청취 랭킹", pgPhoto: "상단 바의 프로필 사진", pgCinematic: "시네마틱 배경", pgTransitions: "페이지 전환", pSearch: "설정 검색…", pSearchNone: "일치 항목 없음.", erTypeface: "글꼴", erPage: "페이지 테마", erText: "글자 색", erBg: "배경", erDefault: "기본값 (ABS)", erAuto: "자동", nrForms: ["낭독자","낭독자"], nrBooksForms: ["권","권"], nrSearch: "낭독자 필터…", nrSortName: "이름", nrSortBooks: "책 많은 순", auForms: ["작가","작가"], auSearch: "작가 필터…" , lfByAuthor: "작가 › 시리즈 순서", lfBySeries: "시리즈 › 순서" , lfSecFilters: "추가 필터", lfSecSort: "다중 정렬", lfAuthor: "작가", lfSeries: "시리즈", lfTitle: "제목", lfYear: "연도", lfAdded: "추가일", lfDuration: "길이", lfNarrator: "낭독자", lfGenre: "장르", lfLanguage: "언어", lfProgress: "진행률", lfPgFinished: "완독", lfPgInProgress: "듣는 중", lfPgNot: "시작 안 함" , logoUpload: '기기에서 업로드…', logoUploaded: '업로드됨 ✓', logoBadType: '지원하지 않는 이미지 형식', logoTooBig: '이미지가 너무 큽니다(최대 4MB)', logoOr: '또는', bookPage: '책 페이지', ratingsToggle: '커뮤니티 평점(별점과 리뷰)', ratingsHint: '각 책 페이지에 짧은 리뷰가 딸린 별점을 표시하며, 이 서버의 모든 사용자와 공유됩니다.', searchCard: '검색', gsToggle: '모든 라이브러리에서 한 번에 검색', gsHint: '접근 가능한 모든 라이브러리를 아우르는 하나의 통합 결과 목록으로, 각 결과에 라이브러리 배지가 붙습니다. 끄면 라이브러리별 메뉴로 돌아갑니다.', gsNoResults: '결과 없음', gsSearching: '검색 중…', gsBooks: '책', gsSeries: '시리즈', gsAuthors: '저자', spTitle: '시작 페이지', spHint: '앱을 항상 선택한 라이브러리 보기로 엽니다(이 브라우저만). 직접 링크는 절대 리디렉션되지 않습니다.', spDefault: '앱 기본값', spViewHome: '홈', spViewLibrary: '라이브러리', spViewSeries: '시리즈', spViewCollections: '컬렉션', spViewAuthors: '저자', scSet: '시리즈 표지 설정', scRemove: '시리즈 표지 제거', scEditDesc: '시리즈 설명 편집', cardStars: '표지에 별점 배지', hoverPolish: '카드 호버 다듬기(부드러운 어두워짐, 강조 재생 버튼, 호버 시 별점)', sectUser: '내 설정', sectAdmin: '관리', lockTitle: '기능 스위치', lockHint: '이 서버의 모든 사용자에 대해 기능을 강제로 끕니다. 개인 설정보다 우선하며, 다른 사용자는 다음 새로고침 후에 보게 됩니다.', lockRatings: '평점을 어디서나 비활성화', lockCardRatings: '표지 별점 비활성화', lockGlobalSearch: '전역 검색 비활성화', lockHoverPolish: '카드 호버 다듬기 비활성화', lockHeroCarousel: '홈 캐러셀 비활성화', lockedNote: '관리자가 비활성화함', lfSort: '평점', lfRated: '평점 있음', lfUnrated: '평점 없음', lfClear: '지우기', lfNone: '일치하는 책이 없습니다', lfSection: '평점', lfWho: '누구의 평점', lfEveryone: '전체', sdSave: '저장', sdClear: '책 설명 복원', sdMore: '더 보기', sdLess: '접기', shSeries: '시리즈', shBy: '저자: ', shMore: ' 외', shBookForms: ['권', '권'], shAvg: '책 평균', shRated: '평가됨', impLabel: '평점 가져오기', impHint: 'StoryGraph나 Goodreads에서 평점을 가져오세요. 거기서 책을 CSV로 내보낸 뒤 여기서 파일을 선택하면 됩니다. 모든 일치 항목은 확인용으로 표시되며 확정 전에는 아무것도 저장되지 않습니다.', impBtn: 'CSV 파일 선택…', impReading: '파일 읽는 중…', impMatching: '라이브러리와 대조 중…', impErrUnknown: 'StoryGraph나 Goodreads 내보내기 파일이 아닌 것 같습니다. "Star Rating" 또는 "My Rating" 열이 있는 CSV여야 합니다.', impErrEmpty: '파일에 행이 없습니다.', impErrNoRated: '파일에 평점이 있는 책이 없습니다.', impErrRead: '파일을 읽을 수 없습니다.', impErrLoad: '라이브러리를 불러오지 못했습니다. 잠시 후 다시 시도하세요.', impErrNoLibs: '평점에 참여하는 라이브러리가 없어 가져올 곳이 없습니다.', impTitle: '평점 가져오기', impFrom: '%s 내보내기: 책 %d권, 그중 %d권 평가됨', impLibs: '검색 대상: %s', impMatched: '일치함', impCheck: '확인 필요', impCheckHint: '비슷하지만 확실하지 않습니다. 일치를 확인하거나 다른 후보를 선택하세요.', impAlready: '이미 평가한 책', impAlreadyHint: '덮어쓰기를 선택하지 않는 한 그대로 둡니다.', impNone: '라이브러리에 없음', impNoneHint: '이 서버에서 일치하는 항목이 없습니다.', impAll: '전체', impSkip: '이 항목 건너뛰기', impDup: '중복', impByTitle: '제목', impYours: '내 평점: %s', impReviews: '작성한 리뷰도 가져오기(%d)', impOver: '이미 있는 내 평점 덮어쓰기', impGo: '평점 %d개 가져오기', impCancel: '취소', impCloseBtn: '닫기', impWorking: '저장 중… %d / %d', impDone: '평점 %d개를 가져왔습니다.', impFail: '%d개를 저장하지 못했습니다.', rdTitle: '이 책을 읽고 있는 사람', rdFin: '완독', socTitle: '소셜 기능', socHint: '이 서버가 사용자 간에 공유하는 것. 청취 시간은 순위표에 쓰입니다. 나머지 둘은 누가 무엇을 읽는지 드러내므로 켜기 전까지 꺼져 있습니다. 모두에게 한 번에 적용됩니다.', socTime: '청취 시간(서버 순위)', socContent: '공유 통계의 상위 제목', socWho: '읽고 있는 사람(책 페이지)', socUsers: '사용자별', socUsersHint: '누군가를 끄면 본인의 선택과 무관하게 그 사람의 어떤 것도 공유되지 않습니다. 되돌릴 수 있는 사람은 관리자뿐입니다. "본인이 거부"는 그 사람이 직접 아니라고 했다는 뜻입니다.', socOpted: '본인이 거부', srvTitle: '서버 기본값', srvHint: '현재 설정을 이 서버 모든 사용자의 기본값으로 저장합니다. 각자 그 위에 개인화할 수 있습니다. 업데이트 후에도 유지하려면 테마 컨테이너의 /data/nh에 볼륨을 마운트하세요.', srvSave: '서버 기본값으로 저장', srvClear: '서버 기본값 지우기', srvSaved: '저장됨', srvCleared: '지워짐', srvErr: '실패: 관리자 로그인이 필요합니다', crMode: '이어서 읽기 선반', crCombine: '캐러셀에 합치기', crSeparate: '별도 선반으로 유지', crHide: '숨김', fsHint: '이 서버의 다른 사람들과 청취 기록을 공유하세요: 순위표를 위한 총 시간과 일별 분 수, 관리자가 켠 경우 상위 제목과 읽는 중이거나 완독한 책도 포함됩니다. 기본적으로 켜져 있습니다. 끄면 공유한 모든 것이 삭제됩니다.' , amfHide: '완독으로 표시 숨기기' , amfConfirm: '이 책을 완독으로 표시할까요?' , fsHint: '청취 요약(합계와 일일 분)을 이 서버의 다른 사람들과 공유합니다, 서버 랭킹의 원천입니다. 기본적으로 켜져 있습니다. 끄면 랭킹과 합계에서 제외되고 공유된 데이터는 삭제됩니다.' , srToggle: '읽는 책 공유', srHint: '지금 읽는 책, 완독한 책, 가장 많이 듣는 책을 다른 사람이 볼 수 있게 합니다. 기본은 꺼짐이며 관리자의 서버 스위치도 켜져 있어야 합니다.' , fsHint: '청취 시간(총합과 일별 분 수)을 이 서버의 다른 사람들과 공유합니다. 순위표의 원천입니다. 기본은 켜짐. 끄면 다른 사용자는 더 이상 볼 수 없지만 서버 관리자는 계속 볼 수 있으며 거부로 표시됩니다.', srHint: '또한 지금 읽는 책, 완독한 책, 가장 많이 듣는 책을 다른 사람이 볼 수 있게 합니다. 관리자가 해당 서버 스위치를 켤 때까지는 아무것도 표시되지 않습니다.', seshTitle: '최근 세션', seshNow: '지금 듣는 중' , hcHint: '내 평점을 Hardcover 계정으로 보냅니다. Hardcover 계정 설정에서 API 키를 붙여넣으세요. 키는 1년 후 만료됩니다.', hcKeyLbl: 'API 키', hcToggle: '새 평점 자동 동기화', hcPushAll: '모든 평점 보내기', hcPushDiff: '차이만 보내기', hcDone: '완료: %d개 동기화, %d개 일치 없음', hcNoKey: '먼저 Hardcover API 키를 붙여넣으세요' , hcMissHead: 'Hardcover에서 찾을 수 없음:' , hcHint: '내 평점을 Hardcover 계정으로 보냅니다. Hardcover 계정 설정에서 API 키를 붙여넣으세요.' , tabStyle: '스타일', tabMain: '메인 페이지', tabBooks: '도서', tabFeat: '기능' , hcImport: 'Hardcover에서 가져오기', impMarkQ: '일치한 %d권을 여기서도 완독으로 표시할까요?', impMarkDone: '%d권을 완독으로 표시했습니다.' , impHint: 'Goodreads, StoryGraph, Hardcover에서 평점을 가져올 수 있습니다. 내보낸 CSV를 여기서 선택하세요. Hardcover는 해당 카드의 버튼으로 직접 가져옵니다. 저장 전에 모든 일치 항목을 확인합니다.' , impErrUnknown: 'Goodreads, StoryGraph, Hardcover 내보내기가 아닌 것 같습니다. 제목과 평점 열을 찾을 수 없습니다.' , fsCard: '소셜', rnToggle: '평점에 내 이름 표시', rsToggle: '내 평점을 다른 사용자와 공유', socNames: '평점에 이름 표시', rtAnon: '익명' },
    zh: { cmStopped: "已停止", cmScanErrs: "{e} 本未检查 (Goodreads 错误)", cmScanBusy: "已有扫描在运行", cmGrErr: "无法连接 Goodreads，稍后重试", cmNoMatchYet: "Goodreads 无匹配", cmLooking: "正在搜索 Goodreads…", cmToolHint: "Goodreads 读者对这本书的看法。点 ? 查看匹配的书并可更改。", cmSearchPh: "搜索 Goodreads…", cmNoVotes: "暂无评分", cmRefresh: "刷新", cmScanAll: "不只缺失的，全部重新扫描", cmScanDone: "完成：{m} 已匹配 · {x} 未找到 · {s} 已有", cmStop: "停止", cmScanLibs: "要扫描的书库", cmScan: "获取评分", lsLongest: "最长的书", lsSize: "磁盘占用", lsDuration: "总时长", lsGenresN: "类型", lsSeriesN: "系列", lsAuthorsN: "作者", lsBooks: "图书", lsGlance: "书库概览", cmOpen: "在 Goodreads 打开", cmDetect: "查找", cmAddr: "助手地址", scmTitle: "系列页面", scmHint: "未上传自定义封面时，系列标题上方生成封面的样式（仅此浏览器）。", scmDeck: "层叠卡组", cmHint: "Goodreads 读者对一本书的看法：平均分和评分人数，显示在图书页面的星星下方，并可用于排序和筛选。图书页面在浏览时会自动补全。", cmGrOff: "Goodreads：未设置", cmSetup: "还没有助手？在主题旁运行它（下面的 compose 行），然后点“查找”。用其他方式运行镜像也可以：把它的地址粘贴到上面。", cmGrOn: "Goodreads：已连接", cmGrDown: "Goodreads：已设置但无响应", cmGrNoNum: "Goodreads：有响应，但此 abs-tract 版本不提供评分数字（请使用 ratings 版本，见 README）", cmTitle: "Goodreads 评分", cmWrong: "不是这本书？", cmFind: "查找社区评分…", cmNone: "无匹配", cmUse: "使用此项", lfCommunity: "Goodreads 评分", mfNatChange: "更改…", lsByTime: "按收听时长", sbTitle: "服务器排行榜", sbWeek: "周", sbMonth: "月", sbYear: "年", sbAll: "全部时间", sbBooks: "书籍", sbTopBooks: "最常收听", sbTotal: "总计", sbListeners: "听众", sbAvg: "平均", wkTitle: "收听分钟数, 最近7天", wkAvg: "日均", wkRow: "连续天数", rpBadge: "未处理的问题报告", rpMenu: "报告问题", rpTitle: "报告问题", rpWhat: "哪里有问题？", rpNote: "还有什么要告诉管理员的吗？（可选）", rpSend: "发送报告", rpSent: "已发送，谢谢。", rpFail: "发送失败", rpMissing: "内容缺失或不完整", rpQuality: "音质差", rpPlay: "无法播放", rpWrong: "书籍、封面或元数据有误", rpChapters: "章节有误", rpOther: "其他", rfRate: "评分", rfSheet: "为这本书评分", rfOpen: "打开书籍页面", rfPickHint: "选择评分", rfTitle: "为听完的书评分", ysFinished: "最近听完", ysAlmost: "即将听完", ysMarkDone: "标记为已听完", ysEditDate: "修改完成日期", ysThisWeek: "本周", ysBestDay: "最佳一天", ysTitle: "你的收听", fsTitle: "服务器排行榜", fsCard: "服务器排行榜", fsToggle: "共享我的收听统计", fsJoin: "在个性化面板开启“共享我的收听统计”，即可出现在服务器排行榜中。", fsWaiting: "共享已开启, 你的摘要很快会和大家一起加入服务器排行榜。", fsHint: "与本服务器的其他人共享你的收听摘要（总量与每日分钟数）- 它是服务器排行榜的数据来源。默认开启。关闭后你将从排行榜及其统计中移除，共享的数据会被删除。", yirBtn: "年度回顾", yirTitle: "你的收听年度", yirYear: "最近12个月", yirDays: "有收听的天数", yirBest: "最佳一天", yirMonths: "按月", yirNone: "暂无收听数据。", ssscmDeck: "层叠卡组", scmFirst: "第一本书的封面", scmGrid: "封面网格（默认）", scmOff: "隐藏" , lsTitle: "服务器统计", lsAll: "所有书库", lsPlayed: "播放过的书", lsBest: "评分最高", lsGenres: "热门类型", srvPick: "保存内容:", srvGRest: "功能与行为", aeBtn: "整理作者", aeTitle: "没有书的作者", aeNone: "这里的每位作者都至少有一本书。", aeRemove: "移除", aeDone: "已移除", aeWorking: "移除中…", rpCard: "已报告的问题", rpHint: "用户从图书页面报告的问题。解决后将从列表中移除。", rpEmpty: "暂无报告。", rpResolve: "解决", rpOpen: "打开图书", apCard: "播放", apToggle: "自动播放系列中的下一本书", apHint: "一本书结束后自动开始同系列的下一本。仅限你已拥有且未听完的书。", apNext: "系列下一本", fdToggle: "在统计中显示已完成图书工具", ysNoDate: "无日期", hmShowing: "图表显示", ysStreak: "连续天数", ysCurrent: "当前", ysLongest: "最长", ysDaysTotal: "总天数", ysPace: "节奏", ysPerDay: "每个收听日", ysWhen: "你何时收听", ysTopBooks: "收听最多的书", ysTopAuthors: "收听最多的作者", ysTopNarrators: "收听最多的朗读者", rfToggle: "主页的“评分”栏目", hoTitle: "主页栏目顺序", hoEmpty: "先打开一次主页，然后回到这里。", hoReset: "恢复默认顺序", ctIcon: "图标", ctColour: "强调色", ctNeedBook: "Audiobookshelf 无法保存空收藏, 请至少选择一本书。之后可自由增删。", colEditIcon: "更换图标", ciTitle: "收藏图标", ciReset: "根据名称自动匹配", colEditDesc: "编辑简介", colBookForms: ["本","本"], colForms: ["收藏","收藏"], ctTitle: "创建收藏", ctBlank: "空收藏", ctNew: "新收藏", ctNewHint: "从模板", ctNamePh: "收藏名称", ctSearch: "搜索图书或作者…", ctCreate: "创建", ctBack: "‹ 返回", ctAddAll: "添加全部图书", abTitle: "添加图书", abAdd: "添加", avRemove: "移除照片", avSet: "选择照片…", ugListened: "已收听", ugPhoto: "头像", ugNever: "从未", ugSeen: "最近活跃", ugEdit: "编辑用户", ugDelete: "删除用户", ugAdd: "添加照片", ugChange: "更换照片", ugSortName: "按名称", ugSortTime: "按收听时长", ugSortSeen: "最近活跃", bsCard: "图书链接", bsHint: "图书页面上的按钮，可在其他网站查找该书。Goodreads 和你语言中最大的网站默认开启。", bsFind: "搜索于", bsGlobal: "全球", bsLocal: "你的语言", bsOther: "其他国家", pgCard: "重新设计的页面", pgHint: "每一项都会用 NanoHive 版本替换 Audiobookshelf 页面。关闭即可恢复原版。", pgNarrators: "朗读者卡片", pgCollections: "收藏页面", pgUsers: "用户卡片（设置）", pgStats: "收听排行榜", pgPhoto: "顶栏头像", pgCinematic: "影院式背景", pgTransitions: "页面过渡", pSearch: "搜索设置…", pSearchNone: "无匹配项。", erTypeface: "字体", erPage: "页面主题", erText: "文字颜色", erBg: "背景", erDefault: "默认 (ABS)", erAuto: "自动", nrForms: ["朗读者","朗读者"], nrBooksForms: ["本","本"], nrSearch: "筛选朗读者…", nrSortName: "名称", nrSortBooks: "书最多", auForms: ["作者","作者"], auSearch: "筛选作者…" , lfByAuthor: "作者 › 系列顺序", lfBySeries: "系列 › 顺序" , lfSecFilters: "更多筛选", lfSecSort: "多级排序", lfAuthor: "作者", lfSeries: "系列", lfTitle: "标题", lfYear: "年份", lfAdded: "添加时间", lfDuration: "时长", lfNarrator: "朗读者", lfGenre: "类型", lfLanguage: "语言", lfProgress: "进度", lfPgFinished: "已听完", lfPgInProgress: "收听中", lfPgNot: "未开始" , logoUpload: '从设备上传…', logoUploaded: '已上传 ✓', logoBadType: '不支持的图片类型', logoTooBig: '图片过大（最大 4 MB）', logoOr: '或', bookPage: '书籍页面', ratingsToggle: '社区评分（星级与评论）', ratingsHint: '每个书籍页面上带简短评论的星级评分，与该服务器的所有用户共享。', searchCard: '搜索', gsToggle: '同时搜索所有书库', gsHint: '跨所有可访问书库的一份合并结果列表，每条结果带书库徽标。关闭后恢复按书库的菜单。', gsNoResults: '无结果', gsSearching: '搜索中…', gsBooks: '书籍', gsSeries: '系列', gsAuthors: '作者', spTitle: '起始页', spHint: '始终在所选的书库视图打开应用（仅此浏览器）。直接链接绝不会被重定向。', spDefault: '应用默认', spViewHome: '主页', spViewLibrary: '书库', spViewSeries: '系列', spViewCollections: '收藏', spViewAuthors: '作者', scSet: '设置系列封面', scRemove: '移除系列封面', scEditDesc: '编辑系列简介', cardStars: '封面上的星级徽标', hoverPolish: '卡片悬停润色（更柔和的变暗、强调色播放键、悬停显示星级）', sectUser: '你的设置', sectAdmin: '管理', lockTitle: '功能开关', lockHint: '为该服务器所有用户强制关闭某项功能。优先于个人设置；其他人在下次刷新后可见。', lockRatings: '全面停用评分', lockCardRatings: '停用封面星级', lockGlobalSearch: '停用全局搜索', lockHoverPolish: '停用卡片悬停润色', lockHeroCarousel: '停用主页轮播', lockedNote: '已被管理员停用', lfSort: '评分', lfRated: '已评分', lfUnrated: '未评分', lfClear: '清除', lfNone: '没有匹配的书', lfSection: '评分', lfWho: '谁的评分', lfEveryone: '所有人', sdSave: '保存', sdClear: '恢复书籍简介', sdMore: '阅读更多', sdLess: '收起', shSeries: '系列', shBy: '作者：', shMore: ' 等', shBookForms: ['本', '本'], shAvg: '书籍均分', shRated: '已评分', impLabel: '导入评分', impHint: '把你的评分从 StoryGraph 或 Goodreads 带过来。在那边把书导出为 CSV，然后在这里选择文件。每个匹配都会展示给你确认，确认前不会保存任何内容。', impBtn: '选择 CSV 文件…', impReading: '正在读取文件…', impMatching: '正在与你的书库比对…', impErrUnknown: '这看起来不像 StoryGraph 或 Goodreads 的导出文件。应为它们提供的 CSV，包含 "Star Rating" 或 "My Rating" 列。', impErrEmpty: '该文件没有内容行。', impErrNoRated: '该文件中没有已评分的书。', impErrRead: '无法读取该文件。', impErrLoad: '无法加载你的书库。请稍后重试。', impErrNoLibs: '没有书库参与评分，因此无处导入。', impTitle: '导入评分', impFrom: '%s 导出：%d 本书，其中 %d 本已评分', impLibs: '搜索范围：%s', impMatched: '已匹配', impCheck: '值得核对', impCheckHint: '接近但不确定。请核对匹配或另选一个。', impAlready: '你已评分', impAlreadyHint: '除非你选择覆盖，否则保持原样。', impNone: '不在你的书库中', impNoneHint: '此服务器上没有与之匹配的内容。', impAll: '全部', impSkip: '跳过此项', impDup: '重复', impByTitle: '书名', impYours: '你的：%s', impReviews: '同时导入书面评论（%d）', impOver: '覆盖我已有的评分', impGo: '导入 %d 条评分', impCancel: '取消', impCloseBtn: '关闭', impWorking: '保存中… %d / %d', impDone: '已导入 %d 条评分。', impFail: '%d 条未能保存。', rdTitle: '谁在读这本书', rdFin: '已读完', socTitle: '社交功能', socHint: '该服务器在用户之间共享的内容。收听时长驱动排行榜；另外两项会暴露每个人在读什么，因此在你开启之前保持关闭。对所有人同时生效。', socTime: '收听时长（服务器排行榜）', socContent: '共享统计中的热门书目', socWho: '谁在读（书籍页面）', socUsers: '按用户', socUsersHint: '关闭某人后，无论其本人如何选择，其数据一概不共享；只有你能恢复。"本人退出" 表示这个人自己说了不。', socOpted: '本人退出', srvTitle: '服务器默认值', srvHint: '把你当前的设置保存为该服务器所有用户的默认值。每个人仍可在此基础上个性化。在主题容器的 /data/nh 挂载卷，以便更新后保留。', srvSave: '保存为服务器默认值', srvClear: '清除服务器默认值', srvSaved: '已保存', srvCleared: '已清除', srvErr: '失败：需要管理员登录', crMode: '继续阅读书架', crCombine: '并入轮播', crSeparate: '保留为单独书架', crHide: '隐藏', fsHint: '与该服务器上的其他人共享你的收听：用于排行榜的总时长和每日分钟数；若管理员开启，还包括你的热门书目以及你在读或已读完的书。默认开启。关闭后，已共享的内容将全部删除。' , amfHide: '隐藏"标记为已读完"' , amfConfirm: '将这本书标记为已读完？' , fsHint: '与本服务器的其他人共享你的收听摘要（总量与每日分钟数）- 它是服务器排行榜的数据来源。默认开启。关闭后你将从排行榜及其统计中移除，共享的数据会被删除。' , srToggle: '分享你在读的书', srHint: '让其他人看到你正在读的书、已读完的书和最常听的书。默认关闭，还需要管理员打开服务器开关。' , fsHint: '与该服务器上的其他人共享你的收听时长（总量和每日分钟数）；它驱动排行榜。默认开启。关闭后其他用户不再看到你；服务器管理员仍能看到活动，并标记为已退出。', srHint: '还会让其他人看到你正在读的书、已读完的书和最常听的书。在管理员打开相应的服务器开关之前，什么都不会显示。', seshTitle: '最近的会话', seshNow: '正在收听' , hcHint: '将你的评分发送到 Hardcover 账户。请粘贴 Hardcover 账户设置中的 API 密钥；密钥一年后过期。', hcKeyLbl: 'API 密钥', hcToggle: '自动同步新评分', hcPushAll: '发送全部评分', hcPushDiff: '仅发送差异', hcDone: '完成：%d 已同步，%d 未匹配', hcNoKey: '请先粘贴你的 Hardcover API 密钥' , hcMissHead: '在 Hardcover 上未找到：' , hcHint: '将你的评分发送到 Hardcover 账户。请粘贴 Hardcover 账户设置中的 API 密钥。' , tabStyle: '样式', tabMain: '主页', tabBooks: '图书', tabFeat: '功能' , hcImport: '从 Hardcover 导入', impMarkQ: '是否也在此处将匹配到的 %d 本书标记为已读完？', impMarkDone: '已将 %d 本标记为读完。' , impHint: '从 Goodreads、StoryGraph 或 Hardcover 导入你的评分。在这里选择导出的 CSV；Hardcover 通过其卡片上的按钮直接导入。保存前会展示每个匹配供你确认。' , impErrUnknown: '这看起来不像 Goodreads、StoryGraph 或 Hardcover 的导出文件：未找到标题和评分列。' , fsCard: '社交', rnToggle: '在评分旁显示我的名字', rsToggle: '与其他用户分享我的评分', socNames: '评分显示名字', rtAnon: '匿名' },
    ar: { cmStopped: "تم الإيقاف", cmScanErrs: "{e} لم يتم التحقق منها (أخطاء Goodreads)", cmScanBusy: "هناك فحص قيد التشغيل بالفعل", cmGrErr: "تعذر الوصول إلى Goodreads، سيُعاد المحاولة", cmNoMatchYet: "لا تطابق في Goodreads", cmLooking: "جارٍ البحث في Goodreads…", cmToolHint: "ما يراه قراء Goodreads في هذا الكتاب. تُظهر ? الكتاب المطابق وتتيح تغييره.", cmSearchPh: "البحث في Goodreads…", cmNoVotes: "لا تقييمات بعد", cmRefresh: "تحديث", cmScanAll: "إعادة فحص الكل، وليس الناقص فقط", cmScanDone: "تم: {m} مطابق · {x} غير موجود · {s} لديها بالفعل", cmStop: "إيقاف", cmScanLibs: "المكتبات المراد فحصها", cmScan: "البحث عن التقييمات", lsLongest: "أطول الكتب", lsSize: "الحجم على القرص", lsDuration: "المدة الإجمالية", lsGenresN: "أنواع", lsSeriesN: "سلاسل", lsAuthorsN: "مؤلفون", lsBooks: "كتب", lsGlance: "المكتبة في لمحة", cmOpen: "فتح في Goodreads", cmDetect: "بحث", cmAddr: "عنوان المساعد", scmTitle: "صفحة السلسلة", scmHint: "شكل الغلاف المولّد فوق عنوان السلسلة عند عدم رفع غلاف خاص (هذا المتصفح فقط).", scmDeck: "رزمة متراكبة", cmHint: "ما يراه قراء Goodreads في كتاب: متوسط التقييم وعدد الأصوات، تحت النجوم في صفحة الكتاب وكترتيب وتصفية. تمتلئ صفحات الكتب من تلقاء نفسها أثناء التصفح.", cmGrOff: "Goodreads: غير مُعدّ", cmSetup: "لا يوجد مساعد بعد؟ شغّل هذا بجانب السمة (أسطر compose أدناه) ثم اضغط بحث. أي طريقة أخرى لتشغيل الصورة تعمل أيضًا: الصق عنوانها أعلاه.", cmGrOn: "Goodreads: متصل", cmGrDown: "Goodreads: مُعدّ لكنه لا يستجيب", cmGrNoNum: "Goodreads: يستجيب، لكن إصدار abs-tract هذا لا يقدّم أرقام التقييم (استخدم إصدار ratings، انظر README)", cmTitle: "تقييمات Goodreads", cmWrong: "كتاب خاطئ؟", cmFind: "البحث عن تقييم المجتمع…", cmNone: "لا تطابق", cmUse: "استخدم هذا", lfCommunity: "تقييم Goodreads", mfNatChange: "تغيير…", lsByTime: "حسب وقت الاستماع", sbTitle: "ترتيب الخادم", sbWeek: "أسبوع", sbMonth: "شهر", sbYear: "سنة", sbAll: "كل الوقت", sbBooks: "الكتب", sbTopBooks: "الأكثر استماعًا", sbTotal: "الإجمالي", sbListeners: "المستمعون", sbAvg: "المتوسط", wkTitle: "دقائق الاستماع, آخر 7 أيام", wkAvg: "المتوسط اليومي", wkRow: "أيام متتالية", rpBadge: "بلاغات مشاكل مفتوحة", rpMenu: "الإبلاغ عن مشكلة", rpTitle: "الإبلاغ عن مشكلة", rpWhat: "ما الخطأ؟", rpNote: "أي شيء آخر ينبغي أن يعرفه المدير؟ (اختياري)", rpSend: "إرسال البلاغ", rpSent: "تم الإرسال. شكرًا.", rpFail: "تعذر الإرسال", rpMissing: "محتوى ناقص أو غير مكتمل", rpQuality: "جودة صوت سيئة", rpPlay: "لا يعمل التشغيل", rpWrong: "كتاب أو غلاف أو بيانات وصفية خاطئة", rpChapters: "فصول خاطئة", rpOther: "شيء آخر", rfRate: "قيّم", rfSheet: "قيّم هذا الكتاب", rfOpen: "فتح صفحة الكتاب", rfPickHint: "اختر تقييمًا", rfTitle: "قيّم ما أنهيته", ysFinished: "أُنهيت مؤخرًا", ysAlmost: "على وشك الانتهاء", ysMarkDone: "وضع علامة منتهٍ", ysEditDate: "تغيير تاريخ الانتهاء", ysThisWeek: "هذا الأسبوع", ysBestDay: "أفضل يوم", ysTitle: "استماعك", fsTitle: "ترتيب الخادم", fsCard: "ترتيب الخادم", fsToggle: "مشاركة إحصاءات استماعي", fsJoin: "فعّل «مشاركة إحصاءات استماعي» في لوحة التخصيص لتظهر في ترتيب الخادم.", fsWaiting: "المشاركة مفعّلة, سينضم ملخصك قريبًا إلى ترتيب الخادم مع الجميع.", fsHint: "شارك ملخص استماعك (الإجماليات والدقائق اليومية) مع الآخرين على هذا الخادم, فهو يغذي ترتيب الخادم. مفعّل افتراضيًا. عند إيقافه تُزال من الترتيب ومجاميعه وتُحذف البيانات المشتركة.", yirBtn: "حصاد العام", yirTitle: "عامك في الاستماع", yirYear: "آخر 12 شهرًا", yirDays: "أيام بها استماع", yirBest: "أفضل يوم", yirMonths: "حسب الشهر", yirNone: "لا توجد بيانات استماع بعد.", ssscmDeck: "رزمة متراكبة", scmFirst: "غلاف الكتاب الأول", scmGrid: "شبكة أغلفة (افتراضي)", scmOff: "مخفي" , lsTitle: "إحصاءات الخادم", lsAll: "كل المكتبات", lsPlayed: "الكتب المشغَّلة", lsBest: "الأعلى تقييمًا", lsGenres: "أبرز التصنيفات", srvPick: "ما الذي يُحفظ:", srvGRest: "الميزات والسلوك", aeBtn: "ترتيب المؤلفين", aeTitle: "مؤلفون بلا كتب", aeNone: "كل مؤلف هنا لديه كتاب واحد على الأقل.", aeRemove: "إزالة", aeDone: "تمت الإزالة", aeWorking: "جارٍ الإزالة…", rpCard: "المشاكل المبلَّغ عنها", rpHint: "مشاكل أبلغ عنها المستخدمون من صفحة كتاب. حلّها يزيلها من القائمة.", rpEmpty: "لا بلاغات.", rpResolve: "حلّ", rpOpen: "فتح الكتاب", apCard: "التشغيل", apToggle: "تشغيل الكتاب التالي في السلسلة تلقائيًا", apHint: "عند انتهاء كتاب يبدأ التالي من السلسلة نفسها تلقائيًا. فقط الكتب الموجودة لديك وغير المنتهية.", apNext: "التالي في السلسلة", fdToggle: "أدوات الكتب المنتهية في إحصاءاتك", ysNoDate: "بلا تاريخ", hmShowing: "يعرض الرسم", ysStreak: "التتابع", ysCurrent: "الحالي", ysLongest: "الأطول", ysDaysTotal: "إجمالي الأيام", ysPace: "الوتيرة", ysPerDay: "لكل يوم استماع", ysWhen: "متى تستمع", ysTopBooks: "الأكثر استماعًا من الكتب", ysTopAuthors: "الأكثر استماعًا من المؤلفين", ysTopNarrators: "الأكثر استماعًا من الرواة", rfToggle: "قسم «قيّم» في الرئيسية", hoTitle: "ترتيب أقسام الرئيسية", hoEmpty: "افتح الصفحة الرئيسية مرة ثم عد إلى هنا.", hoReset: "استعادة الترتيب الافتراضي", ctIcon: "أيقونة", ctColour: "لون مميز", ctNeedBook: "لا يستطيع Audiobookshelf حفظ مجموعة فارغة, اختر كتابًا واحدًا على الأقل. يمكنك الإضافة والحذف لاحقًا بحرية.", colEditIcon: "تغيير الأيقونة", ciTitle: "أيقونة المجموعة", ciReset: "مطابقة الاسم تلقائيًا", colEditDesc: "تحرير الوصف", colBookForms: ["كتاب","كتب"], colForms: ["مجموعة","مجموعات"], ctTitle: "إنشاء مجموعة", ctBlank: "مجموعة فارغة", ctNew: "مجموعة جديدة", ctNewHint: "من قالب", ctNamePh: "اسم المجموعة", ctSearch: "ابحث عن كتب أو مؤلفين…", ctCreate: "إنشاء", ctBack: "‹ رجوع", ctAddAll: "إضافة كل الكتب", abTitle: "إضافة كتب", abAdd: "إضافة", avRemove: "إزالة الصورة", avSet: "اختيار صورة…", ugListened: "وقت الاستماع", ugPhoto: "صورة الملف الشخصي", ugNever: "أبدًا", ugSeen: "آخر نشاط", ugEdit: "تحرير المستخدم", ugDelete: "حذف المستخدم", ugAdd: "إضافة صورة", ugChange: "تغيير الصورة", ugSortName: "أبجديًا", ugSortTime: "الأكثر استماعًا", ugSortSeen: "النشطون مؤخرًا", bsCard: "روابط الكتاب", bsHint: "أزرار في صفحة الكتاب للبحث عن العنوان في مواقع أخرى. Goodreads وأكبر موقع بلغتك مفعّلان مسبقًا.", bsFind: "ابحث في", bsGlobal: "في كل مكان", bsLocal: "للغتك", bsOther: "بلدان أخرى", pgCard: "صفحات معاد تصميمها", pgHint: "كل خيار يستبدل صفحة Audiobookshelf بنسخة NanoHive. عطّله لاستعادة الأصل.", pgNarrators: "بطاقات الرواة", pgCollections: "صفحات المجموعات", pgUsers: "بطاقات المستخدمين (الإعدادات)", pgStats: "تصنيف الاستماع", pgPhoto: "صورة الملف في الشريط العلوي", pgCinematic: "خلفية سينمائية", pgTransitions: "انتقالات الصفحات", pSearch: "ابحث في الإعدادات…", pSearchNone: "لا تطابق.", erTypeface: "الخط", erPage: "سمة الصفحة", erText: "لون النص", erBg: "الخلفية", erDefault: "الافتراضي (ABS)", erAuto: "تلقائي", nrForms: ["راوٍ","رواة"], nrBooksForms: ["كتاب","كتب"], nrSearch: "تصفية الرواة…", nrSortName: "الاسم", nrSortBooks: "الأكثر كتبًا", auForms: ["مؤلف","مؤلفون"], auSearch: "تصفية المؤلفين…" , lfByAuthor: "المؤلف › ترتيب السلسلة", lfBySeries: "السلسلة › الترتيب" , lfSecFilters: "مرشحات إضافية", lfSecSort: "فرز متعدد", lfAuthor: "المؤلف", lfSeries: "السلسلة", lfTitle: "العنوان", lfYear: "السنة", lfAdded: "أضيف", lfDuration: "المدة", lfNarrator: "الراوي", lfGenre: "التصنيف", lfLanguage: "اللغة", lfProgress: "التقدم", lfPgFinished: "منتهية", lfPgInProgress: "قيد الاستماع", lfPgNot: "لم تبدأ" , logoUpload: 'رفع من الجهاز…', logoUploaded: 'تم الرفع ✓', logoBadType: 'نوع صورة غير مدعوم', logoTooBig: 'الصورة كبيرة جدًا (بحد أقصى 4 م.ب)', logoOr: 'أو', bookPage: 'صفحة الكتاب', ratingsToggle: 'تقييمات المجتمع (نجوم ومراجعات)', ratingsHint: 'تقييمات بالنجوم مع مراجعات قصيرة في كل صفحة كتاب، مشتركة بين جميع مستخدمي هذا الخادم.', searchCard: 'البحث', gsToggle: 'البحث في كل المكتبات دفعة واحدة', gsHint: 'قائمة نتائج موحّدة عبر كل المكتبات المتاحة لك، مع شارة المكتبة على كل نتيجة. أوقفه لاستعادة القائمة لكل مكتبة.', gsNoResults: 'لا نتائج', gsSearching: 'جارٍ البحث…', gsBooks: 'كتب', gsSeries: 'سلاسل', gsAuthors: 'مؤلفون', spTitle: 'صفحة البداية', spHint: 'افتح التطبيق دائمًا على عرض مكتبة مختار (هذا المتصفح فقط). الروابط المباشرة لا يُعاد توجيهها أبدًا.', spDefault: 'افتراضي التطبيق', spViewHome: 'الرئيسية', spViewLibrary: 'المكتبة', spViewSeries: 'السلاسل', spViewCollections: 'المجموعات', spViewAuthors: 'المؤلفون', scSet: 'تعيين غلاف السلسلة', scRemove: 'إزالة غلاف السلسلة', scEditDesc: 'تحرير وصف السلسلة', cardStars: 'شارات النجوم على الأغلفة', hoverPolish: 'لمسات عند التمرير على البطاقات (تعتيم أنعم، زر تشغيل مميز، نجوم عند التمرير)', sectUser: 'إعداداتك', sectAdmin: 'الإدارة', lockTitle: 'مفاتيح الميزات', lockHint: 'يعطّل ميزة قسريًا لجميع مستخدمي هذا الخادم. يتغلب على الإعدادات الشخصية؛ يراه الآخرون بعد إعادة التحميل التالية.', lockRatings: 'تعطيل التقييمات في كل مكان', lockCardRatings: 'تعطيل النجوم على الأغلفة', lockGlobalSearch: 'تعطيل البحث الشامل', lockHoverPolish: 'تعطيل لمسات البطاقات', lockHeroCarousel: 'تعطيل شريط الرئيسية الدوار', lockedNote: 'عطّله المدير', lfSort: 'التقييم', lfRated: 'مُقيَّمة', lfUnrated: 'غير مُقيَّمة', lfClear: 'مسح', lfNone: 'لا كتب مطابقة', lfSection: 'التقييم', lfWho: 'تقييمات مَن', lfEveryone: 'الجميع', sdSave: 'حفظ', sdClear: 'استعادة وصف الكتاب', sdMore: 'اقرأ المزيد', sdLess: 'إظهار أقل', shSeries: 'سلسلة', shBy: 'بقلم ', shMore: ' وغيرهم', shBookForms: ['كتاب', 'كتب'], shAvg: 'متوسط الكتب', shRated: 'مُقيَّمة', impLabel: 'استيراد التقييمات', impHint: 'انقل تقييماتك من StoryGraph أو Goodreads. صدّر كتبك هناك بصيغة CSV ثم اختر الملف هنا. كل تطابق يُعرض عليك للتأكيد ولا يُحفظ شيء حتى تؤكد.', impBtn: 'اختر ملف CSV…', impReading: 'جارٍ قراءة الملف…', impMatching: 'جارٍ المطابقة مع مكتباتك…', impErrUnknown: 'هذا لا يبدو تصديرًا من StoryGraph أو Goodreads. يجب أن يكون ملف CSV الخاص بهما، بعمود "Star Rating" أو "My Rating".', impErrEmpty: 'الملف لا يحتوي على صفوف.', impErrNoRated: 'لا توجد كتب مُقيَّمة في الملف.', impErrRead: 'تعذّرت قراءة الملف.', impErrLoad: 'تعذّر تحميل مكتباتك. حاول مجددًا بعد قليل.', impErrNoLibs: 'لا مكتبة تشارك في التقييمات، فلا وجهة للاستيراد.', impTitle: 'استيراد التقييمات', impFrom: 'تصدير %s: %d كتابًا، منها %d مُقيَّم', impLibs: 'البحث في: %s', impMatched: 'مُطابَقة', impCheck: 'تستحق نظرة', impCheckHint: 'قريب لكن غير مؤكد. تحقق من المطابقة أو اختر أخرى.', impAlready: 'قيّمتها بالفعل', impAlreadyHint: 'تبقى كما هي ما لم تطلب الكتابة فوقها.', impNone: 'ليست في مكتبتك', impNoneHint: 'لا شيء على هذا الخادم يطابق هذه.', impAll: 'الكل', impSkip: 'تخطَّ هذا', impDup: 'مكرر', impByTitle: 'العنوان', impYours: 'تقييمك: %s', impReviews: 'استيراد المراجعات المكتوبة أيضًا (%d)', impOver: 'الكتابة فوق تقييماتي الحالية', impGo: 'استيراد %d تقييمًا', impCancel: 'إلغاء', impCloseBtn: 'إغلاق', impWorking: 'جارٍ الحفظ… %d من %d', impDone: 'تم استيراد %d تقييمًا.', impFail: 'تعذّر حفظ %d.', rdTitle: 'من يقرأ هذا الكتاب', rdFin: 'أُنهي', socTitle: 'الميزات الاجتماعية', socHint: 'ما يشاركه هذا الخادم بين المستخدمين. وقت الاستماع يغذي الترتيب؛ الميزتان الأخريان تكشفان ما يقرؤه كل شخص، لذا تبقيان مطفأتين حتى تشغّلهما. يسري على الجميع دفعة واحدة.', socTime: 'وقت الاستماع (ترتيب الخادم)', socContent: 'أبرز العناوين في الإحصاءات المشتركة', socWho: 'من يقرأ (صفحات الكتب)', socUsers: 'لكل مستخدم', socUsersHint: 'إن أطفأت أحدًا فلن يُشارَك شيء من بياناته مهما اختار هو؛ أنت وحدك من يعيده. "انسحب بنفسه" تعني أن الشخص قال لا بنفسه.', socOpted: 'انسحب بنفسه', srvTitle: 'افتراضيات الخادم', srvHint: 'احفظ إعداداتك الحالية كافتراضيات لجميع مستخدمي هذا الخادم. يظل بإمكان كل شخص التخصيص فوقها. ثبّت وحدة تخزين على ‎/data/nh في حاوية السمة لتبقى عبر التحديثات.', srvSave: 'حفظ كافتراضيات الخادم', srvClear: 'مسح افتراضيات الخادم', srvSaved: 'حُفظ', srvCleared: 'مُسح', srvErr: 'فشل: يلزم تسجيل دخول المدير', crMode: 'رف متابعة القراءة', crCombine: 'دمج في الشريط الدوار', crSeparate: 'إبقاؤه رفًا مستقلًا', crHide: 'مخفي', fsHint: 'شارك استماعك مع الآخرين على هذا الخادم: مجاميع الوقت والدقائق اليومية للترتيب، وإن شغّلها المدير فأيضًا أبرز عناوينك وأي الكتب تقرأ أو أنهيت. مفعّل افتراضيًا. عند الإيقاف يُحذف كل ما شاركته.' , amfHide: 'إخفاء وضع علامة منتهٍ' , amfConfirm: 'وضع علامة منتهٍ على هذا الكتاب؟' , fsHint: 'شارك ملخص استماعك (الإجماليات والدقائق اليومية) مع الآخرين على هذا الخادم, فهو يغذي ترتيب الخادم. مفعّل افتراضيًا. عند إيقافه تُزال من الترتيب ومجاميعه وتُحذف البيانات المشتركة.' , srToggle: 'مشاركة الكتب التي تقرؤها', srHint: 'يضيف إلى ما يراه الآخرون الكتب التي تقرؤها الآن والتي أنهيتها والتي تستمع إليها أكثر. مطفأ افتراضيًا؛ ويجب أيضًا تشغيل مفاتيح الخادم لدى المدير.' , fsHint: 'شارك وقت استماعك (المجاميع والدقائق اليومية) مع الآخرين على هذا الخادم؛ فهو يغذي الترتيب. مفعّل افتراضيًا. عند الإيقاف لا يعود المستخدمون الآخرون يرونك؛ ويظل مديرو الخادم يرون النشاط معلَّمًا بالانسحاب.', srHint: 'يتيح للآخرين أيضًا رؤية الكتب التي تقرؤها والتي أنهيتها والتي تستمع إليها أكثر. لا يظهر شيء حتى يشغّل المدير مفاتيح الخادم المقابلة.', seshTitle: 'الجلسات الأخيرة', seshNow: 'يستمعون الآن' , hcHint: 'أرسل تقييماتك إلى حسابك على Hardcover. الصق مفتاح API من إعدادات حساب Hardcover؛ تنتهي صلاحية المفاتيح بعد سنة.', hcKeyLbl: 'مفتاح API', hcToggle: 'مزامنة التقييمات الجديدة تلقائيًا', hcPushAll: 'إرسال كل التقييمات', hcPushDiff: 'إرسال الاختلافات فقط', hcDone: 'تم: %d مزامنة، %d بلا تطابق', hcNoKey: 'الصق أولًا مفتاح API الخاص بك من Hardcover' , hcMissHead: 'غير موجود في Hardcover:' , hcHint: 'أرسل تقييماتك إلى حسابك على Hardcover. الصق مفتاح API من إعدادات حساب Hardcover.' , tabStyle: 'النمط', tabMain: 'الصفحة الرئيسية', tabBooks: 'الكتب', tabFeat: 'الميزات' , hcImport: 'استيراد من Hardcover', impMarkQ: 'هل تريد أيضًا وضع علامة منتهية على %d كتابًا مطابقًا هنا؟', impMarkDone: 'تم وضع علامة منتهية على %d.' , impHint: 'انقل تقييماتك من Goodreads أو StoryGraph أو Hardcover. اختر ملف CSV المصدَّر هنا؛ يستورد Hardcover مباشرة عبر الزر في بطاقته. تُعرض كل مطابقة للتأكيد قبل حفظ أي شيء.' , impErrUnknown: 'لا يبدو هذا ملف تصدير من Goodreads أو StoryGraph أو Hardcover: لم يتم العثور على عمودَي العنوان والتقييم.' , fsCard: 'اجتماعي', rnToggle: 'إظهار اسمي مع التقييمات', rsToggle: 'مشاركة تقييماتي مع الآخرين', socNames: 'الأسماء مع التقييمات', rtAnon: 'مجهول' },
    he: { cmStopped: "הופסק", cmScanErrs: "{e} לא נבדקו (שגיאות Goodreads)", cmScanBusy: "סריקה כבר פועלת", cmGrErr: "אין גישה ל-Goodreads, ננסה שוב", cmNoMatchYet: "אין התאמה ב-Goodreads", cmLooking: "מחפש ב-Goodreads…", cmToolHint: "מה חושבים קוראי Goodreads על הספר הזה. ה-? מציג את הספר שהותאם ומאפשר לשנותו.", cmSearchPh: "חפש ב-Goodreads…", cmNoVotes: "אין דירוגים עדיין", cmRefresh: "רענן", cmScanAll: "לסרוק הכול מחדש, לא רק את החסרים", cmScanDone: "הסתיים: {m} תואמים · {x} לא נמצאו · {s} כבר היו", cmStop: "עצור", cmScanLibs: "ספריות לסריקה", cmScan: "חפש דירוגים", lsLongest: "הספרים הארוכים ביותר", lsSize: "גודל בדיסק", lsDuration: "משך כולל", lsGenresN: "ז׳אנרים", lsSeriesN: "סדרות", lsAuthorsN: "סופרים", lsBooks: "ספרים", lsGlance: "הספרייה במבט", cmOpen: "פתח ב-Goodreads", cmDetect: "חפש", cmAddr: "כתובת העוזר", scmTitle: "עמוד סדרה", scmHint: "כך נראית הכריכה שנוצרת מעל שם הסדרה כשלא הועלתה כריכה משלכם (דפדפן זה בלבד).", scmDeck: "חפיסה בשכבות", cmHint: "מה חושבים קוראי Goodreads על ספר: ציון ממוצע ומספר המצביעים, מתחת לכוכבים בדף הספר וגם כמיון וסינון. דפי ספרים מתמלאים מעצמם תוך כדי גלישה.", cmGrOff: "Goodreads: לא מוגדר", cmSetup: "עדיין אין עוזר? הריצו את זה לצד ערכת הנושא (שורות compose למטה) ולחצו חפש. כל דרך אחרת להריץ את התמונה מתאימה גם: הדביקו את הכתובת למעלה.", cmGrOn: "Goodreads: מחובר", cmGrDown: "Goodreads: מוגדר אך לא מגיב", cmGrNoNum: "Goodreads: מגיב, אך גרסת abs-tract זו אינה מספקת מספרי דירוג (השתמשו בגרסת ratings, ראו README)", cmTitle: "דירוגי Goodreads", cmWrong: "ספר שגוי?", cmFind: "חפש דירוג קהילה…", cmNone: "אין התאמה", cmUse: "השתמש בזה", lfCommunity: "דירוג Goodreads", mfNatChange: "שינוי…", lsByTime: "לפי זמן האזנה", sbTitle: "דירוג השרת", sbWeek: "שבוע", sbMonth: "חודש", sbYear: "שנה", sbAll: "כל הזמן", sbBooks: "ספרים", sbTopBooks: "הנשמעים ביותר", sbTotal: "סה\"כ", sbListeners: "מאזינים", sbAvg: "ממוצע", wkTitle: "דקות האזנה, 7 הימים האחרונים", wkAvg: "ממוצע יומי", wkRow: "ימים ברצף", rpBadge: "דיווחי תקלות פתוחים", rpMenu: "דיווח על תקלה", rpTitle: "דיווח על תקלה", rpWhat: "מה לא בסדר?", rpNote: "עוד משהו שהמנהל צריך לדעת? (רשות)", rpSend: "שליחת דיווח", rpSent: "נשלח. תודה.", rpFail: "השליחה נכשלה", rpMissing: "תוכן חסר או לא שלם", rpQuality: "איכות שמע ירודה", rpPlay: "לא מתנגן", rpWrong: "ספר, כריכה או מטא-נתונים שגויים", rpChapters: "פרקים שגויים", rpOther: "משהו אחר", rfRate: "דרג", rfSheet: "דרגו את הספר הזה", rfOpen: "פתיחת עמוד הספר", rfPickHint: "בחרו דירוג", rfTitle: "דרגו את מה שסיימתם", ysFinished: "הסתיימו לאחרונה", ysAlmost: "כמעט סיימתם", ysMarkDone: "סימון כהושלם", ysEditDate: "שינוי תאריך הסיום", ysThisWeek: "השבוע", ysBestDay: "היום הטוב ביותר", ysTitle: "ההאזנה שלך", fsTitle: "דירוג השרת", fsCard: "דירוג השרת", fsToggle: "שיתוף נתוני ההאזנה שלי", fsJoin: "הפעילו את ״שיתוף נתוני ההאזנה שלי״ בלוח ההתאמה כדי להופיע בדירוג השרת.", fsWaiting: "השיתוף פעיל, הסיכום שלכם יצטרף בקרוב לדירוג השרת יחד עם כולם.", fsHint: "שתפו סיכום של ההאזנה שלכם (סכומים ודקות ליום) עם שאר המשתמשים בשרת, הוא מזין את דירוג השרת. פעיל כברירת מחדל. בכיבוי תוסרו מהדירוג ומסכומיו, והנתונים ששותפו יימחקו.", yirBtn: "סיכום שנה", yirTitle: "שנת ההאזנה שלך", yirYear: "12 החודשים האחרונים", yirDays: "ימים עם האזנה", yirBest: "היום הטוב ביותר", yirMonths: "לפי חודש", yirNone: "אין עדיין נתוני האזנה.", ssscmDeck: "חפיסה בשכבות", scmFirst: "כריכת הספר הראשון", scmGrid: "רשת כריכות (ברירת מחדל)", scmOff: "מוסתר" , lsTitle: "סטטיסטיקות שרת", lsAll: "כל הספריות", lsPlayed: "ספרים שנוגנו", lsBest: "המדורגים ביותר", lsGenres: "ז'אנרים מובילים", srvPick: "מה לשמור:", srvGRest: "תכונות והתנהגות", aeBtn: "סידור סופרים", aeTitle: "סופרים ללא ספרים", aeNone: "לכל סופר כאן יש לפחות ספר אחד.", aeRemove: "הסרה", aeDone: "הוסרו", aeWorking: "מסיר…", rpCard: "תקלות שדווחו", rpHint: "תקלות שמשתמשים דיווחו מעמוד ספר. פתירה מסירה מהרשימה.", rpEmpty: "אין דיווחים.", rpResolve: "פתירה", rpOpen: "פתיחת הספר", apCard: "נגינה", apToggle: "נגן אוטומטית את הספר הבא בסדרה", apHint: "כשספר נגמר, הבא באותה סדרה מתחיל אוטומטית. רק ספרים שכבר יש לך ולא סיימת.", apNext: "הבא בסדרה", fdToggle: "כלי ספרים שהושלמו בסטטיסטיקות", ysNoDate: "ללא תאריך", hmShowing: "הגרף מציג", ysStreak: "רצף", ysCurrent: "נוכחי", ysLongest: "הארוך ביותר", ysDaysTotal: "סך ימים", ysPace: "קצב", ysPerDay: "ליום האזנה", ysWhen: "מתי אתם מאזינים", ysTopBooks: "הספרים הנשמעים ביותר", ysTopAuthors: "הסופרים הנשמעים ביותר", ysTopNarrators: "הקריינים הנשמעים ביותר", rfToggle: "מקטע ״דרגו״ בעמוד הבית", hoTitle: "סדר מקטעי עמוד הבית", hoEmpty: "פתחו פעם את עמוד הבית וחזרו לכאן.", hoReset: "שחזור סדר ברירת המחדל", ctIcon: "סמל", ctColour: "הדגשה", ctNeedBook: "Audiobookshelf לא יכול לשמור אוסף ריק, בחרו לפחות ספר אחד. אחר כך אפשר להוסיף ולהסיר בחופשיות.", colEditIcon: "החלפת סמל", ciTitle: "סמל האוסף", ciReset: "התאמה אוטומטית לשם", colEditDesc: "עריכת תיאור", colBookForms: ["ספר","ספרים"], colForms: ["אוסף","אוספים"], ctTitle: "יצירת אוסף", ctBlank: "אוסף ריק", ctNew: "אוסף חדש", ctNewHint: "מתבנית", ctNamePh: "שם האוסף", ctSearch: "חיפוש ספרים או סופרים…", ctCreate: "יצירה", ctBack: "‹ חזרה", ctAddAll: "הוספת כל הספרים", abTitle: "הוספת ספרים", abAdd: "הוספה", avRemove: "הסרת תמונה", avSet: "בחירת תמונה…", ugListened: "זמן האזנה", ugPhoto: "תמונת פרופיל", ugNever: "אף פעם", ugSeen: "פעילות אחרונה", ugEdit: "עריכת משתמש", ugDelete: "מחיקת משתמש", ugAdd: "הוספת תמונה", ugChange: "החלפת תמונה", ugSortName: "א–ת", ugSortTime: "הנשמעים ביותר", ugSortSeen: "פעילים לאחרונה", bsCard: "קישורי ספר", bsHint: "כפתורים בעמוד הספר לחיפוש הכותר במקומות אחרים. Goodreads והאתר הגדול בשפתכם מופעלים מראש.", bsFind: "חיפוש ב-", bsGlobal: "בכל מקום", bsLocal: "לשפה שלך", bsOther: "מדינות אחרות", pgCard: "עמודים בעיצוב מחודש", pgHint: "כל אפשרות מחליפה עמוד של Audiobookshelf בגרסת NanoHive. כיבוי מחזיר את המקור.", pgNarrators: "כרטיסי קריינים", pgCollections: "עמודי אוספים", pgUsers: "כרטיסי משתמשים (הגדרות)", pgStats: "דירוג האזנה", pgPhoto: "תמונת פרופיל בסרגל העליון", pgCinematic: "רקע קולנועי", pgTransitions: "מעברי עמודים", pSearch: "חיפוש הגדרות…", pSearchNone: "אין התאמות.", erTypeface: "גופן", erPage: "ערכת עמוד", erText: "צבע טקסט", erBg: "רקע", erDefault: "ברירת מחדל (ABS)", erAuto: "אוטומטי", nrForms: ["קריין","קריינים"], nrBooksForms: ["ספר","ספרים"], nrSearch: "סינון קריינים…", nrSortName: "שם", nrSortBooks: "הכי הרבה ספרים", auForms: ["סופר","סופרים"], auSearch: "סינון סופרים…" , lfByAuthor: "סופר › סדר הסדרה", lfBySeries: "סדרה › סדר" , lfSecFilters: "מסננים נוספים", lfSecSort: "מיון מרובה", lfAuthor: "סופר", lfSeries: "סדרה", lfTitle: "כותרת", lfYear: "שנה", lfAdded: "נוסף", lfDuration: "משך", lfNarrator: "קריין", lfGenre: "ז'אנר", lfLanguage: "שפה", lfProgress: "התקדמות", lfPgFinished: "הושלמו", lfPgInProgress: "בהאזנה", lfPgNot: "לא התחילו" , logoUpload: 'העלאה מהמכשיר…', logoUploaded: 'הועלה ✓', logoBadType: 'סוג תמונה לא נתמך', logoTooBig: 'התמונה גדולה מדי (עד 4 MB)', logoOr: 'או', bookPage: 'עמוד הספר', ratingsToggle: 'דירוגי הקהילה (כוכבים וביקורות)', ratingsHint: 'דירוגי כוכבים עם ביקורות קצרות בכל עמוד ספר, משותפים לכל משתמשי השרת הזה.', searchCard: 'חיפוש', gsToggle: 'חיפוש בכל הספריות בבת אחת', gsHint: 'רשימת תוצאות מאוחדת מכל הספריות הזמינות לך, עם תג ספרייה על כל תוצאה. כיבוי מחזיר את התפריט לפי ספרייה.', gsNoResults: 'אין תוצאות', gsSearching: 'מחפש…', gsBooks: 'ספרים', gsSeries: 'סדרות', gsAuthors: 'סופרים', spTitle: 'עמוד פתיחה', spHint: 'פתיחת האפליקציה תמיד בתצוגת ספרייה נבחרת (בדפדפן הזה בלבד). קישורים ישירים לעולם לא מנותבים מחדש.', spDefault: 'ברירת המחדל של האפליקציה', spViewHome: 'בית', spViewLibrary: 'ספרייה', spViewSeries: 'סדרות', spViewCollections: 'אוספים', spViewAuthors: 'סופרים', scSet: 'הגדרת כריכת סדרה', scRemove: 'הסרת כריכת סדרה', scEditDesc: 'עריכת תיאור הסדרה', cardStars: 'תגי כוכבים על כריכות', hoverPolish: 'ליטוש כרטיסים במעבר עכבר (החשכה רכה יותר, כפתור מודגש, כוכבים במעבר)', sectUser: 'ההגדרות שלך', sectAdmin: 'ניהול', lockTitle: 'מתגי תכונות', lockHint: 'כיבוי כפוי של תכונה לכל משתמשי השרת הזה. גובר על הגדרות אישיות; אחרים יראו לאחר הטעינה הבאה.', lockRatings: 'כיבוי דירוגים בכל מקום', lockCardRatings: 'כיבוי כוכבים על כריכות', lockGlobalSearch: 'כיבוי חיפוש גלובלי', lockHoverPolish: 'כיבוי ליטוש כרטיסים', lockHeroCarousel: 'כיבוי קרוסלת הבית', lockedNote: 'כובה על ידי המנהל', lfSort: 'דירוג', lfRated: 'מדורגים', lfUnrated: 'לא מדורגים', lfClear: 'ניקוי', lfNone: 'אין ספרים תואמים', lfSection: 'דירוג', lfWho: 'הדירוגים של מי', lfEveryone: 'כולם', sdSave: 'שמירה', sdClear: 'שחזור תיאור הספר', sdMore: 'קרא עוד', sdLess: 'הצג פחות', shSeries: 'סדרה', shBy: 'מאת ', shMore: ' ועוד', shBookForms: ['ספר', 'ספרים'], shAvg: 'ממוצע ספרים', shRated: 'מדורגים', impLabel: 'ייבוא דירוגים', impHint: 'העבר את הדירוגים שלך מ-StoryGraph או Goodreads. ייצא שם את הספרים כ-CSV ובחר את הקובץ כאן. כל התאמה מוצגת לאישור ושום דבר לא נשמר עד שתאשר.', impBtn: 'בחר קובץ CSV…', impReading: 'קורא את הקובץ…', impMatching: 'משווה לספריות שלך…', impErrUnknown: 'זה לא נראה כמו ייצוא מ-StoryGraph או Goodreads. זה צריך להיות ה-CSV שלהם, עם עמודה "Star Rating" או "My Rating".', impErrEmpty: 'בקובץ אין שורות.', impErrNoRated: 'אין ספרים מדורגים בקובץ.', impErrRead: 'לא ניתן לקרוא את הקובץ.', impErrLoad: 'לא ניתן לטעון את הספריות שלך. נסה שוב בעוד רגע.', impErrNoLibs: 'אף ספרייה לא משתתפת בדירוגים, כך שאין לאן לייבא.', impTitle: 'ייבוא דירוגים', impFrom: 'ייצוא %s: %d ספרים, מהם %d מדורגים', impLibs: 'מחפש ב: %s', impMatched: 'הותאמו', impCheck: 'שווה בדיקה', impCheckHint: 'קרוב אך לא בטוח. בדוק את ההתאמה או בחר אחרת.', impAlready: 'כבר דורגו על ידך', impAlreadyHint: 'נשארים כפי שהם, אלא אם תבקש לדרוס.', impNone: 'לא בספרייה שלך', impNoneHint: 'שום דבר בשרת הזה לא תואם לאלה.', impAll: 'הכול', impSkip: 'דלג על זה', impDup: 'כפילות', impByTitle: 'כותרת', impYours: 'שלך: %s', impReviews: 'ייבא גם ביקורות כתובות (%d)', impOver: 'דרוס דירוגים שכבר יש לי', impGo: 'ייבא %d דירוגים', impCancel: 'ביטול', impCloseBtn: 'סגירה', impWorking: 'שומר… %d מתוך %d', impDone: 'יובאו %d דירוגים.', impFail: '%d לא נשמרו.', rdTitle: 'מי קורא את הספר הזה', rdFin: 'הסתיים', socTitle: 'תכונות חברתיות', socHint: 'מה השרת הזה משתף בין משתמשים. זמן ההאזנה מזין את הדירוג; שתי האחרות חושפות מה כל אחד קורא, ולכן נשארות כבויות עד שתדליק אותן. חל על כולם בבת אחת.', socTime: 'זמן האזנה (דירוג השרת)', socContent: 'כותרים מובילים בסטטיסטיקה המשותפת', socWho: 'מי קורא (עמודי ספרים)', socUsers: 'לפי משתמש', socUsersHint: 'אם תכבה מישהו, שום דבר משלו לא ישותף, לא משנה מה בחר בעצמו; רק אתה יכול להחזיר. "פרש בעצמו" פירושו שהאדם עצמו אמר לא.', socOpted: 'פרש בעצמו', srvTitle: 'ברירות מחדל של השרת', srvHint: 'שמור את ההגדרות הנוכחיות שלך כברירת מחדל לכל משתמשי השרת הזה. כל אחד עדיין יכול להתאים מעליהן. עגן נפח ל-/data/nh בקונטיינר הערכה כדי שיישמרו בין עדכונים.', srvSave: 'שמור כברירת מחדל של השרת', srvClear: 'נקה ברירות מחדל של השרת', srvSaved: 'נשמר', srvCleared: 'נוקה', srvErr: 'נכשל: נדרשת התחברות מנהל', crMode: 'מדף המשך קריאה', crCombine: 'איחוד לקרוסלה', crSeparate: 'השאר כמדף נפרד', crHide: 'מוסתר', fsHint: 'שתף את ההאזנה שלך עם האחרים בשרת הזה: סכומי זמן ודקות יומיות לדירוג, ואם המנהל הפעיל, גם הכותרים המובילים שלך ואילו ספרים אתה קורא או סיימת. פעיל כברירת מחדל. בכיבוי, כל מה ששיתפת נמחק.' , amfHide: 'הסתר סמן כהסתיים' , amfConfirm: 'לסמן את הספר הזה כהסתיים?' , fsHint: 'שתפו סיכום של ההאזנה שלכם (סכומים ודקות ליום) עם שאר המשתמשים בשרת, הוא מזין את דירוג השרת. פעיל כברירת מחדל. בכיבוי תוסרו מהדירוג ומסכומיו, והנתונים ששותפו יימחקו.' , srToggle: 'שיתוף אילו ספרים אתה קורא', srHint: 'מוסיף למה שאחרים רואים אילו ספרים אתה קורא כעת, אילו סיימת ולאילו אתה מאזין הכי הרבה. כבוי כברירת מחדל; גם מתגי השרת של המנהל חייבים להיות פעילים.' , fsHint: 'שתף את זמן ההאזנה שלך (סכומים ודקות יומיות) עם האחרים בשרת הזה; הוא מזין את הדירוג. פעיל כברירת מחדל. בכיבוי משתמשים אחרים כבר לא רואים אותך; מנהלי השרת עדיין רואים את הפעילות, מסומנת כפרישה.', srHint: 'מאפשר לאחרים לראות גם אילו ספרים אתה קורא, אילו סיימת ולאילו אתה מאזין הכי הרבה. שום דבר לא מוצג עד שהמנהל יפעיל את מתגי השרת המתאימים.', seshTitle: 'הפעלות אחרונות', seshNow: 'מאזינים כעת' , hcHint: 'שלחו את הדירוגים שלכם לחשבון Hardcover. הדביקו את מפתח ה-API מהגדרות חשבון Hardcover; מפתחות פגים אחרי שנה.', hcKeyLbl: 'מפתח API', hcToggle: 'סנכרון דירוגים חדשים אוטומטית', hcPushAll: 'שליחת כל הדירוגים', hcPushDiff: 'שליחת הבדלים בלבד', hcDone: 'הושלם: %d סונכרנו, %d ללא התאמה', hcNoKey: 'הדביקו קודם את מפתח ה-API של Hardcover' , hcMissHead: 'לא נמצא ב-Hardcover:' , hcHint: 'שלחו את הדירוגים שלכם לחשבון Hardcover. הדביקו את מפתח ה-API מהגדרות חשבון Hardcover.' , tabStyle: 'סגנון', tabMain: 'עמוד ראשי', tabBooks: 'ספרים', tabFeat: 'תכונות' , hcImport: 'ייבוא מ-Hardcover', impMarkQ: 'לסמן גם כאן %d ספרים תואמים כסיימו?', impMarkDone: '%d סומנו כהושלמו.' , impHint: 'העבירו את הדירוגים שלכם מ-Goodreads, StoryGraph או Hardcover. בחרו כאן את קובץ ה-CSV המיוצא; Hardcover מייבא ישירות בכפתור שבכרטיס שלו. כל התאמה מוצגת לאישור לפני שמירה.' , impErrUnknown: 'זה לא נראה כמו ייצוא מ-Goodreads, StoryGraph או Hardcover: לא נמצאו עמודות כותרת ודירוג.' , fsCard: 'חברתי', rnToggle: 'הצגת השם שלי לצד הדירוגים', rsToggle: 'שיתוף הדירוגים שלי עם אחרים', socNames: 'שמות לצד דירוגים', rtAnon: 'אנונימי' },
    fa: { cmStopped: "متوقف شد", cmScanErrs: "{e} بررسی نشد (خطاهای Goodreads)", cmScanBusy: "یک اسکن از قبل در حال اجراست", cmGrErr: "Goodreads در دسترس نیست، دوباره تلاش می‌شود", cmNoMatchYet: "مطابقتی در Goodreads نیست", cmLooking: "در حال جستجو در Goodreads…", cmToolHint: "نظر خوانندگان Goodreads درباره این کتاب. علامت ? کتاب مطابق را نشان می‌دهد و امکان تغییر آن را می‌دهد.", cmSearchPh: "جستجو در Goodreads…", cmNoVotes: "هنوز امتیازی نیست", cmRefresh: "تازه‌سازی", cmScanAll: "همه را دوباره بررسی کن، نه فقط موارد ناقص", cmScanDone: "انجام شد: {m} مطابق · {x} یافت نشد · {s} از قبل داشتند", cmStop: "توقف", cmScanLibs: "کتابخانه‌های موردنظر", cmScan: "جستجوی امتیازها", lsLongest: "طولانی‌ترین کتاب‌ها", lsSize: "حجم روی دیسک", lsDuration: "مدت کل", lsGenresN: "ژانرها", lsSeriesN: "مجموعه‌ها", lsAuthorsN: "نویسندگان", lsBooks: "کتاب‌ها", lsGlance: "کتابخانه در یک نگاه", cmOpen: "باز کردن در Goodreads", cmDetect: "یافتن", cmAddr: "نشانی کمک‌یار", scmTitle: "صفحه مجموعه", scmHint: "نمای جلد تولیدشده بالای عنوان مجموعه وقتی جلدی بارگذاری نشده است (فقط این مرورگر).", scmDeck: "دسته لایه‌ای", cmHint: "نظر خوانندگان Goodreads درباره یک کتاب: میانگین امتیاز و تعداد رأی‌ها، زیر ستاره‌ها در صفحه کتاب و به‌عنوان مرتب‌سازی و فیلتر. صفحه‌های کتاب هنگام مرور خودبه‌خود پر می‌شوند.", cmGrOff: "Goodreads: تنظیم نشده", cmSetup: "هنوز کمک‌یاری ندارید؟ این را کنار پوسته اجرا کنید (خط‌های compose در پایین) و سپس «یافتن» را بزنید. هر روش دیگری برای اجرای ایمیج هم کار می‌کند: نشانی‌اش را بالا بچسبانید.", cmGrOn: "Goodreads: متصل", cmGrDown: "Goodreads: تنظیم شده ولی پاسخ نمی‌دهد", cmGrNoNum: "Goodreads: پاسخ می‌دهد اما این نسخه abs-tract اعداد امتیاز ندارد (از نسخه ratings استفاده کنید، README را ببینید)", cmTitle: "امتیازهای Goodreads", cmWrong: "کتاب اشتباه است؟", cmFind: "یافتن امتیاز جامعه…", cmNone: "بدون تطابق", cmUse: "استفاده از این", lfCommunity: "امتیاز Goodreads", mfNatChange: "تغییر…", lsByTime: "بر اساس زمان شنیدن", sbTitle: "رتبه‌بندی سرور", sbWeek: "هفته", sbMonth: "ماه", sbYear: "سال", sbAll: "کل زمان", sbBooks: "کتاب‌ها", sbTopBooks: "پرشنیده‌ترین", sbTotal: "مجموع", sbListeners: "شنوندگان", sbAvg: "میانگین", wkTitle: "دقیقه‌های شنیدن, ۷ روز اخیر", wkAvg: "میانگین روزانه", wkRow: "روز پیاپی", rpBadge: "گزارش‌های باز مشکل", rpMenu: "گزارش مشکل", rpTitle: "گزارش مشکل", rpWhat: "مشکل چیست؟", rpNote: "چیز دیگری که مدیر باید بداند؟ (اختیاری)", rpSend: "ارسال گزارش", rpSent: "ارسال شد. سپاس.", rpFail: "ارسال ناموفق بود", rpMissing: "محتوای ناقص یا از‌دست‌رفته", rpQuality: "کیفیت صدای بد", rpPlay: "پخش نمی‌شود", rpWrong: "کتاب، جلد یا فراداده اشتباه", rpChapters: "فصل‌ها اشتباه‌اند", rpOther: "چیز دیگر", rfRate: "امتیاز بده", rfSheet: "به این کتاب امتیاز بده", rfOpen: "باز کردن صفحه کتاب", rfPickHint: "یک امتیاز انتخاب کن", rfTitle: "به آنچه تمام کردی امتیاز بده", ysFinished: "به‌تازگی تمام‌شده", ysAlmost: "تقریباً تمام", ysMarkDone: "علامت‌گذاری به‌عنوان تمام‌شده", ysEditDate: "تغییر تاریخ پایان", ysThisWeek: "این هفته", ysBestDay: "بهترین روز", ysTitle: "شنیدن شما", fsTitle: "رتبه‌بندی سرور", fsCard: "رتبه‌بندی سرور", fsToggle: "اشتراک آمار شنیدن من", fsJoin: "«اشتراک آمار شنیدن من» را در پنل شخصی‌سازی روشن کنید تا در رتبه‌بندی سرور دیده شوید.", fsWaiting: "اشتراک روشن است, خلاصه شما به‌زودی همراه بقیه به رتبه‌بندی سرور می‌پیوندد.", fsHint: "خلاصه‌ای از شنیدن خود (مجموع‌ها و دقیقه‌های روزانه) را با دیگران در این سرور به اشتراک بگذارید, منبع رتبه‌بندی سرور است. به‌طور پیش‌فرض روشن. با خاموش کردن، از رتبه‌بندی و جمع‌های آن حذف می‌شوید و داده‌های اشتراکی پاک می‌شوند.", yirBtn: "مرور سال", yirTitle: "سالِ شنیدن شما", yirYear: "۱۲ ماه اخیر", yirDays: "روزهای دارای شنیدن", yirBest: "بهترین روز", yirMonths: "به تفکیک ماه", yirNone: "هنوز داده شنیداری نیست.", ssscmDeck: "دسته لایه‌ای", scmFirst: "جلد کتاب اول", scmGrid: "شبکه جلدها (پیش‌فرض)", scmOff: "پنهان" , lsTitle: "آمار سرور", lsAll: "همه کتابخانه‌ها", lsPlayed: "کتاب‌های پخش‌شده", lsBest: "بالاترین امتیاز", lsGenres: "ژانرهای برتر", srvPick: "چه چیزی ذخیره شود:", srvGRest: "قابلیت‌ها و رفتار", aeBtn: "مرتب‌سازی نویسندگان", aeTitle: "نویسندگان بدون کتاب", aeNone: "هر نویسنده اینجا دست‌کم یک کتاب دارد.", aeRemove: "حذف", aeDone: "حذف شد", aeWorking: "در حال حذف…", rpCard: "مشکلات گزارش‌شده", rpHint: "مشکلاتی که کاربران از صفحه کتاب گزارش کرده‌اند. حل کردن، آن را از فهرست برمی‌دارد.", rpEmpty: "گزارشی نیست.", rpResolve: "حل", rpOpen: "باز کردن کتاب", apCard: "پخش", apToggle: "پخش خودکار کتاب بعدی مجموعه", apHint: "وقتی کتابی تمام می‌شود، بعدی از همان مجموعه خودکار شروع می‌شود. فقط کتاب‌هایی که داری و تمام نکرده‌ای.", apNext: "بعدی مجموعه", fdToggle: "ابزار کتاب‌های تمام‌شده در آمار", ysNoDate: "بدون تاریخ", hmShowing: "نمودار نشان می‌دهد", ysStreak: "روزهای پیاپی", ysCurrent: "فعلی", ysLongest: "طولانی‌ترین", ysDaysTotal: "مجموع روزها", ysPace: "آهنگ", ysPerDay: "به‌ازای هر روز شنیدن", ysWhen: "چه زمانی می‌شنوی", ysTopBooks: "پرشنیده‌ترین کتاب‌ها", ysTopAuthors: "پرشنیده‌ترین نویسندگان", ysTopNarrators: "پرشنیده‌ترین گویندگان", rfToggle: "بخش «امتیاز بده» در خانه", hoTitle: "ترتیب بخش‌های خانه", hoEmpty: "یک بار صفحه خانه را باز کن و برگرد.", hoReset: "بازگرداندن ترتیب پیش‌فرض", ctIcon: "آیکن", ctColour: "رنگ تأکیدی", ctNeedBook: "Audiobookshelf نمی‌تواند مجموعه خالی ذخیره کند, دست‌کم یک کتاب انتخاب کن. بعداً آزادانه اضافه و حذف می‌کنی.", colEditIcon: "تغییر آیکن", ciTitle: "آیکن مجموعه", ciReset: "تطبیق خودکار با نام", colEditDesc: "ویرایش توضیح", colBookForms: ["کتاب","کتاب"], colForms: ["مجموعه","مجموعه‌ها"], ctTitle: "ساخت مجموعه", ctBlank: "مجموعه خالی", ctNew: "مجموعه جدید", ctNewHint: "از الگو", ctNamePh: "نام مجموعه", ctSearch: "جست‌وجوی کتاب یا نویسنده…", ctCreate: "ساختن", ctBack: "‹ بازگشت", ctAddAll: "افزودن همه کتاب‌ها", abTitle: "افزودن کتاب", abAdd: "افزودن", avRemove: "حذف عکس", avSet: "انتخاب عکس…", ugListened: "شنیده‌شده", ugPhoto: "عکس پروفایل", ugNever: "هرگز", ugSeen: "آخرین فعالیت", ugEdit: "ویرایش کاربر", ugDelete: "حذف کاربر", ugAdd: "افزودن عکس", ugChange: "تغییر عکس", ugSortName: "الفبایی", ugSortTime: "پرشنیده‌ترین", ugSortSeen: "فعال‌های اخیر", bsCard: "پیوندهای کتاب", bsHint: "دکمه‌هایی در صفحه کتاب برای جست‌وجوی عنوان در جاهای دیگر. Goodreads و بزرگ‌ترین سایت زبانت از قبل روشن‌اند.", bsFind: "جست‌وجو در", bsGlobal: "همه‌جا", bsLocal: "برای زبان تو", bsOther: "کشورهای دیگر", pgCard: "صفحات بازطراحی‌شده", pgHint: "هر گزینه صفحه‌ای از Audiobookshelf را با نسخه NanoHive جایگزین می‌کند. خاموش کنی، نسخه اصلی برمی‌گردد.", pgNarrators: "کارت‌های گویندگان", pgCollections: "صفحات مجموعه‌ها", pgUsers: "کارت‌های کاربران (تنظیمات)", pgStats: "رتبه‌بندی شنیدن", pgPhoto: "عکس پروفایل در نوار بالا", pgCinematic: "پس‌زمینه سینمایی", pgTransitions: "گذار صفحات", pSearch: "جست‌وجوی تنظیمات…", pSearchNone: "موردی یافت نشد.", erTypeface: "قلم", erPage: "تم صفحه", erText: "رنگ متن", erBg: "پس‌زمینه", erDefault: "پیش‌فرض (ABS)", erAuto: "خودکار", nrForms: ["گوینده","گویندگان"], nrBooksForms: ["کتاب","کتاب"], nrSearch: "فیلتر گویندگان…", nrSortName: "نام", nrSortBooks: "بیشترین کتاب", auForms: ["نویسنده","نویسندگان"], auSearch: "فیلتر نویسندگان…" , lfByAuthor: "نویسنده › ترتیب مجموعه", lfBySeries: "مجموعه › ترتیب" , lfSecFilters: "فیلترهای بیشتر", lfSecSort: "مرتب‌سازی چندگانه", lfAuthor: "نویسنده", lfSeries: "مجموعه", lfTitle: "عنوان", lfYear: "سال", lfAdded: "افزوده‌شده", lfDuration: "مدت", lfNarrator: "گوینده", lfGenre: "ژانر", lfLanguage: "زبان", lfProgress: "پیشرفت", lfPgFinished: "تمام‌شده", lfPgInProgress: "در حال شنیدن", lfPgNot: "شروع‌نشده" , logoUpload: 'بارگذاری از دستگاه…', logoUploaded: 'بارگذاری شد ✓', logoBadType: 'نوع تصویر پشتیبانی نمی‌شود', logoTooBig: 'تصویر خیلی بزرگ است (حداکثر ۴ مگابایت)', logoOr: 'یا', bookPage: 'صفحه کتاب', ratingsToggle: 'امتیازهای جامعه (ستاره‌ها و نقدها)', ratingsHint: 'امتیازهای ستاره‌ای با نقدهای کوتاه در هر صفحه کتاب، مشترک میان همه کاربران این سرور.', searchCard: 'جستجو', gsToggle: 'جستجو در همه کتابخانه‌ها به‌یکباره', gsHint: 'یک فهرست نتایج یکپارچه از همه کتابخانه‌های در دسترس، با نشان کتابخانه روی هر نتیجه. با خاموش کردن، منوی هر کتابخانه بازمی‌گردد.', gsNoResults: 'نتیجه‌ای نیست', gsSearching: 'در حال جستجو…', gsBooks: 'کتاب‌ها', gsSeries: 'مجموعه‌ها', gsAuthors: 'نویسندگان', spTitle: 'صفحه شروع', spHint: 'برنامه همیشه روی نمای انتخابی یک کتابخانه باز شود (فقط این مرورگر). پیوندهای مستقیم هرگز تغییر مسیر نمی‌یابند.', spDefault: 'پیش‌فرض برنامه', spViewHome: 'خانه', spViewLibrary: 'کتابخانه', spViewSeries: 'مجموعه‌ها', spViewCollections: 'کلکسیون‌ها', spViewAuthors: 'نویسندگان', scSet: 'تنظیم جلد مجموعه', scRemove: 'حذف جلد مجموعه', scEditDesc: 'ویرایش توضیح مجموعه', cardStars: 'نشان ستاره روی جلدها', hoverPolish: 'پرداخت کارت‌ها هنگام اشاره (تیرگی نرم‌تر، دکمه برجسته، ستاره‌ها هنگام اشاره)', sectUser: 'تنظیمات شما', sectAdmin: 'مدیریت', lockTitle: 'کلیدهای قابلیت‌ها', lockHint: 'قابلیتی را برای همه کاربران این سرور اجباری خاموش می‌کند. بر تنظیمات شخصی غلبه دارد؛ دیگران پس از بارگذاری بعدی می‌بینند.', lockRatings: 'خاموش کردن امتیازها در همه‌جا', lockCardRatings: 'خاموش کردن ستاره‌های روی جلد', lockGlobalSearch: 'خاموش کردن جستجوی سراسری', lockHoverPolish: 'خاموش کردن پرداخت کارت‌ها', lockHeroCarousel: 'خاموش کردن چرخ‌وفلک خانه', lockedNote: 'توسط مدیر خاموش شد', lfSort: 'امتیاز', lfRated: 'امتیازدار', lfUnrated: 'بدون امتیاز', lfClear: 'پاک کردن', lfNone: 'کتابی مطابقت ندارد', lfSection: 'امتیاز', lfWho: 'امتیازهای چه کسی', lfEveryone: 'همه', sdSave: 'ذخیره', sdClear: 'بازگرداندن توضیح کتاب', sdMore: 'ادامه', sdLess: 'کمتر', shSeries: 'مجموعه', shBy: 'اثر ', shMore: ' و بیشتر', shBookForms: ['کتاب', 'کتاب'], shAvg: 'میانگین کتاب‌ها', shRated: 'امتیازدار', impLabel: 'درون‌ریزی امتیازها', impHint: 'امتیازهایت را از StoryGraph یا Goodreads بیاور. آنجا کتاب‌هایت را به CSV صادر کن و فایل را اینجا انتخاب کن. هر تطابق برای تأیید نشان داده می‌شود و تا تأیید نکنی چیزی ذخیره نمی‌شود.', impBtn: 'انتخاب فایل CSV…', impReading: 'خواندن فایل…', impMatching: 'تطبیق با کتابخانه‌هایت…', impErrUnknown: 'این شبیه خروجی StoryGraph یا Goodreads نیست. باید CSV خودشان باشد، با ستون "Star Rating" یا "My Rating".', impErrEmpty: 'فایل هیچ ردیفی ندارد.', impErrNoRated: 'کتاب امتیازداری در فایل نیست.', impErrRead: 'فایل خوانده نشد.', impErrLoad: 'کتابخانه‌ها بارگذاری نشدند. کمی بعد دوباره تلاش کن.', impErrNoLibs: 'هیچ کتابخانه‌ای در امتیازدهی شرکت ندارد، پس جایی برای درون‌ریزی نیست.', impTitle: 'درون‌ریزی امتیازها', impFrom: 'خروجی %s: %d کتاب، %d از آنها امتیازدار', impLibs: 'جستجو در: %s', impMatched: 'تطبیق‌یافته', impCheck: 'ارزش بررسی دارد', impCheckHint: 'نزدیک اما نامطمئن. تطابق را بررسی کن یا دیگری را انتخاب کن.', impAlready: 'قبلاً امتیاز داده‌ای', impAlreadyHint: 'دست‌نخورده می‌مانند مگر بازنویسی را بخواهی.', impNone: 'در کتابخانه‌ات نیست', impNoneHint: 'چیزی در این سرور با اینها مطابقت ندارد.', impAll: 'همه', impSkip: 'رد کردن این', impDup: 'تکراری', impByTitle: 'عنوان', impYours: 'امتیاز تو: %s', impReviews: 'نقدهای نوشته‌شده هم درون‌ریزی شود (%d)', impOver: 'بازنویسی امتیازهایی که دارم', impGo: 'درون‌ریزی %d امتیاز', impCancel: 'لغو', impCloseBtn: 'بستن', impWorking: 'در حال ذخیره… %d از %d', impDone: '%d امتیاز درون‌ریزی شد.', impFail: '%d ذخیره نشد.', rdTitle: 'چه کسی این کتاب را می‌خواند', rdFin: 'تمام شد', socTitle: 'قابلیت‌های اجتماعی', socHint: 'آنچه این سرور میان کاربران به اشتراک می‌گذارد. زمان شنیدن رتبه‌بندی را تغذیه می‌کند؛ دو مورد دیگر فاش می‌کنند هر کس چه می‌خواند، پس تا روشنشان نکنی خاموش می‌مانند. برای همه یکجا اعمال می‌شود.', socTime: 'زمان شنیدن (رتبه‌بندی سرور)', socContent: 'عناوین برتر در آمار مشترک', socWho: 'چه کسی می‌خواند (صفحات کتاب)', socUsers: 'به ازای کاربر', socUsersHint: 'اگر کسی را خاموش کنی، هیچ‌چیز او به اشتراک گذاشته نمی‌شود، هر چه خودش انتخاب کرده باشد؛ فقط تو می‌توانی برگردانی. «خودش انصراف داد» یعنی آن شخص خودش نه گفت.', socOpted: 'خودش انصراف داد', srvTitle: 'پیش‌فرض‌های سرور', srvHint: 'تنظیمات فعلی‌ات را به‌عنوان پیش‌فرض همه کاربران این سرور ذخیره کن. هر کس همچنان می‌تواند روی آن شخصی‌سازی کند. برای ماندگاری بین به‌روزرسانی‌ها، یک والیوم به /data/nh در کانتینر پوسته وصل کن.', srvSave: 'ذخیره به‌عنوان پیش‌فرض سرور', srvClear: 'پاک کردن پیش‌فرض‌های سرور', srvSaved: 'ذخیره شد', srvCleared: 'پاک شد', srvErr: 'ناموفق: ورود مدیر لازم است', crMode: 'قفسه ادامه خواندن', crCombine: 'ادغام در چرخ‌وفلک', crSeparate: 'به‌صورت قفسه جدا نگه دار', crHide: 'پنهان', fsHint: 'شنیدن خود را با دیگران در این سرور به اشتراک بگذار: جمع زمان و دقیقه‌های روزانه برای رتبه‌بندی، و اگر مدیر روشن کرده باشد، عناوین برترت و اینکه چه کتاب‌هایی را می‌خوانی یا تمام کرده‌ای. به‌طور پیش‌فرض روشن. با خاموش کردن، هر چه به اشتراک گذاشته‌ای پاک می‌شود.' , amfHide: 'پنهان کردن علامت‌گذاری به‌عنوان تمام‌شده' , amfConfirm: 'این کتاب به‌عنوان تمام‌شده علامت‌گذاری شود؟' , fsHint: 'خلاصه‌ای از شنیدن خود (مجموع‌ها و دقیقه‌های روزانه) را با دیگران در این سرور به اشتراک بگذارید, منبع رتبه‌بندی سرور است. به‌طور پیش‌فرض روشن. با خاموش کردن، از رتبه‌بندی و جمع‌های آن حذف می‌شوید و داده‌های اشتراکی پاک می‌شوند.' , srToggle: 'اشتراک‌گذاری اینکه چه کتاب‌هایی می‌خوانی', srHint: 'به آنچه دیگران می‌بینند اضافه می‌کند که الان چه کتاب‌هایی می‌خوانی، کدام‌ها را تمام کرده‌ای و به کدام‌ها بیشتر گوش می‌دهی. به‌طور پیش‌فرض خاموش؛ کلیدهای سرور مدیر هم باید روشن باشند.' , fsHint: 'زمان شنیدن خود (جمع‌ها و دقیقه‌های روزانه) را با دیگران در این سرور به اشتراک بگذار؛ رتبه‌بندی از آن تغذیه می‌کند. به‌طور پیش‌فرض روشن. با خاموش کردن، کاربران دیگر تو را نمی‌بینند؛ مدیران سرور همچنان فعالیت را می‌بینند، با نشان انصراف.', srHint: 'همچنین به دیگران اجازه می‌دهد ببینند چه کتاب‌هایی می‌خوانی، کدام‌ها را تمام کرده‌ای و به کدام‌ها بیشتر گوش می‌دهی. تا وقتی مدیر کلیدهای سرور مربوط را روشن نکند چیزی نمایش داده نمی‌شود.', seshTitle: 'نشست‌های اخیر', seshNow: 'در حال شنیدن' , hcHint: 'امتیازهای خود را به حساب Hardcover بفرستید. کلید API را از تنظیمات حساب Hardcover بچسبانید؛ کلیدها پس از یک سال منقضی می‌شوند.', hcKeyLbl: 'کلید API', hcToggle: 'همگام‌سازی خودکار امتیازهای جدید', hcPushAll: 'ارسال همه امتیازها', hcPushDiff: 'ارسال فقط تفاوت‌ها', hcDone: 'انجام شد: %d همگام شد، %d بدون تطابق', hcNoKey: 'ابتدا کلید API Hardcover خود را بچسبانید' , hcMissHead: 'در Hardcover پیدا نشد:' , hcHint: 'امتیازهای خود را به حساب Hardcover بفرستید. کلید API را از تنظیمات حساب Hardcover بچسبانید.' , tabStyle: 'سبک', tabMain: 'صفحه اصلی', tabBooks: 'کتاب‌ها', tabFeat: 'قابلیت‌ها' , hcImport: 'وارد کردن از Hardcover', impMarkQ: '%d کتاب منطبق اینجا هم به‌عنوان تمام‌شده علامت بخورد؟', impMarkDone: '%d به‌عنوان تمام‌شده علامت خورد.' , impHint: 'امتیازهای خود را از Goodreads، StoryGraph یا Hardcover بیاورید. CSV صادرشده را اینجا انتخاب کنید؛ Hardcover مستقیماً با دکمه روی کارت خودش وارد می‌شود. هر تطابق پیش از ذخیره برای تأیید نشان داده می‌شود.' , impErrUnknown: 'این شبیه خروجی Goodreads، StoryGraph یا Hardcover نیست: ستون‌های عنوان و امتیاز پیدا نشد.' , fsCard: 'اجتماعی', rnToggle: 'نمایش نام من کنار امتیازها', rsToggle: 'اشتراک امتیازهای من با دیگران', socNames: 'نام‌ها کنار امتیازها', rtAnon: 'ناشناس' },
    hi: { cmStopped: "रोका गया", cmScanErrs: "{e} की जाँच नहीं हो सकी (Goodreads त्रुटियाँ)", cmScanBusy: "एक स्कैन पहले से चल रहा है", cmGrErr: "Goodreads उपलब्ध नहीं, फिर कोशिश होगी", cmNoMatchYet: "Goodreads पर कोई मेल नहीं", cmLooking: "Goodreads पर खोज रहा है…", cmToolHint: "Goodreads के पाठक इस किताब के बारे में क्या सोचते हैं। ? मिलान की गई किताब दिखाता है और बदलने देता है।", cmSearchPh: "Goodreads पर खोजें…", cmNoVotes: "अभी कोई रेटिंग नहीं", cmRefresh: "ताज़ा करें", cmScanAll: "सिर्फ़ छूटे हुए नहीं, सब फिर से स्कैन करें", cmScanDone: "पूरा: {m} मिले · {x} नहीं मिले · {s} पहले से थे", cmStop: "रोकें", cmScanLibs: "स्कैन करने वाली लाइब्रेरी", cmScan: "रेटिंग खोजें", lsLongest: "सबसे लंबी किताबें", lsSize: "डिस्क पर आकार", lsDuration: "कुल अवधि", lsGenresN: "शैलियाँ", lsSeriesN: "सीरीज़", lsAuthorsN: "लेखक", lsBooks: "किताबें", lsGlance: "लाइब्रेरी एक नज़र में", cmOpen: "Goodreads पर खोलें", cmDetect: "खोजें", cmAddr: "सहायक का पता", scmTitle: "श्रृंखला पृष्ठ", scmHint: "जब अपना कवर अपलोड न हो तो श्रृंखला शीर्षक के ऊपर बनने वाले कवर का रूप (केवल यह ब्राउज़र)।", scmDeck: "परतदार डेक", cmHint: "Goodreads के पाठक किसी किताब के बारे में क्या सोचते हैं: औसत स्कोर और वोटों की संख्या, किताब के पेज पर सितारों के नीचे और सॉर्ट व फ़िल्टर के रूप में। किताबों के पेज ब्राउज़ करते समय अपने आप भर जाते हैं।", cmGrOff: "Goodreads: सेट नहीं", cmSetup: "अभी सहायक नहीं है? इसे थीम के साथ चलाएँ (नीचे compose पंक्तियाँ), फिर खोजें दबाएँ। इमेज को किसी और तरीके से चलाना भी चलेगा: उसका पता ऊपर चिपकाएँ।", cmGrOn: "Goodreads: जुड़ा हुआ", cmGrDown: "Goodreads: सेट है पर जवाब नहीं दे रहा", cmGrNoNum: "Goodreads: जवाब दे रहा है, पर इस abs-tract बिल्ड में रेटिंग संख्याएँ नहीं हैं (ratings बिल्ड इस्तेमाल करें, README देखें)", cmTitle: "Goodreads रेटिंग", cmWrong: "गलत किताब?", cmFind: "समुदाय रेटिंग खोजें…", cmNone: "कोई मेल नहीं", cmUse: "इसे चुनें", lfCommunity: "Goodreads रेटिंग", mfNatChange: "बदलें…", lsByTime: "सुनने के समय के अनुसार", sbTitle: "सर्वर रैंकिंग", sbWeek: "सप्ताह", sbMonth: "महीना", sbYear: "वर्ष", sbAll: "कुल समय", sbBooks: "पुस्तकें", sbTopBooks: "सबसे ज़्यादा सुनी गईं", sbTotal: "कुल", sbListeners: "श्रोता", sbAvg: "औसत", wkTitle: "सुनने के मिनट, पिछले 7 दिन", wkAvg: "दैनिक औसत", wkRow: "लगातार दिन", rpBadge: "खुली समस्या रिपोर्टें", rpMenu: "समस्या रिपोर्ट करें", rpTitle: "समस्या रिपोर्ट करें", rpWhat: "क्या गड़बड़ है?", rpNote: "एडमिन को और कुछ बताना है? (वैकल्पिक)", rpSend: "रिपोर्ट भेजें", rpSent: "भेज दी गई। धन्यवाद।", rpFail: "भेजा नहीं जा सका", rpMissing: "सामग्री गुम या अधूरी", rpQuality: "खराब ऑडियो गुणवत्ता", rpPlay: "चलता नहीं", rpWrong: "गलत पुस्तक, कवर या मेटाडेटा", rpChapters: "अध्याय गलत हैं", rpOther: "कुछ और", rfRate: "रेट करें", rfSheet: "इस पुस्तक को रेट करें", rfOpen: "पुस्तक पृष्ठ खोलें", rfPickHint: "रेटिंग चुनें", rfTitle: "जो पूरा किया उसे रेट करें", ysFinished: "हाल में पूरी हुईं", ysAlmost: "लगभग पूरी", ysMarkDone: "पूर्ण के रूप में चिह्नित करें", ysEditDate: "समाप्ति तिथि बदलें", ysThisWeek: "इस सप्ताह", ysBestDay: "सबसे अच्छा दिन", ysTitle: "आपका सुनना", fsTitle: "सर्वर रैंकिंग", fsCard: "सर्वर रैंकिंग", fsToggle: "मेरे सुनने के आँकड़े साझा करें", fsJoin: "सर्वर रैंकिंग में दिखने के लिए कस्टमाइज़ेशन पैनल में “मेरे सुनने के आँकड़े साझा करें” चालू करें।", fsWaiting: "साझा करना चालू है, आपकी सारांश जल्द ही सबके साथ सर्वर रैंकिंग में जुड़ जाएगी।", fsHint: "अपने सुनने का सारांश (कुल और प्रतिदिन मिनट) इस सर्वर के अन्य लोगों के साथ साझा करें, यही सर्वर रैंकिंग को चलाता है। डिफ़ॉल्ट रूप से चालू। बंद करने पर आप रैंकिंग और उसके योगों से हट जाते हैं, और साझा डेटा मिट जाता है।", yirBtn: "वर्ष की झलक", yirTitle: "आपके सुनने का वर्ष", yirYear: "पिछले 12 महीने", yirDays: "सुनने वाले दिन", yirBest: "सबसे अच्छा दिन", yirMonths: "महीने के अनुसार", yirNone: "अभी कोई सुनने का डेटा नहीं।", ssscmDeck: "परतदार डेक", scmFirst: "पहली पुस्तक का कवर", scmGrid: "कवर ग्रिड (डिफ़ॉल्ट)", scmOff: "छिपा" , lsTitle: "सर्वर आँकड़े", lsAll: "सभी लाइब्रेरी", lsPlayed: "चलाई गई पुस्तकें", lsBest: "सर्वोच्च रेटेड", lsGenres: "शीर्ष विधाएँ", srvPick: "क्या सहेजें:", srvGRest: "सुविधाएँ और व्यवहार", aeBtn: "लेखकों को व्यवस्थित करें", aeTitle: "बिना पुस्तक वाले लेखक", aeNone: "यहाँ हर लेखक के पास कम से कम एक पुस्तक है।", aeRemove: "हटाएँ", aeDone: "हटाया गया", aeWorking: "हटाया जा रहा है…", rpCard: "रिपोर्ट की गई समस्याएँ", rpHint: "पुस्तक पृष्ठ से उपयोगकर्ताओं द्वारा रिपोर्ट की गई समस्याएँ। हल करने पर सूची से हट जाती है।", rpEmpty: "कोई रिपोर्ट नहीं।", rpResolve: "हल करें", rpOpen: "पुस्तक खोलें", apCard: "प्लेबैक", apToggle: "श्रृंखला की अगली पुस्तक स्वतः चलाएँ", apHint: "पुस्तक समाप्त होने पर उसी श्रृंखला की अगली स्वतः शुरू होती है। केवल वे पुस्तकें जो आपके पास हैं और अधूरी हैं।", apNext: "श्रृंखला में अगली", fdToggle: "आँकड़ों में पूर्ण पुस्तकों के उपकरण", ysNoDate: "तिथि नहीं", hmShowing: "चार्ट दिखा रहा है", ysStreak: "लगातार दिन", ysCurrent: "वर्तमान", ysLongest: "सबसे लंबा", ysDaysTotal: "कुल दिन", ysPace: "गति", ysPerDay: "प्रति श्रवण दिवस", ysWhen: "आप कब सुनते हैं", ysTopBooks: "सबसे अधिक सुनी पुस्तकें", ysTopAuthors: "सबसे अधिक सुने लेखक", ysTopNarrators: "सबसे अधिक सुने वाचक", rfToggle: "होम पर “रेट करें” खंड", hoTitle: "होम खंडों का क्रम", hoEmpty: "एक बार होम खोलें, फिर यहाँ लौटें।", hoReset: "डिफ़ॉल्ट क्रम बहाल करें", ctIcon: "आइकन", ctColour: "एक्सेंट", ctNeedBook: "Audiobookshelf खाली संग्रह सहेज नहीं सकता, कम से कम एक पुस्तक चुनें। बाद में स्वतंत्र रूप से जोड़-हटा सकते हैं।", colEditIcon: "आइकन बदलें", ciTitle: "संग्रह का आइकन", ciReset: "नाम से स्वतः मिलाएँ", colEditDesc: "विवरण संपादित करें", colBookForms: ["पुस्तक","पुस्तकें"], colForms: ["संग्रह","संग्रह"], ctTitle: "संग्रह बनाएँ", ctBlank: "खाली संग्रह", ctNew: "नया संग्रह", ctNewHint: "टेम्पलेट से", ctNamePh: "संग्रह का नाम", ctSearch: "पुस्तकें या लेखक खोजें…", ctCreate: "बनाएँ", ctBack: "‹ वापस", ctAddAll: "सभी पुस्तकें जोड़ें", abTitle: "पुस्तकें जोड़ें", abAdd: "जोड़ें", avRemove: "फ़ोटो हटाएँ", avSet: "फ़ोटो चुनें…", ugListened: "सुना गया", ugPhoto: "प्रोफ़ाइल फ़ोटो", ugNever: "कभी नहीं", ugSeen: "अंतिम सक्रियता", ugEdit: "उपयोगकर्ता संपादित करें", ugDelete: "उपयोगकर्ता हटाएँ", ugAdd: "फ़ोटो जोड़ें", ugChange: "फ़ोटो बदलें", ugSortName: "अ–ज्ञ", ugSortTime: "सबसे अधिक सुना", ugSortSeen: "हाल में सक्रिय", bsCard: "पुस्तक लिंक", bsHint: "पुस्तक पृष्ठ पर शीर्षक को अन्यत्र खोजने के बटन। Goodreads और आपकी भाषा की सबसे बड़ी साइट पहले से चालू हैं।", bsFind: "यहाँ खोजें", bsGlobal: "हर जगह", bsLocal: "आपकी भाषा के लिए", bsOther: "अन्य देश", pgCard: "पुनर्निर्मित पृष्ठ", pgHint: "हर विकल्प Audiobookshelf पृष्ठ को NanoHive संस्करण से बदलता है। बंद करने पर मूल वापस आ जाता है।", pgNarrators: "वाचक कार्ड", pgCollections: "संग्रह पृष्ठ", pgUsers: "उपयोगकर्ता कार्ड (सेटिंग्स)", pgStats: "श्रवण रैंकिंग", pgPhoto: "शीर्ष पट्टी में प्रोफ़ाइल फ़ोटो", pgCinematic: "सिनेमाई पृष्ठभूमि", pgTransitions: "पृष्ठ संक्रमण", pSearch: "सेटिंग्स खोजें…", pSearchNone: "कोई मेल नहीं।", erTypeface: "टाइपफ़ेस", erPage: "पृष्ठ थीम", erText: "पाठ का रंग", erBg: "पृष्ठभूमि", erDefault: "डिफ़ॉल्ट (ABS)", erAuto: "स्वतः", nrForms: ["वाचक","वाचक"], nrBooksForms: ["पुस्तक","पुस्तकें"], nrSearch: "वाचक फ़िल्टर करें…", nrSortName: "नाम", nrSortBooks: "सबसे अधिक पुस्तकें", auForms: ["लेखक","लेखक"], auSearch: "लेखक फ़िल्टर करें…" , lfByAuthor: "लेखक › श्रृंखला क्रम", lfBySeries: "श्रृंखला › क्रम" , lfSecFilters: "और फ़िल्टर", lfSecSort: "बहु-स्तरीय क्रम", lfAuthor: "लेखक", lfSeries: "श्रृंखला", lfTitle: "शीर्षक", lfYear: "वर्ष", lfAdded: "जोड़ा गया", lfDuration: "अवधि", lfNarrator: "वाचक", lfGenre: "शैली", lfLanguage: "भाषा", lfProgress: "प्रगति", lfPgFinished: "पूर्ण", lfPgInProgress: "जारी", lfPgNot: "शुरू नहीं" , logoUpload: 'डिवाइस से अपलोड करें…', logoUploaded: 'अपलोड हो गया ✓', logoBadType: 'असमर्थित छवि प्रकार', logoTooBig: 'छवि बहुत बड़ी है (अधिकतम 4 MB)', logoOr: 'या', bookPage: 'पुस्तक पृष्ठ', ratingsToggle: 'समुदाय की रेटिंग (सितारे और समीक्षाएँ)', ratingsHint: 'हर पुस्तक पृष्ठ पर छोटी समीक्षाओं के साथ सितारा रेटिंग, इस सर्वर के सभी उपयोगकर्ताओं के साथ साझा।', searchCard: 'खोज', gsToggle: 'सभी लाइब्रेरियों में एक साथ खोजें', gsHint: 'आपकी सभी सुलभ लाइब्रेरियों की एक संयुक्त परिणाम सूची, हर परिणाम पर लाइब्रेरी बैज। बंद करने पर प्रति-लाइब्रेरी मेनू लौट आता है।', gsNoResults: 'कोई परिणाम नहीं', gsSearching: 'खोज रहा है…', gsBooks: 'पुस्तकें', gsSeries: 'श्रृंखलाएँ', gsAuthors: 'लेखक', spTitle: 'आरंभ पृष्ठ', spHint: 'ऐप हमेशा चुने हुए लाइब्रेरी दृश्य पर खोलें (केवल यह ब्राउज़र)। सीधे लिंक कभी पुनर्निर्देशित नहीं होते।', spDefault: 'ऐप डिफ़ॉल्ट', spViewHome: 'होम', spViewLibrary: 'लाइब्रेरी', spViewSeries: 'श्रृंखलाएँ', spViewCollections: 'संग्रह', spViewAuthors: 'लेखक', scSet: 'श्रृंखला कवर सेट करें', scRemove: 'श्रृंखला कवर हटाएँ', scEditDesc: 'श्रृंखला विवरण संपादित करें', cardStars: 'कवर पर सितारा बैज', hoverPolish: 'कार्ड होवर परिष्करण (नरम डिमिंग, हाइलाइट प्ले, होवर पर सितारे)', sectUser: 'आपकी सेटिंग्स', sectAdmin: 'प्रशासन', lockTitle: 'फ़ीचर स्विच', lockHint: 'इस सर्वर के सभी उपयोगकर्ताओं के लिए किसी फ़ीचर को जबरन बंद करता है। व्यक्तिगत सेटिंग्स पर भारी; दूसरों को अगली रीलोड के बाद दिखेगा।', lockRatings: 'रेटिंग हर जगह बंद करें', lockCardRatings: 'कवर पर सितारे बंद करें', lockGlobalSearch: 'वैश्विक खोज बंद करें', lockHoverPolish: 'कार्ड परिष्करण बंद करें', lockHeroCarousel: 'होम कैरोसेल बंद करें', lockedNote: 'व्यवस्थापक ने बंद किया', lfSort: 'रेटिंग', lfRated: 'रेट की गई', lfUnrated: 'बिना रेटिंग', lfClear: 'साफ़ करें', lfNone: 'कोई पुस्तक मेल नहीं खाती', lfSection: 'रेटिंग', lfWho: 'किसकी रेटिंग', lfEveryone: 'सभी', sdSave: 'सहेजें', sdClear: 'पुस्तक विवरण बहाल करें', sdMore: 'और पढ़ें', sdLess: 'कम दिखाएँ', shSeries: 'श्रृंखला', shBy: 'लेखक: ', shMore: ' और अधिक', shBookForms: ['पुस्तक', 'पुस्तकें'], shAvg: 'पुस्तकों का औसत', shRated: 'रेट की गई', impLabel: 'रेटिंग आयात करें', impHint: 'अपनी रेटिंग StoryGraph या Goodreads से लाएँ। वहाँ पुस्तकों को CSV में निर्यात करें और फ़ाइल यहाँ चुनें। हर मिलान पुष्टि के लिए दिखाया जाता है और आपकी पुष्टि से पहले कुछ भी सहेजा नहीं जाता।', impBtn: 'CSV फ़ाइल चुनें…', impReading: 'फ़ाइल पढ़ी जा रही है…', impMatching: 'आपकी लाइब्रेरियों से मिलान…', impErrUnknown: 'यह StoryGraph या Goodreads का निर्यात नहीं लगता। यह उनका CSV होना चाहिए, "Star Rating" या "My Rating" कॉलम के साथ।', impErrEmpty: 'फ़ाइल में कोई पंक्ति नहीं है।', impErrNoRated: 'फ़ाइल में कोई रेट की गई पुस्तक नहीं है।', impErrRead: 'फ़ाइल पढ़ी नहीं जा सकी।', impErrLoad: 'आपकी लाइब्रेरियाँ लोड नहीं हो सकीं। थोड़ी देर में फिर आज़माएँ।', impErrNoLibs: 'कोई लाइब्रेरी रेटिंग में भाग नहीं लेती, इसलिए आयात की जगह नहीं है।', impTitle: 'रेटिंग आयात करें', impFrom: '%s निर्यात: %d पुस्तकें, जिनमें %d रेट की गई', impLibs: 'खोज रहा है: %s', impMatched: 'मेल खाईं', impCheck: 'देखने लायक', impCheckHint: 'करीब लेकिन अनिश्चित। मिलान जाँचें या दूसरा चुनें।', impAlready: 'आपने पहले ही रेट किया', impAlreadyHint: 'जब तक आप ओवरराइट न कहें, अछूती रहती हैं।', impNone: 'आपकी लाइब्रेरी में नहीं', impNoneHint: 'इस सर्वर पर इनसे कुछ मेल नहीं खाता।', impAll: 'सभी', impSkip: 'इसे छोड़ें', impDup: 'डुप्लिकेट', impByTitle: 'शीर्षक', impYours: 'आपकी: %s', impReviews: 'लिखी समीक्षाएँ भी आयात करें (%d)', impOver: 'मेरी मौजूदा रेटिंग ओवरराइट करें', impGo: '%d रेटिंग आयात करें', impCancel: 'रद्द करें', impCloseBtn: 'बंद करें', impWorking: 'सहेजा जा रहा… %d में से %d', impDone: '%d रेटिंग आयात हुईं।', impFail: '%d सहेजी नहीं जा सकीं।', rdTitle: 'यह पुस्तक कौन पढ़ रहा है', rdFin: 'पूर्ण', socTitle: 'सामाजिक सुविधाएँ', socHint: 'यह सर्वर उपयोगकर्ताओं के बीच क्या साझा करता है। सुनने का समय रैंकिंग चलाता है; बाकी दो बताती हैं कि कौन क्या पढ़ रहा है, इसलिए जब तक आप चालू न करें, बंद रहती हैं। सब पर एक साथ लागू।', socTime: 'सुनने का समय (सर्वर रैंकिंग)', socContent: 'साझा आँकड़ों में शीर्ष शीर्षक', socWho: 'कौन पढ़ रहा है (पुस्तक पृष्ठ)', socUsers: 'प्रति उपयोगकर्ता', socUsersHint: 'किसी को बंद करने पर उसका कुछ भी साझा नहीं होता, चाहे उसने खुद जो चुना हो; केवल आप वापस चालू कर सकते हैं। "स्वयं मना किया" का अर्थ है कि उस व्यक्ति ने खुद मना किया।', socOpted: 'स्वयं मना किया', srvTitle: 'सर्वर डिफ़ॉल्ट', srvHint: 'अपनी वर्तमान सेटिंग्स इस सर्वर के सभी उपयोगकर्ताओं के लिए डिफ़ॉल्ट के रूप में सहेजें। हर कोई ऊपर से निजीकरण कर सकता है। अपडेट में बनाए रखने के लिए थीम कंटेनर में /data/nh पर वॉल्यूम माउंट करें।', srvSave: 'सर्वर डिफ़ॉल्ट के रूप में सहेजें', srvClear: 'सर्वर डिफ़ॉल्ट साफ़ करें', srvSaved: 'सहेजा गया', srvCleared: 'साफ़ हुआ', srvErr: 'विफल: व्यवस्थापक लॉगिन आवश्यक', crMode: 'पढ़ना जारी रखें शेल्फ़', crCombine: 'कैरोसेल में मिलाएँ', crSeparate: 'अलग शेल्फ़ रखें', crHide: 'छिपा', fsHint: 'अपनी सुनवाई इस सर्वर के अन्य लोगों के साथ साझा करें: रैंकिंग के लिए कुल समय और प्रतिदिन मिनट, और यदि व्यवस्थापक ने चालू किया हो तो आपके शीर्ष शीर्षक और कौन सी पुस्तकें आप पढ़ रहे हैं या पूरी कर चुके हैं। डिफ़ॉल्ट रूप से चालू। बंद करने पर साझा किया सब कुछ हट जाता है।' , amfHide: 'पूर्ण चिह्नित करें छिपाएँ' , amfConfirm: 'इस पुस्तक को पूर्ण चिह्नित करें?' , fsHint: 'अपने सुनने का सारांश (कुल और प्रतिदिन मिनट) इस सर्वर के अन्य लोगों के साथ साझा करें, यही सर्वर रैंकिंग को चलाता है। डिफ़ॉल्ट रूप से चालू। बंद करने पर आप रैंकिंग और उसके योगों से हट जाते हैं, और साझा डेटा मिट जाता है।' , srToggle: 'साझा करें कि आप कौन सी पुस्तकें पढ़ते हैं', srHint: 'दूसरों को दिखने वाली जानकारी में जोड़ता है कि आप अभी कौन सी पुस्तकें पढ़ रहे हैं, कौन सी पूरी कीं और कौन सी सबसे अधिक सुनते हैं। डिफ़ॉल्ट रूप से बंद; व्यवस्थापक के सर्वर स्विच भी चालू होने चाहिए।' , fsHint: 'अपना सुनने का समय (कुल और प्रतिदिन मिनट) इस सर्वर के अन्य लोगों के साथ साझा करें; यही रैंकिंग चलाता है। डिफ़ॉल्ट रूप से चालू। बंद करने पर अन्य उपयोगकर्ता आपको नहीं देखते; सर्वर व्यवस्थापक गतिविधि फिर भी देखते हैं, ऑप्ट-आउट चिह्न के साथ।', srHint: 'दूसरों को यह भी दिखाता है कि आप कौन सी पुस्तकें पढ़ रहे हैं, कौन सी पूरी कीं और कौन सी सबसे अधिक सुनते हैं। जब तक व्यवस्थापक संबंधित सर्वर स्विच चालू न करे, कुछ नहीं दिखता।', seshTitle: 'हाल के सत्र', seshNow: 'अभी सुन रहे हैं' , hcHint: 'अपनी रेटिंग अपने Hardcover खाते में भेजें। Hardcover खाता सेटिंग से API कुंजी चिपकाएँ; कुंजियाँ एक साल बाद समाप्त हो जाती हैं।', hcKeyLbl: 'API कुंजी', hcToggle: 'नई रेटिंग अपने आप सिंक करें', hcPushAll: 'सभी रेटिंग भेजें', hcPushDiff: 'सिर्फ़ अंतर भेजें', hcDone: 'हो गया: %d सिंक, %d बिना मेल', hcNoKey: 'पहले अपनी Hardcover API कुंजी चिपकाएँ' , hcMissHead: 'Hardcover पर नहीं मिला:' , hcHint: 'अपनी रेटिंग अपने Hardcover खाते में भेजें। Hardcover खाता सेटिंग से API कुंजी चिपकाएँ।' , tabStyle: 'स्टाइल', tabMain: 'मुख्य पेज', tabBooks: 'किताबें', tabFeat: 'फ़ीचर' , hcImport: 'Hardcover से आयात करें', impMarkQ: 'मेल खाई %d किताबों को यहाँ भी पूर्ण चिह्नित करें?', impMarkDone: '%d पूर्ण चिह्नित की गईं।' , impHint: 'अपनी रेटिंग Goodreads, StoryGraph या Hardcover से लाएँ। निर्यात की गई CSV यहाँ चुनें; Hardcover अपने कार्ड के बटन से सीधे आयात करता है। सहेजने से पहले हर मेल पुष्टि के लिए दिखाया जाता है।' , impErrUnknown: 'यह Goodreads, StoryGraph या Hardcover का निर्यात नहीं लगता: शीर्षक और रेटिंग कॉलम नहीं मिले।' , fsCard: 'सोशल', rnToggle: 'रेटिंग के साथ मेरा नाम दिखाएँ', rsToggle: 'मेरी रेटिंग दूसरों के साथ साझा करें', socNames: 'रेटिंग के साथ नाम', rtAnon: 'गुमनाम' },
    bn: { cmStopped: "থামানো হয়েছে", cmScanErrs: "{e}টি যাচাই করা যায়নি (Goodreads ত্রুটি)", cmScanBusy: "একটি স্ক্যান আগে থেকেই চলছে", cmGrErr: "Goodreads-এ পৌঁছানো যাচ্ছে না, আবার চেষ্টা হবে", cmNoMatchYet: "Goodreads-এ কোনো মিল নেই", cmLooking: "Goodreads-এ খোঁজা হচ্ছে…", cmToolHint: "Goodreads-এর পাঠকরা এই বই সম্পর্কে কী ভাবেন। ? মিলে যাওয়া বই দেখায় এবং বদলাতে দেয়।", cmSearchPh: "Goodreads-এ খুঁজুন…", cmNoVotes: "এখনও রেটিং নেই", cmRefresh: "রিফ্রেশ", cmScanAll: "শুধু অনুপস্থিত নয়, সব আবার স্ক্যান করুন", cmScanDone: "সম্পন্ন: {m} মিলেছে · {x} পাওয়া যায়নি · {s} আগেই ছিল", cmStop: "থামান", cmScanLibs: "স্ক্যান করার লাইব্রেরি", cmScan: "রেটিং খুঁজুন", lsLongest: "দীর্ঘতম বই", lsSize: "ডিস্কে আকার", lsDuration: "মোট দৈর্ঘ্য", lsGenresN: "ধরন", lsSeriesN: "সিরিজ", lsAuthorsN: "লেখক", lsBooks: "বই", lsGlance: "লাইব্রেরি এক নজরে", cmOpen: "Goodreads-এ খুলুন", cmDetect: "খুঁজুন", cmAddr: "সহায়কের ঠিকানা", scmTitle: "সিরিজ পাতা", scmHint: "নিজস্ব প্রচ্ছদ আপলোড না থাকলে সিরিজের নামের উপরে তৈরি প্রচ্ছদ যেমন দেখায় (শুধু এই ব্রাউজার)।", scmDeck: "স্তরের ডেক", cmHint: "Goodreads-এর পাঠকরা একটি বই সম্পর্কে কী ভাবেন: গড় স্কোর ও ভোটের সংখ্যা, বইয়ের পাতায় তারার নিচে এবং সাজানো ও ফিল্টার হিসেবে। বইয়ের পাতা ব্রাউজ করার সময় নিজে থেকেই পূরণ হয়।", cmGrOff: "Goodreads: সেট করা নেই", cmSetup: "এখনও সহায়ক নেই? থিমের পাশে এটি চালান (নিচের compose লাইন), তারপর খুঁজুন চাপুন। ইমেজটি অন্য যেকোনোভাবে চালালেও চলবে: তার ঠিকানা উপরে পেস্ট করুন।", cmGrOn: "Goodreads: সংযুক্ত", cmGrDown: "Goodreads: সেট আছে কিন্তু সাড়া দিচ্ছে না", cmGrNoNum: "Goodreads: সাড়া দিচ্ছে, কিন্তু এই abs-tract বিল্ডে রেটিং সংখ্যা নেই (ratings বিল্ড ব্যবহার করুন, README দেখুন)", cmTitle: "Goodreads রেটিং", cmWrong: "ভুল বই?", cmFind: "কমিউনিটি রেটিং খুঁজুন…", cmNone: "কোনো মিল নেই", cmUse: "এটি ব্যবহার করুন", lfCommunity: "Goodreads রেটিং", mfNatChange: "পরিবর্তন…", lsByTime: "শোনার সময় অনুযায়ী", sbTitle: "সার্ভার র‍্যাঙ্কিং", sbWeek: "সপ্তাহ", sbMonth: "মাস", sbYear: "বছর", sbAll: "সব সময়", sbBooks: "বই", sbTopBooks: "সবচেয়ে বেশি শোনা", sbTotal: "মোট", sbListeners: "শ্রোতা", sbAvg: "গড়", wkTitle: "শোনার মিনিট, গত ৭ দিন", wkAvg: "দৈনিক গড়", wkRow: "টানা দিন", rpBadge: "খোলা সমস্যা প্রতিবেদন", rpMenu: "সমস্যা জানান", rpTitle: "সমস্যা জানান", rpWhat: "কী সমস্যা?", rpNote: "অ্যাডমিনকে আর কিছু জানাতে চান? (ঐচ্ছিক)", rpSend: "প্রতিবেদন পাঠান", rpSent: "পাঠানো হয়েছে। ধন্যবাদ।", rpFail: "পাঠানো যায়নি", rpMissing: "বিষয়বস্তু নেই বা অসম্পূর্ণ", rpQuality: "খারাপ অডিও মান", rpPlay: "চালানো যাচ্ছে না", rpWrong: "ভুল বই, প্রচ্ছদ বা মেটাডেটা", rpChapters: "অধ্যায় ভুল", rpOther: "অন্য কিছু", rfRate: "রেট দিন", rfSheet: "এই বইটি রেট দিন", rfOpen: "বইয়ের পাতা খুলুন", rfPickHint: "একটি রেটিং বাছুন", rfTitle: "যা শেষ করেছেন তা রেট দিন", ysFinished: "সম্প্রতি শেষ হওয়া", ysAlmost: "প্রায় শেষ", ysMarkDone: "সম্পন্ন হিসেবে চিহ্নিত করুন", ysEditDate: "শেষের তারিখ বদলান", ysThisWeek: "এই সপ্তাহে", ysBestDay: "সেরা দিন", ysTitle: "আপনার শোনা", fsTitle: "সার্ভার র‍্যাঙ্কিং", fsCard: "সার্ভার র‍্যাঙ্কিং", fsToggle: "আমার শোনার পরিসংখ্যান শেয়ার করুন", fsJoin: "সার্ভার র‍্যাঙ্কিংয়ে দেখা দিতে কাস্টমাইজেশন প্যানেলে “আমার শোনার পরিসংখ্যান শেয়ার করুন” চালু করুন।", fsWaiting: "শেয়ারিং চালু, আপনার সারাংশ শিগগিরই সবার সঙ্গে সার্ভার র‍্যাঙ্কিংয়ে যুক্ত হবে।", fsHint: "আপনার শোনার সারাংশ (মোট ও দৈনিক মিনিট) এই সার্ভারের অন্যদের সঙ্গে শেয়ার করুন, এটিই সার্ভার র‍্যাঙ্কিং চালায়। ডিফল্টে চালু। বন্ধ করলে আপনি র‍্যাঙ্কিং ও তার যোগফল থেকে বাদ পড়বেন, শেয়ার করা ডেটা মুছে যাবে।", yirBtn: "বছরের ফিরে দেখা", yirTitle: "আপনার শোনার বছর", yirYear: "গত ১২ মাস", yirDays: "শোনার দিন", yirBest: "সেরা দিন", yirMonths: "মাস অনুযায়ী", yirNone: "এখনও শোনার ডেটা নেই।", ssscmDeck: "স্তরের ডেক", scmFirst: "প্রথম বইয়ের প্রচ্ছদ", scmGrid: "প্রচ্ছদ গ্রিড (ডিফল্ট)", scmOff: "লুকানো" , lsTitle: "সার্ভার পরিসংখ্যান", lsAll: "সব লাইব্রেরি", lsPlayed: "চালানো বই", lsBest: "সেরা রেটিং", lsGenres: "শীর্ষ ধরন", srvPick: "কী সংরক্ষণ হবে:", srvGRest: "বৈশিষ্ট্য ও আচরণ", aeBtn: "লেখক গোছান", aeTitle: "বইবিহীন লেখক", aeNone: "এখানে প্রত্যেক লেখকের অন্তত একটি বই আছে।", aeRemove: "সরান", aeDone: "সরানো হয়েছে", aeWorking: "সরানো হচ্ছে…", rpCard: "জানানো সমস্যা", rpHint: "বইয়ের পাতা থেকে ব্যবহারকারীদের জানানো সমস্যা। সমাধান করলে তালিকা থেকে চলে যায়।", rpEmpty: "কোনো রিপোর্ট নেই।", rpResolve: "সমাধান", rpOpen: "বই খুলুন", apCard: "প্লেব্যাক", apToggle: "সিরিজের পরের বই স্বয়ংক্রিয়ভাবে চালান", apHint: "একটি বই শেষ হলে একই সিরিজের পরেরটি নিজে থেকে শুরু হয়। শুধু আপনার লাইব্রেরিতে থাকা, অসমাপ্ত বই।", apNext: "সিরিজের পরেরটি", fdToggle: "পরিসংখ্যানে শেষ-করা বইয়ের সরঞ্জাম", ysNoDate: "তারিখ নেই", hmShowing: "চার্ট দেখাচ্ছে", ysStreak: "টানা দিন", ysCurrent: "বর্তমান", ysLongest: "দীর্ঘতম", ysDaysTotal: "মোট দিন", ysPace: "গতি", ysPerDay: "প্রতি শোনার দিনে", ysWhen: "আপনি কখন শোনেন", ysTopBooks: "সবচেয়ে বেশি শোনা বই", ysTopAuthors: "সবচেয়ে বেশি শোনা লেখক", ysTopNarrators: "সবচেয়ে বেশি শোনা পাঠক", rfToggle: "হোমে “রেট দিন” অংশ", hoTitle: "হোম অংশের ক্রম", hoEmpty: "একবার হোম খুলে আবার এখানে আসুন।", hoReset: "ডিফল্ট ক্রম ফেরান", ctIcon: "আইকন", ctColour: "অ্যাকসেন্ট", ctNeedBook: "Audiobookshelf খালি সংগ্রহ সংরক্ষণ করতে পারে না, অন্তত একটি বই বাছুন। পরে ইচ্ছেমতো যোগ-বাদ করা যাবে।", colEditIcon: "আইকন বদলান", ciTitle: "সংগ্রহের আইকন", ciReset: "নাম অনুযায়ী স্বয়ংক্রিয় মিল", colEditDesc: "বিবরণ সম্পাদনা", colBookForms: ["বই","বই"], colForms: ["সংগ্রহ","সংগ্রহ"], ctTitle: "সংগ্রহ তৈরি করুন", ctBlank: "খালি সংগ্রহ", ctNew: "নতুন সংগ্রহ", ctNewHint: "টেমপ্লেট থেকে", ctNamePh: "সংগ্রহের নাম", ctSearch: "বই বা লেখক খুঁজুন…", ctCreate: "তৈরি", ctBack: "‹ পিছনে", ctAddAll: "সব বই যোগ করুন", abTitle: "বই যোগ করুন", abAdd: "যোগ", avRemove: "ছবি সরান", avSet: "ছবি বাছুন…", ugListened: "শোনা হয়েছে", ugPhoto: "প্রোফাইল ছবি", ugNever: "কখনো না", ugSeen: "সর্বশেষ সক্রিয়", ugEdit: "ব্যবহারকারী সম্পাদনা", ugDelete: "ব্যবহারকারী মুছুন", ugAdd: "ছবি যোগ", ugChange: "ছবি বদলান", ugSortName: "অ–হ", ugSortTime: "সবচেয়ে বেশি শোনা", ugSortSeen: "সম্প্রতি সক্রিয়", bsCard: "বইয়ের লিংক", bsHint: "বইয়ের পাতায় শিরোনাম অন্যত্র খোঁজার বোতাম। Goodreads ও আপনার ভাষার বৃহত্তম সাইট আগে থেকেই চালু।", bsFind: "খুঁজুন", bsGlobal: "সব জায়গায়", bsLocal: "আপনার ভাষার জন্য", bsOther: "অন্যান্য দেশ", pgCard: "নতুন নকশার পাতা", pgHint: "প্রতিটি বিকল্প Audiobookshelf-এর পাতা NanoHive সংস্করণে বদলে দেয়। বন্ধ করলে আসলটি ফিরে আসে।", pgNarrators: "পাঠকের কার্ড", pgCollections: "সংগ্রহের পাতা", pgUsers: "ব্যবহারকারীর কার্ড (সেটিংস)", pgStats: "শোনার র‍্যাঙ্কিং", pgPhoto: "উপরের বারে প্রোফাইল ছবি", pgCinematic: "সিনেমাটিক পটভূমি", pgTransitions: "পাতা পরিবর্তন", pSearch: "সেটিংস খুঁজুন…", pSearchNone: "কোনো মিল নেই।", erTypeface: "টাইপফেস", erPage: "পাতার থিম", erText: "লেখার রং", erBg: "পটভূমি", erDefault: "ডিফল্ট (ABS)", erAuto: "স্বয়ংক্রিয়", nrForms: ["পাঠক","পাঠক"], nrBooksForms: ["বই","বই"], nrSearch: "পাঠক ফিল্টার…", nrSortName: "নাম", nrSortBooks: "সবচেয়ে বেশি বই", auForms: ["লেখক","লেখক"], auSearch: "লেখক ফিল্টার…" , lfByAuthor: "লেখক › সিরিজের ক্রম", lfBySeries: "সিরিজ › ক্রম" , lfSecFilters: "আরও ফিল্টার", lfSecSort: "বহুস্তর সাজানো", lfAuthor: "লেখক", lfSeries: "সিরিজ", lfTitle: "শিরোনাম", lfYear: "বছর", lfAdded: "যোগ হয়েছে", lfDuration: "দৈর্ঘ্য", lfNarrator: "পাঠক", lfGenre: "ধরন", lfLanguage: "ভাষা", lfProgress: "অগ্রগতি", lfPgFinished: "শেষ", lfPgInProgress: "চলছে", lfPgNot: "শুরু হয়নি" , logoUpload: 'ডিভাইস থেকে আপলোড করুন…', logoUploaded: 'আপলোড হয়েছে ✓', logoBadType: 'অসমর্থিত ছবির ধরন', logoTooBig: 'ছবিটি খুব বড় (সর্বোচ্চ ৪ MB)', logoOr: 'অথবা', bookPage: 'বইয়ের পাতা', ratingsToggle: 'কমিউনিটি রেটিং (তারা ও রিভিউ)', ratingsHint: 'প্রতিটি বইয়ের পাতায় ছোট রিভিউসহ তারা রেটিং, এই সার্ভারের সব ব্যবহারকারীর সাথে ভাগ করা।', searchCard: 'অনুসন্ধান', gsToggle: 'সব লাইব্রেরিতে একসাথে খুঁজুন', gsHint: 'আপনার সব লাইব্রেরি জুড়ে একটি সম্মিলিত ফলাফলের তালিকা, প্রতিটি ফলাফলে লাইব্রেরি ব্যাজ। বন্ধ করলে লাইব্রেরি-প্রতি মেনু ফিরে আসে।', gsNoResults: 'কোনো ফলাফল নেই', gsSearching: 'খোঁজা হচ্ছে…', gsBooks: 'বই', gsSeries: 'সিরিজ', gsAuthors: 'লেখক', spTitle: 'শুরুর পাতা', spHint: 'অ্যাপটি সবসময় নির্বাচিত লাইব্রেরি ভিউতে খুলুন (শুধু এই ব্রাউজার)। সরাসরি লিংক কখনো রিডাইরেক্ট হয় না।', spDefault: 'অ্যাপের ডিফল্ট', spViewHome: 'হোম', spViewLibrary: 'লাইব্রেরি', spViewSeries: 'সিরিজ', spViewCollections: 'সংগ্রহ', spViewAuthors: 'লেখক', scSet: 'সিরিজের কভার নির্ধারণ', scRemove: 'সিরিজের কভার সরান', scEditDesc: 'সিরিজের বিবরণ সম্পাদনা', cardStars: 'কভারে তারা ব্যাজ', hoverPolish: 'কার্ড হোভার পরিমার্জন (নরম আঁধার, হাইলাইট প্লে, হোভারে তারা)', sectUser: 'আপনার সেটিংস', sectAdmin: 'প্রশাসন', lockTitle: 'ফিচার সুইচ', lockHint: 'এই সার্ভারের সব ব্যবহারকারীর জন্য একটি ফিচার জোর করে বন্ধ করে। ব্যক্তিগত সেটিংসের ওপরে; অন্যরা পরের রিলোডে দেখবে।', lockRatings: 'সব জায়গায় রেটিং বন্ধ', lockCardRatings: 'কভারের তারা বন্ধ', lockGlobalSearch: 'গ্লোবাল অনুসন্ধান বন্ধ', lockHoverPolish: 'কার্ড পরিমার্জন বন্ধ', lockHeroCarousel: 'হোম ক্যারোসেল বন্ধ', lockedNote: 'অ্যাডমিন বন্ধ করেছেন', lfSort: 'রেটিং', lfRated: 'রেট করা', lfUnrated: 'রেট না করা', lfClear: 'মুছুন', lfNone: 'কোনো বই মেলে না', lfSection: 'রেটিং', lfWho: 'কার রেটিং', lfEveryone: 'সবাই', sdSave: 'সংরক্ষণ', sdClear: 'বইয়ের বিবরণ ফিরিয়ে আনুন', sdMore: 'আরও পড়ুন', sdLess: 'কম দেখান', shSeries: 'সিরিজ', shBy: 'লেখক: ', shMore: ' এবং আরও', shBookForms: ['বই', 'বই'], shAvg: 'বইয়ের গড়', shRated: 'রেট করা', impLabel: 'রেটিং আমদানি', impHint: 'StoryGraph বা Goodreads থেকে আপনার রেটিং নিয়ে আসুন। সেখানে বইগুলো CSV হিসেবে এক্সপোর্ট করে ফাইলটি এখানে বেছে নিন। প্রতিটি মিল নিশ্চিতকরণের জন্য দেখানো হয় এবং আপনি নিশ্চিত না করা পর্যন্ত কিছুই সংরক্ষিত হয় না।', impBtn: 'CSV ফাইল বাছুন…', impReading: 'ফাইল পড়া হচ্ছে…', impMatching: 'আপনার লাইব্রেরির সাথে মেলানো হচ্ছে…', impErrUnknown: 'এটি StoryGraph বা Goodreads-এর এক্সপোর্ট মনে হচ্ছে না। তাদের CSV হতে হবে, "Star Rating" বা "My Rating" কলামসহ।', impErrEmpty: 'ফাইলে কোনো সারি নেই।', impErrNoRated: 'ফাইলে রেট করা কোনো বই নেই।', impErrRead: 'ফাইলটি পড়া যায়নি।', impErrLoad: 'আপনার লাইব্রেরি লোড করা যায়নি। একটু পরে আবার চেষ্টা করুন।', impErrNoLibs: 'কোনো লাইব্রেরি রেটিংয়ে অংশ নেয় না, তাই আমদানির জায়গা নেই।', impTitle: 'রেটিং আমদানি', impFrom: '%s এক্সপোর্ট: %d বই, যার %d রেট করা', impLibs: 'খোঁজা হচ্ছে: %s', impMatched: 'মিলেছে', impCheck: 'দেখা দরকার', impCheckHint: 'কাছাকাছি কিন্তু অনিশ্চিত। মিলটি যাচাই করুন বা অন্যটি বাছুন।', impAlready: 'আপনি আগেই রেট করেছেন', impAlreadyHint: 'ওভাররাইট না চাইলে অপরিবর্তিত থাকে।', impNone: 'আপনার লাইব্রেরিতে নেই', impNoneHint: 'এই সার্ভারে এগুলোর সাথে কিছু মেলে না।', impAll: 'সব', impSkip: 'এটি বাদ দিন', impDup: 'ডুপ্লিকেট', impByTitle: 'শিরোনাম', impYours: 'আপনার: %s', impReviews: 'লেখা রিভিউও আমদানি করুন (%d)', impOver: 'আমার বিদ্যমান রেটিং ওভাররাইট করুন', impGo: '%d রেটিং আমদানি করুন', impCancel: 'বাতিল', impCloseBtn: 'বন্ধ', impWorking: 'সংরক্ষণ হচ্ছে… %d এর %d', impDone: '%d রেটিং আমদানি হয়েছে।', impFail: '%d সংরক্ষণ করা যায়নি।', rdTitle: 'এই বইটি কে পড়ছে', rdFin: 'শেষ', socTitle: 'সামাজিক ফিচার', socHint: 'এই সার্ভার ব্যবহারকারীদের মধ্যে যা ভাগ করে। শোনার সময় র‍্যাংকিং চালায়; বাকি দুটি প্রকাশ করে কে কী পড়ছে, তাই আপনি চালু না করা পর্যন্ত বন্ধ থাকে। সবার জন্য একসাথে প্রযোজ্য।', socTime: 'শোনার সময় (সার্ভার র‍্যাংকিং)', socContent: 'ভাগ করা পরিসংখ্যানে শীর্ষ শিরোনাম', socWho: 'কে পড়ছে (বইয়ের পাতা)', socUsers: 'ব্যবহারকারী-প্রতি', socUsersHint: 'কাউকে বন্ধ করলে তার কিছুই ভাগ হয় না, সে নিজে যা-ই বেছে থাকুক; শুধু আপনি ফেরাতে পারেন। "নিজে না বলেছেন" মানে সেই ব্যক্তি নিজেই না বলেছেন।', socOpted: 'নিজে না বলেছেন', srvTitle: 'সার্ভার ডিফল্ট', srvHint: 'আপনার বর্তমান সেটিংস এই সার্ভারের সব ব্যবহারকারীর ডিফল্ট হিসেবে সংরক্ষণ করুন। প্রত্যেকে এর ওপর নিজের মতো সাজাতে পারবে। আপডেটে টিকিয়ে রাখতে থিম কনটেইনারের /data/nh-তে একটি ভলিউম মাউন্ট করুন।', srvSave: 'সার্ভার ডিফল্ট হিসেবে সংরক্ষণ', srvClear: 'সার্ভার ডিফল্ট মুছুন', srvSaved: 'সংরক্ষিত', srvCleared: 'মুছে ফেলা হয়েছে', srvErr: 'ব্যর্থ: অ্যাডমিন লগইন দরকার', crMode: 'পড়া চালিয়ে যান তাক', crCombine: 'ক্যারোসেলে মেশান', crSeparate: 'আলাদা তাক রাখুন', crHide: 'লুকানো', fsHint: 'এই সার্ভারের অন্যদের সাথে আপনার শোনা ভাগ করুন: র‍্যাংকিংয়ের জন্য মোট সময় ও দৈনিক মিনিট, আর অ্যাডমিন চালু করলে আপনার শীর্ষ শিরোনাম এবং কোন বই পড়ছেন বা শেষ করেছেন তাও। ডিফল্টে চালু। বন্ধ করলে ভাগ করা সবকিছু মুছে যায়।' , amfHide: 'সমাপ্ত হিসেবে চিহ্নিত করুন লুকান' , amfConfirm: 'এই বইটি সমাপ্ত হিসেবে চিহ্নিত করবেন?' , fsHint: 'আপনার শোনার সারাংশ (মোট ও দৈনিক মিনিট) এই সার্ভারের অন্যদের সঙ্গে শেয়ার করুন, এটিই সার্ভার র‍্যাঙ্কিং চালায়। ডিফল্টে চালু। বন্ধ করলে আপনি র‍্যাঙ্কিং ও তার যোগফল থেকে বাদ পড়বেন, শেয়ার করা ডেটা মুছে যাবে।' , srToggle: 'আপনি কোন বই পড়ছেন তা শেয়ার করুন', srHint: 'অন্যরা যা দেখে তাতে যোগ করে আপনি এখন কোন বই পড়ছেন, কোনগুলো শেষ করেছেন এবং কোনগুলো সবচেয়ে বেশি শোনেন। ডিফল্টে বন্ধ; অ্যাডমিনের সার্ভার সুইচগুলোও চালু থাকতে হবে।' , fsHint: 'আপনার শোনার সময় (মোট এবং দৈনিক মিনিট) এই সার্ভারের অন্যদের সাথে শেয়ার করুন; এটিই র‍্যাংকিং চালায়। ডিফল্টে চালু। বন্ধ করলে অন্য ব্যবহারকারীরা আর আপনাকে দেখে না; সার্ভার অ্যাডমিনরা তবুও কার্যকলাপ দেখেন, অপ্ট-আউট চিহ্নসহ।', srHint: 'অন্যদের এটাও দেখতে দেয় আপনি কোন বই পড়ছেন, কোনগুলো শেষ করেছেন এবং কোনগুলো সবচেয়ে বেশি শোনেন। অ্যাডমিন সংশ্লিষ্ট সার্ভার সুইচ চালু না করা পর্যন্ত কিছুই দেখায় না।', seshTitle: 'সাম্প্রতিক সেশন', seshNow: 'এখন শুনছেন' , hcHint: 'আপনার রেটিং আপনার Hardcover অ্যাকাউন্টে পাঠান। Hardcover অ্যাকাউন্ট সেটিংস থেকে API কী পেস্ট করুন; কী এক বছর পরে মেয়াদোত্তীর্ণ হয়।', hcKeyLbl: 'API কী', hcToggle: 'নতুন রেটিং স্বয়ংক্রিয়ভাবে সিঙ্ক করুন', hcPushAll: 'সব রেটিং পাঠান', hcPushDiff: 'শুধু পার্থক্য পাঠান', hcDone: 'সম্পন্ন: %d সিঙ্ক হয়েছে, %d মেলেনি', hcNoKey: 'আগে আপনার Hardcover API কী পেস্ট করুন' , hcMissHead: 'Hardcover-এ পাওয়া যায়নি:' , hcHint: 'আপনার রেটিং আপনার Hardcover অ্যাকাউন্টে পাঠান। Hardcover অ্যাকাউন্ট সেটিংস থেকে API কী পেস্ট করুন।' , tabStyle: 'স্টাইল', tabMain: 'প্রধান পাতা', tabBooks: 'বই', tabFeat: 'ফিচার' , hcImport: 'Hardcover থেকে আমদানি', impMarkQ: 'মিলে যাওয়া %d বই এখানেও সম্পন্ন হিসেবে চিহ্নিত করবেন?', impMarkDone: '%d সম্পন্ন হিসেবে চিহ্নিত হয়েছে।' , impHint: 'আপনার রেটিং Goodreads, StoryGraph বা Hardcover থেকে আনুন। রপ্তানি করা CSV এখানে বেছে নিন; Hardcover তার কার্ডের বোতাম দিয়ে সরাসরি আমদানি করে। সংরক্ষণের আগে প্রতিটি মিল নিশ্চিতের জন্য দেখানো হয়।' , impErrUnknown: 'এটি Goodreads, StoryGraph বা Hardcover-এর রপ্তানি বলে মনে হচ্ছে না: শিরোনাম ও রেটিং কলাম পাওয়া যায়নি।' , fsCard: 'সোশ্যাল', rnToggle: 'রেটিংয়ের সাথে আমার নাম দেখান', rsToggle: 'আমার রেটিং অন্যদের সাথে শেয়ার করুন', socNames: 'রেটিংয়ের সাথে নাম', rtAnon: 'বেনামী' },
    gu: { cmStopped: "બંધ કર્યું", cmScanErrs: "{e} તપાસી શકાયા નથી (Goodreads ભૂલો)", cmScanBusy: "એક સ્કૅન પહેલેથી ચાલી રહ્યું છે", cmGrErr: "Goodreads સુધી પહોંચી શકાયું નથી, ફરી પ્રયાસ થશે", cmNoMatchYet: "Goodreads પર કોઈ મેળ નથી", cmLooking: "Goodreads પર શોધી રહ્યું છે…", cmToolHint: "Goodreads ના વાચકો આ પુસ્તક વિશે શું વિચારે છે. ? મેળ ખાતું પુસ્તક બતાવે છે અને બદલવા દે છે.", cmSearchPh: "Goodreads પર શોધો…", cmNoVotes: "હજુ રેટિંગ નથી", cmRefresh: "રિફ્રેશ", cmScanAll: "માત્ર ખૂટતા નહીં, બધું ફરીથી સ્કૅન કરો", cmScanDone: "પૂર્ણ: {m} મળ્યા · {x} મળ્યા નથી · {s} પહેલેથી હતા", cmStop: "રોકો", cmScanLibs: "સ્કૅન કરવાની લાઇબ્રેરીઓ", cmScan: "રેટિંગ શોધો", lsLongest: "સૌથી લાંબાં પુસ્તકો", lsSize: "ડિસ્ક પર કદ", lsDuration: "કુલ સમય", lsGenresN: "શૈલીઓ", lsSeriesN: "શ્રેણીઓ", lsAuthorsN: "લેખકો", lsBooks: "પુસ્તકો", lsGlance: "લાઇબ્રેરી એક નજરે", cmOpen: "Goodreads પર ખોલો", cmDetect: "શોધો", cmAddr: "સહાયકનું સરનામું", scmTitle: "શ્રેણી પાનું", scmHint: "પોતાનું કવર અપલોડ ન હોય ત્યારે શ્રેણીના શીર્ષક ઉપર બનતા કવરનો દેખાવ (ફક્ત આ બ્રાઉઝર).", scmDeck: "સ્તરવાળી ડેક", cmHint: "Goodreads ના વાચકો પુસ્તક વિશે શું વિચારે છે: સરેરાશ સ્કોર અને મતોની સંખ્યા, પુસ્તકના પેજ પર તારાની નીચે અને સૉર્ટ તથા ફિલ્ટર તરીકે. પુસ્તકોના પેજ બ્રાઉઝ કરતી વખતે આપોઆપ ભરાય છે.", cmGrOff: "Goodreads: સેટ નથી", cmSetup: "હજુ સહાયક નથી? થીમની બાજુમાં આ ચલાવો (નીચેની compose લીટીઓ), પછી શોધો દબાવો. ઇમેજ બીજી કોઈ પણ રીતે ચલાવો તો પણ ચાલે: તેનું સરનામું ઉપર પેસ્ટ કરો.", cmGrOn: "Goodreads: જોડાયેલ", cmGrDown: "Goodreads: સેટ છે પણ જવાબ આપતું નથી", cmGrNoNum: "Goodreads: જવાબ આપે છે, પણ આ abs-tract બિલ્ડમાં રેટિંગ આંકડા નથી (ratings બિલ્ડ વાપરો, README જુઓ)", cmTitle: "Goodreads રેટિંગ", cmWrong: "ખોટું પુસ્તક?", cmFind: "સમુદાય રેટિંગ શોધો…", cmNone: "કોઈ મેળ નથી", cmUse: "આ વાપરો", lfCommunity: "Goodreads રેટિંગ", mfNatChange: "બદલો…", lsByTime: "સાંભળવાના સમય પ્રમાણે", sbTitle: "સર્વર રેન્કિંગ", sbWeek: "અઠવાડિયું", sbMonth: "મહિનો", sbYear: "વર્ષ", sbAll: "કુલ સમય", sbBooks: "પુસ્તકો", sbTopBooks: "સૌથી વધુ સાંભળેલી", sbTotal: "કુલ", sbListeners: "શ્રોતાઓ", sbAvg: "સરેરાશ", wkTitle: "સાંભળવાની મિનિટ, છેલ્લા 7 દિવસ", wkAvg: "દૈનિક સરેરાશ", wkRow: "સળંગ દિવસ", rpBadge: "ખુલ્લા સમસ્યા અહેવાલ", rpMenu: "સમસ્યાની જાણ કરો", rpTitle: "સમસ્યાની જાણ કરો", rpWhat: "શું ખોટું છે?", rpNote: "એડમિનને બીજું કંઈ કહેવું છે? (વૈકલ્પિક)", rpSend: "અહેવાલ મોકલો", rpSent: "મોકલાયો. આભાર.", rpFail: "મોકલી શકાયો નહીં", rpMissing: "સામગ્રી ખૂટે છે અથવા અધૂરી", rpQuality: "ખરાબ ઓડિયો ગુણવત્તા", rpPlay: "વાગતું નથી", rpWrong: "ખોટી પુસ્તક, કવર કે મેટાડેટા", rpChapters: "પ્રકરણો ખોટાં", rpOther: "બીજું કંઈક", rfRate: "રેટ કરો", rfSheet: "આ પુસ્તકને રેટ કરો", rfOpen: "પુસ્તક પાનું ખોલો", rfPickHint: "રેટિંગ પસંદ કરો", rfTitle: "પૂરું કરેલું રેટ કરો", ysFinished: "તાજેતરમાં પૂરી થયેલી", ysAlmost: "લગભગ પૂરી", ysMarkDone: "પૂર્ણ તરીકે ચિહ્નિત કરો", ysEditDate: "પૂર્ણ થવાની તારીખ બદલો", ysThisWeek: "આ અઠવાડિયે", ysBestDay: "શ્રેષ્ઠ દિવસ", ysTitle: "તમારું સાંભળવું", fsTitle: "સર્વર રેન્કિંગ", fsCard: "સર્વર રેન્કિંગ", fsToggle: "મારા સાંભળવાના આંકડા શેર કરો", fsJoin: "સર્વર રેન્કિંગમાં દેખાવા માટે કસ્ટમાઇઝેશન પેનલમાં “મારા સાંભળવાના આંકડા શેર કરો” ચાલુ કરો.", fsWaiting: "શેરિંગ ચાલુ છે, તમારો સારાંશ ટૂંક સમયમાં બધા સાથે સર્વર રેન્કિંગમાં જોડાશે.", fsHint: "તમારા સાંભળવાનો સારાંશ (કુલ અને દૈનિક મિનિટ) આ સર્વરના અન્ય લોકો સાથે શેર કરો, તે સર્વર રેન્કિંગ ચલાવે છે. ડિફૉલ્ટ રૂપે ચાલુ. બંધ કરવાથી તમે રેન્કિંગ અને તેના સરવાળામાંથી નીકળી જશો, અને શેર કરેલો ડેટા ભૂંસાઈ જશે.", yirBtn: "વર્ષની ઝલક", yirTitle: "તમારું સાંભળવાનું વર્ષ", yirYear: "છેલ્લા 12 મહિના", yirDays: "સાંભળવાના દિવસો", yirBest: "શ્રેષ્ઠ દિવસ", yirMonths: "મહિના પ્રમાણે", yirNone: "હજી કોઈ ડેટા નથી.", ssscmDeck: "સ્તરવાળી ડેક", scmFirst: "પહેલી પુસ્તકનું કવર", scmGrid: "કવર ગ્રિડ (ડિફૉલ્ટ)", scmOff: "છુપાયેલું" , lsTitle: "સર્વર આંકડા", lsAll: "બધી લાઇબ્રેરી", lsPlayed: "વગાડેલી પુસ્તકો", lsBest: "શ્રેષ્ઠ રેટેડ", lsGenres: "ટોચની શૈલીઓ", srvPick: "શું સાચવવું:", srvGRest: "સુવિધાઓ અને વર્તન", aeBtn: "લેખકો ગોઠવો", aeTitle: "પુસ્તક વગરના લેખકો", aeNone: "અહીં દરેક લેખક પાસે ઓછામાં ઓછી એક પુસ્તક છે.", aeRemove: "દૂર કરો", aeDone: "દૂર થયું", aeWorking: "દૂર કરી રહ્યાં છીએ…", rpCard: "જાણ કરેલી સમસ્યાઓ", rpHint: "પુસ્તક પાનાં પરથી વપરાશકર્તાઓએ જણાવેલી સમસ્યાઓ. ઉકેલવાથી યાદીમાંથી નીકળી જાય છે.", rpEmpty: "કોઈ જાણ નથી.", rpResolve: "ઉકેલો", rpOpen: "પુસ્તક ખોલો", apCard: "પ્લેબેક", apToggle: "શ્રેણીની આગલી પુસ્તક આપોઆપ વગાડો", apHint: "પુસ્તક પૂરી થતાં એ જ શ્રેણીની આગલી આપોઆપ શરૂ થાય છે. ફક્ત તમારી પાસે હોય અને અધૂરી હોય તેવી પુસ્તકો.", apNext: "શ્રેણીમાં આગલી", fdToggle: "આંકડામાં પૂર્ણ પુસ્તકોના સાધનો", ysNoDate: "તારીખ નથી", hmShowing: "ચાર્ટ બતાવે છે", ysStreak: "સળંગ દિવસો", ysCurrent: "વર્તમાન", ysLongest: "સૌથી લાંબો", ysDaysTotal: "કુલ દિવસો", ysPace: "ગતિ", ysPerDay: "શ્રવણ દિવસ દીઠ", ysWhen: "તમે ક્યારે સાંભળો છો", ysTopBooks: "સૌથી વધુ સાંભળેલી પુસ્તકો", ysTopAuthors: "સૌથી વધુ સાંભળેલા લેખકો", ysTopNarrators: "સૌથી વધુ સાંભળેલા વાચકો", rfToggle: "હોમ પર “રેટ કરો” વિભાગ", hoTitle: "હોમ વિભાગોનો ક્રમ", hoEmpty: "એકવાર હોમ ખોલો, પછી અહીં પાછા આવો.", hoReset: "મૂળ ક્રમ પુનઃસ્થાપિત કરો", ctIcon: "આઇકન", ctColour: "એક્સેન્ટ", ctNeedBook: "Audiobookshelf ખાલી સંગ્રહ સાચવી શકતું નથી, ઓછામાં ઓછી એક પુસ્તક પસંદ કરો. પછી મરજી મુજબ ઉમેરી-કાઢી શકશો.", colEditIcon: "આઇકન બદલો", ciTitle: "સંગ્રહનું આઇકન", ciReset: "નામ પ્રમાણે આપોઆપ ગોઠવો", colEditDesc: "વર્ણન સંપાદિત કરો", colBookForms: ["પુસ્તક","પુસ્તકો"], colForms: ["સંગ્રહ","સંગ્રહો"], ctTitle: "સંગ્રહ બનાવો", ctBlank: "ખાલી સંગ્રહ", ctNew: "નવો સંગ્રહ", ctNewHint: "ટેમ્પલેટમાંથી", ctNamePh: "સંગ્રહનું નામ", ctSearch: "પુસ્તકો કે લેખકો શોધો…", ctCreate: "બનાવો", ctBack: "‹ પાછળ", ctAddAll: "બધી પુસ્તકો ઉમેરો", abTitle: "પુસ્તકો ઉમેરો", abAdd: "ઉમેરો", avRemove: "ફોટો દૂર કરો", avSet: "ફોટો પસંદ કરો…", ugListened: "સાંભળ્યું", ugPhoto: "પ્રોફાઇલ ફોટો", ugNever: "ક્યારેય નહીં", ugSeen: "છેલ્લે સક્રિય", ugEdit: "વપરાશકર્તા સંપાદિત કરો", ugDelete: "વપરાશકર્તા કાઢી નાખો", ugAdd: "ફોટો ઉમેરો", ugChange: "ફોટો બદલો", ugSortName: "અ–જ્ઞ", ugSortTime: "સૌથી વધુ સાંભળેલું", ugSortSeen: "તાજેતરમાં સક્રિય", bsCard: "પુસ્તક લિંક", bsHint: "પુસ્તક પાનાં પર શીર્ષક બીજે શોધવાનાં બટન. Goodreads અને તમારી ભાષાની સૌથી મોટી સાઇટ પહેલેથી ચાલુ છે.", bsFind: "અહીં શોધો", bsGlobal: "બધે", bsLocal: "તમારી ભાષા માટે", bsOther: "અન્ય દેશો", pgCard: "નવી ડિઝાઇનનાં પાનાં", pgHint: "દરેક વિકલ્પ Audiobookshelf પાનું NanoHive સંસ્કરણથી બદલે છે. બંધ કરો એટલે મૂળ પાછું આવે.", pgNarrators: "વાચક કાર્ડ", pgCollections: "સંગ્રહ પાનાં", pgUsers: "વપરાશકર્તા કાર્ડ (સેટિંગ્સ)", pgStats: "શ્રવણ રેન્કિંગ", pgPhoto: "ટોચની પટ્ટીમાં પ્રોફાઇલ ફોટો", pgCinematic: "સિનેમેટિક પૃષ્ઠભૂમિ", pgTransitions: "પાનાં સંક્રમણ", pSearch: "સેટિંગ્સ શોધો…", pSearchNone: "કોઈ મેળ નથી.", erTypeface: "ટાઇપફેસ", erPage: "પાનાની થીમ", erText: "લખાણનો રંગ", erBg: "પૃષ્ઠભૂમિ", erDefault: "મૂળભૂત (ABS)", erAuto: "આપોઆપ", nrForms: ["વાચક","વાચકો"], nrBooksForms: ["પુસ્તક","પુસ્તકો"], nrSearch: "વાચકો ફિલ્ટર કરો…", nrSortName: "નામ", nrSortBooks: "સૌથી વધુ પુસ્તકો", auForms: ["લેખક","લેખકો"], auSearch: "લેખકો ફિલ્ટર કરો…" , lfByAuthor: "લેખક › શ્રેણી ક્રમ", lfBySeries: "શ્રેણી › ક્રમ" , lfSecFilters: "વધુ ફિલ્ટર", lfSecSort: "બહુ-સ્તરીય ક્રમ", lfAuthor: "લેખક", lfSeries: "શ્રેણી", lfTitle: "શીર્ષક", lfYear: "વર્ષ", lfAdded: "ઉમેરાયું", lfDuration: "અવધિ", lfNarrator: "વાચક", lfGenre: "શૈલી", lfLanguage: "ભાષા", lfProgress: "પ્રગતિ", lfPgFinished: "પૂર્ણ", lfPgInProgress: "ચાલુ", lfPgNot: "શરૂ નથી" , logoUpload: 'ડિવાઇસમાંથી અપલોડ કરો…', logoUploaded: 'અપલોડ થયું ✓', logoBadType: 'અસમર્થિત છબી પ્રકાર', logoTooBig: 'છબી ખૂબ મોટી છે (મહત્તમ 4 MB)', logoOr: 'અથવા', bookPage: 'પુસ્તક પાનું', ratingsToggle: 'સમુદાયનાં રેટિંગ (તારા અને સમીક્ષાઓ)', ratingsHint: 'દરેક પુસ્તક પાનાં પર ટૂંકી સમીક્ષાઓ સાથે તારા રેટિંગ, આ સર્વરના બધા વપરાશકર્તાઓ સાથે વહેંચાયેલ.', searchCard: 'શોધ', gsToggle: 'બધી લાઇબ્રેરીઓમાં એકસાથે શોધો', gsHint: 'તમારી બધી સુલભ લાઇબ્રેરીઓમાંથી એક સંયુક્ત પરિણામ સૂચિ, દરેક પરિણામ પર લાઇબ્રેરી બેજ. બંધ કરવાથી લાઇબ્રેરી-દીઠ મેનૂ પાછું આવે.', gsNoResults: 'કોઈ પરિણામ નથી', gsSearching: 'શોધી રહ્યાં છીએ…', gsBooks: 'પુસ્તકો', gsSeries: 'શ્રેણીઓ', gsAuthors: 'લેખકો', spTitle: 'શરૂઆતનું પાનું', spHint: 'એપ હંમેશાં પસંદ કરેલા લાઇબ્રેરી દૃશ્ય પર ખોલો (ફક્ત આ બ્રાઉઝર). સીધી લિંક કદી રીડાયરેક્ટ થતી નથી.', spDefault: 'એપ ડિફૉલ્ટ', spViewHome: 'હોમ', spViewLibrary: 'લાઇબ્રેરી', spViewSeries: 'શ્રેણીઓ', spViewCollections: 'સંગ્રહો', spViewAuthors: 'લેખકો', scSet: 'શ્રેણી કવર સેટ કરો', scRemove: 'શ્રેણી કવર દૂર કરો', scEditDesc: 'શ્રેણી વર્ણન સંપાદિત કરો', cardStars: 'કવર પર તારા બેજ', hoverPolish: 'કાર્ડ હોવર પોલિશ (નરમ ડિમિંગ, હાઇલાઇટ પ્લે, હોવર પર તારા)', sectUser: 'તમારી સેટિંગ્સ', sectAdmin: 'વહીવટ', lockTitle: 'ફીચર સ્વિચ', lockHint: 'આ સર્વરના બધા વપરાશકર્તાઓ માટે કોઈ ફીચર બળજબરીથી બંધ કરે છે. વ્યક્તિગત સેટિંગ્સ પર ભારે; બીજા આગલી રીલોડ પછી જોશે.', lockRatings: 'બધે રેટિંગ બંધ કરો', lockCardRatings: 'કવર પરના તારા બંધ કરો', lockGlobalSearch: 'વૈશ્વિક શોધ બંધ કરો', lockHoverPolish: 'કાર્ડ પોલિશ બંધ કરો', lockHeroCarousel: 'હોમ કેરોસેલ બંધ કરો', lockedNote: 'એડમિન દ્વારા બંધ', lfSort: 'રેટિંગ', lfRated: 'રેટ કરેલ', lfUnrated: 'રેટ વગરની', lfClear: 'સાફ કરો', lfNone: 'કોઈ પુસ્તક મળતું નથી', lfSection: 'રેટિંગ', lfWho: 'કોનાં રેટિંગ', lfEveryone: 'બધા', sdSave: 'સાચવો', sdClear: 'પુસ્તક વર્ણન પુનઃસ્થાપિત કરો', sdMore: 'વધુ વાંચો', sdLess: 'ઓછું બતાવો', shSeries: 'શ્રેણી', shBy: 'લેખક: ', shMore: ' અને વધુ', shBookForms: ['પુસ્તક', 'પુસ્તકો'], shAvg: 'પુસ્તકોની સરેરાશ', shRated: 'રેટ કરેલ', impLabel: 'રેટિંગ આયાત કરો', impHint: 'તમારાં રેટિંગ StoryGraph કે Goodreads માંથી લાવો. ત્યાં પુસ્તકોને CSV તરીકે નિકાસ કરો અને અહીં ફાઇલ પસંદ કરો. દરેક મેળ પુષ્ટિ માટે બતાવાય છે અને તમે પુષ્ટિ ન કરો ત્યાં સુધી કંઈ સચવાતું નથી.', impBtn: 'CSV ફાઇલ પસંદ કરો…', impReading: 'ફાઇલ વંચાઈ રહી છે…', impMatching: 'તમારી લાઇબ્રેરીઓ સાથે મેળવણી…', impErrUnknown: 'આ StoryGraph કે Goodreads નું નિકાસ લાગતું નથી. તે તેમની CSV હોવી જોઈએ, "Star Rating" કે "My Rating" કૉલમ સાથે.', impErrEmpty: 'ફાઇલમાં કોઈ પંક્તિ નથી.', impErrNoRated: 'ફાઇલમાં રેટ કરેલી કોઈ પુસ્તક નથી.', impErrRead: 'ફાઇલ વાંચી શકાઈ નથી.', impErrLoad: 'તમારી લાઇબ્રેરીઓ લોડ થઈ શકી નથી. થોડી વારે ફરી પ્રયત્ન કરો.', impErrNoLibs: 'કોઈ લાઇબ્રેરી રેટિંગમાં ભાગ લેતી નથી, તેથી આયાત કરવાની જગ્યા નથી.', impTitle: 'રેટિંગ આયાત કરો', impFrom: '%s નિકાસ: %d પુસ્તકો, જેમાં %d રેટ કરેલ', impLibs: 'શોધ: %s', impMatched: 'મેળ ખાધાં', impCheck: 'જોવા જેવું', impCheckHint: 'નજીક પણ અનિશ્ચિત. મેળ તપાસો કે બીજું પસંદ કરો.', impAlready: 'તમે પહેલેથી રેટ કરેલ', impAlreadyHint: 'ઓવરરાઇટ ન માંગો ત્યાં સુધી યથાવત્ રહે.', impNone: 'તમારી લાઇબ્રેરીમાં નથી', impNoneHint: 'આ સર્વર પર આમની સાથે કંઈ મળતું નથી.', impAll: 'બધાં', impSkip: 'આ છોડો', impDup: 'ડુપ્લિકેટ', impByTitle: 'શીર્ષક', impYours: 'તમારું: %s', impReviews: 'લખેલી સમીક્ષાઓ પણ આયાત કરો (%d)', impOver: 'મારાં હાલનાં રેટિંગ ઓવરરાઇટ કરો', impGo: '%d રેટિંગ આયાત કરો', impCancel: 'રદ કરો', impCloseBtn: 'બંધ કરો', impWorking: 'સાચવી રહ્યાં… %d માંથી %d', impDone: '%d રેટિંગ આયાત થયાં.', impFail: '%d સાચવી શકાયાં નહીં.', rdTitle: 'આ પુસ્તક કોણ વાંચે છે', rdFin: 'પૂર્ણ', socTitle: 'સામાજિક સુવિધાઓ', socHint: 'આ સર્વર વપરાશકર્તાઓ વચ્ચે શું વહેંચે છે. સાંભળવાનો સમય રેંકિંગ ચલાવે છે; બાકીની બે જાહેર કરે છે કે કોણ શું વાંચે છે, તેથી તમે ચાલુ ન કરો ત્યાં સુધી બંધ રહે છે. બધા પર એકસાથે લાગુ.', socTime: 'સાંભળવાનો સમય (સર્વર રેંકિંગ)', socContent: 'વહેંચાયેલ આંકડામાં ટોચનાં શીર્ષકો', socWho: 'કોણ વાંચે છે (પુસ્તક પાનાં)', socUsers: 'વપરાશકર્તા-દીઠ', socUsersHint: 'કોઈને બંધ કરો તો તેની પસંદગી ગમે તે હોય, તેનું કંઈ વહેંચાતું નથી; ફક્ત તમે પાછું ચાલુ કરી શકો. "પોતે ના પાડી" એટલે તે વ્યક્તિએ પોતે ના કહી.', socOpted: 'પોતે ના પાડી', srvTitle: 'સર્વર ડિફૉલ્ટ', srvHint: 'તમારી હાલની સેટિંગ્સ આ સર્વરના બધા વપરાશકર્તાઓ માટે ડિફૉલ્ટ તરીકે સાચવો. દરેક તેની ઉપર વ્યક્તિગત કરી શકે. અપડેટમાં ટકે માટે થીમ કન્ટેનરમાં /data/nh પર વોલ્યુમ માઉન્ટ કરો.', srvSave: 'સર્વર ડિફૉલ્ટ તરીકે સાચવો', srvClear: 'સર્વર ડિફૉલ્ટ સાફ કરો', srvSaved: 'સચવાયું', srvCleared: 'સાફ થયું', srvErr: 'નિષ્ફળ: એડમિન લોગિન જરૂરી', crMode: 'વાંચન ચાલુ રાખો છાજલી', crCombine: 'કેરોસેલમાં ભેળવો', crSeparate: 'અલગ છાજલી રાખો', crHide: 'છુપાયેલ', fsHint: 'તમારું સાંભળવું આ સર્વરના અન્ય લોકો સાથે વહેંચો: રેંકિંગ માટે કુલ સમય અને દૈનિક મિનિટ, અને એડમિને ચાલુ કર્યું હોય તો તમારાં ટોચનાં શીર્ષકો અને કઈ પુસ્તકો વાંચો છો કે પૂરી કરી છે તે પણ. ડિફૉલ્ટે ચાલુ. બંધ કરવાથી વહેંચેલું બધું ભૂંસાય છે.' , amfHide: 'પૂર્ણ તરીકે ચિહ્નિત કરો છુપાવો' , amfConfirm: 'આ પુસ્તકને પૂર્ણ તરીકે ચિહ્નિત કરીએ?' , fsHint: 'તમારા સાંભળવાનો સારાંશ (કુલ અને દૈનિક મિનિટ) આ સર્વરના અન્ય લોકો સાથે શેર કરો, તે સર્વર રેન્કિંગ ચલાવે છે. ડિફૉલ્ટ રૂપે ચાલુ. બંધ કરવાથી તમે રેન્કિંગ અને તેના સરવાળામાંથી નીકળી જશો, અને શેર કરેલો ડેટા ભૂંસાઈ જશે.' , srToggle: 'તમે કઈ પુસ્તકો વાંચો છો તે શેર કરો', srHint: 'બીજાઓ જે જુએ છે તેમાં ઉમેરે છે કે તમે અત્યારે કઈ પુસ્તકો વાંચો છો, કઈ પૂરી કરી અને કઈ સૌથી વધુ સાંભળો છો. ડિફૉલ્ટે બંધ; એડમિનના સર્વર સ્વિચ પણ ચાલુ હોવા જોઈએ.' , fsHint: 'તમારો સાંભળવાનો સમય (કુલ અને દૈનિક મિનિટ) આ સર્વરના અન્ય લોકો સાથે શેર કરો; તે રેંકિંગ ચલાવે છે. ડિફૉલ્ટે ચાલુ. બંધ કરવાથી અન્ય વપરાશકર્તાઓ તમને જોતા નથી; સર્વર એડમિન પ્રવૃત્તિ હજી જુએ છે, ઓપ્ટ-આઉટ ચિહ્ન સાથે.', srHint: 'બીજાઓને એ પણ જોવા દે છે કે તમે કઈ પુસ્તકો વાંચો છો, કઈ પૂરી કરી અને કઈ સૌથી વધુ સાંભળો છો. એડમિન સંબંધિત સર્વર સ્વિચ ચાલુ ન કરે ત્યાં સુધી કંઈ દેખાતું નથી.', seshTitle: 'તાજેતરનાં સત્રો', seshNow: 'હમણાં સાંભળે છે' , hcHint: 'તમારાં રેટિંગ તમારા Hardcover એકાઉન્ટમાં મોકલો. Hardcover એકાઉન્ટ સેટિંગ્સમાંથી API કી પેસ્ટ કરો; કી એક વર્ષ પછી સમાપ્ત થાય છે.', hcKeyLbl: 'API કી', hcToggle: 'નવાં રેટિંગ આપમેળે સિંક કરો', hcPushAll: 'બધાં રેટિંગ મોકલો', hcPushDiff: 'માત્ર તફાવત મોકલો', hcDone: 'થઈ ગયું: %d સિંક, %d મેળ વિના', hcNoKey: 'પહેલા તમારી Hardcover API કી પેસ્ટ કરો' , hcMissHead: 'Hardcover પર મળ્યું નથી:' , hcHint: 'તમારાં રેટિંગ તમારા Hardcover એકાઉન્ટમાં મોકલો. Hardcover એકાઉન્ટ સેટિંગ્સમાંથી API કી પેસ્ટ કરો.' , tabStyle: 'સ્ટાઇલ', tabMain: 'મુખ્ય પેજ', tabBooks: 'પુસ્તકો', tabFeat: 'ફીચર્સ' , hcImport: 'Hardcover માંથી આયાત કરો', impMarkQ: 'મેળ ખાતાં %d પુસ્તકોને અહીં પણ પૂર્ણ તરીકે ચિહ્નિત કરવા?', impMarkDone: '%d પૂર્ણ તરીકે ચિહ્નિત થયાં.' , impHint: 'તમારાં રેટિંગ Goodreads, StoryGraph કે Hardcover માંથી લાવો. નિકાસ કરેલી CSV અહીં પસંદ કરો; Hardcover તેના કાર્ડ પરના બટનથી સીધું આયાત કરે છે. સાચવતાં પહેલાં દરેક મેળ પુષ્ટિ માટે બતાવાય છે.' , impErrUnknown: 'આ Goodreads, StoryGraph કે Hardcover ની નિકાસ જેવું લાગતું નથી: શીર્ષક અને રેટિંગ કૉલમ મળ્યાં નથી.' , fsCard: 'સોશિયલ', rnToggle: 'રેટિંગ સાથે મારું નામ બતાવો', rsToggle: 'મારાં રેટિંગ બીજા સાથે શેર કરો', socNames: 'રેટિંગ સાથે નામ', rtAnon: 'અનામી' },
    vi: { cmStopped: "đã dừng", cmScanErrs: "{e} chưa kiểm tra được (lỗi Goodreads)", cmScanBusy: "Đang có một lần quét chạy rồi", cmGrErr: "Không kết nối được Goodreads, sẽ thử lại", cmNoMatchYet: "Không khớp trên Goodreads", cmLooking: "Đang tìm trên Goodreads…", cmToolHint: "Độc giả Goodreads nghĩ gì về cuốn sách này. Dấu ? cho thấy sách đã khớp và cho phép đổi.", cmSearchPh: "Tìm trên Goodreads…", cmNoVotes: "chưa có đánh giá", cmRefresh: "Làm mới", cmScanAll: "Quét lại tất cả, không chỉ phần thiếu", cmScanDone: "Xong: {m} khớp · {x} không tìm thấy · {s} đã có", cmStop: "Dừng", cmScanLibs: "Thư viện cần quét", cmScan: "Quét đánh giá", lsLongest: "Sách dài nhất", lsSize: "Dung lượng trên đĩa", lsDuration: "Tổng thời lượng", lsGenresN: "Thể loại", lsSeriesN: "Bộ sách", lsAuthorsN: "Tác giả", lsBooks: "Sách", lsGlance: "Thư viện trong nháy mắt", cmOpen: "Mở trên Goodreads", cmDetect: "Tìm", cmAddr: "Địa chỉ trợ thủ", scmTitle: "Trang bộ sách", scmHint: "Bìa được tạo phía trên tên bộ sách trông thế nào khi chưa tải lên bìa riêng (chỉ trình duyệt này).", scmDeck: "Chồng bìa xếp lớp", cmHint: "Độc giả Goodreads nghĩ gì về một cuốn sách: điểm trung bình và số lượt đánh giá, hiện dưới các ngôi sao trên trang sách và dùng để sắp xếp, lọc. Trang sách tự điền khi mọi người duyệt.", cmGrOff: "Goodreads: chưa cài", cmSetup: "Chưa có trợ thủ? Chạy cái này cạnh theme (các dòng compose bên dưới), rồi bấm Tìm. Chạy image theo cách khác cũng được: dán địa chỉ của nó vào ô trên.", cmGrOn: "Goodreads: đã kết nối", cmGrDown: "Goodreads: đã cài nhưng không phản hồi", cmGrNoNum: "Goodreads: có phản hồi nhưng bản abs-tract này không có số điểm (dùng bản ratings, xem README)", cmTitle: "Đánh giá Goodreads", cmWrong: "Sai sách?", cmFind: "Tìm đánh giá cộng đồng…", cmNone: "Không khớp", cmUse: "Dùng cái này", lfCommunity: "Đánh giá Goodreads", mfNatChange: "Thay đổi…", lsByTime: "theo thời gian nghe", sbTitle: "Xếp hạng máy chủ", sbWeek: "Tuần", sbMonth: "Tháng", sbYear: "Năm", sbAll: "Mọi lúc", sbBooks: "Sách", sbTopBooks: "Nghe nhiều nhất", sbTotal: "Tổng", sbListeners: "Người nghe", sbAvg: "Trung bình", wkTitle: "Phút nghe, 7 ngày qua", wkAvg: "Trung bình mỗi ngày", wkRow: "Ngày liên tiếp", rpBadge: "Báo cáo sự cố đang mở", rpMenu: "Báo cáo sự cố", rpTitle: "Báo cáo sự cố", rpWhat: "Có vấn đề gì?", rpNote: "Còn gì cần cho quản trị viên biết? (không bắt buộc)", rpSend: "Gửi báo cáo", rpSent: "Đã gửi. Cảm ơn.", rpFail: "Không gửi được", rpMissing: "Nội dung thiếu hoặc chưa đầy đủ", rpQuality: "Chất lượng âm thanh kém", rpPlay: "Không phát được", rpWrong: "Sai sách, bìa hoặc siêu dữ liệu", rpChapters: "Chương sai", rpOther: "Khác", rfRate: "Đánh giá", rfSheet: "Đánh giá cuốn sách này", rfOpen: "Mở trang sách", rfPickHint: "Chọn mức đánh giá", rfTitle: "Đánh giá những gì bạn đã nghe xong", ysFinished: "Vừa hoàn thành", ysAlmost: "Sắp xong", ysMarkDone: "Đánh dấu đã xong", ysEditDate: "Đổi ngày hoàn thành", ysThisWeek: "Tuần này", ysBestDay: "Ngày tốt nhất", ysTitle: "Việc nghe của bạn", fsTitle: "Xếp hạng máy chủ", fsCard: "Xếp hạng máy chủ", fsToggle: "Chia sẻ thống kê nghe của tôi", fsJoin: "Bật “Chia sẻ thống kê nghe của tôi” trong bảng tùy chỉnh để xuất hiện trong xếp hạng máy chủ.", fsWaiting: "Đang bật chia sẻ, bản tóm tắt của bạn sẽ sớm tham gia xếp hạng máy chủ cùng mọi người.", fsHint: "Chia sẻ tóm tắt việc nghe của bạn (tổng và số phút mỗi ngày) với những người khác trên máy chủ này, nó nuôi xếp hạng máy chủ. Bật mặc định. Tắt đi thì bạn rời khỏi xếp hạng và các tổng của nó, dữ liệu đã chia sẻ bị xóa.", yirBtn: "Nhìn lại năm qua", yirTitle: "Năm nghe của bạn", yirYear: "12 tháng qua", yirDays: "Ngày có nghe", yirBest: "Ngày tốt nhất", yirMonths: "Theo tháng", yirNone: "Chưa có dữ liệu nghe.", ssscmDeck: "Chồng bìa xếp lớp", scmFirst: "Bìa cuốn đầu tiên", scmGrid: "Lưới bìa (mặc định)", scmOff: "Ẩn" , lsTitle: "Thống kê máy chủ", lsAll: "Tất cả thư viện", lsPlayed: "Sách đã phát", lsBest: "Được đánh giá cao nhất", lsGenres: "Thể loại hàng đầu", srvPick: "Lưu những gì:", srvGRest: "Tính năng và hành vi", aeBtn: "Dọn dẹp tác giả", aeTitle: "Tác giả không có sách", aeNone: "Mọi tác giả ở đây đều có ít nhất một cuốn sách.", aeRemove: "Xóa", aeDone: "Đã xóa", aeWorking: "Đang xóa…", rpCard: "Sự cố đã báo cáo", rpHint: "Sự cố người dùng báo từ trang sách. Giải quyết xong sẽ biến mất khỏi danh sách.", rpEmpty: "Chưa có báo cáo.", rpResolve: "Giải quyết", rpOpen: "Mở sách", apCard: "Phát", apToggle: "Tự động phát cuốn tiếp theo trong bộ", apHint: "Khi một cuốn kết thúc, cuốn tiếp theo cùng bộ tự động bắt đầu. Chỉ những sách bạn đã có và chưa nghe xong.", apNext: "Cuốn tiếp theo trong bộ", fdToggle: "Công cụ sách đã xong trong thống kê", ysNoDate: "không có ngày", hmShowing: "biểu đồ hiển thị", ysStreak: "Chuỗi ngày", ysCurrent: "Hiện tại", ysLongest: "Dài nhất", ysDaysTotal: "Tổng số ngày", ysPace: "Nhịp độ", ysPerDay: "Mỗi ngày nghe", ysWhen: "Bạn nghe khi nào", ysTopBooks: "Sách nghe nhiều nhất", ysTopAuthors: "Tác giả nghe nhiều nhất", ysTopNarrators: "Người đọc nghe nhiều nhất", rfToggle: "Mục “Đánh giá” trên trang chủ", hoTitle: "Thứ tự các mục trang chủ", hoEmpty: "Mở trang chủ một lần rồi quay lại đây.", hoReset: "Khôi phục thứ tự mặc định", ctIcon: "Biểu tượng", ctColour: "Màu nhấn", ctNeedBook: "Audiobookshelf không thể lưu bộ sưu tập trống, chọn ít nhất một cuốn. Sau này bạn có thể thêm bớt thoải mái.", colEditIcon: "Đổi biểu tượng", ciTitle: "Biểu tượng bộ sưu tập", ciReset: "Tự khớp theo tên", colEditDesc: "Sửa mô tả", colBookForms: ["cuốn","cuốn"], colForms: ["Bộ sưu tập","Bộ sưu tập"], ctTitle: "Tạo bộ sưu tập", ctBlank: "Bộ sưu tập trống", ctNew: "Bộ sưu tập mới", ctNewHint: "từ mẫu", ctNamePh: "Tên bộ sưu tập", ctSearch: "Tìm sách hoặc tác giả…", ctCreate: "Tạo", ctBack: "‹ Quay lại", ctAddAll: "Thêm tất cả sách", abTitle: "Thêm sách", abAdd: "Thêm", avRemove: "Xóa ảnh", avSet: "Chọn ảnh…", ugListened: "Đã nghe", ugPhoto: "Ảnh hồ sơ", ugNever: "Chưa bao giờ", ugSeen: "Hoạt động gần nhất", ugEdit: "Sửa người dùng", ugDelete: "Xóa người dùng", ugAdd: "Thêm ảnh", ugChange: "Đổi ảnh", ugSortName: "A–Z", ugSortTime: "Nghe nhiều nhất", ugSortSeen: "Hoạt động gần đây", bsCard: "Liên kết sách", bsHint: "Các nút trên trang sách để tra tựa đề ở nơi khác. Goodreads và trang lớn nhất của ngôn ngữ bạn được bật sẵn.", bsFind: "Tìm trên", bsGlobal: "Mọi nơi", bsLocal: "Cho ngôn ngữ của bạn", bsOther: "Nước khác", pgCard: "Trang được thiết kế lại", pgHint: "Mỗi tùy chọn thay một trang Audiobookshelf bằng bản NanoHive. Tắt để trở về bản gốc.", pgNarrators: "Thẻ người đọc", pgCollections: "Trang bộ sưu tập", pgUsers: "Thẻ người dùng (cài đặt)", pgStats: "Xếp hạng nghe", pgPhoto: "Ảnh hồ sơ trên thanh trên", pgCinematic: "Nền điện ảnh", pgTransitions: "Chuyển trang", pSearch: "Tìm cài đặt…", pSearchNone: "Không có kết quả.", erTypeface: "Kiểu chữ", erPage: "Chủ đề trang", erText: "Màu chữ", erBg: "Nền", erDefault: "Mặc định (ABS)", erAuto: "Tự động", nrForms: ["Người đọc","Người đọc"], nrBooksForms: ["cuốn","cuốn"], nrSearch: "Lọc người đọc…", nrSortName: "Tên", nrSortBooks: "Nhiều sách nhất", auForms: ["Tác giả","Tác giả"], auSearch: "Lọc tác giả…" , lfByAuthor: "Tác giả › thứ tự bộ", lfBySeries: "Bộ sách › thứ tự" , lfSecFilters: "Thêm bộ lọc", lfSecSort: "Sắp xếp nhiều cấp", lfAuthor: "Tác giả", lfSeries: "Bộ sách", lfTitle: "Tựa đề", lfYear: "Năm", lfAdded: "Ngày thêm", lfDuration: "Thời lượng", lfNarrator: "Người đọc", lfGenre: "Thể loại", lfLanguage: "Ngôn ngữ", lfProgress: "Tiến độ", lfPgFinished: "Đã xong", lfPgInProgress: "Đang nghe", lfPgNot: "Chưa bắt đầu" , logoUpload: 'Tải lên từ thiết bị…', logoUploaded: 'Đã tải lên ✓', logoBadType: 'Loại ảnh không được hỗ trợ', logoTooBig: 'Ảnh quá lớn (tối đa 4 MB)', logoOr: 'hoặc', bookPage: 'Trang sách', ratingsToggle: 'Đánh giá cộng đồng (sao và nhận xét)', ratingsHint: 'Đánh giá sao kèm nhận xét ngắn trên mỗi trang sách, chia sẻ với mọi người dùng của máy chủ này.', searchCard: 'Tìm kiếm', gsToggle: 'Tìm trong tất cả thư viện cùng lúc', gsHint: 'Một danh sách kết quả hợp nhất từ mọi thư viện bạn truy cập được, mỗi kết quả có huy hiệu thư viện. Tắt để trở về menu theo từng thư viện.', gsNoResults: 'Không có kết quả', gsSearching: 'Đang tìm…', gsBooks: 'Sách', gsSeries: 'Bộ sách', gsAuthors: 'Tác giả', spTitle: 'Trang bắt đầu', spHint: 'Luôn mở ứng dụng ở chế độ xem thư viện đã chọn (chỉ trình duyệt này). Liên kết trực tiếp không bao giờ bị chuyển hướng.', spDefault: 'Mặc định của ứng dụng', spViewHome: 'Trang chủ', spViewLibrary: 'Thư viện', spViewSeries: 'Bộ sách', spViewCollections: 'Bộ sưu tập', spViewAuthors: 'Tác giả', scSet: 'Đặt bìa bộ sách', scRemove: 'Gỡ bìa bộ sách', scEditDesc: 'Sửa mô tả bộ sách', cardStars: 'Sao trên bìa sách', hoverPolish: 'Trau chuốt khi rê chuột (tối dịu hơn, nút phát nổi bật, sao khi rê)', sectUser: 'Cài đặt của bạn', sectAdmin: 'Quản trị', lockTitle: 'Công tắc tính năng', lockHint: 'Buộc tắt một tính năng cho mọi người dùng của máy chủ này. Thắng cài đặt cá nhân; người khác thấy sau lần tải lại tiếp theo.', lockRatings: 'Tắt đánh giá ở mọi nơi', lockCardRatings: 'Tắt sao trên bìa', lockGlobalSearch: 'Tắt tìm kiếm toàn cục', lockHoverPolish: 'Tắt trau chuốt thẻ', lockHeroCarousel: 'Tắt băng chuyền trang chủ', lockedNote: 'bị quản trị viên tắt', lfSort: 'Đánh giá', lfRated: 'Đã đánh giá', lfUnrated: 'Chưa đánh giá', lfClear: 'Xóa', lfNone: 'Không có sách phù hợp', lfSection: 'Đánh giá', lfWho: 'Đánh giá của ai', lfEveryone: 'Mọi người', sdSave: 'Lưu', sdClear: 'Khôi phục mô tả sách', sdMore: 'Đọc thêm', sdLess: 'Thu gọn', shSeries: 'Bộ sách', shBy: 'của ', shMore: ' và nữa', shBookForms: ['cuốn', 'cuốn'], shAvg: 'trung bình sách', shRated: 'đã đánh giá', impLabel: 'Nhập đánh giá', impHint: 'Mang đánh giá của bạn từ StoryGraph hoặc Goodreads sang. Xuất sách ở đó ra CSV rồi chọn tệp tại đây. Mỗi kết quả khớp sẽ hiển thị để bạn xác nhận và không có gì được lưu cho đến khi bạn đồng ý.', impBtn: 'Chọn tệp CSV…', impReading: 'Đang đọc tệp…', impMatching: 'Đang đối chiếu với thư viện của bạn…', impErrUnknown: 'Tệp này không giống bản xuất từ StoryGraph hay Goodreads. Cần là CSV của họ, có cột "Star Rating" hoặc "My Rating".', impErrEmpty: 'Tệp không có dòng nào.', impErrNoRated: 'Không có sách nào được đánh giá trong tệp.', impErrRead: 'Không đọc được tệp.', impErrLoad: 'Không tải được thư viện của bạn. Thử lại sau giây lát.', impErrNoLibs: 'Không thư viện nào tham gia đánh giá nên không có nơi để nhập.', impTitle: 'Nhập đánh giá', impFrom: 'Bản xuất %s: %d cuốn, trong đó %d đã đánh giá', impLibs: 'Đang tìm trong: %s', impMatched: 'Đã khớp', impCheck: 'Nên xem lại', impCheckHint: 'Gần đúng nhưng chưa chắc. Kiểm tra kết quả khớp hoặc chọn cái khác.', impAlready: 'Bạn đã đánh giá', impAlreadyHint: 'Giữ nguyên trừ khi bạn chọn ghi đè.', impNone: 'Không có trong thư viện của bạn', impNoneHint: 'Không có gì trên máy chủ này khớp với các mục này.', impAll: 'Tất cả', impSkip: 'Bỏ qua mục này', impDup: 'trùng lặp', impByTitle: 'tựa đề', impYours: 'của bạn: %s', impReviews: 'Nhập cả nhận xét đã viết (%d)', impOver: 'Ghi đè các đánh giá tôi đã có', impGo: 'Nhập %d đánh giá', impCancel: 'Hủy', impCloseBtn: 'Đóng', impWorking: 'Đang lưu… %d / %d', impDone: 'Đã nhập %d đánh giá.', impFail: '%d không lưu được.', rdTitle: 'Ai đang đọc cuốn này', rdFin: 'Đã đọc xong', socTitle: 'Tính năng xã hội', socHint: 'Những gì máy chủ này chia sẻ giữa người dùng. Thời gian nghe nuôi bảng xếp hạng; hai mục còn lại tiết lộ ai đang đọc gì nên sẽ tắt cho đến khi bạn bật. Áp dụng cho tất cả cùng lúc.', socTime: 'Thời gian nghe (bảng xếp hạng máy chủ)', socContent: 'Các tựa nổi bật trong thống kê chia sẻ', socWho: 'Ai đang đọc (trang sách)', socUsers: 'Theo người dùng', socUsersHint: 'Tắt ai đó thì không dữ liệu nào của họ được chia sẻ, bất kể họ chọn gì; chỉ bạn mới bật lại được. "Tự rút lui" nghĩa là người đó tự nói không.', socOpted: 'tự rút lui', srvTitle: 'Mặc định máy chủ', srvHint: 'Lưu cài đặt hiện tại làm mặc định cho mọi người dùng của máy chủ này. Ai cũng vẫn có thể cá nhân hóa thêm. Gắn một volume vào /data/nh trong container giao diện để giữ chúng qua các bản cập nhật.', srvSave: 'Lưu làm mặc định máy chủ', srvClear: 'Xóa mặc định máy chủ', srvSaved: 'Đã lưu', srvCleared: 'Đã xóa', srvErr: 'Thất bại: cần đăng nhập quản trị viên', crMode: 'Kệ Đọc tiếp', crCombine: 'Gộp vào băng chuyền', crSeparate: 'Giữ làm kệ riêng', crHide: 'Ẩn', fsHint: 'Chia sẻ việc nghe của bạn với những người khác trên máy chủ này: tổng thời gian và số phút mỗi ngày cho bảng xếp hạng, và nếu quản trị viên đã bật, cả các tựa nổi bật và những cuốn bạn đang đọc hoặc đã xong. Bật mặc định. Tắt đi thì mọi thứ đã chia sẻ bị xóa.' , amfHide: 'Ẩn Đánh dấu đã đọc xong' , amfConfirm: 'Đánh dấu cuốn sách này là đã đọc xong?' , fsHint: 'Chia sẻ tóm tắt việc nghe của bạn (tổng và số phút mỗi ngày) với những người khác trên máy chủ này, nó nuôi xếp hạng máy chủ. Bật mặc định. Tắt đi thì bạn rời khỏi xếp hạng và các tổng của nó, dữ liệu đã chia sẻ bị xóa.' , srToggle: 'Chia sẻ bạn đang đọc sách gì', srHint: 'Thêm vào những gì người khác thấy: sách bạn đang đọc, đã đọc xong và nghe nhiều nhất. Tắt theo mặc định; các công tắc máy chủ của quản trị viên cũng phải được bật.' , fsHint: 'Chia sẻ thời gian nghe của bạn (tổng và số phút mỗi ngày) với những người khác trên máy chủ này; nó nuôi bảng xếp hạng. Bật theo mặc định. Tắt đi thì người dùng khác không còn thấy bạn; quản trị viên máy chủ vẫn thấy hoạt động, được đánh dấu là đã rút lui.', srHint: 'Cũng cho phép người khác thấy bạn đang đọc sách gì, đã đọc xong và nghe nhiều nhất. Không có gì hiển thị cho đến khi quản trị viên bật các công tắc máy chủ tương ứng.', seshTitle: 'Phiên gần đây', seshNow: 'Đang nghe' , hcHint: 'Gửi đánh giá của bạn đến tài khoản Hardcover. Dán khóa API từ cài đặt tài khoản Hardcover; khóa hết hạn sau một năm.', hcKeyLbl: 'Khóa API', hcToggle: 'Tự động đồng bộ đánh giá mới', hcPushAll: 'Gửi tất cả đánh giá', hcPushDiff: 'Chỉ gửi khác biệt', hcDone: 'Xong: %d đã đồng bộ, %d không khớp', hcNoKey: 'Hãy dán khóa API Hardcover của bạn trước' , hcMissHead: 'Không tìm thấy trên Hardcover:' , hcHint: 'Gửi đánh giá của bạn đến tài khoản Hardcover. Dán khóa API từ cài đặt tài khoản Hardcover.' , tabStyle: 'Kiểu dáng', tabMain: 'Trang chính', tabBooks: 'Sách', tabFeat: 'Tính năng' , hcImport: 'Nhập từ Hardcover', impMarkQ: 'Đánh dấu %d sách đã khớp là hoàn thành ở đây luôn?', impMarkDone: 'Đã đánh dấu %d là hoàn thành.' , impHint: 'Chuyển đánh giá của bạn từ Goodreads, StoryGraph hoặc Hardcover. Chọn tệp CSV đã xuất tại đây; Hardcover nhập trực tiếp bằng nút trên thẻ của nó. Mỗi kết quả khớp được hiển thị để xác nhận trước khi lưu.' , impErrUnknown: 'Tệp này không giống bản xuất từ Goodreads, StoryGraph hay Hardcover: không tìm thấy cột tiêu đề và đánh giá.' , fsCard: 'Xã hội', rnToggle: 'Hiện tên tôi cùng đánh giá', rsToggle: 'Chia sẻ đánh giá của tôi với người khác', socNames: 'Tên cùng đánh giá', rtAnon: 'Ẩn danh' },
  };
  // v2.1 strings. Kept as their OWN object rather than appended to the giant
  // one-line dicts above: those lines are ~8KB each and a bad edit to one of
  // them is unreviewable. Merged into PANEL_T by the same loop below, so
  // lookups and the `T.x || PANEL_T.en.x` fallback work identically.
  const NH_T_V21 = {
    en: {
      vfRelease: 'Release notes for this version on GitHub',
      spAll: 'Series finished', spAllN: 'All {n} finished', spSome: '{d} of {n} finished', spNone: 'Not started',
      sprLabel: 'Series completion badge',
      sprHint: 'A tick on a series cover: green when every book is finished, amber once you have started it.',
      mfTitle: 'Filtering & Sorting', mfLabel: 'New filter & sort panel',
      mfHint: 'One panel for filters and sorting, with several sort levels at once. Turn this off to go back to Audiobookshelf’s own dropdown menus.',
      mfButton: 'Filter & sort', mfSort: 'Sort', mfFilter: 'Filter', mfClear: 'Clear all', mfDone: 'Done',
      mfAddSort: 'Add a sort level…', mfNoSort: 'Default order', mfSearch: 'Search…',
      mfNoValues: 'Nothing to filter on here', mfMore: 'Show all {n}', mfAsc: 'A to Z', mfDesc: 'Z to A',
      mfAscNum: 'Lowest first', mfDescNum: 'Highest first', mfActive: 'Active', mfLoading: 'Loading…',
      mfShowing: '{n} of {t}', mfRemove: 'Remove', mfBySurname: 'By surname', mfByFirstName: 'By first name',
      lfTag: 'Tag', lfPublisher: 'Publisher', lfFormat: 'Format', lfBooks: 'Number of books',
      lfFmtAudio: 'Audiobook', lfFmtEbook: 'Ebook', lfFmtAbridged: 'Abridged',
      lfFmtExplicit: 'Explicit', lfFmtMissing: 'Missing files', lfFmtInvalid: 'Has a problem',
    },
    pl: {
      vfRelease: 'Informacje o tej wersji na GitHubie',
      spAll: 'Seria ukończona', spAllN: 'Ukończone wszystkie ({n})', spSome: 'Ukończone {d} z {n}', spNone: 'Nierozpoczęta',
      sprLabel: 'Znacznik ukończenia serii',
      sprHint: 'Znacznik na okładce serii: zielony, gdy wszystkie książki są ukończone, bursztynowy po rozpoczęciu.',
      mfTitle: 'Filtrowanie i sortowanie', mfLabel: 'Nowy panel filtrów i sortowania',
      mfHint: 'Jeden panel na filtry i sortowanie, z kilkoma poziomami sortowania naraz. Wyłącz, aby wrócić do menu Audiobookshelf.',
      mfButton: 'Filtruj i sortuj', mfSort: 'Sortowanie', mfFilter: 'Filtry', mfClear: 'Wyczyść', mfDone: 'Gotowe',
      mfAddSort: 'Dodaj poziom sortowania…', mfNoSort: 'Kolejność domyślna', mfSearch: 'Szukaj…',
      mfNoValues: 'Brak wartości do filtrowania', mfMore: 'Pokaż wszystkie ({n})', mfAsc: 'Od A do Z', mfDesc: 'Od Z do A',
      mfAscNum: 'Od najniższych', mfDescNum: 'Od najwyższych', mfActive: 'Aktywne', mfLoading: 'Wczytywanie…',
      mfShowing: '{n} z {t}', mfRemove: 'Usuń', mfBySurname: 'Wg nazwiska', mfByFirstName: 'Wg imienia',
      lfTag: 'Etykieta', lfPublisher: 'Wydawca', lfFormat: 'Format', lfBooks: 'Liczba książek',
      lfFmtAudio: 'Audiobook', lfFmtEbook: 'E-book', lfFmtAbridged: 'Skrócona',
      lfFmtExplicit: 'Dla dorosłych', lfFmtMissing: 'Brakujące pliki', lfFmtInvalid: 'Problem z plikami',
    },
    de: { cmStopped: "abgebrochen", cmScanErrs: "{e} nicht geprüft (Goodreads-Fehler)", cmScanBusy: "Es läuft bereits ein Durchlauf", cmGrErr: "Goodreads nicht erreichbar, neuer Versuch folgt", cmNoMatchYet: "Kein Goodreads-Treffer", cmLooking: "Suche auf Goodreads…", cmToolHint: "Was Goodreads-Leser von diesem Buch halten. Das ? zeigt das zugeordnete Buch und lässt es ändern.", cmSearchPh: "Goodreads durchsuchen…", cmNoVotes: "noch keine Bewertungen", cmRefresh: "Aktualisieren", cmScanAll: "Alles neu prüfen, nicht nur die fehlenden", cmScanDone: "Fertig: {m} zugeordnet · {x} nicht gefunden · {s} hatten schon eine", cmStop: "Stopp", cmScanLibs: "Zu durchsuchende Bibliotheken", cmScan: "Nach Bewertungen suchen", lsLongest: "Längste Bücher", lsSize: "Größe auf der Platte", lsDuration: "Gesamtlänge", lsGenresN: "Genres", lsSeriesN: "Serien", lsAuthorsN: "Autoren", lsBooks: "Bücher", lsGlance: "Bibliothek auf einen Blick", cmSetup: "Noch kein Helfer? Diesen neben dem Theme starten (Compose-Zeilen unten), dann auf Suchen drücken. Jede andere Art, das Image zu starten, geht auch: Adresse oben eintragen.", cmOpen: "Auf Goodreads öffnen", cmDetect: "Suchen", cmAddr: "Adresse des Helfers", scmTitle: "Serien-Seite", scmHint: "So sieht die erzeugte Titelgrafik einer Serie aus, wenn kein eigenes Cover hochgeladen wurde (nur dieser Browser).", scmDeck: "Gestapeltes Deck", vfRelease: 'Versionshinweise zu dieser Version auf GitHub', spAll: 'Serie abgeschlossen', spSome: '{d} von {n} abgeschlossen', spNone: 'Nicht begonnen' },
    fr: { cmStopped: "arrêté", cmScanErrs: "{e} non vérifiés (erreurs Goodreads)", cmScanBusy: "Une analyse est déjà en cours", cmGrErr: "Goodreads injoignable, nouvel essai à venir", cmNoMatchYet: "Aucune correspondance sur Goodreads", cmLooking: "Recherche sur Goodreads…", cmToolHint: "Ce que les lecteurs Goodreads pensent de ce livre. Le ? montre le livre associé et permet de le changer.", cmSearchPh: "Chercher sur Goodreads…", cmNoVotes: "pas encore de notes", cmRefresh: "Actualiser", cmScanAll: "Tout réanalyser, pas seulement les manquants", cmScanDone: "Terminé : {m} trouvés · {x} introuvables · {s} en avaient déjà", cmStop: "Arrêter", cmScanLibs: "Bibliothèques à parcourir", cmScan: "Rechercher les notes", lsLongest: "Livres les plus longs", lsSize: "Taille sur le disque", lsDuration: "Durée totale", lsGenresN: "Genres", lsSeriesN: "Séries", lsAuthorsN: "Auteurs", lsBooks: "Livres", lsGlance: "La bibliothèque en un coup d’œil", cmSetup: "Pas encore d’assistant ? Lancez ceci à côté du thème (lignes compose ci-dessous), puis cliquez sur Chercher. Toute autre façon de lancer l’image convient aussi : collez son adresse ci-dessus.", cmOpen: "Ouvrir sur Goodreads", cmDetect: "Chercher", cmAddr: "Adresse de l’assistant", scmTitle: "Page de série", scmHint: "Apparence de la couverture générée au-dessus du titre d’une série quand aucune couverture n’a été téléversée (ce navigateur uniquement).", scmDeck: "Pile en éventail", vfRelease: 'Notes de version sur GitHub', spAll: 'Série terminée', spSome: '{d} sur {n} terminés', spNone: 'Non commencée' },
    es: { cmStopped: "detenido", cmScanErrs: "{e} sin comprobar (errores de Goodreads)", cmScanBusy: "Ya hay un escaneo en marcha", cmGrErr: "Goodreads no responde, se reintentará", cmNoMatchYet: "Sin coincidencia en Goodreads", cmLooking: "Buscando en Goodreads…", cmToolHint: "Lo que opinan los lectores de Goodreads de este libro. El ? muestra el libro asociado y permite cambiarlo.", cmSearchPh: "Buscar en Goodreads…", cmNoVotes: "sin valoraciones aún", cmRefresh: "Actualizar", cmScanAll: "Volver a revisar todo, no solo los que faltan", cmScanDone: "Listo: {m} encontrados · {x} sin resultado · {s} ya tenían", cmStop: "Detener", cmScanLibs: "Bibliotecas a recorrer", cmScan: "Buscar valoraciones", lsLongest: "Libros más largos", lsSize: "Tamaño en disco", lsDuration: "Duración total", lsGenresN: "Géneros", lsSeriesN: "Series", lsAuthorsN: "Autores", lsBooks: "Libros", lsGlance: "La biblioteca de un vistazo", cmSetup: "¿Aún sin ayudante? Ejecuta esto junto al tema (líneas compose abajo) y pulsa Buscar. Cualquier otra forma de ejecutar la imagen también vale: pega su dirección arriba.", cmOpen: "Abrir en Goodreads", cmDetect: "Buscar", cmAddr: "Dirección del ayudante", scmTitle: "Página de serie", scmHint: "Aspecto de la portada generada sobre el título de una serie cuando no se ha subido una propia (solo este navegador).", scmDeck: "Baraja apilada", vfRelease: 'Notas de esta versión en GitHub', spAll: 'Serie terminada', spSome: '{d} de {n} terminados', spNone: 'Sin empezar' },
    nl: { cmStopped: "gestopt", cmScanErrs: "{e} niet gecontroleerd (Goodreads-fouten)", cmScanBusy: "Er loopt al een scan", cmGrErr: "Goodreads onbereikbaar, probeert opnieuw", cmNoMatchYet: "Geen match op Goodreads", cmLooking: "Zoeken op Goodreads…", cmToolHint: "Wat Goodreads-lezers van dit boek vinden. Het ? toont het gekoppelde boek en laat je het wijzigen.", cmSearchPh: "Zoeken op Goodreads…", cmNoVotes: "nog geen beoordelingen", cmRefresh: "Vernieuwen", cmScanAll: "Alles opnieuw nakijken, niet alleen wat ontbreekt", cmScanDone: "Klaar: {m} gevonden · {x} niet gevonden · {s} hadden er al een", cmStop: "Stoppen", cmScanLibs: "Te doorzoeken bibliotheken", cmScan: "Beoordelingen zoeken", lsLongest: "Langste boeken", lsSize: "Grootte op schijf", lsDuration: "Totale duur", lsGenresN: "Genres", lsSeriesN: "Series", lsAuthorsN: "Auteurs", lsBooks: "Boeken", lsGlance: "Bibliotheek in één oogopslag", cmSetup: "Nog geen helper? Draai dit naast het thema (compose-regels hieronder) en druk op Zoeken. Elke andere manier om de image te draaien werkt ook: plak het adres hierboven.", cmOpen: "Openen op Goodreads", cmDetect: "Zoeken", cmAddr: "Adres van de helper", scmTitle: "Seriepagina", scmHint: "Hoe de gegenereerde omslag boven een serietitel eruitziet als er geen eigen omslag is geüpload (alleen deze browser).", scmDeck: "Gestapelde waaier", vfRelease: 'Release-opmerkingen voor deze versie op GitHub', spAll: 'Serie voltooid', spSome: '{d} van {n} voltooid', spNone: 'Niet begonnen' },
    it: { cmStopped: "interrotto", cmScanErrs: "{e} non controllati (errori Goodreads)", cmScanBusy: "Una scansione è già in corso", cmGrErr: "Goodreads non raggiungibile, riproverò", cmNoMatchYet: "Nessuna corrispondenza su Goodreads", cmLooking: "Ricerca su Goodreads…", cmToolHint: "Cosa pensano di questo libro i lettori di Goodreads. Il ? mostra il libro abbinato e permette di cambiarlo.", cmSearchPh: "Cerca su Goodreads…", cmNoVotes: "nessuna valutazione", cmRefresh: "Aggiorna", cmScanAll: "Ricontrolla tutto, non solo i mancanti", cmScanDone: "Fatto: {m} trovati · {x} non trovati · {s} già presenti", cmStop: "Ferma", cmScanLibs: "Librerie da analizzare", cmScan: "Cerca valutazioni", lsLongest: "Libri più lunghi", lsSize: "Spazio su disco", lsDuration: "Durata totale", lsGenresN: "Generi", lsSeriesN: "Serie", lsAuthorsN: "Autori", lsBooks: "Libri", lsGlance: "La libreria a colpo d’occhio", cmSetup: "Nessun aiutante ancora? Avvia questo accanto al tema (righe compose sotto), poi premi Cerca. Va bene anche qualsiasi altro modo di avviare l’immagine: incolla il suo indirizzo sopra.", cmOpen: "Apri su Goodreads", cmDetect: "Cerca", cmAddr: "Indirizzo dell’aiutante", scmTitle: "Pagina della serie", scmHint: "Aspetto della copertina generata sopra il titolo di una serie quando non ne è stata caricata una (solo questo browser).", scmDeck: "Mazzo sovrapposto", vfRelease: 'Note di rilascio di questa versione su GitHub', spAll: 'Serie completata', spSome: '{d} di {n} completati', spNone: 'Non iniziata' },
  };

  function navLabel() {
    const lang = getUserLanguage().split('-')[0].toLowerCase();
    const map = { en: 'Customizations', pl: 'Personalizacja', de: 'Anpassung', fr: 'Personnalisation' };
    return map[lang] || map.en;
  }

  const PANEL_T = {
    en: {"title": "Theme Customizations", "subtitle": "Personalise the look of your library. Changes save automatically.", "branding": "Branding & Style", "colour": "Colour & Theme", "homeCar": "Home & Carousel", "sidebar": "Sidebar Menus", "appName": "App Name", "appNameHint": "Leave empty for the default name.", "logoUrl": "Custom Logo URL", "logoHint": "Leave empty for the default logo.", "accent": "Accent Colour", "baseTheme": "Base Theme", "mainFont": "Main Font", "carousel": "Carousel Auto-Advance", "carouselHint": "Seconds between slides. Set to 0 to disable.", "customSeries": "Expanded Recent Series", "seriesCount": "Recent Series Count", "seriesCountHint": "How many series to show in the expanded shelf.", "hideShelves": "Hide Homepage Shelves", "sidebarHint": "Hide left-rail entries you don't use.", "showAppName": "Show App Name Text", "colorizeLogo": "Colorize Logo with Accent Colour", "hideSeries": "Hide Series", "hideCollections": "Hide Collections", "hideAuthors": "Hide Authors", "hideNarrators": "Hide Narrators", "hideStats": "Hide Stats", "hideRecentlyAdded": "Hide Recently Added", "hideRecentSeries": "Hide Recent Series", "hideContinueSeries": "Hide Continue Series", "hideListenAgain": "Hide Listen Again", "hideDiscover": "Hide Discover", "hideNewestAuthors": "Hide Newest Authors", "seriesCards": "Stacked Series Covers", "heroCarousel": "Home Carousel", "gearLabel": "Theme", "srvTitle": "Server Defaults", "srvHint": "Save your current settings as the defaults for every user of this server. Users can still personalise their own look on top. Mount a volume at /data/nh in the theme container to keep these across updates.", "srvSave": "Save as server defaults", "srvClear": "Clear server defaults", "srvSaved": "Saved", "srvCleared": "Cleared", "srvErr": "Failed, admin login required", "crMode": "Continue Reading Shelf", "crCombine": "Combine into carousel", "crSeparate": "Keep as separate shelf", "crHide": "Hidden", "logoUpload": "Upload from device…", "logoUploaded": "Uploaded ✓", "logoBadType": "Unsupported image type", "logoTooBig": "Image too large (max 4 MB)", "logoOr": "or", "bookPage": "Book Page", "ratingsToggle": "Community ratings (stars & reviews)", "ratingsHint": "Star ratings with short reviews on each book page, shared with every user of this server.", "searchCard": "Search", "gsToggle": "Search all libraries at once", "gsHint": "One merged result list across every library you can access, with a library badge on each match. Turn off to restore the per-library dropdown.", "gsNoResults": "No results", "gsSearching": "Searching…", "gsBooks": "Books", "gsSeries": "Series", "gsAuthors": "Authors", "spTitle": "Start Page", "spHint": "Open the app on a chosen library view every time (this browser only). Direct links are never redirected.", "spDefault": "App default", "spViewHome": "Home", "spViewLibrary": "Library", "spViewSeries": "Series", "spViewCollections": "Collections", "spViewAuthors": "Authors", "scSet": "Set series cover", "scRemove": "Remove series cover", "scEditDesc": "Edit series description", "scmTitle": "Series Page", "scmHint": "How the generated cover above a series title looks when no custom cover was uploaded (this browser only).", "scmDeck": "Layered deck", "scmFirst": "First book's cover", "scmGrid": "Cover grid (default)", "scmOff": "Hidden", "cardStars": "Star badges on book covers", "hoverPolish": "Card hover polish (softer dim, accent play, stars on hover)", "sectUser": "Your settings", "sectAdmin": "Administration", "lockTitle": "Feature switches", "lockHint": "Force-disable a feature for every user of this server. Wins over personal settings; others see it after their next reload.", "lockRatings": "Disable ratings everywhere", "lockCardRatings": "Disable star badges on covers", "lockGlobalSearch": "Disable global search", "lockHoverPolish": "Disable card hover polish", "lockHeroCarousel": "Disable home carousel", "lockedNote": "disabled by admin", "nrBooksForms": ["book", "books"], "nrSearch": "Filter narrators…", "nrSortName": "Name", "nrSortBooks": "Most books", "nrForms": ["Narrator", "Narrators"], "lfSort": "Rating", "lfRated": "Rated", "lfUnrated": "Unrated", "lfClear": "Clear", "lfNone": "No books match", "lfSection": "Rating", "sdSave": "Save", "sdClear": "Restore book description", "sdMore": "Read more", "sdLess": "Show less", "shSeries": "Series", "shBy": "by ", "shMore": " & more", "shBookForms": ["book", "books"], "shAvg": "book avg", "shRated": "rated", "ssLabel": "Star rating steps", "ssHint": "How precise a rating can be. Applies to what you can pick and to how every rating is drawn.", "ssFull": "Full stars", "ssHalf": "Half stars", "ssQuarter": "Quarter stars", "rlLabel": "Ratings per library", "rlHint": "Which libraries take part in ratings. Podcast libraries are off unless you turn them on.", "rlPodcast": "podcasts", "rfHide": "Hide Rate what you finished", "impLabel": "Import ratings", "impHint": "Bring ratings over from StoryGraph or Goodreads. Export your books there as CSV, then pick the file here, every match is shown for you to confirm and nothing is saved until you do.", "impBtn": "Choose CSV file…", "impReading": "Reading the file…", "impMatching": "Matching against your libraries…", "impErrUnknown": "That does not look like a StoryGraph or Goodreads export. It should be the CSV they give you, with a \"Star Rating\" or \"My Rating\" column.", "impErrEmpty": "That file has no rows.", "impErrNoRated": "There are no rated books in that file.", "impErrRead": "The file could not be read.", "impErrLoad": "Your libraries could not be loaded. Try again in a moment.", "impErrNoLibs": "No library takes part in ratings, so there is nothing to import into.", "impTitle": "Import ratings", "impFrom": "%s export, %d books, %d of them rated", "impLibs": "Searching: %s", "impMatched": "Matched", "impCheck": "Worth a look", "impCheckHint": "Close but not certain, check the match, or pick another one.", "impAlready": "Already rated by you", "impAlreadyHint": "Left alone unless you ask for them to be overwritten.", "impNone": "Not in your library", "impNoneHint": "Nothing on this server matches these.", "impAll": "All", "impSkip": "Skip this one", "impDup": "duplicate", "impByTitle": "title", "impYours": "yours: %s", "impReviews": "Import written reviews too (%d)", "impOver": "Overwrite ratings I already have", "impGo": "Import %d ratings", "impCancel": "Cancel", "impCloseBtn": "Close", "impWorking": "Saving… %d of %d", "impDone": "Imported %d ratings.", "impFail": "%d could not be saved." },
    pl: {"title": "Personalizacja motywu", "subtitle": "Dostosuj wygląd swojej biblioteki. Zmiany zapisują się automatycznie.", "branding": "Marka i styl", "colour": "Kolor i motyw", "homeCar": "Strona główna i karuzela", "sidebar": "Menu boczne", "appName": "Nazwa aplikacji", "appNameHint": "Pozostaw puste, aby użyć domyślnej nazwy.", "logoUrl": "Adres URL własnego logo", "logoHint": "Pozostaw puste, aby użyć domyślnego logo.", "accent": "Kolor akcentu", "baseTheme": "Motyw bazowy", "mainFont": "Czcionka główna", "carousel": "Automatyczne przewijanie karuzeli", "carouselHint": "Sekundy między slajdami. Ustaw 0, aby wyłączyć.", "customSeries": "Rozszerzone ostatnie serie", "seriesCount": "Liczba ostatnich serii", "seriesCountHint": "Ile serii pokazać na rozszerzonej półce.", "hideShelves": "Ukryj półki strony głównej", "sidebarHint": "Ukryj nieużywane pozycje menu bocznego.", "showAppName": "Pokaż nazwę aplikacji", "colorizeLogo": "Pokoloruj logo kolorem akcentu", "hideSeries": "Ukryj Serie", "hideCollections": "Ukryj Kolekcje", "hideAuthors": "Ukryj Autorów", "hideNarrators": "Ukryj Lektorów", "hideStats": "Ukryj Statystyki", "hideRecentlyAdded": "Ukryj Ostatnio dodane", "hideRecentSeries": "Ukryj Ostatnie serie", "hideContinueSeries": "Ukryj Kontynuuj serię", "hideListenAgain": "Ukryj Słuchaj ponownie", "hideDiscover": "Ukryj Odkrywaj", "hideNewestAuthors": "Ukryj Najnowszych autorów", "seriesCards": "Nakładane okładki serii", "heroCarousel": "Karuzela na stronie głównej", "gearLabel": "Motyw", "srvTitle": "Domyślne ustawienia serwera", "srvHint": "Zapisz bieżące ustawienia jako domyślne dla wszystkich użytkowników tego serwera. Użytkownicy nadal mogą personalizować swój wygląd. Zamontuj wolumin w /data/nh w kontenerze motywu, aby zachować je między aktualizacjami.", "srvSave": "Zapisz jako domyślne serwera", "srvClear": "Wyczyść domyślne serwera", "srvSaved": "Zapisano", "srvCleared": "Wyczyszczono", "srvErr": "Błąd, wymagane konto administratora", "crMode": "Półka Kontynuuj czytanie", "crCombine": "Połącz z karuzelą", "crSeparate": "Osobna półka", "crHide": "Ukryta", "logoUpload": "Wgraj z urządzenia…", "logoUploaded": "Wgrano ✓", "logoBadType": "Nieobsługiwany typ obrazu", "logoTooBig": "Obraz za duży (maks. 4 MB)", "logoOr": "lub", "bookPage": "Strona książki", "ratingsToggle": "Oceny społeczności (gwiazdki i recenzje)", "ratingsHint": "Oceny z krótkimi recenzjami na stronie książki, wspólne dla wszystkich użytkowników tego serwera.", "searchCard": "Wyszukiwanie", "gsToggle": "Szukaj we wszystkich bibliotekach naraz", "gsHint": "Jedna lista wyników ze wszystkich dostępnych bibliotek, z odznaką biblioteki przy każdym trafieniu. Wyłącz, aby wrócić do wyszukiwania w bieżącej bibliotece.", "gsNoResults": "Brak wyników", "gsSearching": "Szukam…", "gsBooks": "Książki", "gsSeries": "Serie", "gsAuthors": "Autorzy", "spTitle": "Strona startowa", "spHint": "Otwieraj aplikację na wybranym widoku biblioteki (tylko ta przeglądarka). Linki bezpośrednie nie są przekierowywane.", "spDefault": "Domyślna aplikacji", "spViewHome": "Strona główna", "spViewLibrary": "Biblioteka", "spViewSeries": "Serie", "spViewCollections": "Kolekcje", "spViewAuthors": "Autorzy", "scSet": "Ustaw okładkę serii", "scRemove": "Usuń okładkę serii", "scEditDesc": "Edytuj opis serii", "scmTitle": "Strona serii", "scmHint": "Wygląd generowanej okładki nad tytułem serii, gdy nie wgrano własnej okładki (tylko ta przeglądarka).", "scmDeck": "Warstwowa talia", "scmFirst": "Okładka pierwszej książki", "scmGrid": "Siatka okładek (domyślnie)", "scmOff": "Ukryta", "cardStars": "Gwiazdki na okładkach książek", "hoverPolish": "Dopracowany hover kart (mniej przyciemnienia, akcentowy przycisk, gwiazdki po najechaniu)", "sectUser": "Twoje ustawienia", "sectAdmin": "Administracja", "lockTitle": "Przełączniki funkcji", "lockHint": "Wyłącza funkcję dla wszystkich użytkowników tego serwera. Nadpisuje ustawienia osobiste; inni zobaczą zmianę po odświeżeniu.", "lockRatings": "Wyłącz oceny całkowicie", "lockCardRatings": "Wyłącz gwiazdki na okładkach", "lockGlobalSearch": "Wyłącz wyszukiwanie globalne", "lockHoverPolish": "Wyłącz dopracowany hover kart", "lockHeroCarousel": "Wyłącz karuzelę główną", "lockedNote": "wyłączone przez administratora", "nrBooksForms": ["książka", "książki", "książek"], "nrSearch": "Filtruj lektorów…", "nrSortName": "Nazwa", "nrSortBooks": "Najwięcej książek", "nrForms": ["Lektor", "Lektorzy", "Lektorów"], "lfSort": "Ocena", "lfRated": "Ocenione", "lfUnrated": "Nieocenione", "lfClear": "Wyczyść", "lfNone": "Brak pasujących książek", "lfSection": "Ocena", "sdSave": "Zapisz", "sdClear": "Przywróć opis z książki", "sdMore": "Czytaj dalej", "sdLess": "Zwiń", "shSeries": "Seria", "shBy": "", "shMore": " i inni", "shBookForms": ["książka", "książki", "książek"], "shAvg": "śr. ocen książek", "shRated": "ocenionych", "ssLabel": "Dokładność ocen", "ssHint": "Jak precyzyjna może być ocena. Dotyczy zarówno wyboru, jak i sposobu rysowania każdej oceny.", "ssFull": "Całe gwiazdki", "ssHalf": "Połówki gwiazdek", "ssQuarter": "Ćwiartki gwiazdek", "rlLabel": "Oceny wg biblioteki", "rlHint": "Które biblioteki biorą udział w ocenach. Biblioteki podcastów są wyłączone, dopóki ich nie włączysz.", "rlPodcast": "podcasty", "rfHide": "Ukryj Oceń, co przeczytałeś", "impLabel": "Import ocen", "impHint": "Przenieś oceny ze StoryGraph lub Goodreads. Wyeksportuj tam swoje książki do pliku CSV i wskaż go tutaj, każde dopasowanie zobaczysz przed zapisem i nic nie zostanie zapisane, dopóki nie potwierdzisz.", "impBtn": "Wybierz plik CSV…", "impReading": "Czytanie pliku…", "impMatching": "Dopasowywanie do Twoich bibliotek…", "impErrUnknown": "To nie wygląda na eksport ze StoryGraph ani Goodreads. Potrzebny jest plik CSV z tych serwisów, z kolumną „Star Rating” lub „My Rating”.", "impErrEmpty": "Ten plik nie zawiera żadnych wierszy.", "impErrNoRated": "W tym pliku nie ma ocenionych książek.", "impErrRead": "Nie udało się odczytać pliku.", "impErrLoad": "Nie udało się wczytać bibliotek. Spróbuj ponownie za chwilę.", "impErrNoLibs": "Żadna biblioteka nie uczestniczy w ocenach, więc nie ma gdzie zaimportować.", "impTitle": "Import ocen", "impFrom": "Eksport z %s, %d książek, ocenionych %d", "impLibs": "Szukam w: %s", "impMatched": "Dopasowane", "impCheck": "Warto sprawdzić", "impCheckHint": "Blisko, ale bez pewności, sprawdź dopasowanie albo wybierz inne.", "impAlready": "Już ocenione przez Ciebie", "impAlreadyHint": "Pozostaną bez zmian, chyba że zaznaczysz nadpisywanie.", "impNone": "Brak w Twojej bibliotece", "impNoneHint": "Nic na tym serwerze nie odpowiada tym pozycjom.", "impAll": "Wszystkie", "impSkip": "Pomiń tę pozycję", "impDup": "duplikat", "impByTitle": "tytuł", "impYours": "Twoja: %s", "impReviews": "Importuj też recenzje (%d)", "impOver": "Nadpisz oceny, które już mam", "impGo": "Importuj %d ocen", "impCancel": "Anuluj", "impCloseBtn": "Zamknij", "impWorking": "Zapisywanie… %d z %d", "impDone": "Zaimportowano %d ocen.", "impFail": "Nie udało się zapisać: %d." },
    de: {"title": "Design-Anpassungen", "subtitle": "Personalisiere das Aussehen deiner Bibliothek. Änderungen werden automatisch gespeichert.", "branding": "Branding & Stil", "colour": "Farbe & Design", "homeCar": "Startseite & Karussell", "sidebar": "Seitenmenüs", "appName": "App-Name", "appNameHint": "Leer lassen für den Standardnamen.", "logoUrl": "Eigene Logo-URL", "logoHint": "Leer lassen für das Standardlogo.", "accent": "Akzentfarbe", "baseTheme": "Basis-Design", "mainFont": "Hauptschriftart", "carousel": "Karussell-Autowechsel", "carouselHint": "Sekunden zwischen Folien. 0 zum Deaktivieren.", "customSeries": "Erweiterte neueste Serien", "seriesCount": "Anzahl neuester Serien", "seriesCountHint": "Wie viele Serien im erweiterten Regal gezeigt werden.", "hideShelves": "Startseiten-Regale ausblenden", "sidebarHint": "Nicht genutzte Menüeinträge ausblenden.", "showAppName": "App-Namen anzeigen", "colorizeLogo": "Logo mit Akzentfarbe einfärben", "hideSeries": "Serien ausblenden", "hideCollections": "Sammlungen ausblenden", "hideAuthors": "Autoren ausblenden", "hideNarrators": "Sprecher ausblenden", "hideStats": "Statistiken ausblenden", "hideRecentlyAdded": "Kürzlich hinzugefügt ausblenden", "hideRecentSeries": "Neueste Serien ausblenden", "hideContinueSeries": "Serie fortsetzen ausblenden", "hideListenAgain": "Erneut anhören ausblenden", "hideDiscover": "Entdecken ausblenden", "hideNewestAuthors": "Neueste Autoren ausblenden", "seriesCards": "Gestapelte Serien-Cover", "heroCarousel": "Startseiten-Karussell", "gearLabel": "Design", "srvTitle": "Server-Standardwerte", "srvHint": "Speichere deine aktuellen Einstellungen als Standard für alle Nutzer dieses Servers. Nutzer können ihren Look weiterhin selbst anpassen. Binde ein Volume unter /data/nh im Theme-Container ein, um sie über Updates hinweg zu behalten.", "srvSave": "Als Server-Standard speichern", "srvClear": "Server-Standard löschen", "srvSaved": "Gespeichert", "srvCleared": "Gelöscht", "srvErr": "Fehlgeschlagen, Admin-Anmeldung erforderlich", "crMode": "Weiterlesen-Regal", "crCombine": "In Karussell integrieren", "crSeparate": "Als eigenes Regal", "crHide": "Ausgeblendet", "ssLabel": "Bewertungsschritte", "ssHint": "Wie genau eine Bewertung sein kann. Gilt für die Auswahl und für die Darstellung jeder Bewertung.", "ssFull": "Ganze Sterne", "ssHalf": "Halbe Sterne", "ssQuarter": "Viertelsterne", "rlLabel": "Bewertungen pro Bibliothek", "rlHint": "Welche Bibliotheken an Bewertungen teilnehmen. Podcast-Bibliotheken sind aus, bis du sie einschaltest.", "rlPodcast": "Podcasts", "rfHide": "Bewerte, was du beendet hast ausblenden"},
    fr: {"title": "Personnalisation du thème", "subtitle": "Personnalisez l’apparence de votre bibliothèque. Les modifications sont enregistrées automatiquement.", "branding": "Image de marque et style", "colour": "Couleur et thème", "homeCar": "Accueil et carrousel", "sidebar": "Menus latéraux", "appName": "Nom de l’application", "appNameHint": "Laissez vide pour le nom par défaut.", "logoUrl": "URL du logo personnalisé", "logoHint": "Laissez vide pour le logo par défaut.", "accent": "Couleur d’accent", "baseTheme": "Thème de base", "mainFont": "Police principale", "carousel": "Défilement automatique du carrousel", "carouselHint": "Secondes entre les diapositives. 0 pour désactiver.", "customSeries": "Séries récentes étendues", "seriesCount": "Nombre de séries récentes", "seriesCountHint": "Nombre de séries à afficher dans l’étagère étendue.", "hideShelves": "Masquer les étagères d’accueil", "sidebarHint": "Masquer les entrées du menu latéral inutilisées.", "showAppName": "Afficher le nom de l’application", "colorizeLogo": "Coloriser le logo avec la couleur d’accent", "hideSeries": "Masquer les séries", "hideCollections": "Masquer les collections", "hideAuthors": "Masquer les auteurs", "hideNarrators": "Masquer les narrateurs", "hideStats": "Masquer les statistiques", "hideRecentlyAdded": "Masquer Ajouts récents", "hideRecentSeries": "Masquer Séries récentes", "hideContinueSeries": "Masquer Continuer la série", "hideListenAgain": "Masquer Réécouter", "hideDiscover": "Masquer Découvrir", "hideNewestAuthors": "Masquer Nouveaux auteurs", "seriesCards": "Couvertures de séries empilées", "heroCarousel": "Carrousel d’accueil", "gearLabel": "Thème", "ssLabel": "Précision des notes", "ssHint": "La précision possible d’une note. S’applique au choix et à l’affichage de chaque note.", "ssFull": "Étoiles entières", "ssHalf": "Demi-étoiles", "ssQuarter": "Quarts d’étoile", "rlLabel": "Notes par bibliothèque", "rlHint": "Quelles bibliothèques participent aux notes. Les bibliothèques de podcasts sont désactivées tant que vous ne les activez pas.", "rlPodcast": "podcasts", "rfHide": "Masquer Notez ce que vous avez terminé"},
    es: {"title": "Personalización del tema", "subtitle": "Personaliza el aspecto de tu biblioteca. Los cambios se guardan automáticamente.", "branding": "Marca y estilo", "colour": "Color y tema", "homeCar": "Inicio y carrusel", "sidebar": "Menús laterales", "appName": "Nombre de la aplicación", "appNameHint": "Déjalo vacío para el nombre predeterminado.", "logoUrl": "URL de logotipo personalizado", "logoHint": "Déjalo vacío para el logotipo predeterminado.", "accent": "Color de acento", "baseTheme": "Tema base", "mainFont": "Fuente principal", "carousel": "Avance automático del carrusel", "carouselHint": "Segundos entre diapositivas. 0 para desactivar.", "customSeries": "Series recientes ampliadas", "seriesCount": "Número de series recientes", "seriesCountHint": "Cuántas series mostrar en el estante ampliado.", "hideShelves": "Ocultar estantes de inicio", "sidebarHint": "Oculta entradas del menú lateral que no uses.", "showAppName": "Mostrar nombre de la aplicación", "colorizeLogo": "Colorear logotipo con el color de acento", "hideSeries": "Ocultar Series", "hideCollections": "Ocultar Colecciones", "hideAuthors": "Ocultar Autores", "hideNarrators": "Ocultar Narradores", "hideStats": "Ocultar Estadísticas", "hideRecentlyAdded": "Ocultar Añadidos recientemente", "hideRecentSeries": "Ocultar Series recientes", "hideContinueSeries": "Ocultar Continuar serie", "hideListenAgain": "Ocultar Escuchar de nuevo", "hideDiscover": "Ocultar Descubrir", "hideNewestAuthors": "Ocultar Autores más recientes", "seriesCards": "Portadas de series apiladas", "heroCarousel": "Carrusel de inicio", "gearLabel": "Tema", "ssLabel": "Precisión de las valoraciones", "ssHint": "Con qué precisión se puede valorar. Se aplica a lo que puedes elegir y a cómo se dibuja cada valoración.", "ssFull": "Estrellas completas", "ssHalf": "Medias estrellas", "ssQuarter": "Cuartos de estrella", "rlLabel": "Valoraciones por biblioteca", "rlHint": "Qué bibliotecas participan en las valoraciones. Las bibliotecas de pódcast están desactivadas salvo que las actives.", "rlPodcast": "pódcast", "rfHide": "Ocultar Valora lo que has terminado"},
    it: {"title": "Personalizzazione del tema", "subtitle": "Personalizza l’aspetto della tua libreria. Le modifiche vengono salvate automaticamente.", "branding": "Brand e stile", "colour": "Colore e tema", "homeCar": "Home e carosello", "sidebar": "Menu laterali", "appName": "Nome dell’app", "appNameHint": "Lascia vuoto per il nome predefinito.", "logoUrl": "URL logo personalizzato", "logoHint": "Lascia vuoto per il logo predefinito.", "accent": "Colore d’accento", "baseTheme": "Tema di base", "mainFont": "Font principale", "carousel": "Avanzamento automatico carosello", "carouselHint": "Secondi tra le diapositive. 0 per disattivare.", "customSeries": "Serie recenti estese", "seriesCount": "Numero di serie recenti", "seriesCountHint": "Quante serie mostrare nello scaffale esteso.", "hideShelves": "Nascondi scaffali della home", "sidebarHint": "Nascondi le voci del menu laterale che non usi.", "showAppName": "Mostra nome dell’app", "colorizeLogo": "Colora il logo con il colore d’accento", "hideSeries": "Nascondi Serie", "hideCollections": "Nascondi Raccolte", "hideAuthors": "Nascondi Autori", "hideNarrators": "Nascondi Narratori", "hideStats": "Nascondi Statistiche", "hideRecentlyAdded": "Nascondi Aggiunti di recente", "hideRecentSeries": "Nascondi Serie recenti", "hideContinueSeries": "Nascondi Continua serie", "hideListenAgain": "Nascondi Riascolta", "hideDiscover": "Nascondi Scopri", "hideNewestAuthors": "Nascondi Autori più recenti", "seriesCards": "Copertine serie impilate", "heroCarousel": "Carosello della home", "gearLabel": "Tema", "ssLabel": "Precisione delle valutazioni", "ssHint": "Quanto può essere precisa una valutazione. Vale per la scelta e per come ogni valutazione viene disegnata.", "ssFull": "Stelle intere", "ssHalf": "Mezze stelle", "ssQuarter": "Quarti di stella", "rlLabel": "Valutazioni per libreria", "rlHint": "Quali librerie partecipano alle valutazioni. Le librerie di podcast sono disattivate finché non le attivi.", "rlPodcast": "podcast", "rfHide": "Nascondi Valuta ciò che hai finito"},
    pt: {"title": "Personalização do tema", "subtitle": "Personalize a aparência da sua biblioteca. As alterações são salvas automaticamente.", "branding": "Marca e estilo", "colour": "Cor e tema", "homeCar": "Início e carrossel", "sidebar": "Menus laterais", "appName": "Nome do aplicativo", "appNameHint": "Deixe vazio para o nome padrão.", "logoUrl": "URL de logotipo personalizado", "logoHint": "Deixe vazio para o logotipo padrão.", "accent": "Cor de destaque", "baseTheme": "Tema base", "mainFont": "Fonte principal", "carousel": "Avanço automático do carrossel", "carouselHint": "Segundos entre slides. 0 para desativar.", "customSeries": "Séries recentes expandidas", "seriesCount": "Número de séries recentes", "seriesCountHint": "Quantas séries mostrar na estante expandida.", "hideShelves": "Ocultar estantes da página inicial", "sidebarHint": "Oculte itens do menu lateral que você não usa.", "showAppName": "Mostrar nome do aplicativo", "colorizeLogo": "Colorir logotipo com a cor de destaque", "hideSeries": "Ocultar Séries", "hideCollections": "Ocultar Coleções", "hideAuthors": "Ocultar Autores", "hideNarrators": "Ocultar Narradores", "hideStats": "Ocultar Estatísticas", "hideRecentlyAdded": "Ocultar Adicionados recentemente", "hideRecentSeries": "Ocultar Séries recentes", "hideContinueSeries": "Ocultar Continuar série", "hideListenAgain": "Ocultar Ouvir novamente", "hideDiscover": "Ocultar Descobrir", "hideNewestAuthors": "Ocultar Autores mais recentes", "seriesCards": "Capas de séries empilhadas", "heroCarousel": "Carrossel da página inicial", "gearLabel": "Tema", "ssLabel": "Precisão das avaliações", "ssHint": "Quão precisa pode ser uma avaliação. Aplica-se ao que pode escolher e a como cada avaliação é desenhada.", "ssFull": "Estrelas inteiras", "ssHalf": "Meias estrelas", "ssQuarter": "Quartos de estrela", "rlLabel": "Avaliações por biblioteca", "rlHint": "Que bibliotecas participam nas avaliações. As bibliotecas de podcasts estão desligadas até as ligar.", "rlPodcast": "podcasts", "rfHide": "Ocultar Avalie o que terminou"},
    nl: {"title": "Thema-aanpassingen", "subtitle": "Personaliseer het uiterlijk van je bibliotheek. Wijzigingen worden automatisch opgeslagen.", "branding": "Branding & stijl", "colour": "Kleur & thema", "homeCar": "Home & carrousel", "sidebar": "Zijbalkmenu’s", "appName": "App-naam", "appNameHint": "Laat leeg voor de standaardnaam.", "logoUrl": "Aangepaste logo-URL", "logoHint": "Laat leeg voor het standaardlogo.", "accent": "Accentkleur", "baseTheme": "Basisthema", "mainFont": "Hoofdlettertype", "carousel": "Carrousel automatisch doorschuiven", "carouselHint": "Seconden tussen dia’s. 0 om uit te schakelen.", "customSeries": "Uitgebreide recente series", "seriesCount": "Aantal recente series", "seriesCountHint": "Hoeveel series in de uitgebreide plank tonen.", "hideShelves": "Homepagina-planken verbergen", "sidebarHint": "Verberg zijbalkitems die je niet gebruikt.", "showAppName": "App-naam tonen", "colorizeLogo": "Logo kleuren met accentkleur", "hideSeries": "Series verbergen", "hideCollections": "Collecties verbergen", "hideAuthors": "Auteurs verbergen", "hideNarrators": "Vertellers verbergen", "hideStats": "Statistieken verbergen", "hideRecentlyAdded": "Onlangs toegevoegd verbergen", "hideRecentSeries": "Recente series verbergen", "hideContinueSeries": "Serie voortzetten verbergen", "hideListenAgain": "Opnieuw luisteren verbergen", "hideDiscover": "Ontdekken verbergen", "hideNewestAuthors": "Nieuwste auteurs verbergen", "seriesCards": "Gestapelde seriecovers", "heroCarousel": "Home-carrousel", "gearLabel": "Thema", "ssLabel": "Nauwkeurigheid van beoordelingen", "ssHint": "Hoe nauwkeurig een beoordeling kan zijn. Geldt voor wat je kunt kiezen en hoe elke beoordeling wordt getekend.", "ssFull": "Hele sterren", "ssHalf": "Halve sterren", "ssQuarter": "Kwart sterren", "rlLabel": "Beoordelingen per bibliotheek", "rlHint": "Welke bibliotheken meedoen aan beoordelingen. Podcastbibliotheken staan uit tot je ze aanzet.", "rlPodcast": "podcasts", "rfHide": "Verberg Beoordeel wat je uit hebt"},
    cs: {"title": "Přizpůsobení motivu", "subtitle": "Přizpůsobte vzhled své knihovny. Změny se ukládají automaticky.", "branding": "Značka a styl", "colour": "Barva a motiv", "homeCar": "Domů a karusel", "sidebar": "Boční nabídky", "appName": "Název aplikace", "appNameHint": "Ponechte prázdné pro výchozí název.", "logoUrl": "URL vlastního loga", "logoHint": "Ponechte prázdné pro výchozí logo.", "accent": "Barva zvýraznění", "baseTheme": "Základní motiv", "mainFont": "Hlavní písmo", "carousel": "Automatické posouvání karuselu", "carouselHint": "Sekundy mezi snímky. 0 pro vypnutí.", "customSeries": "Rozšířené nedávné série", "seriesCount": "Počet nedávných sérií", "seriesCountHint": "Kolik sérií zobrazit v rozšířené poličce.", "hideShelves": "Skrýt poličky domovské stránky", "sidebarHint": "Skryjte položky bočního menu, které nepoužíváte.", "showAppName": "Zobrazit název aplikace", "colorizeLogo": "Obarvit logo barvou zvýraznění", "hideSeries": "Skrýt Série", "hideCollections": "Skrýt Kolekce", "hideAuthors": "Skrýt Autory", "hideNarrators": "Skrýt Vypravěče", "hideStats": "Skrýt Statistiky", "hideRecentlyAdded": "Skrýt Nedávno přidané", "hideRecentSeries": "Skrýt Nedávné série", "hideContinueSeries": "Skrýt Pokračovat v sérii", "hideListenAgain": "Skrýt Poslechnout znovu", "hideDiscover": "Skrýt Objevovat", "hideNewestAuthors": "Skrýt Nejnovější autory", "seriesCards": "Skládané obálky sérií", "heroCarousel": "Karusel na domovské stránce", "gearLabel": "Motiv", "ssLabel": "Přesnost hodnocení", "ssHint": "Jak přesné může hodnocení být. Platí pro výběr i pro vykreslení každého hodnocení.", "ssFull": "Celé hvězdy", "ssHalf": "Půlhvězdy", "ssQuarter": "Čtvrthvězdy", "rlLabel": "Hodnocení podle knihovny", "rlHint": "Které knihovny se účastní hodnocení. Knihovny podcastů jsou vypnuté, dokud je nezapnete.", "rlPodcast": "podcasty", "rfHide": "Skrýt Ohodnoťte, co jste dočetli"},
    sk: {"title": "Prispôsobenie motívu", "subtitle": "Prispôsobte vzhľad svojej knižnice. Zmeny sa ukladajú automaticky.", "branding": "Značka a štýl", "colour": "Farba a motív", "homeCar": "Domov a karusel", "sidebar": "Bočné ponuky", "appName": "Názov aplikácie", "appNameHint": "Ponechajte prázdne pre predvolený názov.", "logoUrl": "URL vlastného loga", "logoHint": "Ponechajte prázdne pre predvolené logo.", "accent": "Farba zvýraznenia", "baseTheme": "Základný motív", "mainFont": "Hlavné písmo", "carousel": "Automatické posúvanie karuselu", "carouselHint": "Sekundy medzi snímkami. 0 pre vypnutie.", "customSeries": "Rozšírené nedávne série", "seriesCount": "Počet nedávnych sérií", "seriesCountHint": "Koľko sérií zobraziť v rozšírenej poličke.", "hideShelves": "Skryť poličky domovskej stránky", "sidebarHint": "Skryte položky bočného menu, ktoré nepoužívate.", "showAppName": "Zobraziť názov aplikácie", "colorizeLogo": "Zafarbiť logo farbou zvýraznenia", "hideSeries": "Skryť Série", "hideCollections": "Skryť Kolekcie", "hideAuthors": "Skryť Autorov", "hideNarrators": "Skryť Rozprávačov", "hideStats": "Skryť Štatistiky", "hideRecentlyAdded": "Skryť Nedávno pridané", "hideRecentSeries": "Skryť Nedávne série", "hideContinueSeries": "Skryť Pokračovať v sérii", "hideListenAgain": "Skryť Počúvať znova", "hideDiscover": "Skryť Objavovať", "hideNewestAuthors": "Skryť Najnovších autorov", "seriesCards": "Skladané obálky sérií", "heroCarousel": "Karusel na domovskej stránke", "gearLabel": "Motív", "ssLabel": "Presnosť hodnotenia", "ssHint": "Aké presné môže hodnotenie byť. Platí pre výber aj pre vykreslenie každého hodnotenia.", "ssFull": "Celé hviezdy", "ssHalf": "Polhviezdy", "ssQuarter": "Štvrťhviezdy", "rlLabel": "Hodnotenia podľa knižnice", "rlHint": "Ktoré knižnice sa zúčastňujú hodnotenia. Knižnice podcastov sú vypnuté, kým ich nezapnete.", "rlPodcast": "podcasty", "rfHide": "Skryť Ohodnoťte, čo ste dočítali"},
    da: {"title": "Tematilpasninger", "subtitle": "Tilpas udseendet af dit bibliotek. Ændringer gemmes automatisk.", "branding": "Branding & stil", "colour": "Farve & tema", "homeCar": "Hjem & karrusel", "sidebar": "Sidemenuer", "appName": "Appnavn", "appNameHint": "Lad stå tomt for standardnavnet.", "logoUrl": "Brugerdefineret logo-URL", "logoHint": "Lad stå tomt for standardlogoet.", "accent": "Accentfarve", "baseTheme": "Basistema", "mainFont": "Hovedskrifttype", "carousel": "Automatisk karruselskift", "carouselHint": "Sekunder mellem slides. 0 for at deaktivere.", "customSeries": "Udvidede seneste serier", "seriesCount": "Antal seneste serier", "seriesCountHint": "Hvor mange serier der vises på den udvidede hylde.", "hideShelves": "Skjul hylder på forsiden", "sidebarHint": "Skjul menupunkter du ikke bruger.", "showAppName": "Vis appnavn", "colorizeLogo": "Farvelæg logo med accentfarve", "hideSeries": "Skjul Serier", "hideCollections": "Skjul Samlinger", "hideAuthors": "Skjul Forfattere", "hideNarrators": "Skjul Fortællere", "hideStats": "Skjul Statistik", "hideRecentlyAdded": "Skjul Senest tilføjet", "hideRecentSeries": "Skjul Seneste serier", "hideContinueSeries": "Skjul Fortsæt serie", "hideListenAgain": "Skjul Lyt igen", "hideDiscover": "Skjul Opdag", "hideNewestAuthors": "Skjul Nyeste forfattere", "seriesCards": "Stablede seriecovers", "heroCarousel": "Forside-karrusel", "gearLabel": "Tema", "ssLabel": "Bedømmelsens trin", "ssHint": "Hvor præcis en bedømmelse kan være. Gælder både valget og hvordan hver bedømmelse tegnes.", "ssFull": "Hele stjerner", "ssHalf": "Halve stjerner", "ssQuarter": "Kvarte stjerner", "rlLabel": "Bedømmelser pr. bibliotek", "rlHint": "Hvilke biblioteker deltager i bedømmelser. Podcastbiblioteker er slået fra, indtil du slår dem til.", "rlPodcast": "podcasts", "rfHide": "Skjul Bedøm hvad du har læst færdig"},
    sv: {"title": "Temaanpassningar", "subtitle": "Anpassa utseendet på ditt bibliotek. Ändringar sparas automatiskt.", "branding": "Varumärke & stil", "colour": "Färg & tema", "homeCar": "Hem & karusell", "sidebar": "Sidomenyer", "appName": "Appnamn", "appNameHint": "Lämna tomt för standardnamnet.", "logoUrl": "Anpassad logotyp-URL", "logoHint": "Lämna tomt för standardlogotypen.", "accent": "Accentfärg", "baseTheme": "Bastema", "mainFont": "Huvudtypsnitt", "carousel": "Automatisk karusellväxling", "carouselHint": "Sekunder mellan bilder. 0 för att inaktivera.", "customSeries": "Utökade senaste serier", "seriesCount": "Antal senaste serier", "seriesCountHint": "Hur många serier som visas på den utökade hyllan.", "hideShelves": "Dölj hyllor på startsidan", "sidebarHint": "Dölj sidomenyposter du inte använder.", "showAppName": "Visa appnamn", "colorizeLogo": "Färglägg logotypen med accentfärgen", "hideSeries": "Dölj Serier", "hideCollections": "Dölj Samlingar", "hideAuthors": "Dölj Författare", "hideNarrators": "Dölj Uppläsare", "hideStats": "Dölj Statistik", "hideRecentlyAdded": "Dölj Nyligen tillagda", "hideRecentSeries": "Dölj Senaste serier", "hideContinueSeries": "Dölj Fortsätt serie", "hideListenAgain": "Dölj Lyssna igen", "hideDiscover": "Dölj Upptäck", "hideNewestAuthors": "Dölj Nyaste författare", "seriesCards": "Staplade serieomslag", "heroCarousel": "Startsidans karusell", "gearLabel": "Tema", "ssLabel": "Betygsstegen", "ssHint": "Hur exakt ett betyg kan vara. Gäller både vad du kan välja och hur varje betyg ritas.", "ssFull": "Hela stjärnor", "ssHalf": "Halva stjärnor", "ssQuarter": "Kvartsstjärnor", "rlLabel": "Betyg per bibliotek", "rlHint": "Vilka bibliotek deltar i betyg. Podcastbibliotek är av tills du slår på dem.", "rlPodcast": "poddar", "rfHide": "Dölj Betygsätt det du läst klart"},
    no: {"title": "Tematilpasninger", "subtitle": "Tilpass utseendet på biblioteket ditt. Endringer lagres automatisk.", "branding": "Merkevare og stil", "colour": "Farge og tema", "homeCar": "Hjem og karusell", "sidebar": "Sidemenyer", "appName": "Appnavn", "appNameHint": "La stå tomt for standardnavnet.", "logoUrl": "Egendefinert logo-URL", "logoHint": "La stå tomt for standardlogoen.", "accent": "Aksentfarge", "baseTheme": "Basistema", "mainFont": "Hovedskrift", "carousel": "Automatisk karusellbytte", "carouselHint": "Sekunder mellom lysbilder. 0 for å deaktivere.", "customSeries": "Utvidede nylige serier", "seriesCount": "Antall nylige serier", "seriesCountHint": "Hvor mange serier som vises i den utvidede hyllen.", "hideShelves": "Skjul hyller på forsiden", "sidebarHint": "Skjul sidemenyoppføringer du ikke bruker.", "showAppName": "Vis appnavn", "colorizeLogo": "Fargelegg logoen med aksentfargen", "hideSeries": "Skjul Serier", "hideCollections": "Skjul Samlinger", "hideAuthors": "Skjul Forfattere", "hideNarrators": "Skjul Fortellere", "hideStats": "Skjul Statistikk", "hideRecentlyAdded": "Skjul Nylig lagt til", "hideRecentSeries": "Skjul Nylige serier", "hideContinueSeries": "Skjul Fortsett serie", "hideListenAgain": "Skjul Lytt igjen", "hideDiscover": "Skjul Oppdag", "hideNewestAuthors": "Skjul Nyeste forfattere", "seriesCards": "Stablede serieomslag", "heroCarousel": "Forsidekarusell", "gearLabel": "Tema", "ssLabel": "Vurderingstrinn", "ssHint": "Hvor presis en vurdering kan være. Gjelder både det du kan velge og hvordan hver vurdering tegnes.", "ssFull": "Hele stjerner", "ssHalf": "Halve stjerner", "ssQuarter": "Kvarte stjerner", "rlLabel": "Vurderinger per bibliotek", "rlHint": "Hvilke biblioteker deltar i vurderinger. Podkastbiblioteker er av til du slår dem på.", "rlPodcast": "podkaster", "rfHide": "Skjul Vurder det du har fullført"},
    fi: {"title": "Teeman mukautukset", "subtitle": "Mukauta kirjastosi ulkoasua. Muutokset tallentuvat automaattisesti.", "branding": "Brändi ja tyyli", "colour": "Väri ja teema", "homeCar": "Koti ja karuselli", "sidebar": "Sivuvalikot", "appName": "Sovelluksen nimi", "appNameHint": "Jätä tyhjäksi käyttääksesi oletusnimeä.", "logoUrl": "Mukautetun logon URL", "logoHint": "Jätä tyhjäksi käyttääksesi oletuslogoa.", "accent": "Korostusväri", "baseTheme": "Perusteema", "mainFont": "Pääfontti", "carousel": "Karusellin automaattinen vaihto", "carouselHint": "Sekunteja diojen välillä. 0 poistaa käytöstä.", "customSeries": "Laajennetut viimeisimmät sarjat", "seriesCount": "Viimeisimpien sarjojen määrä", "seriesCountHint": "Montako sarjaa näytetään laajennetulla hyllyllä.", "hideShelves": "Piilota etusivun hyllyt", "sidebarHint": "Piilota sivuvalikon kohteet, joita et käytä.", "showAppName": "Näytä sovelluksen nimi", "colorizeLogo": "Väritä logo korostusvärillä", "hideSeries": "Piilota Sarjat", "hideCollections": "Piilota Kokoelmat", "hideAuthors": "Piilota Kirjailijat", "hideNarrators": "Piilota Lukijat", "hideStats": "Piilota Tilastot", "hideRecentlyAdded": "Piilota Äskettäin lisätyt", "hideRecentSeries": "Piilota Viimeisimmät sarjat", "hideContinueSeries": "Piilota Jatka sarjaa", "hideListenAgain": "Piilota Kuuntele uudelleen", "hideDiscover": "Piilota Löydä", "hideNewestAuthors": "Piilota Uusimmat kirjailijat", "seriesCards": "Pinotut sarjakannet", "heroCarousel": "Etusivun karuselli", "gearLabel": "Teema", "ssLabel": "Arvioinnin tarkkuus", "ssHint": "Kuinka tarkka arvio voi olla. Koskee sekä valintaa että jokaisen arvion piirtämistä.", "ssFull": "Kokonaiset tähdet", "ssHalf": "Puolikkaat tähdet", "ssQuarter": "Neljännestähdet", "rlLabel": "Arviot kirjastoittain", "rlHint": "Mitkä kirjastot osallistuvat arviointiin. Podcast-kirjastot ovat pois päältä, kunnes otat ne käyttöön.", "rlPodcast": "podcastit", "rfHide": "Piilota Arvioi lukemasi"},
    ru: {"title": "Настройки темы", "subtitle": "Настройте внешний вид вашей библиотеки. Изменения сохраняются автоматически.", "branding": "Бренд и стиль", "colour": "Цвет и тема", "homeCar": "Главная и карусель", "sidebar": "Боковые меню", "appName": "Название приложения", "appNameHint": "Оставьте пустым для названия по умолчанию.", "logoUrl": "URL собственного логотипа", "logoHint": "Оставьте пустым для логотипа по умолчанию.", "accent": "Акцентный цвет", "baseTheme": "Базовая тема", "mainFont": "Основной шрифт", "carousel": "Автопрокрутка карусели", "carouselHint": "Секунд между слайдами. 0, отключить.", "customSeries": "Расширенные недавние серии", "seriesCount": "Количество недавних серий", "seriesCountHint": "Сколько серий показывать на расширенной полке.", "hideShelves": "Скрыть полки главной страницы", "sidebarHint": "Скройте неиспользуемые пункты бокового меню.", "showAppName": "Показывать название приложения", "colorizeLogo": "Окрашивать логотип акцентным цветом", "hideSeries": "Скрыть Серии", "hideCollections": "Скрыть Коллекции", "hideAuthors": "Скрыть Авторов", "hideNarrators": "Скрыть Чтецов", "hideStats": "Скрыть Статистику", "hideRecentlyAdded": "Скрыть Недавно добавленные", "hideRecentSeries": "Скрыть Недавние серии", "hideContinueSeries": "Скрыть Продолжить серию", "hideListenAgain": "Скрыть Слушать снова", "hideDiscover": "Скрыть Обзор", "hideNewestAuthors": "Скрыть Новейших авторов", "seriesCards": "Стопки обложек серий", "heroCarousel": "Карусель на главной", "gearLabel": "Тема", "ssLabel": "Шаг оценки", "ssHint": "Насколько точной может быть оценка. Влияет и на выбор, и на отображение каждой оценки.", "ssFull": "Целые звёзды", "ssHalf": "Половины звёзд", "ssQuarter": "Четверти звёзд", "rlLabel": "Оценки по библиотекам", "rlHint": "Какие библиотеки участвуют в оценках. Библиотеки подкастов выключены, пока вы их не включите.", "rlPodcast": "подкасты", "rfHide": "Скрыть «Оцените прочитанное»"},
    uk: {"title": "Налаштування теми", "subtitle": "Налаштуйте вигляд вашої бібліотеки. Зміни зберігаються автоматично.", "branding": "Бренд і стиль", "colour": "Колір і тема", "homeCar": "Головна та карусель", "sidebar": "Бічні меню", "appName": "Назва застосунку", "appNameHint": "Залиште порожнім для назви за замовчуванням.", "logoUrl": "URL власного логотипа", "logoHint": "Залиште порожнім для логотипа за замовчуванням.", "accent": "Акцентний колір", "baseTheme": "Базова тема", "mainFont": "Основний шрифт", "carousel": "Автопрокручування каруселі", "carouselHint": "Секунд між слайдами. 0, вимкнути.", "customSeries": "Розширені нещодавні серії", "seriesCount": "Кількість нещодавніх серій", "seriesCountHint": "Скільки серій показувати на розширеній полиці.", "hideShelves": "Приховати полиці головної сторінки", "sidebarHint": "Приховайте невикористовувані пункти бічного меню.", "showAppName": "Показувати назву застосунку", "colorizeLogo": "Забарвити логотип акцентним кольором", "hideSeries": "Приховати Серії", "hideCollections": "Приховати Колекції", "hideAuthors": "Приховати Авторів", "hideNarrators": "Приховати Читців", "hideStats": "Приховати Статистику", "hideRecentlyAdded": "Приховати Нещодавно додані", "hideRecentSeries": "Приховати Нещодавні серії", "hideContinueSeries": "Приховати Продовжити серію", "hideListenAgain": "Приховати Слухати знову", "hideDiscover": "Приховати Огляд", "hideNewestAuthors": "Приховати Найновіших авторів", "seriesCards": "Стос обкладинок серій", "heroCarousel": "Карусель на головній", "gearLabel": "Тема", "ssLabel": "Крок оцінки", "ssHint": "Наскільки точною може бути оцінка. Стосується і вибору, і відображення кожної оцінки.", "ssFull": "Цілі зірки", "ssHalf": "Половини зірок", "ssQuarter": "Чверті зірок", "rlLabel": "Оцінки за бібліотеками", "rlHint": "Які бібліотеки беруть участь в оцінках. Бібліотеки подкастів вимкнені, доки ви їх не увімкнете.", "rlPodcast": "подкасти", "rfHide": "Сховати «Оцініть прочитане»"},
    be: {"title": "Налады тэмы", "subtitle": "Наладзьце выгляд вашай бібліятэкі. Змены захоўваюцца аўтаматычна.", "branding": "Брэнд і стыль", "colour": "Колер і тэма", "homeCar": "Галоўная і карусель", "sidebar": "Бакавыя меню", "appName": "Назва праграмы", "appNameHint": "Пакіньце пустым для назвы па змаўчанні.", "logoUrl": "URL уласнага лагатыпа", "logoHint": "Пакіньце пустым для лагатыпа па змаўчанні.", "accent": "Акцэнтны колер", "baseTheme": "Базавая тэма", "mainFont": "Асноўны шрыфт", "carousel": "Аўтапракрутка каруселі", "carouselHint": "Секунд паміж слайдамі. 0, адключыць.", "customSeries": "Пашыраныя нядаўнія серыі", "seriesCount": "Колькасць нядаўніх серый", "seriesCountHint": "Колькі серый паказваць на пашыранай паліцы.", "hideShelves": "Схаваць паліцы галоўнай старонкі", "sidebarHint": "Схавайце пункты бакавога меню, якімі не карыстаецеся.", "showAppName": "Паказваць назву праграмы", "colorizeLogo": "Афарбаваць лагатып акцэнтным колерам", "hideSeries": "Схаваць Серыі", "hideCollections": "Схаваць Калекцыі", "hideAuthors": "Схаваць Аўтараў", "hideNarrators": "Схаваць Чытальнікаў", "hideStats": "Схаваць Статыстыку", "hideRecentlyAdded": "Схаваць Нядаўна дададзеныя", "hideRecentSeries": "Схаваць Нядаўнія серыі", "hideContinueSeries": "Схаваць Працягнуць серыю", "hideListenAgain": "Схаваць Слухаць зноў", "hideDiscover": "Схаваць Агляд", "hideNewestAuthors": "Схаваць Найноўшых аўтараў", "seriesCards": "Стос вокладак серый", "heroCarousel": "Карусель на галоўнай", "gearLabel": "Тэма", "ssLabel": "Крок ацэнкі", "ssHint": "Наколькі дакладнай можа быць ацэнка. Тычыцца і выбару, і адлюстравання кожнай ацэнкі.", "ssFull": "Цэлыя зоркі", "ssHalf": "Паловы зорак", "ssQuarter": "Чвэрці зорак", "rlLabel": "Ацэнкі па бібліятэках", "rlHint": "Якія бібліятэкі ўдзельнічаюць у ацэнках. Бібліятэкі падкастаў выключаны, пакуль вы іх не ўключыце.", "rlPodcast": "падкасты", "rfHide": "Схаваць «Ацаніце прачытанае»"},
    bg: {"title": "Персонализация на темата", "subtitle": "Персонализирайте вида на библиотеката си. Промените се записват автоматично.", "branding": "Марка и стил", "colour": "Цвят и тема", "homeCar": "Начало и въртележка", "sidebar": "Странични менюта", "appName": "Име на приложението", "appNameHint": "Оставете празно за името по подразбиране.", "logoUrl": "URL на собствено лого", "logoHint": "Оставете празно за логото по подразбиране.", "accent": "Акцентен цвят", "baseTheme": "Основна тема", "mainFont": "Основен шрифт", "carousel": "Автоматично превъртане на въртележката", "carouselHint": "Секунди между слайдовете. 0 за изключване.", "customSeries": "Разширени скорошни поредици", "seriesCount": "Брой скорошни поредици", "seriesCountHint": "Колко поредици да се показват на разширения рафт.", "hideShelves": "Скриване на рафтовете на началната страница", "sidebarHint": "Скрийте неизползвани елементи от страничното меню.", "showAppName": "Показване на името на приложението", "colorizeLogo": "Оцветяване на логото с акцентния цвят", "hideSeries": "Скрий Поредици", "hideCollections": "Скрий Колекции", "hideAuthors": "Скрий Автори", "hideNarrators": "Скрий Разказвачи", "hideStats": "Скрий Статистика", "hideRecentlyAdded": "Скрий Наскоро добавени", "hideRecentSeries": "Скрий Скорошни поредици", "hideContinueSeries": "Скрий Продължи поредицата", "hideListenAgain": "Скрий Слушай отново", "hideDiscover": "Скрий Открий", "hideNewestAuthors": "Скрий Най-нови автори", "seriesCards": "Подредени корици на поредици", "heroCarousel": "Въртележка на началната страница", "gearLabel": "Тема", "ssLabel": "Стъпка на оценката", "ssHint": "Колко точна може да бъде една оценка. Отнася се и за избора, и за начина на изобразяване.", "ssFull": "Цели звезди", "ssHalf": "Половин звезди", "ssQuarter": "Четвърт звезди", "rlLabel": "Оценки по библиотека", "rlHint": "Кои библиотеки участват в оценките. Библиотеките с подкасти са изключени, докато не ги включите.", "rlPodcast": "подкасти", "rfHide": "Скриване на „Оценете прочетеното“"},
    hr: {"title": "Prilagodbe teme", "subtitle": "Prilagodite izgled svoje knjižnice. Promjene se spremaju automatski.", "branding": "Brend i stil", "colour": "Boja i tema", "homeCar": "Početna i vrtuljak", "sidebar": "Bočni izbornici", "appName": "Naziv aplikacije", "appNameHint": "Ostavite prazno za zadani naziv.", "logoUrl": "URL prilagođenog logotipa", "logoHint": "Ostavite prazno za zadani logotip.", "accent": "Boja isticanja", "baseTheme": "Osnovna tema", "mainFont": "Glavni font", "carousel": "Automatsko pomicanje vrtuljka", "carouselHint": "Sekunde između slajdova. 0 za isključivanje.", "customSeries": "Proširene nedavne serije", "seriesCount": "Broj nedavnih serija", "seriesCountHint": "Koliko serija prikazati na proširenoj polici.", "hideShelves": "Sakrij police početne stranice", "sidebarHint": "Sakrijte stavke bočnog izbornika koje ne koristite.", "showAppName": "Prikaži naziv aplikacije", "colorizeLogo": "Oboji logotip bojom isticanja", "hideSeries": "Sakrij Serije", "hideCollections": "Sakrij Zbirke", "hideAuthors": "Sakrij Autore", "hideNarrators": "Sakrij Pripovjedače", "hideStats": "Sakrij Statistiku", "hideRecentlyAdded": "Sakrij Nedavno dodano", "hideRecentSeries": "Sakrij Nedavne serije", "hideContinueSeries": "Sakrij Nastavi seriju", "hideListenAgain": "Sakrij Slušaj ponovno", "hideDiscover": "Sakrij Otkrij", "hideNewestAuthors": "Sakrij Najnovije autore", "seriesCards": "Naslagane naslovnice serija", "heroCarousel": "Vrtuljak početne stranice", "gearLabel": "Tema", "ssLabel": "Koraci ocjene", "ssHint": "Koliko precizna ocjena može biti. Vrijedi za odabir i za način iscrtavanja svake ocjene.", "ssFull": "Cijele zvjezdice", "ssHalf": "Pola zvjezdice", "ssQuarter": "Četvrt zvjezdice", "rlLabel": "Ocjene po knjižnici", "rlHint": "Koje knjižnice sudjeluju u ocjenama. Knjižnice podcasta isključene su dok ih ne uključite.", "rlPodcast": "podcasti", "rfHide": "Sakrij Ocijeni što si pročitao"},
    sl: {"title": "Prilagoditve teme", "subtitle": "Prilagodite videz svoje knjižnice. Spremembe se samodejno shranijo.", "branding": "Blagovna znamka in slog", "colour": "Barva in tema", "homeCar": "Domov in vrtiljak", "sidebar": "Stranski meniji", "appName": "Ime aplikacije", "appNameHint": "Pustite prazno za privzeto ime.", "logoUrl": "URL lastnega logotipa", "logoHint": "Pustite prazno za privzeti logotip.", "accent": "Poudarna barva", "baseTheme": "Osnovna tema", "mainFont": "Glavna pisava", "carousel": "Samodejno premikanje vrtiljaka", "carouselHint": "Sekunde med diapozitivi. 0 za izklop.", "customSeries": "Razširjene nedavne serije", "seriesCount": "Število nedavnih serij", "seriesCountHint": "Koliko serij prikazati na razširjeni polici.", "hideShelves": "Skrij police domače strani", "sidebarHint": "Skrijte elemente stranskega menija, ki jih ne uporabljate.", "showAppName": "Prikaži ime aplikacije", "colorizeLogo": "Pobarvaj logotip s poudarno barvo", "hideSeries": "Skrij Serije", "hideCollections": "Skrij Zbirke", "hideAuthors": "Skrij Avtorje", "hideNarrators": "Skrij Pripovedovalce", "hideStats": "Skrij Statistiko", "hideRecentlyAdded": "Skrij Nedavno dodano", "hideRecentSeries": "Skrij Nedavne serije", "hideContinueSeries": "Skrij Nadaljuj serijo", "hideListenAgain": "Skrij Poslušaj znova", "hideDiscover": "Skrij Odkrij", "hideNewestAuthors": "Skrij Najnovejše avtorje", "seriesCards": "Zložene naslovnice serij", "heroCarousel": "Vrtiljak domače strani", "gearLabel": "Tema", "ssLabel": "Koraki ocene", "ssHint": "Kako natančna je lahko ocena. Velja za izbiro in za izris vsake ocene.", "ssFull": "Cele zvezdice", "ssHalf": "Pol zvezdice", "ssQuarter": "Četrt zvezdice", "rlLabel": "Ocene po knjižnici", "rlHint": "Katere knjižnice sodelujejo pri ocenah. Knjižnice podkastov so izklopljene, dokler jih ne vklopite.", "rlPodcast": "podkasti", "rfHide": "Skrij Oceni, kar si prebral"},
    hu: {"title": "Téma testreszabása", "subtitle": "Szabja testre könyvtára megjelenését. A módosítások automatikusan mentődnek.", "branding": "Arculat és stílus", "colour": "Szín és téma", "homeCar": "Kezdőlap és körhinta", "sidebar": "Oldalsó menük", "appName": "Alkalmazás neve", "appNameHint": "Hagyja üresen az alapértelmezett névhez.", "logoUrl": "Egyéni logó URL", "logoHint": "Hagyja üresen az alapértelmezett logóhoz.", "accent": "Kiemelőszín", "baseTheme": "Alaptéma", "mainFont": "Fő betűtípus", "carousel": "Körhinta automatikus léptetése", "carouselHint": "Másodpercek a diák között. 0 a kikapcsoláshoz.", "customSeries": "Bővített legutóbbi sorozatok", "seriesCount": "Legutóbbi sorozatok száma", "seriesCountHint": "Hány sorozat jelenjen meg a bővített polcon.", "hideShelves": "Kezdőlapi polcok elrejtése", "sidebarHint": "Rejtse el a nem használt oldalmenü-elemeket.", "showAppName": "Alkalmazásnév megjelenítése", "colorizeLogo": "Logó színezése a kiemelőszínnel", "hideSeries": "Sorozatok elrejtése", "hideCollections": "Gyűjtemények elrejtése", "hideAuthors": "Szerzők elrejtése", "hideNarrators": "Felolvasók elrejtése", "hideStats": "Statisztikák elrejtése", "hideRecentlyAdded": "Nemrég hozzáadottak elrejtése", "hideRecentSeries": "Legutóbbi sorozatok elrejtése", "hideContinueSeries": "Sorozat folytatása elrejtése", "hideListenAgain": "Újrahallgatás elrejtése", "hideDiscover": "Felfedezés elrejtése", "hideNewestAuthors": "Legújabb szerzők elrejtése", "seriesCards": "Halmozott sorozatborítók", "heroCarousel": "Kezdőlapi körhinta", "gearLabel": "Téma", "ssLabel": "Értékelés lépésköze", "ssHint": "Mennyire pontos lehet egy értékelés. A választásra és minden értékelés megjelenítésére is vonatkozik.", "ssFull": "Egész csillagok", "ssHalf": "Fél csillagok", "ssQuarter": "Negyed csillagok", "rlLabel": "Értékelések könyvtáranként", "rlHint": "Mely könyvtárak vesznek részt az értékelésben. A podcast-könyvtárak ki vannak kapcsolva, amíg be nem kapcsolod őket.", "rlPodcast": "podcastok", "rfHide": "Értékeld, amit befejeztél elrejtése"},
    ro: {"title": "Personalizarea temei", "subtitle": "Personalizați aspectul bibliotecii dvs. Modificările se salvează automat.", "branding": "Brand și stil", "colour": "Culoare și temă", "homeCar": "Acasă și carusel", "sidebar": "Meniuri laterale", "appName": "Numele aplicației", "appNameHint": "Lăsați gol pentru numele implicit.", "logoUrl": "URL logo personalizat", "logoHint": "Lăsați gol pentru logo-ul implicit.", "accent": "Culoare de accent", "baseTheme": "Temă de bază", "mainFont": "Font principal", "carousel": "Avansare automată carusel", "carouselHint": "Secunde între diapozitive. 0 pentru dezactivare.", "customSeries": "Serii recente extinse", "seriesCount": "Numărul de serii recente", "seriesCountHint": "Câte serii să fie afișate pe raftul extins.", "hideShelves": "Ascunde rafturile paginii principale", "sidebarHint": "Ascundeți elementele din meniul lateral pe care nu le folosiți.", "showAppName": "Afișează numele aplicației", "colorizeLogo": "Colorează logo-ul cu culoarea de accent", "hideSeries": "Ascunde Serii", "hideCollections": "Ascunde Colecții", "hideAuthors": "Ascunde Autori", "hideNarrators": "Ascunde Naratori", "hideStats": "Ascunde Statistici", "hideRecentlyAdded": "Ascunde Adăugate recent", "hideRecentSeries": "Ascunde Serii recente", "hideContinueSeries": "Ascunde Continuă seria", "hideListenAgain": "Ascunde Ascultă din nou", "hideDiscover": "Ascunde Descoperă", "hideNewestAuthors": "Ascunde Cei mai noi autori", "seriesCards": "Coperți de serii suprapuse", "heroCarousel": "Carusel pagină principală", "gearLabel": "Temă", "ssLabel": "Pasul evaluării", "ssHint": "Cât de precisă poate fi o evaluare. Se aplică alegerii și modului în care este desenată fiecare evaluare.", "ssFull": "Stele întregi", "ssHalf": "Jumătăți de stea", "ssQuarter": "Sferturi de stea", "rlLabel": "Evaluări pe bibliotecă", "rlHint": "Ce biblioteci participă la evaluări. Bibliotecile de podcasturi sunt oprite până le pornești.", "rlPodcast": "podcasturi", "rfHide": "Ascunde Evaluează ce ai terminat"},
    lt: {"title": "Temos tinkinimas", "subtitle": "Tinkinkite savo bibliotekos išvaizdą. Pakeitimai išsaugomi automatiškai.", "branding": "Prekės ženklas ir stilius", "colour": "Spalva ir tema", "homeCar": "Pradžia ir karuselė", "sidebar": "Šoniniai meniu", "appName": "Programos pavadinimas", "appNameHint": "Palikite tuščią numatytajam pavadinimui.", "logoUrl": "Pasirinktinio logotipo URL", "logoHint": "Palikite tuščią numatytajam logotipui.", "accent": "Akcento spalva", "baseTheme": "Bazinė tema", "mainFont": "Pagrindinis šriftas", "carousel": "Automatinis karuselės sukimas", "carouselHint": "Sekundės tarp skaidrių. 0, išjungti.", "customSeries": "Išplėstos naujausios serijos", "seriesCount": "Naujausių serijų skaičius", "seriesCountHint": "Kiek serijų rodyti išplėstoje lentynoje.", "hideShelves": "Slėpti pradžios puslapio lentynas", "sidebarHint": "Slėpkite nenaudojamus šoninio meniu punktus.", "showAppName": "Rodyti programos pavadinimą", "colorizeLogo": "Nuspalvinti logotipą akcento spalva", "hideSeries": "Slėpti Serijas", "hideCollections": "Slėpti Kolekcijas", "hideAuthors": "Slėpti Autorius", "hideNarrators": "Slėpti Skaitovus", "hideStats": "Slėpti Statistiką", "hideRecentlyAdded": "Slėpti Neseniai pridėta", "hideRecentSeries": "Slėpti Naujausias serijas", "hideContinueSeries": "Slėpti Tęsti seriją", "hideListenAgain": "Slėpti Klausyti dar kartą", "hideDiscover": "Slėpti Atrasti", "hideNewestAuthors": "Slėpti Naujausius autorius", "seriesCards": "Sudėtos serijų viršelės", "heroCarousel": "Pradžios puslapio karuselė", "gearLabel": "Tema", "ssLabel": "Vertinimo žingsnis", "ssHint": "Kiek tikslus gali būti įvertinimas. Taikoma ir pasirinkimui, ir kiekvieno įvertinimo piešimui.", "ssFull": "Visos žvaigždutės", "ssHalf": "Pusės žvaigždutės", "ssQuarter": "Ketvirčiai žvaigždutės", "rlLabel": "Vertinimai pagal biblioteką", "rlHint": "Kurios bibliotekos dalyvauja vertinimuose. Tinklalaidžių bibliotekos išjungtos, kol jų neįjungsite.", "rlPodcast": "tinklalaidės", "rfHide": "Slėpti „Įvertinkite, ką perskaitėte“"},
    lv: {"title": "Motīva pielāgošana", "subtitle": "Pielāgojiet savas bibliotēkas izskatu. Izmaiņas tiek saglabātas automātiski.", "branding": "Zīmols un stils", "colour": "Krāsa un motīvs", "homeCar": "Sākums un karuselis", "sidebar": "Sānu izvēlnes", "appName": "Lietotnes nosaukums", "appNameHint": "Atstājiet tukšu noklusējuma nosaukumam.", "logoUrl": "Pielāgota logotipa URL", "logoHint": "Atstājiet tukšu noklusējuma logotipam.", "accent": "Akcenta krāsa", "baseTheme": "Pamata motīvs", "mainFont": "Galvenais fonts", "carousel": "Karuseļa automātiskā pārslēgšana", "carouselHint": "Sekundes starp slaidiem. 0, lai atspējotu.", "customSeries": "Paplašinātas nesenās sērijas", "seriesCount": "Neseno sēriju skaits", "seriesCountHint": "Cik sērijas rādīt paplašinātajā plauktā.", "hideShelves": "Paslēpt sākumlapas plauktus", "sidebarHint": "Paslēpiet neizmantotos sānu izvēlnes vienumus.", "showAppName": "Rādīt lietotnes nosaukumu", "colorizeLogo": "Iekrāsot logotipu ar akcenta krāsu", "hideSeries": "Paslēpt Sērijas", "hideCollections": "Paslēpt Kolekcijas", "hideAuthors": "Paslēpt Autorus", "hideNarrators": "Paslēpt Lasītājus", "hideStats": "Paslēpt Statistiku", "hideRecentlyAdded": "Paslēpt Nesen pievienotos", "hideRecentSeries": "Paslēpt Nesenās sērijas", "hideContinueSeries": "Paslēpt Turpināt sēriju", "hideListenAgain": "Paslēpt Klausīties vēlreiz", "hideDiscover": "Paslēpt Atklāt", "hideNewestAuthors": "Paslēpt Jaunākos autorus", "seriesCards": "Sakrautas sēriju vāku bildes", "heroCarousel": "Sākumlapas karuselis", "gearLabel": "Motīvs", "ssLabel": "Vērtējuma solis", "ssHint": "Cik precīzs var būt vērtējums. Attiecas gan uz izvēli, gan uz katra vērtējuma attēlošanu.", "ssFull": "Veselas zvaigznes", "ssHalf": "Pusa zvaigznes", "ssQuarter": "Ceturtdaļzvaigznes", "rlLabel": "Vērtējumi pa bibliotēkām", "rlHint": "Kuras bibliotēkas piedalās vērtējumos. Podkāstu bibliotēkas ir izslēgtas, līdz tās ieslēdzat.", "rlPodcast": "podkāsti", "rfHide": "Slēpt “Novērtējiet izlasīto”"},
    et: {"title": "Teema kohandamine", "subtitle": "Kohandage oma raamatukogu välimust. Muudatused salvestatakse automaatselt.", "branding": "Bränd ja stiil", "colour": "Värv ja teema", "homeCar": "Avaleht ja karussell", "sidebar": "Külgmenüüd", "appName": "Rakenduse nimi", "appNameHint": "Jätke tühjaks vaikimisi nime jaoks.", "logoUrl": "Kohandatud logo URL", "logoHint": "Jätke tühjaks vaikimisi logo jaoks.", "accent": "Rõhuvärv", "baseTheme": "Põhiteema", "mainFont": "Peamine font", "carousel": "Karusselli automaatne liikumine", "carouselHint": "Sekundid slaidide vahel. 0 keelamiseks.", "customSeries": "Laiendatud hiljutised sarjad", "seriesCount": "Hiljutiste sarjade arv", "seriesCountHint": "Mitu sarja laiendatud riiulil kuvada.", "hideShelves": "Peida avalehe riiulid", "sidebarHint": "Peitke külgmenüü kirjed, mida te ei kasuta.", "showAppName": "Kuva rakenduse nimi", "colorizeLogo": "Värvi logo rõhuvärviga", "hideSeries": "Peida Sarjad", "hideCollections": "Peida Kogud", "hideAuthors": "Peida Autorid", "hideNarrators": "Peida Lugejad", "hideStats": "Peida Statistika", "hideRecentlyAdded": "Peida Hiljuti lisatud", "hideRecentSeries": "Peida Hiljutised sarjad", "hideContinueSeries": "Peida Jätka sarja", "hideListenAgain": "Peida Kuula uuesti", "hideDiscover": "Peida Avasta", "hideNewestAuthors": "Peida Uusimad autorid", "seriesCards": "Virnastatud sarjakaaned", "heroCarousel": "Avalehe karussell", "gearLabel": "Teema", "ssLabel": "Hinnangu samm", "ssHint": "Kui täpne saab hinnang olla. Kehtib nii valiku kui ka iga hinnangu kuvamise kohta.", "ssFull": "Täistärnid", "ssHalf": "Pooltärnid", "ssQuarter": "Veerandtärnid", "rlLabel": "Hinnangud kogude kaupa", "rlHint": "Millised kogud osalevad hinnangutes. Taskuhäälingute kogud on väljas, kuni need sisse lülitad.", "rlPodcast": "taskuhäälingud", "rfHide": "Peida „Hinda, mille lõpetasid“"},
    el: {"title": "Προσαρμογές θέματος", "subtitle": "Προσαρμόστε την εμφάνιση της βιβλιοθήκης σας. Οι αλλαγές αποθηκεύονται αυτόματα.", "branding": "Επωνυμία και στυλ", "colour": "Χρώμα και θέμα", "homeCar": "Αρχική και καρουζέλ", "sidebar": "Πλευρικά μενού", "appName": "Όνομα εφαρμογής", "appNameHint": "Αφήστε κενό για το προεπιλεγμένο όνομα.", "logoUrl": "URL προσαρμοσμένου λογότυπου", "logoHint": "Αφήστε κενό για το προεπιλεγμένο λογότυπο.", "accent": "Χρώμα έμφασης", "baseTheme": "Βασικό θέμα", "mainFont": "Κύρια γραμματοσειρά", "carousel": "Αυτόματη προώθηση καρουζέλ", "carouselHint": "Δευτερόλεπτα μεταξύ διαφανειών. 0 για απενεργοποίηση.", "customSeries": "Εκτεταμένες πρόσφατες σειρές", "seriesCount": "Αριθμός πρόσφατων σειρών", "seriesCountHint": "Πόσες σειρές θα εμφανίζονται στο εκτεταμένο ράφι.", "hideShelves": "Απόκρυψη ραφιών αρχικής σελίδας", "sidebarHint": "Αποκρύψτε στοιχεία πλευρικού μενού που δεν χρησιμοποιείτε.", "showAppName": "Εμφάνιση ονόματος εφαρμογής", "colorizeLogo": "Χρωματισμός λογότυπου με το χρώμα έμφασης", "hideSeries": "Απόκρυψη Σειρών", "hideCollections": "Απόκρυψη Συλλογών", "hideAuthors": "Απόκρυψη Συγγραφέων", "hideNarrators": "Απόκρυψη Αφηγητών", "hideStats": "Απόκρυψη Στατιστικών", "hideRecentlyAdded": "Απόκρυψη Πρόσφατα προστεθέντων", "hideRecentSeries": "Απόκρυψη Πρόσφατων σειρών", "hideContinueSeries": "Απόκρυψη Συνέχισης σειράς", "hideListenAgain": "Απόκρυψη Ακούστε ξανά", "hideDiscover": "Απόκρυψη Ανακαλύψτε", "hideNewestAuthors": "Απόκρυψη Νεότερων συγγραφέων", "seriesCards": "Στοιβαγμένα εξώφυλλα σειρών", "heroCarousel": "Καρουζέλ αρχικής σελίδας", "gearLabel": "Θέμα", "ssLabel": "Βήμα βαθμολογίας", "ssHint": "Πόσο ακριβής μπορεί να είναι μια βαθμολογία. Ισχύει για την επιλογή και για τον τρόπο σχεδίασης κάθε βαθμολογίας.", "ssFull": "Ολόκληρα αστέρια", "ssHalf": "Μισά αστέρια", "ssQuarter": "Τέταρτα αστεριού", "rlLabel": "Βαθμολογίες ανά βιβλιοθήκη", "rlHint": "Ποιες βιβλιοθήκες συμμετέχουν στις βαθμολογίες. Οι βιβλιοθήκες podcast είναι ανενεργές μέχρι να τις ενεργοποιήσετε.", "rlPodcast": "podcast", "rfHide": "Απόκρυψη «Βαθμολογήστε όσα τελειώσατε»"},
    tr: {"title": "Tema Özelleştirmeleri", "subtitle": "Kitaplığınızın görünümünü kişiselleştirin. Değişiklikler otomatik kaydedilir.", "branding": "Marka ve Stil", "colour": "Renk ve Tema", "homeCar": "Ana Sayfa ve Slayt", "sidebar": "Yan Menüler", "appName": "Uygulama Adı", "appNameHint": "Varsayılan ad için boş bırakın.", "logoUrl": "Özel Logo URL’si", "logoHint": "Varsayılan logo için boş bırakın.", "accent": "Vurgu Rengi", "baseTheme": "Temel Tema", "mainFont": "Ana Yazı Tipi", "carousel": "Slayt Otomatik İlerleme", "carouselHint": "Slaytlar arası saniye. Devre dışı bırakmak için 0.", "customSeries": "Genişletilmiş Son Seriler", "seriesCount": "Son Seri Sayısı", "seriesCountHint": "Genişletilmiş rafta kaç seri gösterileceği.", "hideShelves": "Ana Sayfa Raflarını Gizle", "sidebarHint": "Kullanmadığınız yan menü öğelerini gizleyin.", "showAppName": "Uygulama Adını Göster", "colorizeLogo": "Logoyu Vurgu Rengiyle Renklendir", "hideSeries": "Serileri Gizle", "hideCollections": "Koleksiyonları Gizle", "hideAuthors": "Yazarları Gizle", "hideNarrators": "Seslendirenleri Gizle", "hideStats": "İstatistikleri Gizle", "hideRecentlyAdded": "Son Eklenenleri Gizle", "hideRecentSeries": "Son Serileri Gizle", "hideContinueSeries": "Seriye Devam Et’i Gizle", "hideListenAgain": "Tekrar Dinle’yi Gizle", "hideDiscover": "Keşfet’i Gizle", "hideNewestAuthors": "En Yeni Yazarları Gizle", "seriesCards": "Yığılmış Seri Kapakları", "heroCarousel": "Ana Sayfa Slaytı", "gearLabel": "Tema", "ssLabel": "Puanlama adımı", "ssHint": "Bir puanın ne kadar hassas olabileceği. Hem seçime hem de her puanın çizilmesine uygulanır.", "ssFull": "Tam yıldız", "ssHalf": "Yarım yıldız", "ssQuarter": "Çeyrek yıldız", "rlLabel": "Kitaplığa göre puanlar", "rlHint": "Hangi kitaplıklar puanlamaya katılır. Podcast kitaplıkları siz açana kadar kapalıdır.", "rlPodcast": "podcastler", "rfHide": "Bitirdiklerini puanla’yı gizle"},
    ca: {"title": "Personalització del tema", "subtitle": "Personalitzeu l’aspecte de la vostra biblioteca. Els canvis es desen automàticament.", "branding": "Marca i estil", "colour": "Color i tema", "homeCar": "Inici i carrusel", "sidebar": "Menús laterals", "appName": "Nom de l’aplicació", "appNameHint": "Deixeu-ho buit per al nom predeterminat.", "logoUrl": "URL de logotip personalitzat", "logoHint": "Deixeu-ho buit per al logotip predeterminat.", "accent": "Color d’èmfasi", "baseTheme": "Tema base", "mainFont": "Tipus de lletra principal", "carousel": "Avanç automàtic del carrusel", "carouselHint": "Segons entre diapositives. 0 per desactivar.", "customSeries": "Sèries recents ampliades", "seriesCount": "Nombre de sèries recents", "seriesCountHint": "Quantes sèries es mostren al prestatge ampliat.", "hideShelves": "Amaga els prestatges d’inici", "sidebarHint": "Amagueu entrades del menú lateral que no feu servir.", "showAppName": "Mostra el nom de l’aplicació", "colorizeLogo": "Acoloreix el logotip amb el color d’èmfasi", "hideSeries": "Amaga Sèries", "hideCollections": "Amaga Col·leccions", "hideAuthors": "Amaga Autors", "hideNarrators": "Amaga Narradors", "hideStats": "Amaga Estadístiques", "hideRecentlyAdded": "Amaga Afegits recentment", "hideRecentSeries": "Amaga Sèries recents", "hideContinueSeries": "Amaga Continua la sèrie", "hideListenAgain": "Amaga Escolta de nou", "hideDiscover": "Amaga Descobreix", "hideNewestAuthors": "Amaga Autors més nous", "seriesCards": "Cobertes de sèries apilades", "heroCarousel": "Carrusel d’inici", "gearLabel": "Tema", "ssLabel": "Precisió de les valoracions", "ssHint": "Com de precisa pot ser una valoració. S’aplica al que pots triar i a com es dibuixa cada valoració.", "ssFull": "Estrelles senceres", "ssHalf": "Mitges estrelles", "ssQuarter": "Quarts d’estrella", "rlLabel": "Valoracions per biblioteca", "rlHint": "Quines biblioteques participen en les valoracions. Les biblioteques de pòdcasts estan desactivades fins que les actives.", "rlPodcast": "pòdcasts", "rfHide": "Amaga Valora el que has acabat"},
    eu: {"title": "Gaiaren pertsonalizazioa", "subtitle": "Pertsonalizatu zure liburutegiaren itxura. Aldaketak automatikoki gordetzen dira.", "branding": "Marka eta estiloa", "colour": "Kolorea eta gaia", "homeCar": "Hasiera eta karrusela", "sidebar": "Alboko menuak", "appName": "Aplikazioaren izena", "appNameHint": "Utzi hutsik izen lehenetsirako.", "logoUrl": "Logotipo pertsonalizatuaren URLa", "logoHint": "Utzi hutsik logotipo lehenetsirako.", "accent": "Azentu-kolorea", "baseTheme": "Oinarrizko gaia", "mainFont": "Letra-tipo nagusia", "carousel": "Karruselaren aurrerapen automatikoa", "carouselHint": "Diapositiben arteko segundoak. 0 desgaitzeko.", "customSeries": "Azken serie hedatuak", "seriesCount": "Azken serieen kopurua", "seriesCountHint": "Zenbat serie erakutsi apal hedatuan.", "hideShelves": "Ezkutatu hasierako apalak", "sidebarHint": "Ezkutatu erabiltzen ez dituzun alboko menuko sarrerak.", "showAppName": "Erakutsi aplikazioaren izena", "colorizeLogo": "Koloreztatu logotipoa azentu-kolorearekin", "hideSeries": "Ezkutatu Serieak", "hideCollections": "Ezkutatu Bildumak", "hideAuthors": "Ezkutatu Egileak", "hideNarrators": "Ezkutatu Narratzaileak", "hideStats": "Ezkutatu Estatistikak", "hideRecentlyAdded": "Ezkutatu Duela gutxi gehituak", "hideRecentSeries": "Ezkutatu Azken serieak", "hideContinueSeries": "Ezkutatu Jarraitu seriea", "hideListenAgain": "Ezkutatu Entzun berriro", "hideDiscover": "Ezkutatu Aurkitu", "hideNewestAuthors": "Ezkutatu Egile berrienak", "seriesCards": "Serieen azal pilatuak", "heroCarousel": "Hasierako karrusela", "gearLabel": "Gaia", "ssLabel": "Balorazio-urratsa", "ssHint": "Balorazio bat zein zehatza izan daitekeen. Aukeratzeari eta balorazio bakoitza marrazteari aplikatzen zaio.", "ssFull": "Izar osoak", "ssHalf": "Izar erdiak", "ssQuarter": "Izar laurdenak", "rlLabel": "Balorazioak liburutegika", "rlHint": "Zein liburutegik parte hartzen duten balorazioetan. Podcast-liburutegiak itzalita daude piztu arte.", "rlPodcast": "podcastak", "rfHide": "Ezkutatu Baloratu amaitutakoa"},
    is: {"title": "Þemastillingar", "subtitle": "Sérsníddu útlit safnsins þíns. Breytingar vistast sjálfkrafa.", "branding": "Vörumerki og stíll", "colour": "Litur og þema", "homeCar": "Heim og hringekja", "sidebar": "Hliðarvalmyndir", "appName": "Heiti forrits", "appNameHint": "Skildu eftir autt fyrir sjálfgefið heiti.", "logoUrl": "Slóð á sérsniðið merki", "logoHint": "Skildu eftir autt fyrir sjálfgefið merki.", "accent": "Áherslulitur", "baseTheme": "Grunnþema", "mainFont": "Aðalletur", "carousel": "Sjálfvirk hringekjuskipting", "carouselHint": "Sekúndur milli glæra. 0 til að slökkva.", "customSeries": "Útvíkkaðar nýlegar seríur", "seriesCount": "Fjöldi nýlegra sería", "seriesCountHint": "Hversu margar seríur birtast í útvíkkuðu hillunni.", "hideShelves": "Fela hillur forsíðu", "sidebarHint": "Feldu hliðarvalmyndaratriði sem þú notar ekki.", "showAppName": "Sýna heiti forrits", "colorizeLogo": "Lita merkið með áherslulitnum", "hideSeries": "Fela Seríur", "hideCollections": "Fela Söfn", "hideAuthors": "Fela Höfunda", "hideNarrators": "Fela Lesara", "hideStats": "Fela Tölfræði", "hideRecentlyAdded": "Fela Nýlega bætt við", "hideRecentSeries": "Fela Nýlegar seríur", "hideContinueSeries": "Fela Halda áfram með seríu", "hideListenAgain": "Fela Hlusta aftur", "hideDiscover": "Fela Uppgötva", "hideNewestAuthors": "Fela Nýjustu höfunda", "seriesCards": "Staflaðar seríukápur", "heroCarousel": "Hringekja forsíðu", "gearLabel": "Þema", "ssLabel": "Þrep einkunnar", "ssHint": "Hversu nákvæm einkunn getur verið. Á við um valið og hvernig hver einkunn er teiknuð.", "ssFull": "Heilar stjörnur", "ssHalf": "Hálfar stjörnur", "ssQuarter": "Fjórðungsstjörnur", "rlLabel": "Einkunnir eftir safni", "rlHint": "Hvaða söfn taka þátt í einkunnum. Hlaðvarpssöfn eru slökkt þar til þú kveikir á þeim.", "rlPodcast": "hlaðvörp", "rfHide": "Fela Gefðu einkunn fyrir það sem þú kláraðir"},
    ja: {"title": "テーマのカスタマイズ", "subtitle": "ライブラリの外観をカスタマイズします。変更は自動的に保存されます。", "branding": "ブランドとスタイル", "colour": "カラーとテーマ", "homeCar": "ホームとカルーセル", "sidebar": "サイドメニュー", "appName": "アプリ名", "appNameHint": "デフォルト名を使う場合は空欄のままにします。", "logoUrl": "カスタムロゴURL", "logoHint": "デフォルトロゴを使う場合は空欄のままにします。", "accent": "アクセントカラー", "baseTheme": "ベーステーマ", "mainFont": "メインフォント", "carousel": "カルーセル自動送り", "carouselHint": "スライド間の秒数。0で無効。", "customSeries": "拡張された最近のシリーズ", "seriesCount": "最近のシリーズ数", "seriesCountHint": "拡張シェルフに表示するシリーズ数。", "hideShelves": "ホームのシェルフを非表示", "sidebarHint": "使わないサイドメニュー項目を非表示にします。", "showAppName": "アプリ名を表示", "colorizeLogo": "ロゴをアクセントカラーで着色", "hideSeries": "シリーズを非表示", "hideCollections": "コレクションを非表示", "hideAuthors": "著者を非表示", "hideNarrators": "ナレーターを非表示", "hideStats": "統計を非表示", "hideRecentlyAdded": "最近追加を非表示", "hideRecentSeries": "最近のシリーズを非表示", "hideContinueSeries": "シリーズを続けるを非表示", "hideListenAgain": "もう一度聴くを非表示", "hideDiscover": "見つけるを非表示", "hideNewestAuthors": "最新の著者を非表示", "seriesCards": "重ねたシリーズカバー", "heroCarousel": "ホームカルーセル", "gearLabel": "テーマ", "ssLabel": "評価の刻み", "ssHint": "評価をどこまで細かくできるか。選べる値と、すべての評価の表示に適用されます。", "ssFull": "星1つ単位", "ssHalf": "星0.5単位", "ssQuarter": "星0.25単位", "rlLabel": "ライブラリごとの評価", "rlHint": "評価に参加するライブラリ。ポッドキャストのライブラリは、オンにするまで対象外です。", "rlPodcast": "ポッドキャスト", "rfHide": "「読み終えた本を評価」を非表示"},
    ko: {"title": "테마 사용자화", "subtitle": "라이브러리 모양을 개인화하세요. 변경 사항은 자동으로 저장됩니다.", "branding": "브랜딩 및 스타일", "colour": "색상 및 테마", "homeCar": "홈 및 캐러셀", "sidebar": "사이드 메뉴", "appName": "앱 이름", "appNameHint": "기본 이름을 사용하려면 비워 두세요.", "logoUrl": "사용자 지정 로고 URL", "logoHint": "기본 로고를 사용하려면 비워 두세요.", "accent": "강조 색상", "baseTheme": "기본 테마", "mainFont": "기본 글꼴", "carousel": "캐러셀 자동 전환", "carouselHint": "슬라이드 간 초. 0이면 비활성화.", "customSeries": "확장된 최근 시리즈", "seriesCount": "최근 시리즈 수", "seriesCountHint": "확장 선반에 표시할 시리즈 수.", "hideShelves": "홈 선반 숨기기", "sidebarHint": "사용하지 않는 사이드 메뉴 항목을 숨깁니다.", "showAppName": "앱 이름 표시", "colorizeLogo": "로고를 강조 색상으로 칠하기", "hideSeries": "시리즈 숨기기", "hideCollections": "컬렉션 숨기기", "hideAuthors": "저자 숨기기", "hideNarrators": "낭독자 숨기기", "hideStats": "통계 숨기기", "hideRecentlyAdded": "최근 추가 숨기기", "hideRecentSeries": "최근 시리즈 숨기기", "hideContinueSeries": "시리즈 계속 숨기기", "hideListenAgain": "다시 듣기 숨기기", "hideDiscover": "발견 숨기기", "hideNewestAuthors": "최신 저자 숨기기", "seriesCards": "겹쳐진 시리즈 표지", "heroCarousel": "홈 캐러셀", "gearLabel": "테마", "ssLabel": "별점 단위", "ssHint": "별점을 얼마나 세밀하게 매길 수 있는지. 선택할 수 있는 값과 모든 별점의 표시에 적용됩니다.", "ssFull": "별 1개 단위", "ssHalf": "별 0.5개 단위", "ssQuarter": "별 0.25개 단위", "rlLabel": "라이브러리별 별점", "rlHint": "어떤 라이브러리가 별점에 참여할지. 팟캐스트 라이브러리는 켜기 전까지 제외됩니다.", "rlPodcast": "팟캐스트", "rfHide": "‘완독한 책 평가’ 숨기기"},
    zh: {"title": "主题自定义", "subtitle": "个性化你的媒体库外观。更改会自动保存。", "branding": "品牌与样式", "colour": "颜色与主题", "homeCar": "主页与轮播", "sidebar": "侧边菜单", "appName": "应用名称", "appNameHint": "留空则使用默认名称。", "logoUrl": "自定义徽标 URL", "logoHint": "留空则使用默认徽标。", "accent": "强调色", "baseTheme": "基础主题", "mainFont": "主字体", "carousel": "轮播自动切换", "carouselHint": "幻灯片间隔秒数。设为 0 可禁用。", "customSeries": "扩展的最近系列", "seriesCount": "最近系列数量", "seriesCountHint": "扩展书架中显示的系列数量。", "hideShelves": "隐藏主页书架", "sidebarHint": "隐藏不使用的侧边菜单项。", "showAppName": "显示应用名称", "colorizeLogo": "用强调色为徽标着色", "hideSeries": "隐藏系列", "hideCollections": "隐藏收藏", "hideAuthors": "隐藏作者", "hideNarrators": "隐藏朗读者", "hideStats": "隐藏统计", "hideRecentlyAdded": "隐藏最近添加", "hideRecentSeries": "隐藏最近系列", "hideContinueSeries": "隐藏继续系列", "hideListenAgain": "隐藏再听一次", "hideDiscover": "隐藏发现", "hideNewestAuthors": "隐藏最新作者", "seriesCards": "堆叠系列封面", "heroCarousel": "主页轮播", "gearLabel": "主题", "ssLabel": "评分精度", "ssHint": "评分可以有多精确。同时影响可选的值和每个评分的显示方式。", "ssFull": "整星", "ssHalf": "半星", "ssQuarter": "四分之一星", "rlLabel": "按媒体库评分", "rlHint": "哪些媒体库参与评分。播客媒体库默认关闭，除非你打开它。", "rlPodcast": "播客", "rfHide": "隐藏“为读完的书评分”"},
    ar: {"title": "تخصيص السمة", "subtitle": "خصص مظهر مكتبتك. تُحفظ التغييرات تلقائيًا.", "branding": "العلامة والأسلوب", "colour": "اللون والسمة", "homeCar": "الرئيسية والشريط الدوار", "sidebar": "القوائم الجانبية", "appName": "اسم التطبيق", "appNameHint": "اتركه فارغًا للاسم الافتراضي.", "logoUrl": "رابط شعار مخصص", "logoHint": "اتركه فارغًا للشعار الافتراضي.", "accent": "لون التمييز", "baseTheme": "السمة الأساسية", "mainFont": "الخط الرئيسي", "carousel": "التقدم التلقائي للشريط الدوار", "carouselHint": "الثواني بين الشرائح. 0 للتعطيل.", "customSeries": "السلاسل الأخيرة الموسعة", "seriesCount": "عدد السلاسل الأخيرة", "seriesCountHint": "عدد السلاسل المعروضة في الرف الموسع.", "hideShelves": "إخفاء أرفف الصفحة الرئيسية", "sidebarHint": "أخفِ عناصر القائمة الجانبية التي لا تستخدمها.", "showAppName": "إظهار اسم التطبيق", "colorizeLogo": "تلوين الشعار بلون التمييز", "hideSeries": "إخفاء السلاسل", "hideCollections": "إخفاء المجموعات", "hideAuthors": "إخفاء المؤلفين", "hideNarrators": "إخفاء الرواة", "hideStats": "إخفاء الإحصائيات", "hideRecentlyAdded": "إخفاء المضاف حديثًا", "hideRecentSeries": "إخفاء السلاسل الأخيرة", "hideContinueSeries": "إخفاء متابعة السلسلة", "hideListenAgain": "إخفاء الاستماع مجددًا", "hideDiscover": "إخفاء الاستكشاف", "hideNewestAuthors": "إخفاء أحدث المؤلفين", "seriesCards": "أغلفة سلاسل متراكبة", "heroCarousel": "الشريط الدوار للرئيسية", "gearLabel": "السمة", "ssLabel": "دقة التقييم", "ssHint": "مدى دقة التقييم. ينطبق على ما يمكنك اختياره وعلى طريقة عرض كل تقييم.", "ssFull": "نجوم كاملة", "ssHalf": "أنصاف نجوم", "ssQuarter": "أرباع نجوم", "rlLabel": "التقييمات حسب المكتبة", "rlHint": "المكتبات التي تشارك في التقييمات. مكتبات البودكاست معطّلة حتى تقوم بتفعيلها.", "rlPodcast": "بودكاست", "rfHide": "إخفاء «قيّم ما أنهيته»"},
    he: {"title": "התאמות ערכת נושא", "subtitle": "התאם אישית את מראה הספרייה שלך. השינויים נשמרים אוטומטית.", "branding": "מיתוג וסגנון", "colour": "צבע וערכת נושא", "homeCar": "דף הבית וקרוסלה", "sidebar": "תפריטי צד", "appName": "שם האפליקציה", "appNameHint": "השאר ריק לשם ברירת המחדל.", "logoUrl": "כתובת לוגו מותאם אישית", "logoHint": "השאר ריק ללוגו ברירת המחדל.", "accent": "צבע הדגשה", "baseTheme": "ערכת נושא בסיסית", "mainFont": "גופן ראשי", "carousel": "קידום אוטומטי של הקרוסלה", "carouselHint": "שניות בין שקופיות. 0 לביטול.", "customSeries": "סדרות אחרונות מורחבות", "seriesCount": "מספר סדרות אחרונות", "seriesCountHint": "כמה סדרות להציג במדף המורחב.", "hideShelves": "הסתר מדפי דף הבית", "sidebarHint": "הסתר פריטי תפריט צד שאינך משתמש בהם.", "showAppName": "הצג את שם האפליקציה", "colorizeLogo": "צבע את הלוגו בצבע ההדגשה", "hideSeries": "הסתר סדרות", "hideCollections": "הסתר אוספים", "hideAuthors": "הסתר מחברים", "hideNarrators": "הסתר מקריאים", "hideStats": "הסתר סטטיסטיקות", "hideRecentlyAdded": "הסתר נוספו לאחרונה", "hideRecentSeries": "הסתר סדרות אחרונות", "hideContinueSeries": "הסתר המשך סדרה", "hideListenAgain": "הסתר האזן שוב", "hideDiscover": "הסתר גלה", "hideNewestAuthors": "הסתר מחברים חדשים", "seriesCards": "כריכות סדרה בערימה", "heroCarousel": "קרוסלת דף הבית", "gearLabel": "ערכת נושא", "ssLabel": "רזולוציית הדירוג", "ssHint": "עד כמה דירוג יכול להיות מדויק. חל על מה שאפשר לבחור ועל אופן הצגת כל דירוג.", "ssFull": "כוכבים שלמים", "ssHalf": "חצאי כוכבים", "ssQuarter": "רבעי כוכבים", "rlLabel": "דירוגים לפי ספרייה", "rlHint": "אילו ספריות משתתפות בדירוגים. ספריות פודקאסטים כבויות עד שתפעיל אותן.", "rlPodcast": "פודקאסטים", "rfHide": "הסתר \"דרג את מה שסיימת\""},
    fa: {"title": "شخصی‌سازی پوسته", "subtitle": "ظاهر کتابخانه خود را شخصی‌سازی کنید. تغییرات به‌طور خودکار ذخیره می‌شوند.", "branding": "برند و سبک", "colour": "رنگ و پوسته", "homeCar": "خانه و چرخ‌ونما", "sidebar": "منوهای کناری", "appName": "نام برنامه", "appNameHint": "برای نام پیش‌فرض خالی بگذارید.", "logoUrl": "نشانی لوگوی سفارشی", "logoHint": "برای لوگوی پیش‌فرض خالی بگذارید.", "accent": "رنگ تأکید", "baseTheme": "پوسته پایه", "mainFont": "قلم اصلی", "carousel": "پیشروی خودکار چرخ‌ونما", "carouselHint": "ثانیه بین اسلایدها. برای غیرفعال‌سازی 0.", "customSeries": "سری‌های اخیر گسترش‌یافته", "seriesCount": "تعداد سری‌های اخیر", "seriesCountHint": "چند سری در قفسه گسترش‌یافته نمایش داده شود.", "hideShelves": "پنهان‌کردن قفسه‌های صفحه اصلی", "sidebarHint": "موارد منوی کناری که استفاده نمی‌کنید را پنهان کنید.", "showAppName": "نمایش نام برنامه", "colorizeLogo": "رنگ‌آمیزی لوگو با رنگ تأکید", "hideSeries": "پنهان‌کردن سری‌ها", "hideCollections": "پنهان‌کردن مجموعه‌ها", "hideAuthors": "پنهان‌کردن نویسندگان", "hideNarrators": "پنهان‌کردن گویندگان", "hideStats": "پنهان‌کردن آمار", "hideRecentlyAdded": "پنهان‌کردن به‌تازگی افزوده‌شده", "hideRecentSeries": "پنهان‌کردن سری‌های اخیر", "hideContinueSeries": "پنهان‌کردن ادامه سری", "hideListenAgain": "پنهان‌کردن دوباره گوش کن", "hideDiscover": "پنهان‌کردن کشف", "hideNewestAuthors": "پنهان‌کردن جدیدترین نویسندگان", "seriesCards": "جلدهای سری روی‌هم", "heroCarousel": "چرخ‌ونمای صفحه اصلی", "gearLabel": "پوسته", "ssLabel": "گام امتیازدهی", "ssHint": "یک امتیاز چقدر می‌تواند دقیق باشد. هم بر انتخاب و هم بر نحوهٔ نمایش هر امتیاز اثر می‌گذارد.", "ssFull": "ستاره کامل", "ssHalf": "نیم‌ستاره", "ssQuarter": "یک‌چهارم ستاره", "rlLabel": "امتیازها بر پایهٔ کتابخانه", "rlHint": "کدام کتابخانه‌ها در امتیازدهی شرکت دارند. کتابخانه‌های پادکست تا زمانی که روشنشان نکنید خاموش‌اند.", "rlPodcast": "پادکست‌ها", "rfHide": "پنهان کردن «آنچه تمام کرده‌اید را امتیاز دهید»"},
    hi: {"title": "थीम अनुकूलन", "subtitle": "अपनी लाइब्रेरी का रूप वैयक्तिकृत करें। परिवर्तन स्वतः सहेजे जाते हैं।", "branding": "ब्रांडिंग और शैली", "colour": "रंग और थीम", "homeCar": "होम और कैरोसेल", "sidebar": "साइड मेनू", "appName": "ऐप का नाम", "appNameHint": "डिफ़ॉल्ट नाम के लिए खाली छोड़ें।", "logoUrl": "कस्टम लोगो URL", "logoHint": "डिफ़ॉल्ट लोगो के लिए खाली छोड़ें।", "accent": "एक्सेंट रंग", "baseTheme": "मूल थीम", "mainFont": "मुख्य फ़ॉन्ट", "carousel": "कैरोसेल स्वतः आगे बढ़ना", "carouselHint": "स्लाइडों के बीच सेकंड। बंद करने के लिए 0।", "customSeries": "विस्तारित हाल की श्रृंखलाएँ", "seriesCount": "हाल की श्रृंखलाओं की संख्या", "seriesCountHint": "विस्तारित शेल्फ़ में कितनी श्रृंखलाएँ दिखाएँ।", "hideShelves": "होमपेज शेल्फ़ छिपाएँ", "sidebarHint": "साइड मेनू की अनुपयोगी प्रविष्टियाँ छिपाएँ।", "showAppName": "ऐप का नाम दिखाएँ", "colorizeLogo": "लोगो को एक्सेंट रंग से रंगें", "hideSeries": "श्रृंखलाएँ छिपाएँ", "hideCollections": "संग्रह छिपाएँ", "hideAuthors": "लेखक छिपाएँ", "hideNarrators": "वाचक छिपाएँ", "hideStats": "आँकड़े छिपाएँ", "hideRecentlyAdded": "हाल में जोड़े गए छिपाएँ", "hideRecentSeries": "हाल की श्रृंखलाएँ छिपाएँ", "hideContinueSeries": "श्रृंखला जारी रखें छिपाएँ", "hideListenAgain": "फिर से सुनें छिपाएँ", "hideDiscover": "खोजें छिपाएँ", "hideNewestAuthors": "नवीनतम लेखक छिपाएँ", "seriesCards": "परतदार श्रृंखला कवर", "heroCarousel": "होम कैरोसेल", "gearLabel": "थीम", "ssLabel": "रेटिंग की सटीकता", "ssHint": "रेटिंग कितनी सटीक हो सकती है। यह आपके चुनाव और हर रेटिंग के प्रदर्शन दोनों पर लागू होता है।", "ssFull": "पूरे तारे", "ssHalf": "आधे तारे", "ssQuarter": "चौथाई तारे", "rlLabel": "लाइब्रेरी के अनुसार रेटिंग", "rlHint": "कौन-सी लाइब्रेरी रेटिंग में शामिल हैं। पॉडकास्ट लाइब्रेरी तब तक बंद रहती हैं जब तक आप उन्हें चालू न करें।", "rlPodcast": "पॉडकास्ट", "rfHide": "“जो पूरा किया उसे रेटिंग दें” छिपाएँ"},
    bn: {"title": "থিম কাস্টমাইজেশন", "subtitle": "আপনার লাইব্রেরির চেহারা ব্যক্তিগতকৃত করুন। পরিবর্তন স্বয়ংক্রিয়ভাবে সংরক্ষিত হয়।", "branding": "ব্র্যান্ডিং ও শৈলী", "colour": "রঙ ও থিম", "homeCar": "হোম ও ক্যারোজেল", "sidebar": "সাইড মেনু", "appName": "অ্যাপের নাম", "appNameHint": "ডিফল্ট নামের জন্য খালি রাখুন।", "logoUrl": "কাস্টম লোগো URL", "logoHint": "ডিফল্ট লোগোর জন্য খালি রাখুন।", "accent": "অ্যাকসেন্ট রঙ", "baseTheme": "মূল থিম", "mainFont": "প্রধান ফন্ট", "carousel": "ক্যারোজেল স্বয়ংক্রিয় অগ্রগতি", "carouselHint": "স্লাইডের মধ্যে সেকেন্ড। বন্ধ করতে 0।", "customSeries": "সম্প্রসারিত সাম্প্রতিক সিরিজ", "seriesCount": "সাম্প্রতিক সিরিজের সংখ্যা", "seriesCountHint": "সম্প্রসারিত তাকে কতটি সিরিজ দেখানো হবে।", "hideShelves": "হোমপেজের তাক লুকান", "sidebarHint": "অব্যবহৃত সাইড মেনু আইটেম লুকান।", "showAppName": "অ্যাপের নাম দেখান", "colorizeLogo": "লোগোকে অ্যাকসেন্ট রঙে রাঙান", "hideSeries": "সিরিজ লুকান", "hideCollections": "সংগ্রহ লুকান", "hideAuthors": "লেখক লুকান", "hideNarrators": "পাঠক লুকান", "hideStats": "পরিসংখ্যান লুকান", "hideRecentlyAdded": "সম্প্রতি যোগ করা লুকান", "hideRecentSeries": "সাম্প্রতিক সিরিজ লুকান", "hideContinueSeries": "সিরিজ চালিয়ে যান লুকান", "hideListenAgain": "আবার শুনুন লুকান", "hideDiscover": "আবিষ্কার লুকান", "hideNewestAuthors": "নতুনতম লেখক লুকান", "seriesCards": "স্তূপীকৃত সিরিজ কভার", "heroCarousel": "হোম ক্যারোজেল", "gearLabel": "থিম", "ssLabel": "রেটিংয়ের ধাপ", "ssHint": "একটি রেটিং কতটা নিখুঁত হতে পারে। আপনি যা বাছতে পারেন এবং প্রতিটি রেটিং কীভাবে দেখানো হয়, দুটোতেই প্রযোজ্য।", "ssFull": "পূর্ণ তারা", "ssHalf": "অর্ধেক তারা", "ssQuarter": "সিকি তারা", "rlLabel": "লাইব্রেরি অনুযায়ী রেটিং", "rlHint": "কোন লাইব্রেরিগুলি রেটিংয়ে অংশ নেবে। পডকাস্ট লাইব্রেরি আপনি চালু না করা পর্যন্ত বন্ধ থাকে।", "rlPodcast": "পডকাস্ট", "rfHide": "“যা শেষ করেছেন তা রেট করুন” লুকান"},
    gu: {"title": "થીમ કસ્ટમાઇઝેશન", "subtitle": "તમારી લાઇબ્રેરીનો દેખાવ વ્યક્તિગત બનાવો. ફેરફારો આપમેળે સાચવાય છે.", "branding": "બ્રાન્ડિંગ અને શૈલી", "colour": "રંગ અને થીમ", "homeCar": "હોમ અને કેરોસેલ", "sidebar": "સાઇડ મેનૂ", "appName": "એપ્લિકેશનનું નામ", "appNameHint": "ડિફૉલ્ટ નામ માટે ખાલી છોડો.", "logoUrl": "કસ્ટમ લોગો URL", "logoHint": "ડિફૉલ્ટ લોગો માટે ખાલી છોડો.", "accent": "એક્સેન્ટ રંગ", "baseTheme": "મૂળ થીમ", "mainFont": "મુખ્ય ફૉન્ટ", "carousel": "કેરોસેલ સ્વયં આગળ વધવું", "carouselHint": "સ્લાઇડ્સ વચ્ચે સેકંડ. બંધ કરવા 0.", "customSeries": "વિસ્તૃત તાજેતરની શ્રેણીઓ", "seriesCount": "તાજેતરની શ્રેણીઓની સંખ્યા", "seriesCountHint": "વિસ્તૃત શેલ્ફમાં કેટલી શ્રેણીઓ બતાવવી.", "hideShelves": "હોમપેજ શેલ્ફ છુપાવો", "sidebarHint": "ન વપરાતી સાઇડ મેનૂ આઇટમ છુપાવો.", "showAppName": "એપ્લિકેશનનું નામ બતાવો", "colorizeLogo": "લોગોને એક્સેન્ટ રંગથી રંગો", "hideSeries": "શ્રેણીઓ છુપાવો", "hideCollections": "સંગ્રહો છુપાવો", "hideAuthors": "લેખકો છુપાવો", "hideNarrators": "વાચકો છુપાવો", "hideStats": "આંકડા છુપાવો", "hideRecentlyAdded": "તાજેતરમાં ઉમેરાયેલ છુપાવો", "hideRecentSeries": "તાજેતરની શ્રેણીઓ છુપાવો", "hideContinueSeries": "શ્રેણી ચાલુ રાખો છુપાવો", "hideListenAgain": "ફરી સાંભળો છુપાવો", "hideDiscover": "શોધો છુપાવો", "hideNewestAuthors": "નવીનતમ લેખકો છુપાવો", "seriesCards": "સ્તરબદ્ધ શ્રેણી કવર", "heroCarousel": "હોમ કેરોસેલ", "gearLabel": "થીમ", "ssLabel": "રેટિંગનું પગલું", "ssHint": "રેટિંગ કેટલું ચોક્કસ હોઈ શકે. તમે શું પસંદ કરી શકો અને દરેક રેટિંગ કેવી રીતે દેખાય, બંનેને લાગુ પડે છે.", "ssFull": "આખા તારા", "ssHalf": "અડધા તારા", "ssQuarter": "પા તારા", "rlLabel": "લાઇબ્રેરી પ્રમાણે રેટિંગ", "rlHint": "કઈ લાઇબ્રેરીઓ રેટિંગમાં ભાગ લે છે. પોડકાસ્ટ લાઇબ્રેરીઓ તમે ચાલુ ન કરો ત્યાં સુધી બંધ રહે છે.", "rlPodcast": "પોડકાસ્ટ", "rfHide": "“તમે પૂરું કર્યું તેને રેટ કરો” છુપાવો"},
    vi: {"title": "Tùy chỉnh giao diện", "subtitle": "Cá nhân hóa giao diện thư viện của bạn. Thay đổi được lưu tự động.", "branding": "Thương hiệu & phong cách", "colour": "Màu sắc & giao diện", "homeCar": "Trang chủ & băng chuyền", "sidebar": "Menu bên", "appName": "Tên ứng dụng", "appNameHint": "Để trống để dùng tên mặc định.", "logoUrl": "URL logo tùy chỉnh", "logoHint": "Để trống để dùng logo mặc định.", "accent": "Màu nhấn", "baseTheme": "Giao diện cơ bản", "mainFont": "Phông chữ chính", "carousel": "Tự động chuyển băng chuyền", "carouselHint": "Số giây giữa các slide. 0 để tắt.", "customSeries": "Bộ truyện gần đây mở rộng", "seriesCount": "Số bộ truyện gần đây", "seriesCountHint": "Số bộ truyện hiển thị trên kệ mở rộng.", "hideShelves": "Ẩn các kệ trang chủ", "sidebarHint": "Ẩn các mục menu bên bạn không dùng.", "showAppName": "Hiện tên ứng dụng", "colorizeLogo": "Tô màu logo bằng màu nhấn", "hideSeries": "Ẩn Bộ truyện", "hideCollections": "Ẩn Bộ sưu tập", "hideAuthors": "Ẩn Tác giả", "hideNarrators": "Ẩn Người đọc", "hideStats": "Ẩn Thống kê", "hideRecentlyAdded": "Ẩn Mới thêm gần đây", "hideRecentSeries": "Ẩn Bộ truyện gần đây", "hideContinueSeries": "Ẩn Tiếp tục bộ truyện", "hideListenAgain": "Ẩn Nghe lại", "hideDiscover": "Ẩn Khám phá", "hideNewestAuthors": "Ẩn Tác giả mới nhất", "seriesCards": "Bìa bộ truyện xếp chồng", "heroCarousel": "Băng chuyền trang chủ", "gearLabel": "Giao diện", "ssLabel": "Bước đánh giá", "ssHint": "Một đánh giá có thể chính xác đến mức nào. Áp dụng cho lựa chọn của bạn và cho cách mọi đánh giá được vẽ.", "ssFull": "Sao nguyên", "ssHalf": "Nửa sao", "ssQuarter": "Một phần tư sao", "rlLabel": "Đánh giá theo thư viện", "rlHint": "Thư viện nào tham gia đánh giá. Thư viện podcast tắt cho đến khi bạn bật lên.", "rlPodcast": "podcast", "rfHide": "Ẩn Đánh giá sách bạn đã đọc xong"}
  };
  // merge the per-language additions (see NH_T_EXTRA and NH_T_V21 above)
  Object.keys(NH_T_EXTRA).forEach((lang) => {
    if (PANEL_T[lang]) Object.assign(PANEL_T[lang], NH_T_EXTRA[lang]);
  });
  Object.keys(NH_T_V21).forEach((lang) => {
    if (PANEL_T[lang]) Object.assign(PANEL_T[lang], NH_T_V21[lang]);
  });

  function panelT() {
    const lang = getUserLanguage().split('-')[0].toLowerCase();
    return PANEL_T[lang] || PANEL_T.en;
  }

  // book-details.js (loaded after this file) reuses the panel dictionary for
  // its report dialog instead of carrying a second translation table - merged
  // per key so a language missing one string falls back to English for that
  // string only.
  window.__nhPanelT = function () {
    const T = panelT();
    if (T === PANEL_T.en) return T;
    const o = {};
    Object.keys(PANEL_T.en).forEach((k) => { o[k] = T[k] !== undefined ? T[k] : PANEL_T.en[k]; });
    Object.keys(T).forEach((k) => { if (o[k] === undefined) o[k] = T[k]; });
    return o;
  };

  function injectPanelStyles() {
    if (document.getElementById('nh-panel-style')) return;
    const s = document.createElement('style');
    s.id = 'nh-panel-style';
    s.textContent = `
      #nh-settings-panel { max-width: 1560px; margin: 0 auto; padding: 4px 12px 80px; }
      /* Tabs (Pawel: the one long scroll got messy once the admin cards piled up).
         Search suspends them (.nh-searching) so it can match across both tabs. */
      #nh-ptabs { display: flex; flex-wrap: wrap; gap: 8px; margin: 16px 0 4px; }
      .nh-ptab { padding: 8px 20px; border-radius: 999px; border: 1px solid var(--nh-hairline-lit, rgba(255,255,255,0.14)); background: transparent; color: var(--nh-muted-2, #9a9085); font-family: var(--nh-sans, system-ui); font-size: 0.9rem; cursor: pointer; transition: color .15s, background-color .15s, border-color .15s; }
      .nh-ptab:hover { color: var(--nh-text-1, #f4eee2); }
      .nh-ptab.nh-on { background: var(--nh-amber, #e0c27a); border-color: transparent; color: #14110d; font-weight: 600; }
      #nh-settings-panel[data-nh-tab="look"]:not(.nh-searching) .nh-grid > *:not([data-nh-group="look"]),
      #nh-settings-panel[data-nh-tab="home"]:not(.nh-searching) .nh-grid > *:not([data-nh-group="home"]),
      #nh-settings-panel[data-nh-tab="book"]:not(.nh-searching) .nh-grid > *:not([data-nh-group="book"]),
      #nh-settings-panel[data-nh-tab="feat"]:not(.nh-searching) .nh-grid > *:not([data-nh-group="feat"]),
      #nh-settings-panel[data-nh-tab="admin"]:not(.nh-searching) .nh-grid > *:not([data-nh-group="admin"]) { display: none !important; }
      #nh-settings-panel[data-nh-tab] .nh-sect { display: none; }
      /* Tabs holding only two or three cards cap at two WIDER columns instead of
         leaving the third one empty (Pawel); 340px floor keeps phones at one. */
      #nh-settings-panel[data-nh-tab="look"]:not(.nh-searching) .nh-grid,
      #nh-settings-panel[data-nh-tab="home"]:not(.nh-searching) .nh-grid,
      #nh-settings-panel[data-nh-tab="book"]:not(.nh-searching) .nh-grid { columns: 340px 2; }
      /* Administration is always three columns on desktop: defaults + switches,
         Social, Sessions. Without the count cap the multicol balancer packs its
         four cards into however many 340px tracks fit, which varies by window. */
      #nh-settings-panel[data-nh-tab="admin"]:not(.nh-searching) .nh-grid { columns: 340px 3; }
      /* two per-user switches: ranking time and books, icon + toggle each.
         One column always - two cells per row never fit a folded roster. The
         name yields (ellipsis), the switch cells never wrap or shrink. */
      .nh-soc-row { min-width: 0; }
      .nh-soc-row > p { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; padding-right: 6px; }
      .nh-soc-tg { display: flex; align-items: center; gap: 5px; margin-left: 12px; flex: none; }
      .nh-soc-ico { font-size: 0.95rem; color: var(--nh-muted-2, #9a9085); }
      /* wide only while the panel is the content; the config sub-rail is FIXED,
         so the stretch must start right of it or the header slides underneath */
      .configContent.nh-panel-wide { width: 100% !important; }
      @media (min-width: 768px) {
        .configContent.nh-panel-wide { width: calc(100% - 200px) !important; margin-left: 200px !important; }
      }
      #nh-settings-panel .nh-head { margin-bottom: 30px; }
      #nh-settings-panel .nh-head h1 { font-family: var(--nh-serif); font-size: 2rem; font-weight: 600; color: var(--nh-amber); margin: 0; }
      #nh-settings-panel .nh-head p { color: var(--nh-muted, #9a9085); font-size: 0.95rem; margin: 6px 0 0; }
      #nh-settings-panel .nh-grid { column-width: 340px; column-gap: 22px; }
      /* readability: a filter box in the header, calmer card rhythm */
      #nh-settings-panel .nh-psearch-wrap { position: relative; margin-top: 16px; max-width: 420px; }
      #nh-settings-panel .nh-psearch-wrap input { width: 100%; box-sizing: border-box; padding: 10px 34px 10px 38px; border-radius: 999px; border: 1px solid var(--nh-hairline, rgba(255,255,255,0.12)); background: rgba(0,0,0,0.25); color: var(--nh-text-1, #f4eee2); font-family: var(--nh-sans, system-ui); font-size: 0.9rem; }
      #nh-settings-panel .nh-psearch-wrap input:focus { outline: none; border-color: var(--nh-amber, #e0c27a); }
      #nh-settings-panel .nh-psearch-wrap input::-webkit-search-cancel-button { display: none; }
      #nh-settings-panel .nh-psearch-ico { position: absolute; left: 12px; top: 50%; transform: translateY(-50%); font-size: 1.15rem; color: var(--nh-muted-2, #9a9085); pointer-events: none; }
      #nh-settings-panel #nh-psearch-x { display: none; position: absolute; right: 8px; top: 50%; transform: translateY(-50%); background: none; border: none; color: var(--nh-muted-2, #9a9085); font-size: 1.2rem; line-height: 1; cursor: pointer; padding: 4px 8px; }
      #nh-settings-panel #nh-psearch-x:hover { color: var(--nh-text-1, #f4eee2); }
      #nh-settings-panel .nh-psearch-none { display: none; color: var(--nh-muted-2, #9a9085); font-size: 0.9rem; margin: 14px 0 0; }
      #nh-settings-panel .nh-card-title { display: flex; align-items: center; gap: 8px; }
      #nh-settings-panel .nh-card { background: var(--nh-raised, #221e1a); border: 1px solid var(--nh-hairline, rgba(255,255,255,0.06)); border-radius: 18px; padding: 24px 26px; box-shadow: 0 8px 24px rgba(0,0,0,0.30); width: 100%; box-sizing: border-box; margin-bottom: 24px; break-inside: avoid; -webkit-column-break-inside: avoid; }
      #nh-settings-panel .nh-card-title { font-family: var(--nh-serif); color: var(--nh-amber); font-size: 1.15rem; font-weight: 600; margin: 0 0 18px; padding-bottom: 12px; border-bottom: 1px solid var(--nh-hairline, rgba(255,255,255,0.08)); }
      #nh-settings-panel .nh-field { margin-bottom: 20px; }
      #nh-settings-panel .nh-field:last-child { margin-bottom: 0; }
      #nh-settings-panel .nh-label { display: block; font-size: 0.8rem; font-weight: 600; color: var(--nh-text-3, #cfc6ba); margin-bottom: 7px; letter-spacing: 0.01em; }
      #nh-settings-panel .nh-hint { font-size: 0.74rem; color: var(--nh-muted, #9a9085); margin: -2px 0 8px; }
      #nh-settings-panel input[type=text], #nh-settings-panel input[type=number], #nh-settings-panel input[type=password] { width: 100%; box-sizing: border-box; background: rgba(0,0,0,0.25); border: 1px solid var(--nh-hairline-lit, rgba(255,255,255,0.14)); border-radius: 10px; padding: 10px 13px; color: var(--nh-text-1, #f4eee2); outline: none; transition: border-color .15s; font-size: 0.95rem; }
      #nh-settings-panel input[type=text]:focus, #nh-settings-panel input[type=number]:focus, #nh-settings-panel input[type=password]:focus { border-color: var(--nh-amber); }
      #nh-settings-panel input.nh-in-bad, #nh-settings-panel input.nh-in-bad:focus { border-color: #d9534f; }
      /* the library scan: switch rows, a progress bar with the numbers above it, the title under it */
      #nh-settings-panel .nh-cm-libs { margin: 2px 0 4px; }
      #nh-settings-panel .nh-cm-all { border-top: 1px solid var(--nh-hairline, rgba(255,255,255,0.08)); margin-top: 6px; padding-top: 10px; }
      #nh-settings-panel .nh-cm-prog { margin: 16px 0 0; }
      #nh-settings-panel .nh-cm-meta { display: flex; justify-content: space-between; align-items: baseline; font-family: var(--nh-sans, system-ui); font-variant-numeric: tabular-nums; margin-bottom: 6px; }
      #nh-settings-panel .nh-cm-pct { font-size: 1.15rem; font-weight: 600; color: var(--nh-text-1, #f4eee2); }
      #nh-settings-panel .nh-cm-cnt { font-size: 0.86rem; color: var(--nh-muted-2, #9a9085); }
      #nh-settings-panel .nh-cm-bar { height: 6px; border-radius: 999px; background: rgba(255,255,255,0.08); overflow: hidden; }
      #nh-settings-panel .nh-cm-fill { height: 100%; width: 0; border-radius: 999px; background: var(--nh-amber, #e0c27a); transition: width 0.5s ease; }
      #nh-settings-panel .nh-cm-scantitle { font-size: 0.86rem; color: var(--nh-muted-2, #9a9085); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; min-height: 1.4em; margin: 8px 0 0; }
      #nh-settings-panel .nh-cm-actions { display: flex; gap: 10px; align-items: center; flex-wrap: wrap; margin-top: 16px; }
      #nh-settings-panel .nh-cm-go { background: var(--nh-amber-tint, rgba(224,194,122,0.14)); border-color: var(--nh-amber, #e0c27a); }
      #nh-settings-panel .nh-cm-done { margin: 10px 0 0; }
      #nh-settings-panel .nh-cm-link { background: none; border: 0; padding: 0; font: inherit; color: var(--nh-amber, #e0c27a); cursor: pointer; text-decoration: underline dotted; text-underline-offset: 3px; }
      #nh-settings-panel .nh-cm-missed { margin-top: 8px; max-height: 280px; overflow-y: auto; border: 1px solid var(--nh-hairline, rgba(255,255,255,0.08)); border-radius: 10px; background: rgba(0,0,0,0.18); padding: 4px 12px; font-size: 0.86rem; }
      #nh-settings-panel .nh-cm-mrow { display: flex; justify-content: space-between; gap: 12px; padding: 6px 0; border-top: 1px solid var(--nh-hairline, rgba(255,255,255,0.06)); color: var(--nh-text-1, #f4eee2); text-decoration: none !important; }
      #nh-settings-panel .nh-cm-mrow:first-child { border-top: 0; }
      #nh-settings-panel .nh-cm-mrow:hover .nh-cm-mt { color: var(--nh-amber, #e0c27a); }
      #nh-settings-panel .nh-cm-mt { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
      #nh-settings-panel .nh-cm-ma { flex: none; color: var(--nh-muted-2, #9a9085); font-size: 0.8rem; }
      #nh-settings-panel .nh-cm-code { margin: 6px 0 0; padding: 10px 12px; background: rgba(0,0,0,0.3); border: 1px solid var(--nh-hairline-lit, rgba(255,255,255,0.14)); border-radius: 10px; font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; font-size: 0.78rem; line-height: 1.45; color: var(--nh-text-1, #f4eee2); white-space: pre; overflow-x: auto; user-select: all; }
      /* explicit column breaks - the balancer must not re-decide placement */
      #nh-settings-panel:not(.nh-searching) .nh-grid > .nh-card[data-nh-break] { break-before: column; }
      /* "?" hint popovers: the little button rides its label, the text drops
         down on click */
      .nh-qm { position: relative; display: inline-flex; align-items: center; justify-content: center; width: 17px; height: 17px; margin-left: 7px; vertical-align: 1px; border-radius: 50%; border: 1px solid var(--nh-hairline-lit, rgba(255,255,255,0.2)); background: transparent; color: var(--nh-muted-2, #9a9085); font-size: 0.7rem; font-weight: 700; line-height: 1; cursor: pointer; padding: 0; }
      .nh-qm:hover { color: var(--nh-amber, #e0c27a); border-color: var(--nh-amber, #e0c27a); }
      .nh-qm-pop { display: none; position: absolute; top: calc(100% + 8px); left: -8px; z-index: 70; width: 300px; max-width: 74vw; background: var(--nh-raised, #221e1a); border: 1px solid var(--nh-hairline-lit, rgba(255,255,255,0.14)); border-radius: 10px; padding: 10px 13px; color: var(--nh-text-2, #d8cfc2); font-size: 0.82rem; font-weight: 400; line-height: 1.5; text-align: left; text-transform: none; letter-spacing: normal; box-shadow: 0 12px 30px rgba(0,0,0,0.45); cursor: default; white-space: normal; }
      .nh-qm.nh-qm-open .nh-qm-pop { display: block; }
      /* Hardcover bulk push: spinner while it works, unmatched titles as links */
      .nh-hc-spin { display: inline-block; font-size: 1rem; vertical-align: -3px; margin-right: 6px; color: var(--nh-amber, #e0c27a); animation: nh-hc-rot 1s linear infinite; }
      @keyframes nh-hc-rot { to { transform: rotate(360deg); } }
      .nh-hc-miss-row { display: block; font-size: 0.85rem; color: var(--nh-text-2, #d8cfc2); padding: 2px 0; text-decoration: none; }
      .nh-hc-miss-row:hover { color: var(--nh-amber, #e0c27a); }
      /* the three import sources, small caps above the CSV button */
      .nh-imp-srcs { font-family: var(--nh-sans, system-ui); font-size: 0.72rem; letter-spacing: 0.06em; text-transform: uppercase; color: var(--nh-muted-2, #9a9085); margin: 0 0 8px; }
      #nh-settings-panel .nh-upload-btn { background: rgba(255,255,255,0.06); border: 1px solid var(--nh-hairline-lit, rgba(255,255,255,0.14)); border-radius: 10px; padding: 9px 16px; color: var(--nh-text-1, #f4eee2); font-size: 0.86rem; font-weight: 600; cursor: pointer; white-space: nowrap; transition: background .15s, border-color .15s; }
      #nh-settings-panel .nh-upload-btn:hover { background: var(--nh-amber-tint, rgba(224,194,122,0.14)); border-color: var(--nh-amber); }
      #nh-settings-panel .nh-subhead { font-size: 0.72rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.12em; color: var(--nh-muted, #9a9085); margin: 4px 0 6px; }
      #nh-settings-panel .nh-toggle-group { border-left: 2px solid var(--nh-hairline, rgba(255,255,255,0.07)); padding-left: 14px; }
      #nh-settings-panel .nh-divider { border-top: 1px solid var(--nh-hairline, rgba(255,255,255,0.08)); margin: 22px 0; }
      #nh-settings-modal { position: fixed; inset: 0; z-index: 1000; display: flex; align-items: center; justify-content: center; }
      #nh-settings-modal .nh-modal-backdrop { position: absolute; inset: 0; background: rgba(0,0,0,0.6); backdrop-filter: blur(4px); -webkit-backdrop-filter: blur(4px); }
      #nh-settings-modal .nh-modal-container { position: relative; z-index: 1; width: 94%; max-width: 1560px; max-height: 85vh; background: var(--nh-canvas, #0b1618); border: 1px solid var(--nh-hairline-lit, rgba(255,255,255,0.12)); border-radius: 20px; overflow: hidden; display: flex; flex-direction: column; box-shadow: 0 20px 60px rgba(0,0,0,0.7); }
      #nh-settings-modal .nh-modal-close-row { display: flex; justify-content: flex-end; padding: 12px 16px 0; }
      #nh-settings-modal .nh-modal-close { background: none; border: none; color: var(--nh-text-2, #d8cfc2); font-size: 1.8rem; cursor: pointer; padding: 4px 12px; border-radius: 8px; line-height: 1; }
      #nh-settings-modal .nh-modal-close:hover { background: rgba(255,255,255,0.1); }
      #nh-settings-modal .nh-modal-body { flex: 1; overflow-y: auto; padding: 0 24px 32px; }
    `;
    document.head.appendChild(s);
  }

  // ===================== THEMED SELECT (nhSelectify) =====================
  // A native <select>'s dropdown list is OS chrome - the white system list in
  // Pawel's screenshot - and no CSS reaches it. Every themed select keeps its
  // native element (hidden) as the source of truth for value + change events,
  // and gets a frosted button/menu pair (same chrome as the unified menus).
  // Handles async option rebuilds (start-page select) and optgroups (fonts).

  // The close() of the menu that is currently open, if any. One at a time, and a
  // handle the route guard can pull.
  let nhSelOpen = null;
  // Is the button hidden inside one of its own scrollers right now? A portaled
  // menu follows the button, and the button can be scrolled clean out of the
  // pop-up while the menu still floats over the page.
  // The box the menu should try to stay inside: the viewport intersected with
  // every clipping ancestor. Fixed positioning MAY escape the pop-up, but a menu
  // hanging past a dialog's rounded corner looks like a mistake - so we prefer
  // the side that fits inside the dialog, and only fall back to the raw viewport
  // when neither side can hold a usable menu there.
  function nhSelClipBox(btn) {
    let top = 0, bottom = innerHeight;
    let el = btn.parentElement;
    while (el && el !== document.body && el !== document.documentElement) {
      const cs = getComputedStyle(el);
      if (!/visible/.test(cs.overflowY) || !/visible/.test(cs.overflowX)) {
        const b = el.getBoundingClientRect();
        if (b.height) { top = Math.max(top, b.top); bottom = Math.min(bottom, b.bottom); }
      }
      el = el.parentElement;
    }
    return { top: top, bottom: bottom };
  }
  function nhSelBtnClipped(btn) {
    const r = btn.getBoundingClientRect();
    const mid = r.top + r.height / 2;
    let el = btn.parentElement;
    while (el && el !== document.body && el !== document.documentElement) {
      const cs = getComputedStyle(el);
      if (/auto|scroll|hidden/.test(cs.overflowY) || /auto|scroll|hidden/.test(cs.overflowX)) {
        const b = el.getBoundingClientRect();
        if (b.height && (mid < b.top || mid > b.bottom)) return true;
        if (b.width && (r.left > b.right || r.right < b.left)) return true;
      }
      el = el.parentElement;
    }
    return false;
  }
  // Keyboard or programmatic navigation fires no pointerdown, so nothing would
  // close a portaled menu - it would float over the next page (the same trap the
  // date picker hit in round 10). The menu closes itself in place() when its
  // button goes away, but place() only runs on scroll/resize; this runs on the tick.
  // The same tick sweeps menus whose select has been destroyed (the panel is rebuilt
  // on navigation, and the import dialog's rows go with it) - they live in <body>,
  // so nothing else would ever collect them.
  function nhSelGuard() {
    if (nhSelOpen && !document.querySelector('.nh-sel.nh-open')) nhSelOpen();
    document.querySelectorAll('body > .nh-sel-menu').forEach((m) => {
      if (!m.__nhOwner || !m.__nhOwner.isConnected) m.remove();
    });
  }

  function nhSelectify(sel, inline) {
    if (!sel || sel.__nhSel) return;
    sel.__nhSel = true;
    const wrap = document.createElement('div');
    wrap.className = 'nh-sel' + (inline ? ' nh-sel-inline' : '');
    sel.parentNode.insertBefore(wrap, sel);
    wrap.appendChild(sel);
    sel.classList.add('nh-sel-native');
    const btn = document.createElement('button');
    btn.type = 'button';
    btn.className = 'nh-sel-btn';
    btn.setAttribute('aria-haspopup', 'listbox');
    const lab = document.createElement('span'); lab.className = 'nh-sel-lab';
    const chev = document.createElement('span'); chev.className = 'nh-sel-chev'; chev.textContent = '▾';
    btn.appendChild(lab); btn.appendChild(chev);
    const menu = document.createElement('div');
    menu.className = 'nh-sel-menu';
    menu.setAttribute('role', 'listbox');
    wrap.appendChild(btn);

    // THE MENU LIVES IN <body>, NOT IN THE SELECT, and is a position:fixed layer
    // when open. It used to be an absolutely positioned CHILD of the panel, which
    // in the customizations pop-up is a scroller inside an overflow:hidden dialog
    // AND a multi-column box. Three defects came out of that, all measured:
    //   - the menu was sliced off at the pop-up's bottom edge (14px on the Series
    //     Page select at 1500x1000);
    //   - its overflow inflated the scroller's scrollHeight by however far it stuck
    //     out, so scrolling down to see the cut-off part and then closing the menu
    //     snapped scrollTop back and the WHOLE panel jumped (126px, end to end);
    //   - and in FIREFOX, merely adding or removing nodes inside a card made the
    //     multi-column balancer re-settle on a different solution, nudging the panel
    //     ~8px on every open. That last one is why the menu is parented to <body> at
    //     creation instead of being moved there on open: the panel's DOM is then
    //     never touched at all, so there is nothing for the balancer to react to.
    // A fixed layer at body level also cannot be clipped by an ancestor and takes
    // part in no scroll geometry. It costs us the automatic tracking an absolute
    // child got for free, hence place()/reflow below, and it has to be swept when
    // its select goes away (nhSelGuard).
    menu.__nhOwner = wrap;
    document.body.appendChild(menu);
    let placeQueued = false;
    const place = () => {
      // panel rebuilt or navigated away underneath us
      if (!btn.isConnected) { close(); return; }
      const r = btn.getBoundingClientRect();
      if (!r.width && !r.height) { close(); return; }
      const GAP = 6, EDGE = 10, MIN = 120;
      // Measuring costs the reader their place in a long list: lifting the max-height
      // below makes the menu its full height for an instant, which clamps scrollTop to
      // 0, and re-applying it does not put it back. Saved and restored around the
      // measurement - a resize or an outer scroll must not dump you back to the top
      // half way down a list of candidates.
      const keepTop = menu.scrollTop;
      // scrollHeight is the unclamped content height; read it with no max-height on
      menu.style.maxHeight = 'none';
      const want = Math.min(menu.scrollHeight + 2, Math.min(innerHeight * 0.42, 380));
      const box = nhSelClipBox(btn);
      let below = box.bottom - r.bottom - GAP - EDGE;
      let above = r.top - box.top - GAP - EDGE;
      // Inside the dialog is preferred, but not at the price of a useless sliver.
      if (Math.max(below, above) < Math.min(want, MIN)) {
        below = innerHeight - r.bottom - GAP - EDGE;
        above = r.top - GAP - EDGE;
      }
      // flip up only when down genuinely cannot hold it AND up holds more
      const up = want > below && above > below;
      menu.style.maxHeight = Math.max(96, Math.min(want, up ? above : below)) + 'px';
      menu.style.minWidth = Math.round(r.width) + 'px';
      menu.style.left = Math.round(r.left) + 'px';
      // a long option list can be wider than its button - clamp after measuring
      const mw = menu.getBoundingClientRect().width;
      menu.style.left = Math.round(Math.max(EDGE, Math.min(r.left, innerWidth - mw - EDGE))) + 'px';
      if (up) {
        menu.style.top = 'auto';
        menu.style.bottom = Math.round(innerHeight - r.top + GAP) + 'px';
      } else {
        menu.style.bottom = 'auto';
        menu.style.top = Math.round(r.bottom + GAP) + 'px';
      }
      wrap.classList.toggle('nh-up', up);
      // The button can be scrolled out of its own scroller while the menu floats
      // free of it; hide rather than leave a menu pointing at nothing.
      menu.style.visibility = nhSelBtnClipped(btn) ? 'hidden' : '';
      if (menu.scrollTop !== keepTop) menu.scrollTop = keepTop;
    };
    const reflow = () => {
      if (placeQueued) return;
      placeQueued = true;
      requestAnimationFrame(() => { placeQueued = false; if (wrap.classList.contains('nh-open')) place(); });
    };
    const close = () => {
      wrap.classList.remove('nh-open', 'nh-up');
      btn.setAttribute('aria-expanded', 'false');
      menu.classList.remove('nh-sel-fixed');
      menu.removeAttribute('style');
      if (nhSelOpen === close) nhSelOpen = null;
      document.removeEventListener('pointerdown', outside, true);
      document.removeEventListener('keydown', onKey, true);
      removeEventListener('scroll', onScroll, true);
      removeEventListener('resize', reflow, true);
    };
    // A scroll INSIDE the menu is the reader using the menu - not the page moving
    // under it - and re-placing on it made a scrollable menu impossible to scroll:
    // every wheel tick re-measured and put the list back at the top. Reported from
    // the field on the ratings import, where a row that needs a second look offers
    // up to seven candidates and the picker sits low enough in the dialog to be
    // squeezed to its 96px floor. It affected every dropdown long enough to scroll.
    const onScroll = (e) => { if (e.target !== menu && !(e.target && e.target.nodeType === 1 && menu.contains(e.target))) reflow(); };
    // The menu is no longer inside `wrap`, so it has to be tested separately or
    // pointerdown on an option closes the menu before its click can fire.
    const outside = (e) => { if (!wrap.contains(e.target) && !menu.contains(e.target)) close(); };

    // Keyboard support. The native <select> is hidden, so until now an open menu
    // answered to nothing but Escape - the reader who could not reach an option with
    // the wheel could not reach it with the arrows either, which is how the import
    // picker became a dead end. Arrow keys move an active row (and scroll it into
    // view), Enter/Space takes it, Home/End jump the ends.
    const items = () => Array.prototype.slice.call(menu.querySelectorAll('.nh-sel-item'));
    const setActive = (list, i) => {
      list.forEach((el, n) => el.classList.toggle('nh-active', n === i));
      if (list[i]) list[i].scrollIntoView({ block: 'nearest' });
    };
    const activeIndex = (list) => {
      const i = list.findIndex((el) => el.classList.contains('nh-active'));
      return i >= 0 ? i : list.findIndex((el) => el.classList.contains('nh-on'));
    };
    const onKey = (e) => {
      if (e.key === 'Escape') { e.stopPropagation(); close(); btn.focus(); return; }
      const list = items();
      if (!list.length) return;
      const cur = activeIndex(list);
      if (e.key === 'ArrowDown' || e.key === 'ArrowUp') {
        e.preventDefault(); e.stopPropagation();   // or the page scrolls behind the menu
        const next = e.key === 'ArrowDown'
          ? Math.min(list.length - 1, (cur < 0 ? -1 : cur) + 1)
          : Math.max(0, (cur < 0 ? list.length : cur) - 1);
        setActive(list, next);
      } else if (e.key === 'Home' || e.key === 'End') {
        e.preventDefault(); e.stopPropagation();
        setActive(list, e.key === 'Home' ? 0 : list.length - 1);
      } else if (e.key === 'Enter' || e.key === ' ') {
        if (cur < 0) return;
        e.preventDefault(); e.stopPropagation();
        list[cur].click();                          // one selection path, not two
        btn.focus();
      }
    };
    const rebuild = () => {
      const opt = sel.options[sel.selectedIndex];
      // Only write the label when it actually changed. Assigning the SAME string
      // still replaces the text node, and in FIREFOX that alone makes the
      // customizations panel's multi-column balancer re-settle on a different
      // solution - measured 8.4px of card movement on every open, with the class
      // toggles, the aria attribute and the body-level menu all ruled out one by one.
      const txt = opt ? opt.textContent : '';
      if (lab.textContent !== txt) lab.textContent = txt;
      menu.textContent = '';
      const addOpt = (o) => {
        const row = document.createElement('div');
        row.className = 'nh-sel-item' + (o.selected ? ' nh-on' : '');
        row.setAttribute('role', 'option');
        row.textContent = o.textContent;
        if (o.style && o.style.fontFamily) row.style.fontFamily = o.style.fontFamily;
        row.addEventListener('click', () => {
          sel.value = o.value;
          sel.dispatchEvent(new Event('change', { bubbles: true }));
          rebuild();
          close();
        });
        menu.appendChild(row);
      };
      Array.prototype.forEach.call(sel.children, (ch) => {
        if (ch.tagName === 'OPTGROUP') {
          const g = document.createElement('div'); g.className = 'nh-sel-group'; g.textContent = ch.label;
          menu.appendChild(g);
          Array.prototype.forEach.call(ch.children, addOpt);
        } else if (ch.tagName === 'OPTION') addOpt(ch);
      });
    };
    btn.addEventListener('click', () => {
      if (wrap.classList.contains('nh-open')) { close(); return; }
      if (nhSelOpen) nhSelOpen();               // only one open at a time
      rebuild();
      wrap.classList.add('nh-open');
      btn.setAttribute('aria-expanded', 'true');
      menu.classList.add('nh-sel-fixed');
      nhSelOpen = close;
      place();
      document.addEventListener('pointerdown', outside, true);
      document.addEventListener('keydown', onKey, true);
      // capture:true so a scroll inside ANY ancestor scroller is seen, not just window
      addEventListener('scroll', onScroll, true);
      addEventListener('resize', reflow, true);
      // Open with the current value in view and armed for the arrow keys.
      const list = items();
      const on = list.findIndex((el) => el.classList.contains('nh-on'));
      if (on >= 0) { list[on].classList.add('nh-active'); list[on].scrollIntoView({ block: 'nearest' }); }
    });
    // The button is a real <button>, so Enter and Space already open the menu; the
    // arrow keys should too, the way a native select does.
    btn.addEventListener('keydown', (e) => {
      if ((e.key === 'ArrowDown' || e.key === 'ArrowUp') && !wrap.classList.contains('nh-open')) {
        e.preventDefault();
        btn.click();
      }
    });
    sel.addEventListener('change', rebuild);
    // options that arrive async (start-page libraries) refresh the label
    try { new MutationObserver(rebuild).observe(sel, { childList: true, subtree: true }); } catch (e) {}
    rebuild();
  }

  function ensureConfigNavItem() {
    const nav = document.querySelector('[aria-label="Config Navigation"]');
    if (!nav) return;

    let item = nav.querySelector('#nh-config-nav-item');
    if (!item) {
      item = document.createElement('a');
      item.id = 'nh-config-nav-item';
      item.href = '/audiobookshelf/config' + NH_HASH;
      item.className = 'w-full px-3 h-12 border-b border-primary/30 flex items-center cursor-pointer relative hover:bg-primary/30';
      item.innerHTML = `<p class="leading-4"></p><div class="h-full w-0.5 bg-yellow-400 absolute top-0 left-0" style="display:none;"></div>`;

      const libLink = nav.querySelector('a[href$="/config/libraries"]');
      const settingsLink = nav.querySelector('a[href$="/config"]');
      if (libLink) {
        libLink.parentNode.insertBefore(item, libLink);
      } else if (settingsLink) {
        settingsLink.parentNode.insertBefore(item, settingsLink.nextSibling);
      } else {
        nav.appendChild(item);
      }

      item.addEventListener('click', (e) => {
        e.preventDefault();
        // Non-admins cannot open /config - ABS's guard bounces them to home
        // (Pawel: "click customizations … brings me back to the home screen").
        // They get the same modal the appbar gear opens, right where they are.
        if (!isUserAdmin()) { openSettingsModal(); return; }
        const onConfigRoot = /\/config\/?$/.test(window.location.pathname);
        if (onConfigRoot) {
          if (window.location.hash !== NH_HASH) window.location.hash = NH_HASH;
        } else {
          window.location.href = '/audiobookshelf/config' + NH_HASH;
        }
        setTimeout(() => { try { injectSettingsPanel(); } catch (err) {} }, 0);
      });
    }

    const p = item.querySelector('p');
    if (p && p.textContent !== navLabel()) p.textContent = navLabel();
  }

  function setNavActive(active) {
    const nav = document.querySelector('[aria-label="Config Navigation"]');
    if (!nav) return;
    const item = nav.querySelector('#nh-config-nav-item');
    const settings = nav.querySelector('a[href$="/config"]');
    if (active) {
      if (item) { item.classList.add('bg-primary/70'); item.classList.remove('hover:bg-primary/30'); item.setAttribute('aria-current', 'page'); }
      if (settings) { settings.classList.remove('bg-primary/70', 'nuxt-link-exact-active', 'nuxt-link-active'); settings.classList.add('hover:bg-primary/30'); settings.removeAttribute('aria-current'); }
    } else {
      if (item) { item.classList.remove('bg-primary/70'); item.classList.add('hover:bg-primary/30'); item.removeAttribute('aria-current'); }
    }
  }

  function createCustomizationsPanel(configPage, hideBranding = false, isConfigPage = false) {
    injectPanelStyles();

    const panel = document.createElement('div');
    panel.id = 'nh-settings-panel';

    const T = panelT();

    const makeSwatch = (c) => `<button type="button" data-color="${c}" class="nh-color-preset w-8 h-8 rounded-full border-2 border-transparent hover:scale-110 transition-transform shadow-md shrink-0" style="background-color: ${c};"></button>`;

    const swatchesHtml = `
      <div style="display: flex; flex-wrap: wrap; gap: 11px; margin-top: 4px; align-items: center;">
        ${presetColors.map(makeSwatch).join('')}
        <label id="nh-custom-color-label" class="relative w-8 h-8 rounded-full flex items-center justify-center shrink-0 cursor-pointer border-2 border-gray-500 hover:scale-110 transition-transform shadow-md" title="Custom Color" style="background: conic-gradient(red, yellow, lime, aqua, blue, magenta, red);">
          <input type="color" id="nh-in-color" class="absolute opacity-0 w-full h-full cursor-pointer" value="${nhSettings.accentColor}">
        </label>
      </div>
    `;

    panel.innerHTML = `
      <div class="nh-head">
        <h1>${T.title}</h1>
        <p>${T.subtitle}</p>
        <div class="nh-psearch-wrap">
          <span class="material-symbols nh-psearch-ico">search</span>
          <input type="search" id="nh-psearch" placeholder="${T.pSearch || PANEL_T.en.pSearch}" autocomplete="off">
          <button type="button" id="nh-psearch-x" aria-label="clear">×</button>
        </div>
        <p class="nh-psearch-none" id="nh-psearch-none">${T.pSearchNone || PANEL_T.en.pSearchNone}</p>
      </div>

      <div class="nh-grid">

        <section class="nh-card">
          <h2 class="nh-card-title">${T.branding}</h2>
          <div class="nh-field">
            <label class="nh-label">${T.appName}</label>
            <p class="nh-hint">${T.appNameHint}</p>
            <input type="text" id="nh-in-name" placeholder="audiobookshelf" autocomplete="off" data-lpignore="true" data-1p-ignore data-bwignore data-form-type="other">
          </div>
          <div class="nh-field" id="nh-tog-text"></div>
          <div class="nh-field">
            <label class="nh-label">${T.logoUrl}</label>
            <p class="nh-hint">${T.logoHint}</p>
            <input type="text" id="nh-in-logo" placeholder="https://... ${T.logoOr || 'or'} /_nh/logo.png" autocomplete="off" data-lpignore="true" data-1p-ignore data-bwignore data-form-type="other" spellcheck="false">
            <div id="nh-logo-upload-row" style="display:none; align-items:center; gap:10px; margin-top:10px;">
              <input type="file" id="nh-in-logo-file" accept="image/png,image/svg+xml,image/jpeg,image/webp,image/gif,image/avif,image/x-icon" style="display:none">
              <button type="button" id="nh-logo-upload-btn" class="nh-upload-btn">${T.logoUpload || 'Upload from device…'}</button>
              <span id="nh-logo-upload-status" class="nh-hint" style="margin:0;"></span>
            </div>
          </div>
          <div class="nh-field" id="nh-tog-colorize"></div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.colour}</h2>
          <div class="nh-field">
            <label class="nh-label">${T.accent}</label>
            ${swatchesHtml}
          </div>
          <div class="nh-divider"></div>
          <div class="nh-field">
            <label class="nh-label">${T.baseTheme}</label>
            <div id="nh-theme-buttons"></div>
          </div>
          <div class="nh-divider"></div>
          <div class="nh-field">
            <label class="nh-label">${T.mainFont}</label>
            <div id="nh-font-buttons"></div>
          </div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.homeCar}</h2>
          <div class="nh-field">
            <label class="nh-label">${T.carousel}</label>
            <p class="nh-hint">${T.carouselHint}</p>
            <input type="number" id="nh-in-carousel" min="0" step="5">
          </div>
          <div class="nh-field" id="nh-tog-customseries"></div>
          <div class="nh-field" id="nh-tog-seriescards"></div>
          <div class="nh-field" id="nh-tog-herocarousel"></div>
          <div class="nh-field"><label class="nh-label" id="nh-cr-label"></label><div id="nh-sel-crmode"></div></div>
          <div class="nh-field">
            <label class="nh-label">${T.seriesCount}</label>
            <p class="nh-hint">${T.seriesCountHint}</p>
            <input type="number" id="nh-in-series-count" min="1" max="50" step="1">
          </div>
          <div class="nh-divider"></div>
          <div class="nh-subhead">${T.hideShelves}</div>
          <div id="nh-togs-home" class="nh-toggle-group"></div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.sidebar}</h2>
          <p class="nh-hint" style="margin-top:0;">${T.sidebarHint}</p>
          <div id="nh-togs-rail" class="nh-toggle-group"></div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.bookPage || PANEL_T.en.bookPage}</h2>
          <p class="nh-hint" style="margin-top:0;">${T.ratingsHint || PANEL_T.en.ratingsHint}</p>
          <div class="nh-field" id="nh-tog-ratings"></div>
          <div class="nh-divider"></div>
          <div class="nh-subhead">${T.ssLabel || PANEL_T.en.ssLabel}</div>
          <p class="nh-hint" style="margin-top:0;">${T.ssHint || PANEL_T.en.ssHint}</p>
          <div class="nh-field"><div id="nh-sel-starstep"></div></div>
          <div class="nh-divider"></div>
          <div class="nh-subhead">${T.rlLabel || PANEL_T.en.rlLabel}</div>
          <p class="nh-hint" style="margin-top:0;">${T.rlHint || PANEL_T.en.rlHint}</p>
          <div id="nh-rating-libs" class="nh-toggle-group"></div>
          <div class="nh-divider"></div>
          <div class="nh-subhead">${T.impLabel || PANEL_T.en.impLabel}</div>
          <p class="nh-hint" style="margin-top:0;">${T.impHint || PANEL_T.en.impHint}</p>
          <div class="nh-field" id="nh-imp-mount"></div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">Hardcover</h2>
          <p class="nh-hint" style="margin-top:0;">${T.hcHint || PANEL_T.en.hcHint}</p>
          <div class="nh-field">
            <label class="nh-label">${T.hcKeyLbl || PANEL_T.en.hcKeyLbl}</label>
            <div style="display:flex; gap:10px; align-items:center;">
              <input type="password" id="nh-hc-key" placeholder="Bearer eyJ..." autocomplete="new-password" data-lpignore="true" data-1p-ignore data-bwignore data-form-type="other" style="flex:1;">
              <button type="button" id="nh-hc-save" class="nh-upload-btn">${T.sdSave || PANEL_T.en.sdSave}</button>
            </div>
          </div>
          <div class="nh-field" id="nh-tog-hcsync"></div>
          <div class="nh-field" style="display:flex; gap:10px; align-items:center; flex-wrap:wrap;">
            <button type="button" id="nh-hc-all" class="nh-upload-btn">${T.hcPushAll || PANEL_T.en.hcPushAll}</button>
            <button type="button" id="nh-hc-diff" class="nh-upload-btn">${T.hcPushDiff || PANEL_T.en.hcPushDiff}</button>
            <button type="button" id="nh-hc-import" class="nh-upload-btn">${T.hcImport || PANEL_T.en.hcImport}</button>
          </div>
          <span class="nh-rt-status" id="nh-hc-status"></span>
          <div id="nh-hc-miss"></div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.searchCard || PANEL_T.en.searchCard}</h2>
          <p class="nh-hint" style="margin-top:0;">${T.gsHint || PANEL_T.en.gsHint}</p>
          <div class="nh-field" id="nh-tog-gsearch"></div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.bsCard || PANEL_T.en.bsCard}</h2>
          <p class="nh-hint" style="margin-top:0;">${T.bsHint || PANEL_T.en.bsHint}</p>
          <div class="nh-field" id="nh-booksites"></div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.fsCard || PANEL_T.en.fsCard}</h2>
          <p class="nh-hint" style="margin-top:0;">${T.fsHint || PANEL_T.en.fsHint}</p>
          <div class="nh-field" id="nh-tog-familystats"></div>
          <div class="nh-divider"></div>
          <p class="nh-hint" style="margin-top:0;">${T.srHint || PANEL_T.en.srHint}</p>
          <div class="nh-field" id="nh-tog-sharereading"></div>
          <div class="nh-field" id="nh-tog-ratingname"></div>
          <div class="nh-field" id="nh-tog-shareratings"></div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.apCard || PANEL_T.en.apCard}</h2>
          <p class="nh-hint" style="margin-top:0;">${T.apHint || PANEL_T.en.apHint}</p>
          <div class="nh-field" id="nh-tog-autoplay"></div>
          <div class="nh-field" id="nh-tog-finishedtools"></div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.spTitle || PANEL_T.en.spTitle}</h2>
          <p class="nh-hint" style="margin-top:0;">${T.spHint || PANEL_T.en.spHint}</p>
          <div class="nh-field"><div id="nh-sel-startpage"></div></div>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.scmTitle || PANEL_T.en.scmTitle}</h2>
          <p class="nh-hint" style="margin-top:0;">${T.scmHint || PANEL_T.en.scmHint}</p>
          <div class="nh-field"><div id="nh-sel-seriescover"></div></div>
          <div class="nh-field" id="nh-tog-seriesprogress"></div>
          <p class="nh-hint">${T.sprHint || PANEL_T.en.sprHint}</p>
        </section>

        <section class="nh-card">
          <h2 class="nh-card-title">${T.mfTitle || PANEL_T.en.mfTitle}</h2>
          <p class="nh-hint" style="margin-top:0;">${T.mfHint || PANEL_T.en.mfHint}</p>
          <div class="nh-field" id="nh-tog-modernfilters"></div>
        </section>

      </div>
    `;

    configPage.appendChild(panel);

    renderThemeButtons(panel.querySelector('#nh-theme-buttons'));
    renderFontButtons(panel.querySelector('#nh-font-buttons'));

    panel.querySelector('#nh-tog-text').appendChild(createToggle(T.showAppName, 'showLogoText'));
    panel.querySelector('#nh-tog-colorize').appendChild(createToggle(T.colorizeLogo, 'colorizeLogo'));

    const railTogs = panel.querySelector('#nh-togs-rail');
    railTogs.appendChild(createToggle(T.hideSeries, 'hideRailSeries'));
    railTogs.appendChild(createToggle(T.hideCollections, 'hideRailCollections'));
    railTogs.appendChild(createToggle(T.hideAuthors, 'hideRailAuthors'));
    railTogs.appendChild(createToggle(T.hideNarrators, 'hideRailNarrators'));
    railTogs.appendChild(createToggle(T.hideStats, 'hideRailStats'));

    const homeTogs = panel.querySelector('#nh-togs-home');
    homeTogs.appendChild(createToggle(T.hideRecentlyAdded, 'hideHomeRecentlyAdded'));
    homeTogs.appendChild(createToggle(T.hideRecentSeries, 'hideHomeRecentSeries'));
    homeTogs.appendChild(createToggle(T.hideContinueSeries, 'hideHomeContinueSeries'));
    homeTogs.appendChild(createToggle(T.hideListenAgain, 'hideHomeListenAgain'));
    homeTogs.appendChild(createToggle(T.hideDiscover, 'hideHomeDiscover'));
    homeTogs.appendChild(createToggle(T.hideNewestAuthors, 'hideHomeNewAuthors'));
    // Inverted: showRateFinished is a show* key, but every row in this group means
    // "on = hidden", and each section listed under Home section order needs its
    // switch here rather than somewhere else in the panel.
    homeTogs.appendChild(createToggle(T.rfHide || PANEL_T.en.rfHide, 'showRateFinished', true));
    homeTogs.appendChild(createToggle(T.amfHide || PANEL_T.en.amfHide, 'showMarkFinished', true));
    nhHomeOrderUI(homeTogs);

    panel.querySelector('#nh-tog-ratings').appendChild(createToggle(T.ratingsToggle || PANEL_T.en.ratingsToggle, 'showRatings'));
    panel.querySelector('#nh-tog-ratings').appendChild(createToggle(T.cardStars || PANEL_T.en.cardStars, 'showCardRatings'));
    (function () {
      const sel = document.createElement('select');
      sel.className = 'nh-er-select';
      [['1', T.ssFull || PANEL_T.en.ssFull], ['0.5', T.ssHalf || PANEL_T.en.ssHalf], ['0.25', T.ssQuarter || PANEL_T.en.ssQuarter]].forEach((o) => {
        const opt = document.createElement('option');
        opt.value = o[0]; opt.textContent = o[1];
        sel.appendChild(opt);
      });
      sel.value = String(nhStarStep());
      sel.addEventListener('change', () => {
        nhSettings.starStep = parseFloat(sel.value);
        saveSettings();
        // Every star widget caches its own rendered value; drop those signatures
        // so the next tick repaints at the new precision instead of next reload.
        document.querySelectorAll('.nh-cr').forEach((b) => { delete b.dataset.nhCrSig; });
        nhLf.viewSig = '';
        applySettings();
      });
      panel.querySelector('#nh-sel-starstep').appendChild(sel);
      nhSelectify(sel);
    })();
    // Importing writes ratings, so it follows the same admin kill-switch as the
    // rest of the ratings feature rather than offering a button that cannot work.
    (function () {
      const host = panel.querySelector('#nh-imp-mount');
      if (!host) return;
      if (nhIsLocked('showRatings')) {
        host.innerHTML = '<p class="nh-hint" style="margin:0;">' + (T.lockedNote || PANEL_T.en.lockedNote) + '</p>';
        return;
      }
      nhImpMountCard(host);
    })();
    // Per-library ratings. The list is the libraries you can actually see, so a
    // server with one book library gets one row rather than an empty concept.
    (function () {
      const host = panel.querySelector('#nh-rating-libs');
      if (!host) return;
      nhGsLibs().then((libs) => {
        if (!libs || !libs.length) return;
        libs.forEach((lib) => host.appendChild(createLibRatingToggle(lib)));
      });
    })();
    panel.querySelector('#nh-tog-gsearch').appendChild(createToggle(T.gsToggle || PANEL_T.en.gsToggle, 'globalSearch'));
    panel.querySelector('#nh-tog-familystats').appendChild(createToggle(T.fsToggle || PANEL_T.en.fsToggle, 'familyStats'));
    panel.querySelector('#nh-tog-hcsync').appendChild(createToggle(T.hcToggle || PANEL_T.en.hcToggle, 'hcSync'));
    (function wireHardcover() {
      const status = panel.querySelector('#nh-hc-status');
      const miss = panel.querySelector('#nh-hc-miss');
      panel.querySelector('#nh-hc-all').addEventListener('click', () => nhHcBulk(false, status, miss));
      panel.querySelector('#nh-hc-diff').addEventListener('click', () => nhHcBulk(true, status, miss));
      panel.querySelector('#nh-hc-import').addEventListener('click', () => nhHcImportPull(status));
      // Explicit save (Pawel): typing already saves live, but a button that
      // answers with a tick makes "did it take?" a non-question. The key lands
      // in the caller's OWN prefs record - per user by construction.
      const saveBtn = panel.querySelector('#nh-hc-save');
      saveBtn.addEventListener('click', () => {
        nhSettings.hcKey = panel.querySelector('#nh-hc-key').value;
        saveSettings();
        const orig = saveBtn.textContent;
        saveBtn.textContent = '✓';
        setTimeout(() => { saveBtn.textContent = orig; }, 2000);
      });
    })();
    panel.querySelector('#nh-tog-sharereading').appendChild(createToggle(T.srToggle || PANEL_T.en.srToggle, 'shareReading'));
    panel.querySelector('#nh-tog-ratingname').appendChild(createToggle(T.rnToggle || PANEL_T.en.rnToggle, 'shareRatingName'));
    panel.querySelector('#nh-tog-shareratings').appendChild(createToggle(T.rsToggle || PANEL_T.en.rsToggle, 'shareRatings'));
    panel.querySelector('#nh-tog-autoplay').appendChild(createToggle(T.apToggle || PANEL_T.en.apToggle, 'autoplaySeries'));
    panel.querySelector('#nh-tog-finishedtools').appendChild(createToggle(T.fdToggle || PANEL_T.en.fdToggle, 'finishedTools'));
    // Live filter over the cards: with this many settings, being able to type
    // "rating" and see only what matters is the difference between usable and not.
    (function panelSearch() {
      const box = panel.querySelector('#nh-psearch');
      const clear = panel.querySelector('#nh-psearch-x');
      const none = panel.querySelector('#nh-psearch-none');
      if (!box) return;
      const run = function () {
        const q = box.value.trim().toLowerCase();
        // searching spans BOTH tabs; the tab split comes back when the box clears
        panel.classList.toggle('nh-searching', !!q);
        const cards = panel.querySelectorAll('.nh-grid > .nh-card');
        let shown = 0;
        cards.forEach(function (c) {
          const hit = !q || c.textContent.toLowerCase().indexOf(q) !== -1;
          c.style.display = hit ? '' : 'none';
          if (hit) shown++;
        });
        // section headings only make sense while everything is visible
        panel.querySelectorAll('.nh-grid > .nh-sect').forEach(function (h) { h.style.display = q ? 'none' : ''; });
        if (none) none.style.display = (q && !shown) ? 'block' : 'none';
        if (clear) clear.style.display = q ? 'block' : 'none';
      };
      box.addEventListener('input', run);
      box.addEventListener('search', run);
      if (clear) clear.addEventListener('click', function () { box.value = ''; run(); box.focus(); });
      run();
    })();
    // The redesigned-pages toggles moved to the ADMIN tab (Pawel: only the
    // admin decides which redesigned pages the server runs) - see the
    // Administration block below.
    (function buildBookSites() {
      const host = panel.querySelector('#nh-booksites');
      if (!host) return;
      const lang = getUserLanguage().split('-')[0].toLowerCase();
      const chosen = nhBookSitesSelected().slice();
      const groups = [
        [T.bsGlobal || PANEL_T.en.bsGlobal, NH_BOOK_SITES.filter((x) => x.langs.indexOf('*') !== -1)],
        [T.bsLocal || PANEL_T.en.bsLocal, NH_BOOK_SITES.filter((x) => x.langs.indexOf(lang) !== -1)],
        [T.bsOther || PANEL_T.en.bsOther, NH_BOOK_SITES.filter((x) => x.langs.indexOf('*') === -1 && x.langs.indexOf(lang) === -1)],
      ];
      groups.forEach(function (g) {
        if (!g[1].length) return;
        const h = document.createElement('div');
        h.className = 'nh-bs-group';
        h.textContent = g[0];
        host.appendChild(h);
        const wrap = document.createElement('div');
        wrap.className = 'nh-bs-list';
        g[1].forEach(function (site) {
          const lab = document.createElement('label');
          lab.className = 'nh-bs-check';
          const cb = document.createElement('input');
          cb.type = 'checkbox';
          cb.checked = chosen.indexOf(site.id) !== -1;
          cb.addEventListener('change', function () {
            const cur = nhBookSitesSelected().slice();
            const at = cur.indexOf(site.id);
            if (cb.checked && at === -1) cur.push(site.id);
            if (!cb.checked && at !== -1) cur.splice(at, 1);
            nhSettings.bookSites = cur;
            saveSettings();
            document.querySelectorAll('.nh-bs-row').forEach(function (r) { r.dataset.sig = ''; });
          });
          const txt = document.createElement('span');
          txt.textContent = site.name;
          lab.appendChild(cb); lab.appendChild(txt);
          wrap.appendChild(lab);
        });
        host.appendChild(wrap);
      });
    })();
    (function () {
      const sel = document.createElement('select');
      sel.className = 'nh-er-select';
      const addOpt = (v, txt) => { const o = document.createElement('option'); o.value = v; o.textContent = txt; sel.appendChild(o); };
      addOpt('', T.spDefault || PANEL_T.en.spDefault);
      nhGsLibs().then((libs) => {
        if (!libs) return;
        const views = [
          ['home', T.spViewHome || PANEL_T.en.spViewHome],
          ['bookshelf', T.spViewLibrary || PANEL_T.en.spViewLibrary],
          ['series', T.spViewSeries || PANEL_T.en.spViewSeries],
          ['collections', T.spViewCollections || PANEL_T.en.spViewCollections],
          ['authors', T.spViewAuthors || PANEL_T.en.spViewAuthors]
        ];
        libs.forEach((lib) => views.forEach((v) => addOpt(v[0] + ':' + lib.id, lib.name + ' · ' + v[1])));
        if (nhSettings.startPage && sel.querySelector('option[value="' + nhSettings.startPage + '"]')) sel.value = nhSettings.startPage;
      });
      sel.addEventListener('change', () => { nhSettings.startPage = sel.value; saveSettings(); });
      panel.querySelector('#nh-sel-startpage').appendChild(sel);
      nhSelectify(sel);
    })();
    (function () {
      const sel = document.createElement('select');
      sel.className = 'nh-er-select';
      [['deck', T.scmDeck || PANEL_T.en.scmDeck], ['first', T.scmFirst || PANEL_T.en.scmFirst], ['grid', T.scmGrid || PANEL_T.en.scmGrid], ['off', T.scmOff || PANEL_T.en.scmOff]].forEach((o) => {
        const opt = document.createElement('option');
        opt.value = o[0]; opt.textContent = o[1];
        sel.appendChild(opt);
      });
      sel.value = ['deck', 'first', 'grid', 'off'].indexOf(nhSettings.seriesCoverMode) !== -1 ? nhSettings.seriesCoverMode : 'grid';
      sel.addEventListener('change', () => { nhSettings.seriesCoverMode = sel.value; saveSettings(); });
      panel.querySelector('#nh-sel-seriescover').appendChild(sel);
      nhSelectify(sel);
    })();

    const bindInput = (id, key) => {
      const el = panel.querySelector(id);
      if (nhSettings[key] !== undefined) el.value = nhSettings[key];
      el.addEventListener('input', (e) => {
        nhSettings[key] = el.type === 'number' ? parseInt(e.target.value, 10) : e.target.value;
        saveSettings(); applySettings();
      });
      return el;
    };

    bindInput('#nh-in-name', 'appName');
    bindInput('#nh-hc-key', 'hcKey');
    // The logo URL is NOT bound live like the others (#28): a value only lands
    // in the settings (and syncs to the user's other browsers) once it is empty
    // or a usable image source. Anything else just marks the field.
    const logoInput = panel.querySelector('#nh-in-logo');
    if (nhSettings.logoUrl !== undefined) logoInput.value = nhSettings.logoUrl;
    logoInput.addEventListener('input', () => {
      const raw = logoInput.value.trim();
      const ok = raw === '' ? '' : nhLogoOk(raw);
      logoInput.classList.toggle('nh-in-bad', raw !== '' && !ok);
      if (raw !== '' && !ok) return;
      nhSettings.logoUrl = ok;
      saveSettings(); applySettings();
    });

    logoInput.addEventListener('change', (e) => {
      if (e.target.value.trim() === '') {
        nhSettings.colorizeLogo = false;
        saveSettings(); applySettings();
        const colorizeTogBtn = panel.querySelector('#nh-tog-colorize button');
        if (colorizeTogBtn) colorizeTogBtn.className = 'border rounded-full flex items-center bg-primary border-black-100 justify-start';
      }
    });

    // Logo upload (admin only): browse a device image, PUT it into the /data/nh volume,
    // then point the logo at the same-origin /_nh/logo.<ext> so it renders with NO internet.
    const logoUploadRow = panel.querySelector('#nh-logo-upload-row');
    if (logoUploadRow && isUserAdmin()) {
      logoUploadRow.style.display = 'flex';
      const logoFile = panel.querySelector('#nh-in-logo-file');
      const logoStatus = panel.querySelector('#nh-logo-upload-status');
      panel.querySelector('#nh-logo-upload-btn').addEventListener('click', () => logoFile.click());
      logoFile.addEventListener('change', () => {
        const file = logoFile.files && logoFile.files[0];
        if (!file) return;
        const byMime = { 'image/png': 'png', 'image/svg+xml': 'svg', 'image/jpeg': 'jpg', 'image/webp': 'webp', 'image/gif': 'gif', 'image/avif': 'avif', 'image/x-icon': 'ico', 'image/vnd.microsoft.icon': 'ico' };
        let ext = byMime[file.type] || (file.name.split('.').pop() || '').toLowerCase();
        if (ext === 'jpeg') ext = 'jpg';
        if (!/^(png|svg|jpg|webp|gif|avif|ico)$/.test(ext)) { logoStatus.textContent = T.logoBadType || PANEL_T.en.logoBadType; logoFile.value = ''; return; }
        if (file.size > 4 * 1024 * 1024) { logoStatus.textContent = T.logoTooBig || PANEL_T.en.logoTooBig; logoFile.value = ''; return; }
        logoStatus.textContent = '…';
        let token = '';
        try { const st = window.$nuxt.$store; token = st.getters['user/getToken'] || (st.state.user.user && (st.state.user.user.accessToken || st.state.user.user.token)) || ''; } catch (e) {}
        fetch('/_nh/data/logo.' + ext, { method: 'PUT', headers: { 'Authorization': 'Bearer ' + token }, body: file })
          .then((r) => {
            if (!r.ok) { logoStatus.textContent = T.srvErr || PANEL_T.en.srvErr; return; }
            // Cache-bust in the stored URL so the new logo shows at once and re-uploads
            // refresh too (applySettings re-asserts this exact src each tick).
            nhSettings.logoUrl = '/_nh/logo.' + ext + '?v=' + Date.now();
            logoInput.value = nhSettings.logoUrl;
            saveSettings(); applySettings();
            logoStatus.textContent = T.logoUploaded || PANEL_T.en.logoUploaded;
            setTimeout(() => { logoStatus.textContent = ''; }, 2500);
          })
          .catch(() => { logoStatus.textContent = T.srvErr || PANEL_T.en.srvErr; });
        logoFile.value = '';
      });
    }

    bindInput('#nh-in-carousel', 'carouselTiming');
    bindInput('#nh-in-series-count', 'recentSeriesCount');
    panel.querySelector('#nh-tog-customseries').appendChild(createToggle(T.customSeries, 'showCustomRecentSeries'));
    panel.querySelector('#nh-tog-seriescards').appendChild(createToggle(T.seriesCards || PANEL_T.en.seriesCards, 'customSeriesCards'));
    panel.querySelector('#nh-tog-seriesprogress').appendChild(createToggle(T.sprLabel || PANEL_T.en.sprLabel, 'seriesProgressBadge'));
    panel.querySelector('#nh-tog-modernfilters').appendChild(createToggle(T.mfLabel || PANEL_T.en.mfLabel, 'modernFilters'));
    panel.querySelector('#nh-tog-herocarousel').appendChild(createToggle(T.heroCarousel || PANEL_T.en.heroCarousel, 'showHeroCarousel'));
    panel.querySelector('#nh-cr-label').textContent = T.crMode || PANEL_T.en.crMode;
    (function () {
      const sel = document.createElement('select');
      sel.className = 'nh-er-select';
      [['combine', T.crCombine || PANEL_T.en.crCombine], ['separate', T.crSeparate || PANEL_T.en.crSeparate], ['hide', T.crHide || PANEL_T.en.crHide]].forEach(function (o) {
        const opt = document.createElement('option');
        opt.value = o[0]; opt.textContent = o[1];
        sel.appendChild(opt);
      });
      sel.value = nhSettings.continueReadingMode || 'combine';
      sel.addEventListener('change', function () {
        nhSettings.continueReadingMode = sel.value;
        saveSettings(); applySettings();
      });
      panel.querySelector('#nh-sel-crmode').appendChild(sel);
      nhSelectify(sel);
    })();

    const customPicker = panel.querySelector('#nh-in-color');
    customPicker.addEventListener('input', (e) => {
      nhSettings.accentColor = e.target.value;
      saveSettings(); applySettings(); updateSwatchesUI();
    });

    panel.querySelectorAll('.nh-color-preset').forEach(btn => {
      btn.addEventListener('click', (e) => {
        nhSettings.accentColor = e.target.dataset.color;
        customPicker.value = nhSettings.accentColor;
        saveSettings(); applySettings(); updateSwatchesUI();
      });
    });

    updateSwatchesUI();

    if (hideBranding) {
      const brandingCard = panel.querySelector('#nh-in-name');
      if (brandingCard) { const card = brandingCard.closest('.nh-card'); if (card) card.style.display = 'none'; }
    }

    // B6: labelled sections - personal settings above, administration below.
    const gridEl = panel.querySelector('.nh-grid') || panel;
    const mkSect = (txt) => {
      const h = document.createElement('div');
      h.className = 'nh-sect';
      h.textContent = txt;
      return h;
    };
    gridEl.insertBefore(mkSect(T.sectUser || PANEL_T.en.sectUser), gridEl.firstChild);


    // Server defaults - admins, config page only (not the quick modal). Saves the
    // current settings to the proxy (/data/nh volume) via an admin-authenticated PUT;
    // nginx injects the file into every page before first paint.
    if (isConfigPage && isUserAdmin()) {
      const adminSect = mkSect(T.sectAdmin || PANEL_T.en.sectAdmin);
      adminSect.classList.add('nh-tab-admin');
      gridEl.appendChild(adminSect);

      // Redesigned pages (Pawel: admin-only): these switch whole page redesigns
      // on or off for EVERYONE. State lives in the server config; users' own
      // stored copies are overridden (nhForcePages).
      const pagesSec = document.createElement('section');
      pagesSec.className = 'nh-card nh-tab-admin';
      pagesSec.innerHTML = '<h2 class="nh-card-title">' + (T.pgCard || PANEL_T.en.pgCard) + '</h2>' +
        '<p class="nh-hint" style="margin-top:0;">' + (T.pgHint || PANEL_T.en.pgHint) + '</p>' +
        '<div class="nh-field" id="nh-tog-pages"></div><span class="nh-rt-status" id="nh-pages-status"></span>';
      gridEl.appendChild(pagesSec);
      (function pagesAdmin() {
        const host = pagesSec.querySelector('#nh-tog-pages');
        const status = pagesSec.querySelector('#nh-pages-status');
        const btnCls = (on) => 'border rounded-full flex items-center ' + (on ? 'bg-success border-success justify-end' : 'bg-primary border-black-100 justify-start');
        [['pgNarrators', 'narratorsCards'], ['pgCollections', 'collectionsPages'], ['pgUsers', 'usersCards'],
          ['pgStats', 'statsRanking'], ['pgPhoto', 'accountPhoto'], ['pgCinematic', 'cinematicBg'],
          ['pgTransitions', 'pageTransitions']].forEach(function (o) {
          const key = o[1];
          const row = document.createElement('div');
          row.className = 'flex items-center py-2 cursor-pointer group';
          row.innerHTML = '<div><button type="button" class="' + btnCls(uiServerSettings[key] !== false) + '" style="width: 40px; transition: all 0.2s;">' +
            '<span class="rounded-full border border-black-50 shadow-sm bg-white" style="width: 20px; height: 20px;"></span></button></div>' +
            '<p class="pl-4 text-gray-200 group-hover:text-white transition-colors text-sm"></p>';
          row.querySelector('p').textContent = T[o[0]] || PANEL_T.en[o[0]];
          row.addEventListener('click', function () {
            const next = uiServerSettings[key] === false;
            if (next) delete uiServerSettings[key]; else uiServerSettings[key] = false;
            row.querySelector('button').className = btnCls(next);
            nhSettings[key] = next;
            applySettings();
            putServerConfig(Object.assign({}, uiServerSettings), status, '✓');
          });
          host.appendChild(row);
        });
      })();

      const sec = document.createElement('section');
      sec.className = 'nh-card nh-tab-admin';
      sec.innerHTML = '<h2 class="nh-card-title">' + (T.srvTitle || PANEL_T.en.srvTitle) + '</h2>' +
        '<p class="nh-hint" style="margin-bottom:14px;">' + (T.srvHint || PANEL_T.en.srvHint) + '</p>' +
        '<div style="display:flex; gap:12px; flex-wrap:wrap;">' +
        '<button type="button" id="nh-srv-save" style="background: var(--nh-amber, #e0c27a); color: #14110d; border: none; border-radius: 10px; padding: 10px 22px; font-weight: 600; cursor: pointer;">' + (T.srvSave || PANEL_T.en.srvSave) + '</button>' +
        '<button type="button" id="nh-srv-clear" style="background: transparent; color: var(--nh-muted-2, #a99f8f); border: 1px solid var(--nh-hairline, rgba(255,255,255,0.15)); border-radius: 10px; padding: 10px 22px; font-weight: 600; cursor: pointer;">' + (T.srvClear || PANEL_T.en.srvClear) + '</button>' +
        '</div>';
      (panel.querySelector('.nh-grid') || panel).appendChild(sec);

      const flashBtn = function (btn, msg) {
        const orig = btn.dataset.nhOrig || btn.textContent;
        btn.dataset.nhOrig = orig;
        btn.textContent = msg;
        setTimeout(function () { btn.textContent = orig; }, 2500);
      };
      const nhAbsToken = function () {
        try {
          const st = window.$nuxt.$store;
          return st.getters['user/getToken'] || (st.state.user.user && (st.state.user.user.accessToken || st.state.user.user.token)) || '';
        } catch (e) { return ''; }
      };
      const putServerConfig = function (obj, btn, okText) {
        const clean = {};
        Object.keys(obj).forEach(function (k) {
          const v = obj[k];
          clean[k] = (typeof v === 'string') ? v.replace(/[<>]/g, '') : v;
        });
        return fetch('/_nh/data/server-config.json', {
          method: 'PUT',
          headers: { 'Authorization': 'Bearer ' + nhAbsToken(), 'Content-Type': 'application/json' },
          body: JSON.stringify(clean)
        }).then(function (r) {
          flashBtn(btn, r.ok ? okText : (T.srvErr || PANEL_T.en.srvErr));
        }).catch(function () { flashBtn(btn, T.srvErr || PANEL_T.en.srvErr); });
      };
      // What to include (Pawel): the save used to be all-or-nothing, so making
      // one thing the server default dragged every other current setting along
      // with it. Four areas, all on by default; an UNCHECKED area keeps whatever
      // the server config already holds for those keys rather than overwriting it.
      const NH_SRV_GROUPS = {
        look: ['appName', 'showLogoText', 'colorizeLogo', 'logoUrl', 'accentColor', 'baseTheme', 'mainFont', 'fontScale'],
        home: ['carouselTiming', 'showHeroCarousel', 'showCustomRecentSeries', 'recentSeriesCount', 'customSeriesCards',
          'continueReadingMode', 'showRateFinished', 'showMarkFinished', 'homeOrder',
          'hideHomeRecentlyAdded', 'hideHomeRecentSeries', 'hideHomeContinueSeries', 'hideHomeListenAgain', 'hideHomeDiscover', 'hideHomeNewAuthors',
          'hideRailSeries', 'hideRailCollections', 'hideRailAuthors', 'hideRailNarrators', 'hideRailStats'],
        pages: ['narratorsCards', 'collectionsPages', 'usersCards', 'statsRanking', 'accountPhoto', 'cinematicBg', 'pageTransitions', 'seriesCoverMode', 'seriesProgressBadge', 'modernFilters'],
        // rest = every key not claimed above, so new settings are covered
        // automatically instead of silently never becoming saveable.
      };
      const grpRow = document.createElement('div');
      grpRow.id = 'nh-srv-groups';
      const grpLbl = document.createElement('p');
      grpLbl.className = 'nh-hint';
      grpLbl.id = 'nh-srv-pick-lbl'; // a LABEL, not an explanation - the id keeps it out of the "?" popover sweep
      grpLbl.style.margin = '10px 0 4px';
      grpLbl.textContent = T.srvPick || PANEL_T.en.srvPick;
      const grpChecks = {};
      const grpNames = [['look', T.branding || PANEL_T.en.branding], ['home', T.homeCar || PANEL_T.en.homeCar],
        ['pages', T.pgCard || PANEL_T.en.pgCard], ['rest', T.srvGRest || PANEL_T.en.srvGRest]];
      grpNames.forEach(function (g) {
        const lab = document.createElement('label');
        lab.className = 'nh-srv-grp';
        const cb = document.createElement('input');
        cb.type = 'checkbox'; cb.checked = true;
        grpChecks[g[0]] = cb;
        lab.appendChild(cb);
        lab.appendChild(document.createTextNode(' ' + g[1]));
        grpRow.appendChild(lab);
      });
      const btnRow = sec.querySelector('#nh-srv-save').parentElement;
      btnRow.parentElement.insertBefore(grpLbl, btnRow);
      btnRow.parentElement.insertBefore(grpRow, btnRow);

      sec.querySelector('#nh-srv-save').addEventListener('click', function (e) {
        const btn = e.target;
        const claimed = [].concat(NH_SRV_GROUPS.look, NH_SRV_GROUPS.home, NH_SRV_GROUPS.pages);
        const pickKey = function (k) {
          if (NH_SRV_GROUPS.look.indexOf(k) >= 0) return grpChecks.look.checked;
          if (NH_SRV_GROUPS.home.indexOf(k) >= 0) return grpChecks.home.checked;
          if (NH_SRV_GROUPS.pages.indexOf(k) >= 0) return grpChecks.pages.checked;
          return grpChecks.rest.checked;
        };
        // Merge over what is already saved, so an unchecked area is left alone
        // instead of vanishing from the server defaults.
        fetch('/_nh/server-config.json', { headers: { Authorization: 'Bearer ' + nhAbsToken() } })
          .then(function (r) { return r.ok ? r.json() : {}; })
          .catch(function () { return {}; })
          .then(function (existing) {
            const merged = Object.assign({}, existing && typeof existing === 'object' ? existing : {});
            Object.keys(nhSettings).forEach(function (k) { if (pickKey(k)) merged[k] = nhSettings[k]; });
            // keep any active kill-switches alongside the saved defaults
            Object.keys(NH_LOCKS).forEach(function (lk) { if (uiServerSettings[lk]) merged[lk] = true; });
            // runtime-only: never persist the build stamp (see uiServerSettings)
            delete merged.themeVersion;
            // consent stays personal: never save the sharing switches as defaults
            delete merged.familyStats;
            delete merged.shareReading;
            delete merged.shareRatingName;
            delete merged.shareRatings;
            // and never anyone's Hardcover token or its switch (#26)
            delete merged.hcSync;
            delete merged.hcKey;
            // the redesigned-pages switches are ADMIN state, not this admin's
            // personal look - a defaults save must not overwrite or drop them
            NH_PAGES_KEYS.forEach(function (k) {
              if (Object.prototype.hasOwnProperty.call(uiServerSettings, k)) merged[k] = uiServerSettings[k];
              else delete merged[k];
            });
            putServerConfig(merged, btn, T.srvSaved || PANEL_T.en.srvSaved);
          });
      });
      sec.querySelector('#nh-srv-clear').addEventListener('click', function (e) {
        const btn = e.target;
        const clean = {};
        fetch('/_nh/data/server-config.json', {
          method: 'PUT',
          headers: { 'Authorization': 'Bearer ' + nhAbsToken(), 'Content-Type': 'application/json' },
          body: JSON.stringify(clean)
        }).then(function (r) {
          if (!r.ok) { flashBtn(btn, T.srvErr || PANEL_T.en.srvErr); return; }
          // Also drop this browser's personal overrides so the admin lands on the
          // true stock look - server clear alone can't touch localStorage.
          try { localStorage.removeItem(nhSettingsKey(nhSetUid)); } catch (err) {}
          nhPrefsPush({ _v: 2 }); // and clear this admin's server-side copy too
          flashBtn(btn, T.srvCleared || PANEL_T.en.srvCleared);
          setTimeout(function () { window.location.reload(); }, 700);
        }).catch(function () { flashBtn(btn, T.srvErr || PANEL_T.en.srvErr); });
      });

      // Feature kill-switches (B6): red toggles, applied to THIS session at
      // once and to everyone else on their next reload.
      // (The reported-problems queue moved to the ACCOUNT page in round 12 -
      // the appbar badge's button leads there; see nhAccountReports.)
      const lockSec = document.createElement('section');
      lockSec.className = 'nh-card nh-tab-admin';
      lockSec.innerHTML = '<h2 class="nh-card-title">' + (T.lockTitle || PANEL_T.en.lockTitle) + '</h2>' +
        '<p class="nh-hint" style="margin-bottom:8px;">' + (T.lockHint || PANEL_T.en.lockHint) + '</p>' +
        '<div id="nh-locks"></div><span class="nh-rt-status" id="nh-locks-status"></span>';
      const locksHost = lockSec.querySelector('#nh-locks');
      const locksStatus = lockSec.querySelector('#nh-locks-status');
      const lockBtnCls = (on) => 'border rounded-full flex items-center ' + (on ? 'bg-error border-error justify-end' : 'bg-primary border-black-100 justify-start');
      Object.keys(NH_LOCKS).forEach(function (lk) {
        const row = document.createElement('div');
        row.className = 'flex items-center py-2 cursor-pointer group';
        row.innerHTML = '<div><button type="button" class="' + lockBtnCls(!!uiServerSettings[lk]) + '" style="width: 40px; transition: all 0.2s;">' +
          '<span class="rounded-full border border-black-50 shadow-sm bg-white" style="width: 20px; height: 20px;"></span></button></div>' +
          '<p class="pl-4 text-gray-200 group-hover:text-white transition-colors text-sm"></p>';
        row.querySelector('p').textContent = T[lk] || PANEL_T.en[lk];
        row.addEventListener('click', function () {
          if (uiServerSettings[lk]) delete uiServerSettings[lk];
          else uiServerSettings[lk] = true;
          row.querySelector('button').className = lockBtnCls(!!uiServerSettings[lk]);
          // apply to this session immediately
          if (uiServerSettings[lk]) nhSettings[NH_LOCKS[lk]] = false;
          applySettings();
          // persist: existing server config + lock flags
          const cfg = Object.assign({}, uiServerSettings);
          putServerConfig(cfg, locksStatus, '✓');
        });
        locksHost.appendChild(row);
      });
      gridEl.appendChild(lockSec);

      // Social switches (#25): the three server-wide flags plus per-user
      // exclusion. State lives on the proxy (social.json + admin tombstones in
      // both shared stores), so this card only renders what the server answers.
      const socSec = document.createElement('section');
      socSec.className = 'nh-card nh-tab-admin';
      socSec.innerHTML = '<h2 class="nh-card-title">' + (T.socTitle || PANEL_T.en.socTitle) + '</h2>' +
        '<p class="nh-hint" style="margin-bottom:8px;">' + (T.socHint || PANEL_T.en.socHint) + '</p>' +
        '<div id="nh-soc-flags"></div>' +
        '<div class="nh-divider"></div>' +
        '<div class="nh-subhead">' + (T.socUsers || PANEL_T.en.socUsers) + '</div>' +
        '<p class="nh-hint" style="margin-top:0;">' + (T.socUsersHint || PANEL_T.en.socUsersHint) + '</p>' +
        '<div id="nh-soc-users"></div><span class="nh-rt-status" id="nh-soc-status"></span>';
      gridEl.appendChild(socSec);
      (function socialCard() {
        const flagsHost = socSec.querySelector('#nh-soc-flags');
        const usersHost = socSec.querySelector('#nh-soc-users');
        const status = socSec.querySelector('#nh-soc-status');
        const tgCls = (on) => 'border rounded-full flex items-center ' + (on ? 'bg-success border-success justify-end' : 'bg-primary border-black-100 justify-start');
        const tgRow = (label, on, dim) =>
          '<div><button type="button" class="' + tgCls(on) + '" style="width: 40px; transition: all 0.2s;' + (dim ? ' opacity: 0.4;' : '') + '">' +
          '<span class="rounded-full border border-black-50 shadow-sm bg-white" style="width: 20px; height: 20px;"></span></button></div>' +
          '<p class="pl-4 text-gray-200 group-hover:text-white transition-colors text-sm"></p>';
        let state = null;   // { eff, cfg, tombs } from the server
        let roster = null;  // active users, for the exclusion list
        const post = (body) => {
          fetch('/_nh/api/social-admin', { method: 'POST', headers: { Authorization: 'Bearer ' + nhAbsToken(), 'Content-Type': 'application/json' }, credentials: 'include', body: JSON.stringify(body) })
            .then((r) => (r.ok ? r.json() : null))
            .then((j) => {
              if (!j) { status.textContent = T.srvErr || PANEL_T.en.srvErr; return; }
              status.textContent = '';
              state = j;
              nhSoc.eff = j.eff; // this admin's session follows immediately
              nhLf.viewSig = '';
              render();
            })
            .catch(() => { status.textContent = T.srvErr || PANEL_T.en.srvErr; });
        };
        const FLAGS = [['time', 'socTime'], ['content', 'socContent'], ['whoReading', 'socWho'], ['names', 'socNames']];
        const render = () => {
          if (!state || !state.eff) return;
          flagsHost.textContent = '';
          FLAGS.forEach((f) => {
            const row = document.createElement('div');
            row.className = 'flex items-center py-2 cursor-pointer group';
            row.innerHTML = tgRow(T[f[1]] || PANEL_T.en[f[1]], !!state.eff[f[0]]);
            row.querySelector('p').textContent = T[f[1]] || PANEL_T.en[f[1]];
            row.addEventListener('click', () => { const set = {}; set[f[0]] = !state.eff[f[0]]; post({ set: set }); });
            flagsHost.appendChild(row);
          });
          usersHost.textContent = '';
          // one column always: two switch cells per row never fit two columns
          // side by side (Pawel tried, twice)
          // Two per-user toggles (Pawel): ranking time and books, mirroring the
          // user's own pair. A user's own opt-out shows dimmed and stays
          // theirs; an admin exclusion is flippable. Old union tombs kept for
          // everything else, per-kind lists drive these switches.
          const tombs = state.tombs || {};
          const kinds = [
            { kind: 'time', icon: 'leaderboard', label: T.socTime || PANEL_T.en.socTime, excl: tombs.exclTime || tombs.excluded || [], opt: tombs.optTime || tombs.optedOut || [] },
            { kind: 'books', icon: 'menu_book', label: T.srToggle || PANEL_T.en.srToggle, excl: tombs.exclBooks || tombs.excluded || [], opt: tombs.optBooks || [] },
            { kind: 'names', icon: 'badge', label: T.socNames || PANEL_T.en.socNames, excl: tombs.exclNames || [], opt: tombs.optNames || [] },
          ];
          (roster || []).forEach((u) => {
            const row = document.createElement('div');
            row.className = 'nh-soc-row flex items-center py-2 group';
            const nm = document.createElement('p');
            nm.className = 'text-gray-200 text-sm';
            nm.style.flex = '1';
            nm.textContent = u.username;
            row.appendChild(nm);
            kinds.forEach((k) => {
              const excluded = k.excl.indexOf(u.id) >= 0;
              const opted = !excluded && k.opt.indexOf(u.id) >= 0;
              const cell = document.createElement('span');
              cell.className = 'nh-soc-tg';
              cell.title = k.label + (opted ? ' · ' + (T.socOpted || PANEL_T.en.socOpted) : '');
              const ico = document.createElement('span');
              ico.className = 'material-symbols nh-soc-ico';
              ico.textContent = k.icon;
              cell.appendChild(ico);
              const btn = document.createElement('span');
              btn.innerHTML = tgRow('', !excluded && !opted, opted);
              const b = btn.querySelector('button');
              if (!opted) {
                b.style.cursor = 'pointer';
                b.addEventListener('click', () => {
                  if (excluded) post({ include: u.id, kind: k.kind });
                  else post({ exclude: u.id, user: u.username, kind: k.kind });
                });
              }
              cell.appendChild(btn.firstChild);
              row.appendChild(cell);
            });
            usersHost.appendChild(row);
          });
        };
        Promise.all([
          fetch('/_nh/api/social-admin', { headers: { Authorization: 'Bearer ' + nhAbsToken() }, credentials: 'include' }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
          fetch('/api/users', { headers: { Authorization: 'Bearer ' + nhAbsToken() } }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
        ]).then((res) => {
          state = res[0];
          roster = ((res[1] && (res[1].users || res[1])) || []).filter((u) => u && u.isActive !== false);
          if (!state) { status.textContent = T.srvErr || PANEL_T.en.srvErr; return; }
          render();
        });
      })();

      // Sessions card (Pawel): the last 10 listening sessions across the whole
      // server, with anything active right now pinned on top. Refreshes itself
      // every 30 seconds for as long as the card is on screen.
      // ---- Goodreads ratings card (#27): switch, helper address, discovery ----
      // Book pages fill the map in by themselves, so the controls are the
      // server-wide switch and WHERE the abs-tract helper lives. The address is
      // found by itself when possible (ABS's own metadata-provider list, the
      // compose service name, this host) and saved server-side; nginx reads it
      // per request, no restart.
      const cmSec = document.createElement('section');
      cmSec.className = 'nh-card nh-tab-admin';
      cmSec.innerHTML = '<h2 class="nh-card-title">' + (T.cmTitle || PANEL_T.en.cmTitle) + '</h2>' +
        '<p class="nh-hint" style="margin-top:0;">' + (T.cmHint || PANEL_T.en.cmHint) + '</p>' +
        '<div id="nh-cm-toggle"></div>' +
        '<div class="nh-field"><label class="nh-label">' + (T.cmAddr || PANEL_T.en.cmAddr) + '</label>' +
        '<div style="display:flex; gap:10px; align-items:center; flex-wrap:wrap;">' +
        '<input type="text" id="nh-cm-url" placeholder="http://abs-tract:5555/goodreads" autocomplete="off" data-lpignore="true" data-1p-ignore data-bwignore data-form-type="other" spellcheck="false" style="flex:1; min-width:200px;">' +
        '<button type="button" id="nh-cm-save" class="nh-upload-btn">' + (T.sdSave || PANEL_T.en.sdSave) + '</button>' +
        '<button type="button" id="nh-cm-detect" class="nh-upload-btn">' + (T.cmDetect || PANEL_T.en.cmDetect) + '</button>' +
        '</div></div>' +
        '<p class="nh-hint" id="nh-cm-source" style="margin-top:2px;"></p>' +
        '<div id="nh-cm-scan" style="display:none;"><div class="nh-divider"></div>' +
        '<div class="nh-subhead">' + (T.cmScanLibs || PANEL_T.en.cmScanLibs) + '</div>' +
        '<div id="nh-cm-libs" class="nh-cm-libs"></div>' +
        '<div id="nh-cm-scan-all" class="nh-cm-lib nh-cm-all flex items-center py-2 cursor-pointer group" data-on="0"></div>' +
        '<div id="nh-cm-scan-prog" class="nh-cm-prog" style="display:none;"><div class="nh-cm-meta"><span class="nh-cm-pct"></span><span class="nh-cm-cnt"></span></div>' +
        '<div class="nh-cm-bar"><div class="nh-cm-fill"></div></div></div>' +
        '<div id="nh-cm-scan-title" class="nh-cm-scantitle" style="display:none;"></div>' +
        '<div class="nh-cm-actions">' +
        '<button type="button" id="nh-cm-scan-btn" class="nh-upload-btn nh-cm-go">' + (T.cmScan || PANEL_T.en.cmScan) + '</button>' +
        '<button type="button" id="nh-cm-scan-stop" class="nh-upload-btn" style="display:none;">' + (T.cmStop || PANEL_T.en.cmStop) + '</button>' +
        '<span class="nh-rt-status" id="nh-cm-scan-status"></span></div>' +
        '<p class="nh-hint nh-cm-done" id="nh-cm-scan-done"></p>' +
        '<div id="nh-cm-missed" class="nh-cm-missed" style="display:none;"></div></div>' +
        '<div id="nh-cm-setup" style="display:none;"><p class="nh-hint" id="nh-cm-setup-hint" style="margin-top:6px;"></p>' +
        '<pre class="nh-cm-code">services:\n  abs-tract:\n    image: ghcr.io/rodzalendo/abs-tract-ratings:latest\n    restart: unless-stopped</pre></div>' +
        '<span class="nh-rt-status" id="nh-cm-status"></span>';
      gridEl.appendChild(cmSec);
      (function communityCard() {
        const status = cmSec.querySelector('#nh-cm-status');
        const srcLine = cmSec.querySelector('#nh-cm-source');
        const setup = cmSec.querySelector('#nh-cm-setup');
        const urlIn = cmSec.querySelector('#nh-cm-url');
        urlIn.value = nhGoodreadsUrl || '';
        cmSec.querySelector('#nh-cm-setup-hint').textContent = T.cmSetup || PANEL_T.en.cmSetup;
        // Whole-library scan (#34): the HELPER runs it. This card uploads the
        // book list once, then only watches (and can stop) the run; another
        // tab, a phone, or the card reopened later all show the same run.
        const scanBox = cmSec.querySelector('#nh-cm-scan');
        const libsHost = cmSec.querySelector('#nh-cm-libs');
        const scanBtn = cmSec.querySelector('#nh-cm-scan-btn');
        const scanStop = cmSec.querySelector('#nh-cm-scan-stop');
        const scanStatus = cmSec.querySelector('#nh-cm-scan-status');
        const scanProg = cmSec.querySelector('#nh-cm-scan-prog');
        const scanTitle = cmSec.querySelector('#nh-cm-scan-title');
        const scanDone = cmSec.querySelector('#nh-cm-scan-done');
        const scanAll = cmSec.querySelector('#nh-cm-scan-all');
        // The same switch rows as every other card (click anywhere on the row).
        const tglCls = (on) => 'border rounded-full flex items-center ' + (on ? 'bg-success border-success justify-end' : 'bg-primary border-black-100 justify-start');
        // The choice is remembered server-side (hobesman: came back to the card
        // mid-scan and every library was ticked again): the switched-OFF
        // libraries and the "everything again" switch live in the server
        // config, so any device shows the same, and a new library starts on.
        const libsOff = () => Array.isArray(uiServerSettings.cmScanLibsOff) ? uiServerSettings.cmScanLibsOff : [];
        const remember = () => {
          uiServerSettings.cmScanLibsOff = Array.from(libsHost.querySelectorAll('.nh-cm-lib[data-on="0"]')).map((r) => r.dataset.id);
          if (scanAll.dataset.on === '1') uiServerSettings.cmScanAll = true; else delete uiServerSettings.cmScanAll;
          putServerConfig(Object.assign({}, uiServerSettings), status, '✓');
        };
        const tglRow = (row, label, on) => {
          row.dataset.on = on ? '1' : '0';
          row.innerHTML = '<div><button type="button" class="' + tglCls(on) + '" style="width: 40px; transition: all 0.2s;">' +
            '<span class="rounded-full border border-black-50 shadow-sm bg-white" style="width: 20px; height: 20px;"></span></button></div>' +
            '<p class="pl-4 text-gray-200 group-hover:text-white transition-colors text-sm"></p>';
          row.querySelector('p').textContent = label;
          row.addEventListener('click', () => { const next = row.dataset.on !== '1'; row.dataset.on = next ? '1' : '0'; row.querySelector('button').className = tglCls(next); remember(); });
          return row;
        };
        tglRow(scanAll, T.cmScanAll || PANEL_T.en.cmScanAll, uiServerSettings.cmScanAll === true);
        fetch('/api/libraries', { headers: { Authorization: 'Bearer ' + nhSrToken() } }).then((r) => (r.ok ? r.json() : null)).then((j) => {
          ((j && j.libraries) || []).filter((l) => l.mediaType === 'book').forEach((l) => {
            const row = document.createElement('div');
            row.className = 'nh-cm-lib flex items-center py-1 cursor-pointer group';
            row.dataset.id = l.id;
            libsHost.appendChild(tglRow(row, l.name, libsOff().indexOf(l.id) < 0));
          });
        }).catch(() => {});
        // Digits sit in fixed-width slots (tabular numerals + a reserved width
        // from the total) so the line does not jump as the numbers change.
        const setStatus = (done, total, title) => {
          const pct = total ? Math.floor(done * 100 / total) : 0;
          scanProg.querySelector('.nh-cm-pct').textContent = total ? pct + '%' : '…';
          scanProg.querySelector('.nh-cm-cnt').textContent = total ? done + ' / ' + total : '';
          scanProg.querySelector('.nh-cm-fill').style.width = (total ? Math.max(0.5, done * 100 / total).toFixed(1) : 0) + '%';
          scanTitle.textContent = title || ' ';
        };
        const errsText = (n) => (T.cmScanErrs || PANEL_T.en.cmScanErrs).replace('{e}', n);
        // The summary line, built piece by piece so the "N not found" piece
        // can be a button that opens the list of those books (with links).
        const missedBox = cmSec.querySelector('#nh-cm-missed');
        const showMissed = () => {
          if (missedBox.style.display !== 'none') { missedBox.style.display = 'none'; return; }
          missedBox.textContent = '…'; missedBox.style.display = '';
          nhCmScan.missed().then((items) => {
            missedBox.textContent = '';
            items.forEach((b) => {
              const a = document.createElement('a');
              a.className = 'nh-cm-mrow'; a.href = '/item/' + b.id;
              a.innerHTML = '<span class="nh-cm-mt"></span><span class="nh-cm-ma"></span>';
              a.querySelector('.nh-cm-mt').textContent = b.title || b.id;
              a.querySelector('.nh-cm-ma').textContent = b.author || '';
              a.addEventListener('click', (ev) => { ev.preventDefault(); try { window.$nuxt.$router.push('/item/' + b.id); } catch (e) { location.href = a.href; } });
              missedBox.appendChild(a);
            });
            if (!items.length) missedBox.style.display = 'none';
          });
        };
        const summaryText = (st) => {
          let tpl = T.cmScanDone || PANEL_T.en.cmScanDone;
          if (nhCmScan.skipped == null) tpl = tpl.replace(/\s*·[^·]*\{s\}[^·]*$/, '');
          let s = tpl.replace('{m}', st.matched || 0).replace('{x}', st.missed || 0).replace('{s}', nhCmScan.skipped || 0);
          if (st.errors) s += ' · ' + errsText(st.errors);
          if (st.stopped) s += ' · ' + (T.cmStopped || PANEL_T.en.cmStopped);
          return s;
        };
        const summary = (st) => {
          scanDone.textContent = '';
          missedBox.style.display = 'none';
          let tpl = T.cmScanDone || PANEL_T.en.cmScanDone;
          // "already had one" is only known by the tab that started the run
          if (nhCmScan.skipped == null) tpl = tpl.replace(/\s*·[^·]*\{s\}[^·]*$/, '');
          const segs = tpl.split(' · ');
          segs.forEach((seg, i) => {
            if (i) scanDone.appendChild(document.createTextNode(' · '));
            const txt = seg.replace('{m}', st.matched || 0).replace('{x}', st.missed || 0).replace('{s}', nhCmScan.skipped || 0);
            if (seg.indexOf('{x}') >= 0 && st.missed > 0) {
              const b = document.createElement('button'); b.type = 'button'; b.className = 'nh-cm-link'; b.textContent = txt;
              b.addEventListener('click', showMissed);
              scanDone.appendChild(b);
            } else scanDone.appendChild(document.createTextNode(txt));
          });
          if (st.errors) scanDone.appendChild(document.createTextNode(' · ' + errsText(st.errors)));
          if (st.stopped) scanDone.appendChild(document.createTextNode(' · ' + (T.cmStopped || PANEL_T.en.cmStopped)));
          return scanDone.textContent;
        };
        const showRunning = (on) => {
          scanBtn.style.display = on ? 'none' : ''; scanStop.style.display = on ? '' : 'none';
          scanProg.style.display = on ? '' : 'none'; scanTitle.style.display = on ? '' : 'none';
        };
        let pollT = null, lastSeq = null, holdDone = false, wasOnPage = false;
        const poll = () => {
          clearTimeout(pollT); pollT = null;
          // the status answer can come back before the panel is on the page:
          // try again in a moment; once it has been on the page and is gone, stop
          if (!document.body.contains(cmSec)) { if (!wasOnPage) pollT = setTimeout(poll, 1000); return; }
          wasOnPage = true;
          nhCmScan.status().then((st) => {
            if (!document.body.contains(cmSec)) return;
            if (!st) { pollT = setTimeout(poll, 10000); return; }
            const changed = st.seq !== lastSeq; lastSeq = st.seq;
            if (st.running) {
              holdDone = false;
              showRunning(true); setStatus(st.done, st.total, st.current);
              scanStop.disabled = !!st.stopped; scanStop.textContent = st.stopped ? '…' : (T.cmStop || PANEL_T.en.cmStop);
              scanStatus.textContent = st.errors ? errsText(st.errors) : '';
              if (changed) nhCmScan.sync();
              pollT = setTimeout(poll, 3000);
            } else {
              showRunning(false); scanStatus.textContent = ''; scanStop.disabled = false; scanStop.textContent = T.cmStop || PANEL_T.en.cmStop;
              if (st.total && !holdDone && scanDone.textContent !== summaryText(st)) summary(st);
              if (changed) nhCmScan.sync().then(() => { nhCm.at = 0; nhCm.items = null; nhCmItems(); });
              pollT = setTimeout(poll, 15000); // a run started elsewhere shows up here too
            }
          });
        };
        scanBtn.addEventListener('click', () => {
          const libs = Array.from(libsHost.querySelectorAll('.nh-cm-lib[data-on="1"]')).map((r) => r.dataset.id);
          if (!libs.length || scanBtn.disabled) return;
          scanDone.textContent = ''; scanStatus.textContent = '…'; scanBtn.disabled = true; holdDone = true;
          nhCmScan.start(libs, scanAll.dataset.on === '1').then((r) => {
            scanBtn.disabled = false; scanStatus.textContent = '';
            holdDone = false;
            if (r && r.busy) scanStatus.textContent = T.cmScanBusy || PANEL_T.en.cmScanBusy;
            else if (r && r.nothing) { summary({ matched: 0, missed: 0 }); holdDone = true; } // keep this, not the previous run's line
            lastSeq = null; poll();
          }).catch(() => { scanBtn.disabled = false; holdDone = false; scanStatus.textContent = T.cmGrDown || PANEL_T.en.cmGrDown; });
        });
        scanStop.addEventListener('click', () => { scanStop.disabled = true; scanStop.textContent = '…'; nhCmScan.stop().then(poll); });
        // The switch: an admin-owned server flag, absent = on (same shape as the
        // Redesigned pages toggles, so "save server defaults" keeps it).
        const key = 'communityRatings';
        const btnCls = (on) => 'border rounded-full flex items-center ' + (on ? 'bg-success border-success justify-end' : 'bg-primary border-black-100 justify-start');
        const row = document.createElement('div');
        row.className = 'flex items-center py-2 cursor-pointer group';
        row.innerHTML = '<div><button type="button" class="' + btnCls(uiServerSettings[key] !== false) + '" style="width: 40px; transition: all 0.2s;">' +
          '<span class="rounded-full border border-black-50 shadow-sm bg-white" style="width: 20px; height: 20px;"></span></button></div>' +
          '<p class="pl-4 text-gray-200 group-hover:text-white transition-colors text-sm"></p>';
        row.querySelector('p').textContent = T.cmTitle || PANEL_T.en.cmTitle;
        row.addEventListener('click', function () {
          const next = uiServerSettings[key] === false;
          if (next) delete uiServerSettings[key]; else uiServerSettings[key] = false;
          row.querySelector('button').className = btnCls(next);
          nhSettings[key] = next;
          applySettings();
          putServerConfig(Object.assign({}, uiServerSettings), status, '✓');
        });
        cmSec.querySelector('#nh-cm-toggle').appendChild(row);
        // Is the helper there? One question through the relay (which reads the
        // saved address). Without an answer the card shows the setup lines.
        let checkT = null;
        const check = (again) => {
          const n = Number(again) || 0;
          clearTimeout(checkT); checkT = null;
          srcLine.textContent = '…';
          // A tab opened a moment ago may not have its login token yet: every
          // question would come back 401 and the card would call a live
          // helper dead. Wait for the token first.
          if (!nhSrToken() && n < 6) return new Promise((res) => setTimeout(res, 1000)).then(() => check(n + 1));
          const H = { headers: { Authorization: 'Bearer ' + nhSrToken() }, credentials: 'include' };
          const ask = (p) => fetch('/_nh/gr/' + p + '?query=Dune&author=Frank%20Herbert', H);
          // Our helper answers its scan status without touching Goodreads, and
          // that is the "is it there" question: a Goodreads test lookup fails
          // while a scan hogs the shared key and made a live helper look dead.
          // A stock abs-tract (404 there) still gets the Dune question.
          return fetch('/_nh/gr/scan', H).then((r) => (r.ok ? r.json().then(() => { nhCm.gr = true; return 'on'; }) : null)).catch(() => null)
            .then((st) => st || ask('ratings').then((r) => (r.status === 404 ? ask('search') : r))
              .then((r) => { if (r.status === 404) { nhCm.gr = false; return 'off'; } if (!r.ok) return 'down'; return r.json().then((j) => { const m = (j && j.matches) || []; nhCm.gr = true; return m.some((x) => typeof x.goodreadsRatingsCount === 'number') ? 'on' : 'nonum'; }); })
              .catch(() => 'down'))
            .then((st) => {
              const K = { off: 'cmGrOff', down: 'cmGrDown', on: 'cmGrOn', nonum: 'cmGrNoNum' }[st];
              // A hiccup (helper just restarted, busy with a scan, Goodreads
              // slow) is not a verdict: three more tries, spaced out.
              if (st === 'down' && n < 3) return new Promise((res) => setTimeout(res, 3000 + n * 2000)).then(() => check(n + 1));
              srcLine.textContent = (T[K] || PANEL_T.en[K]) + (st === 'on' && urlIn.value ? ' · ' + urlIn.value : '');
              // setup lines only when there is nothing to talk to (or the wrong
              // build); "set up but not answering" keeps asking by itself
              setup.style.display = (st === 'off' || st === 'nonum') ? '' : 'none';
              scanBox.style.display = st === 'on' ? '' : 'none';
              if (st === 'on') poll(); // a run already going shows at once
              if (st === 'down') checkT = setTimeout(() => { if (document.body.contains(cmSec)) check(0); }, 20000);
              return st;
            });
        };
        const saveUrl = (u) => {
          u = String(u || '').trim().replace(/\/+$/, '');
          urlIn.value = u;
          nhCm.gr = null;
          const cfg = Object.assign({}, uiServerSettings);
          if (u) cfg.goodreadsUrl = u;
          // Only ask the relay once the new address is on disk; one retry for
          // a transient hiccup, the helper takes a moment on first contact.
          Promise.resolve(putServerConfig(cfg, status, '✓')).catch(() => {}).then(() => check());
        };
        cmSec.querySelector('#nh-cm-save').addEventListener('click', () => saveUrl(urlIn.value));
        urlIn.addEventListener('keydown', (e) => { if (e.key === 'Enter') saveUrl(urlIn.value); });
        cmSec.querySelector('#nh-cm-detect').addEventListener('click', () => {
          srcLine.textContent = '…';
          nhCmDiscover().then((hit) => { if (hit) saveUrl(hit); else check(); });
        });
        // First open with nothing saved: look around once by ourselves.
        check().then((st) => { if (st !== 'on' && !urlIn.value) nhCmDiscover().then((hit) => { if (hit) saveUrl(hit); }); });
      })();

      const sessSec = document.createElement('section');
      sessSec.className = 'nh-card nh-tab-admin';
      sessSec.innerHTML = '<h2 class="nh-card-title">' + (T.seshTitle || PANEL_T.en.seshTitle) + '</h2>' +
        '<div id="nh-sesh-now"></div><div id="nh-sesh-list"></div><span class="nh-rt-status" id="nh-sesh-status"></span>';
      gridEl.appendChild(sessSec);
      (function sessionsCard() {
        const nowHost = sessSec.querySelector('#nh-sesh-now');
        const listHost = sessSec.querySelector('#nh-sesh-list');
        const status = sessSec.querySelector('#nh-sesh-status');
        const NH_SESH_ACTIVE_MS = 2 * 60 * 1000;
        const mkRow = (s2, names, tk, active) => {
          const a = document.createElement('a');
          a.className = 'nh-ys-row nh-sesh-row';
          a.href = getBaseNH() + '/item/' + s2.libraryItemId;
          const th = document.createElement('span'); th.className = 'nh-ys-thumb';
          th.style.backgroundImage = 'url("/api/items/' + s2.libraryItemId + '/cover?width=80' + (tk ? '&token=' + encodeURIComponent(tk) : '') + '")';
          a.appendChild(th);
          const mid = document.createElement('span'); mid.className = 'nh-ys-mid';
          const nm = document.createElement('span'); nm.className = 'nh-ys-name'; nm.textContent = s2.displayTitle || '?';
          const sub = document.createElement('span'); sub.className = 'nh-sesh-sub';
          const pct = (s2.duration > 0 && s2.currentTime >= 0) ? Math.min(100, Math.round(s2.currentTime / s2.duration * 100)) + '%' : '';
          sub.textContent = [names[s2.userId] || '?', active ? '' : nhRdAgo(s2.updatedAt), pct].filter(Boolean).join(' · ');
          if (active) {
            const dot = document.createElement('span'); dot.className = 'nh-sesh-dot';
            sub.insertBefore(dot, sub.firstChild);
          }
          mid.appendChild(nm); mid.appendChild(sub); a.appendChild(mid);
          return a;
        };
        const load = () => {
          const tk = nhAbsToken();
          if (!tk) return;
          Promise.all([
            // sort params are REQUIRED: the bare endpoint returns OLDEST first
            fetch('/api/sessions?itemsPerPage=10&page=0&sort=updatedAt&desc=1', { headers: { Authorization: 'Bearer ' + tk } }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
            fetch('/api/users', { headers: { Authorization: 'Bearer ' + tk } }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
          ]).then((res) => {
            const sessions = (res[0] && res[0].sessions) || [];
            const names = {};
            (((res[1] && (res[1].users || res[1])) || [])).forEach((u) => { names[u.id] = u.username; });
            if (!sessions.length) { status.textContent = ' '; return; }
            status.textContent = '';
            const cutoff = Date.now() - NH_SESH_ACTIVE_MS;
            const active = sessions.filter((s2) => (s2.updatedAt || 0) >= cutoff);
            const rest = sessions.filter((s2) => (s2.updatedAt || 0) < cutoff);
            nowHost.textContent = '';
            if (active.length) {
              const h = document.createElement('div'); h.className = 'nh-subhead';
              h.textContent = T.seshNow || PANEL_T.en.seshNow;
              nowHost.appendChild(h);
              active.forEach((s2) => nowHost.appendChild(mkRow(s2, names, tk, true)));
            }
            listHost.textContent = '';
            rest.forEach((s2) => listHost.appendChild(mkRow(s2, names, tk, false)));
          });
        };
        load();
        const iv = setInterval(() => {
          if (!sessSec.isConnected) { clearInterval(iv); return; }
          load();
        }, 30 * 1000);
      })();

    }

    // Tabs instead of one long scroll (Pawel, three times: first the admin
    // cards, then "group it better", then "adjust the popup menu to match") -
    // so they build for BOTH surfaces, the config page and the gear modal.
    // Labels reuse keys every language already carries. Cards are matched by a
    // distinctive child; anything unmatched (a future card) falls into the
    // Features tab rather than becoming unreachable. The Administration tab
    // only exists where admin cards do. The panel search suspends the split so
    // a query matches across every tab.
    (function buildTabs() {
      const NH_TAB_GROUPS = [
        ['look', ['#nh-in-name', '#nh-theme-buttons']],
        ['home', ['#nh-in-carousel', '#nh-togs-rail', '#nh-sel-startpage']],
        ['book', ['#nh-tog-ratings', '#nh-booksites', '#nh-sel-seriescover', '#nh-hc-key']],
        ['feat', ['#nh-tog-gsearch', '#nh-tog-familystats', '#nh-tog-autoplay', '#nh-tog-modernfilters']],
      ];
      NH_TAB_GROUPS.forEach((g) => {
        g[1].forEach((sel) => {
          const el = panel.querySelector(sel);
          const card = el && el.closest('.nh-card');
          if (card) card.dataset.nhGroup = g[0];
        });
      });
      panel.querySelectorAll('.nh-grid > .nh-card').forEach((card) => {
        if (!card.dataset.nhGroup) card.dataset.nhGroup = card.classList.contains('nh-tab-admin') ? 'admin' : 'feat';
      });
      const head = panel.querySelector('.nh-head');
      if (!head) return;
      // Short labels (Pawel): Style / Main Page / Books / Features / Administration
      const defs = [['look', T.tabStyle || PANEL_T.en.tabStyle],
        ['home', T.tabMain || PANEL_T.en.tabMain],
        ['book', T.tabBooks || PANEL_T.en.tabBooks],
        ['feat', T.tabFeat || PANEL_T.en.tabFeat]];
      if (panel.querySelector('.nh-card.nh-tab-admin')) defs.push(['admin', T.sectAdmin || PANEL_T.en.sectAdmin]);
      const tabs = document.createElement('div');
      tabs.id = 'nh-ptabs';
      defs.forEach((t) => {
        const b = document.createElement('button');
        b.type = 'button';
        b.className = 'nh-ptab' + (t[0] === 'look' ? ' nh-on' : '');
        b.textContent = t[1];
        b.addEventListener('click', () => {
          panel.dataset.nhTab = t[0];
          tabs.querySelectorAll('.nh-ptab').forEach((x) => x.classList.toggle('nh-on', x === b));
        });
        tabs.appendChild(b);
      });
      const searchWrap = head.querySelector('.nh-psearch-wrap');
      head.insertBefore(tabs, searchWrap || null);
      panel.dataset.nhTab = 'look';
    })();

    // Deterministic columns (Pawel keeps asking for specific placement, and
    // the multicol balancer keeps deciding otherwise): explicit column breaks.
    // Books tab: col1 = ratings + Hardcover, col2 = the rest, break at the
    // Book links card. Administration: col1 = pages + defaults + switches,
    // col2 = Social, col3 = Sessions.
    (function pinColumns() {
      const mark = (sel) => {
        const el = panel.querySelector(sel);
        const card = el && el.closest('.nh-card');
        if (card) card.dataset.nhBreak = '1';
      };
      mark('#nh-booksites');
      mark('#nh-soc-users');
      mark('#nh-sesh-list');
    })();

    // Every static hint collapses into a "?" popover (Pawel: the panel read
    // like a manual). Status lines and dynamic hosts keep their class but have
    // ids or live inside known containers, so they are skipped.
    (function hintPopovers() {
      panel.querySelectorAll('.nh-card p.nh-hint').forEach((h) => {
        if (h.id || !h.textContent.trim()) return;
        if (h.closest('#nh-hc-miss') || h.closest('#nh-srv-groups') || h.closest('#nh-logo-upload-row')) return;
        const card = h.closest('.nh-card');
        // Anchor priority (Pawel: the ? belongs ON the toggle it explains, not
        // piled on the card title): a label/subhead right above; else the
        // single toggle row in the sibling field (hints usually sit right
        // before or after the field they describe); else the card title.
        const singleRow = (el) => {
          if (!el || el.tagName !== 'DIV') return null;
          const ps = el.querySelectorAll('p.pl-4');
          return ps.length === 1 ? ps[0] : null;
        };
        let anchor = null;
        const prev = h.previousElementSibling;
        if (prev && (prev.classList.contains('nh-label') || prev.classList.contains('nh-subhead'))) anchor = prev;
        if (!anchor) anchor = singleRow(prev);
        if (!anchor) anchor = singleRow(h.nextElementSibling);
        if (!anchor) anchor = card.querySelector('.nh-card-title');
        if (!anchor) return;
        const qm = document.createElement('button');
        qm.type = 'button';
        qm.className = 'nh-qm';
        qm.textContent = '?';
        const pop = document.createElement('span');
        pop.className = 'nh-qm-pop';
        pop.textContent = h.textContent;
        qm.appendChild(pop);
        qm.addEventListener('click', (e) => {
          e.preventDefault();
          e.stopPropagation();
          const open = qm.classList.contains('nh-qm-open');
          panel.querySelectorAll('.nh-qm-open').forEach((x) => x.classList.remove('nh-qm-open'));
          if (!open) qm.classList.add('nh-qm-open');
        });
        anchor.appendChild(qm);
        h.remove();
      });
      document.addEventListener('click', (e) => {
        if (!e.target.closest || !e.target.closest('.nh-qm')) {
          panel.querySelectorAll('.nh-qm-open').forEach((x) => x.classList.remove('nh-qm-open'));
        }
      });
    })();

    return panel;
  }

  // ==========================================
  // 7b. GEAR BUTTON (ALL USERS) + SETTINGS MODAL
  // ==========================================
  function isUserAdmin() {
    try { return !!(window.$nuxt && window.$nuxt.$store && window.$nuxt.$store.getters['user/getIsAdminOrUp']); } catch (e) { return false; }
  }

  // The drawer mirrors the rail, and the rail's contents change with the library
  // (a podcast library has Latest/Add/Queue where a book library has Series/
  // Authors/Narrators). Signature = what is actually on screen, so a library
  // switch rebuilds the links instead of leaving the previous library's menu.
  function nhDrawerSig(rail) {
    return Array.from(rail.querySelectorAll('a')).map((a) => {
      const span = a.querySelector('span.material-symbols, span.abs-icons');
      const label = a.querySelector('p, .truncate');
      return (a.getAttribute('href') || '') + '|' + (span ? (span.dataset.nhGlyph || span.textContent || span.className) : '') +
        '|' + (label ? label.textContent.trim() : '') + '|' + (a.classList.contains('nuxt-link-exact-active') ? 1 : 0);
    }).join('~');
  }

  function nhFillDrawer(drawer, rail) {
    drawer.innerHTML = '';
    rail.querySelectorAll('a').forEach((a) => {
      const link = document.createElement('a');
      link.href = a.href;
      const span = a.querySelector('span.material-symbols, span.abs-icons');
      const label = a.querySelector('p, .truncate');
      const icon = document.createElement('span');
      icon.className = span ? span.className : 'material-symbols';
      if (span) {
        // Carry the remapped glyph across as an attribute too, so the drawer
        // paints it the same CSS way the rail does.
        if (span.dataset.nhGlyph) icon.dataset.nhGlyph = span.dataset.nhGlyph;
        icon.textContent = span.textContent;
      }
      link.appendChild(icon);
      const lbl = document.createElement('span');
      lbl.className = 'nh-drawer-label';
      lbl.textContent = label ? label.textContent.trim() : '';
      link.appendChild(lbl);
      if (a.classList.contains('nuxt-link-exact-active')) link.classList.add('nh-drawer-active');
      link.addEventListener('click', (e) => {
        e.preventDefault();
        document.body.classList.remove('nh-menu-open');
        // $router.push() needs a base-relative path; a.href is a full absolute URL,
        // so the push silently no-ops. a.href already routes correctly.
        const href = link.getAttribute('href');
        if (href) window.location.assign(href);
      });
      drawer.appendChild(link);
    });
    drawer.dataset.sig = nhDrawerSig(rail);
  }

  function nhSyncMobileDrawer() {
    const drawer = document.getElementById('nh-mobile-drawer');
    const rail = document.getElementById('siderail-buttons-container');
    if (!drawer || !rail || !rail.querySelector('a')) return;
    const sig = nhDrawerSig(rail);
    if (drawer.dataset.sig === sig) return;
    nhFillDrawer(drawer, rail);
  }

  function injectMobileMenuButton() {
    if (document.getElementById('nh-menu-btn')) return;
    const rail = document.getElementById('siderail-buttons-container');
    const logo = document.querySelector('#appbar a[href$="/"]');
    if (!rail || !logo || !logo.parentNode) return;

    // Build our own drawer from the rail links (ABS hides the rail's parent on mobile)
    const drawer = document.createElement('div');
    drawer.id = 'nh-mobile-drawer';
    nhFillDrawer(drawer, rail);

    const backdrop = document.createElement('div');
    backdrop.id = 'nh-menu-backdrop';

    const btn = document.createElement('a');
    btn.id = 'nh-menu-btn';
    btn.href = 'javascript:void(0)';
    btn.style.cssText = 'cursor:pointer; display:none; align-items:center; justify-content:center; padding:8px; border-radius:10px; margin-right:8px; color:var(--nh-text-2, #d8cfc2);';
    btn.innerHTML = `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M3 6h18M3 12h18M3 18h18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>`;

    const closeMenu = () => document.body.classList.remove('nh-menu-open');
    btn.addEventListener('click', (e) => { e.preventDefault(); document.body.classList.toggle('nh-menu-open'); });
    backdrop.addEventListener('click', closeMenu);
    // Link clicks are bound in nhFillDrawer, which also owns every rebuild.

    logo.parentNode.insertBefore(btn, logo);
    document.body.appendChild(backdrop);
    document.body.appendChild(drawer);

    const mq = window.matchMedia('(max-width: 640px)');
    const applyVisibility = () => { btn.style.display = mq.matches ? 'flex' : 'none'; if (!mq.matches) closeMenu(); };
    applyVisibility();
    mq.addEventListener('change', applyVisibility);
  }

  function hideMobileUploadButton() {
    const links = document.querySelectorAll('#appbar a, #appbar button');
    links.forEach(el => {
      if (el.dataset.nhUploadChecked) return;
      const signals = [
        el.getAttribute('aria-label') || '',
        el.getAttribute('title') || '',
        el.getAttribute('href') || '',
        el.innerHTML || ''
      ].join(' ');
      if (/upload/i.test(signals)) {
        el.classList.add('nh-hide-upload-mobile');
        el.dataset.nhUploadChecked = '1';
      }
    });
  }

  function injectGearButton() {
    if (document.getElementById('nh-gear-btn')) return;
    const statsBtn = document.querySelector('#appbar a[href*="/stats"]');
    if (!statsBtn) return;

    const gear = document.createElement('a');
    gear.id = 'nh-gear-btn';
    gear.href = 'javascript:void(0)';
    gear.title = panelT().gearLabel;
    gear.style.cssText = 'cursor:pointer; display:flex; align-items:center; justify-content:center; padding:8px; border-radius:10px; margin-right:4px;';
    gear.innerHTML = `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" style="display:block;"><rect x="2" y="3" width="10" height="14" rx="2" stroke="currentColor" stroke-width="1.8" fill="none"/><rect x="7" y="7" width="10" height="14" rx="2" stroke="currentColor" stroke-width="1.8" fill="rgba(255,255,255,0.15)"/><circle cx="12" cy="14" r="2" fill="currentColor"/></svg>`;

    gear.addEventListener('click', (e) => {
      e.preventDefault();
      openSettingsModal();
    });

    statsBtn.parentNode.insertBefore(gear, statsBtn);
  }

  function openSettingsModal() {
    let modal = document.getElementById('nh-settings-modal');
    if (modal) { modal.remove(); }

    injectPanelStyles();
    modal = document.createElement('div');
    modal.id = 'nh-settings-modal';
    modal.innerHTML = `
      <div class="nh-modal-backdrop"></div>
      <div class="nh-modal-container">
        <div class="nh-modal-close-row"><button class="nh-modal-close">&times;</button></div>
        <div class="nh-modal-body"></div>
      </div>
    `;
    document.body.appendChild(modal);

    const body = modal.querySelector('.nh-modal-body');
    const oldPanel = document.getElementById('nh-settings-panel');
    if (oldPanel) oldPanel.remove();

    try {
      createCustomizationsPanel(body, !isUserAdmin());
    } catch (e) {
      try { console.warn('[NanoHive] settings panel build failed:', e); } catch (e2) {}
    }

    const close = () => modal.remove();
    modal.querySelector('.nh-modal-backdrop').addEventListener('click', close);
    modal.querySelector('.nh-modal-close').addEventListener('click', close);
    document.addEventListener('keydown', function esc(e) { if (e.key === 'Escape') { close(); document.removeEventListener('keydown', esc); } });
  }

  function injectSettingsPanel() {
    ensureConfigNavItem();

    const path = window.location.pathname;
    const isConfigRoot = path === '/audiobookshelf/config' || path === '/audiobookshelf/config/';

    if (!isConfigRoot) {
      const ep = document.getElementById('nh-settings-panel');
      if (ep && !ep.closest('#nh-settings-modal')) ep.remove();
      setNavActive(false);
      return;
    }

    const configPage = document.querySelector('.configContent');
    if (!configPage) return;

    const wantCustom = window.location.hash === NH_HASH;
    let panel = document.querySelector('#nh-settings-panel:not(#nh-settings-modal #nh-settings-panel)');

    if (wantCustom) {
      if (!panel) {
        try { panel = createCustomizationsPanel(configPage, false, true); }
        catch (e) { try { console.warn('[NanoHive] settings panel build failed:', e); } catch (e2) {} }
      }
      if (!panel) return;
      panel.style.display = '';
      // ABS pins .configContent at 900px - two columns forever. Only while the
      // panel is the page's content may it stretch (class off again below, so
      // the native Settings page keeps its stock measure).
      configPage.classList.add('nh-panel-wide');
      Array.from(configPage.children).forEach(ch => {
        if (ch !== panel && ch.style.display !== 'none') {
          ch.dataset.nhHidden = ch.style.display || '';
          ch.style.display = 'none';
        }
      });
      setNavActive(true);
    } else {
      if (panel) panel.style.display = 'none';
      configPage.classList.remove('nh-panel-wide');
      Array.from(configPage.children).forEach(ch => {
        if (ch !== panel && ch.dataset.nhHidden !== undefined) {
          ch.style.display = ch.dataset.nhHidden;
          delete ch.dataset.nhHidden;
        }
      });
      setNavActive(false);
    }
  }

  // ==========================================
  // 8. JS SWEEPER FOR VUE FOOTER ARTIFACTS
  // ==========================================
  function sweepFooters() {
     document.querySelectorAll('div.border-t.bg-bg').forEach(el => {
         const txt = el.textContent || '';
         if (txt.includes('v2.') || txt.includes('docker')) {
             el.style.display = 'none';
             el.innerHTML = '';
             el.remove();
         }
     });
  }

  // ==========================================
  // 9. SWAP STATS LINK
  // ==========================================
  function swapStatsLinks() {
     const railStats = document.querySelector('#siderail-buttons-container a[href*="/stats"]');
     const topStats = document.querySelector('#appbar a[href*="/stats"]');

     if (railStats && railStats.getAttribute('href').includes('/library/')) {
         const libHref = railStats.getAttribute('href');
         railStats.setAttribute('href', '/audiobookshelf/config/stats');
         if (topStats) topStats.setAttribute('href', libHref);
     }
  }

  // ==========================================
  // 2. TRANSLATION DICTIONARY
  // ==========================================
  const getUserLanguage = () => {
    try {
      if (window.$nuxt && window.$nuxt.$i18n && window.$nuxt.$i18n.locale) {
        return window.$nuxt.$i18n.locale;
      }
      const vuexStr = localStorage.getItem('vuex');
      if (vuexStr) {
         const vuex = JSON.parse(vuexStr);
         if (vuex.user?.settings?.language) return vuex.user.settings.language;
      }
    } catch(e) {}
    return document.documentElement.lang || navigator.language || 'en';
  };

  const getTranslations = (langCode) => {
    const baseLang = langCode.split('-')[0].toLowerCase();
    const dictionary = {
      en: {"morning": "GOOD MORNING", "afternoon": "GOOD AFTERNOON", "evening": "GOOD EVENING", "welcome": "Welcome back", "pickup": "Your books are waiting", "by": "by", "continue": "Continue", "left": "left", "narratedBy": "Narrated by", "unknown": "Unknown Title", "fallbackDesc": "Pick up right where you left off.", "heroPause": "Pause", "heroPlay": "Play"},
      pl: {"morning": "DZIEŃ DOBRY", "afternoon": "DOBREGO POPOŁUDNIA", "evening": "DOBRY WIECZÓR", "welcome": "Witaj ponownie", "pickup": "Twoje książki czekają", "by": "", "continue": "Kontynuuj", "left": "pozostało", "narratedBy": "Czyta", "unknown": "Nieznany tytuł", "fallbackDesc": "Ciąg dalszy czeka.", "heroPause": "Wstrzymaj", "heroPlay": "Odtwórz"},
      de: {"morning": "GUTEN MORGEN", "afternoon": "GUTEN TAG", "evening": "GUTEN ABEND", "welcome": "Willkommen zurück", "pickup": "Deine Bücher warten", "by": "von", "continue": "Weiter", "left": "verbleibend", "narratedBy": "Gelesen von", "unknown": "Unbekannter Titel", "fallbackDesc": "Mach genau dort weiter, wo du aufgehört hast.", "heroPause": "Pause", "heroPlay": "Wiedergabe"},
      fr: {"morning": "BONJOUR", "afternoon": "BON APRÈS-MIDI", "evening": "BONSOIR", "welcome": "Bon retour", "pickup": "Vos livres vous attendent", "by": "de", "continue": "Continuer", "left": "restant", "narratedBy": "Lu par", "unknown": "Titre inconnu", "fallbackDesc": "Reprenez exactement là où vous vous étiez arrêté.", "heroPause": "Pause", "heroPlay": "Lecture"},
      es: {"morning": "BUENOS DÍAS", "afternoon": "BUENAS TARDES", "evening": "BUENAS NOCHES", "welcome": "Bienvenido de nuevo", "pickup": "Tus libros te esperan", "by": "de", "continue": "Continuar", "left": "restante", "narratedBy": "Narrado por", "unknown": "Título desconocido", "fallbackDesc": "Continúa justo donde lo dejaste.", "heroPause": "Pausa", "heroPlay": "Reproducir"},
      it: {"morning": "BUONGIORNO", "afternoon": "BUON POMERIGGIO", "evening": "BUONASERA", "welcome": "Bentornato", "pickup": "I tuoi libri ti aspettano", "by": "di", "continue": "Continua", "left": "rimanente", "narratedBy": "Narrato da", "unknown": "Titolo sconosciuto", "fallbackDesc": "Riprendi esattamente da dove avevi interrotto.", "heroPause": "Pausa", "heroPlay": "Riproduci"},
      pt: {"morning": "BOM DIA", "afternoon": "BOA TARDE", "evening": "BOA NOITE", "welcome": "Bem-vindo de volta", "pickup": "Seus livros te esperam", "by": "de", "continue": "Continuar", "left": "restante", "narratedBy": "Narrado por", "unknown": "Título desconhecido", "fallbackDesc": "Continue exatamente de onde parou.", "heroPause": "Pausar", "heroPlay": "Reproduzir"},
      nl: {"morning": "GOEDEMORGEN", "afternoon": "GOEDEMIDDAG", "evening": "GOEDENAVOND", "welcome": "Welkom terug", "pickup": "Je boeken wachten op je", "by": "door", "continue": "Doorgaan", "left": "resterend", "narratedBy": "Verteld door", "unknown": "Onbekende titel", "fallbackDesc": "Ga verder waar je gebleven was.", "heroPause": "Pauzeren", "heroPlay": "Afspelen"},
      cs: {"morning": "DOBRÉ RÁNO", "afternoon": "DOBRÉ ODPOLEDNE", "evening": "DOBRÝ VEČER", "welcome": "Vítejte zpět", "pickup": "Vaše knihy čekají", "by": "od", "continue": "Pokračovat", "left": "zbývá", "narratedBy": "Čte", "unknown": "Neznámý název", "fallbackDesc": "Pokračujte přesně tam, kde jste skončili.", "heroPause": "Pozastavit", "heroPlay": "Přehrát"},
      sk: {"morning": "DOBRÉ RÁNO", "afternoon": "DOBRÉ POPOLUDNIE", "evening": "DOBRÝ VEČER", "welcome": "Vitajte späť", "pickup": "Vaše knihy čakajú", "by": "od", "continue": "Pokračovať", "left": "zostáva", "narratedBy": "Číta", "unknown": "Neznámy názov", "fallbackDesc": "Pokračujte presne tam, kde ste skončili.", "heroPause": "Pozastaviť", "heroPlay": "Prehrať"},
      da: {"morning": "GODMORGEN", "afternoon": "GOD EFTERMIDDAG", "evening": "GODAFTEN", "welcome": "Velkommen tilbage", "pickup": "Dine bøger venter", "by": "af", "continue": "Fortsæt", "left": "tilbage", "narratedBy": "Fortalt af", "unknown": "Ukendt titel", "fallbackDesc": "Fortsæt lige hvor du slap.", "heroPause": "Pause", "heroPlay": "Afspil"},
      sv: {"morning": "GOD MORGON", "afternoon": "GOD EFTERMIDDAG", "evening": "GOD KVÄLL", "welcome": "Välkommen tillbaka", "pickup": "Dina böcker väntar", "by": "av", "continue": "Fortsätt", "left": "kvar", "narratedBy": "Uppläst av", "unknown": "Okänd titel", "fallbackDesc": "Fortsätt precis där du slutade.", "heroPause": "Pausa", "heroPlay": "Spela upp"},
      no: {"morning": "GOD MORGEN", "afternoon": "GOD ETTERMIDDAG", "evening": "GOD KVELD", "welcome": "Velkommen tilbake", "pickup": "Bøkene dine venter", "by": "av", "continue": "Fortsett", "left": "igjen", "narratedBy": "Fortalt av", "unknown": "Ukjent tittel", "fallbackDesc": "Fortsett akkurat der du slapp.", "heroPause": "Pause", "heroPlay": "Spill av"},
      fi: {"morning": "HYVÄÄ HUOMENTA", "afternoon": "HYVÄÄ PÄIVÄÄ", "evening": "HYVÄÄ ILTAA", "welcome": "Tervetuloa takaisin", "pickup": "Kirjasi odottavat", "by": "–", "continue": "Jatka", "left": "jäljellä", "narratedBy": "Lukija", "unknown": "Tuntematon nimi", "fallbackDesc": "Jatka juuri siitä mihin jäit.", "heroPause": "Keskeytä", "heroPlay": "Toista"},
      ru: {"morning": "ДОБРОЕ УТРО", "afternoon": "ДОБРЫЙ ДЕНЬ", "evening": "ДОБРЫЙ ВЕЧЕР", "welcome": "С возвращением", "pickup": "Ваши книги ждут", "by": "-", "continue": "Продолжить", "left": "осталось", "narratedBy": "Читает", "unknown": "Неизвестное название", "fallbackDesc": "Продолжите с того места, где остановились.", "heroPause": "Пауза", "heroPlay": "Воспроизвести"},
      uk: {"morning": "ДОБРОГО РАНКУ", "afternoon": "ДОБРОГО ДНЯ", "evening": "ДОБРОГО ВЕЧОРА", "welcome": "З поверненням", "pickup": "Ваші книги чекають", "by": "-", "continue": "Продовжити", "left": "залишилося", "narratedBy": "Читає", "unknown": "Невідома назва", "fallbackDesc": "Продовжте з того місця, де зупинилися.", "heroPause": "Пауза", "heroPlay": "Відтворити"},
      be: {"morning": "ДОБРАЙ РАНІЦЫ", "afternoon": "ДОБРЫ ДЗЕНЬ", "evening": "ДОБРЫ ВЕЧАР", "welcome": "З вяртаннем", "pickup": "Вашы кнігі чакаюць", "by": "-", "continue": "Працягнуць", "left": "засталося", "narratedBy": "Чытае", "unknown": "Невядомая назва", "fallbackDesc": "Працягніце з таго месца, дзе спыніліся.", "heroPause": "Паўза", "heroPlay": "Прайграць"},
      bg: {"morning": "ДОБРО УТРО", "afternoon": "ДОБЪР ДЕН", "evening": "ДОБЪР ВЕЧЕР", "welcome": "Добре дошли отново", "pickup": "Вашите книги чакат", "by": "от", "continue": "Продължи", "left": "остават", "narratedBy": "Разказва", "unknown": "Неизвестно заглавие", "fallbackDesc": "Продължете точно откъдето спряхте.", "heroPause": "Пауза", "heroPlay": "Възпроизвеждане"},
      hr: {"morning": "DOBRO JUTRO", "afternoon": "DOBAR DAN", "evening": "DOBRA VEČER", "welcome": "Dobrodošli natrag", "pickup": "Vaše knjige čekaju", "by": "od", "continue": "Nastavi", "left": "preostalo", "narratedBy": "Pripovijeda", "unknown": "Nepoznat naslov", "fallbackDesc": "Nastavite točno gdje ste stali.", "heroPause": "Pauza", "heroPlay": "Reproduciraj"},
      sl: {"morning": "DOBRO JUTRO", "afternoon": "DOBER DAN", "evening": "DOBER VEČER", "welcome": "Dobrodošli nazaj", "pickup": "Vaše knjige čakajo", "by": "od", "continue": "Nadaljuj", "left": "preostalo", "narratedBy": "Pripoveduje", "unknown": "Neznan naslov", "fallbackDesc": "Nadaljujte točno tam, kjer ste ostali.", "heroPause": "Premor", "heroPlay": "Predvajaj"},
      hu: {"morning": "JÓ REGGELT", "afternoon": "JÓ NAPOT", "evening": "JÓ ESTÉT", "welcome": "Üdvözöljük újra", "pickup": "A könyveid várnak rád", "by": "–", "continue": "Folytatás", "left": "van hátra", "narratedBy": "Felolvassa", "unknown": "Ismeretlen cím", "fallbackDesc": "Folytassa pontosan ott, ahol abbahagyta.", "heroPause": "Szünet", "heroPlay": "Lejátszás"},
      ro: {"morning": "BUNĂ DIMINEAȚA", "afternoon": "BUNĂ ZIUA", "evening": "BUNĂ SEARA", "welcome": "Bine ați revenit", "pickup": "Cărțile tale te așteaptă", "by": "de", "continue": "Continuă", "left": "rămas", "narratedBy": "Narat de", "unknown": "Titlu necunoscut", "fallbackDesc": "Continuați exact de unde ați rămas.", "heroPause": "Pauză", "heroPlay": "Redare"},
      lt: {"morning": "LABAS RYTAS", "afternoon": "LABA DIENA", "evening": "LABAS VAKARAS", "welcome": "Sveiki sugrįžę", "pickup": "Tavo knygos laukia", "by": "–", "continue": "Tęsti", "left": "liko", "narratedBy": "Skaito", "unknown": "Nežinomas pavadinimas", "fallbackDesc": "Tęskite tiksliai nuo ten, kur baigėte.", "heroPause": "Pristabdyti", "heroPlay": "Leisti"},
      lv: {"morning": "LABRĪT", "afternoon": "LABDIEN", "evening": "LABVAKAR", "welcome": "Laipni lūdzam atpakaļ", "pickup": "Tavas grāmatas gaida", "by": "–", "continue": "Turpināt", "left": "atlicis", "narratedBy": "Lasa", "unknown": "Nezināms nosaukums", "fallbackDesc": "Turpiniet tieši no vietas, kur pārtraucāt.", "heroPause": "Pauzēt", "heroPlay": "Atskaņot"},
      et: {"morning": "TERE HOMMIKUST", "afternoon": "TERE PÄEVAST", "evening": "TERE ÕHTUST", "welcome": "Tere tulemast tagasi", "pickup": "Sinu raamatud ootavad", "by": "–", "continue": "Jätka", "left": "jäänud", "narratedBy": "Loeb", "unknown": "Tundmatu pealkiri", "fallbackDesc": "Jätkake täpselt sealt, kus pooleli jäite.", "heroPause": "Peata", "heroPlay": "Esita"},
      el: {"morning": "ΚΑΛΗΜΕΡΑ", "afternoon": "ΚΑΛΟ ΑΠΟΓΕΥΜΑ", "evening": "ΚΑΛΗΣΠΕΡΑ", "welcome": "Καλώς ήρθατε ξανά", "pickup": "Τα βιβλία σας περιμένουν", "by": "του", "continue": "Συνέχεια", "left": "απομένουν", "narratedBy": "Αφήγηση", "unknown": "Άγνωστος τίτλος", "fallbackDesc": "Συνεχίστε από εκεί που σταματήσατε.", "heroPause": "Παύση", "heroPlay": "Αναπαραγωγή"},
      tr: {"morning": "GÜNAYDIN", "afternoon": "İYİ GÜNLER", "evening": "İYİ AKŞAMLAR", "welcome": "Tekrar hoş geldiniz", "pickup": "Kitapların seni bekliyor", "by": "–", "continue": "Devam Et", "left": "kaldı", "narratedBy": "Seslendiren", "unknown": "Bilinmeyen Başlık", "fallbackDesc": "Kaldığınız yerden devam edin.", "heroPause": "Duraklat", "heroPlay": "Oynat"},
      ca: {"morning": "BON DIA", "afternoon": "BONA TARDA", "evening": "BONA NIT", "welcome": "Benvingut de nou", "pickup": "Els teus llibres t’esperen", "by": "de", "continue": "Continua", "left": "restant", "narratedBy": "Narrat per", "unknown": "Títol desconegut", "fallbackDesc": "Continueu exactament on ho vau deixar.", "heroPause": "Pausa", "heroPlay": "Reprodueix"},
      eu: {"morning": "EGUN ON", "afternoon": "ARRATSALDE ON", "evening": "GABON", "welcome": "Ongi etorri berriro", "pickup": "Zure liburuak zain daude", "by": "–", "continue": "Jarraitu", "left": "geratzen da", "narratedBy": "Narratzailea", "unknown": "Izenburu ezezaguna", "fallbackDesc": "Jarraitu utzi zenuen toki beretik.", "heroPause": "Pausatu", "heroPlay": "Erreproduzitu"},
      is: {"morning": "GÓÐAN DAGINN", "afternoon": "GÓÐAN DAG", "evening": "GOTT KVÖLD", "welcome": "Velkomin aftur", "pickup": "Bækurnar þínar bíða", "by": "eftir", "continue": "Halda áfram", "left": "eftir", "narratedBy": "Lesari", "unknown": "Óþekktur titill", "fallbackDesc": "Haltu áfram þar sem frá var horfið.", "heroPause": "Gera hlé", "heroPlay": "Spila"},
      ja: {"morning": "おはようございます", "afternoon": "こんにちは", "evening": "こんばんは", "welcome": "おかえりなさい", "pickup": "本があなたを待っています", "by": "著", "continue": "再開する", "left": "残り", "narratedBy": "朗読", "unknown": "不明なタイトル", "fallbackDesc": "続きから再開しましょう。", "heroPause": "一時停止", "heroPlay": "再生"},
      ko: {"morning": "좋은 아침입니다", "afternoon": "좋은 오후입니다", "evening": "좋은 저녁입니다", "welcome": "다시 오신 것을 환영합니다", "pickup": "책이 기다리고 있어요", "by": "저자", "continue": "계속하기", "left": "남음", "narratedBy": "낭독", "unknown": "알 수 없는 제목", "fallbackDesc": "멈춘 곳에서 이어서 시작하세요.", "heroPause": "일시정지", "heroPlay": "재생"},
      zh: {"morning": "早上好", "afternoon": "下午好", "evening": "晚上好", "welcome": "欢迎回来", "pickup": "你的书在等你", "by": "作者", "continue": "继续", "left": "剩余", "narratedBy": "朗读", "unknown": "未知标题", "fallbackDesc": "从上次的位置继续。", "heroPause": "暂停", "heroPlay": "播放"},
      ar: {"morning": "صباح الخير", "afternoon": "مساء الخير", "evening": "مساء الخير", "welcome": "مرحبًا بعودتك", "pickup": "كتبك بانتظارك", "by": "لـ", "continue": "متابعة", "left": "متبقٍ", "narratedBy": "بصوت", "unknown": "عنوان غير معروف", "fallbackDesc": "تابع من حيث توقفت.", "heroPause": "إيقاف مؤقت", "heroPlay": "تشغيل"},
      he: {"morning": "בוקר טוב", "afternoon": "צהריים טובים", "evening": "ערב טוב", "welcome": "ברוך שובך", "pickup": "הספרים שלך מחכים לך", "by": "מאת", "continue": "המשך", "left": "נותרו", "narratedBy": "מקריא", "unknown": "כותרת לא ידועה", "fallbackDesc": "המשך מהמקום שבו הפסקת.", "heroPause": "השהה", "heroPlay": "נגן"},
      fa: {"morning": "صبح بخیر", "afternoon": "عصر بخیر", "evening": "شب بخیر", "welcome": "خوش برگشتید", "pickup": "کتاب‌هایت منتظر تو هستند", "by": "از", "continue": "ادامه", "left": "باقی‌مانده", "narratedBy": "با صدای", "unknown": "عنوان ناشناخته", "fallbackDesc": "از جایی که رها کردید ادامه دهید.", "heroPause": "توقف", "heroPlay": "پخش"},
      hi: {"morning": "सुप्रभात", "afternoon": "शुभ दोपहर", "evening": "शुभ संध्या", "welcome": "वापसी पर स्वागत है", "pickup": "आपकी किताबें आपका इंतज़ार कर रही हैं", "by": "द्वारा", "continue": "जारी रखें", "left": "शेष", "narratedBy": "वाचन", "unknown": "अज्ञात शीर्षक", "fallbackDesc": "जहाँ छोड़ा था वहीं से जारी रखें।", "heroPause": "रोकें", "heroPlay": "चलाएँ"},
      bn: {"morning": "সুপ্রভাত", "afternoon": "শুভ অপরাহ্ন", "evening": "শুভ সন্ধ্যা", "welcome": "ফিরে আসায় স্বাগতম", "pickup": "আপনার বইগুলি অপেক্ষা করছে", "by": "লেখক", "continue": "চালিয়ে যান", "left": "বাকি", "narratedBy": "পাঠ করেছেন", "unknown": "অজানা শিরোনাম", "fallbackDesc": "যেখানে থেমেছিলেন সেখান থেকে চালিয়ে যান।", "heroPause": "বিরতি", "heroPlay": "চালান"},
      gu: {"morning": "સુપ્રભાત", "afternoon": "શુભ બપોર", "evening": "શુભ સાંજ", "welcome": "પાછા આવવા બદલ સ્વાગત છે", "pickup": "તમારાં પુસ્તકો રાહ જુએ છે", "by": "દ્વારા", "continue": "ચાલુ રાખો", "left": "બાકી", "narratedBy": "વાચન", "unknown": "અજાણ્યું શીર્ષક", "fallbackDesc": "જ્યાં છોડ્યું હતું ત્યાંથી ચાલુ રાખો.", "heroPause": "થોભો", "heroPlay": "ચલાવો"},
      vi: {"morning": "CHÀO BUỔI SÁNG", "afternoon": "CHÀO BUỔI CHIỀU", "evening": "CHÀO BUỔI TỐI", "welcome": "Chào mừng trở lại", "pickup": "Sách của bạn đang chờ", "by": "của", "continue": "Tiếp tục", "left": "còn lại", "narratedBy": "Người đọc", "unknown": "Tiêu đề không xác định", "fallbackDesc": "Tiếp tục từ chỗ bạn đã dừng.", "heroPause": "Tạm dừng", "heroPlay": "Phát"}
    };
    // The carousel X (#18). Added here rather than into all ~40 one-line dicts
    // above; anything without its own wording falls back to English.
    const heroReset = {
      en: 'Reset progress and remove from here',
      pl: 'Zresetuj postęp i usuń stąd',
      de: 'Fortschritt zurücksetzen und entfernen',
      fr: 'Réinitialiser la progression et retirer',
      es: 'Restablecer el progreso y quitar de aquí',
      nl: 'Voortgang wissen en hier verwijderen',
      it: 'Azzera i progressi e rimuovi da qui',
    };
    const base = dictionary[baseLang] || dictionary.en;
    return { ...base, heroReset: heroReset[baseLang] || heroReset.en };
  };

  // ==========================================
  // 3. ICONS & TIME HELPERS
  // ==========================================
  const RAIL_ICONS = [
    [/\/bookshelf\/series/,      'layers'],
    [/\/bookshelf\/collections/, 'collections_bookmark'],
    [/\/bookshelf\/authors/,     'groups'],
    [/\/bookshelf$/,             'menu_book'],
    [/\/narrators/,              'mic'],
    [/\/stats/,                  'bar_chart'],
    [/\/library\/[^/]+$/,        'home'],
  ];

  // This used to rewrite the icon span's className + textContent once per link
  // and stamp it done. Both halves were wrong. Vue owns that text node and keeps
  // a reference to the one it created, so overwriting it leaves the reference
  // dangling: Vue can no longer remove the text when it recycles the same <a>
  // for a different library, and a podcast library ended up drawing the book
  // library's "groups" ligature over its podcast icon (github #3). The stamp
  // then guaranteed the stale glyph was never revisited.
  // Now: the glyph is a data attribute painted by CSS (::before, invisible to
  // the vdom), the native text is only hidden, and the mapping is re-resolved
  // from the current href every tick so a recycled link corrects itself.
  function remapRailIcons() {
    document.querySelectorAll('#siderail-buttons-container a').forEach((a) => {
      const span = a.querySelector('span.material-symbols, span.abs-icons');
      if (!span) return;
      const href = a.getAttribute('href') || '';
      const match = RAIL_ICONS.find(([re]) => re.test(href));
      // Entries ABS draws from its own icon font (the podcast library's "Add")
      // already have an icon and no text - leave them exactly as they are.
      const glyph = (match && !span.classList.contains('abs-icons')) ? match[1] : '';
      if ((span.dataset.nhGlyph || '') === glyph) {
        // Vue rewrites className wholesale when the class binding changes, which
        // drops our marker while the attribute survives; re-assert it.
        if (glyph) span.classList.add('nh-rail-icon');
        return;
      }
      if (glyph) { span.dataset.nhGlyph = glyph; span.classList.add('nh-rail-icon'); }
      else { delete span.dataset.nhGlyph; span.classList.remove('nh-rail-icon'); }
    });
  }

  // The appbar's account button shows only a name; give it the user's photo
  // (Pawel). Vue owns that button, so re-check the circle is still there every
  // tick rather than trusting a stamp.
  function nhAccountAvatar() {
    if (nhSettings.accountPhoto === false) return;
    const link = document.querySelector('a[href$="/account"]');
    if (!link) return;
    const nameSpan = link.querySelector('span.items-center') || link.firstElementChild;
    if (!nameSpan) return;
    const uid = nhHeroUser();
    if (!uid) return;
    nhScFetch(); // avatars map
    const sig = uid + ':' + ((nhSc.avatars && nhSc.avatars[uid]) || '') + ':' + (nhSc.ts || 0);
    let av = nameSpan.querySelector('.nh-acc-av');
    if (av && nameSpan.dataset.nhAv === sig) return;
    nameSpan.dataset.nhAv = sig;
    if (av) av.remove();
    av = document.createElement('span');
    av.className = 'nh-acc-av';
    let uname = '';
    try { uname = (window.$nuxt.$store.state.user.user.username) || ''; } catch (e) {}
    av.textContent = (uname || '?').charAt(0).toUpperCase();
    nhAvatarInto(av, uid);
    nameSpan.insertBefore(av, nameSpan.firstChild);
  }

  function localizeRail() {
    const lang = getUserLanguage().split('-')[0].toLowerCase();
    if (lang !== 'pl') return;
    document.querySelectorAll('#siderail-buttons-container a').forEach((a) => {
      const href = a.getAttribute('href') || '';
      if (!/\/library\/[^/]+$/.test(href)) return;
      a.querySelectorAll('p, .truncate, span:not(.material-symbols):not(.abs-icons)').forEach((el) => {
        if (el.textContent.trim() === 'Strona główna') el.textContent = 'Start';
      });
    });
  }

  function formatTime(sec) {
    if (!sec || isNaN(sec) || sec <= 0) return '0m';
    const h = Math.floor(sec / 3600);
    const m = Math.floor((sec % 3600) / 60);
    if (h > 0) return `${h}h ${m.toString().padStart(2, '0')}m`;
    return `${m}m`;
  }

  // Phones: the hero must fit the first screen whole - no CTA or shadow lip
  // past the fold (Pawel). The cover is the one flexible block, so overflow is
  // taken out of it via a CSS var on the CONTAINER (every slide shares it, so
  // all slides keep one size); an extreme shortfall also tightens the
  // description to two lines. innerHeight at load is the small viewport
  // (browser chrome visible), so the worst case is what gets measured.
  function nhHeroFit(cont) {
    cont = cont || document.getElementById('nh-hero-container');
    if (!cont) return;
    // Two measured passes: the first takes the overflow out of the cover (and
    // tightens the description when that cannot cover it); the second measures
    // the REAL post-reflow height and trims the remainder, down to a hard
    // 64px cover. Bounded - never a resize loop.
    const overBy = () => {
      const r = cont.getBoundingClientRect();
      const scroller = cont.closest('#bookshelf') || document.getElementById('bookshelf');
      const unscrolledTop = r.top + (scroller ? scroller.scrollTop : 0);
      return r.height - (innerHeight - unscrolledTop - 12);
    };
    const apply = () => {
      if (!document.contains(cont)) return;
      cont.style.removeProperty('--nh-hero-cover-h');
      cont.classList.remove('nh-hero-tight');
      if (innerWidth > 640) return;
      const img = cont.querySelector('.nh-hero-banner > div:nth-child(4) img');
      if (!img) return;
      const over = overBy();
      if (over <= 0) return;
      const ih = img.getBoundingClientRect().height || 0;
      let target = Math.max(64, ih - over);
      cont.style.setProperty('--nh-hero-cover-h', Math.round(target) + 'px');
      if (ih - target < over) cont.classList.add('nh-hero-tight');
      requestAnimationFrame(() => {
        if (!document.contains(cont)) return;
        const still = overBy();
        if (still > 0) {
          target = Math.max(64, target - still);
          cont.style.setProperty('--nh-hero-cover-h', Math.round(target) + 'px');
        }
      });
    };
    requestAnimationFrame(() => requestAnimationFrame(apply));
  }
  let nhHeroFitT = null;
  window.addEventListener('resize', () => { clearTimeout(nhHeroFitT); nhHeroFitT = setTimeout(() => nhHeroFit(), 200); });

  // ==========================================
  // 4. HERO CAROUSEL INJECTION
  // ==========================================
  async function buildSlideData(card, t) {
    let title = card.querySelector('[cy-id="title"]')?.textContent.trim() || t.unknown;
    let author = card.querySelector('[cy-id="line2"]')?.textContent.trim() || "";
    let coverUrl = "";
    let id = null;

    id = nhCardItemId(card);
    const coverImg = card.querySelector('img[cy-id="coverImage"], img');
    if (coverImg && coverImg.src) {
      try {
        const urlObj = new URL(coverImg.src, window.location.origin);
        urlObj.searchParams.set('width', '800');
        coverUrl = urlObj.toString();
      } catch(e) {
        coverUrl = coverImg.src;
      }
    }

    let hasAudio = true;
    let hasEbook = false;

    let leftSideText = "0%";
    let rightSideText = `0m ${t.left}`;
    let progressPercent = 0;
    let tagsHtml = "";
    let description = t.fallbackDesc;

    if (id) {
      let basePath = '/';
      try { basePath = window.__NUXT__.config.routerBasePath || '/'; } catch (e) {}
      if (!basePath.endsWith('/')) basePath += '/';
      const apiUrl = basePath + 'api/items/' + id + '?expanded=1&include=progress';

      let token = '';
      try {
        if (window.$nuxt && window.$nuxt.$store && window.$nuxt.$store.state.user) {
          token = window.$nuxt.$store.state.user.token;
        }
        if (!token) token = localStorage.getItem('token') || '';
        if (!token) {
          const vuexStr = localStorage.getItem('vuex');
          if (vuexStr) token = JSON.parse(vuexStr)?.user?.token || '';
        }
      } catch (e) {}

      try {
        const headersOpt = token ? { 'Authorization': 'Bearer ' + token } : {};
        const res = await fetch(apiUrl, { headers: headersOpt, credentials: 'include' });

        if (res.ok) {
          const itemData = await res.json();

          if (itemData.media?.metadata?.title) title = itemData.media.metadata.title;
          if (itemData.media?.metadata?.authorName) author = itemData.media.metadata.authorName;

          const descRaw = itemData.media?.metadata?.description;
          if (descRaw) {
            description = nhHtmlToText(descRaw);
            if (description.length > 450) description = description.substring(0, 445) + '...';
          }

          let durationSec = 0;
          let playedSec = 0;

          try {
            const vuexStr = localStorage.getItem('vuex');
            if (vuexStr) {
              const vuexData = JSON.parse(vuexStr);
              const mpList = vuexData?.user?.mediaProgress || [];
              const localProg = mpList.find(p => p.libraryItemId === id);
              if (localProg) {
                 if (localProg.duration) durationSec = Number(localProg.duration);
                 if (localProg.currentTime) playedSec = Number(localProg.currentTime);
                 if (localProg.progress) progressPercent = Number(localProg.progress) * 100;
              }
            }
          } catch(e) {}

          if (!durationSec) durationSec = Number(itemData.media?.duration) || Number(itemData.media?.metadata?.duration) || 0;

          const nhEbookFormat = itemData.media?.ebookFormat || itemData.media?.ebookFile?.ebookFormat || '';
          hasAudio = Number(itemData.media?.duration) > 0;
          hasEbook = !!nhEbookFormat;
          const isEbookOnly = !hasAudio && hasEbook;

          const ump = itemData.userMediaProgress;
          if (ump) {
            if (ump.currentTime != null && !playedSec) playedSec = Number(ump.currentTime);
            if (ump.progress != null && !progressPercent) progressPercent = Number(ump.progress) * 100;
            if (isEbookOnly && ump.ebookProgress != null && !progressPercent) progressPercent = Number(ump.ebookProgress) * 100;
          }

          if (!progressPercent) {
             const pDiv = card.querySelector('.bg-yellow-400.absolute.bottom-0, [cy-id="progressBar"]');
             if (pDiv && pDiv.style.width) {
               const w = pDiv.style.width;
               progressPercent = w.includes('%') ? parseFloat(w) : (parseFloat(w) / (card.offsetWidth || 192)) * 100;
             }
          }
          if (!playedSec && durationSec > 0 && progressPercent > 0) {
            playedSec = durationSec * (progressPercent / 100);
          }

          if (isNaN(playedSec)) playedSec = 0;
          if (isNaN(durationSec)) durationSec = 0;
          if (isNaN(progressPercent)) progressPercent = 0;
          if (playedSec > durationSec) playedSec = durationSec;

          const leftSec = Math.max(0, durationSec - playedSec);

          leftSideText = `${Math.round(progressPercent)}%`;
          // Ebooks have no time axis: show only the percentage, no "0m left".
          rightSideText = isEbookOnly ? '' : (leftSec > 0 ? `${formatTime(leftSec)} ${t.left}` : `0m ${t.left}`);
          if (isEbookOnly && description === t.fallbackDesc) description = t.pickup + '.';

          const tags = [];
          if (durationSec > 0) tags.push(formatTime(durationSec));
          if (isEbookOnly && nhEbookFormat) tags.push(String(nhEbookFormat).toUpperCase());

          const narrator = itemData.media?.metadata?.narratorName;
          if (narrator) tags.push(`${t.narratedBy} ${narrator}`);

          const genres = itemData.media?.metadata?.genres || [];
          if (genres.length > 0) tags.push(genres[0]);

          if (tags.length > 0) {
            // Narrator and genre are item metadata, so they are ESCAPED here. They used
            // to be interpolated raw, which made a genre or a narrator name a script
            // injection into everyone's home page (see slideInner).
            tagsHtml = tags.map(tag => `<span style="background: rgba(255,255,255,0.10); border: 1px solid rgba(255,255,255,0.16); padding: 4px 14px; border-radius: 20px; font-size: 0.90rem; color: #ffffff; backdrop-filter: blur(4px);">${escapeHtmlNH(tag)}</span>`).join('');
          }
        }
      } catch (e) {}
    }

    return { card, id, title, author, coverUrl, leftSideText, rightSideText, progressPercent, tagsHtml, description, hasAudio, hasEbook };
  }

  // ---- hero snapshot cache (B2) -------------------------------------------
  // The carousel used to wait a full item-fetch before it could paint. We keep a
  // per-library localStorage snapshot of the last visit's slides keyed by item id,
  // paint from it the instant the Continue Listening cards are in the DOM, then
  // refresh every slide in the background and patch the fields in place.
  // 3: tagsHtml is escaped as of the metadata-escaping fix. A snapshot written by an
  // earlier build holds UNESCAPED markup and is re-injected verbatim on the next
  // visit, so those records have to be thrown away rather than trusted.
  const NH_HERO_CACHE_V = 3;
  const NH_HERO_CACHE_TTL = 14 * 24 * 3600 * 1000;

  // The card's library-item id, synchronously. The card's own element id is only
  // positional (`book-card-3`), so the Vue instance is the reliable source; the cover
  // URL is the fallback, and it is EMPTY for books without a cover (ABS serves
  // book_placeholder.jpg) - which is why the id has to come from Vue first.
  function nhCardItemId(card) {
    if (!card) return null;
    try {
      const host = card.id && card.id.indexOf('book-card-') === 0 ? card : (card.closest('[id^="book-card-"]') || card);
      const vm = host.__vue__;
      const li = vm && (vm.libraryItem || (vm.$props && vm.$props.libraryItem));
      if (li && li.id) return li.id;
    } catch (e) {}
    const img = card.querySelector('img[cy-id="coverImage"], img');
    const m = img && img.src && img.src.match(/\/api\/items\/([^/]+)\/cover/);
    return m ? m[1] : null;
  }

  function nhHeroCacheKey() { return 'nh-hero-cache:' + (getLibIdNH() || 'default'); }

  // ---------- scroll arrows for OUR shelves (GitHub #21) ----------
  // ABS puts a pair of round arrow buttons in each shelf's header, beside the
  // title. Our own rows (Recent Series, Rate what you finished) are built from
  // scratch and so never got them, which left their last items unreachable
  // without a trackpad swipe.
  // Same placement and size as ABS's, but shown only when the strip actually
  // overflows, and the one that cannot move is dimmed. Inline SVG chevrons rather
  // than material-symbols ligatures, which do not substitute in injected markup on
  // every build (learned on staging, where they rendered as the literal word).
  const NH_SA_CHEV = {
    left: '<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 5 8 12l7 7"/></svg>',
    right: '<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 5l7 7-7 7"/></svg>',
  };
  function nhShelfArrows(row, headingSel, scrollerSel) {
    if (!row) return;
    const heading = row.querySelector(headingSel);
    const scroller = row.querySelector(scrollerSel);
    if (!heading || !scroller) return;

    let bar = row.querySelector(':scope > .nh-sa-head');
    if (!bar) {
      // Wrap the existing heading so the buttons can sit on the same line without
      // the heading's own margins being disturbed.
      bar = document.createElement('div');
      bar.className = 'nh-sa-head';
      heading.parentNode.insertBefore(bar, heading);
      bar.appendChild(heading);
      const mk = (dir) => {
        const b = document.createElement('button');
        b.type = 'button';
        b.className = 'nh-sa-btn nh-sa-' + dir;
        b.innerHTML = NH_SA_CHEV[dir];
        b.setAttribute('aria-label', nhAbsString(dir === 'left' ? 'ButtonPrevious' : 'ButtonNext', dir === 'left' ? 'Scroll left' : 'Scroll right'));
        b.addEventListener('click', (e) => {
          e.preventDefault();
          e.stopPropagation();
          const step = Math.max(160, Math.round(scroller.clientWidth * 0.9));
          scroller.scrollBy({ left: dir === 'left' ? -step : step, behavior: 'smooth' });
        });
        return b;
      };
      const group = document.createElement('div');
      group.className = 'nh-sa-btns';
      group.appendChild(mk('left'));
      group.appendChild(mk('right'));
      bar.appendChild(group);
      const sync = () => nhShelfArrowsSync(row, scroller);
      scroller.addEventListener('scroll', sync, { passive: true });
      window.addEventListener('resize', sync);
    }
    nhShelfArrowsSync(row, scroller);
  }
  // Re-measure from the tick. The first sync runs the moment the row is built,
  // when the strip has not laid out yet and reads as no-overflow, so the arrows
  // never appeared. Idempotent: classList.toggle to the state it is already in
  // mutates nothing, so this cannot feed the mutation-observer loop.
  function nhShelfArrowsTick() {
    [['nh-recent-series-row', '.nh-rs-scroll'], ['nh-rate-finished-row', '.nh-rf-scroll']].forEach((pair) => {
      const row = document.getElementById(pair[0]);
      if (!row) return;
      const scroller = row.querySelector(pair[1]);
      if (scroller) nhShelfArrowsSync(row, scroller);
    });
  }
  function nhShelfArrowsSync(row, scroller) {
    const bar = row.querySelector(':scope > .nh-sa-head');
    if (!bar) return;
    const max = scroller.scrollWidth - scroller.clientWidth;
    // 4px of slack: sub-pixel widths leave a permanent 1-2px "overflow" that would
    // otherwise show arrows on a shelf that has nothing to scroll.
    bar.classList.toggle('nh-sa-on', max > 4);
    const l = bar.querySelector('.nh-sa-left');
    const r = bar.querySelector('.nh-sa-right');
    if (l) l.classList.toggle('nh-sa-off', scroller.scrollLeft <= 2);
    if (r) r.classList.toggle('nh-sa-off', scroller.scrollLeft >= max - 2);
  }

  // ---------- carousel: reset progress on a slide (GitHub #18) ----------
  // The carousel replaced the Continue Listening shelf, and ABS's per-card menu went
  // with it, so there was no way to drop a book you started by accident short of
  // switching the shelf back on in settings. This is the same action as the X beside
  // the progress bar on the book page: DELETE the media-progress RECORD.
  // NOTE the id: the route is /api/me/progress/:id where :id is the PROGRESS RECORD
  // id, not the libraryItemId. Passing the item id 404s (verified against ABS 2.36's
  // MeController.removeMediaProgress, which looks the record up by its own id).
  const nhHeroDropped = new Set();

  // Two choices now (GitHub #20): take the book off the shelf and keep your place,
  // which is what stock ABS does, or that plus reset your progress. Only these two
  // strings are ours. "Remove from Continue Listening" and Cancel come from ABS's
  // own table, already correct everywhere and always matching the shelf label.
  const NH_HERO_ASK = {
    en: 'What would you like to do with this book?',
    pl: 'Co chcesz zrobić z tą książką?',
    de: 'Was möchtest du mit diesem Buch machen?',
    fr: 'Que voulez-vous faire de ce livre ?',
    es: '¿Qué quieres hacer con este libro?',
    it: 'Cosa vuoi fare con questo libro?',
    pt: 'O que quer fazer com este livro?',
    nl: 'Wat wil je met dit boek doen?',
    cs: 'Co chceš s touto knihou udělat?',
    sk: 'Čo chceš s touto knihou urobiť?',
    da: 'Hvad vil du gøre med denne bog?',
    sv: 'Vad vill du göra med den här boken?',
    no: 'Hva vil du gjøre med denne boken?',
    fi: 'Mitä haluat tehdä tälle kirjalle?',
    ru: 'Что сделать с этой книгой?',
    uk: 'Що зробити з цією книгою?',
    be: 'Што зрабіць з гэтай кнігай?',
    bg: 'Какво искате да направите с тази книга?',
    hr: 'Što želiš učiniti s ovom knjigom?',
    sl: 'Kaj želiš narediti s to knjigo?',
    hu: 'Mit szeretnél tenni ezzel a könyvvel?',
    ro: 'Ce vrei să faci cu această carte?',
    lt: 'Ką norite padaryti su šia knyga?',
    lv: 'Ko vēlies darīt ar šo grāmatu?',
    et: 'Mida soovid selle raamatuga teha?',
    el: 'Τι θέλετε να κάνετε με αυτό το βιβλίο;',
    tr: 'Bu kitapla ne yapmak istersiniz?',
    ca: 'Què vols fer amb aquest llibre?',
    eu: 'Zer egin nahi duzu liburu honekin?',
    is: 'Hvað viltu gera við þessa bók?',
    ja: 'この本をどうしますか？',
    ko: '이 책을 어떻게 할까요?',
    zh: '想对这本书做什么？',
    ar: 'ماذا تريد أن تفعل بهذا الكتاب؟',
    he: 'מה לעשות עם הספר הזה?',
    fa: 'می‌خواهید با این کتاب چه کار کنید؟',
    hi: 'इस किताब का क्या करना चाहेंगे?',
    bn: 'এই বইটি নিয়ে কী করতে চান?',
    gu: 'આ પુસ્તકનું શું કરવા માંગો છો?',
    vi: 'Bạn muốn làm gì với cuốn sách này?',
  };
  const NH_HERO_RESET_BTN = {
    en: 'Remove and reset progress',
    pl: 'Usuń i zresetuj postęp',
    de: 'Entfernen und Fortschritt zurücksetzen',
    fr: 'Retirer et réinitialiser la progression',
    es: 'Quitar y restablecer el progreso',
    it: 'Rimuovi e azzera i progressi',
    pt: 'Remover e repor o progresso',
    nl: 'Verwijderen en voortgang wissen',
    cs: 'Odebrat a resetovat postup',
    sk: 'Odstrániť a resetovať postup',
    da: 'Fjern og nulstil fremgang',
    sv: 'Ta bort och nollställ framsteg',
    no: 'Fjern og nullstill fremgang',
    fi: 'Poista ja nollaa edistyminen',
    ru: 'Убрать и сбросить прогресс',
    uk: 'Прибрати і скинути прогрес',
    be: 'Прыбраць і скінуць прагрэс',
    bg: 'Премахни и нулирай напредъка',
    hr: 'Ukloni i poništi napredak',
    sl: 'Odstrani in ponastavi napredek',
    hu: 'Eltávolítás és haladás törlése',
    ro: 'Elimină și resetează progresul',
    lt: 'Pašalinti ir atstatyti progresą',
    lv: 'Noņemt un atiestatīt progresu',
    et: 'Eemalda ja lähtesta edenemine',
    el: 'Αφαίρεση και μηδενισμός προόδου',
    tr: 'Kaldır ve ilerlemeyi sıfırla',
    ca: 'Treu i restableix el progrés',
    eu: 'Kendu eta aurrerapena berrezarri',
    is: 'Fjarlægja og núllstilla framvindu',
    ja: '削除して進捗をリセット',
    ko: '제거하고 진행 상황 초기화',
    zh: '移除并重置进度',
    ar: 'إزالة وإعادة تعيين التقدم',
    he: 'הסרה ואיפוס ההתקדמות',
    fa: 'حذف و بازنشانی پیشرفت',
    hi: 'हटाएँ और प्रगति रीसेट करें',
    bn: 'সরান এবং অগ্রগতি রিসেট করুন',
    gu: 'દૂર કરો અને પ્રગતિ રીસેટ કરો',
    vi: 'Gỡ và đặt lại tiến độ',
  };

  function nhAbsString(key, fallback) {
    try {
      const v = window.$nuxt && window.$nuxt.$strings && window.$nuxt.$strings[key];
      if (v) return v;
    } catch (e) {}
    return fallback;
  }
  // `ebook` picks the Continue READING strings, since that is the shelf an
  // ebook-only slide actually came from.
  // Calls back with 'hide' (take it off the shelf, keep your place, which is what
  // stock ABS does) or 'reset' (that plus discard your progress).
  function nhHeroConfirm(isEbook, onChoice) {
    const lang = getUserLanguage().split('-')[0].toLowerCase();
    const msg = NH_HERO_ASK[lang] || NH_HERO_ASK.en;
    const hideLabel = isEbook
      ? nhAbsString('ButtonRemoveFromContinueReading', 'Remove from Continue Reading')
      : nhAbsString('ButtonRemoveFromContinueListening', 'Remove from Continue Listening');
    const resetLabel = NH_HERO_RESET_BTN[lang] || NH_HERO_RESET_BTN.en;

    const old = document.getElementById('nh-hx-modal');
    if (old) old.remove();
    const overlay = document.createElement('div');
    overlay.id = 'nh-hx-modal';
    const bg = document.createElement('div');
    bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div');
    box.className = 'nh-rt-modal-box';
    const close = () => {
      overlay.remove();
      document.removeEventListener('keydown', onKey, true);
    };
    // Enter takes the SAFE option. There are two actions now and one of them
    // discards your place, so a stray Enter must not be the destructive one.
    const onKey = (e) => {
      if (e.key === 'Escape') { e.stopPropagation(); close(); }
      else if (e.key === 'Enter') { e.stopPropagation(); close(); onChoice('hide'); }
    };
    bg.addEventListener('click', close);
    document.addEventListener('keydown', onKey, true);

    const p = document.createElement('p');
    p.className = 'nh-hx-msg';
    p.textContent = msg;
    box.appendChild(p);

    const row = document.createElement('div');
    row.className = 'nh-hx-actions';
    const no = document.createElement('button');
    no.type = 'button';
    no.className = 'nh-hx-btn nh-hx-cancel';
    no.textContent = nhAbsString('ButtonCancel', 'Cancel');
    no.addEventListener('click', close);
    // Keeps your place. Stock ABS's own behaviour, so it is the default here too.
    const hide = document.createElement('button');
    hide.type = 'button';
    hide.className = 'nh-hx-btn nh-hx-hide';
    hide.textContent = hideLabel;
    hide.addEventListener('click', () => { close(); onChoice('hide'); });
    // Also throws your place away, so it is styled as the weightier of the two.
    const reset = document.createElement('button');
    reset.type = 'button';
    reset.className = 'nh-hx-btn nh-hx-reset';
    reset.textContent = resetLabel;
    reset.addEventListener('click', () => { close(); onChoice('reset'); });
    row.appendChild(no);
    row.appendChild(reset);
    row.appendChild(hide);
    box.appendChild(row);

    overlay.appendChild(bg);
    overlay.appendChild(box);
    document.body.appendChild(overlay);
    try { hide.focus(); } catch (e) {}
  }

  // Take it off the shelf but keep where you got to. ABS's own action, and the same
  // :id rule as the delete: the PROGRESS RECORD id, not the libraryItemId.
  function nhHeroHide(itemId, done) {
    const pid = nhHeroProgressId(itemId);
    const tok = nhSrToken();
    if (!pid || !tok) { if (done) done(false); return; }
    fetch('/api/me/progress/' + encodeURIComponent(pid) + '/remove-from-continue-listening', {
      headers: { Authorization: 'Bearer ' + tok },
    }).then((r) => {
      if (!r.ok) { if (done) done(false); return; }
      nhHeroDropped.add(itemId);
      try {
        const mp = (window.$nuxt.$store.state.user.user.mediaProgress || []).find((p) => p && p.libraryItemId === itemId);
        if (mp) mp.hideFromContinueListening = true;
      } catch (e) {}
      try { localStorage.removeItem(nhHeroCacheKey()); } catch (e) {}
      if (done) done(true);
    }).catch(() => { if (done) done(false); });
  }

  function nhHeroProgressId(itemId) {
    try {
      const mp = (window.$nuxt.$store.state.user.user.mediaProgress || []).find((p) => p && p.libraryItemId === itemId);
      return mp ? mp.id : null;
    } catch (e) { return null; }
  }
  function nhHeroResetProgress(itemId, done) {
    const pid = nhHeroProgressId(itemId);
    const tok = nhSrToken();
    if (!pid || !tok) { if (done) done(false); return; }
    fetch('/api/me/progress/' + encodeURIComponent(pid), {
      method: 'DELETE',
      headers: { Authorization: 'Bearer ' + tok },
    }).then((r) => {
      if (!r.ok) { if (done) done(false); return; }
      nhHeroDropped.add(itemId);
      // Keep the local store in step so anything else reading progress this tick
      // (the series badge, the filters) agrees with the server.
      try {
        const u = window.$nuxt.$store.state.user.user;
        u.mediaProgress = (u.mediaProgress || []).filter((p) => p.libraryItemId !== itemId);
      } catch (e) {}
      // The hero cache is a snapshot of these very slides.
      try { localStorage.removeItem(nhHeroCacheKey()); } catch (e) {}
      if (done) done(true);
    }).catch(() => { if (done) done(false); });
  }
  // Tear the carousel down so the next tick rebuilds it without the dropped slide.
  function nhHeroRebuild() {
    const c = document.getElementById('nh-hero-container');
    const row = c && c.parentElement;
    if (c) c.remove();
    if (row && row.dataset) delete row.dataset.heroInjected;
    document.querySelectorAll('[data-hero-injected]').forEach((r) => { delete r.dataset.heroInjected; });
  }

  // Progress is per user, so a snapshot is only valid for whoever wrote it - on a
  // shared browser the next user must not see the previous one's position flash by.
  function nhHeroUser() {
    try {
      const u = window.$nuxt && window.$nuxt.$store && window.$nuxt.$store.state.user && window.$nuxt.$store.state.user.user;
      if (u && (u.id || u.username)) return u.id || u.username;
      return JSON.parse(localStorage.getItem('vuex') || '{}')?.user?.user?.id || '';
    } catch (e) { return ''; }
  }

  // Returns {id: slideData} for the current library, or null. Translated strings are
  // baked into the records (tags, "left"), so a language switch invalidates the snapshot.
  function nhHeroCacheRead(langCode) {
    try {
      const raw = localStorage.getItem(nhHeroCacheKey());
      if (!raw) return null;
      const rec = JSON.parse(raw);
      if (!rec || rec.v !== NH_HERO_CACHE_V || rec.lang !== langCode) return null;
      if (rec.user !== nhHeroUser()) return null;
      if (!rec.ts || Date.now() - rec.ts > NH_HERO_CACHE_TTL) return null;
      const map = {};
      (rec.slides || []).forEach(function (s) { if (s && s.id) map[s.id] = s; });
      return map;
    } catch (e) { return null; }
  }

  function nhHeroCacheWrite(langCode, slides) {
    try {
      const keep = slides.filter(function (s) { return s && s.id; }).map(function (s) {
        const c = {};
        Object.keys(s).forEach(function (k) { if (k !== 'card') c[k] = s[k]; });
        return c;
      });
      if (!keep.length) return;
      localStorage.setItem(nhHeroCacheKey(), JSON.stringify({ v: NH_HERO_CACHE_V, lang: langCode, user: nhHeroUser(), ts: Date.now(), slides: keep }));
    } catch (e) {}
  }

  // Which action buttons a slide carries - the only part patching can't fix in place.
  function nhHeroSig(d) { return (d.hasAudio ? 'a' : '') + (d.hasEbook ? 'e' : ''); }

  // Update a rendered slide to fresh data without tearing it down (no flicker, no
  // scroll jump). Falls back to a full re-render when the button set changed.
  function nhPatchSlide(el, oldD, newD, t, wire) {
    if (!el) return;
    if (nhHeroSig(oldD) !== nhHeroSig(newD)) {
      el.innerHTML = slideInner(newD, t);
      wire(el, newD);
      return;
    }
    const q = function (sel) { return el.querySelector(sel); };
    const setText = function (sel, v) {
      const n = q(sel);
      if (n && n.textContent.trim() !== String(v)) n.textContent = v;
    };
    setText('.nh-hero-title', newD.title);
    setText('.nh-hero-author', (t.by ? t.by + ' ' : '') + newD.author);
    setText('.nh-hero-desc', newD.description);
    setText('.nh-hero-prog-left', newD.leftSideText);
    setText('.nh-hero-prog-right', newD.rightSideText);
    const tags = q('.nh-hero-tags');
    if (tags) {
      if (tags.innerHTML !== (newD.tagsHtml || '')) tags.innerHTML = newD.tagsHtml || '';
      tags.style.display = newD.tagsHtml ? 'flex' : 'none';
    }
    const prog = q('.nh-hero-prog');
    if (prog) prog.style.width = newD.progressPercent + '%';
    if (newD.coverUrl && newD.coverUrl !== oldD.coverUrl) {
      const img = q('.nh-hero-cover');
      if (img) img.src = newD.coverUrl;
      const bg = q('.nh-hero-bg');
      if (bg) bg.style.backgroundImage = "url('" + newD.coverUrl + "')";
    }
  }

  // EVERY item-derived value below is escaped. This template is assigned to innerHTML
  // by four callers, so anything interpolated raw is script injection with the reach
  // of the home page: title, author, narrator and genre went in verbatim, and the
  // description survived its tag-stripping step whenever the payload was written as
  // HTML entities (that step DECODES them and hands live markup back). Reported by a
  // user and reproduced on the sandbox - five fields, five executing handlers, and it
  // fired on a phone too, where the description is display:none: hiding an element
  // does not stop the browser building it.
  // `tagsHtml` is the one exception and is escaped where it is BUILT (buildSlideData),
  // because it is markup by the time it arrives here.
  function slideInner(d, t) {
    const eTitle = escapeHtmlNH(d.title);
    const eAuthor = escapeHtmlNH(d.author);
    const eDesc = escapeHtmlNH(d.description);
    const eCover = escapeHtmlNH(d.coverUrl);
    const eLeft = escapeHtmlNH(d.leftSideText);
    const eRight = escapeHtmlNH(d.rightSideText);
    return `
        <div class="nh-hero-banner" style="width: 100%; position: relative; overflow: hidden; background-color: var(--nh-raised); border-radius: 24px; padding: 48px; display: flex; justify-content: space-between; align-items: center; box-shadow: 0 20px 50px rgba(0,0,0,0.5); cursor: pointer; transition: transform 0.2s ease;">

          <div class="nh-hero-bg" style="position: absolute; inset: -12%; background-image: url('${eCover}'); background-size: cover; background-position: center; filter: blur(60px) brightness(0.5) saturate(1.4); z-index: 0; pointer-events: none;"></div>
          <button class="nh-hero-x" type="button" title="${escapeHtmlNH(t.heroReset || '')}" aria-label="${escapeHtmlNH(t.heroReset || '')}">✕</button>
          <div style="position: absolute; inset: 0; background: linear-gradient(110deg, rgba(var(--nh-bg-rgb), 0.92) 0%, rgba(var(--nh-bg-rgb), 0.62) 50%, rgba(var(--nh-bg-rgb), 0.22) 100%); z-index: 1; pointer-events: none;"></div>

          <div style="position: relative; z-index: 2; flex: 1; min-width: 0; padding-right: 64px; display: flex; flex-direction: column;">
            <div style="color: var(--nh-amber); font-size: 0.85rem; font-weight: 700; letter-spacing: 0.15em; text-transform: uppercase; margin-bottom: 16px; font-family: system-ui, sans-serif;">${t.pickup}</div>
            <div class="nh-hero-title" style="font-family: var(--nh-serif); font-size: 3.4rem; font-weight: 600; line-height: 1.2; color: #ffffff; margin-bottom: 8px; padding-bottom: 4px; letter-spacing: -0.01em; text-shadow: 0 2px 10px rgba(0,0,0,0.5); display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; max-width: 100%;">${eTitle}</div>
            <div class="nh-hero-author" style="font-size: 1.25rem; color: #d8cfc2; margin-bottom: 20px; font-family: system-ui, sans-serif;">${t.by ? t.by + ' ' : ''}${eAuthor}</div>

            <div class="nh-hero-tags" style="display: ${d.tagsHtml ? 'flex' : 'none'}; gap: 10px; margin-bottom: 20px; flex-wrap: wrap; font-family: system-ui, sans-serif;">${d.tagsHtml || ''}</div>

            <div class="nh-hero-desc" style="color: #c9bfb1; font-size: 1.15rem; line-height: 1.6; margin-bottom: 32px; display: -webkit-box; -webkit-line-clamp: 4; -webkit-box-orient: vertical; overflow: hidden; font-family: system-ui, sans-serif; max-width: 90%; text-shadow: 0 1px 8px rgba(0,0,0,0.5);">
              ${eDesc}
            </div>

            <div style="display: flex; align-items: center; gap: 32px; font-family: system-ui, sans-serif;">
              ${d.hasAudio ? `<button class="nh-hero-play" style="background: var(--nh-amber); color: #14110d; border: none; border-radius: 12px; padding: 14px 32px; font-size: 1.15rem; font-weight: 600; display: flex; align-items: center; gap: 8px; cursor: pointer; transition: transform 0.2s; box-shadow: 0 0 25px var(--nh-amber-shadow);">
                <span class="material-symbols" style="font-size: 1.6rem; color: #14110d;">play_arrow</span> ${t.continue}
              </button>` : ''}
              ${d.hasEbook ? `<button class="nh-hero-read" style="background: var(--nh-amber); color: #14110d; border: none; border-radius: 12px; padding: 14px 32px; font-size: 1.15rem; font-weight: 600; display: flex; align-items: center; gap: 8px; cursor: pointer; transition: transform 0.2s; box-shadow: 0 0 25px var(--nh-amber-shadow);">
                <span class="material-symbols" style="font-size: 1.6rem; color: #14110d;">auto_stories</span> ${(window.$nuxt && window.$nuxt.$strings && window.$nuxt.$strings.ButtonRead) || 'Read'}
              </button>` : ''}

              <div style="flex: 1; max-width: 320px;">
                <div style="height: 5px; background: rgba(255,255,255,0.15); border-radius: 3px; margin-bottom: 10px; overflow: hidden;">
                  <div class="nh-hero-prog" style="height: 100%; width: ${d.progressPercent}%; background: var(--nh-amber); border-radius: 3px; transition: width 0.4s ease;"></div>
                </div>
                <div style="display: flex; justify-content: space-between; font-size: 0.95rem; color: #c9bfb1;">
                  <span class="nh-hero-prog-left" style="font-weight: 500;">${eLeft}</span>
                  <span class="nh-hero-prog-right">${eRight}</span>
                </div>
              </div>
            </div>
          </div>

          <div style="position: relative; z-index: 2; flex-shrink: 0; display: flex; align-items: center;">
            <img class="nh-hero-cover" src="${eCover}" style="height: 380px; width: auto; max-width: 420px; object-fit: contain; border-radius: 20px; box-shadow: 0 15px 40px rgba(0,0,0,0.6);" />
          </div>
        </div>
    `;
  }

  function slideMarkup(d, t) {
    return '<div class="nh-hero-slide" style="flex: 0 0 100%; min-width: 100%; box-sizing: border-box; display: flex;">' + slideInner(d, t) + '</div>';
  }

  // ------------------------------------------------------------------
  // Home-page DOM compatibility (ABS home-view modes + newer builds)
  // ------------------------------------------------------------------
  // ABS renders the home shelves in two different ways, and the theme has to work
  // in both - plus stay resilient to newer builds that restructure the card DOM:
  //
  //  • DETAIL view    : each shelf is a `.bookshelf-row` (widgets-item-slider) that
  //                     directly contains its <h2> heading and its cards.
  //  • STANDARD view  : the ABS DEFAULT (skeuomorphic). The card strip carries
  //    (skeuomorphic)   `.bookshelf-row.categorizedBookshelfRow`, but the <h2> lives
  //                     in a SIBLING placard, so `strip.querySelector('h2')` is null.
  //                     The real per-shelf container is the strip's component-root
  //                     parent, which holds both the placard <h2> and the strip; its
  //                     Vue instance also exposes `shelf.id` (continue-listening, …).
  //  • newer builds   : may drop `.bookshelf-row` entirely and expose only
  //                     `[id^="cover-area-"]` cards - derive shelves from headings.
  //
  // The hero + shelf logic below routes every shelf/card lookup through these three
  // helpers so it no longer assumes the DETAIL-view DOM. (Reported by a user on the
  // default STANDARD view: hero carousel never appeared because `.bookshelf-row`
  // rows had no <h2> inside them.)

  // Every card-like element inside a shelf. Tiered so builds that expose BOTH the
  // book-card wrapper AND its inner cover-area don't count each card twice.
  const NH_CARD_SEL = '[cy-id="card"], [id^="book-card-"]';
  function nhCardsIn(root) {
    if (!root) return [];
    let cards = Array.from(root.querySelectorAll(NH_CARD_SEL));
    if (!cards.length) cards = Array.from(root.querySelectorAll('[id^="cover-area-"]'));
    return cards;
  }

  // Stable shelf id from a shelf row's Vue instance, across both home views
  // (DETAIL exposes `shelfId`; STANDARD exposes `shelf.id`).
  function nhShelfId(row) {
    const vm = row && row.__vue__;
    if (!vm) return null;
    return vm.shelfId || (vm.$props && vm.$props.shelfId) || (vm.shelf && vm.shelf.id) || null;
  }

  // The home-page shelf rows: elements that each contain ONE shelf's heading and
  // its cards. Falls through DETAIL → STANDARD → classless newer builds.
  function nhShelfRows() {
    const scope = document.getElementById('bookshelf');
    if (!scope) return [];
    const strips = Array.from(scope.querySelectorAll('.bookshelf-row'));
    if (strips.length) {
      const rows = [];
      strips.forEach(function (strip) {
        let row = strip;
        // STANDARD view: the heading is a sibling placard, so climb to the shelf
        // component root that holds both the <h2> and this strip.
        if (!strip.querySelector('h2')) {
          let p = strip.parentElement, hops = 0;
          while (p && p !== scope && hops < 4) {
            if (p.querySelector('h2')) { row = p; break; }
            p = p.parentElement; hops++;
          }
        }
        if (rows.indexOf(row) === -1) rows.push(row);
      });
      return rows;
    }
    // Newer builds with no `.bookshelf-row` at all: anchor each shelf on its heading
    // and climb to the nearest ancestor that also holds its cards/covers.
    const anyCard = NH_CARD_SEL + ', [id^="cover-area-"]';
    const rows = [];
    scope.querySelectorAll('h2').forEach(function (h2) {
      let el = h2.parentElement, hops = 0, row = null;
      while (el && el !== scope.parentElement && hops < 8) {
        if (el.querySelector(anyCard)) { row = el; break; }
        el = el.parentElement; hops++;
      }
      if (row && rows.indexOf(row) === -1) rows.push(row);
    });
    return rows;
  }

  // ABS can draw both the home page and the library grid as skeuomorphic wooden
  // shelves - its "bookshelf view", server settings `homeBookshelfView` and
  // `bookshelfView` (STANDARD = 0, DETAIL = 1). The theme owns that layout instead,
  // so both are forced to DETAIL and their two switches are hidden from Settings →
  // General (core.js hides them by ABS's own element ids).
  // WHY, on the library grid specifically: the bookshelf view drops the card
  // captions entirely - books sit on a shelf with no title line under them - and the
  // caption line is where every rating badge lives. Measured on the sandbox: 8 badges
  // in detail view, 0 in bookshelf view. A headline feature of the theme silently
  // disappears, and there is nowhere sensible to put it back.
  // Only an admin may write server settings, so for everyone else this is a no-op and
  // the CSS normalisation of the standard HOME view carries the page until an admin
  // next opens the app. ONE attempt per load either way - never a retry loop against
  // a server that is refusing the write.
  let nhBsvDone = false;
  const NH_BSV_DETAIL = 1;
  function nhForceDetailView() {
    if (nhBsvDone) return;
    const st = window.$nuxt && window.$nuxt.$store;
    if (!st || !st.state || !st.state.serverSettings) return;
    if (!st.getters['user/getIsAdminOrUp']) { nhBsvDone = true; return; }
    const s = st.state.serverSettings;
    if (s.homeBookshelfView === NH_BSV_DETAIL && s.bookshelfView === NH_BSV_DETAIL) { nhBsvDone = true; return; }
    nhBsvDone = true;
    fetch('/api/settings', {
      method: 'PATCH',
      headers: { Authorization: 'Bearer ' + (window.__NH_TOKEN || getTokenNH()), 'Content-Type': 'application/json' },
      body: JSON.stringify({ homeBookshelfView: NH_BSV_DETAIL, bookshelfView: NH_BSV_DETAIL })
    })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => { if (j && j.serverSettings) st.commit('setServerSettings', j.serverSettings); })
      .catch(() => {});
  }

  // The left indent of a shelf's CARDS. In DETAIL view the row is the strip and
  // carries it directly; in STANDARD view the row is a bare wrapper with no padding
  // at all and the indent (an em value ABS computes from the cover size) sits on the
  // inner strip. Reading the wrapper is what put our injected rows flush against the
  // window while every ABS shelf beside them was indented.
  function nhShelfIndent(row) {
    const strip = row && (row.classList && row.classList.contains('bookshelf-row') ? row : row.querySelector('.bookshelf-row'));
    if (!strip) return null;
    const cs = getComputedStyle(strip);
    return { left: cs.paddingLeft, right: cs.paddingRight };
  }

  // STANDARD view only: the shelf title is a placard drawn on a wooden shelf edge,
  // a sibling of the strip. core.js promotes it above the strip and strips its
  // chrome; the one thing CSS cannot do is match the strip's em-based indent, so it
  // is copied here. Runs from the tick - ABS re-renders these rows freely.
  function nhStdShelfTitles() {
    document.querySelectorAll('#bookshelf .bookshelf-row.categorizedBookshelfRow').forEach(function (strip) {
      const shelf = strip.parentElement;
      if (!shelf) return;
      const placard = shelf.querySelector('.categoryPlacard');
      if (!placard) return;
      const pad = getComputedStyle(strip).paddingLeft;
      if (placard.style.paddingLeft !== pad) placard.style.paddingLeft = pad;
    });
  }

  async function injectHeroBanner() {
    // Toggle off: remove the hero and restore the stock Continue Listening shelf.
    if (nhSettings.showHeroCarousel === false) {
      const ex = document.getElementById('nh-hero-container');
      if (ex) ex.remove();
      document.querySelectorAll('[data-hero-injected="true"]').forEach(function (injectedRow) {
        injectedRow.style.display = '';
        Array.from(injectedRow.children).forEach(c => { if (c.id !== 'nh-hero-container') c.style.display = ''; });
        delete injectedRow.dataset.heroInjected;
      });
      return;
    }
    // Continue Reading shelf handling runs every tick, before any carousel guard:
    // 'hide' hides the whole row; leaving 'hide' restores it (combined rows are
    // restored by the mode-change teardown in applySettings instead).
    const nhFindShelf = function (sid) {
      return nhShelfRows().find(function (r) { return nhShelfId(r) === sid; });
    };
    const crRow = nhFindShelf('continue-reading');
    if (crRow) {
      if (nhSettings.continueReadingMode === 'hide') crRow.style.display = 'none';
      else if (crRow.style.display === 'none' && crRow.dataset.heroInjected !== 'true') crRow.style.display = '';
    }

    if (isInjectingHero || document.getElementById('nh-hero-container')) return;

    // Identify Continue Listening structurally (shelfId prop on the slider's Vue
    // instance) - header-text matching missed languages like Spanish ("Seguir
    // Escuchando"), so the carousel never appeared there. Text match kept as fallback.
    const rows = nhShelfRows();
    const row = rows.find(r => {
      const sid = nhShelfId(r);
      if (sid) return sid === 'continue-listening';
      const h = r.querySelector('h2');
      const txt = h ? h.textContent.trim().toLowerCase() : '';
      return txt.includes('continue') || txt.includes('kontynuuj') || txt.includes('weiter') || txt.includes('continu');
    });
    if (!row || row.dataset.heroInjected === 'true') return;

    let cards = nhCardsIn(row);
    if (!cards.length) return;

    // Combine mode: fold Continue Reading items into the carousel and hide that shelf.
    let crCombined = null;
    if (nhSettings.continueReadingMode === 'combine' && crRow && crRow.dataset.heroInjected !== 'true') {
      const crCards = nhCardsIn(crRow);
      if (crCards.length) {
        cards = cards.concat(crCards);
        crCombined = crRow;
      }
    }
    // Items whose progress was just reset from the carousel's own X (#18). ABS's
    // shelf keeps serving them until it refetches, so without this the slide comes
    // straight back on the next rebuild and the button looks broken.
    if (nhHeroDropped.size) {
      cards = cards.filter((c) => {
        let id = null;
        try { id = c.__vue__ && c.__vue__.libraryItem && c.__vue__.libraryItem.id; } catch (e) {}
        return !(id && nhHeroDropped.has(id));
      });
      if (!cards.length) return;
    }
    cards = cards.slice(0, 10);

    isInjectingHero = true;

    const langCode = getUserLanguage();
    const t = getTranslations(langCode);

    // B2: last visit's snapshot for this library. If the book on slide 1 is in it we
    // paint the whole carousel on this tick - no skeleton, no item-fetch wait.
    const cardIds = cards.map(nhCardItemId);
    const cacheMap = nhHeroCacheRead(langCode);
    const cachedFirst = (cacheMap && cardIds[0] && cacheMap[cardIds[0]]) || null;

    let nativeChildren = Array.from(row.children);
    if (crCombined) {
      crCombined.dataset.heroInjected = 'true';
      crCombined.style.display = 'none'; // the emptied row still paints its own box
    }
    nativeChildren.forEach(c => { c.style.display = 'none'; });
    if (!cachedFirst) {
      const skeleton = document.createElement('div');
      skeleton.id = 'nh-hero-skeleton';
      skeleton.style.marginBottom = '20px';
      skeleton.innerHTML = `
        <div style="margin-bottom:24px;padding:0 10px;">
          <div style="width:180px;height:13px;border-radius:6px;background:var(--nh-raised);opacity:.55;margin-bottom:12px;"></div>
          <div style="width:320px;height:40px;border-radius:8px;background:var(--nh-raised);opacity:.55;"></div>
        </div>
        <div style="height:436px;border-radius:24px;background:var(--nh-raised);position:relative;overflow:hidden;box-shadow:0 20px 50px rgba(0,0,0,0.5);">
          <div class="nh-skel-shimmer" style="position:absolute;inset:0;background:linear-gradient(90deg,transparent,rgba(255,255,255,0.06),transparent);transform:translateX(-100%);"></div>
        </div>
      `;
      row.appendChild(skeleton);
      try {
        const _sh = skeleton.querySelector('.nh-skel-shimmer');
        if (_sh && _sh.animate) _sh.animate([{ transform: 'translateX(-100%)' }, { transform: 'translateX(100%)' }], { duration: 1400, iterations: Infinity, easing: 'ease-in-out' });
      } catch (e) {}
    }

    const restoreNative = () => {
      const sk = document.getElementById('nh-hero-skeleton');
      if (sk) sk.remove();
      nativeChildren.forEach(c => { c.style.display = ''; });
      if (crCombined) { crCombined.style.display = ''; delete crCombined.dataset.heroInjected; }
    };

    try {
      // Slide 1 comes from the snapshot when we have it (instant); otherwise only
      // slide 1 is visible at mount, so wait for JUST its data and render the
      // carousel immediately - the other slides are empty shells filled in the
      // background - off-screen, so the fill is invisible. This cuts the carousel's
      // appearance from "all N item fetches" to "one item fetch", or none.
      const slides = new Array(cards.length).fill(null);
      let first;
      if (cachedFirst) {
        first = Object.assign({}, cachedFirst, { card: cards[0] });
      } else {
        first = await buildSlideData(cards[0], t);
        if (!first) {
          restoreNative();
          const fails = (parseInt(row.dataset.heroFails, 10) || 0) + 1;
          row.dataset.heroFails = String(fails);
          if (fails >= 2) row.dataset.heroInjected = 'true'; // stop re-trying; keep the stock shelf
          return;
        }
      }
      slides[0] = first;
      // Any other Continue Listening book we have a snapshot for is DATA now but not
      // markup yet: only slide 1 is on screen, and building all ten banners up front
      // costs ~70ms of main thread at exactly the moment the page wants to reveal.
      // They get filled on the first idle callback instead (and on demand in goTo).
      if (cacheMap) {
        for (let n = 1; n < cards.length; n++) {
          const c = cardIds[n] && cacheMap[cardIds[n]];
          if (c) slides[n] = Object.assign({}, c, { card: cards[n] });
        }
      }
      // Per-slide markup state: null = empty shell, 'cache' = snapshot markup (patch
      // it when live data lands), 'live' = built from a fresh fetch.
      const rendered = new Array(cards.length).fill(null);
      rendered[0] = cachedFirst ? 'cache' : 'live';

      const dateString = new Intl.DateTimeFormat(langCode, { weekday: 'long' }).format(new Date()).toUpperCase();
      const hour = new Date().getHours();
      let greeting = t.evening;
      if (hour >= 5 && hour < 12) greeting = t.morning;
      else if (hour >= 12 && hour < 17) greeting = t.afternoon;
      const localizedDateHeader = `${dateString} · ${greeting}`;

      let username = "";
      const accLink = document.querySelector('a[href$="/account"] span.truncate');
      if (accLink) {
        username = accLink.textContent.trim();
        username = username.charAt(0).toUpperCase() + username.slice(1);
      }
      const welcomeText = username ? `${t.welcome}, ${username}` : t.welcome;

      const multi = cards.length > 1;
      const dotsHtml = cards.map((_, n) =>
        `<button class="nh-dot" data-i="${n}" style="border: none; padding: 0; height: 8px; border-radius: 4px; cursor: pointer; background: rgba(255,255,255,0.2); width: 8px; transition: width 0.25s ease, background 0.25s ease;"></button>`
      ).join('');

      const navHtml = multi ? `
        <div id="nh-hero-nav" style="display: flex; align-items: center; justify-content: center; gap: 18px; margin-top: 22px;">
          <button class="nh-nav-arrow" data-dir="-1" style="background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.12); border-radius: 50%; width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: background 0.2s, opacity 0.2s;">
            <span class="material-symbols" style="font-size: 1.5rem; color: #d8cfc2;">chevron_left</span>
          </button>
          <div id="nh-hero-dots" style="display: flex; align-items: center; gap: 10px;">${dotsHtml}</div>
          <button class="nh-nav-arrow" data-dir="1" style="background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.12); border-radius: 50%; width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: background 0.2s, opacity 0.2s;">
            <span class="material-symbols" style="font-size: 1.5rem; color: #d8cfc2;">chevron_right</span>
          </button>
          <button id="nh-hero-pause" class="nh-nav-arrow" type="button" aria-pressed="false" style="background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.12); border-radius: 50%; width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: background 0.2s, opacity 0.2s;">
            <span class="material-symbols" style="font-size: 1.35rem; color: #d8cfc2;">pause</span>
          </button>
        </div>
      ` : '';

      const heroContainer = document.createElement('div');
      heroContainer.id = 'nh-hero-container';
      heroContainer.style.marginBottom = '20px';

      heroContainer.innerHTML = `
        <div style="margin-bottom: 24px; padding: 0 10px; display: flex; justify-content: space-between; align-items: flex-end;">
          <div>
            <div style="color: #8a8075; font-size: 0.85rem; letter-spacing: 0.15em; text-transform: uppercase; margin-bottom: 8px; font-family: system-ui, sans-serif;">${localizedDateHeader}</div>
            <h1 style="font-family: var(--nh-serif); font-size: 2.6rem; color: #f4eee2; font-weight: 500; margin: 0; line-height: 1;">${welcomeText}</h1>
          </div>
        </div>

        <div id="nh-hero-viewport" style="overflow: hidden; border-radius: 24px;">
          <div id="nh-hero-track" style="display: flex; transition: transform 0.9s ease;">
            ${slideMarkup(first, t)}${cards.slice(1).map(() => '<div class="nh-hero-slide" style="flex: 0 0 100%; min-width: 100%; box-sizing: border-box; display: flex;"></div>').join('')}
          </div>
        </div>

        ${navHtml}
      `;

      const sk = document.getElementById('nh-hero-skeleton');
      if (sk) sk.remove();
      heroContainer.style.opacity = '0';
      heroContainer.style.transition = 'opacity 0.4s ease';
      row.appendChild(heroContainer);
      row.dataset.heroInjected = 'true';
      requestAnimationFrame(() => { heroContainer.style.opacity = '1'; });

      try {
        // Compensate only the DIFFERENCE between the row's side paddings. The
        // old rule added the full left padding as our right inset - right for
        // desktop shelf rows (padding only on the left, content bleeds right)
        // but on phones the row pads BOTH sides, so the hero ended up with a
        // doubled right inset and sat 9px left of centre (Pawel's screenshot).
        const rcs = getComputedStyle(row);
        const delta = (parseFloat(rcs.paddingLeft) || 0) - (parseFloat(rcs.paddingRight) || 0);
        if (delta > 0) {
          heroContainer.style.boxSizing = 'border-box';
          heroContainer.style.width = '100%';
          heroContainer.style.paddingRight = delta + 'px';
        }
      } catch (e) {}
      nhHeroFit(heroContainer);

      const slideEls = Array.from(heroContainer.querySelectorAll('.nh-hero-slide'));
      const wireSlide = (el, d) => {
        const card = d.card;
        const banner = el.querySelector('.nh-hero-banner');
        if (!banner) return;
        // Programmatic .click() fires handlers even on v-show-hidden elements (the
        // hosting card is itself hidden), so no visibility check - routing by cy-id
        // is what keeps audio and ebook actions apart.
        const clickNative = function (sel) {
          const host = card.querySelector(sel);
          if (!host) return false;
          const b = host.querySelector('.pointer-events-auto') || host.querySelector('div, button') || host;
          b.click();
          return true;
        };
        const wireBtn = function (btn, sel) {
          if (!btn) return;
          btn.addEventListener('mouseenter', () => btn.style.transform = 'scale(1.03)');
          btn.addEventListener('mouseleave', () => btn.style.transform = 'scale(1)');
          btn.addEventListener('click', (e) => {
            e.stopPropagation();
            if (!clickNative(sel)) card.click();
          });
        };
        wireBtn(el.querySelector('.nh-hero-play'), '[cy-id="playButton"]');
        wireBtn(el.querySelector('.nh-hero-read'), '[cy-id="readButton"]');

        // Reset progress and drop this slide (#18). stopPropagation matters: the
        // whole banner is a click target that opens the book.
        const x = el.querySelector('.nh-hero-x');
        if (x) {
          x.addEventListener('click', (e) => {
            e.stopPropagation();
            e.preventDefault();
            if (x.dataset.busy) return;
            // Ask first, and let them choose whether their place survives (#20).
            nhHeroConfirm(!d.hasAudio && d.hasEbook, (choice) => {
              x.dataset.busy = '1';
              x.textContent = '…';
              const act = choice === 'reset' ? nhHeroResetProgress : nhHeroHide;
              act(d.id, (ok) => {
                if (!ok) { delete x.dataset.busy; x.textContent = '✕'; return; }
                nhHeroRebuild();
              });
            });
          });
        }

        banner.addEventListener('click', () => card.click());
      };
      wireSlide(slideEls[0], first);

      // Paint an off-screen slide from its snapshot. No-op once anything else has
      // rendered that slot, so it can never clobber fresher markup.
      const fillFromCache = (i) => {
        if (rendered[i] || !slides[i] || !slideEls[i]) return;
        slideEls[i].innerHTML = slideInner(slides[i], t);
        wireSlide(slideEls[i], slides[i]);
        rendered[i] = 'cache';
      };
      const pendingCached = [];
      for (let n = 1; n < slides.length; n++) if (slides[n]) pendingCached.push(n);
      if (pendingCached.length) {
        const idle = window.requestIdleCallback || function (fn) { return setTimeout(fn, 50); };
        idle(function () { pendingCached.forEach(fillFromCache); }, { timeout: 400 });
      }

      // Refresh pass: fill the empty shells and bring every snapshot-rendered slide up
      // to live data (progress above all) by patching in place - no teardown, no
      // flicker. buildSlideData never rejects; it degrades to card-DOM values.
      const refreshed = cards.map((card, i) => {
        // Slide 1 was already built live when there was no snapshot to paint from.
        if (i === 0 && !cachedFirst) return Promise.resolve();
        return buildSlideData(card, t).then((d) => {
          if (!d || !document.body.contains(heroContainer)) return;
          const shell = slideEls[i];
          if (!shell) return;
          const prev = slides[i];
          slides[i] = d;
          if (rendered[i] === 'cache' && prev) nhPatchSlide(shell, prev, d, t, wireSlide);
          else { shell.innerHTML = slideInner(d, t); wireSlide(shell, d); }
          rendered[i] = 'live';
        }).catch(() => {});
      });
      Promise.all(refreshed).then(() => {
        if (document.body.contains(heroContainer)) nhHeroCacheWrite(langCode, slides);
      });

      nhHomeCover = slides[0].coverUrl;
      setHomeBg(slides[0].coverUrl);

      if (multi) {
        const track = heroContainer.querySelector('#nh-hero-track');
        const dots = Array.from(heroContainer.querySelectorAll('.nh-dot'));
        const arrows = Array.from(heroContainer.querySelectorAll('.nh-nav-arrow[data-dir]'));
        let idx = 0;
        let timer = null;

        const goTo = (i) => {
          if (!document.body.contains(track)) { clearInterval(timer); return; }
          const len = slides.length;
          idx = ((i % len) + len) % len;
          fillFromCache(idx); // beat the idle callback if the user gets there first
          track.style.transform = `translateX(-${idx * 100}%)`;
          dots.forEach((d, n) => {
            const active = n === idx;
            d.style.width = active ? '24px' : '8px';
            d.style.background = active ? 'var(--nh-amber)' : 'rgba(255,255,255,0.2)';
          });
          if (slides[idx]) { // background fill may still be in flight
            nhHomeCover = slides[idx].coverUrl;
            setHomeBg(slides[idx].coverUrl);
          }
        };

        // Auto-advance is a preference, not a per-visit state: the hero is rebuilt
        // on every return to home, so a pause that lived in a local variable would
        // undo itself the moment you navigated away and back.
        let paused = false;
        try { paused = localStorage.getItem('nh-hero-paused') === '1'; } catch (e) {}

        const startTimer = () => {
          clearInterval(timer);
          const timing = parseInt(nhSettings.carouselTiming, 10);
          if (timing > 0 && !paused) {
            timer = setInterval(() => goTo(idx + 1), timing * 1000);
          }
        };

        const pauseBtn = heroContainer.querySelector('#nh-hero-pause');
        const paintPause = () => {
          if (!pauseBtn) return;
          const ic = pauseBtn.querySelector('.material-symbols');
          if (ic) ic.textContent = paused ? 'play_arrow' : 'pause';
          const lbl = paused ? (t.heroPlay || 'Play') : (t.heroPause || 'Pause');
          pauseBtn.setAttribute('aria-pressed', paused ? 'true' : 'false');
          pauseBtn.setAttribute('aria-label', lbl);
          pauseBtn.title = lbl;
          pauseBtn.classList.toggle('nh-hero-paused', paused);
        };
        if (pauseBtn) {
          paintPause();
          pauseBtn.addEventListener('click', () => {
            paused = !paused;
            try { localStorage.setItem('nh-hero-paused', paused ? '1' : '0'); } catch (e) {}
            paintPause();
            startTimer();
          });
        }

        // [data-dir], not every .nh-nav-arrow - the pause button shares the arrows'
        // styling and would otherwise be bound as a step of NaN slides.
        arrows.forEach(a => a.addEventListener('click', () => { goTo(idx + parseInt(a.dataset.dir, 10)); startTimer(); }));
        dots.forEach((d, n) => d.addEventListener('click', () => { goTo(n); startTimer(); }));

        let touchStartX = 0, touchStartY = 0, touchDeltaX = 0, touching = false;
        track.addEventListener('touchstart', (e) => {
          touching = true;
          touchStartX = e.touches[0].clientX;
          touchStartY = e.touches[0].clientY;
          touchDeltaX = 0;
          clearInterval(timer);
        }, { passive: true });
        track.addEventListener('touchmove', (e) => {
          if (!touching) return;
          touchDeltaX = e.touches[0].clientX - touchStartX;
        }, { passive: true });
        track.addEventListener('touchend', () => {
          if (!touching) return;
          touching = false;
          const threshold = 40;
          if (Math.abs(touchDeltaX) > threshold) goTo(idx + (touchDeltaX < 0 ? 1 : -1));
          startTimer();
        });

        goTo(0);
        startTimer();
      }

    } catch (e) {
      restoreNative();
      // A data-specific failure used to retry every tick: hide shelf -> fail ->
      // restore -> hide again = permanent home-page flicker + scroll jumps.
      // Log it and give up after the 2nd failure, keeping the stock shelf.
      try { console.warn('[NanoHive] hero carousel build failed:', e); } catch (e2) {}
      const fails = (parseInt(row.dataset.heroFails, 10) || 0) + 1;
      row.dataset.heroFails = String(fails);
      if (fails >= 2) row.dataset.heroInjected = 'true';
    } finally {
      isInjectingHero = false;
    }
  }

  // ==========================================
  // 5. HOME CINEMATIC BACKGROUND
  // ==========================================
  function setHomeBg(url) {
    if (!url || nhSettings.cinematicBg === false) return;
    let bg = document.getElementById('nh-home-bg');
    if (!bg) {
      bg = document.createElement('div');
      bg.id = 'nh-home-bg';
      bg.appendChild(Object.assign(document.createElement('div'), { className: 'nh-bg-layer' }));
      bg.appendChild(Object.assign(document.createElement('div'), { className: 'nh-bg-layer' }));
      document.body.insertBefore(bg, document.body.firstChild);
      bg.dataset.active = '-1';
    }
    if (bg.dataset.url === url) return;
    const layers = bg.querySelectorAll('.nh-bg-layer');
    const activeIdx = parseInt(bg.dataset.active, 10);
    const nextIdx = activeIdx === 0 ? 1 : 0;
    layers[nextIdx].style.setProperty('background-image', `url("${url}")`, 'important');
    requestAnimationFrame(() => {
      layers[nextIdx].style.opacity = '1';
      if (activeIdx >= 0 && layers[activeIdx]) layers[activeIdx].style.opacity = '0';
    });
    bg.dataset.active = String(nextIdx);
    bg.dataset.url = url;
  }

  function getHomeBgUrl() {
    if (nhHomeCover) return nhHomeCover;
    try { return localStorage.getItem('nh-home-bg') || ''; } catch (e) { return ''; }
  }

  function manageCinematic() {
    if (nhSettings.cinematicBg === false) {
      // switched off: take down anything already painted (the early shim paints
      // the cached backdrop before settings are known)
      const b = document.getElementById('nh-home-bg');
      if (b) b.remove();
      document.body.classList.remove('nh-cinematic', 'nh-cinematic-item');
      return;
    }
    const path = window.location.pathname;
    const itemPage = document.getElementById('item-page-wrapper');
    const bg = document.getElementById('nh-home-bg');

    const isSeriesDetail = /\/library\/[^/]+\/series\/[^/]+/.test(path);
    const isHome = /\/library\/[^/]+\/?$/.test(path);
    const isConfig = path.includes('/config');
    const isLibrarySub = /\/library\/[^/]+\//.test(path);

    const widen = (src) => { try { const u = new URL(src, location.origin); u.searchParams.set('width', '800'); return u.toString(); } catch (e) { return src; } };

    // Book item page → its own cover (darker). The cover URL is derived from the
    // ROUTE id immediately - waiting for Vue to render its <img> added a visible
    // beat before the backdrop started changing.
    const itemMatch = path.match(/\/item\/([^/?#]+)/);
    if (itemPage || itemMatch) {
      document.body.classList.add('nh-cinematic');
      document.body.classList.add('nh-cinematic-item');
      const id = itemMatch && itemMatch[1];
      const cur = (bg && bg.dataset.url) || '';
      if (id && cur.indexOf(id) === -1) {
        const tk = window.__NH_TOKEN || getTokenNH();
        setHomeBg(location.origin + '/api/items/' + id + '/cover?width=800' + (tk ? '&token=' + encodeURIComponent(tk) : ''));
      } else if (!id) {
        const img = itemPage && itemPage.querySelector('img[src*="/cover"]');
        if (img && img.src) setHomeBg(widen(img.src));
      }
      if (bg) bg.style.opacity = '1';
      return;
    }

    // Series detail page → the cover of the series' FIRST BOOK (darker).
    // NOT the first cover <img> in the DOM: the shelf is virtualised, so ABS
    // recycles those elements as you scroll and the "first" one is simply
    // whatever is at the top of the viewport - the backdrop changed under you
    // the whole way down the page. The first entity's id is stable, so the URL
    // it produces is stable, and setHomeBg then no-ops on every later tick.
    if (isSeriesDetail) {
      document.body.classList.add('nh-cinematic');
      document.body.classList.add('nh-cinematic-item');
      let firstId = null;
      try {
        const bs = document.getElementById('bookshelf');
        const ents = bs && bs.__vue__ && bs.__vue__.entities;
        firstId = ents && ents.length ? ents[0].id : null;
      } catch (e) {}
      const cur = (bg && bg.dataset.url) || '';
      if (firstId && cur.indexOf(firstId) === -1) {
        const tk = window.__NH_TOKEN || getTokenNH();
        setHomeBg(location.origin + '/api/items/' + firstId + '/cover?width=800' + (tk ? '&token=' + encodeURIComponent(tk) : ''));
      }
      if (bg && firstId) bg.style.opacity = '1';
      return;
    }

    // Home → carousel cover (lighter)
    if (isHome) {
      document.body.classList.add('nh-cinematic');
      document.body.classList.remove('nh-cinematic-item');
      if (nhHomeCover) { setHomeBg(nhHomeCover); try { localStorage.setItem('nh-home-bg', nhHomeCover); } catch (e) {} }
      if (bg) bg.style.opacity = '1';
      return;
    }

    // Author page → their first book's cover, in the DARKER item-page mode so
    // the artwork actually reads (user: "background should follow the first
    // book's cover"). The author portrait is /api/authors/<id>/image, so the
    // [src*="/cover"] filter below reliably grabs a BOOK cover instead.
    const isAuthor = /\/author\/[^/?#]+/.test(path);
    document.body.classList.toggle('nh-author-page', isAuthor);
    if (isAuthor) {
      document.body.classList.add('nh-cinematic');
      document.body.classList.add('nh-cinematic-item');
      const img = document.querySelector('#app-content .page img[src*="/cover"]');
      if (img && img.src) { setHomeBg(widen(img.src)); if (bg) bg.style.opacity = '1'; return; }
      const url = getHomeBgUrl();
      if (url) { setHomeBg(url); if (bg) bg.style.opacity = '1'; }
      return;
    }

    // Library sub-pages + Settings → reuse the Home background (lighter)
    if (isLibrarySub || isConfig) {
      const url = getHomeBgUrl();
      if (url) {
        document.body.classList.add('nh-cinematic');
        document.body.classList.remove('nh-cinematic-item');
        setHomeBg(url);
        if (bg) bg.style.opacity = '1';
        return;
      }
    }

    document.body.classList.remove('nh-cinematic');
    document.body.classList.remove('nh-cinematic-item');
    if (bg) bg.style.opacity = '0';
  }

  // ==========================================
  // 6. GOODREADS LINK (book detail)
  // ==========================================
  // Look this book up elsewhere. Goodreads is the global default; every language
  // that has a big local book site gets one too, and the user picks which
  // buttons they want in the customization panel (Pawel). Text pills, not remote
  // icons - the old single Goodreads button pulled its logo from a CDN, which is
  // exactly the kind of outbound dependency an offline server should not need.
  // `langs` drives BOTH the default selection and the ordering in the panel.
  const NH_BOOK_SITES = [
    { id: 'goodreads', name: 'Goodreads', langs: ['*'], url: 'https://www.goodreads.com/search?q=' },
    { id: 'storygraph', name: 'StoryGraph', langs: ['*'], url: 'https://app.thestorygraph.com/browse?search_term=' },
    { id: 'hardcover', name: 'Hardcover', langs: ['*'], url: 'https://hardcover.app/search?q=' },
    { id: 'openlibrary', name: 'Open Library', langs: ['*'], url: 'https://openlibrary.org/search?q=' },
    { id: 'lubimyczytac', name: 'Lubimyczytać', langs: ['pl'], url: 'https://lubimyczytac.pl/szukaj/ksiazki?phrase=' },
    { id: 'lovelybooks', name: 'LovelyBooks', langs: ['de'], url: 'https://www.lovelybooks.de/suche/?searchTerm=' },
    { id: 'babelio', name: 'Babelio', langs: ['fr'], url: 'https://www.babelio.com/resrecherche.php?Recherche=' },
    { id: 'casadellibro', name: 'Casa del Libro', langs: ['es'], url: 'https://www.casadellibro.com/busqueda-generica?busqueda=' },
    { id: 'hebban', name: 'Hebban', langs: ['nl'], url: 'https://www.hebban.nl/zoeken?q=' },
    { id: 'databazeknih', name: 'Databáze knih', langs: ['cs'], url: 'https://www.databazeknih.cz/search?q=' },
    { id: 'moly', name: 'Moly', langs: ['hu'], url: 'https://moly.hu/kereses?q=' },
    { id: 'livelib', name: 'LiveLib', langs: ['ru'], url: 'https://www.livelib.ru/find/' },
    { id: 'bookmeter', name: 'Bookmeter', langs: ['ja'], url: 'https://bookmeter.com/search?keyword=' },
    { id: 'douban', name: 'Douban', langs: ['zh'], url: 'https://search.douban.com/book/subject_search?search_text=' },
    { id: 'kitap1000', name: '1000Kitap', langs: ['tr'], url: 'https://1000kitap.com/arama?q=' },
    { id: 'amazon_com', name: 'Amazon.com', langs: ['en'], url: 'https://www.amazon.com/s?i=stripbooks&k=' },
    { id: 'amazon_uk', name: 'Amazon.co.uk', langs: ['en'], url: 'https://www.amazon.co.uk/s?i=stripbooks&k=' },
    { id: 'amazon_de', name: 'Amazon.de', langs: ['de'], url: 'https://www.amazon.de/s?i=stripbooks&k=' },
    { id: 'amazon_pl', name: 'Amazon.pl', langs: ['pl'], url: 'https://www.amazon.pl/s?i=stripbooks&k=' },
    { id: 'amazon_fr', name: 'Amazon.fr', langs: ['fr'], url: 'https://www.amazon.fr/s?i=stripbooks&k=' },
    { id: 'amazon_es', name: 'Amazon.es', langs: ['es'], url: 'https://www.amazon.es/s?i=stripbooks&k=' },
    { id: 'amazon_it', name: 'Amazon.it', langs: ['it'], url: 'https://www.amazon.it/s?i=stripbooks&k=' },
    { id: 'amazon_nl', name: 'Amazon.nl', langs: ['nl'], url: 'https://www.amazon.nl/s?i=stripbooks&k=' },
    { id: 'amazon_jp', name: 'Amazon.co.jp', langs: ['ja'], url: 'https://www.amazon.co.jp/s?i=stripbooks&k=' },
    { id: 'amazon_br', name: 'Amazon.com.br', langs: ['pt'], url: 'https://www.amazon.com.br/s?i=stripbooks&k=' },
    { id: 'audible', name: 'Audible', langs: ['*'], url: 'https://www.audible.com/search?keywords=' },
  ];

  function nhBookSiteById(id) { return NH_BOOK_SITES.filter((s) => s.id === id)[0] || null; }

  // Default when the user has not chosen: Goodreads plus the biggest local site
  // for the interface language (Pawel: "goodreads and one local one").
  function nhBookSitesDefault() {
    const lang = getUserLanguage().split('-')[0].toLowerCase();
    const forLang = NH_BOOK_SITES.filter((s) => s.langs.indexOf(lang) !== -1);
    // Prefer a real local book community (Lubimyczytać, Babelio…); fall back to
    // that language's Amazon store, which is all some languages have here.
    const local = forLang.filter((s) => s.id.indexOf('amazon') !== 0)[0] || forLang[0];
    const ids = ['goodreads'];
    if (local) ids.push(local.id);
    return ids;
  }
  function nhBookSitesSelected() {
    const v = nhSettings.bookSites;
    if (Array.isArray(v)) return v;          // [] is a valid choice: show none
    return nhBookSitesDefault();
  }

  // Site ids that have a bundled logo under theme/booksites (served at
  // /_nh/booksites/<id>.png). Anything not listed falls back to a monogram.
  const NH_BS_LOGOS = ["amazon_br","amazon_com","amazon_de","amazon_es","amazon_fr","amazon_it","amazon_jp","amazon_nl","amazon_pl","amazon_uk","audible","babelio","bookmeter","casadellibro","databazeknih","douban","goodreads","hardcover","hebban","kitap1000","livelib","lovelybooks","lubimyczytac","moly","openlibrary","storygraph"];

  function injectBookSites() {
    const wrapper = document.getElementById('item-page-wrapper');
    if (!wrapper) return;
    const h1 = wrapper.querySelector('h1');
    if (!h1) return;
    // The primary action button is `.abs-btn`; only OLDER ABS builds also give
    // it `.bg-success` (the previous selector required that and quietly did
    // nothing on current builds).
    const playBtn = wrapper.querySelector('.abs-btn.bg-success') || wrapper.querySelector('.abs-btn');
    if (!playBtn) return;

    let row = playBtn.parentElement;
    while (row && row !== wrapper && !(row.classList.contains('flex') && row.querySelectorAll('button, a').length >= 1)) {
      row = row.parentElement;
    }
    if (!row || row === wrapper) row = playBtn.closest('.flex');
    if (!row) return;

    const title = h1.textContent.trim();
    if (!title) return;
    const subEl = wrapper.querySelector('[cy-id="subtitle"], .nh-subtitle, [class*="subtitle"]');
    const subtitle = subEl ? subEl.textContent.trim() : '';
    const authEl = wrapper.querySelector('p.mb-2 a') || wrapper.querySelector('p.mb-2');
    const author = authEl ? authEl.textContent.trim() : '';
    const q = encodeURIComponent([title, subtitle, author].filter(Boolean).join(' '));

    const old = wrapper.querySelector('a[data-nh-goodreads]'); // the pre-2.0 icon button
    if (old) old.remove();

    const ids = nhBookSitesSelected();
    const sig = ids.join(',') + '|' + q;
    // Pawel: these belong IN the action row as small square icon buttons, the
    // same shape as the edit pencil - text pills next to it looked bolted on.
    let box = wrapper.querySelector('.nh-bs-row');
    if (box && box.dataset.sig === sig) return;
    if (box) box.remove();
    if (!ids.length) return;
    box = document.createElement('span');
    box.className = 'nh-bs-row';
    box.dataset.sig = sig;
    ids.forEach((id) => {
      const site = nhBookSiteById(id);
      if (!site) return;
      const a = document.createElement('a');
      a.className = 'icon-btn nh-bs-btn';
      a.href = site.url + q;
      a.target = '_blank';
      a.rel = 'noopener noreferrer';
      a.title = (nhGsT().bsFind || PANEL_T.en.bsFind) + ' ' + site.name;
      // A flat one-colour monogram, NOT the service's favicon. The favicons were
      // full-colour logos on a filled tile, so these two buttons were the only
      // things in the action row that did not read as outlined icon buttons
      // (Pawel). Dropping them also removes an outbound request to each site on
      // every book page, which matters for a server with no internet access.
      // The site name stays in the title attribute.
      // Ship a real logo where we have one. The files are white-on-transparent,
      // so they are used as a CSS MASK rather than an <img>: the glyph then takes
      // the button's own colour and its amber hover, exactly like the material
      // icons next to it, instead of being a second visual language. Sites with
      // no logo keep the monogram.
      if (NH_BS_LOGOS.indexOf(site.id) !== -1) {
        const lg = document.createElement('span');
        lg.className = 'nh-bs-logo';
        lg.style.setProperty('--nh-bs-logo', 'url("/_nh/booksites/' + site.id + '.png")');
        a.appendChild(lg);
      } else {
        const letter = document.createElement('span');
        letter.className = 'nh-bs-letter';
        letter.textContent = site.name.charAt(0).toUpperCase();
        a.appendChild(letter);
      }
      box.appendChild(a);
    });
    // Sit directly after "mark as finished" and therefore BEFORE the 3-dot menu,
    // rather than trailing the whole row. Anchored on the beenhere glyph; if a
    // build has no such button we fall back to the old append.
    const finished = Array.prototype.find.call(row.children, (c) => {
      const g = c.querySelector('.material-symbols');
      return g && g.textContent.trim() === 'beenhere';
    });
    if (finished && finished.nextSibling) row.insertBefore(box, finished.nextSibling);
    else if (finished) row.appendChild(box);
    else row.appendChild(box);
  }

  // ---- Who's reading this (#23) ----
  // A facepile button in the action row, sitting before the book-site links:
  // it exists only when at least one OTHER user has this book in progress or
  // finished (data from /_nh/api/progress?item=, the shared store each browser
  // feeds - see nhFamilyProgressSync). Click opens a popup listing everyone.
  const nhRd = { key: '', rows: null, fetching: false };

  function nhRdAgo(ts) {
    if (!ts) return '';
    try {
      const rtf = new Intl.RelativeTimeFormat(getUserLanguage() || 'en', { numeric: 'auto' });
      const d = Date.now() - ts;
      const min = 60000, hr = 3600000, day = 86400000;
      if (d < hr) return rtf.format(-Math.max(1, Math.round(d / min)), 'minute');
      if (d < day) return rtf.format(-Math.round(d / hr), 'hour');
      if (d < 30 * day) return rtf.format(-Math.round(d / day), 'day');
      if (d < 365 * day) return rtf.format(-Math.round(d / (30 * day)), 'month');
      return rtf.format(-Math.round(d / (365 * day)), 'year');
    } catch (e) {
      return new Date(ts).toLocaleDateString();
    }
  }

  function nhRdMyId() {
    try { const u = window.$nuxt.$store.state.user.user; return (u && u.id) || ''; } catch (e) { return ''; }
  }

  function nhRdAv(uid, name, cls) {
    const av = document.createElement('span');
    av.className = cls;
    av.textContent = (name || '?').charAt(0).toUpperCase();
    nhAvatarInto(av, uid);
    return av;
  }

  function nhRdOpen() {
    const T = nhGsT();
    const old = document.getElementById('nh-rd-modal'); if (old) old.remove();
    const overlay = document.createElement('div'); overlay.id = 'nh-rd-modal';
    const bg = document.createElement('div'); bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div'); box.className = 'nh-rt-modal-box';
    const close = () => overlay.remove();
    bg.addEventListener('click', close);
    const head = document.createElement('div'); head.className = 'nh-rt-modal-head';
    const title = document.createElement('span'); title.textContent = T.rdTitle || PANEL_T.en.rdTitle;
    const x = document.createElement('button'); x.type = 'button'; x.className = 'nh-rt-modal-x'; x.textContent = '×';
    x.addEventListener('click', close);
    head.appendChild(title); head.appendChild(x);
    const list = document.createElement('div'); list.className = 'nh-rd-list';
    (nhRd.rows || []).forEach((row) => {
      const li = document.createElement('div'); li.className = 'nh-rd-row';
      li.appendChild(nhRdAv(row.uid, row.name, 'nh-rd-av'));
      const name = document.createElement('span'); name.className = 'nh-rd-name'; name.textContent = row.name;
      if (row.out) { // admin view: user opted out, marked instead of hidden
        const off = document.createElement('span');
        off.className = 'material-symbols nh-sb-opted';
        off.textContent = 'visibility_off';
        off.title = T.socOpted || PANEL_T.en.socOpted;
        name.appendChild(off);
      }
      li.appendChild(name);
      const stat = document.createElement('span'); stat.className = 'nh-rd-stat';
      if (row.fin) {
        const fin = document.createElement('span'); fin.className = 'nh-rd-fin';
        fin.textContent = T.rdFin || PANEL_T.en.rdFin;
        stat.appendChild(fin);
      } else {
        const bar = document.createElement('span'); bar.className = 'nh-rd-bar';
        const fill = document.createElement('span'); fill.className = 'nh-rd-fill';
        fill.style.width = Math.round(row.p * 100) + '%';
        bar.appendChild(fill);
        const pct = document.createElement('span'); pct.className = 'nh-rd-pct';
        pct.textContent = Math.round(row.p * 100) + '%';
        stat.appendChild(bar); stat.appendChild(pct);
      }
      const when = document.createElement('span'); when.className = 'nh-rd-when'; when.textContent = nhRdAgo(row.ts);
      stat.appendChild(when);
      li.appendChild(stat);
      list.appendChild(li);
    });
    box.appendChild(head); box.appendChild(list);
    overlay.appendChild(bg); overlay.appendChild(box);
    document.body.appendChild(overlay);
  }

  function nhWhoReading() {
    const wrapper = document.getElementById('item-page-wrapper');
    const m = window.location.pathname.match(/\/item\/([A-Za-z0-9_-]+)/);
    if (!wrapper || !m) { nhRd.key = ''; nhRd.rows = null; return; }
    // #25: off by default now - the server decides whether this row exists
    const soc = nhSocEff();
    if (!soc || !soc.whoReading) {
      const fp0 = wrapper.querySelector('#nh-rd-fp');
      if (fp0) fp0.remove();
      return;
    }
    const itemId = m[1];
    nhScFetch(); // the avatars map rides the series-meta payload

    if (nhRd.key !== itemId) {
      if (nhRd.fetching) return;
      const tok = nhSrToken();
      if (!tok) return;
      nhRd.fetching = true;
      // Admins read the gated twin: it includes opted-out users (flagged) and
      // is never dark - they see everything, marked (Pawel).
      const rdEp = isUserAdmin() ? '/_nh/api/progress-admin?item=' : '/_nh/api/progress?item=';
      fetch(rdEp + encodeURIComponent(itemId), { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => {
          nhRd.fetching = false;
          nhRd.key = itemId;
          const users = (j && j.users) || {};
          const me = nhRdMyId();
          const rows = [];
          Object.keys(users).forEach((uid) => {
            if (uid === me) return;
            const u = users[uid] || {};
            rows.push({ uid: uid, name: u.user || '?', p: u.f ? 1 : (Number(u.p) || 0), fin: !!u.f, ts: Number(u.ts) || 0, out: !!u.out });
          });
          // in-progress first, then finished, each newest first
          rows.sort((a, b) => (a.fin - b.fin) || (b.ts - a.ts));
          nhRd.rows = rows;
        })
        .catch(() => { nhRd.fetching = false; nhRd.key = itemId; nhRd.rows = []; });
      return; // rendered on the tick after the data lands
    }

    const existing = wrapper.querySelector('#nh-rd-fp');
    if (!nhRd.rows || !nhRd.rows.length) { if (existing) existing.remove(); return; }
    const bsRow = wrapper.querySelector('.nh-bs-row');
    const sig = itemId + '|' + nhRd.rows.map((r) => r.uid + ':' + (r.fin ? 'f' : r.p) + (r.out ? '!' : '')).join(',') + '|' + (nhSc.ts || 0);
    if (existing && existing.dataset.sig === sig) {
      // injectBookSites re-inserts its row right after the beenhere button, which
      // can leapfrog it in front of this one - keep the facepile before it.
      if (bsRow && existing.nextElementSibling !== bsRow && bsRow.parentElement) bsRow.parentElement.insertBefore(existing, bsRow);
      return;
    }
    if (existing) existing.remove();

    const T = nhGsT();
    const fp = document.createElement('button');
    fp.type = 'button';
    fp.id = 'nh-rd-fp';
    fp.className = 'nh-rd-btn';
    fp.dataset.sig = sig;
    fp.title = T.rdTitle || PANEL_T.en.rdTitle;
    const shown = nhRd.rows.slice(0, nhRd.rows.length > 3 ? 2 : 3);
    shown.forEach((r) => fp.appendChild(nhRdAv(r.uid, r.name, 'nh-rd-fp-av')));
    if (nhRd.rows.length > shown.length) {
      const more = document.createElement('span');
      more.className = 'nh-rd-fp-av nh-rd-fp-more';
      more.textContent = '+' + (nhRd.rows.length - shown.length);
      fp.appendChild(more);
    }
    fp.addEventListener('click', nhRdOpen);

    if (bsRow && bsRow.parentElement) { bsRow.parentElement.insertBefore(fp, bsRow); return; }
    // No book-site links chosen: anchor the same way injectBookSites does.
    const playBtn = wrapper.querySelector('.abs-btn.bg-success') || wrapper.querySelector('.abs-btn');
    if (!playBtn) { fp.remove(); return; }
    let row = playBtn.parentElement;
    while (row && row !== wrapper && !(row.classList.contains('flex') && row.querySelectorAll('button, a').length >= 1)) {
      row = row.parentElement;
    }
    if (!row || row === wrapper) row = playBtn.closest('.flex');
    if (!row) { fp.remove(); return; }
    const finished = Array.prototype.find.call(row.children, (c) => {
      const g = c.querySelector('.material-symbols');
      return g && g.textContent.trim() === 'beenhere';
    });
    if (finished && finished.nextSibling) row.insertBefore(fp, finished.nextSibling);
    else row.appendChild(fp);
  }

  // ==========================================
  // 10. CUSTOM EXPANDED "RECENT SERIES" SHELF
  // ==========================================
  let nhRecentSeries = { key: '', loading: false, data: null };

  function getBaseNH() {
    const p = window.location.pathname;
    const i = p.indexOf('/library/');
    if (i >= 0) return p.slice(0, i);
    try { const bp = window.__NUXT__ && window.__NUXT__.config && window.__NUXT__.config.routerBasePath; if (bp) return bp.replace(/\/$/, ''); } catch (e) {}
    return '';
  }
  function getLibIdNH() {
    const m = window.location.pathname.match(/\/library\/([^/]+)/);
    if (m) return m[1];
    // ABS 2.36 stopped persisting its vuex blob to localStorage, so the old
    // fallback here always returned '' - read the live store first.
    try {
      const id = window.$nuxt && window.$nuxt.$store && window.$nuxt.$store.state.libraries.currentLibraryId;
      if (id) return id;
    } catch (e) {}
    try { return JSON.parse(localStorage.getItem('vuex') || '{}')?.libraries?.currentLibraryId || ''; } catch (e) { return ''; }
  }
  function getTokenNH() {
    try {
      if (window.$nuxt && window.$nuxt.$store && window.$nuxt.$store.state.user && window.$nuxt.$store.state.user.token) return window.$nuxt.$store.state.user.token;
      return localStorage.getItem('token') || JSON.parse(localStorage.getItem('vuex') || '{}')?.user?.token || '';
    } catch (e) { return ''; }
  }
  function escapeHtmlNH(s) {
    return String(s == null ? '' : s).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
  }

  // HTML -> plain text for descriptions, which ABS stores as markup. Parsed with
  // DOMParser into an INERT document: it has no browsing context, so nothing in the
  // string can load a resource or run a handler on the way past. The old way was to
  // assign it to a detached <div>'s innerHTML and read textContent back, that div
  // still belongs to the live document, and whether an <img src=x onerror> in it
  // fires is left to the engine. Chrome did not; nothing says the next one will not.
  // NOTE this returns TEXT, and text is not safe to interpolate into HTML, an
  // `&lt;img …&gt;` in the source decodes to real markup here. Escape at the sink.
  function nhHtmlToText(html, keepBreaks) {
    const src = keepBreaks
      ? String(html).replace(/<br\s*\/?>/gi, '\n').replace(/<\/p>/gi, '\n\n')
      : String(html);
    try {
      const doc = new DOMParser().parseFromString(src, 'text/html');
      return (doc.body && doc.body.textContent) || '';
    } catch (e) {
      // No DOMParser (should not happen in any supported browser): strip tags crudely
      // rather than fall back to a live-document parse.
      return src.replace(/<[^>]*>/g, '');
    }
  }

  async function fetchRecentSeries(libId, count) {
    const base = getBaseNH();
    const token = getTokenNH();
    const url = `${base}/api/libraries/${libId}/series?sort=addedAt&desc=1&limit=${count}&page=0`;
    const res = await fetch(url, { headers: token ? { Authorization: 'Bearer ' + token } : {}, credentials: 'include' });
    if (!res.ok) return null;
    const d = await res.json();
    await nhScReady(); // custom series covers (A1), have the map for the first render
    return (d.results || []).map(s => {
      const books = s.books || [];
      const covers = books.slice(0, 3).map(b => `${base}/api/items/${b.id || b.libraryItemId}/cover?width=400`);
      // book ids feed the completion badge (#13); progress itself is read live at
      // render time, so this cached list never goes stale as you listen.
      const bookIds = books.map(b => b.id || b.libraryItemId).filter(Boolean);
      return { id: s.id, name: s.name, count: books.length, covers, bookIds };
    });
  }

  function findNativeRecentSeriesRow() {
    return nhShelfRows().find(r => {
      if (nhShelfId(r) === 'recent-series') return true;
      const h2 = r.querySelector('h2'); if (!h2) return false;
      const t = h2.textContent.trim().toLowerCase();
      return t.includes('recent series') || t.includes('ostatnie serie') || t.includes('najnowsze serie');
    }) || null;
  }

  function renderRecentSeriesInner(list, libId, base) {
    const lang = getUserLanguage().split('-')[0].toLowerCase();
    const titleMap = { en: 'Recent Series', pl: 'Ostatnie serie', de: 'Neue Serien', fr: 'Séries récentes' };
    const title = titleMap[lang] || titleMap.en;
    const token = getTokenNH();
    const tq = token ? '&token=' + encodeURIComponent(token) : '';
    const layers = ['c1', 'c2', 'c3'];
    const cards = list.map(s => {
      // Custom series cover (A1): the uploaded image becomes the FRONT of the
      // stack, books #1/#2 still peek out behind, matching stock series.
      const custom = nhScUrl(s.id);
      const base3 = (s.covers || []).slice(0, 3);
      const cv = custom ? [custom].concat(base3.slice(0, 2)) : base3;
      let inner = cv.length
        ? cv.map((url, i) => {
            const isCustom = custom && i === 0;
            const q = isCustom ? '' : tq; // custom covers are public, no token needed
            return `<div class="nh-rs-cover ${layers[i]}${isCustom ? ' custom' : ''}"><i class="nh-rs-bg" style="background-image:url('${url}${q}')"></i><i class="nh-rs-fg" style="background-image:url('${url}${q}')"></i></div>`;
          }).join('')
        : `<div class="nh-rs-cover c1" style="background:var(--nh-raised)"></div>`;
      const route = `/library/${libId}/series/${s.id}`;
      // Completion badge (#13). Built here rather than by the tick decorator
      // because this row is rendered as one HTML string, not from live cards.
      const sp = nhSettings.seriesProgressBadge === false ? null : nhSpState(s.bookIds);
      const spHtml = sp
        ? `<div class="nh-sp nh-sp-${sp.state}" title="${escapeHtmlNH(nhSpLabel(sp))}" aria-label="${escapeHtmlNH(nhSpLabel(sp))}">✓</div>`
        : '';
      return `<a class="nh-rs-card" data-route="${route}" href="${base}${route}">
        <div class="nh-rs-covers">${inner}<div class="nh-rs-count">${s.count}</div>${spHtml}</div>
        <p class="nh-rs-name">${escapeHtmlNH(s.name)}</p>
      </a>`;
    }).join('');
    return `<h2 class="nh-rs-heading">${escapeHtmlNH(title)}</h2><div class="nh-rs-scroll">${cards}</div>`;
  }

  function injectRecentSeriesStyles() {
    if (document.getElementById('nh-recent-series-style')) return;
    const s = document.createElement('style');
    s.id = 'nh-recent-series-style';
    s.textContent = `
      #nh-recent-series-row { margin: 0 0 2.2rem; box-sizing: border-box; width: 100%; }
      #nh-recent-series-row .nh-rs-heading { font-family: var(--nh-serif) !important; font-weight: 500; font-size: 1.55rem; letter-spacing: -0.01em; color: var(--nh-text-1, #f4eee2); margin: 0 0 1rem; }
      #nh-recent-series-row .nh-rs-scroll { display: flex; flex-wrap: nowrap; gap: calc(var(--nh-rs-cw, 140px) * 0.076); overflow-x: auto; overflow-y: hidden; padding: 6px 2px 14px; scrollbar-width: none; -ms-overflow-style: none; }
      #nh-recent-series-row .nh-rs-scroll::-webkit-scrollbar { display: none; height: 0; width: 0; }
      #nh-recent-series-row .nh-rs-card { flex: 0 0 auto; width: calc(var(--nh-rs-cw, 140px) * 1.171); text-decoration: none; }
      #nh-recent-series-row .nh-rs-covers { position: relative; width: var(--nh-rs-cw, 140px); height: var(--nh-rs-cw, 140px); margin-bottom: 8px; overflow: visible; }
      #nh-recent-series-row .nh-rs-cover { position: absolute; top: 0; left: 0; width: var(--nh-rs-cw, 140px); height: var(--nh-rs-cw, 140px); border-radius: 12px; overflow: hidden; background-color: var(--nh-raised); box-shadow: 0 10px 24px rgba(0,0,0,0.42); transition: filter .2s ease, box-shadow .2s ease; }
      #nh-recent-series-row .nh-rs-bg, #nh-recent-series-row .nh-rs-fg { position: absolute; inset: 0; display: block; background-position: center; background-repeat: no-repeat; border-radius: inherit; }
      #nh-recent-series-row .nh-rs-bg { background-size: cover; filter: blur(14px) brightness(0.85); transform: scale(1.15); }
      #nh-recent-series-row .nh-rs-fg { background-size: contain; }
      #nh-recent-series-row .nh-rs-cover.custom .nh-rs-fg { background-size: cover; }
      html.nh-covers-std #nh-recent-series-row .nh-rs-covers { height: calc(var(--nh-rs-cw, 140px) * 1.6); }
      html.nh-covers-std #nh-recent-series-row .nh-rs-cover { height: calc(var(--nh-rs-cw, 140px) * 1.6); }
      #nh-recent-series-row .nh-rs-cover.c1 { transform: translate(0,0); z-index: 3; }
  /* Only the FRONT cover keeps the deep 0 10px 24px shadow. The back layers sit
     lowest, so theirs is what reached the title, short and tight here, which is
     what lets the caption ride 10px higher without landing in it (Pawel). */
  #nh-recent-series-row .nh-rs-cover.c2, #nh-recent-series-row .nh-rs-cover.c3 { box-shadow: 0 4px 10px rgba(0,0,0,0.38); }
      #nh-recent-series-row .nh-rs-cover.c2 { transform: translate(calc(var(--nh-rs-cw, 140px) * 0.086), 0); z-index: 2; filter: brightness(0.78); }
      #nh-recent-series-row .nh-rs-cover.c3 { transform: translate(calc(var(--nh-rs-cw, 140px) * 0.171), 0); z-index: 1; filter: brightness(0.60); }
      #nh-recent-series-row .nh-rs-card:hover .nh-rs-cover.c1 { filter: brightness(0.7); box-shadow: 0 10px 24px rgba(0,0,0,0.42); }
      #nh-recent-series-row .nh-rs-count { position: absolute; left: 8px; top: 8px; z-index: 5; background: rgba(255,255,255,0.55); backdrop-filter: blur(10px) brightness(1.2) saturate(1.05); -webkit-backdrop-filter: blur(10px) brightness(1.2) saturate(1.05); border: 1px solid rgba(255,255,255,0.35); box-shadow: 0 2px 8px rgba(0,0,0,0.4); border-radius: 999px; min-width: 1.7em; text-align: center; padding: 0.12em 0.45em; color: #000; font-weight: 700; font-size: clamp(8px, calc(var(--nh-rs-cw, 140px) * 0.078), 12.5px); font-family: var(--nh-sans, system-ui); }
      #nh-recent-series-row .nh-rs-name { font-family: var(--nh-serif) !important; font-weight: 500; color: var(--nh-text-2, #d8cfc2); font-size: var(--nh-rs-fs, 1rem); margin: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
    `;
    document.head.appendChild(s);
  }

  function nhRouterPush(route) {
    // Page-world path: direct router access (proxy injection, userscript in page context).
    try { if (window.$nuxt && window.$nuxt.$router) { window.$nuxt.$router.push(route); return true; } } catch (e) {}
    try {
      const app = document.getElementById('__layout') || document.getElementById('__nuxt');
      const vue = app && (app.__vue__ || (app.firstElementChild && app.firstElementChild.__vue__));
      if (vue && vue.$router) { vue.$router.push(route); return true; }
    } catch (e) {}
    // Isolated-world path (Chrome content script): window.$nuxt is unreachable, so drive
    // Vue Router through the History API. It listens for popstate and navigates
    // client-side without a full reload, so the media player survives.
    try {
      const base = getBaseNH().replace(/\/$/, '');
      const url = base + route;
      if (window.history && typeof window.history.pushState === 'function') {
        window.history.pushState({}, '', url);
        window.dispatchEvent(new PopStateEvent('popstate', { state: {} }));
        return true;
      }
    } catch (e) {}
    return false;
  }

  // Re-centre the ABS virtual grid. ABS lays each card out in a slot that is the
  // card PLUS a trailing gap, and centres those slots inside a width that also
  // holds back room for a scrollbar; both live only on the right, so every row
  // lands left of true centre (measured 17px at 360px/1 column and at
  // 900px/3 columns alike). The offset is measured, not hardcoded: it depends on
  // ABS's gap and on whether the scroller is actually showing a scrollbar.
  // Only the `left` of each card is touched, its inline translate3d and the Vue
  // sizing chain (entitiesPerShelf / bookshelfMarginLeft) are left alone, since
  // overriding those broke card widths and collapsed the series view on resize.
  function nhShelfNudge() {
    const bs = document.getElementById('bookshelf');
    if (!bs || document.body.classList.contains('nh-home')) return;
    let vm = null;
    try { vm = bs.__vue__; } catch (e) {}
    // Derived from the shelf's INTENDED geometry (entitiesPerShelf slots), never
    // from measuring rendered cards. Measuring looked equivalent but was not: a
    // filtered result of two books got centred as a two-card block, landing at a
    // different left edge than the unfiltered grid (Pawel's screenshots). The
    // intended row is eps slots whether or not they are filled, so filtered and
    // unfiltered margins are now identical by construction, and there is no
    // measure-apply feedback loop left to converge.
    if (!vm || !vm.entitiesPerShelf || !vm.totalEntityCardWidth || typeof vm.bookshelfMarginLeft !== 'number') return;
    const eps = vm.entitiesPerShelf;
    const tec = vm.totalEntityCardWidth;                    // card + trailing gap
    const cw = vm.cardWidth || vm.entityWidth || (tec - 24);
    const block = (eps - 1) * tec + cw;                     // no gap after the last card
    const want = (bs.clientWidth - block) / 2;
    // Clamp kept as a guard against a vm caught mid-rebuild reporting nonsense.
    const next = Math.max(-60, Math.min(60, Math.round(want - vm.bookshelfMarginLeft)));
    const cur = parseFloat(bs.style.getPropertyValue('--nh-shelf-nudge')) || 0;
    // 2px deadband, without it a sub-pixel layout oscillates the var every tick.
    if (Math.abs(next - cur) >= 2) bs.style.setProperty('--nh-shelf-nudge', next + 'px');
  }

  function applyRecentSeriesSize() {
    const row = document.getElementById('nh-recent-series-row');
    if (!row) return;
    let w = 0;
    // Use the same rendered size as a real (visible) book cover. Skip hidden
    // Continue-Listening covers (the hero replaces them, so they measure 0).
    const els = document.querySelectorAll('#bookshelf [id^="cover-area-"]');
    for (const el of els) {
      if (el.closest('#nh-hero-container') || el.closest('#nh-recent-series-row')) continue;
      const r = el.getBoundingClientRect();
      if (r.width > 40) { w = r.width; break; }
    }
    if (!w) { try { w = (window.$nuxt && window.$nuxt.$store && window.$nuxt.$store.state.globals && window.$nuxt.$store.state.globals.bookshelfBookWidth) || 0; } catch (e) {} }
    if (w > 40) row.style.setProperty('--nh-rs-cw', Math.round(w) + 'px'); // square: width === height in CSS

    // Match the series title font to the book card titles (they scale with the size button too).
    const titles = document.querySelectorAll('#bookshelf [cy-id="title"]');
    for (const t of titles) {
      if (t.closest('#nh-hero-container') || t.closest('#nh-recent-series-row')) continue;
      if (t.getBoundingClientRect().width > 10) { row.style.setProperty('--nh-rs-fs', getComputedStyle(t).fontSize); break; }
    }
  }

  async function manageRecentSeries() {
    try {
      const homeOk = /\/library\/[^/]+\/?$/.test(window.location.pathname);
      if (!homeOk || !nhSettings.showCustomRecentSeries || nhSettings.customSeriesCards === false || nhSettings.hideHomeRecentSeries) {
        const ex = document.getElementById('nh-recent-series-row');
        if (ex) ex.remove();
        return;
      }
      const libId = getLibIdNH();
      if (!libId) return;
      const count = Math.max(1, parseInt(nhSettings.recentSeriesCount, 10) || 12);
      const key = libId + ':' + count;

      if (nhRecentSeries.key !== key && !nhRecentSeries.loading) {
        nhRecentSeries.loading = true;
        const data = await fetchRecentSeries(libId, count);
        nhRecentSeries = { key, loading: false, data };
        const stale = document.getElementById('nh-recent-series-row');
        if (stale) stale.remove();
      }
      if (nhRecentSeries.loading || !nhRecentSeries.data || !nhRecentSeries.data.length) return;

      if (document.getElementById('nh-recent-series-row')) {
        // Row already built. Re-park it before the (hidden) native Recent Series
        // row every cycle so it never strands at the bottom when the native row
        // mounts late, and re-copy padding to track ABS's cover-size slider zoom.
        const existing = document.getElementById('nh-recent-series-row');
        const nativeRow = findNativeRecentSeriesRow();
        if (nativeRow && nativeRow.parentNode && existing.nextElementSibling !== nativeRow) {
          nativeRow.parentNode.insertBefore(existing, nativeRow);
        }
        try {
          const sib = nativeRow || nhShelfRows().find(r => r.id !== 'nh-recent-series-row');
          const pad = nhShelfIndent(sib);
          if (pad) { existing.style.paddingLeft = pad.left; existing.style.paddingRight = pad.right; }
        } catch (e) {}
        // Refresh the completion badges in place (#13). The row itself is built
        // once per library, so without this a series you just finished would keep
        // its amber tick until the row happened to be rebuilt.
        try {
          const byId = {};
          (nhRecentSeries.data || []).forEach((s) => { byId[s.id] = s.bookIds; });
          existing.querySelectorAll('.nh-rs-card').forEach((a) => {
            const covers = a.querySelector('.nh-rs-covers');
            const sid = ((a.dataset.route || '').split('/series/')[1] || '').split(/[?#]/)[0];
            if (!covers || !sid) return;
            const st = nhSettings.seriesProgressBadge === false ? null : nhSpState(byId[sid]);
            nhSpPaint(covers, st, sid + ':' + (st ? st.state + st.done + '/' + st.n : 'none'));
          });
        } catch (e) {}
        return;
      }

      injectRecentSeriesStyles();
      const base = getBaseNH();
      const nativeRow = findNativeRecentSeriesRow();
      const row = document.createElement('div');
      row.id = 'nh-recent-series-row';
      row.dataset.key = key;
      row.innerHTML = renderRecentSeriesInner(nhRecentSeries.data, libId, base);

      if (nativeRow && nativeRow.parentNode) nativeRow.parentNode.insertBefore(row, nativeRow);
      else { const bs = document.getElementById('bookshelf'); if (bs) bs.appendChild(row); else return; }

      try {
        const sib = nativeRow || nhShelfRows().find(r => r.id !== 'nh-recent-series-row');
        const pad = nhShelfIndent(sib);
        if (pad) { row.style.paddingLeft = pad.left; row.style.paddingRight = pad.right; }
      } catch (e) {}

      // Navigation is handled by a single delegated listener (nhBindRecentSeriesNav),
      // not per-card handlers, the row's innerHTML is rebuilt on data changes, which
      // would strip listeners bound to individual <a> elements and let the browser
      // follow the raw href, reloading the page and killing playback.
      nhBindRecentSeriesNav();
      // Scroll arrows, the ones ABS gives its own shelves (#21).
      nhShelfArrows(row, '.nh-rs-heading', '.nh-rs-scroll');
    } catch (e) {}
  }

  // One capture-phase listener on a stable ancestor. Intercepts clicks on any current
  // or future .nh-rs-card and routes them client-side, so the media player survives.
  // Tagged on the node itself (not a module flag) so it re-binds if #bookshelf is
  // replaced on library switches.
  function nhBindRecentSeriesNav() {
    const host = document.getElementById('bookshelf');
    if (!host || host.dataset.nhRsNav === '1') return;
    host.dataset.nhRsNav = '1';
    host.addEventListener('click', (e) => {
      const card = e.target.closest && e.target.closest('.nh-rs-card');
      if (!card) return;
      const rt = card.getAttribute('data-route');
      if (rt && nhRouterPush(rt)) { e.preventDefault(); e.stopPropagation(); }
    }, true);
  }

  // Series deck scaling. Baseline constants (196/168/12/24) were designed at slider=100
  // (getSizeMultiplier = 100/120), so scale = m * 1.2 keeps slider 100 pixel-identical
  // to the baseline. Pure store read, no DOM measurement, no calibration. If the getter
  // is missing the vars stay unset and CSS fallbacks freeze the baseline look.
  // A store.watch updates the vars synchronously on slider commits: ABS re-measures its
  // dummy card right after the commit, and if the vars lag one mutation tick the shelf
  // computes slots from the previous size (rows overlap / titles clipped).
  let nhSeriesWatched = false;
  function nhSeriesScale() {
    if (nhSettings.customSeriesCards === false) return;
    const store = window.$nuxt && window.$nuxt.$store;
    if (!store) return;
    let m;
    try { m = store.getters['user/getSizeMultiplier']; } catch (e) { return; }
    if (typeof m !== 'number' || !(m > 0.05) || !(m < 10)) return;
    // 1.6:1 mode grows tile heights x1.6, so shrink the base scale to keep the
    // stacked cards' footprint close to the stock series cards at the same slider.
    const s = m * (document.documentElement.classList.contains('nh-covers-std') ? 0.85 : 1.2);
    const root = document.documentElement.style;
    root.setProperty('--nh-series-w', Math.round(196 * s) + 'px');
    root.setProperty('--nh-series-cover', Math.round(168 * s) + 'px');
    root.setProperty('--nh-series-off1', Math.round(12 * s) + 'px');
    root.setProperty('--nh-series-off2', Math.round(24 * s) + 'px');
    if (!nhSeriesWatched && typeof store.watch === 'function') {
      nhSeriesWatched = true;
      try { store.watch((state, getters) => getters['user/getSizeMultiplier'], () => nhSeriesScale()); } catch (e) {}
      // ABS measures its dummy card once in mounted(), before these vars existed on a
      // cold load, and window resize only recomputes columns from that stale measurement
      // (windowResize -> executeRebuild -> initSizeData; no re-measure). Call the
      // component's own setCardSize + executeRebuild once, as soon as it exists.
      let tries = 0;
      const nudge = setInterval(() => {
        tries++;
        const el = document.getElementById('bookshelf');
        const vm = el && el.__vue__;
        if (vm && typeof vm.setCardSize === 'function') {
          clearInterval(nudge);
          Promise.resolve(vm.setCardSize()).then(() => { if (typeof vm.executeRebuild === 'function') vm.executeRebuild(); }).catch(() => {});
        } else if (tries > 20) clearInterval(nudge);
      }, 300);
    }
  }

  // Mark <html> when the current library prefers standard 1.6:1 covers, so CSS can
  // adapt card typography per mode. Getter returns 1.6 or 1 (square).
  function nhCoverModeClass() {
    let r = 1;
    try { r = window.$nuxt.$store.getters['libraries/getBookCoverAspectRatio'] || 1; } catch (e) { return; }
    const want = r > 1;
    if (document.documentElement.classList.contains('nh-covers-std') !== want) {
      document.documentElement.classList.toggle('nh-covers-std', want);
    }
  }

  // Series page single-scroll: shift the header up in lockstep with the grid,
  // capped at the header's own overflow, a short header effectively stays
  // pinned, a long one (expanded description) scrolls away with the books.
  function nhSeriesSyncScroll() {
    const h = document.getElementById('nh-series-header');
    const bs = document.getElementById('bookshelf');
    if (!h || !bs) return;
    if (window.innerWidth < 1024) {
      if (h.scrollTop) h.scrollTop = 0;
      if (bs.dataset.nhSpExtra) { bs.style.removeProperty('--nh-sp-extra'); delete bs.dataset.nhSpExtra; }
      // Phones: the page wrapper scrolls and the shelf is full-height, header
      // and books are one flow. Nothing to sync, and nothing may be left over
      // from the desktop path.
      if (h.style.transform) h.style.transform = '';
      return;
    }
    if (h.style.transform) h.style.transform = '';
    // The header is a column-height box with hidden internal scroll; we drive
    // its scrollTop in lockstep with the grid, so the page reads as ONE scroll.
    const over = Math.max(0, h.scrollHeight - h.clientHeight);
    // Guarantee the grid can scroll at least `over` px (short shelf + long
    // description = unreachable header tail). naturalRange is deliberately NOT
    // clamped: when the grid's content is SHORTER than its viewport it goes
    // negative, and the spacer must absorb that deficit too.
    const extra = parseInt(bs.dataset.nhSpExtra || '0', 10) || 0;
    const naturalRange = (bs.scrollHeight - extra) - bs.clientHeight;
    const need = Math.max(0, Math.ceil(over - naturalRange));
    if (Math.abs(need - extra) > 2) {
      bs.dataset.nhSpExtra = String(need);
      if (need) bs.style.setProperty('--nh-sp-extra', need + 'px');
      else bs.style.removeProperty('--nh-sp-extra');
    }
    const want = Math.min(bs.scrollTop, over);
    if (Math.abs(h.scrollTop - want) > 1) h.scrollTop = want;
  }

  // Tag finished covers. Anchored on [id^="cover-area-"], the progressBar's real
  // positioned ancestor, book cards on this build don't carry cy-id="card", which is
  // why the previous card-anchored tagger matched nothing. bg-success is confirmed
  // present on finished bars (user-supplied DOM), so the class check is sufficient
  // and needs no Vue access (works from extensions too).
  function nhTagFinished() {
    document.querySelectorAll('[cy-id="progressBar"]').forEach((p) => {
      const host = p.closest('[id^="cover-area-"]') || p.closest('[cy-id="card"]');
      if (host) host.classList.toggle('nh-finished', p.classList.contains('bg-success'));
    });
  }

  // Series detail page header: series name, authors, count · duration, and the
  // description of book #1 (smallest whole sequence ≥ 1; 0.x novellas lose to real
  // entries; fractional beats none). Data read straight from the bookshelf vm's
  // loaded entities, no extra API call. Idempotent: updates in place each cycle.
  function nhSeriesHeader() {
    const onSeries = /\/library\/[^/]+\/series\/[^/?#]+/.test(location.pathname);
    const bookshelf = document.getElementById('bookshelf');
    const existing = document.getElementById('nh-series-header');
    if (!onSeries || !bookshelf) {
      if (existing) {
        const par = existing.parentNode;
        existing.remove();
        if (par && par.classList) par.classList.remove('nh-series-cols');
      }
      if (bookshelf) {
        bookshelf.classList.remove('nh-with-series-header');
        if (bookshelf.parentNode && bookshelf.parentNode.classList) bookshelf.parentNode.classList.remove('nh-series-cols');
      }
      document.documentElement.style.removeProperty('--nh-sh-h');
      document.body.classList.remove('nh-series-page');
      document.body.classList.remove('nh-series-ready');
      if (window.__nhRatingsMount) try { window.__nhRatingsMount(null, null); } catch (e) {}
      return;
    }
    // PHASE 1, structure, synchronously on the FIRST tick of the route (before any
    // data): build the header shell with skeleton bars, enter the two-column layout,
    // and re-measure the shelf ONCE. Books then render at their final width straight
    // away; the fetched text later fills a column whose size never changes, no more
    // "everything shifts left once the description arrives".
    let h = existing;
    if (!h) {
      h = document.createElement('div');
      h.id = 'nh-series-header';
      h.className = 'nh-sh-loading';
      h.dataset.born = String(Date.now());
      h.innerHTML = '<div class="nh-sh-cover"></div><div class="nh-sh-eyebrow"><span class="nh-sh-eyelabel">Series</span><span class="nh-sh-tools"></span></div><h1></h1><div class="nh-sh-author"></div><div class="nh-sh-stats"></div><div class="nh-sh-rate"></div><p class="nh-sh-desc"></p><button type="button" class="nh-sh-more" style="display:none"></button>';
      bookshelf.parentNode.insertBefore(h, bookshelf);
    }
    bookshelf.classList.add('nh-with-series-header');
    document.body.classList.add('nh-series-page');
    if (bookshelf.parentNode && bookshelf.parentNode.classList) bookshelf.parentNode.classList.add('nh-series-cols');
    // The header is always a SIBLING above the shelf: on phones the wrapper
    // scrolls both as one flow, on desktop it is the left column pane. It lived
    // inside #bookshelf for one round; that put it at the mercy of ABS's shelf
    // rebuilds and of a scroller whose position resets on every viewport-height
    // change (Android URL bar).
    if (h.parentElement === bookshelf) bookshelf.parentNode.insertBefore(h, bookshelf);
    document.documentElement.style.setProperty('--nh-sh-h', h.offsetHeight + 'px');

    // ONE scroll surface (user request): the header column has no scrollbar of
    // its own anymore, it translates in sync with the grid until its overflow
    // is used up, and wheel input over it drives the grid. ABS's #bookshelf
    // stays the real scroller (its lazy card mounting depends on it).
    if (bookshelf.dataset.nhScrollSync !== '1') {
      bookshelf.dataset.nhScrollSync = '1';
      bookshelf.addEventListener('scroll', nhSeriesSyncScroll, { passive: true });
    }
    if (!h.dataset.nhWheel) {
      h.dataset.nhWheel = '1';
      h.addEventListener('wheel', (e) => {
        const bs = document.getElementById('bookshelf');
        if (bs && window.innerWidth >= 1024) { bs.scrollTop += e.deltaY; e.preventDefault(); }
      }, { passive: false });
    }

    // The column layout changes #bookshelf's width, but ABS only re-measures on real
    // window resizes or settings changes, nudge one setCardSize + rebuild per route.
    // Tracked on the BOOKSHELF element (not the header): during a route transition a
    // tick can catch the OLD page's shelf, and marking the header would stop the NEW
    // shelf from ever being nudged.
    const vm = bookshelf.__vue__;
    if (bookshelf.dataset.nhNudged !== location.pathname && vm) {
      bookshelf.dataset.nhNudged = location.pathname;
      const nudgedPath = location.pathname;
      // The reveal must wait for the re-measure to actually LAND, not merely be
      // requested, ABS applies executeRebuild's layout asynchronously (~200ms),
      // and revealing early shows the cards hopping to their final positions.
      const done = () => setTimeout(() => { bookshelf.dataset.nhRebuilt = nudgedPath; }, 280);
      if (typeof vm.setCardSize === 'function') {
        Promise.resolve(vm.setCardSize()).then(() => {
          if (typeof vm.executeRebuild === 'function') vm.executeRebuild();
          done();
        }).catch(done);
      } else {
        done();
      }
    }

    // PHASE 2, content, once ABS has the series' items.
    const ents = vm && Array.isArray(vm.entities) ? vm.entities.filter(Boolean) : [];
    if (!ents.length) return;

    const sm = location.pathname.match(/\/series\/([^/?#]+)/);
    const seriesId = sm ? sm[1] : '';

    let seriesName = '', best = null, bestScore = Infinity, dur = 0;
    const authors = [];
    const authorIds = {};
    ents.forEach((e) => {
      const md = (e.media && e.media.metadata) || {};
      if (e.media && e.media.duration) dur += e.media.duration;
      if (md.authorName && authors.indexOf(md.authorName) === -1) authors.push(md.authorName);
      (md.authors || []).forEach((a) => { if (a && a.id && a.name && !authorIds[a.name]) authorIds[a.name] = a.id; });
      const se = md.series;
      if (se && se.name && !seriesName) seriesName = se.name;
      const q = se ? parseFloat(se.sequence) : NaN;
      // integer ≥1 scores its own value (1 wins), fractional ≥1 after all integers,
      // sub-1 novellas after those, sequence-less last
      const score = isFinite(q) ? (q >= 1 ? (Number.isInteger(q) ? q : q + 1000) : q + 2000) : 3000;
      if (score < bestScore) { bestScore = score; best = e; }
    });

    let desc = '';
    try {
      const raw = best && best.media && best.media.metadata && best.media.metadata.description;
      if (raw) {
        // Keep paragraph structure: <br>/<p> become newlines, then strip the rest.
        // .nh-sh-desc renders white-space:pre-line, so these breaks survive,         // same treatment the admin's pasted formatting gets. Parsed inertly.
        desc = nhHtmlToText(raw, true).replace(/\n{3,}/g, '\n\n').trim();
      }
    } catch (e) {}
    // Admin-written override wins over book #1's blurb (null while still loading).
    const customDesc = nhSdText(seriesId);
    if (customDesc) desc = customDesc;

    const TT = nhGsT();
    const total = (vm.totalEntities && vm.totalEntities > ents.length) ? vm.totalEntities : ents.length;
    const allLoaded = !vm.totalEntities || ents.length >= vm.totalEntities;
    const durStr = allLoaded && dur > 60 ? Math.floor(dur / 3600) + 'h ' + Math.round((dur % 3600) / 60) + 'm' : '';
    const statsLine = [total + ' ' + nhWordForm(total, TT.shBookForms || PANEL_T.en.shBookForms), durStr].filter(Boolean).join(' · ');

    h.classList.remove('nh-sh-loading');
    const set = (sel, txt) => { const el = h.querySelector(sel); if (el && el.textContent !== txt) el.textContent = txt; };
    set('.nh-sh-eyelabel', TT.shSeries || PANEL_T.en.shSeries);
    set('h1', seriesName);
    set('.nh-sh-stats', statsLine);
    set('.nh-sh-desc', desc);
    const dEl = h.querySelector('.nh-sh-desc');
    dEl.style.display = desc ? '' : 'none';

    // Read-more: shown only when the clamp actually cuts the text off.
    const moreBtn = h.querySelector('.nh-sh-more');
    if (moreBtn) {
      if (!moreBtn.dataset.nhInit) {
        moreBtn.dataset.nhInit = '1';
        moreBtn.addEventListener('click', () => dEl.classList.toggle('nh-open'));
      }
      const openD = dEl.classList.contains('nh-open');
      const clamped = !openD && !!desc && dEl.scrollHeight > dEl.clientHeight + 2;
      moreBtn.style.display = (openD || clamped) ? '' : 'none';
      moreBtn.textContent = openD ? (TT.sdLess || PANEL_T.en.sdLess) : (TT.sdMore || PANEL_T.en.sdMore);
    }

    // Author line with clickable names (SPA-routed to the author page). Rebuilt
    // only when the author set changes; names without a resolvable id stay text.
    const authEl = h.querySelector('.nh-sh-author');
    if (authEl) {
      const libm = location.pathname.match(/\/library\/([^/]+)\//);
      const auMap = nhAuthorIdMap(libm ? libm[1] : '');
      Object.keys(auMap).forEach((n) => { if (!authorIds[n]) authorIds[n] = auMap[n]; });
      const sig = authors.slice(0, 2).map((n) => n + ':' + (authorIds[n] || '')).join('|') + (authors.length > 2 ? '|+' : '');
      if (authEl.dataset.nhSig !== sig) {
        authEl.dataset.nhSig = sig;
        authEl.textContent = '';
        if (authors.length) {
          const byTxt = typeof TT.shBy === 'string' ? TT.shBy : PANEL_T.en.shBy;
          if (byTxt) authEl.appendChild(document.createTextNode(byTxt));
          authors.slice(0, 2).forEach((name, i) => {
            if (i) authEl.appendChild(document.createTextNode(', '));
            const aid = authorIds[name];
            if (aid) {
              const a = document.createElement('a');
              a.textContent = name;
              a.href = getBaseNH() + '/author/' + aid;
              a.addEventListener('click', (e) => { e.preventDefault(); nhRouterPush('/author/' + aid); });
              authEl.appendChild(a);
            } else {
              authEl.appendChild(document.createTextNode(name));
            }
          });
          if (authors.length > 2) authEl.appendChild(document.createTextNode(typeof TT.shMore === 'string' ? TT.shMore : PANEL_T.en.shMore));
        }
      }
    }

    // A3 (revised per user): the series rating is DERIVED from the books' own
    // ratings, there is no separate user-entered series rating. Tear down any
    // widget instance an older build left mounted, then render the aggregate.
    if (window.__nhRatingsMount) { try { window.__nhRatingsMount(null, null); } catch (e) {} }
    nhSeriesBooksAvg(h, ents);
    nhSeriesHeaderCover(h, seriesId, ents, vm.entities.length);
    nhSeriesSyncScroll();

    document.documentElement.style.setProperty('--nh-sh-h', h.offsetHeight + 'px');
  }

  // Books-average line for the series header: mean of each rated book's average
  // rating, over the series' loaded entities. Needs the WHOLE ratings store,   // fetched once per series route (tiny at family scale) and recomputed from
  // cache on later ticks. Skipped entirely while the ratings widget itself is
  // disabled or its backend is absent (rtLive=false, same backend, same fate).
  const nhSrAvg = { path: null, items: null, fetching: false, tries: 0 };
  function nhSrToken() {
    if (window.__NH_TOKEN) return window.__NH_TOKEN;
    try {
      const st = window.$nuxt && window.$nuxt.$store;
      const t = st && (st.getters['user/getToken'] || (st.state.user.user && (st.state.user.user.accessToken || st.state.user.user.token)));
      if (t) return t;
    } catch (e) {}
    return '';
  }
  function nhSeriesBooksAvg(h, ents) {
    const slot = h.querySelector('.nh-sh-rate');
    if (!slot) return;
    if (nhSrAvg.path !== location.pathname) {
      nhSrAvg.path = location.pathname;
      nhSrAvg.items = null;
      nhSrAvg.tries = 0;
      slot.textContent = '';
      delete slot.dataset.nhVal;
    }
    if (!nhSrAvg.items) {
      const path = nhSrAvg.path;
      const token = nhSrToken();
      if (!nhSrAvg.fetching && nhSrAvg.tries < 5 && token) {
        nhSrAvg.fetching = true;
        nhSrAvg.tries++;
        fetch('/_nh/api/ratings', { headers: { Authorization: 'Bearer ' + token }, credentials: 'include' })
          .then((r) => (r.ok ? r.json() : null))
          .then((j) => {
            nhSrAvg.fetching = false;
            if (nhSrAvg.path !== path) return;
            if (j && j.items) nhSrAvg.items = j.items;
            else nhSrAvg.tries = 5; // backend said no, stop asking this route
          })
          .catch(() => { nhSrAvg.fetching = false; });
      }
      return;
    }
    let sum = 0, rated = 0;
    ents.forEach((e) => {
      const rs = nhSrAvg.items[e.id];
      if (!rs) return;
      const stars = Object.keys(rs).map((k) => rs[k] && rs[k].stars).filter((v) => typeof v === 'number');
      if (!stars.length) return;
      sum += stars.reduce((a, b) => a + b, 0) / stars.length;
      rated++;
    });
    if (!rated) { slot.textContent = ''; slot.style.display = 'none'; delete slot.dataset.nhVal; return; }
    const avg = sum / rated;
    slot.style.display = '';
    const sig = avg.toFixed(2) + ':' + rated + ':' + ents.length;
    if (slot.dataset.nhVal === sig) return;
    slot.dataset.nhVal = sig;
    slot.textContent = '';
    const stars = document.createElement('span');
    stars.className = 'nh-rt-stars';
    const base = document.createElement('span');
    base.textContent = '★★★★★';
    const fill = document.createElement('span');
    fill.className = 'nh-rt-fill';
    fill.textContent = '★★★★★';
    stars.appendChild(base);
    stars.appendChild(fill);
    nhStarFill(stars, fill, avg);
    const score = document.createElement('span');
    score.className = 'nh-rt-score';
    score.textContent = nhStarText(avg);
    const TT = nhGsT();
    const txt = document.createElement('span');
    txt.className = 'nh-sh-rate-note';
    txt.textContent = rated + '/' + ents.length + ' ' + (TT.shRated || PANEL_T.en.shRated);
    slot.appendChild(stars);
    slot.appendChild(score);
    slot.appendChild(txt);
  }

  // ===================== GLOBAL CROSS-LIBRARY SEARCH (A2) =====================
  // Replaces the appbar's per-library search with a fan-out across every library
  // the user can access (the native panel stays available via the toggle). ABS's
  // own input is kept, identical look, v-model untouched, we listen on it, hide
  // the native .globalSearchMenu with a body class, and render our own merged
  // panel: books deduped by normalized title+author carrying one library chip per
  // copy; series and authors merged by name the same way. All user strings render
  // via textContent. /api/libraries is already permission-filtered server-side,
  // so restricted users only ever fan out over their own libraries.
  const nhGs = { libs: null, q: '', seq: 0, results: null, flat: [], sel: -1, open: false, timer: null, fetching: false };

  // ABS has no $nuxt.$i18n, language comes from the user's ABS setting via
  // getUserLanguage(), exactly like the settings panel (panelT). An earlier
  // version read $i18n.locale, which threw and silently pinned everything
  // added in v2.0 to English (user report).
  function nhGsT() { return panelT(); }

  // Pluralize with Polish three-form support: [one, few, many]; two-form
  // languages pass [one, many]. (Mirror of book-details' nhRtWord.)
  function nhWordForm(n, forms) {
    if (!Array.isArray(forms) || !forms.length) return '';
    if (forms.length === 2) return n === 1 ? forms[0] : forms[1];
    if (n === 1) return forms[0];
    const d = n % 10, h = n % 100;
    if (d >= 2 && d <= 4 && (h < 12 || h > 14)) return forms[1];
    return forms[2];
  }

  function nhGsNorm(s) {
    s = String(s || '').toLowerCase();
    try { s = s.normalize('NFD').replace(/[̀-ͯ]/g, ''); } catch (e) {}
    return s.replace(/[^a-z0-9]+/g, ' ').trim();
  }

  function nhGsLibs() {
    if (nhGs.libs) return Promise.resolve(nhGs.libs);
    const tok = nhSrToken();
    if (!tok) return Promise.resolve(null);
    return fetch('/api/libraries', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => {
        const libs = j && (j.libraries || j);
        if (Array.isArray(libs) && libs.length) nhGs.libs = libs.map((l) => ({ id: l.id, name: l.name, mt: l.mediaType || '' }));
        return nhGs.libs;
      })
      .catch(() => null);
  }

  function nhGsMerge(res) {
    const books = new Map(), series = new Map(), authors = new Map();
    res.forEach((pair) => {
      const lib = pair.lib, j = pair.j;
      if (!j) return;
      (j.book || []).concat(j.podcast || []).forEach((b) => {
        const li = b.libraryItem;
        if (!li) return;
        const md = (li.media && li.media.metadata) || {};
        const key = nhGsNorm(md.title) + '|' + nhGsNorm(md.authorName || md.author);
        let row = books.get(key);
        if (!row) { row = { title: md.title || '?', author: md.authorName || md.author || '', copies: [] }; books.set(key, row); }
        if (!row.copies.some((c) => c.itemId === li.id)) row.copies.push({ itemId: li.id, lib: lib });
      });
      (j.series || []).forEach((s) => {
        const se = s.series || s;
        if (!se || !se.name) return;
        const key = nhGsNorm(se.name);
        let row = series.get(key);
        if (!row) { row = { name: se.name, copies: [] }; series.set(key, row); }
        row.copies.push({ seriesId: se.id, lib: lib });
      });
      (j.authors || []).forEach((a) => {
        if (!a || !a.name) return;
        const key = nhGsNorm(a.name);
        let row = authors.get(key);
        if (!row) { row = { name: a.name, copies: [] }; authors.set(key, row); }
        row.copies.push({ authorId: a.id, lib: lib });
      });
    });
    const arr = (m) => Array.from(m.values());
    return { books: arr(books), series: arr(series), authors: arr(authors) };
  }

  function nhGsRun(q) {
    const seq = ++nhGs.seq;
    nhGs.fetching = true;
    nhRsItems(); // warm the ratings map so result rows can carry stars (A7)
    nhGsRender();
    nhGsLibs().then((libs) => {
      if (!libs || seq !== nhGs.seq) return;
      const tok = nhSrToken();
      Promise.all(libs.map((lib) =>
        fetch('/api/libraries/' + lib.id + '/search?q=' + encodeURIComponent(q) + '&limit=8', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
          .then((r) => (r.ok ? r.json() : null))
          .catch(() => null)
          .then((j) => ({ lib: lib, j: j }))
      )).then((res) => {
        if (seq !== nhGs.seq) return;
        nhGs.fetching = false;
        nhGs.results = nhGsMerge(res);
        nhGs.sel = -1;
        nhGsRender();
      });
    });
  }

  function nhGsGo(t) {
    nhGsClose();
    try {
      const r = window.$nuxt.$router;
      if (t.itemId) r.push('/item/' + t.itemId);
      else if (t.seriesId) r.push('/library/' + t.lib.id + '/series/' + t.seriesId);
      else if (t.authorId) r.push('/author/' + t.authorId);
    } catch (e) {}
  }

  function nhGsClose() {
    nhGs.open = false;
    nhGs.sel = -1;
    const p = document.getElementById('nh-gs-panel');
    if (p) p.remove();
  }

  function nhGsChip(target, name, solo) {
    const chip = document.createElement('span');
    chip.className = 'nh-gs-chip' + (solo ? ' nh-gs-chip-solo' : '');
    chip.textContent = name;
    chip.addEventListener('click', (e) => { e.stopPropagation(); e.preventDefault(); nhGsGo(target); });
    return chip;
  }

  function nhGsRender() {
    if (!nhGs.open) return;
    const input = document.querySelector('#appbar form[role="search"] input');
    const wrap = input && input.closest('form') && input.closest('form').parentElement;
    if (!wrap) return;
    let panel = document.getElementById('nh-gs-panel');
    if (!panel) {
      panel = document.createElement('div');
      panel.id = 'nh-gs-panel';
      // mousedown, not click: it fires before the input's blur can close us
      panel.addEventListener('mousedown', (e) => e.preventDefault());
      wrap.appendChild(panel);
    }
    panel.textContent = '';
    const T = nhGsT();
    const multiLib = nhGs.libs && nhGs.libs.length > 1;
    nhGs.flat = [];

    if (nhGs.fetching && !nhGs.results) {
      const m = document.createElement('div');
      m.className = 'nh-gs-msg';
      m.textContent = T.gsSearching || PANEL_T.en.gsSearching;
      panel.appendChild(m);
      return;
    }
    const r = nhGs.results || { books: [], series: [], authors: [] };
    if (!r.books.length && !r.series.length && !r.authors.length) {
      const m = document.createElement('div');
      m.className = 'nh-gs-msg';
      m.textContent = T.gsNoResults || PANEL_T.en.gsNoResults;
      panel.appendChild(m);
      return;
    }

    const addHead = (txt) => {
      const hd = document.createElement('div');
      hd.className = 'nh-gs-head';
      hd.textContent = txt;
      panel.appendChild(hd);
    };
    const addRow = (target, build) => {
      const row = document.createElement('div');
      row.className = 'nh-gs-row';
      row.dataset.idx = String(nhGs.flat.length);
      nhGs.flat.push(target);
      build(row);
      row.addEventListener('click', () => nhGsGo(target));
      row.addEventListener('mousemove', () => {
        const i = Number(row.dataset.idx);
        if (nhGs.sel !== i) { nhGs.sel = i; nhGsMark(); }
      });
      panel.appendChild(row);
    };

    if (r.books.length) {
      addHead(T.gsBooks || PANEL_T.en.gsBooks);
      r.books.forEach((b) => {
        addRow(b.copies[0], (row) => {
          const img = document.createElement('img');
          img.className = 'nh-gs-cover';
          img.loading = 'lazy';
          img.src = '/api/items/' + b.copies[0].itemId + '/cover?width=120';
          img.addEventListener('error', () => { img.style.visibility = 'hidden'; });
          row.appendChild(img);
          const txt = document.createElement('div');
          txt.className = 'nh-gs-text';
          const t1 = document.createElement('p');
          t1.className = 'nh-gs-title';
          t1.textContent = b.title;
          const t2 = document.createElement('p');
          t2.className = 'nh-gs-sub';
          t2.textContent = b.author;
          txt.appendChild(t1);
          txt.appendChild(t2);
          // Search spans every library, so the gate is the HIT's library, not the
          // one being browsed.
          if (nhSettings.showRatings !== false && nhSettings.showCardRatings !== false
              && nhRatingsLibOn(b.copies[0].lib && b.copies[0].lib.id)) {
            const rr = nhRsAvg(b.copies[0].itemId);
            if (rr) txt.appendChild(nhCrStarsEl(rr, 'nh-gs-stars'));
          }
          row.appendChild(txt);
          if (multiLib) {
            const chips = document.createElement('div');
            chips.className = 'nh-gs-chips';
            b.copies.forEach((c) => chips.appendChild(nhGsChip(c, c.lib.name, b.copies.length === 1)));
            row.appendChild(chips);
          }
        });
      });
    }
    const entitySection = (list, headTxt, icon) => {
      if (!list.length) return;
      addHead(headTxt);
      list.forEach((s) => {
        addRow(s.copies[0], (row) => {
          const ic = document.createElement('span');
          ic.className = 'nh-gs-icon material-symbols';
          ic.textContent = icon;
          row.appendChild(ic);
          const txt = document.createElement('div');
          txt.className = 'nh-gs-text';
          const t1 = document.createElement('p');
          t1.className = 'nh-gs-title';
          t1.textContent = s.name;
          txt.appendChild(t1);
          row.appendChild(txt);
          if (multiLib) {
            const chips = document.createElement('div');
            chips.className = 'nh-gs-chips';
            s.copies.forEach((c) => chips.appendChild(nhGsChip(c, c.lib.name, s.copies.length === 1)));
            row.appendChild(chips);
          }
        });
      });
    };
    entitySection(r.series, T.gsSeries || PANEL_T.en.gsSeries, 'stacks');
    entitySection(r.authors, T.gsAuthors || PANEL_T.en.gsAuthors, 'person');
    nhGsMark();
  }

  function nhGsMark() {
    const panel = document.getElementById('nh-gs-panel');
    if (!panel) return;
    panel.querySelectorAll('.nh-gs-row').forEach((el) => {
      const on = Number(el.dataset.idx) === nhGs.sel;
      el.classList.toggle('nh-gs-sel', on);
      if (on) el.scrollIntoView({ block: 'nearest' });
    });
  }

  function nhGsOnInput(input) {
    const q = input.value.trim();
    nhGs.q = input.value;
    clearTimeout(nhGs.timer);
    if (q.length < 2) {
      nhGs.seq++;
      nhGs.results = null;
      nhGs.fetching = false;
      nhGsClose();
      return;
    }
    nhGs.open = true;
    nhGs.timer = setTimeout(() => nhGsRun(q), 300);
    nhGsRender();
  }

  // Search placeholder override: ABS ships some awkward forms ("Szukanie.."),   // replace with the natural imperative where we know it; other languages keep
  // ABS's native placeholder untouched.
  const NH_SEARCH_PH = { en: 'Search…', pl: 'Szukaj…', de: 'Suchen…', fr: 'Rechercher…', es: 'Buscar…', it: 'Cerca…', pt: 'Pesquisar…', nl: 'Zoeken…', cs: 'Hledat…', sk: 'Hľadať…', da: 'Søg…', sv: 'Sök…', no: 'Søk…', fi: 'Hae…', ru: 'Искать…', uk: 'Шукати…', be: 'Шукаць…', bg: 'Търсене…', hr: 'Traži…', sl: 'Išči…', hu: 'Keresés…', ro: 'Căutare…', lt: 'Ieškoti…', lv: 'Meklēt…', et: 'Otsi…', el: 'Αναζήτηση…', tr: 'Ara…', ca: 'Cerca…', eu: 'Bilatu…', is: 'Leita…', ja: '検索…', ko: '검색…', zh: '搜索…', ar: 'بحث…', he: 'חיפוש…', fa: 'جستجو…', hi: 'खोजें…', bn: 'খুঁজুন…', gu: 'શોધો…', vi: 'Tìm kiếm…' };

  function nhGlobalSearch() {
    const on = nhSettings.globalSearch !== false;
    document.body.classList.toggle('nh-global-search', on);
    const input = document.querySelector('#appbar form[role="search"] input');
    if (input) {
      const ph = NH_SEARCH_PH[getUserLanguage().split('-')[0].toLowerCase()];
      if (ph && input.placeholder !== ph) input.placeholder = ph;
    }
    if (!on) { nhGsClose(); return; }
    if (!input) { nhGsClose(); return; }

    if (input.dataset.nhGsHooked !== '1') {
      input.dataset.nhGsHooked = '1';
      input.addEventListener('input', () => { if (nhSettings.globalSearch !== false) nhGsOnInput(input); });
      input.addEventListener('focus', () => {
        if (nhSettings.globalSearch !== false && nhGs.q.trim().length >= 2) { nhGs.open = true; nhGsRender(); }
      });
      input.addEventListener('blur', () => setTimeout(() => {
        if (document.activeElement !== input) nhGsClose();
      }, 150));
      input.addEventListener('keydown', (e) => {
        if (nhSettings.globalSearch !== false) {
          if (e.key === 'Enter') { e.preventDefault(); e.stopPropagation(); }
          if (!nhGs.open) return;
          if (e.key === 'ArrowDown' || e.key === 'ArrowUp') {
            e.preventDefault(); e.stopPropagation();
            const n = nhGs.flat.length;
            if (!n) return;
            nhGs.sel = e.key === 'ArrowDown' ? (nhGs.sel + 1) % n : (nhGs.sel - 1 + n) % n;
            nhGsMark();
          } else if (e.key === 'Enter') {
            const t = nhGs.flat[nhGs.sel >= 0 ? nhGs.sel : 0];
            if (t) nhGsGo(t);
          } else if (e.key === 'Escape') {
            e.preventDefault(); e.stopPropagation();
            nhGsClose();
          }
        }
      }, true);
      const form = input.closest('form');
      if (form) form.addEventListener('submit', (e) => { if (nhSettings.globalSearch !== false) e.preventDefault(); });
    }

    // The native clear button (×) empties the input through Vue without firing
    // an input event at us, resync when the DOM value diverges from ours.
    if (input.value !== nhGs.q) nhGsOnInput(input);
  }

  // ===================== SERIES COVERS (A1) =====================
  // Admin-uploaded custom cover per series, stored in the nh_theme_data volume
  // (/data/nh/series-covers/<seriesId>.<ext>) via the existing admin DAV path.
  // Discovery is ONE njs call returning {seriesId: ext}, no per-card probing.
  // Consumers: series grid cards (tile replaces the stack), the Recent Series
  // row, and the series header (plus its admin pencil/remove buttons).
  const nhSc = { map: null, descs: null, avatars: null, descText: {}, fetching: false, tries: 0, ts: 0 };

  // Cache-buster for the three mutable public files (series covers, series
  // descriptions, user avatars). It MUST be wall-clock, not a counter.
  // This started life as `ts = (ts || 1) + 1`, which restarts at 0 on every page
  // load and therefore hands out ?v=2, ?v=3 ... again and again, for different
  // bytes each time. nginx serves those files with a 300s cache, so the browser
  // was perfectly entitled to answer the second ?v=2 from its cache and show the
  // PREVIOUS image. That is why replacing a series cover looked like it did
  // nothing, and why it started working again about five minutes later.
  // Reproduced in Chrome: PUT red, fetch ?v=2, PUT blue, fetch ?v=2, still red.
  // Date.now() never repeats across loads, so every write gets its own URL.
  function nhScBump() { nhSc.ts = Date.now(); }

  function nhScFetch() {
    if (nhSc.fetching || nhSc.map || nhSc.tries >= 5) return;
    const tok = nhSrToken();
    if (!tok) return;
    nhSc.fetching = true;
    nhSc.tries++;
    fetch('/_nh/api/series-meta', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
      .then((r) => {
        if (r.status === 404 || r.status === 405) { nhSc.map = {}; nhSc.descs = {}; nhSc.avatars = {}; nhSc.tries = 99; return null; } // old proxy: feature off, stay quiet
        return r.ok ? r.json() : null;
      })
      .then((j) => {
        nhSc.fetching = false;
        if (j && j.covers) { nhSc.map = j.covers; nhSc.descs = j.descs || {}; nhSc.avatars = j.avatars || {}; }
      })
      .catch(() => { nhSc.fetching = false; });
  }

  // Custom series description (admin override, /data/nh/series-desc/<id>.txt).
  // Returns the text once loaded; triggers a lazy fetch and returns null while
  // pending, the tick re-renders when it lands. Keyed by ts so saves refresh.
  function nhSdText(seriesId) {
    if (!seriesId || !nhSc.descs || !nhSc.descs[seriesId]) return null;
    const key = seriesId + ':' + nhSc.ts;
    const cached = nhSc.descText[key];
    if (cached !== undefined) return cached || null;
    nhSc.descText[key] = ''; // fetch exactly once per key
    fetch('/_nh/series-desc/' + seriesId + '.txt' + (nhSc.ts ? '?v=' + nhSc.ts : ''))
      .then((r) => (r.ok ? r.text() : ''))
      .then((t) => { nhSc.descText[key] = String(t || '').trim(); })
      .catch(() => {});
    return null;
  }

  function nhSdSave(sid, text, cb) {
    const body = String(text || '').trim().slice(0, 5000);
    if (!body) return nhSdClear(sid, cb);
    fetch('/_nh/data/series-desc/' + sid + '.txt', { method: 'PUT', headers: { Authorization: 'Bearer ' + nhSrToken(), 'Content-Type': 'text/plain; charset=utf-8' }, body: body })
      .then((r) => {
        if (!r.ok) { if (cb) cb(false); return; }
        if (!nhSc.descs) nhSc.descs = {};
        nhSc.descs[sid] = 1;
        nhScBump();
        nhSc.descText[sid + ':' + nhSc.ts] = body;
        if (cb) cb(true);
      })
      .catch(() => { if (cb) cb(false); });
  }

  function nhSdClear(sid, cb) {
    fetch('/_nh/data/series-desc/' + sid + '.txt', { method: 'DELETE', headers: { Authorization: 'Bearer ' + nhSrToken() } })
      .then((r) => {
        const good = r.ok || r.status === 404;
        if (good && nhSc.descs) { delete nhSc.descs[sid]; nhScBump(); }
        if (cb) cb(good);
      })
      .catch(() => { if (cb) cb(false); });
  }

  // Small editor modal (reuses the ratings popup chrome). Save writes the
  // override; "Restore book description" deletes it and the header falls back
  // to book #1's blurb on the next tick.
  function nhSdOpenEditor(sid) {
    const T = nhGsT();
    const old = document.getElementById('nh-sd-modal');
    if (old) old.remove();
    const overlay = document.createElement('div');
    overlay.id = 'nh-sd-modal';
    const bg = document.createElement('div');
    bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div');
    box.className = 'nh-rt-modal-box';
    const closeModal = () => overlay.remove();
    bg.addEventListener('click', closeModal);
    const head = document.createElement('div');
    head.className = 'nh-rt-modal-head';
    const title = document.createElement('span');
    title.textContent = T.scEditDesc || PANEL_T.en.scEditDesc;
    const x = document.createElement('button');
    x.type = 'button';
    x.className = 'nh-rt-modal-x';
    x.textContent = '×';
    x.addEventListener('click', closeModal);
    head.appendChild(title);
    head.appendChild(x);
    box.appendChild(head);
    const ta = document.createElement('textarea');
    ta.className = 'nh-sd-ta';
    ta.maxLength = 5000;
    const hdrDesc = document.querySelector('#nh-series-header .nh-sh-desc');
    ta.value = (nhSdText(sid) || (hdrDesc ? hdrDesc.textContent : '') || '').trim();
    box.appendChild(ta);
    const row = document.createElement('div');
    row.className = 'nh-rt-actions';
    const save = document.createElement('button');
    save.type = 'button';
    save.className = 'nh-rt-btn';
    save.textContent = T.sdSave || PANEL_T.en.sdSave;
    const clear = document.createElement('button');
    clear.type = 'button';
    clear.className = 'nh-rt-link';
    clear.textContent = T.sdClear || PANEL_T.en.sdClear;
    const status = document.createElement('span');
    status.className = 'nh-rt-status';
    save.addEventListener('click', () => { status.textContent = '…'; nhSdSave(sid, ta.value, (ok2) => { if (ok2) closeModal(); else status.textContent = '✗'; }); });
    clear.addEventListener('click', () => { status.textContent = '…'; nhSdClear(sid, (ok2) => { if (ok2) closeModal(); else status.textContent = '✗'; }); });
    row.appendChild(save);
    row.appendChild(clear);
    row.appendChild(status);
    box.appendChild(row);
    overlay.appendChild(bg);
    overlay.appendChild(box);
    document.body.appendChild(overlay);
    ta.focus();
  }

  // Resolves once the cover map is loaded (or after ~1.5s), lets the Recent
  // Series row render with custom covers on the first pass.
  function nhScReady() {
    return new Promise((res) => {
      const t0 = Date.now();
      (function chk() {
        if (nhSc.map || Date.now() - t0 > 1500) return res();
        nhScFetch();
        setTimeout(chk, 150);
      })();
    });
  }

  function nhScUrl(seriesId) {
    const ext = nhSc.map && seriesId ? nhSc.map[seriesId] : null;
    return ext ? '/_nh/series-covers/' + seriesId + '.' + ext + (nhSc.ts ? '?v=' + nhSc.ts : '') : null;
  }

  // Series grid cards: swap the stacked tile for the uploaded cover.
  function nhSeriesCardCovers() {
    const cards = document.querySelectorAll('[id^="series-card-"]');
    if (!cards.length) return;
    if (!nhSc.map) { nhScFetch(); return; }
    cards.forEach((card) => {
      let sid = card.dataset.nhSid;
      if (!sid) {
        try { sid = card.__vue__ && card.__vue__.series && card.__vue__.series.id; } catch (e) {}
        if (sid) card.dataset.nhSid = sid;
      }
      if (!sid) return;
      const url = nhScUrl(sid);
      let tile = card.querySelector('.nh-sc-tile');
      if (url) {
        card.classList.add('nh-has-custom');
        const area = card.querySelector('[cy-id="covers-area"]');
        if (!tile && area) {
          tile = document.createElement('div');
          tile.className = 'nh-sc-tile';
          area.appendChild(tile);
        }
        if (tile) {
          const bg = 'url("' + url + '")';
          // Re-assert via COMPUTED style too: a Vue card re-mount can clone the
          // tile without its inline style, leaving a blank fallback-colored box.
          if (tile.dataset.nhBg !== url || getComputedStyle(tile).backgroundImage === 'none') {
            tile.style.backgroundImage = bg;
            tile.dataset.nhBg = url;
          }
        }
      } else {
        card.classList.remove('nh-has-custom');
        if (tile) tile.remove();
      }
    });
  }

  // ===================== SERIES COMPLETION BADGE (GitHub #13) =====================
  // Stock ABS paints a green/yellow progress bar across the bottom of a series
  // card. The theme rebuilds covers-area into the stacked deck, so that bar is
  // gone, this puts the information back as a corner badge on the front cover.
  // Green tick = every book finished, amber tick = started but not all done.
  // An untouched series gets NOTHING on purpose: a badge on every card is noise,
  // and "not started" is exactly what the absence of a badge already means. This
  // matches what ABS itself signals.
  // NOTE: books[] only ever holds the books IN THIS LIBRARY, so "complete" means
  // complete as far as this server knows, a series missing book 5 reads as done.
  //
  // FIELD FIX (Pawel, v2.1.1): a card's own books[] is NOT a reliable book list.
  // Reported: series showing green with nothing finished, and 1-of-3 showing green.
  // Both are the same fault, if the card only knows about ONE book, one finished
  // book IS "all of them". So the badge no longer trusts card data: it counts
  // against a per-library map built from the series endpoint, which always returns
  // the complete books[] per series. Card ids are a fallback for the brief window
  // before that map lands, and while it is missing NO badge is drawn at all,   // showing a wrong badge and correcting it a second later is worse than waiting.
  const NH_SP_TICK = '<svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="4.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4.5 12.8 9.5 17.8 19.5 6.8"/></svg>';
  // Started-but-not-finished draws a RING FILLED TO THE FRACTION READ, not a tick.
  // Two reasons: it cannot be mistaken for the finished badge even if the colours
  // are hard to tell apart (Pawel saw them as one colour at two brightnesses), and
  // it says how far in you are without hovering.
  function nhSpRing(frac) {
    const C = 2 * Math.PI * 8; // r=8 in the 24x24 viewBox
    const on = Math.max(C * 0.08, C * Math.min(1, Math.max(0, frac))); // always a visible arc
    return '<svg viewBox="0 0 24 24" width="14" height="14" fill="none" aria-hidden="true">' +
      '<circle cx="12" cy="12" r="8" stroke="currentColor" stroke-width="4.2" opacity="0.32"/>' +
      '<circle cx="12" cy="12" r="8" stroke="currentColor" stroke-width="4.2" stroke-linecap="round"' +
      ' stroke-dasharray="' + on.toFixed(2) + ' ' + (C - on).toFixed(2) + '" transform="rotate(-90 12 12)"/></svg>';
  }
  // (nhSpBooks, not nhSp, nhSp is already the start-page redirect state.)
  // Cached PER LIBRARY. One shared map meant that switching libraries left the
  // previous library's data in place, and any series missing from it fell back to
  // card data, the exact untrustworthy source this was built to stop using.
  const nhSpBooks = { maps: {}, fetching: {} };
  function nhSpEnsureMap(libId) {
    if (!libId) return null;
    if (nhSpBooks.maps[libId]) return nhSpBooks.maps[libId];
    if (nhSpBooks.fetching[libId]) return null;
    nhSpBooks.fetching[libId] = true;
    const base = getBaseNH();
    const tok = getTokenNH();
    // limit=0 means ZERO ROWS on the series endpoint (unlike items), see the A8 notes.
    fetch(base + '/api/libraries/' + libId + '/series?limit=100000&page=0', {
      headers: tok ? { Authorization: 'Bearer ' + tok } : {},
      credentials: 'include',
    })
      .then((r) => (r.ok ? r.json() : null))
      .then((d) => {
        delete nhSpBooks.fetching[libId];
        if (!d) return;
        const map = {};
        (d.results || []).forEach((s) => {
          if (!s || !s.id) return;
          map[s.id] = (s.books || []).map((b) => b.id || b.libraryItemId).filter(Boolean);
        });
        nhSpBooks.maps[libId] = map;
      })
      .catch(() => { delete nhSpBooks.fetching[libId]; });
    return null;
  }
  // Authoritative ids only. There is deliberately NO fallback to the card's own
  // books[]: that list can be short, and a short list makes a part-read series
  // look finished, which is the bug this whole path exists to prevent. A series
  // the map does not know about (another library, a stale card) gets no badge.
  function nhSpIdsFor(libId, sid) {
    const map = libId ? nhSpBooks.maps[libId] : null;
    const ids = map && map[sid];
    return ids && ids.length ? ids : null;
  }
  // FINISHED BOOKS ONLY (Pawel, v2.1.3). Merely being part-way through a book no
  // longer marks the series: the badge answers "how much of this have I actually
  // finished", so a series you have dipped into but completed nothing in stays
  // clean, exactly like one you have never opened.
  // (The Progress FILTER is unchanged and still has its own "In progress" value -
  // that one is for finding what you are mid-way through, which is a different
  // question from what this badge is for.)
  function nhSpState(bookIds) {
    const pg = nhLfProgressMap();
    let done = 0;
    (bookIds || []).forEach((id) => { if (pg[id] === 'finished') done++; });
    const n = (bookIds || []).length;
    if (!n || !done) return null;
    return { state: done === n ? 'all' : 'some', done: done, n: n };
  }
  function nhSpLabel(st) {
    const T = panelT();
    // Green says the COUNT too ("All 3 finished"). It used to say a bare "Series
    // finished", which meant a badge computed from a wrong book count looked
    // exactly like a right one - hovering now tells you which it is.
    if (st.state === 'all') return (T.spAllN || PANEL_T.en.spAllN).replace('{n}', st.n);
    return (T.spSome || PANEL_T.en.spSome).replace('{d}', st.done).replace('{n}', st.n);
  }
  // One badge element, reused. Returns the element so callers can place it.
  function nhSpPaint(host, st, sig) {
    let b = host.querySelector(':scope > .nh-sp');
    if (!st) { if (b) b.remove(); return null; }
    if (!b) {
      b = document.createElement('div');
      b.className = 'nh-sp';
      host.appendChild(b);
    }
    if (b.dataset.sig !== sig) {
      b.dataset.sig = sig;
      b.classList.toggle('nh-sp-all', st.state === 'all');
      b.classList.toggle('nh-sp-some', st.state === 'some');
      // Inline SVG, never the material-symbols font (injected spans do not get
      // ligature substitution on every build) and never a text glyph either - its
      // weight is whatever the UI font happens to give, which came out spindly
      // (Pawel). Finished = tick, part-read = a ring filled to the fraction, so
      // the two never read as "the same badge, one brighter".
      b.innerHTML = st.state === 'all' ? NH_SP_TICK : nhSpRing(st.n ? st.done / st.n : 0);
      b.title = nhSpLabel(st);
      b.setAttribute('aria-label', b.title);
    }
    return b;
  }
  // Console aid: window.__nhSpDebug() prints, for every series on screen, the book
  // count the badge used and the TITLES it counted as finished. A "finished green
  // but I finished nothing" report is then one paste instead of a round trip -
  // either the titles are wrong (our count is off) or they are books ABS really
  // does have marked finished (and the badge is right).
  function nhSpDebug() {
    const libId = getLibIdNH();
    const map = nhSpBooks.maps[libId];
    if (!map) { console.log('[NanoHive] series map not loaded yet for library', libId); return; }
    const pg = nhLfProgressMap();
    const base = getBaseNH();
    const tok = getTokenNH();
    return fetch(base + '/api/libraries/' + libId + '/items?limit=0', { headers: tok ? { Authorization: 'Bearer ' + tok } : {}, credentials: 'include' })
      .then((r) => (r.ok ? r.json() : null))
      .then((d) => {
        const title = {};
        ((d && d.results) || []).forEach((li) => { title[li.id] = ((li.media && li.media.metadata) || {}).title || li.id; });
        const rows = [];
        document.querySelectorAll('[id^="series-card-"]').forEach((c) => {
          let s = null;
          try { s = c.__vue__ && c.__vue__.series; } catch (e) {}
          if (!s || !s.id) return;
          const ids = map[s.id] || [];
          const fin = ids.filter((i) => pg[i] === 'finished');
          const started = ids.filter((i) => pg[i] === 'progress');
          rows.push({
            series: s.name,
            booksKnown: ids.length,
            booksOnCard: (s.books || []).length,
            finished: fin.length,
            started: started.length,
            // Mirrors nhSpState: started-but-none-finished draws nothing. `started`
            // is still reported because it is the first thing you want to know when
            // a series you expected to be marked is not.
            badge: !fin.length ? 'none' : (fin.length === ids.length ? 'GREEN' : 'amber'),
            finishedTitles: fin.map((i) => title[i] || i).join(' | '),
          });
        });
        console.table(rows);
        return rows;
      });
  }
  try { window.__nhSpDebug = nhSpDebug; } catch (e) {}

  function nhSeriesProgress() {
    if (nhSettings.seriesProgressBadge === false) {
      document.querySelectorAll('.nh-sp').forEach((n) => n.remove());
      return;
    }
    const libId = getLibIdNH();
    if (libId) nhSpEnsureMap(libId);

    // (a) the series grid and the home shelves: real series cards.
    document.querySelectorAll('[id^="series-card-"]').forEach((card) => {
      const area = card.querySelector('[cy-id="covers-area"]');
      if (!area) return;
      // Cards are virtual and recycled, so the ids are re-read every tick and the
      // signature carries the series id - a stale badge on a reused card is the
      // exact bug the rating badge had.
      let sid = '';
      try {
        const s = card.__vue__ && card.__vue__.series;
        sid = (s && s.id) || '';
      } catch (e) {}
      if (!sid) return;
      const ids = nhSpIdsFor(libId, sid);
      // No authoritative book list for this series (map still loading, or the card
      // belongs to another library) -> clear any badge rather than guess.
      if (!ids) { const old = area.querySelector(':scope > .nh-sp'); if (old) old.remove(); return; }
      const st = nhSpState(ids);
      nhSpPaint(area, st, sid + ':' + (st ? st.state + st.done + '/' + st.n : 'none'));
    });

    // (b) "Collapse series" on the library page does NOT produce series cards: it
    // produces book-card-* whose libraryItem carries a collapsedSeries object.
    // Those were getting no badge at all. Its libraryItemIds IS the complete list
    // (numBooks matches it), so it needs no map lookup.
    document.querySelectorAll('[id^="book-card-"]').forEach((card) => {
      let cs = null;
      try { cs = card.__vue__ && card.__vue__.libraryItem && card.__vue__.libraryItem.collapsedSeries; } catch (e) {}
      const area = card.querySelector('[id^="cover-area-"]');
      if (!cs || !area) {
        // the card may have been recycled from a collapsed series to a plain book
        const stale = area && area.querySelector(':scope > .nh-sp');
        if (stale) stale.remove();
        return;
      }
      const ids = (cs.libraryItemIds || []).filter(Boolean);
      if (!ids.length) return;
      const st = nhSpState(ids);
      nhSpPaint(area, st, (cs.id || cs.name || '') + ':' + (st ? st.state + st.done + '/' + st.n : 'none'));
    });
  }

  // An image over the 4MB write cap is DOWNSCALED here, not rejected. It used to
  // be a hard stop, and the only feedback was a tiny "✗ 4MB" glyph next to the
  // SERIES eyebrow. Pawel hit exactly that (2026-08-13): a big cover appeared to
  // upload and silently change nothing, on a page where the identical flow had
  // worked before with smaller files. Nothing on screen ever uses more than the
  // header composite's 1400px, so re-encoding at 1600px JPEG loses nothing
  // visible and lands far under the cap, which /_nh/data/ still enforces
  // server-side (client_max_body_size). Trade-off: an oversized animated GIF
  // comes out as its first frame. A small file still uploads byte-identical.
  function nhScShrink(f) {
    return new Promise((resolve, reject) => {
      const url = URL.createObjectURL(f);
      const im = new Image();
      im.onload = () => {
        URL.revokeObjectURL(url);
        try {
          const MAX = 1600;
          const sc = Math.min(1, MAX / Math.max(im.width, im.height));
          const cv = document.createElement('canvas');
          cv.width = Math.max(1, Math.round(im.width * sc));
          cv.height = Math.max(1, Math.round(im.height * sc));
          cv.getContext('2d').drawImage(im, 0, 0, cv.width, cv.height);
          // 0.88 is visually lossless for cover art; the 0.72 retry is a
          // safety net that no real image should ever need.
          cv.toBlob((b) => {
            if (b && b.size <= 4 * 1024 * 1024) return resolve(b);
            cv.toBlob((b2) => (b2 && b2.size <= 4 * 1024 * 1024 ? resolve(b2) : reject(new Error('too big'))), 'image/jpeg', 0.72);
          }, 'image/jpeg', 0.88);
        } catch (e) { reject(e); }
      };
      im.onerror = () => { URL.revokeObjectURL(url); reject(new Error('decode')); };
      im.src = url;
    });
  }

  function nhScUpload(fileInput, sid, statusEl) {
    const f = fileInput.files && fileInput.files[0];
    if (!f || !sid) return;
    const byMime = { 'image/png': 'png', 'image/jpeg': 'jpg', 'image/webp': 'webp', 'image/gif': 'gif', 'image/avif': 'avif' };
    let ext = byMime[f.type] || (f.name.split('.').pop() || '').toLowerCase();
    if (ext === 'jpeg') ext = 'jpg';
    if (!/^(png|jpg|webp|gif|avif)$/.test(ext)) { if (statusEl) statusEl.textContent = '✗'; fileInput.value = ''; return; }
    if (statusEl) statusEl.textContent = '…';
    const tok = nhSrToken();
    const doPut = (body, ex) => fetch('/_nh/data/series-covers/' + sid + '.' + ex, { method: 'PUT', headers: { Authorization: 'Bearer ' + tok }, body: body })
      .then((r) => {
        if (!r.ok) throw new Error(r.status);
        const old = nhSc.map && nhSc.map[sid];
        if (old && old !== ex) {
          fetch('/_nh/data/series-covers/' + sid + '.' + old, { method: 'DELETE', headers: { Authorization: 'Bearer ' + tok } }).catch(() => {});
        }
        if (!nhSc.map) nhSc.map = {};
        nhSc.map[sid] = ex;
        nhScBump(); // cache-bust every consumer
        if (statusEl) statusEl.textContent = '';
        fileInput.value = '';
      });
    const up = f.size > 4 * 1024 * 1024 ? nhScShrink(f).then((b) => doPut(b, 'jpg')) : doPut(f, ext);
    up.catch(() => { if (statusEl) statusEl.textContent = '✗'; fileInput.value = ''; });
  }

  function nhScDelete(sid, statusEl) {
    const ext = nhSc.map && nhSc.map[sid];
    if (!ext) return;
    fetch('/_nh/data/series-covers/' + sid + '.' + ext, { method: 'DELETE', headers: { Authorization: 'Bearer ' + nhSrToken() } })
      .then((r) => {
        if (r.ok || r.status === 404) { delete nhSc.map[sid]; nhScBump(); }
        else if (statusEl) statusEl.textContent = '✗';
      })
      .catch(() => { if (statusEl) statusEl.textContent = '✗'; });
  }

  // Author name -> id map for the header's clickable author links. The series
  // page's bookshelf entities are MINIFIED (authorName string only, no authors[]
  // with ids), so ids come from the library's authors list, fetched once per
  // library and cached. Family-scale libraries make this a small payload.
  const nhAu = { libId: null, map: null, fetching: false };
  function nhAuthorIdMap(libId) {
    if (!libId) return {};
    if (nhAu.libId === libId && nhAu.map) return nhAu.map;
    if (nhAu.libId !== libId) { nhAu.libId = libId; nhAu.map = null; }
    if (!nhAu.fetching) {
      const tok = nhSrToken();
      if (tok) {
        nhAu.fetching = true;
        fetch('/api/libraries/' + libId + '/authors', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
          .then((r) => (r.ok ? r.json() : null))
          .then((j) => {
            nhAu.fetching = false;
            if (j && Array.isArray(j.authors) && nhAu.libId === libId) {
              const m = {};
              j.authors.forEach((a) => { if (a && a.id && a.name) m[a.name] = a.id; });
              nhAu.map = m;
            }
          })
          .catch(() => { nhAu.fetching = false; });
      }
    }
    return nhAu.map || {};
  }

  // Composite header cover: when a series has NO uploaded artwork, collage the
  // first few book covers on a canvas (client-side, cached per session).
  // Header-only - cards and the recent-series row keep the stacked look unless
  // an admin uploads a real cover (user request). The seriesCoverMode setting
  // (A6) picks the fallback layout: 'deck' (layered diagonal), 'grid' (2×2),
  // 'first' (book #1's cover, no canvas), or 'off'.
  const nhScComp = {};
  const nhScCompTries = {};
  function nhScCompositeUrl(seriesId, ents, mode, total) {
    const grid = mode === 'grid';
    const key = (grid ? 'g:' : 'd:') + seriesId;
    const cur = nhScComp[key];
    if (cur === 'pending') return null;
    if (cur) return cur === 'none' ? null : cur;
    const ids = [];
    const max = grid ? 4 : 3;
    // Over-collect: books without a cover file 404 at load time, so extras keep
    // the layout full (first `max` that actually load are used).
    (ents || []).forEach((e) => { if (e && e.id && ids.length < max + 4 && ids.indexOf(e.id) === -1) ids.push(e.id); });
    if (!ids.length) return null;
    // The shelf loads entities lazily, so an early tick can see fewer books than
    // the layout wants (grid especially). Wait a few ticks for the rest before
    // committing to the session cache; render with what's there if they never come.
    const want = Math.min(max, total || ids.length);
    if (ids.length < want && (nhScCompTries[key] = (nhScCompTries[key] || 0) + 1) < 12) return null;
    nhScComp[key] = 'pending';
    const std = document.documentElement.classList.contains('nh-covers-std');
    // Sized for HiDPI/ultrawide displays: 1200px sources into a 1400px canvas at
    // JPEG .9 - the earlier 600px/900px/.82 pipeline read visibly soft on 5K.
    const W = 1400, H = std ? 2240 : 1400;
    const tk = window.__NH_TOKEN || getTokenNH();
    const tq = tk ? '&token=' + encodeURIComponent(tk) : '';
    Promise.all(ids.map((id) => new Promise((res) => {
      const im = new Image();
      im.onload = () => res(im);
      im.onerror = () => res(null);
      im.src = '/api/items/' + id + '/cover?width=1200' + tq;
    }))).then((imgs) => {
      const good = imgs.filter(Boolean).slice(0, max);
      if (!good.length) { nhScComp[key] = 'none'; return; }
      try {
        const cv = document.createElement('canvas');
        cv.width = W;
        cv.height = H;
        const ctx = cv.getContext('2d');
        const n = good.length;
        // object-fit: cover crop of im into a dw×dh destination
        const coverFit = (im, dw, dh) => {
          const sRatio = im.width / im.height, dRatio = dw / dh;
          let sx = 0, sy = 0, sW = im.width, sH = im.height;
          if (sRatio > dRatio) { sW = im.height * dRatio; sx = (im.width - sW) / 2; }
          else { sH = im.width / dRatio; sy = (im.height - sH) / 2; }
          return [sx, sy, sW, sH];
        };
        if (grid) {
          // COVER GRID: 2×2 with thin gaps; fewer books degrade gracefully
          // (3 = two up top + one full-width, 2 = side by side, 1 = full bleed).
          const G = 10;
          ctx.fillStyle = '#141414';
          ctx.fillRect(0, 0, W, H);
          const cw = (W - G) / 2, ch = (H - G) / 2;
          const cells =
            n >= 4 ? [[0, 0, cw, ch], [cw + G, 0, cw, ch], [0, ch + G, cw, ch], [cw + G, ch + G, cw, ch]] :
            n === 3 ? [[0, 0, cw, ch], [cw + G, 0, cw, ch], [0, ch + G, W, ch]] :
            n === 2 ? [[0, 0, cw, H], [cw + G, 0, cw, H]] :
            [[0, 0, W, H]];
          cells.forEach((c, idx) => {
            const im = good[idx];
            const f = coverFit(im, c[2], c[3]);
            ctx.drawImage(im, f[0], f[1], f[2], f[3], c[0], c[1], c[2], c[3]);
          });
        } else {
          // LAYERED diagonal deck (user request): every cover is drawn FULL-BLEED
          // on its own layer; each successive layer is clipped from its slanted
          // boundary to the right edge, so the cut reveals the layer beneath -
          // with a soft shadow cast along the cut for real depth.
          const sw = W / n;
          const k = W * 0.08;
          const topX = (i) => i * sw + k;
          const botX = (i) => i * sw - k;
          good.forEach((im, idx) => {
            ctx.save();
            if (idx > 0) {
              ctx.beginPath();
              ctx.moveTo(topX(idx), 0);
              ctx.lineTo(W, 0);
              ctx.lineTo(W, H);
              ctx.lineTo(botX(idx), H);
              ctx.closePath();
              ctx.clip();
            }
            const f = coverFit(im, W, H);
            ctx.drawImage(im, f[0], f[1], f[2], f[3], 0, 0, W, H);
            if (idx > 0) {
              // uniform-width shadow hugging the slanted cut: gradient axis runs
              // perpendicular to the boundary line
              const L = Math.sqrt(H * H + 4 * k * k), sh = 90;
              const g = ctx.createLinearGradient(topX(idx), 0, topX(idx) + sh * H / L, sh * 2 * k / L);
              g.addColorStop(0, 'rgba(0,0,0,0.55)');
              g.addColorStop(1, 'rgba(0,0,0,0)');
              ctx.fillStyle = g;
              ctx.fillRect(0, 0, W, H); // clip confines it to this layer
            }
            ctx.restore();
          });
        }
        nhScComp[key] = cv.toDataURL('image/jpeg', 0.9);
      } catch (e) { nhScComp[key] = 'none'; }
    });
    return null;
  }

  // Header cover, run from nhSeriesHeader phase 2 each tick. Admin actions live in
  // the series toolbar kebab menu (user request) - the header only hosts the hidden
  // file input and a tiny status glyph. Custom upload wins; otherwise the user's
  // seriesCoverMode picks the generated fallback (deck / grid / first book / off).
  function nhSeriesHeaderCover(h, seriesId, ents, total) {
    const cov = h.querySelector('.nh-sh-cover');
    if (!cov) return;
    nhScFetch();
    const mode = ['deck', 'first', 'grid', 'off'].indexOf(nhSettings.seriesCoverMode) !== -1 ? nhSettings.seriesCoverMode : 'grid';
    let url = nhScUrl(seriesId);
    if (!url && mode !== 'off') {
      if (mode === 'first') {
        const first = (ents || []).filter((e) => e && e.id)[0];
        if (first) {
          const tk = window.__NH_TOKEN || getTokenNH();
          url = '/api/items/' + first.id + '/cover?width=1200' + (tk ? '&token=' + encodeURIComponent(tk) : '');
        }
      } else {
        url = nhScCompositeUrl(seriesId, ents, mode, total);
      }
    }
    cov.classList.toggle('nh-on', !!url);
    if (url) {
      const bg = 'url("' + url + '")';
      if (cov.style.backgroundImage !== bg || getComputedStyle(cov).backgroundImage === 'none') cov.style.backgroundImage = bg;
    } else {
      cov.style.backgroundImage = '';
    }
    const tools = h.querySelector('.nh-sh-tools');
    if (!tools || !isUserAdmin()) return;
    tools.dataset.sid = seriesId;
    if (!tools.dataset.nhInit) {
      tools.dataset.nhInit = '1';
      const file = document.createElement('input');
      file.type = 'file';
      file.accept = 'image/png,image/jpeg,image/webp,image/gif,image/avif';
      file.style.display = 'none';
      file.className = 'nh-sc-file';
      const status = document.createElement('span');
      status.className = 'nh-sh-editstatus';
      file.addEventListener('change', () => nhScUpload(file, tools.dataset.sid, status));
      tools.appendChild(status);
      tools.appendChild(file);
    }
    nhScMenuItems(seriesId, tools);
  }

  // Inject "Set/Remove series cover" into the series-page toolbar kebab (the same
  // three-dot menu that has Mark as Finished etc). Rows copy the native item
  // markup so the unified menu CSS styles them identically; the tick re-adds them
  // if a Vue re-render wipes the menu. Closing is done by toggling the kebab's own
  // trigger button so Vue's showMenu state stays in sync.
  function nhScMenuItems(seriesId, tools) {
    const kebab = document.querySelector('body.nh-series-page #toolbar button[aria-haspopup="menu"]');
    const menu = kebab && kebab.parentElement && kebab.parentElement.querySelector('[role="menu"]');
    if (!menu) return;
    if (!menu.querySelector('.nh-sc-mi-set')) {
      const T = nhGsT();
      const mk = (cls, label) => {
        const b = document.createElement('button');
        b.type = 'button';
        b.setAttribute('role', 'menuitem');
        b.className = 'flex items-center px-2 py-1.5 hover:bg-white/5 text-white text-xs cursor-pointer w-full ' + cls;
        const pp = document.createElement('p');
        pp.className = 'text-left';
        pp.textContent = label;
        b.appendChild(pp);
        return b;
      };
      const close = () => { try { kebab.click(); } catch (e) {} };
      const set = mk('nh-sc-mi-set', T.scSet || PANEL_T.en.scSet);
      const rem = mk('nh-sc-mi-rem', T.scRemove || PANEL_T.en.scRemove);
      const edd = mk('nh-sc-mi-desc', T.scEditDesc || PANEL_T.en.scEditDesc);
      set.addEventListener('click', (e) => {
        e.stopPropagation();
        close();
        const f = tools.querySelector('.nh-sc-file');
        if (f) f.click();
      });
      rem.addEventListener('click', (e) => {
        e.stopPropagation();
        close();
        nhScDelete(tools.dataset.sid, tools.querySelector('.nh-sh-editstatus'));
      });
      edd.addEventListener('click', (e) => {
        e.stopPropagation();
        close();
        nhSdOpenEditor(tools.dataset.sid);
      });
      menu.appendChild(set);
      menu.appendChild(rem);
      menu.appendChild(edd);
    }
    const rem = menu.querySelector('.nh-sc-mi-rem');
    if (rem) rem.style.display = nhScUrl(seriesId) ? '' : 'none';
  }

  // ===================== SHARED RATINGS MAP + CARD BADGES (A7) =====================
  // ONE session-wide copy of the whole ratings store feeds every browse surface:
  // card badges, global-search rows, the library rating filter (A8), and the
  // series header aggregate. Refreshed in the background when >60s old so other
  // users' ratings drift in; local saves patch it instantly via the
  // 'nh-rating-change' event book-details fires (no refetch).
  const nhRs = { items: null, at: 0, sig: '', fetching: false, tries: 0, dead: false };

  // ---- Community ratings map (#27): itemId -> { r, n, src, url } or { miss } ----
  // Same shape of life as nhRs: content signature, dead on 404, 60s refresh.
  const nhCm = { items: null, at: 0, sig: '', fetching: false, tries: 0, dead: false, gr: null, lean: null };
  function nhCmSig(items) {
    let n = 0, s = 0;
    Object.keys(items || {}).forEach((k) => { const e = items[k]; if (e && typeof e.r === 'number') { n++; s += e.r * 100 + e.n; } });
    return n + ':' + s;
  }
  function nhCmOn() { return nhSettings.communityRatings !== false; }
  function nhLfDimsOk(arr) { return nhCmOn() ? arr : arr.filter((d) => d !== 'community'); }
  function nhCmItems() {
    if (nhCm.dead || !nhCmOn()) return nhCm.items;
    const stale = Date.now() - nhCm.at > 60000;
    if ((!nhCm.items || stale) && !nhCm.fetching && nhCm.tries < 5) {
      const tok = nhSrToken();
      if (tok) {
        nhCm.fetching = true;
        nhCm.tries++;
        fetch('/_nh/api/community', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
          .then((r) => {
            if (r.status === 404 || r.status === 405) { nhCm.dead = true; return null; }
            return r.ok ? r.json() : null;
          })
          .then((j) => {
            nhCm.fetching = false;
            if (j && j.items) { nhCm.items = j.items; nhCm.at = Date.now(); nhCm.sig = nhCmSig(j.items); nhCm.tries = 0; }
          })
          .catch(() => { nhCm.fetching = false; });
      }
    }
    return nhCm.items;
  }
  function nhCmEntry(id) { const m = nhCm.items; const e = m && m[id]; return e && typeof e.r === 'number' ? e : null; }
  function nhCmScore(id) {
    const e = nhCmEntry(id);
    if (!e || typeof e.r !== 'number') return -1;
    const n = e.n;
    if (!n || n <= 0) return -1;
    const r = Math.max(1, Math.min(5, e.r));
    const p = (r - 1) / 4; // normalize 1..5 star scale to [0, 1]
    const z = 1.0; // 1-sigma interval (~68.3% confidence; z=1.0)
    const z2 = 1.0; // z^2
    const denom = 1 + z2 / n;
    const center = p + z2 / (2 * n);
    const spread = z * Math.sqrt((p * (1 - p)) / n + z2 / (4 * n * n));
    const lower = Math.max(0, (center - spread) / denom);
    return 1 + 4 * lower; // scale back to 1..5 range
  }
  function nhCmBucket(id) {
    const e = nhCmEntry(id);
    if (!e) return 'none';
    return e.r >= 4.5 ? '4.5+' : e.r >= 4 ? '4+' : e.r >= 3.5 ? '3.5+' : e.r >= 3 ? '3+' : 'under3';
  }
  window.__nhCmEntry = nhCmEntry;
  // Content signature, NOT a timestamp. The A8 view and the open panel key their
  // rebuild on this; keying on nhRs.at meant every 60s background refetch bumped
  // the signature with IDENTICAL data and the whole filtered shelf tore itself
  // down and re-rendered - Pawel's "it refreshes every once in a while" flicker.
  function nhRsSig(items) {
    let s = '';
    try { s = JSON.stringify(items); } catch (e) { return String(Date.now()); }
    let h = 0;
    for (let i = 0; i < s.length; i++) { h = ((h << 5) - h + s.charCodeAt(i)) | 0; }
    return s.length + ':' + h;
  }
  function nhRsItems() {
    if (nhRs.dead) return nhRs.items;
    const stale = Date.now() - nhRs.at > 60000;
    if ((!nhRs.items || stale) && !nhRs.fetching && nhRs.tries < 5) {
      const tok = nhSrToken();
      if (tok) {
        nhRs.fetching = true;
        nhRs.tries++;
        // admins read the gated twin: every name, masked rows flagged anon
        fetch(isUserAdmin() ? '/_nh/api/ratings-admin' : '/_nh/api/ratings', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
          .then((r) => {
            if (r.status === 404 || r.status === 405) { nhRs.dead = true; return null; } // old proxy: feature off
            return r.ok ? r.json() : null;
          })
          .then((j) => {
            nhRs.fetching = false;
            if (j && j.items) { nhRs.items = j.items; nhRs.at = Date.now(); nhRs.sig = nhRsSig(j.items); nhRs.tries = 0; }
          })
          .catch(() => { nhRs.fetching = false; });
      }
    }
    return nhRs.items;
  }

  function nhRsAvg(itemId) {
    const rs = nhRs.items && itemId ? nhRs.items[itemId] : null;
    if (!rs) return null;
    let sum = 0, n = 0;
    Object.keys(rs).forEach((k) => {
      const v = rs[k] && rs[k].stars;
      if (typeof v === 'number') { sum += v; n++; }
    });
    return n ? { avg: sum / n, n: n } : null;
  }

  // Scoped variant (#23): one user's stars instead of the average. n stays a
  // count so every rated/unrated check downstream keeps working unchanged.
  function nhRsAvgFor(itemId, uid) {
    if (!uid) return nhRsAvg(itemId);
    const rs = nhRs.items && itemId ? nhRs.items[itemId] : null;
    const v = rs && rs[uid] && rs[uid].stars;
    return typeof v === 'number' ? { avg: v, n: 1 } : null;
  }

  // Everyone who has rated anything, for the "whose ratings" picker, with how
  // many ratings each of them holds (shown as the row badge - Pawel: without
  // it, a one-rating user in the list reads like a bug).
  // `within`: an optional Set of item ids. When given, only ratings on THOSE
  // items count and zero-count users are dropped - the Monika case: her one
  // rating lives in the Polish library, so offering her in the English library
  // and answering "0 books" read as broken. Series-key ratings never count
  // (they are not books the filter could show).
  function nhRsRaters(within) {
    const rs = nhRs.items || {};
    const seen = {};
    Object.keys(rs).forEach((item) => {
      if (item.indexOf('series:') === 0) return;
      if (within && !within.has(item)) return;
      Object.keys(rs[item] || {}).forEach((uid) => {
        const e = rs[item][uid];
        if (!e || typeof e.stars !== 'number') return;
        // a masked name (names-privacy, #27 follow-up) cannot be offered in
        // the picker; admins get the name with an anon mark instead
        if (!e.user && !e.anon && !e.hidden) return;
        if (!seen[uid]) seen[uid] = { name: e.user || '?', n: 0, anon: !!(e.anon || e.hidden) };
        seen[uid].n++;
        if (e.anon || e.hidden) seen[uid].anon = true;
      });
    });
    return Object.keys(seen).map((id) => ({ id: id, name: seen[id].name, n: seen[id].n }))
      .sort((a, b) => a.name.localeCompare(b.name));
  }

  window.addEventListener('nh-rating-change', (e) => {
    const d = (e && e.detail) || {};
    if (!d.itemId || !nhRs.items) return;
    if (d.ratings && Object.keys(d.ratings).length) nhRs.items[d.itemId] = d.ratings;
    else delete nhRs.items[d.itemId];
    nhRs.at = Date.now();
    nhRs.sig = nhRsSig(nhRs.items); // a real change SHOULD rebuild the view
    // wipe render signatures so the next tick redraws badges + the A8 panel
    document.querySelectorAll('.nh-cr').forEach((b) => { delete b.dataset.nhCrSig; });
    nhLf.viewSig = '';
  });

  // ===================== HARDCOVER SYNC (#26) =====================
  // Push the user's NanoHive ratings to their Hardcover account. Every call
  // goes through /_nh/hc: nginx relays to Hardcover's GraphQL API because the
  // browser cannot reach it directly (CORS). The user's own token rides the
  // X-NH-HC header and the location is gated on the ABS token, so the relay
  // only works for signed-in users of THIS server. Hardcover ratings are HALF
  // stars 0.5-5, so quarter ratings round to the nearest half.
  const nhHc = { q: Promise.resolve(), last: 0, map: null, busy: false };
  const NH_HC_GAP = 1100; // Hardcover allows 60 requests/min - space them out

  function nhHcKey() {
    const k = String(nhSettings.hcKey || '').trim();
    return k ? k.replace(/^Bearer\s+/i, '') : '';
  }
  // Hardcover takes halves; quarters round DOWN (Pawel: a 4.25 is a 4, not a 4.5)
  function nhHcHalf(s) { return Math.max(0.5, Math.floor(Number(s) * 2) / 2); }
  function nhHcMap() {
    if (!nhHc.map) {
      try { nhHc.map = JSON.parse(localStorage.getItem('nh-hc-map')) || {}; } catch (e) { nhHc.map = {}; }
    }
    return nhHc.map;
  }
  function nhHcMapSave() { try { localStorage.setItem('nh-hc-map', JSON.stringify(nhHc.map || {})); } catch (e) {} }
  const nhHcStr = (s) => JSON.stringify(String(s == null ? '' : s)); // JSON strings are valid GraphQL literals
  const nhHcNorm = (s) => String(s || '').toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '').replace(/[^a-z0-9]+/g, ' ').trim();

  // One serialized, throttled GraphQL call: a bulk push and a live save can
  // never interleave past the rate limit because everything shares the queue.
  // Transient failures (HTML error pages, 429/5xx, network blips - observed in
  // the wild: they cost Pawel a "Mind Hack unmatched") retry with a backoff;
  // real GraphQL errors do not.
  function nhHcGql(query) {
    const once = () => {
      const wait = Math.max(0, nhHc.last + NH_HC_GAP - Date.now());
      return new Promise((res) => setTimeout(res, wait)).then(() => {
        nhHc.last = Date.now();
        return fetch('/_nh/hc', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            Authorization: 'Bearer ' + nhSrToken(),
            'X-NH-HC': 'Bearer ' + nhHcKey(),
          },
          credentials: 'include',
          body: JSON.stringify({ query: query }),
        });
      }).then((r) => r.text().then((txt) => {
        let j = null;
        try { j = JSON.parse(txt); } catch (e) {}
        if (!r.ok || !j) { const e2 = new Error('hc transport ' + r.status); e2.transient = true; throw e2; }
        if (j.errors && j.errors.length) {
          const msg = j.errors[0].message || 'hc error';
          const e3 = new Error(msg);
          e3.transient = /rate|throttl|timeout|limit/i.test(msg);
          throw e3;
        }
        return j.data || {};
      }));
    };
    const run = () => once().catch((e) => {
      if (!e.transient) throw e;
      return new Promise((res) => setTimeout(res, 2500)).then(once).catch((e2) => {
        if (!e2.transient) throw e2;
        return new Promise((res) => setTimeout(res, 6000)).then(once);
      });
    });
    const p = nhHc.q.then(run, run);
    nhHc.q = p.catch(() => {});
    return p;
  }

  // ---- Community matcher (#27): Goodreads through the abs-tract helper ----
  // Book pages fill the map in by themselves (book-details.js), one book per
  // visit; there is no whole-library run any more.
  function nhCmNorm(s) {
    return String(s || '').toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '').replace(/&/g, ' and ').replace(/[^a-z0-9]+/g, ' ').trim();
  }
  function nhCmTokenScore(a, b) {
    a = nhCmNorm(a); b = nhCmNorm(b);
    if (!a || !b) return 0;
    if (a === b) return 100;
    if (a.indexOf(b) === 0 || b.indexOf(a) === 0) return 90;
    const A = a.split(' '), B = b.split(' ');
    let c = 0;
    A.forEach((w) => { if (B.indexOf(w) >= 0) c++; });
    return Math.round(200 * c / (A.length + B.length));
  }
  // Title forms to try, most specific first: the title as given (minus
  // "(Unabridged)"-style tags), then without a subtitle / series tail
  // ("Camino Ghosts: A Novel (Camino, Book 3)" -> "Camino Ghosts",
  // "Throne of Glass bk 1" -> "Throne of Glass"), then for long titles the
  // first two words ("Freefall Expeditionary Force Mavericks" -> "Freefall
  // Expeditionary"), which is what Goodreads' title search actually answers.
  // Title forms. For SCORING: the title as given (minus "(Unabridged)"-style
  // tags) and its parts around a subtitle / series separator, each cleaned of
  // "Book 3", "#1", "(BBC)", "[02]". For QUERIES (forQuery): the same, ordered
  // most-promising first, plus a two-word stub for long titles. A part that is
  // just a series label with a number ("Minecraft 11") goes LAST: it finds
  // twenty other Minecraft books and never the right one.
  function nhCmTitleVariants(t, forQuery) {
    const base = String(t || '').replace(/\s*[\(\[][^\)\]]*(unabridged|abridged|audiobook|audio|edition|narrated)[^\)\]]*[\)\]]/ig, '').trim();
    const clean = (v) => String(v || '')
      .replace(/\s*\[[^\]]*\]\s*/g, ' ')                                     // [02], [Unabridged], anywhere
      .replace(/\s*\([^\)]*\)\s*$/, '').replace(/^\s*\([^\)]*\)\s*/, '')
      .replace(/,\s*(?:book|bk|vol\.?|volume|part|tom)\s*\d+.*$/i, '')
      .replace(/\s+(?:book|bk|vol\.?|volume|part|tom)\.?\s*\d+\s*$/i, '').replace(/\s*#\d+\s*$/, '').trim();
    const weak = (v) => { const w = v.split(/\s+/); return w.length <= 2 && /\d/.test(v) || /^(?:book|bk|vol\.?|volume|part|tom)\s*\d+$/i.test(v); };
    const out = [];
    const add = (v) => { v = String(v || '').trim(); if (v.length >= 3 && out.indexOf(v) < 0) out.push(v); };
    add(base);
    // "Boxcar Children 107- The Giant Yo-Yo Mystery": a dash glued to a number is a separator too
    const parts = base.replace(/(\d)-\s+/g, '$1 - ').split(/\s*[:–—]\s+|\s+-\s+/).map(clean).filter((p) => p.length >= 3);
    const strong = parts.filter((p) => !weak(p)), weakOnes = parts.filter(weak);
    strong.forEach(add);
    if (forQuery) {
      const first = strong[0] || clean(base);
      const words = first.split(/\s+/);
      if (words.length >= 4) add(words.slice(0, 2).join(' '));
      weakOnes.forEach(add);
    }
    return out;
  }
  // Goodreads titles carry the series in brackets: "Jade City (The Green Bone
  // Saga, #1)". Scoring ignores that tail, display keeps it.
  function nhCmBare(t) {
    let s = String(t || '').trim(), prev = '';
    while (s !== prev) { prev = s; s = s.replace(/\s*[\(\[][^\(\)\[\]]*[\)\]]\s*$/, '').trim(); }
    return s;
  }
  // Goodreads through abs-tract (#27), a small container the admin runs next
  // to the theme. The relay answers 404 while NH_GOODREADS_UPSTREAM is unset;
  // remembered for the session, and the card shows the setup steps instead.
  // /ratings is NanoHive's lean endpoint (one Goodreads call); a stock abs-tract
  // only has /search, which is slow and fails under load, so it is the fallback.
  // A 5xx gets two more tries (2s, 5s): the shared Goodreads key is rate limited.
  function nhCmGr(query, attempt) {
    if (nhCm.gr === false) return Promise.resolve(null);
    const path = nhCm.lean === false ? 'search' : 'ratings';
    return fetch('/_nh/gr/' + path + '?' + query, { headers: { Authorization: 'Bearer ' + nhSrToken() }, credentials: 'include' })
      .then((r) => {
        if (r.status === 404) {
          if (path === 'ratings') { nhCm.lean = false; return nhCmGr(query, attempt); }
          nhCm.gr = false; return null;
        }
        if (!r.ok) {
          // an expired login is not Goodreads being slow: no retries, ABS will ask to sign in again
          if (r.status === 401 || r.status === 403) throw new Error('gr auth ' + r.status);
          if ((attempt || 0) < 2) return new Promise((res) => setTimeout(res, (attempt || 0) ? 5000 : 2000)).then(() => nhCmGr(query, (attempt || 0) + 1));
          throw new Error('gr ' + r.status);
        }
        nhCm.gr = true;
        return r.json();
      })
      .then((j) => (j && Array.isArray(j.matches) ? j.matches : (Array.isArray(j) ? j : [])));
  }
  function nhCmCandGr(m) {
    const t = String(m.title || '') + (m.subtitle ? ': ' + m.subtitle : '');
    return { src: 'gr', key: String(m.goodreadsId || ''), url: String(m.goodreadsUrl || ''), title: t, by: m.author || '', year: m.publishedYear || '', r: typeof m.goodreadsAverageRating === 'number' ? m.goodreadsAverageRating : null, n: Number(m.goodreadsRatingsCount) || 0 };
  }
  // Best candidate by title+author overlap; among the near-equal ones the most
  // rated wins, that is the edition Goodreads treats as the book itself.
  function nhCmBest(meta, cands) {
    let bestC = 0;
    const scored = (cands || []).filter((c) => c.r != null && c.n > 0).map((c) => {
      let t = 0;
      const ct = nhCmBare(c.title);
      // Score against the real title forms only, never the two-word search
      // stub: "Wrath of" must not make a Percy Jackson book look right.
      // Every form of ours against every form of theirs: "Drama Alert!" has to
      // meet "Diary of Nate the Minecraft Ninja 22: Drama Alert!" on its own part.
      const theirs = nhCmTitleVariants(ct);
      nhCmTitleVariants(meta.title).forEach((v) => { theirs.forEach((x) => { t = Math.max(t, nhCmTokenScore(v, x)); }); });
      let a = nhCmTokenScore(meta.author, c.by);
      const surname = nhCmNorm(meta.author).split(' ').pop();
      if (a < 60 && surname && surname.length > 2 && nhCmNorm(c.by).indexOf(surname) >= 0) a = 70;
      let conf = Math.round(t * 0.7 + a * 0.3);
      // Same title, unrelated author, hardly anyone rated it: that is another
      // book that happens to share the name, not ours. Popular ones with an
      // exact title (a co-author listed differently) still pass.
      if (meta.author && a === 0 && c.n < 1000 && conf < 85) conf = 0;
      if (conf > bestC) bestC = conf;
      return { c: c, conf: conf };
    });
    if (bestC < 70) return null;
    const near = scored.filter((s) => s.conf >= Math.max(70, bestC - 8));
    near.sort((x, y) => (y.c.n - x.c.n) || (y.conf - x.conf));
    return near[0].c;
  }
  function nhCmEntryOf(c) {
    return { r: Math.round(c.r * 100) / 100, n: c.n, src: c.src, key: c.key, url: c.url, title: c.title, by: c.by, at: Date.now() };
  }
  // ISBN query first (exact), then title + author. The helper being down or
  // not set up is NOT a miss: the promise rejects and the caller tries later.
  // ISBN first (exact), then each title form in turn until one matches. Every
  // query is one Goodreads call, so the forms only get tried on a miss.
  function nhCmLookup(meta) {
    const isbn = String(meta.isbn || '').replace(/[^0-9Xx]/g, '');
    const au = meta.author ? '&author=' + encodeURIComponent(meta.author) : '';
    if (nhCm.gr === false) return Promise.reject(new Error('gr off'));
    const queries = (isbn ? ['query=' + encodeURIComponent(isbn)] : []).concat(nhCmTitleVariants(meta.title, true).map((v) => 'query=' + encodeURIComponent(v) + au));
    const step = (i) => {
      if (i >= queries.length) return null;
      return nhCmGr(queries[i]).then((m) => {
        if (nhCm.gr === false) throw new Error('gr off');
        return nhCmBest(meta, (m || []).map(nhCmCandGr)) || step(i + 1);
      });
    };
    return step(0).then((c) => {
      if (nhCm.gr === false) throw new Error('gr off');
      if (c) return nhCmEntryOf(c);
      return { miss: 1, at: Date.now() };
    });
  }
  // For the admin's "wrong book?" picker: rated candidates on top.
  // The short title form finds the most; the full one is tried only when the
  // short one returns nothing.
  function nhCmCandidates(meta) {
    // Cleaned parts first, the raw title next, the stub and weak parts last.
    const forms = nhCmTitleVariants(meta.title, true);
    const order = forms.length > 1 ? [forms[1], forms[0]].concat(forms.slice(2)) : forms;
    const au = meta.author ? '&author=' + encodeURIComponent(meta.author) : '';
    const step = (i) => {
      if (i >= order.length) return Promise.resolve([]);
      return nhCmGr('query=' + encodeURIComponent(order[i]) + au).catch(() => null).then((m) => ((m && m.length) ? m : step(i + 1)));
    };
    return step(0).then((m) => {
      const out = (m || []).map(nhCmCandGr);
      const rated = out.filter((c) => c.r != null && c.n > 0), rest = out.filter((c) => !(c.r != null && c.n > 0));
      return rated.concat(rest).slice(0, 12);
    });
  }
  window.__nhCmCandidates = nhCmCandidates;
  // Whole-library scan (#34), run BY THE HELPER: the books to check are
  // uploaded once, the helper works through them on its own (one Goodreads
  // call at a time), and every open page pulls the results into the store
  // now and then. Closing the tab, a sleeping phone or an expired login no
  // longer stop it. One run at a time (the helper answers 409 otherwise).
  // Books that already have a score or a manual pick are skipped; a miss
  // younger than six hours too.
  const nhCmScan = {
    skipped: null,
    H() { return { headers: { Authorization: 'Bearer ' + nhSrToken() }, credentials: 'include' }; },
    start(libIds, all) {
      const self = this, H = self.H();
      const books = [];
      const pageLib = (libId, n) => fetch('/api/libraries/' + libId + '/items?limit=500&page=' + n, H).then((r) => (r.ok ? r.json() : null)).then((j) => {
        const rows = (j && j.results) || [];
        rows.forEach((it) => { const md = (it.media && it.media.metadata) || {}; books.push({ id: it.id, title: md.title || '', author: md.authorName || '', isbn: md.isbn || '' }); });
        if (rows.length === 500 && n < 39) return pageLib(libId, n + 1);
      });
      return libIds.reduce((p, id) => p.then(() => pageLib(id, 0)), Promise.resolve())
        .then(() => fetch('/_nh/api/community', H).then((r) => (r.ok ? r.json() : null)))
        .then((j) => {
          const have = (j && j.items) || {};
          const fresh = Date.now() - 6 * 3600000;
          let skipped = 0;
          // "Everything again" re-checks every book except the admin's manual picks.
          const todo = books.filter((b) => { const e = have[b.id]; if (!e) return true; if (e.manual || (!all && (typeof e.r === 'number' || (e.miss && e.at > fresh)))) { skipped++; return false; } return true; });
          self.skipped = skipped;
          if (!todo.length) return { nothing: true };
          return fetch('/_nh/gr-admin/scan', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + nhSrToken() }, credentials: 'include', body: JSON.stringify({ books: todo }) })
            .then((r) => { if (r.status === 409) return { busy: true }; if (!r.ok) throw new Error('scan ' + r.status); return r.json(); });
        });
    },
    status() { return fetch('/_nh/gr/scan', this.H()).then((r) => (r.ok ? r.json() : null)).catch(() => null); },
    missed() { return fetch('/_nh/gr/scan/missed', this.H()).then((r) => (r.ok ? r.json() : null)).then((j) => ((j && j.items) || [])).catch(() => []); },
    stop() { return fetch('/_nh/gr-admin/scan/stop', Object.assign({ method: 'POST' }, this.H())).catch(() => {}); },
    // Pull whatever the helper has finished into the community store (server
    // side; nothing is uploaded from here). Cheap when there is nothing new.
    sync() { return fetch('/_nh/api/community-sync', this.H()).then((r) => (r.ok ? r.json() : null)).catch(() => null); },
  };
  // Every open page does the pulling, once a minute, for as long as a helper
  // is set up: a scan finishes into the store even if the admin closed the
  // settings long ago. Stops for the session when there is no helper.
  const nhCmSync = { at: Date.now() - 50000, off: false, busy: false };
  function nhCmSyncTick() {
    if (nhCmSync.off || nhCmSync.busy || nhCm.dead || nhCm.gr === false || !nhCmOn()) return;
    if (Date.now() - nhCmSync.at < 60000 || !nhSrToken()) return;
    nhCmSync.at = Date.now(); nhCmSync.busy = true;
    nhCmScan.sync().then((j) => {
      nhCmSync.busy = false;
      if (j && j.helper === 'off') { nhCmSync.off = true; return; }
      if (j && j.pulled) { nhCm.at = 0; nhCm.items = null; nhCmItems(); try { nhLf.viewSig = ''; } catch (e) {} }
    });
  }
  // Where might the helper be? ABS's own custom metadata providers first (an
  // abs-tract used for metadata already has its URL there), then the compose
  // service name, the docker host, and the host this page was opened on. Each
  // candidate is asked one question through the admin-only probe; the first
  // one that answers with rating numbers wins.
  function nhCmDiscover() {
    const H = { headers: { Authorization: 'Bearer ' + nhSrToken() }, credentials: 'include' };
    const norm = (u) => String(u || '').trim().replace(/\/+$/, '');
    const cands = [];
    const push = (u) => { u = norm(u); if (u && /^https?:\/\//.test(u) && cands.indexOf(u) < 0) cands.push(u); };
    return fetch('/api/custom-metadata-providers', H).then((r) => (r.ok ? r.json() : null)).catch(() => null).then((j) => {
      ((j && j.providers) || []).forEach((p) => {
        const u = norm(p.url);
        if (!u) return;
        push(u);
        if (!/\/goodreads$/.test(u)) push(u.replace(/\/[^/]*$/, '') + '/goodreads');
      });
      ['http://abs-tract:5555/goodreads', 'http://host.docker.internal:5555/goodreads', 'http://' + location.hostname + ':5555/goodreads', 'http://172.17.0.1:5555/goodreads', 'http://127.0.0.1:5555/goodreads'].forEach(push);
      const tryOne = (i) => {
        if (i >= cands.length) return null;
        const probe = (p) => fetch('/_nh/gr-probe?p=' + p + '&up=' + cands[i], H).then((r) => (r.ok ? r.json() : (r.status === 404 && p === 'ratings' ? probe('search') : null))).catch(() => null);
        return probe('ratings')
          .then((j2) => {
            const m = (j2 && j2.matches) || [];
            if (m.length && m.some((x) => typeof x.goodreadsRatingsCount === 'number')) return cands[i];
            return tryOne(i + 1);
          });
      };
      return tryOne(0);
    });
  }
  window.__nhCmOff = () => nhCm.gr === false || !nhCmOn();
  window.__nhCmEnabled = nhCmOn;
  window.__nhCmLookup = nhCmLookup;

  function nhHcMeta(itemId) {
    if (!nhHc.meta) nhHc.meta = {};
    if (nhHc.meta[itemId]) return Promise.resolve(nhHc.meta[itemId]);
    return fetch('/api/items/' + itemId, { headers: { Authorization: 'Bearer ' + nhSrToken() } })
      .then((r) => (r.ok ? r.json() : null))
      .then((it) => {
        const md = (it && it.media && it.media.metadata) || {};
        const m = {
          title: md.title || '',
          author: (md.authors && md.authors[0] && md.authors[0].name) || md.authorName || '',
          isbn: md.isbn || '',
          asin: md.asin || '',
        };
        nhHc.meta[itemId] = m;
        return m;
      });
  }

  // ABS item -> Hardcover book id. Identity first (ISBN/ASIN edition lookup),
  // then a title+author search whose best hit must actually match the title.
  // Resolutions are cached in localStorage; misses retry after a day.
  function nhHcResolve(itemId, retryMiss) {
    const map = nhHcMap();
    const hit = map[itemId];
    if (hit && hit.b) return Promise.resolve(hit.b);
    // a manual bulk run always retries cached misses - the cache only guards
    // the automatic per-save path from hammering the same absent book
    if (!retryMiss && hit && hit.miss && Date.now() - hit.ts < 24 * 3600 * 1000) return Promise.resolve(null);
    return nhHcMeta(itemId).then((m) => {
      if (!m || !m.title) return null;
      const idQ = [];
      if (m.isbn) {
        const digits = m.isbn.replace(/[^0-9Xx]/g, '');
        idQ.push('{' + (digits.length === 10 ? 'isbn_10' : 'isbn_13') + ': {_eq: ' + nhHcStr(digits) + '}}');
      }
      if (m.asin) idQ.push('{asin: {_eq: ' + nhHcStr(m.asin) + '}}');
      const viaEditions = idQ.length
        ? nhHcGql('query { editions(where: {_or: [' + idQ.join(', ') + ']}, limit: 1) { book_id } }')
            .then((d) => (d.editions && d.editions[0] && d.editions[0].book_id) || null)
            .catch(() => null)
        : Promise.resolve(null);
      return viaEditions.then((bid) => {
        if (bid) return bid;
        // Search by TITLE ONLY: the books search matches on titles, so a query
        // with the author glued on can miss books that are plainly there
        // (Pawel's Mind Hack). The author is checked against the HITS instead.
        return nhHcGql('query { search(query: ' + nhHcStr(m.title) + ', query_type: "Book", per_page: 10, page: 1) { results } }')
          .then((d) => {
            let hits = [];
            try {
              const res = typeof d.search.results === 'string' ? JSON.parse(d.search.results) : d.search.results;
              hits = (res && res.hits) || [];
            } catch (e) {}
            // leading articles are noise: ABS "Stars My Destination" must find
            // Hardcover's "The Stars My Destination"
            const deArticle = (s) => s.replace(/^(the|a|an) /, '');
            const want = deArticle(nhHcNorm(m.title));
            const wantAuthor = nhHcNorm(m.author);
            const titleOk = (h) => {
              const t = deArticle(nhHcNorm(h.document && h.document.title));
              return t && want && (t === want || t.indexOf(want) === 0 || want.indexOf(t) === 0);
            };
            const authorOk = (h) => {
              if (!wantAuthor) return true;
              const d2 = h.document || {};
              const names = d2.author_names || d2.author_name || (d2.contributions || []).map((c) => (c && c.author && c.author.name) || '');
              const a = nhHcNorm(Array.isArray(names) ? names.join(' ') : String(names || ''));
              if (!a) return true; // no author on the hit: do not veto a title match
              return wantAuthor.split(' ').some((tok) => tok.length > 2 && a.indexOf(tok) >= 0);
            };
            const best = hits.find((h) => titleOk(h) && authorOk(h)) || hits.find(titleOk);
            return best ? Number(best.document.id) : null;
          })
          .catch(() => null);
      });
    }).then((bid) => {
      const m2 = nhHcMap();
      m2[itemId] = bid ? { b: bid } : { miss: 1, ts: Date.now() };
      nhHcMapSave();
      return bid;
    });
  }

  // Returns 'created' | 'updated' | 'same' | 'unmatched'. force always writes
  // (the overwrite mode); without it an equal rating is left alone.
  function nhHcPush(itemId, stars, force, retryMiss) {
    if (!nhHcKey()) return Promise.reject(new Error('no key'));
    return nhHcResolve(itemId, retryMiss).then((bid) => {
      if (!bid) return 'unmatched';
      return nhHcGql('query { me { id user_books(where: {book_id: {_eq: ' + bid + '}}) { id rating } } }').then((d) => {
        const me = Array.isArray(d.me) ? d.me[0] : d.me;
        const ub = me && me.user_books && me.user_books[0];
        const rating = nhHcHalf(stars);
        if (ub && !force && Number(ub.rating) === rating) return 'same';
        if (ub) return nhHcGql('mutation { update_user_book(id: ' + ub.id + ', object: {rating: ' + rating + '}) { id } }').then(() => 'updated');
        // creating an entry needs a status - Read (3) is the only honest one
        // for a book being rated
        return nhHcGql('mutation { insert_user_book(object: {book_id: ' + bid + ', status_id: 3, rating: ' + rating + '}) { id } }').then(() => 'created');
      });
    });
  }

  // Live: each rating saved on a book page goes straight out while the toggle
  // is on. Removals stay local - wiping someone's Hardcover entry because they
  // cleared a star here would lose their reading dates over a misclick.
  window.addEventListener('nh-rating-change', (e) => {
    try {
      if (nhSettings.hcSync !== true || !nhHcKey()) return;
      const d = (e && e.detail) || {};
      if (!d.itemId || String(d.itemId).indexOf('series:') === 0) return;
      let uid = null;
      try { uid = window.$nuxt.$store.state.user.user.id; } catch (err) {}
      const mine = uid && d.ratings && d.ratings[uid];
      if (!mine || !mine.stars) return;
      nhHcPush(d.itemId, mine.stars, true).catch((err) => console.warn('[NanoHive] hardcover: ' + err.message));
    } catch (err) {}
  });

  // Bulk push from the panel: all ratings (overwrite) or only the ones that
  // differ from what Hardcover already has. While it runs, a spinner plus the
  // book currently being matched (Pawel: "it was not obvious"); afterwards the
  // unmatched titles are listed as links, not just counted.
  function nhHcBulk(diffOnly, status, missHost) {
    const T = nhGsT();
    if (nhHc.busy) return;
    if (!nhHcKey()) { if (status) status.textContent = T.hcNoKey || PANEL_T.en.hcNoKey; return; }
    nhHc.busy = true;
    if (missHost) missHost.textContent = '';
    const setStatus = (txt, spin) => {
      if (!status) return;
      status.textContent = '';
      if (spin) {
        const s = document.createElement('span');
        s.className = 'material-symbols nh-hc-spin';
        s.textContent = 'progress_activity';
        status.appendChild(s);
      }
      status.appendChild(document.createTextNode(txt));
    };
    setStatus('…', true);
    let uid = null;
    try { uid = window.$nuxt.$store.state.user.user.id; } catch (e) {}
    fetch('/_nh/api/ratings', { headers: { Authorization: 'Bearer ' + nhSrToken() }, credentials: 'include' })
      .then((r) => (r.ok ? r.json() : null))
      .then(async (store) => {
        const items = (store && store.items) || {};
        const mine = Object.keys(items).filter((id) => id.indexOf('series:') !== 0 && items[id][uid] && items[id][uid].stars);
        let done = 0, n = 0;
        const missedIds = [];
        for (const id of mine) {
          n++;
          let title = '';
          try { title = (await nhHcMeta(id)).title; } catch (e) {}
          setStatus(n + ' / ' + mine.length + (title ? ' · ' + title : ''), true);
          try {
            const res = await nhHcPush(id, items[id][uid].stars, !diffOnly, true);
            if (res === 'unmatched') missedIds.push(id); else done++;
          } catch (e) { missedIds.push(id); }
        }
        setStatus((T.hcDone || PANEL_T.en.hcDone).replace('%d', done).replace('%d', missedIds.length), false);
        if (missHost && missedIds.length) {
          const head = document.createElement('p');
          head.className = 'nh-hint';
          head.style.marginBottom = '2px';
          head.textContent = T.hcMissHead || PANEL_T.en.hcMissHead;
          missHost.appendChild(head);
          for (const id of missedIds) {
            let title = id;
            try { title = (await nhHcMeta(id)).title || id; } catch (e) {}
            const a = document.createElement('a');
            a.className = 'nh-hc-miss-row';
            a.href = getBaseNH() + '/item/' + id;
            a.textContent = title;
            missHost.appendChild(a);
          }
        }
      })
      .catch(() => { setStatus(T.srvErr || PANEL_T.en.srvErr, false); })
      .then(() => { nhHc.busy = false; });
  }

  // The other direction (#26): PULL the user's Hardcover ratings and feed them
  // into the SAME import pipeline the CSV files use - Hardcover has no CSV
  // export (open roadmap request), but with the key on file one API call
  // replaces the file dance. status_id 3 = Read drives the finished-marking.
  function nhHcImportPull(status) {
    const T = nhGsT();
    if (nhHc.busy) return;
    if (!nhHcKey()) { if (status) status.textContent = T.hcNoKey || PANEL_T.en.hcNoKey; return; }
    nhHc.busy = true;
    if (status) status.textContent = '…';
    nhHcGql('query { me { user_books(limit: 1000) { rating status_id book { title contributions { author { name } } editions(limit: 5) { isbn_13 isbn_10 asin } } } } }')
      .then((d) => {
        const me = Array.isArray(d.me) ? d.me[0] : d.me;
        const ubs = (me && me.user_books) || [];
        const parsed = { src: 'hardcover', total: ubs.length, unrated: 0, items: [] };
        ubs.forEach((u, i) => {
          const stars = Number(u.rating);
          const bk = u.book || {};
          if (!isFinite(stars) || stars <= 0 || !bk.title) { parsed.unrated++; return; }
          const authors = (bk.contributions || []).map((c) => c && c.author && c.author.name).filter(Boolean).join(', ');
          const isbns = [], asins = [];
          (bk.editions || []).forEach((ed) => {
            [ed && ed.isbn_13, ed && ed.isbn_10].forEach((v) => {
              if (!v) return;
              nhImpIsbnKeys(String(v)).forEach((k) => { if (isbns.indexOf(k) < 0) isbns.push(k); });
            });
            const a = ed && ed.asin && nhImpAsin(String(ed.asin));
            if (a && asins.indexOf(a) < 0) asins.push(a);
          });
          parsed.items.push({
            n: i + 1, title: bk.title, authors: authors,
            stars: Math.max(0.25, Math.min(5, Math.round(stars * 4) / 4)),
            review: '', isbns: isbns, asins: asins,
            read: u.status_id === 3,
            tKeys: nhImpTitleKeys(bk.title), surs: nhImpSurnames(authors),
          });
        });
        if (!parsed.items.length) { if (status) status.textContent = (nhImpT())('impErrNoRated'); return null; }
        return Promise.all([nhImpFetchIndex(), nhImpFetchMine()]).then((out) => {
          if (status) status.textContent = '';
          nhImpOpen(parsed, out[0].idx, out[1], out[0].libs);
        });
      })
      .catch(() => { if (status) status.textContent = T.srvErr || PANEL_T.en.srvErr; })
      .then(() => { nhHc.busy = false; });
  }

  // ===================== RATING IMPORT - StoryGraph / Goodreads CSV =====================
  // Bring an existing reading history in. The export has no idea what a library
  // item id is, so everything hinges on matching its rows to books on THIS server:
  //   1. ISBN-13 / ISBN-10 (indexed in both forms, so a 10 in the file finds a 13
  //      in the library and vice versa) or Amazon ASIN - an identity, trusted.
  //   2. Normalised title + author, exact after folding.
  //   3. A scored fuzzy pass, which is what actually carries most real files:
  //      StoryGraph leaves the ISBN empty for a good share of rows (35 of 317 in
  //      the file this was built against, 8 of them rated) and the titles differ
  //      in subtitle, edition noise and punctuation.
  // Nothing is written until the user has seen the matches: everything below only
  // BUILDS a plan, and the modal is where it gets confirmed.

  // --- CSV ---------------------------------------------------------------------
  // Written out rather than split(',') because reviews contain commas, quotes and
  // newlines, and Goodreads wraps its ISBNs as ="9780…" to stop Excel eating them.
  function nhImpCsv(text) {
    const s = String(text == null ? '' : text).replace(/^\uFEFF/, '');
    const rows = [];
    let row = [], cur = '', q = false;
    for (let i = 0; i < s.length; i++) {
      const c = s[i];
      if (q) {
        if (c === '"') {
          if (s[i + 1] === '"') { cur += '"'; i++; } else q = false;
        } else if (c !== '\r') cur += c;
      } else if (c === '"') q = true;
      else if (c === ',') { row.push(cur); cur = ''; }
      else if (c === '\n') { row.push(cur); rows.push(row); row = []; cur = ''; }
      else if (c !== '\r') cur += c;
    }
    if (cur !== '' || row.length) { row.push(cur); rows.push(row); }
    return rows;
  }

  function nhImpCell(v) {
    // Goodreads: ="9780306406157"  ->  9780306406157
    const s = String(v == null ? '' : v).trim();
    const m = /^="?(.*?)"?$/.exec(s);
    return (m ? m[1] : s).trim();
  }

  // --- ISBN --------------------------------------------------------------------
  function nhIsbnTo13(i10) {
    if (!/^[0-9]{9}[0-9X]$/.test(i10)) return '';
    const core = '978' + i10.slice(0, 9);
    let sum = 0;
    for (let i = 0; i < 12; i++) sum += (+core[i]) * (i % 2 ? 3 : 1);
    return core + String((10 - (sum % 10)) % 10);
  }
  function nhIsbnTo10(i13) {
    if (!/^978[0-9]{10}$/.test(i13)) return '';
    const core = i13.slice(3, 12);
    let sum = 0;
    for (let i = 0; i < 9; i++) sum += (+core[i]) * (10 - i);
    const c = (11 - (sum % 11)) % 11;
    return core + (c === 10 ? 'X' : String(c));
  }
  // Every form one identifier can be written as, so both sides of a comparison can
  // be indexed under all of them and the lookup is a plain hash hit.
  function nhImpIsbnKeys(raw) {
    const out = [];
    const add = (v) => { if (v && out.indexOf(v) < 0) out.push(v); };
    String(raw == null ? '' : raw).split(/[\s,;|]+/).forEach((part) => {
      const v = nhImpCell(part).toUpperCase().replace(/[^0-9X]/g, '');
      if (/^[0-9]{13}$/.test(v)) { add(v); add(nhIsbnTo10(v)); }
      else if (/^[0-9]{9}[0-9X]$/.test(v)) { add(v); add(nhIsbnTo13(v)); }
    });
    return out;
  }
  function nhImpAsin(raw) {
    const v = nhImpCell(raw).toUpperCase().replace(/[^0-9A-Z]/g, '');
    return /^B[0-9A-Z]{9}$/.test(v) ? v : '';
  }

  // --- folding + keys ----------------------------------------------------------
  // NFD strips accents from composed letters, but a fair few are single code points
  // that never decompose - Polish ł above all, which matters here (the file that
  // drove this is half Polish). Those are transliterated by hand first.
  const NH_IMP_XLIT = {
    'ł': 'l', 'đ': 'd', 'ø': 'o', 'æ': 'ae', 'œ': 'oe', 'ß': 'ss',
    'þ': 'th', 'ð': 'd', 'ı': 'i', 'ħ': 'h', 'ŋ': 'n', 'ŧ': 't'
  };
  function nhImpFold(s) {
    let out = String(s == null ? '' : s).toLowerCase();
    out = out.replace(/[‘’ʼ´`]/g, "'")
      .replace(/[“”]/g, '"')
      .replace(/[–-−]/g, '-');
    out = out.replace(/[łđøæœßþðıħŋŧ]/g, (c) => NH_IMP_XLIT[c] || c);
    try { out = out.normalize('NFD').replace(/[\u0300-\u036f]/g, ''); } catch (e) {}
    return out;
  }
  function nhImpKey(t) {
    return nhImpFold(t)
      .replace(/\([^)]*\)/g, ' ')        // (Discworld #3), (Unabridged)
      .replace(/\[[^\]]*\]/g, ' ')
      .replace(/[^a-z0-9]+/g, ' ')
      .trim()
      .replace(/\s+/g, ' ');
  }
  // A leading article is dropped as an ALTERNATIVE key, never as the only one: this
  // list cannot be right for 40 languages ("de" is an article in Dutch and a
  // preposition in Polish), so both forms are indexed and either may match.
  const NH_IMP_ART = /^(?:the|a|an|der|die|das|den|dem|le|la|les|un|une|el|los|las|il|lo|gli|os|het|een|de|ett|en)\s+/;
  function nhImpTitleKeys(title, subtitle) {
    const keys = [];
    const push = (v) => {
      const k = nhImpKey(v);
      if (k && keys.indexOf(k) < 0) keys.push(k);
    };
    const t = String(title == null ? '' : title);
    if (subtitle) push(t + ' ' + subtitle);
    push(t);
    // "The Sixth Extinction: An Unnatural History" -> "The Sixth Extinction",
    // which is how the same book is usually catalogued in a library.
    push(t.split(/\s*[:;]\s+|\s+[-–-]\s+/)[0]);
    keys.slice().forEach((k) => { const s = k.replace(NH_IMP_ART, ''); if (s && keys.indexOf(s) < 0) keys.push(s); });
    return keys;
  }
  // Surnames, not full names: "H.P. Lovecraft" and "Howard Phillips Lovecraft" have
  // to agree, and the same book is credited differently on either side. The two-word
  // tail is kept as well for "Le Guin" / "van Gogh" style names.
  // Author fields on BOTH sides are comma-joined lists that can hold translators and
  // illustrators (StoryGraph's own export puts the Polish translator first on some
  // rows), so this deliberately returns a SET and matching is any-to-any.
  function nhImpSurnames(raw) {
    const out = [];
    nhImpFold(raw).split(/[,;&/|]|\band\b/).forEach((part) => {
      const toks = part.replace(/\([^)]*\)/g, ' ').replace(/[^a-z0-9']+/g, ' ').trim().split(/\s+/).filter(Boolean);
      if (!toks.length) return;
      const last = toks[toks.length - 1];
      if (last.length >= 2 && out.indexOf(last) < 0) out.push(last);
      if (toks.length >= 2) {
        const tail = toks[toks.length - 2] + ' ' + last;
        if (out.indexOf(tail) < 0) out.push(tail);
      }
    });
    return out;
  }

  // Dice coefficient over character bigrams: cheap, needs no matrix, and unlike a
  // raw edit distance it is not thrown by a missing subtitle or a swapped word.
  function nhImpSim(a, b) {
    if (!a || !b) return 0;
    if (a === b) return 1;
    if (a.length < 2 || b.length < 2) return a === b ? 1 : 0;
    const A = {};
    let na = 0, nb = 0, inter = 0;
    for (let i = 0; i < a.length - 1; i++) { const g = a.substr(i, 2); A[g] = (A[g] || 0) + 1; na++; }
    for (let i = 0; i < b.length - 1; i++) {
      const g = b.substr(i, 2);
      nb++;
      if (A[g] > 0) { A[g]--; inter++; }
    }
    return (na + nb) ? (2 * inter) / (na + nb) : 0;
  }

  // --- parse a whole export ----------------------------------------------------
  // Both exports carry a rating column and a title; everything else is optional,
  // so the detector keys on the rating column's name and nothing more fragile.
  function nhImpParse(text) {
    const rows = nhImpCsv(text).filter((r) => r.length > 1);
    if (rows.length < 2) return { error: 'empty' };
    const head = rows[0].map((h) => nhImpCell(h));
    const at = (name) => head.indexOf(name);
    const col = { title: at('Title') };
    let src = '';
    if (at('Star Rating') >= 0) {
      src = 'storygraph';
      col.stars = at('Star Rating'); col.authors = at('Authors');
      col.review = at('Review'); col.uid = [at('ISBN/UID')];
      col.shelf = at('Read Status'); // 'read' = they finished it
    } else if (at('My Rating') >= 0) {
      src = 'goodreads';
      col.stars = at('My Rating'); col.authors = at('Author');
      col.extra = at('Additional Authors'); col.review = at('My Review');
      col.uid = [at('ISBN13'), at('ISBN')];
      col.shelf = at('Exclusive Shelf'); // 'read' = they finished it
    } else {
      // Generic branch, built for the HARDCOVER export (Pawel brought one and
      // it bounced): their columns are not published anywhere, so headers are
      // matched loosely - title + rating found = a usable file, whatever wrote
      // it. Every ISBN-ish column is collected.
      const norm = head.map((x) => String(x).toLowerCase().replace(/[^a-z0-9]+/g, ' ').trim());
      const find = (names) => {
        for (let k = 0; k < names.length; k++) { const i2 = norm.indexOf(names[k]); if (i2 >= 0) return i2; }
        return -1;
      };
      col.title = find(['title', 'book title', 'book', 'name']);
      col.stars = find(['rating', 'my rating', 'star rating', 'stars', 'user rating']);
      if (col.title >= 0 && col.stars >= 0) {
        src = 'hardcover';
        col.authors = find(['author', 'authors', 'author name', 'author names', 'contributors']);
        col.review = find(['review', 'my review', 'review text']);
        col.shelf = find(['status', 'read status', 'shelf', 'exclusive shelf']);
        col.uid = [];
        norm.forEach((hname, i2) => { if (hname.indexOf('isbn') >= 0 || hname.indexOf('asin') >= 0) col.uid.push(i2); });
      }
    }
    if (!src || col.title < 0) return { error: 'unknown' };

    const out = { src: src, total: rows.length - 1, unrated: 0, items: [] };
    for (let i = 1; i < rows.length; i++) {
      const r = rows[i];
      const cell = (n) => (n >= 0 && n < r.length ? nhImpCell(r[n]) : '');
      const raw = parseFloat(cell(col.stars));
      if (!isFinite(raw) || raw <= 0) { out.unrated++; continue; }
      // The store takes quarter steps, which is exactly StoryGraph's own precision,
      // so a 3.75 survives intact. Goodreads only ever exports whole stars.
      const stars = Math.max(0.25, Math.min(5, Math.round(raw * 4) / 4));
      const title = cell(col.title);
      if (!title) { out.unrated++; continue; }
      let authors = cell(col.authors);
      if (col.extra >= 0) {
        const ex = cell(col.extra);
        if (ex) authors = authors ? (authors + ', ' + ex) : ex;
      }
      const isbns = [], asins = [];
      (col.uid || []).forEach((n) => {
        const v = cell(n);
        if (!v) return;
        nhImpIsbnKeys(v).forEach((k) => { if (isbns.indexOf(k) < 0) isbns.push(k); });
        const a = nhImpAsin(v);
        if (a && asins.indexOf(a) < 0) asins.push(a);
      });
      // finished-marking (Pawel): the shelf column says whether they read it;
      // a file without one still gets read=true - a rating implies they did
      const shelf = col.shelf >= 0 ? cell(col.shelf).toLowerCase() : '';
      const isRead = col.shelf < 0 || shelf === 'read' || shelf === 'finished' || shelf === 'completed' || shelf === 'done' || shelf === '3';
      out.items.push({
        n: i, title: title, authors: authors, stars: stars,
        review: cell(col.review), isbns: isbns, asins: asins,
        read: isRead,
        tKeys: nhImpTitleKeys(title), surs: nhImpSurnames(authors)
      });
    }
    if (!out.items.length) return { error: 'norated', total: out.total };
    return out;
  }

  // --- library index -----------------------------------------------------------
  // The token buckets keep the fuzzy pass off a full cross product. Three-letter
  // words have to be in them: an earlier cut indexed only words of 4+, which left
  // every title built entirely of short words unreachable, "The Art of War" sat in
  // no bucket at all, so nothing could ever fuzzy-match it. Three letters only pays
  // off with a stop list, or "the" alone would put the whole library in one bucket.
  const NH_IMP_STOP = (
    'the and of a an to in on for with at by or from my is it as but be no not ' +
    'de la le les el il un une los las das der die den dem ein eine het een ett ' +
    'van von di del al e y o os as w z i na do po za od se si sie jest'
  ).split(' ').reduce((m, w) => { m[w] = 1; return m; }, {});
  const nhImpTok = (tk) => tk.split(' ').filter((w) => w.length >= 3 && !NH_IMP_STOP[w]);

  function nhImpIndex(recs) {
    const idx = { recs: recs, isbn: {}, asin: {}, ta: {}, t: {}, tok: {} };
    const push = (bag, k, rec) => { if (!k) return; (bag[k] || (bag[k] = [])).push(rec); };
    recs.forEach((rec) => {
      rec.isbns.forEach((k) => push(idx.isbn, k, rec));
      if (rec.asin) push(idx.asin, rec.asin, rec);
      rec.tKeys.forEach((tk) => {
        push(idx.t, tk, rec);
        rec.surs.forEach((s) => push(idx.ta, tk + '|' + s, rec));
        nhImpTok(tk).forEach((w) => push(idx.tok, w, rec));
      });
    });
    return idx;
  }

  function nhImpRec(it, libName) {
    const md = (it.media && it.media.metadata) || {};
    const isbns = md.isbn ? nhImpIsbnKeys(md.isbn) : [];
    return {
      id: it.id, libId: it.libraryId, libName: libName || '',
      title: md.title || '', subtitle: md.subtitle || '', author: md.authorName || '',
      isbns: isbns, asin: nhImpAsin(md.asin),
      tKeys: nhImpTitleKeys(md.title, md.subtitle), surs: nhImpSurnames(md.authorName)
    };
  }

  const nhImpOnce = (arr) => arr.filter((v, i) => arr.indexOf(v) === i);

  // --- match one row -----------------------------------------------------------
  // Confidence, in order: an identifier is an identity; an exact folded title WITH
  // an agreeing author is safe; anything else is a suggestion for the user to
  // confirm. `how` is what the row shows as its reason, so a questionable match is
  // never presented as a certainty.
  const NH_IMP_AUTO = 0.90;   // fuzzy score that may be pre-checked, author agreeing
  const NH_IMP_SHOW = 0.70;   // fuzzy score worth offering at all
  const NH_IMP_BLIND = 0.95;  // fuzzy score accepted with no author to check against

  function nhImpMatch(row, idx) {
    const seen = {};
    const cands = [];
    const add = (rec, how, score) => {
      if (!rec || seen[rec.id]) return;
      seen[rec.id] = 1;
      cands.push({ rec: rec, how: how, score: score });
    };
    const titleFit = (rec) => {
      let best = 0;
      row.tKeys.forEach((a) => { rec.tKeys.forEach((b) => { const s = nhImpSim(a, b); if (s > best) best = s; }); });
      return best;
    };
    const authorFit = (rec) => row.surs.some((s) => rec.surs.indexOf(s) >= 0);

    // 1. identifiers
    row.isbns.forEach((k) => (idx.isbn[k] || []).forEach((rec) => add(rec, 'isbn', 1)));
    row.asins.forEach((k) => (idx.asin[k] || []).forEach((rec) => add(rec, 'asin', 1)));
    // 2. exact folded title + agreeing author
    row.tKeys.forEach((tk) => {
      row.surs.forEach((s) => (idx.ta[tk + '|' + s] || []).forEach((rec) => add(rec, 'title', 1)));
    });
    // 3. exact folded title, author unknown or credited differently
    row.tKeys.forEach((tk) => (idx.t[tk] || []).forEach((rec) => add(rec, 'titleonly', titleFit(rec))));
    // 4. fuzzy, over books that share a distinctive word with the title
    const pool = {};
    row.tKeys.forEach((tk) => nhImpTok(tk).forEach((w) => {
      (idx.tok[w] || []).forEach((rec) => { pool[rec.id] = rec; });
    }));
    // A title made only of stop words ("It", "Us") shares no indexable word with
    // anything, so it would never reach the fuzzy pass. Fall back to the whole
    // shelf, but only while that is cheap, a huge library must not freeze the tab.
    if (!Object.keys(pool).length && idx.recs.length <= 4000) {
      idx.recs.forEach((rec) => { pool[rec.id] = rec; });
    }
    Object.keys(pool).forEach((id) => {
      const rec = pool[id];
      const s = titleFit(rec);
      if (s >= NH_IMP_SHOW) add(rec, 'fuzzy', s);
    });

    cands.forEach((c) => { c.author = authorFit(c.rec); });
    const rank = { isbn: 4, asin: 4, title: 3, titleonly: 2, fuzzy: 1 };
    cands.sort((a, b) => (rank[b.how] - rank[a.how]) || (b.score - a.score) || (b.author - a.author));

    if (!cands.length) return { row: row, how: 'none', cands: [] };
    const top = cands[0];
    // Two different books answering the same identifier or the same exact title is
    // not a match, it is a question, a duplicate edition, or two books that really
    // are called the same thing. Only fall through to certainty when it is unique.
    const ties = cands.filter((c) => c.how === top.how && c.score >= top.score - 0.001).length;
    let sure = false;
    if (top.how === 'isbn' || top.how === 'asin') sure = ties === 1;
    else if (top.how === 'title') sure = ties === 1;
    else if (top.how === 'fuzzy' && top.author && top.score >= NH_IMP_AUTO) sure = ties === 1;
    else if ((top.how === 'fuzzy' || top.how === 'titleonly') && !row.surs.length && top.score >= NH_IMP_BLIND) sure = ties === 1;
    return { row: row, how: sure ? top.how : 'check', pick: top, cands: cands.slice(0, 6), ties: ties };
  }

  // Build the whole plan: parsed rows x the libraries that take part in ratings.
  // Returns groups the modal renders directly. `already` is split out rather than
  // silently overwritten, someone else's import must not quietly rewrite a rating
  // the user typed in by hand.
  function nhImpPlan(parsed, idx, mine) {
    const groups = { auto: [], check: [], already: [], none: [] };
    parsed.items.forEach((row) => {
      const m = nhImpMatch(row, idx);
      m.sel = m.how !== 'none' && m.how !== 'check';
      m.chosen = m.pick ? m.pick.rec.id : '';
      if (m.how === 'none') groups.none.push(m);
      else if (mine[m.chosen] !== undefined) { m.mine = mine[m.chosen]; m.sel = false; groups.already.push(m); }
      else if (m.how === 'check') groups.check.push(m);
      else groups.auto.push(m);
    });
    // Two rows landing on one book (two editions of the same title), the second
    // would silently overwrite the first, so it is flagged and left unchecked.
    const taken = {};
    ['auto', 'check', 'already'].forEach((g) => groups[g].forEach((m) => {
      if (!m.chosen) return;
      if (taken[m.chosen]) { m.dup = true; m.sel = false; }
      else taken[m.chosen] = m;
    }));
    groups.check.sort((a, b) => ((b.pick && b.pick.score) || 0) - ((a.pick && a.pick.score) || 0));
    return groups;
  }

  // --- data the plan needs -----------------------------------------------------
  function nhImpMe() {
    try {
      const u = window.$nuxt && window.$nuxt.$store.state.user.user;
      return u && u.id ? String(u.id) : '';
    } catch (e) { return ''; }
  }

  // Only libraries that take part in ratings, a match in a library whose ratings
  // are switched off would be written and then never shown anywhere.
  function nhImpFetchIndex() {
    const tok = nhSrToken();
    if (!tok) return Promise.reject(new Error('no token'));
    return nhGsLibs().then((libs) => {
      const use = (libs || []).filter((l) => nhRatingsLibOn(l.id));
      if (!use.length) return { idx: nhImpIndex([]), libs: [] };
      return Promise.all(use.map((l) => fetch('/api/libraries/' + l.id + '/items?limit=0', {
        headers: { Authorization: 'Bearer ' + tok }, credentials: 'include'
      }).then((r) => (r.ok ? r.json() : null)).catch(() => null).then((j) => ({ lib: l, j: j }))))
        .then((res) => {
          const recs = [];
          res.forEach((pair) => {
            if (!pair.j || !Array.isArray(pair.j.results)) return;
            pair.j.results.forEach((it) => recs.push(nhImpRec(it, pair.lib.name)));
          });
          return { idx: nhImpIndex(recs), libs: use.map((l) => l.name), failed: res.filter((x) => !x.j).length };
        });
    });
  }

  // What the caller has already rated, so those rows can be held back instead of
  // silently overwritten. Read fresh rather than from the 60s cache: the whole
  // point is to be accurate about what is already there.
  function nhImpFetchMine() {
    const tok = nhSrToken(), me = nhImpMe();
    if (!tok || !me) return Promise.resolve({});
    return fetch('/_nh/api/ratings', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => {
        const out = {};
        const items = (j && j.items) || {};
        Object.keys(items).forEach((id) => {
          const mine = items[id] && items[id][me];
          if (mine && typeof mine.stars === 'number') out[id] = mine.stars;
        });
        return out;
      })
      .catch(() => ({}));
  }

  // --- write -------------------------------------------------------------------
  // Chunked by SERIALISED SIZE, not by row count: the nginx location caps the body
  // at 64k and njs cannot read a body nginx spooled to a temp file, so a hundred
  // long reviews must not be sent as one request. Each chunk is one store rewrite.
  const NH_IMP_CHUNK = 44000;
  function nhImpChunks(rows) {
    const out = [];
    let cur = [], size = 2;
    rows.forEach((row) => {
      const len = JSON.stringify(row).length + 1;
      if (cur.length && (size + len > NH_IMP_CHUNK || cur.length >= 200)) { out.push(cur); cur = []; size = 2; }
      cur.push(row); size += len;
    });
    if (cur.length) out.push(cur);
    return out;
  }

  function nhImpWrite(rows, onProgress) {
    const tok = nhSrToken();
    if (!tok) return Promise.reject(new Error('no token'));
    const chunks = nhImpChunks(rows);
    const res = { saved: 0, rejected: 0, failed: 0 };
    let done = 0;
    return chunks.reduce((chain, chunk) => chain.then(() => fetch('/_nh/api/ratings', {
      method: 'POST',
      headers: { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ items: chunk })
    }).then((r) => (r.ok ? r.json() : null)).then((j) => {
      if (j && j.ok) { res.saved += j.saved || 0; res.rejected += j.rejected || 0; }
      else res.failed += chunk.length;
      done += chunk.length;
      if (onProgress) onProgress(done, rows.length);
    }).catch(() => {
      res.failed += chunk.length;
      done += chunk.length;
      if (onProgress) onProgress(done, rows.length);
    })), Promise.resolve()).then(() => res);
  }

  // Everything above is pure; expose it so the test rig can drive the matcher on
  // real exports without a browser session.
  window.__nhImp = {
    csv: nhImpCsv, parse: nhImpParse, index: nhImpIndex, rec: nhImpRec,
    match: nhImpMatch, plan: nhImpPlan, sim: nhImpSim, fold: nhImpFold,
    titleKeys: nhImpTitleKeys, surnames: nhImpSurnames, isbnKeys: nhImpIsbnKeys,
    to13: nhIsbnTo13, to10: nhIsbnTo10, chunks: nhImpChunks,
    fetchIndex: nhImpFetchIndex, fetchMine: nhImpFetchMine, write: nhImpWrite,
    open: nhImpOpen
  };

  // --- the review dialog -------------------------------------------------------
  // A dry run is the whole feature: fuzzy matching against someone's library WILL
  // get things wrong, so every row is shown with the reason it matched before
  // anything is written, and the questionable ones carry a picker over the runners-up.
  function nhImpT() {
    const T = nhGsT();
    return (k) => T[k] || PANEL_T.en[k] || k;
  }
  // The dialog shows what is in the FILE and what will be written, at the store's
  // own quarter precision, deliberately NOT through nhStarText(), which snaps to
  // the reader's display-granularity setting. On the default half-star setting that
  // printed an imported 4.25 as "4.5" next to a row that was about to save 4.25.
  const nhImpStars = (v) => String(Number((+v || 0).toFixed(2)));

  // "%d rows, %d rated" style filling, positional, in order, no library needed.
  function nhImpFmt(s, args) {
    let i = 0;
    return String(s == null ? '' : s).replace(/%[sd]/g, () => {
      const v = args[i++];
      return v === undefined ? '' : String(v);
    });
  }

  function nhImpClose() {
    const m = document.getElementById('nh-imp-modal');
    if (m) m.remove();
    nhModalLock();
  }

  function nhImpOpen(parsed, idx, mine, libs) {
    nhImpClose();
    const t = nhImpT();
    const groups = nhImpPlan(parsed, idx, mine);
    const all = groups.auto.concat(groups.check, groups.already);
    const anyReview = parsed.items.some((r) => r.review);

    const overlay = document.createElement('div');
    overlay.id = 'nh-imp-modal';
    overlay.innerHTML =
      '<div class="nh-imp-bg"></div>' +
      '<div class="nh-imp-box" role="dialog" aria-modal="true">' +
        '<div class="nh-imp-head">' +
          '<span class="nh-imp-h">' + t('impTitle') + '</span>' +
          '<button type="button" class="nh-imp-x" aria-label="' + t('impCancel') + '">×</button>' +
        '</div>' +
        '<div class="nh-imp-sub"></div>' +
        '<div class="nh-imp-tiles"></div>' +
        '<div class="nh-imp-opts"></div>' +
        '<div class="nh-imp-body"></div>' +
        '<div class="nh-imp-foot">' +
          '<div class="nh-imp-prog"><div class="nh-imp-bar"></div><span class="nh-imp-pt"></span></div>' +
          '<div class="nh-imp-acts">' +
            '<button type="button" class="nh-imp-cancel">' + t('impCancel') + '</button>' +
            '<button type="button" class="nh-imp-go"></button>' +
          '</div>' +
        '</div>' +
      '</div>';
    document.body.appendChild(overlay);
    nhModalLock();

    const box = overlay.querySelector('.nh-imp-box');
    const sub = overlay.querySelector('.nh-imp-sub');
    const tiles = overlay.querySelector('.nh-imp-tiles');
    const opts = overlay.querySelector('.nh-imp-opts');
    const body = overlay.querySelector('.nh-imp-body');
    const go = overlay.querySelector('.nh-imp-go');
    const foot = overlay.querySelector('.nh-imp-foot');

    sub.textContent = nhImpFmt(t('impFrom'), [parsed.src === 'goodreads' ? 'Goodreads' : parsed.src === 'hardcover' ? 'Hardcover' : 'StoryGraph', parsed.total, parsed.items.length]);
    if (libs && libs.length) {
      const l = document.createElement('div');
      l.className = 'nh-imp-libs';
      l.textContent = nhImpFmt(t('impLibs'), [libs.join(', ')]);
      sub.appendChild(l);
    }

    const tile = (n, lab, cls) => {
      const d = document.createElement('div');
      d.className = 'nh-imp-tile' + (cls ? ' ' + cls : '');
      d.innerHTML = '<div class="nh-imp-tn">' + n + '</div><div class="nh-imp-tl"></div>';
      d.querySelector('.nh-imp-tl').textContent = lab;
      return d;
    };
    tiles.appendChild(tile(groups.auto.length, t('impMatched'), 'nh-imp-ok'));
    tiles.appendChild(tile(groups.check.length, t('impCheck'), 'nh-imp-warn'));
    tiles.appendChild(tile(groups.already.length, t('impAlready')));
    tiles.appendChild(tile(groups.none.length, t('impNone')));

    // options
    const optRow = (label, on, cb) => {
      const w = document.createElement('label');
      w.className = 'nh-imp-opt';
      const cb2 = document.createElement('input');
      cb2.type = 'checkbox'; cb2.checked = !!on;
      const sp = document.createElement('span');
      sp.textContent = label;
      w.appendChild(cb2); w.appendChild(sp);
      cb2.addEventListener('change', () => cb(cb2.checked));
      return w;
    };
    const state = { reviews: anyReview, over: false };
    if (anyReview) {
      opts.appendChild(optRow(nhImpFmt(t('impReviews'), [parsed.items.filter((r) => r.review).length]), true, (v) => { state.reviews = v; refresh(); }));
    }
    if (groups.already.length) {
      opts.appendChild(optRow(t('impOver'), false, (v) => {
        state.over = v;
        groups.already.forEach((m) => { m.sel = v && !m.dup; });
        render();
        refresh();
      }));
    }

    // rows
    const howLabel = (m) => {
      if (m.dup) return t('impDup');
      const c = m.cands.find((x) => x.rec.id === m.chosen) || m.pick;
      const h = c ? c.how : '';
      if (!c) return '';
      if (h === 'isbn') return 'ISBN';
      if (h === 'asin') return 'ASIN';
      if (h === 'title') return t('impByTitle');
      return Math.round((c.score || 0) * 100) + '%';
    };
    // Two candidates can be the SAME book in two libraries, the sandbox has Pride
    // and Prejudice twice, and then a title-and-author label reads identically for
    // both, which makes the picker useless. The library name is what tells them apart.
    const candLabel = (c) => c.rec.title + (c.rec.author ? ' · ' + c.rec.author : '')
      + (libs && libs.length > 1 && c.rec.libName ? ' · ' + c.rec.libName : '')
      + '  (' + (c.how === 'isbn' || c.how === 'asin' ? c.how.toUpperCase() : Math.round(c.score * 100) + '%') + ')';

    const mkRow = (m, group) => {
      const row = document.createElement('div');
      row.className = 'nh-imp-row' + (m.dup ? ' nh-imp-isdup' : '');
      row.dataset.n = String(m.row.n);
      const cb = document.createElement('input');
      cb.type = 'checkbox';
      cb.className = 'nh-imp-cb';
      cb.checked = !!m.sel;
      // an already-rated row with the SAME value has nothing to offer; one with
      // a DIFFERENT value stays checkable = "change mine to the imported one"
      cb.disabled = !m.chosen || (group === 'already' && m.mine === m.row.stars);
      cb.addEventListener('change', () => { m.sel = cb.checked; refresh(); });
      // A 17px checkbox is not a thumb-sized target; the label around it is what
      // gets the 34px hit area on a touchscreen (a wrapping label toggles natively).
      const cbw = document.createElement('label');
      cbw.className = 'nh-imp-cbw';
      cbw.appendChild(cb);
      row.appendChild(cbw);

      const stars = document.createElement('span');
      stars.className = 'nh-imp-stars';
      stars.textContent = nhImpStars(m.row.stars);
      row.appendChild(stars);

      const src = document.createElement('span');
      src.className = 'nh-imp-src';
      src.innerHTML = '<b></b><i></i>';
      src.querySelector('b').textContent = m.row.title;
      src.querySelector('i').textContent = m.row.authors || '';
      row.appendChild(src);

      const arrow = document.createElement('span');
      arrow.className = 'nh-imp-arrow';
      arrow.textContent = '→';
      row.appendChild(arrow);

      const why = document.createElement('span');
      why.className = 'nh-imp-why' + (m.dup ? ' nh-imp-whydup' : '');

      // The questionable rows get a picker over the runners-up, plus an explicit
      // "skip", leaving a wrong match selectable but unchecked is worse UX than
      // letting the reader correct it here. The picker IS the destination on those
      // rows: printing the chosen book beside it said the same thing twice and left
      // the row cramped on a phone.
      if (group === 'check' && m.cands.length) {
        const sel = document.createElement('select');
        sel.className = 'nh-imp-pick';
        m.cands.forEach((c) => {
          const o = document.createElement('option');
          o.value = c.rec.id;
          o.textContent = candLabel(c);
          sel.appendChild(o);
        });
        const skip = document.createElement('option');
        skip.value = ''; skip.textContent = t('impSkip');
        sel.appendChild(skip);
        sel.value = m.chosen || '';
        sel.addEventListener('change', () => {
          m.chosen = sel.value;
          if (!m.chosen) { m.sel = false; cb.checked = false; }
          cb.disabled = !m.chosen;
          why.textContent = howLabel(m);
          refresh();
        });
        row.appendChild(sel);
        nhSelectify(sel, true);
      } else {
        const dst = document.createElement('span');
        dst.className = 'nh-imp-dst';
        const rec = (m.cands.find((c) => c.rec.id === m.chosen) || m.pick || {}).rec;
        if (rec) {
          const b = document.createElement('b');
          b.textContent = rec.title;
          const i2 = document.createElement('i');
          i2.textContent = rec.author || '';
          dst.appendChild(b); dst.appendChild(i2);
          if (libs && libs.length > 1 && rec.libName) {
            const chip = document.createElement('em');
            chip.className = 'nh-imp-lib';
            chip.textContent = rec.libName;
            dst.appendChild(chip);
          }
        }
        row.appendChild(dst);
      }

      why.textContent = howLabel(m);
      row.appendChild(why);

      if (group === 'already' && typeof m.mine === 'number') {
        const cur = document.createElement('span');
        cur.className = 'nh-imp-mine';
        cur.textContent = nhImpFmt(t('impYours'), [nhImpStars(m.mine)]);
        row.appendChild(cur);
      }

      return row;
    };

    const section = (key, list, group, hint) => {
      if (!list.length) return;
      const sec = document.createElement('section');
      sec.className = 'nh-imp-sec';
      sec.dataset.group = group;
      const h = document.createElement('h3');
      h.innerHTML = '<span></span>';
      h.querySelector('span').textContent = t(key) + ' (' + list.length + ')';
      if (group !== 'none') {
        const tog = document.createElement('button');
        tog.type = 'button';
        tog.className = 'nh-imp-all';
        tog.textContent = t('impAll');
        tog.addEventListener('click', () => {
          const want = list.some((m) => !m.sel && m.chosen);
          list.forEach((m) => {
            if (m.chosen && !m.dup && !(group === 'already' && m.mine === m.row.stars)) m.sel = want;
          });
          render();
          refresh();
        });
        h.appendChild(tog);
      }
      sec.appendChild(h);
      if (hint) {
        const p = document.createElement('p');
        p.className = 'nh-imp-hint';
        p.textContent = hint;
        sec.appendChild(p);
      }
      if (group === 'none') {
        const ul = document.createElement('div');
        ul.className = 'nh-imp-nolist';
        list.forEach((m) => {
          const d = document.createElement('div');
          d.textContent = m.row.title + (m.row.authors ? ' · ' + m.row.authors : '');
          ul.appendChild(d);
        });
        sec.appendChild(ul);
      } else {
        list.forEach((m) => sec.appendChild(mkRow(m, group)));
      }
      body.appendChild(sec);
    };

    function render() {
      body.textContent = '';
      section('impCheck', groups.check, 'check', t('impCheckHint'));
      section('impMatched', groups.auto, 'auto');
      section('impAlready', groups.already, 'already', t('impAlreadyHint'));
      section('impNone', groups.none, 'none', t('impNoneHint'));
    }

    function chosenRows() {
      const out = [], taken = {};
      all.forEach((m) => {
        if (!m.sel || !m.chosen || taken[m.chosen]) return;
        taken[m.chosen] = 1;
        const row = { itemId: m.chosen, stars: m.row.stars };
        if (state.reviews && m.row.review) row.review = m.row.review;
        if (m.row.read) row.read = true; // for the finished-marking offer; the store POST ignores it
        out.push(row);
      });
      return out;
    }

    function refresh() {
      const n = chosenRows().length;
      go.textContent = nhImpFmt(t('impGo'), [n]);
      go.disabled = !n;
    }

    render();
    refresh();

    // The Escape listener is on `document`, so it has to come off on EVERY close path
    // (the ✕ and the scrim included) or each opened dialog leaves one behind.
    const esc = (e) => {
      if (e.key !== 'Escape' || !document.getElementById('nh-imp-modal')) return;
      // a themed dropdown inside the dialog owns Escape first
      if (document.querySelector('.nh-sel.nh-open')) return;
      e.stopPropagation();
      close();
    };
    const close = () => {
      document.removeEventListener('keydown', esc, true);
      nhImpClose();
    };
    overlay.querySelector('.nh-imp-x').addEventListener('click', close);
    overlay.querySelector('.nh-imp-cancel').addEventListener('click', close);
    overlay.querySelector('.nh-imp-bg').addEventListener('click', close);
    document.addEventListener('keydown', esc, true);
    overlay.__nhClose = close;

    go.addEventListener('click', () => {
      // After the run this same button becomes "Close" (nhImpDone rebinds it), and an
      // `onclick` assignment does NOT replace a listener added with addEventListener,       // so without this guard clicking Close would re-import everything first.
      if (overlay.dataset.nhDone) return;
      const rows = chosenRows();
      if (!rows.length) return;
      box.classList.add('nh-imp-busy');
      go.disabled = true;
      const bar = overlay.querySelector('.nh-imp-bar');
      const pt = overlay.querySelector('.nh-imp-pt');
      foot.classList.add('nh-imp-working');
      pt.textContent = nhImpFmt(t('impWorking'), [0, rows.length]);
      nhImpWrite(rows, (done, total) => {
        bar.style.width = Math.round((done / total) * 100) + '%';
        pt.textContent = nhImpFmt(t('impWorking'), [done, total]);
      }).then((res) => {
        // Every browse surface caches the store for 60s and card badges cache their
        // own rendered value; both have to be dropped or nothing shows until reload.
        nhRs.items = null; nhRs.at = 0; nhRs.tries = 0;
        document.querySelectorAll('.nh-cr').forEach((b) => { delete b.dataset.nhCrSig; });
        nhLf.viewSig = '';
        nhImpDone(res, rows);
      });
    });

    return overlay;
  }

  // The result replaces the dialog's body: a list of rows the user has already
  // approved is not information any more, and a bare toast would hide a failure.
  function nhImpDone(res, rows) {
    const t = nhImpT();
    const overlay = document.getElementById('nh-imp-modal');
    if (!overlay) return;
    overlay.dataset.nhDone = '1';          // the import handler must not fire again
    const box = overlay.querySelector('.nh-imp-box');
    box.classList.remove('nh-imp-busy');
    overlay.querySelector('.nh-imp-sub').textContent = '';
    overlay.querySelector('.nh-imp-tiles').textContent = '';
    overlay.querySelector('.nh-imp-opts').textContent = '';
    const body = overlay.querySelector('.nh-imp-body');
    body.textContent = '';
    const p = document.createElement('p');
    p.className = 'nh-imp-result';
    p.textContent = nhImpFmt(t('impDone'), [res.saved]);
    body.appendChild(p);
    const lost = (res.failed || 0) + (res.rejected || 0);
    if (lost) {
      const w = document.createElement('p');
      w.className = 'nh-imp-result nh-imp-bad';
      w.textContent = nhImpFmt(t('impFail'), [lost]);
      body.appendChild(w);
    }
    // Finished-marking (Pawel): the rows the source marks as read can become
    // finished on THIS server too, one PATCH each, offered - never assumed.
    const finIds = (rows || []).filter((r) => r.read).map((r) => r.itemId);
    if (finIds.length) {
      const q = document.createElement('p');
      q.className = 'nh-imp-result';
      q.style.marginTop = '14px';
      q.textContent = nhImpFmt(t('impMarkQ'), [finIds.length]);
      body.appendChild(q);
      const mk = document.createElement('button');
      mk.type = 'button';
      mk.className = 'nh-imp-btn';
      mk.textContent = (nhGsT().ysMarkDone || PANEL_T.en.ysMarkDone);
      body.appendChild(mk);
      const st = document.createElement('p');
      st.className = 'nh-imp-result';
      body.appendChild(st);
      mk.addEventListener('click', async () => {
        if (mk.disabled) return;
        mk.disabled = true;
        const tok = nhSrToken();
        let done = 0;
        for (const id of finIds) {
          st.textContent = done + ' / ' + finIds.length;
          try {
            const r2 = await fetch('/api/me/progress/' + id, {
              method: 'PATCH',
              headers: { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/json' },
              body: JSON.stringify({ isFinished: true }),
            });
            if (r2.ok) done++;
          } catch (e) {}
        }
        st.textContent = nhImpFmt(t('impMarkDone'), [done]);
      });
    }
    overlay.querySelector('.nh-imp-foot').classList.remove('nh-imp-working');
    const go = overlay.querySelector('.nh-imp-go');
    go.textContent = t('impCloseBtn');
    go.disabled = false;
    // the dialog's own close, so the document-level Escape listener comes off too
    go.onclick = overlay.__nhClose || nhImpClose;
    overlay.querySelector('.nh-imp-cancel').style.display = 'none';
  }

  // --- the panel card ----------------------------------------------------------
  function nhImpMountCard(host) {
    if (!host || host.dataset.nhImp) return;
    host.dataset.nhImp = '1';
    const t = nhImpT();
    // the supported sources, spelled out above the button (Pawel) - brand
    // names, no translation needed
    const srcs = document.createElement('div');
    srcs.className = 'nh-imp-srcs';
    srcs.textContent = 'Goodreads · StoryGraph · Hardcover';
    host.appendChild(srcs);
    const btn = document.createElement('button');
    btn.type = 'button';
    btn.className = 'nh-imp-btn';
    btn.textContent = t('impBtn');
    const file = document.createElement('input');
    file.type = 'file';
    file.accept = '.csv,text/csv';
    file.className = 'nh-imp-file';
    const note = document.createElement('div');
    note.className = 'nh-imp-note';
    host.appendChild(btn);
    host.appendChild(file);
    host.appendChild(note);

    const say = (msg, bad) => {
      note.textContent = msg || '';
      note.classList.toggle('nh-imp-bad', !!bad);
    };

    btn.addEventListener('click', () => { file.value = ''; file.click(); });
    file.addEventListener('change', () => {
      const f = file.files && file.files[0];
      if (!f) return;
      say(t('impReading'));
      const fr = new FileReader();
      fr.onerror = () => say(t('impErrRead'), true);
      fr.onload = () => {
        let parsed;
        try { parsed = nhImpParse(fr.result); } catch (e) { parsed = { error: 'unknown' }; }
        if (parsed.error) {
          say(t(parsed.error === 'norated' ? 'impErrNoRated' : (parsed.error === 'empty' ? 'impErrEmpty' : 'impErrUnknown')), true);
          return;
        }
        say(t('impMatching'));
        Promise.all([nhImpFetchIndex(), nhImpFetchMine()]).then((out) => {
          const built = out[0];
          if (!built.libs.length) { say(t('impErrNoLibs'), true); return; }
          say('');
          nhImpOpen(parsed, built.idx, out[1], built.libs);
        }).catch(() => say(t('impErrLoad'), true));
      };
      fr.readAsText(f, 'utf-8');
    });
  }

  // ===================== STAR-FILL GEOMETRY (shared) =====================
  // Every star widget is two text layers ('★★★★★' grey + amber) with the amber
  // one clipped by width. Clipping at value/5 of the ROW lands mid-BOX of the
  // fractional glyph, but the ★ ink sits left of its box middle (side bearings,
  // plus trailing letter-spacing, which Chrome counts into the row and Firefox
  // does not) so a half star read visibly past its middle (Pawel's screenshot).
  // This clips at the fractional glyph's INK instead: char boxes via Range
  // (engine truth in both browsers), ink span via canvas, cached per font|size.
  // Exposed as window.__nhStarFill for book-details.js (loaded after this file).
  const nhStarInkCache = {};
  function nhStarInkFor(cs) {
    const key = cs.fontFamily + '|' + cs.fontSize + '|' + cs.fontWeight;
    let m = nhStarInkCache[key];
    if (m) return m;
    try {
      const px = Math.max(12, Math.min(96, parseFloat(cs.fontSize) || 16));
      const cv = document.createElement('canvas');
      cv.width = Math.ceil(px * 3); cv.height = Math.ceil(px * 2);
      const ctx = cv.getContext('2d', { willReadFrequently: true });
      ctx.font = (cs.fontStyle || 'normal') + ' ' + (cs.fontWeight || '400') + ' ' + px + 'px ' + (cs.fontFamily || 'sans-serif');
      ctx.textBaseline = 'top';
      const x0 = Math.ceil(px / 2);
      ctx.fillText('★', x0, Math.ceil(px / 4));
      const adv = ctx.measureText('★').width;
      const d = ctx.getImageData(0, 0, cv.width, cv.height).data;
      let minX = -1, maxX = -1;
      for (let x = 0; x < cv.width; x++) {
        for (let y = 0; y < cv.height; y++) {
          if (d[(y * cv.width + x) * 4 + 3] > 16) { if (minX < 0) minX = x; maxX = x; break; }
        }
      }
      m = (adv > 0 && minX >= 0) ? { adv: adv / px, l: (minX - x0) / adv, w: (maxX + 1 - minX) / adv } : null;
    } catch (e) { m = null; }
    nhStarInkCache[key] = m || { adv: 1, l: 0, w: 1 }; // degenerate → same as naive
    return nhStarInkCache[key];
  }

  // Rating precision (github #1). Half stars used to be hardcoded into every
  // picker; whole stars and StoryGraph-style quarter stars are now both offered.
  // The step governs what a new rating can BE and how any value is DRAWN, so a
  // reader on whole stars never sees a partly-lit glyph.
  function nhStarStep() {
    const s = parseFloat(nhSettings.starStep);
    return (s === 1 || s === 0.5 || s === 0.25) ? s : 0.5;
  }
  function nhStarSnap(v) {
    const st = nhStarStep();
    return Math.round(Math.max(0, Math.min(5, +v || 0)) / st) * st;
  }
  // Number shown beside the stars. Whole/half steps never need more than one
  // decimal; quarters do, and trailing zeros are dropped either way (4.25, 4.5, 4).
  function nhStarText(v) {
    const s = nhStarSnap(v);
    return String(Number(s.toFixed(nhStarStep() === 0.25 ? 2 : 1)));
  }
  window.__nhStarStep = nhStarStep;
  window.__nhStarSnap = nhStarSnap;
  window.__nhStarText = nhStarText;

  function nhStarFill(starsEl, fillEl, v) {
    // One choke point for every star widget in the theme, including the book
    // page's (it calls this through window.__nhStarFill), snapping here is what
    // makes the precision setting reach all of them.
    v = nhStarSnap(v);
    const n = Math.floor(v), f = v - n;
    if (!f) { fillEl.style.width = (n / 5 * 100) + '%'; return; }
    let done = false;
    try {
      const base = starsEl.firstElementChild;
      const tn = base && base.firstChild;
      if (tn && tn.nodeType === 3 && tn.textContent.length >= n + 1) {
        const sr = starsEl.getBoundingClientRect();
        if (sr.width > 0) {
          const rg = document.createRange();
          rg.setStart(tn, n); rg.setEnd(tn, n + 1);
          const b = rg.getBoundingClientRect();
          if (b.width > 0) {
            const cs = getComputedStyle(base);
            const ink = nhStarInkFor(cs);
            const advPx = ink.adv * (parseFloat(cs.fontSize) || 16);
            const clip = (b.left - sr.left) + (ink.l + f * ink.w) * advPx;
            fillEl.style.width = Math.max(0, clip).toFixed(1) + 'px';
            fillEl.__nhSfN = 0;
            done = true;
          }
        }
      }
    } catch (e) {}
    if (!done) {
      // Detached or hidden right now (badges are built before being appended):
      // naive % as a stopgap, then a few frame-delayed retries once measurable.
      fillEl.style.width = (v / 5 * 100) + '%';
      const tries = fillEl.__nhSfN = (fillEl.__nhSfN || 0) + 1;
      if (tries <= 4) requestAnimationFrame(() => nhStarFill(starsEl, fillEl, v));
    }
  }
  window.__nhStarFill = nhStarFill;

  // Small reusable stars element. Default: "★★★★☆ (3)" (stars + review count,
  // used in search rows). noCount: "4.5 ★★★★☆" (avg number + stars, the card
  // badge form, user request).
  function nhCrStarsEl(r, cls, noCount) {
    const wrap = document.createElement('span');
    wrap.className = 'nh-cr-wrap' + (cls ? ' ' + cls : '');
    if (noCount) {
      const num = document.createElement('span');
      num.className = 'nh-cr-num';
      num.textContent = nhStarText(r.avg);
      wrap.appendChild(num);
    }
    const stars = document.createElement('span');
    stars.className = 'nh-rt-stars nh-cr-stars';
    const base = document.createElement('span');
    base.textContent = '★★★★★';
    const fill = document.createElement('span');
    fill.className = 'nh-rt-fill';
    fill.textContent = '★★★★★';
    stars.appendChild(base);
    stars.appendChild(fill);
    nhStarFill(stars, fill, r.avg);
    wrap.appendChild(stars);
    if (!noCount) {
      const n = document.createElement('span');
      n.className = 'nh-cr-n';
      n.textContent = '(' + r.n + ')';
      wrap.appendChild(n);
    }
    return wrap;
  }

  // Tick decorator: star badge over every rated book cover (library grid, home
  // shelves, series page, search results page). Cards are VIRTUAL, ABS recycles
  // the same book-card-<n> element for different books while scrolling, so the
  // badge is re-validated against the card vm's CURRENT item id each tick.
  // Paints (or clears) the caption badge for one card. `host` is the caption box
  // ABS already renders -- the badge is absolutely positioned inside it, so the
  // card keeps the height Vue computed for it. `anchor` is the truncating text
  // line the badge shares; it gets a measured padding-right so the two never
  // overlap. A fixed reserve cannot work: the badge scales with the cover-size
  // control, so its width is only knowable after it is in the DOM.
  function nhCrPaint(host, anchor, r, sig) {
    let badge = host.querySelector('.nh-cr');
    if (!r) {
      if (badge) badge.remove();
      if (anchor && anchor.style.paddingRight) anchor.style.paddingRight = '';
      return;
    }
    if (badge && badge.dataset.nhCrSig === sig) return;
    if (!badge) {
      badge = document.createElement('div');
      badge.className = 'nh-cr';
      host.appendChild(badge);
    }
    badge.dataset.nhCrSig = sig;
    badge.textContent = '';
    badge.appendChild(nhCrStarsEl(r, '', true));
    badge.title = nhStarText(r.avg) + ' / 5 (' + r.n + ')';
    if (anchor) {
      // Only on a signature change, so this forced measure runs about as often
      // as a card is recycled -- not every tick.
      const w = Math.ceil(badge.getBoundingClientRect().width);
      if (w > 0) anchor.style.paddingRight = (w + 8) + 'px';
      // The badge is bottom-anchored in CSS, which is only the author line while
      // the caption has two lines. ABS adds a THIRD line ("Finished <date>",
      // "Added <date>") whenever the shelf is sorted by a field the card does
      // not otherwise show, and the badge then sat on top of that text. Pin it
      // to the band the anchor line actually occupies. Measured against the host
      // rather than offsetTop so it does not depend on the offsetParent chain.
      const hr = host.getBoundingClientRect();
      const ar = anchor.getBoundingClientRect();
      if (hr.height > 0 && ar.height > 0) {
        badge.style.top = (ar.top - hr.top) + 'px';
        badge.style.height = ar.height + 'px';
        badge.style.bottom = 'auto';
      }
    }
  }

  function nhCardRatings() {
    const on = nhSettings.showRatings !== false && nhSettings.showCardRatings !== false
      && nhRatingsLibOn(nhCurLibId());
    if (!on) {
      document.querySelectorAll('.nh-cr').forEach((b) => b.remove());
      document.querySelectorAll('[cy-id="line2"][style*="padding-right"], [cy-id="detailBottomDisplayTitle"][style*="padding-right"]')
        .forEach((el) => { el.style.paddingRight = ''; });
      return;
    }
    if (!nhRsItems()) return;
    // series cards: derived rating = mean of the member books' averages (same
    // math as the series header/A8 filter), on the caption's title line
    document.querySelectorAll('[id^="series-card-"]').forEach((card) => {
      const host = card.querySelector('[cy-id="detailBottomText"]');
      if (!host) return;
      let s = null;
      try { s = card.__vue__ && card.__vue__.series; } catch (e) {}
      let sum = 0, n = 0;
      if (s && Array.isArray(s.books)) s.books.forEach((bk) => { const r2 = nhRsAvg(bk.id); if (r2) { sum += r2.avg; n++; } });
      const r = n ? { avg: sum / n, n: n } : null;
      nhCrPaint(host, host.querySelector('[cy-id="detailBottomDisplayTitle"]'), r, (s && s.id ? s.id : '') + ':' + (r ? r.avg.toFixed(2) : '') + ':' + n);
    });
    // our own book tiles on a collection page: the author row is a flex line, so
    // the badge is a sibling and needs no measured reserve
    document.querySelectorAll('.nh-col-tile').forEach((tile) => {
      const host = tile.querySelector('.nh-col-arow');
      if (!host) return;
      const id = ((tile.getAttribute('href') || '').split('/item/')[1] || '').split(/[?#]/)[0];
      const r = id ? nhRsAvg(id) : null;
      nhCrPaint(host, null, r, id + ':' + (r ? r.avg.toFixed(2) + ':' + r.n : 'none'));
    });
    const cards = document.querySelectorAll('[id^="book-card-"]');
    if (!cards.length) return;
    cards.forEach((card) => {
      const host = card.querySelector('[id^="description-area-"]');
      if (!host) return;
      let id = null;
      try { id = card.__vue__ && card.__vue__.libraryItem && card.__vue__.libraryItem.id; } catch (e) {}
      const r = id ? nhRsAvg(id) : null;
      // line2 is the author; on a card with no author it is absent and the title
      // row is the last line, so that becomes the anchor instead.
      const anchor = host.querySelector('[cy-id="line2"]') || host.querySelector('[cy-id="title"]');
      nhCrPaint(host, anchor, r, id + ':' + (r ? r.avg.toFixed(2) + ':' + r.n : 'none'));
    });
  }

  // Swap the card-hover play triangle for the rounder play_circle glyph (user
  // request; the read button's auto_stories glyph stays). The overlay markup is
  // always in the DOM (hover only reveals it), so the swap never flashes. CSS
  // can't select by ligature text, hence JS.
  function nhCardPlayIcon() {
    // Unconditional now that the hover-polish toggle is gone (see the CSS block
    // in applySettings for why the removed key must not be read here either).
    document.querySelectorAll('[id^="cover-area-"] div[class*="hover:scale-110"] .material-symbols').forEach((s) => {
      if (s.textContent === 'play_arrow') s.textContent = 'play_circle';
    });
    // Hide the ebook-format tag (epub/pdf, hover overlay) on books that HAVE
    // audio, it reads like the whole book is an ebook (user report). Cards are
    // recycled, so re-assert against the CURRENT item every tick.
    document.querySelectorAll('[id^="book-card-"]').forEach((card) => {
      const ca = card.querySelector('[id^="cover-area-"]');
      if (!ca) return;
      let hasAudio = false;
      try {
        const md = card.__vue__ && card.__vue__.libraryItem && card.__vue__.libraryItem.media;
        hasAudio = !!(md && (md.numTracks || md.numEpisodes || md.duration));
      } catch (e) {}
      const fmt = ca.querySelector('div[class*="absolute"] > span[class*="text-white/80"]');
      if (fmt && fmt.parentElement) fmt.parentElement.style.display = hasAudio ? 'none' : '';
    });
  }

  // ===================== LIBRARY RATING SORT & FILTER (A8 v2) =====================
  // Rating options live INSIDE the native Filter/Sort dropdowns (user request,   // one control surface, not two): a "Rating" section injected into the filter
  // menu and a "Rating" entry in the sort menu, rows cloned from native markup.
  // They COMPOSE with the native state: the overlay grid fetches with the user's
  // current filterBy/orderBy applied server-side (genre filter + rated-only works
  // together; native order is kept unless rating sort is chosen). While active
  // the dropdown button labels carry the combined state; clearing every rating
  // option hands straight back to the untouched native shelf.
  // sorts: ORDERED list of levels [{d, dir}], pick order = precedence, each
  // click cycles ↑ → ↓ → off. filters: {dim: [values]}, OR within a dimension,
  // AND across dimensions, AND with ABS's native filter (server-side) and the
  // rating quick-filters. nhLf.sort stays a derived STRING signature so every
  // existing truthiness/signature check keeps working unchanged.
  // who: '' = everyone's average; a userId scopes BOTH the rating sort and the
  // rating quick-filters to that one person's stars (GitHub #23).
  const nhLf = { mode: 'items', sort: '', sorts: [], filter: '', filters: {}, who: '', sub: null, libId: null, items: null, itemsKey: '', fetching: false, view: null, viewSig: '', needReset: false, libTotal: null, libTotalKey: '', libTotalFetching: false };
  const NH_LF_FILTERS = ['rated', 'min4', 'min3', 'unrated'];
  const NH_LF_SORT_DIMS = ['author', 'series', 'title', 'year', 'added', 'duration', 'narrator', 'rating', 'community'];
  // v2.1: tag / publisher / series / format added so the new panel is a SUPERSET
  // of ABS's own filter menu. That matters because the panel replaces those
  // dropdowns rather than sitting next to them, anything ABS could filter by and
  // this could not would simply have become unreachable.
  const NH_LF_FILTER_DIMS = ['genre', 'author', 'narrator', 'series', 'tag', 'publisher', 'language', 'decade', 'progress', 'format', 'community'];
  // The series LIST page has different objects (no media metadata of its own), so
  // it gets the dimensions that can be derived from its member books.
  const NH_LF_SERIES_SORT_DIMS = ['title', 'rating', 'books', 'progress'];
  const NH_LF_SERIES_FILTER_DIMS = ['progress'];

  function nhLfSyncSortSig() {
    // `opt` is part of the signature: switching Author between surname order and
    // first-name order changes the result, so the view has to be rebuilt for it.
    nhLf.sort = nhLf.sorts.map((s) => s.d + (s.opt ? ':' + s.opt : '') + (s.dir < 0 ? '-' : '+')).join(',');
  }
  // Per-level options. Author is the only dimension with one so far: ABS gives us
  // both "Dickens, Charles" (authorNameLF) and "Charles Dickens" (authorName), and
  // sorting was silently always the former (Pawel asked for the choice).
  const NH_LF_SORT_OPTS = {
    author: [{ v: '', k: 'mfBySurname' }, { v: 'first', k: 'mfByFirstName' }],
  };
  function nhLfOptLabel(T, d, opt) {
    const list = NH_LF_SORT_OPTS[d];
    if (!list) return '';
    const found = list.find((o) => (o.v || '') === (opt || '')) || list[0];
    return T[found.k] || PANEL_T.en[found.k] || found.v || '';
  }
  function nhLfFxSig() {
    const ks = Object.keys(nhLf.filters).filter((k) => nhLf.filters[k] && nhLf.filters[k].length);
    return ks.sort().map((k) => k + ':' + nhLf.filters[k].slice().sort().join('|')).join(';');
  }
  function nhLfFxCount() {
    return Object.keys(nhLf.filters).reduce((n, k) => n + ((nhLf.filters[k] || []).length), 0);
  }
  function nhLfDimLabel(T, d) {
    const K = {
      author: 'lfAuthor', series: 'lfSeries', title: 'lfTitle', year: 'lfYear', added: 'lfAdded',
      duration: 'lfDuration', narrator: 'lfNarrator', rating: 'lfSort', genre: 'lfGenre',
      language: 'lfLanguage', decade: 'lfYear', progress: 'lfProgress',
      tag: 'lfTag', publisher: 'lfPublisher', format: 'lfFormat', books: 'lfBooks', community: 'lfCommunity',
    }[d];
    return T[K] || PANEL_T.en[K] || d;
  }
  function nhLfMeta(li) { return (li.media && li.media.metadata) || {}; }
  function nhLfSeriesKey(md) {
    // seriesName is "Name #2" ("A #1, B #4" for multi-series, first wins);
    // a standalone book interleaves by TITLE where a series name would sort.
    const first = String(md.seriesName || '').split(', ')[0];
    const m2 = first.match(/^(.*?)\s+#([\d.]+)$/);
    return {
      group: (m2 ? m2[1] : (first || String(md.title || ''))).toLowerCase(),
      seq: m2 ? parseFloat(m2[2]) : Infinity,
    };
  }
  function nhLfProgressOf(pgMap, id) { return pgMap[id] || 'none'; }
  function nhLfProgressMap() {
    const map = {};
    try {
      (window.$nuxt.$store.state.user.user.mediaProgress || []).forEach((p) => {
        if (!p || !p.libraryItemId) return;
        map[p.libraryItemId] = p.isFinished ? 'finished' : ((p.progress || 0) > 0 ? 'progress' : 'none');
      });
    } catch (e) {}
    return map;
  }
  // What a series counts as, from its member books: done / started / untouched.
  // Same three buckets a single book has, so one Progress filter covers both pages.
  function nhLfSeriesProgress(pg, li) {
    const ids = (li.books || []).map((b) => b.id || b.libraryItemId).filter(Boolean);
    if (!ids.length) return 'none';
    let done = 0, started = 0;
    ids.forEach((id) => { const s = pg[id]; if (s === 'finished') done++; else if (s === 'progress') started++; });
    if (done === ids.length) return 'finished';
    return (done || started) ? 'progress' : 'none';
  }
  // Format/status flags, the part of ABS's native filter menu that is not a
  // free-text field. Multi-valued: an item can be both audio and ebook.
  function nhLfFormats(li) {
    const md = nhLfMeta(li);
    const media = li.media || {};
    const out = [];
    if (media.numTracks || media.duration || media.numAudioFiles) out.push('audio');
    if (media.ebookFormat || media.ebookFile) out.push('ebook');
    if (md.abridged) out.push('abridged');
    if (md.explicit) out.push('explicit');
    if (li.isMissing) out.push('missing');
    if (li.isInvalid) out.push('invalid');
    return out;
  }
  // value catalog for a filter dimension, with counts, from the fetched items
  function nhLfValues(dim, T) {
    const cnt = new Map();
    const add = (v) => { if (v || v === 0) cnt.set(v, (cnt.get(v) || 0) + 1); };
    const pg = dim === 'progress' ? nhLfProgressMap() : null;
    const series = nhLf.mode === 'series';
    (nhLf.items || []).forEach((li) => {
      if (series) {
        if (dim === 'progress') add(nhLfSeriesProgress(pg, li));
        return;
      }
      const md = nhLfMeta(li);
      if (dim === 'genre') (md.genres || []).forEach(add);
      else if (dim === 'tag') (li.media && li.media.tags ? li.media.tags : (md.tags || [])).forEach(add);
      else if (dim === 'author') String(md.authorName || '').split(', ').forEach((s) => add(s.trim()));
      else if (dim === 'narrator') String(md.narratorName || '').split(', ').forEach((s) => add(s.trim()));
      else if (dim === 'publisher') add(md.publisher);
      // "Name #3, Other #1" -> each series name without its sequence number
      else if (dim === 'series') String(md.seriesName || '').split(', ').forEach((s) => add(s.replace(/\s+#[\d.]+$/, '').trim()));
      else if (dim === 'language') add(md.language);
      else if (dim === 'decade') { const y = parseInt(md.publishedYear, 10); if (y) add(Math.floor(y / 10) * 10 + 's'); }
      else if (dim === 'progress') add(nhLfProgressOf(pg, li.id));
      else if (dim === 'format') nhLfFormats(li).forEach(add);
      else if (dim === 'community') add(nhCmBucket(li.id));
    });
    let vals = [...cnt.entries()].map(([v, n]) => ({ v: v, n: n }));
    if (dim === 'progress') {
      const order = { finished: 0, progress: 1, none: 2 };
      vals.sort((a, b) => (order[a.v] === undefined ? 9 : order[a.v]) - (order[b.v] === undefined ? 9 : order[b.v]));
    } else if (dim === 'decade') vals.sort((a, b) => b.v.localeCompare(a.v));
    else if (dim === 'format') {
      const order = ['audio', 'ebook', 'abridged', 'explicit', 'missing', 'invalid'];
      vals.sort((a, b) => order.indexOf(a.v) - order.indexOf(b.v));
    } else if (dim === 'community') {
      const order = ['4.5+', '4+', '3.5+', '3+', 'under3', 'none'];
      vals.sort((a, b) => order.indexOf(a.v) - order.indexOf(b.v));
    } else vals.sort((a, b) => (b.n - a.n) || String(a.v).localeCompare(String(b.v)));
    // No cap: the panel searches and paginates these itself, and a 60-value cut
    // silently hid most authors in a big library.
    return vals;
  }
  function nhLfValueLabel(T, dim, v) {
    if (dim === 'progress') {
      return v === 'finished' ? (T.lfPgFinished || PANEL_T.en.lfPgFinished)
        : v === 'progress' ? (T.lfPgInProgress || PANEL_T.en.lfPgInProgress)
        : (T.lfPgNot || PANEL_T.en.lfPgNot);
    }
    if (dim === 'format') {
      const K = { audio: 'lfFmtAudio', ebook: 'lfFmtEbook', abridged: 'lfFmtAbridged', explicit: 'lfFmtExplicit', missing: 'lfFmtMissing', invalid: 'lfFmtInvalid' }[v];
      return (K && (T[K] || PANEL_T.en[K])) || v;
    }
    if (dim === 'community') {
      if (v === 'none') return T.lfUnrated || PANEL_T.en.lfUnrated;
      if (v === 'under3') return '< 3';
      return v.replace('+', '★+');
    }
    return v;
  }

  // Restore the shelf's own fetcher and refresh it back to native data.
  function nhLfRelease(vm) {
    if (vm && vm.__nhLfPatched) {
      if (vm.__nhOrigFetch) vm.fetchEntites = vm.__nhOrigFetch;
      vm.__nhLfPatched = false;
      // The natSig block has usually JUST scheduled its stale-margin nudge for
      // this same clear (filter leaving changes natSig). Letting that
      // setCardSize+executeRebuild fire 60ms into the resetEntities re-fetch
      // races two rebuilds over one shelf, cards from both layouts pile up as
      // a broken mosaic (Pawel's clear-filters screenshot). The reset alone
      // re-lays everything; the nudge is redundant here.
      clearTimeout(nhLf.relayoutT);
      try { vm.resetEntities(); } catch (e) {} // patched ⇒ we were serving; refresh to native
    }
    nhLf.viewSig = '';
    nhLf.view = null;
  }

  function nhLfReset() {
    document.querySelectorAll('#toolbar .nh-lf-count, #toolbar .nh-lf-clearx').forEach((el) => el.remove());
    clearTimeout(nhLf.relayoutT); // never let a scheduled nudge outlive the page it measured
    try { nhFfTeardown(); } catch (e) {}
    nhLf.sort = '';
    nhLf.sorts = [];
    nhLf.filter = '';
    nhLf.filters = {};
    nhLf.sub = null;
    // Clear the native signature too. nhLfNative() returns a DIFFERENT default per
    // mode ('name' for series vs 'media.metadata.title' for items), so a stale
    // signature from the previous mode made a plain Books<->Series navigation look
    // like a filter change and fired the relayout below on every such hop.
    nhLf.natSig = undefined;
    const bs = document.getElementById('bookshelf');
    nhLfRelease(bs && bs.__vue__);
    // The no-match panel is only maintained by the ACTIVE-filter render path, so
    // clearing the filter left it (and the body class hiding the shelf) in place
    // forever, the Clear button looked dead (Pawel). Tear it down here, where
    // every clear route converges.
    nhLfEmptyState(false);
  }

  // ---------- remember the view per library (GitHub #17) ----------
  // ABS keeps your sort and filter in its user settings, so its choice survives a
  // reload. Ours lived only in memory and was gone the moment you navigated away,
  // which reads as the sort "not working" rather than "not saved".
  // Stored in the theme's own per-user settings, so it rides the same server sync
  // and follows you to another browser. Keyed by library AND mode, because the
  // books view and the series view have different dimensions entirely.
  function nhLfViewKey(libId, mode) { return libId + ':' + (mode || 'items'); }
  function nhLfPersist() {
    if (!nhLf.libId) return;
    const key = nhLfViewKey(nhLf.libId, nhLf.mode);
    const views = { ...(nhSettings.libViews || {}) };
    const fx = {};
    Object.keys(nhLf.filters || {}).forEach((k) => {
      if (nhLf.filters[k] && nhLf.filters[k].length) fx[k] = nhLf.filters[k].slice();
    });
    const empty = !nhLf.sorts.length && !nhLf.filter && !Object.keys(fx).length;
    if (empty) delete views[key];
    else views[key] = { s: nhLf.sorts.map((x) => ({ d: x.d, dir: x.dir, opt: x.opt })), f: nhLf.filter || '', fx: fx };
    // Cap it: one entry per library per mode is tiny, but a server that has had
    // libraries deleted should not accumulate their views forever.
    const keys = Object.keys(views);
    if (keys.length > 40) delete views[keys[0]];
    nhSettings.libViews = views;
    saveSettings();
  }
  function nhLfRestore(libId, mode) {
    const saved = (nhSettings.libViews || {})[nhLfViewKey(libId, mode)];
    if (!saved) return;
    const dims = nhLfDimsOk(mode === 'series' ? NH_LF_SERIES_SORT_DIMS : NH_LF_SORT_DIMS);
    const fdims = nhLfDimsOk(mode === 'series' ? NH_LF_SERIES_FILTER_DIMS : NH_LF_FILTER_DIMS);
    // Drop anything this build no longer offers, so a removed dimension cannot
    // wedge a library into a sort nobody can see or clear.
    nhLf.sorts = (saved.s || []).filter((x) => x && dims.indexOf(x.d) >= 0)
      .map((x) => ({ d: x.d, dir: x.dir < 0 ? -1 : 1, opt: x.opt }));
    nhLf.filter = NH_LF_FILTERS.indexOf(saved.f) >= 0 ? saved.f : '';
    nhLf.filters = {};
    Object.keys(saved.fx || {}).forEach((k) => {
      if (fdims.indexOf(k) >= 0 && Array.isArray(saved.fx[k]) && saved.fx[k].length) nhLf.filters[k] = saved.fx[k].slice();
    });
    nhLfSyncSortSig();
    nhLf.viewSig = '';
  }

  function nhLfFilterLabel(T) {
    return nhLf.filter === 'rated' ? (T.lfRated || PANEL_T.en.lfRated) :
      nhLf.filter === 'unrated' ? (T.lfUnrated || PANEL_T.en.lfUnrated) :
      nhLf.filter === 'min4' ? '4★+' : nhLf.filter === 'min3' ? '3★+' : '';
  }

  // The two toolbar dropdowns: [0] = filter, [1] = sort (the kebab comes later).
  function nhLfDropdowns(toolbar) {
    const btns = Array.from(toolbar.querySelectorAll('button[aria-haspopup="menu"]'));
    return { filter: btns[0] || null, sort: btns[1] || null };
  }

  // Fetch (once per native filter/sort state) the full item list the overlay
  // composes over. Also called the moment a dropdown OPENS, so on big libraries
  // the list is usually already loaded by the time a rating option is picked.
  // Mode-aware native state: the items page and the series page keep their
  // sort/filter under different user-settings keys.
  function nhLfNative() {
    let st = {};
    try { st = window.$nuxt.$store.state.user.settings || {}; } catch (e) {}
    if (nhLf.mode === 'series') {
      return { fb: st.seriesFilterBy || 'all', ob: st.seriesSortBy || 'name', od: !!st.seriesSortDesc, fbKey: 'seriesFilterBy' };
    }
    return { fb: st.filterBy || 'all', ob: st.orderBy || 'media.metadata.title', od: !!st.orderDesc, fbKey: 'filterBy' };
  }
  // ABS's own filter / sort select components (#29). The modern panel hides
  // their dropdowns, so it shows and drives that state itself, reusing their
  // localized selectedText and the sort component's item list.
  function nhLfNativeVm(kind) {
    try {
      const toolbar = document.getElementById('toolbar');
      let w = toolbar && nhLfDropdowns(toolbar)[kind];
      // Books use controls-library-sort-select (items in selectItems); the series
      // page uses the plain controls-sort-select (items prop). Same shape otherwise.
      const want = kind === 'filter' ? ['controls-library-filter-select'] : ['controls-library-sort-select', 'controls-sort-select'];
      while (w && w !== toolbar) {
        const vm = w.__vue__;
        // Nuxt's auto-registered components carry the tag, not always a name.
        const nm = vm && vm.$options && (vm.$options.name || vm.$options._componentTag);
        if (want.indexOf(nm) >= 0) return vm;
        w = w.parentElement;
      }
    } catch (e) {}
    return null;
  }
  function nhLfNativePatch(patch) {
    try { window.$nuxt.$store.dispatch('user/updateUserSettings', patch); } catch (e) {}
    nhLf.viewSig = '';
    setTimeout(() => { try { nhFfRender(); } catch (e) {} }, 80);
  }

  // Unfiltered library total (denominator for the count pill when only a
  // NATIVE filter is active, the shelf itself then holds the filtered count).
  function nhLfEnsureLibTotal(libId) {
    const key = nhLf.mode + '|' + libId;
    if (nhLf.libTotalKey === key && nhLf.libTotal !== null) return;
    if (nhLf.libTotalKey !== key) { nhLf.libTotalKey = key; nhLf.libTotal = null; }
    if (nhLf.libTotalFetching) return;
    const tok = nhSrToken();
    if (!tok) return;
    nhLf.libTotalFetching = true;
    fetch('/api/libraries/' + libId + '/' + (nhLf.mode === 'series' ? 'series' : 'items') + '?limit=1', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => {
        nhLf.libTotalFetching = false;
        if (j && typeof j.total === 'number' && nhLf.libTotalKey === key) nhLf.libTotal = j.total;
      })
      .catch(() => { nhLf.libTotalFetching = false; });
  }

  function nhLfEnsureItems(libId) {
    const nat = nhLfNative();
    const key = nhLf.mode + '|' + libId + '|' + nat.fb + '|' + nat.ob + '|' + (nat.od ? 1 : 0);
    if (nhLf.itemsKey !== key) {
      nhLf.itemsKey = key;
      nhLf.items = null;
      nhLf.fetching = false;
    }
    if (!nhLf.items && !nhLf.fetching) {
      const tok = nhSrToken();
      if (tok) {
        nhLf.fetching = true;
        // items: limit=0 = unlimited; series: limit=0 returns ZERO rows, use a big cap
        const url = '/api/libraries/' + libId + '/' + (nhLf.mode === 'series' ? 'series?limit=100000' : 'items?limit=0') +
          '&filter=' + encodeURIComponent(nat.fb) + '&sort=' + encodeURIComponent(nat.ob) + '&desc=' + (nat.od ? 1 : 0);
        fetch(url, { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
          .then((r) => (r.ok ? r.json() : null))
          .then((j) => {
            nhLf.fetching = false;
            if (j && Array.isArray(j.results) && nhLf.itemsKey === key) nhLf.items = j.results;
          })
          .catch(() => { nhLf.fetching = false; });
      }
    }
    return { key: key, nat: nat };
  }

  // Build a menu row that matches the native listbox markup: li AND inner text
  // span classes are cloned from a real native row so typography/padding always
  // match. The active mark is a plain ✓/↓/↑ character, the material-symbols
  // ligature does NOT reliably apply to injected spans (renders as the literal
  // word "check" on some builds).
  function nhLfMenuItem(ul, key, label, mark, onPick) {
    // clone from a NON-selected native row, the first row is usually the
    // active one and carries a selected-state bg- class we must not inherit.
    // `mark` is the right-side character(s): '✓', '1↑', '›', '' for none,     // plain characters only (material-symbols ligatures don't apply reliably
    // to injected spans).
    const lis = ul.querySelectorAll('li:not(.nh-lf-mi):not(.nh-lf-mhead)');
    const nativeLi = Array.prototype.find.call(lis, (el) => !/(^|\s)bg-/.test(String(el.className))) || lis[0] || null;
    const nativeSpan = nativeLi ? nativeLi.querySelector('span:not(.material-symbols)') : null;
    // Vue SCOPED styles key off data-v-* attributes, classes alone aren't
    // enough, without the scope attribute the native row background/padding
    // rules never match the injected rows.
    const cloneScope = (from, to) => {
      if (!from) return;
      Array.prototype.forEach.call(from.attributes, (at) => {
        if (at.name.indexOf('data-v-') === 0) to.setAttribute(at.name, at.value);
      });
    };
    const li = document.createElement('li');
    li.className = (nativeLi ? nativeLi.className : 'select-none relative py-2 pr-9 cursor-pointer hover:bg-white/5') + ' nh-lf-mi';
    cloneScope(nativeLi, li);
    li.dataset.key = key;
    const txt = document.createElement('span');
    txt.className = (nativeSpan ? nativeSpan.className : 'font-normal block truncate') + ' nh-lf-mtxt';
    cloneScope(nativeSpan, txt);
    txt.textContent = label;
    li.appendChild(txt);
    if (mark) {
      const chk = document.createElement('span');
      chk.className = 'nh-lf-mcheck';
      chk.textContent = mark;
      li.appendChild(chk);
    }
    li.addEventListener('click', (e) => { e.stopPropagation(); onPick(); });
    return li;
  }

  // Inject/refresh the Rating section in an open dropdown menu. Menus are
  // Vue-rendered on open and swapped for submenus, so this runs each tick and
  // rebuilds our rows whenever they are missing or stale.
  function nhLfInjectMenus(toolbar, T) {
    const dd = nhLfDropdowns(toolbar);
    [['filter', dd.filter], ['sort', dd.sort]].forEach((pair) => {
      const kind = pair[0], btn = pair[1];
      if (!btn) return;
      if (!btn.dataset.nhLfWarm) {
        btn.dataset.nhLfWarm = '1';
        btn.addEventListener('click', () => {
          if (nhLf.libId) { nhLfEnsureItems(nhLf.libId); nhRsItems(); nhCmItems(); }
          // a fresh open always starts at the MAIN list, a submenu drill-down
          // must not survive the close. Vue's own toggle ran first (its
          // listener predates ours), so showMenu already holds the new state.
          let r2 = btn.parentElement;
          while (r2 && !/(^|\s)w-36(\s|$)/.test(String(r2.className))) r2 = r2.parentElement;
          if (r2 && r2.__vue__ && r2.__vue__.showMenu) nhLf.sub = null;
        });
      }
      // the dropdown's OUTER wrapper (w-36 sm:w-44 md:w-48) holds both the
      // button and its menu; btn.closest('.relative') is an inner box without it
      let rel = btn.parentElement;
      while (rel && rel !== toolbar && !/(^|\s)w-36(\s|$)/.test(String(rel.className))) rel = rel.parentElement;
      if (!rel || rel === toolbar) return;
      // the filter dropdown has a main list AND a submenu page (first row =
      // arrow_left "Back"), only ever inject into the main one. The items
      // page's sort menu is ul.librarySortMenu; the series page's sort menu is
      // a plain listbox, hence the fallback.
      const isNatSub = (u) => {
        const g = u.firstElementChild && u.firstElementChild.querySelector('.material-symbols');
        return !!(g && g.textContent.trim() === 'arrow_left');
      };
      // Tag ABS's own value lists (compact styling hook that does not depend on
      // :has support) in their OWN pass, find() below short-circuits on the
      // main list and would never reach the submenu sitting after it.
      Array.prototype.forEach.call(rel.querySelectorAll('ul:not(.librarySortMenu)'), (u) => {
        u.classList.toggle('nh-lf-natsub', isNatSub(u));
      });
      const pickUl = () => Array.prototype.find.call(rel.querySelectorAll('ul:not(.librarySortMenu)'), (u) => !isNatSub(u)) || null;
      const ul = kind === 'sort' ? (rel.querySelector('ul.librarySortMenu') || pickUl()) : pickUl();
      // menu closed (unmounted, or v-show hidden)? a submenu drill-down must
      // not survive into the next open, reopening lands on the main list.
      if (kind === 'filter' && nhLf.sub && (!ul || !ul.getBoundingClientRect().height)) nhLf.sub = null;
      if (!ul) return;
      // rebuild our rows when missing or when the active state changed (menus
      // are re-rendered by Vue on open and swapped for submenu pages)
      const cur = nhLf.sort + '|' + nhLf.filter + '|' + nhLfFxSig() + '|' + (nhLf.sub || '') + '|' + ((nhLf.items || []).length ? 1 : 0);
      if (ul.querySelector('.nh-lf-mi') && ul.dataset.nhLfState === cur) return;
      ul.querySelectorAll('.nh-lf-mi, .nh-lf-mhead').forEach((n) => n.remove());
      ul.dataset.nhLfState = cur;
      const mkHead = (txt) => {
        const h = document.createElement('li');
        h.className = 'nh-lf-mhead';
        h.textContent = txt;
        return h;
      };
      // clicks mutate state and let the next tick redraw the rows in place,       // the menu STAYS OPEN so several levels/values can be stacked in one go
      // classic mode's equivalent of nhFfApply, same persistence (#17)
      const touch = () => { nhLf.viewSig = ''; try { nhLfPersist(); } catch (e) {} };
      if (kind === 'filter') {
        // native submenu page? (content replaced, e.g. genre list), detect by
        // the "All" row being absent; skip injecting there
        const first = ul.querySelector('li');
        if (!first) return;
        const natives = () => Array.prototype.filter.call(ul.children, (n) => !/nh-lf-/.test(n.className));
        if (nhLf.sub) {
          ul.classList.add('nh-lf-subopen');
          // OUR value submenu: native rows give way (restored on Back/close)
          natives().forEach((n) => { if (!n.dataset.nhLfHid) { n.dataset.nhLfHid = n.style.display || ''; n.style.display = 'none'; } });
          ul.style.maxHeight = '360px';
          ul.style.overflowY = 'auto';
          const dim = nhLf.sub;
          ul.appendChild(mkHead(nhLfDimLabel(T, dim)));
          ul.appendChild(nhLfMenuItem(ul, 'back', (T.ctBack || PANEL_T.en.ctBack), '', () => {
            nhLf.sub = null;
          }));
          nhLfValues(dim, T).forEach((o) => {
            const on = (nhLf.filters[dim] || []).indexOf(o.v) >= 0;
            ul.appendChild(nhLfMenuItem(ul, 'v:' + o.v, nhLfValueLabel(T, dim, o.v) + '  (' + o.n + ')', on ? '✓' : '', () => {
              const arr = nhLf.filters[dim] = (nhLf.filters[dim] || []);
              const i2 = arr.indexOf(o.v);
              if (i2 >= 0) arr.splice(i2, 1); else arr.push(o.v);
              touch();
            }));
          });
          if (!(nhLf.items || []).length) ul.appendChild(mkHead('…'));
          return;
        }
        ul.classList.remove('nh-lf-subopen');
        natives().forEach((n) => { if (n.dataset.nhLfHid !== undefined) { n.style.display = n.dataset.nhLfHid; delete n.dataset.nhLfHid; } });
        ul.style.maxHeight = '';
        ul.style.overflowY = '';
        ul.appendChild(mkHead(T.lfSecFilters || PANEL_T.en.lfSecFilters));
        NH_LF_FILTERS.forEach((key) => {
          const label = key === 'rated' ? (T.lfRated || PANEL_T.en.lfRated) : key === 'unrated' ? (T.lfUnrated || PANEL_T.en.lfUnrated) : key === 'min4' ? '4★+' : '3★+';
          ul.appendChild(nhLfMenuItem(ul, key, label, nhLf.filter === key ? '✓' : '', () => {
            nhLf.filter = nhLf.filter === key ? '' : key;
            touch();
          }));
        });
        // stackable dimension filters: value submenus, OR within a dimension,
        // AND across dimensions and with the native filter
        {
          nhLfDimsOk(nhLf.mode === 'series' ? NH_LF_SERIES_FILTER_DIMS : NH_LF_FILTER_DIMS).forEach((dim) => {
            const nOn = (nhLf.filters[dim] || []).length;
            ul.appendChild(nhLfMenuItem(ul, 'dim:' + dim, nhLfDimLabel(T, dim) + (nOn ? '  (' + nOn + ')' : ''), '›', () => {
              nhLf.sub = dim;
              if (nhLf.libId) { nhLfEnsureItems(nhLf.libId); nhRsItems(); nhCmItems(); }
            }));
          });
        }
      } else {
        // SORT: a multi-level builder. Every dimension is a row; clicking
        // cycles ↑ → ↓ → off; pick order = precedence, shown as "1↑".
        ul.appendChild(mkHead(T.lfSecSort || PANEL_T.en.lfSecSort));
        const dims = nhLfDimsOk(nhLf.mode === 'series' ? NH_LF_SERIES_SORT_DIMS : NH_LF_SORT_DIMS);
        dims.forEach((d) => {
          const idx = nhLf.sorts.findIndex((s) => s.d === d);
          const lvl = idx >= 0 ? nhLf.sorts[idx] : null;
          const mark = lvl ? (idx + 1) + (lvl.dir < 0 ? '↓' : '↑') : '';
          // rating starts DESCENDING (best first), everything else A→Z first
          const def = (d === 'rating' || d === 'community') ? -1 : 1;
          ul.appendChild(nhLfMenuItem(ul, 's:' + d, nhLfDimLabel(T, d), mark, () => {
            if (!lvl) nhLf.sorts.push({ d: d, dir: def });
            else if (lvl.dir === def) lvl.dir = -def;
            else nhLf.sorts.splice(idx, 1);
            nhLfSyncSortSig();
            touch();
          }));
        });
      }
    });
  }

  // Reflect the combined state on the dropdown buttons' labels. NEVER mutate
  // Vue's own text node (replacing it detaches it from the vdom and future
  // native updates silently patch the orphan): the native span is only hidden,
  // and a sibling span of ours shows the combined text, re-derived from the
  // still-live hidden native label each tick.
  // A funnel on the filter button, arrows on the sort button, the two
  // dropdowns were indistinguishable (Pawel). Inline SVG, never the icon font
  // (ligatures don't apply to injected spans on some builds).
  const NH_LF_ICONS = {
    filter: '<svg viewBox="0 0 24 24" width="13" height="13" fill="currentColor" aria-hidden="true"><path d="M3 5h18l-7 8.4V19l-4 2.2v-7.8L3 5z"/></svg>',
    sort: '<svg viewBox="0 0 24 24" width="13" height="13" fill="currentColor" aria-hidden="true"><path d="M7.5 4 3.5 9h3v11h2V9h3l-4-5zm9 16 4-5h-3V4h-2v11h-3l4 5z"/></svg>',
  };
  function nhLfButtonLabels(toolbar, T) {
    const dd = nhLfDropdowns(toolbar);
    const ensureIcon = (btn, kind) => {
      if (!btn || btn.querySelector('.nh-lf-ico')) return;
      // INSIDE the button's flex span, as a flex item, the button itself is
      // inline-block, so a sibling span pushed the (block) label row to a
      // second line, rendering it under the pill (Pawel's phone screenshot).
      const host = btn.querySelector(':scope > .flex') || btn;
      host.classList.add('nh-lf-hasico');
      const ico = document.createElement('span');
      ico.className = 'nh-lf-ico';
      ico.innerHTML = NH_LF_ICONS[kind];
      host.insertBefore(ico, host.firstChild);
    };
    ensureIcon(dd.filter, 'filter');
    ensureIcon(dd.sort, 'sort');
    const apply = (btn, activeLabel, replaceEntirely) => {
      if (!btn) return;
      const span = btn.querySelector('.truncate:not(.nh-lf-lbl)');
      if (!span) return;
      let ours = btn.querySelector('.nh-lf-lbl');
      if (activeLabel) {
        if (!ours) {
          ours = document.createElement('span');
          ours.className = span.className + ' nh-lf-lbl';
          span.parentElement.insertBefore(ours, span.nextSibling);
        }
        span.style.display = 'none';
        const text = replaceEntirely ? activeLabel : span.textContent.trim() + ' · ' + activeLabel;
        if (ours.textContent !== text) ours.textContent = text;
      } else {
        if (ours) ours.remove();
        span.style.display = '';
      }
    };
    // filter button: native label (+ rating quick-filter) (+ how many stacked
    // dimension values are active, e.g. "+2")
    const fxN = nhLfFxCount();
    const fParts = [];
    if (nhLf.filter) fParts.push(nhLfFilterLabel(T));
    if (fxN) fParts.push('+' + fxN);
    const fLbl = fParts.join(' · ');
    apply(dd.filter, fLbl, nhLfNative().fb === 'all');
    // sort button: the level chain, e.g. "Author ↑ · Series ↑"
    const sLbl = nhLf.sorts.length
      ? nhLf.sorts.map((s) => nhLfDimLabel(T, s.d) + (s.dir < 0 ? ' ↓' : ' ↑')).join(' · ')
      : '';
    apply(dd.sort, sLbl, true);
  }

  // ===================== FILTER & SORT PANEL (v2.1, Reddit request) =====================
  // The old surface injected rows into ABS's two dropdowns: sorting was a stack you
  // could only build by clicking rows in the right order and reading "1↑ 2↓" marks,
  // filter values hid behind a drill-down that showed at most 60 of them, and the
  // whole state was only legible by opening both menus. This replaces both dropdowns
  // with ONE pill and one panel where the entire state is visible and editable at
  // once, plus a chip row so the common case (drop one filter) needs no panel at all.
  //
  // The engine underneath is unchanged, nhLf.sorts / nhLf.filters and the shelf
  // takeover already did multi-level sorting and stacked filters. This is a new way
  // to drive them, which is why `modernFilters: false` can hand the old dropdowns
  // back without losing a single feature.
  //
  // Rendering is EVENT-DRIVEN, never from the 500ms tick: a tick-driven rebuild
  // would steal focus from the search box on every keystroke. The tick only asks for
  // a redraw when the underlying data changes (items arriving, ratings refreshing).
  const nhFf = { open: false, q: '', dataSig: '', openDims: {}, showAll: {}, addOpen: false };

  function nhFfOn() { return nhSettings.modernFilters !== false; }
  function nhFfDims() { return nhLfDimsOk(nhLf.mode === 'series' ? NH_LF_SERIES_FILTER_DIMS : NH_LF_FILTER_DIMS); }
  function nhFfSortDims() { return nhLfDimsOk(nhLf.mode === 'series' ? NH_LF_SERIES_SORT_DIMS : NH_LF_SORT_DIMS); }
  function nhFfT(k) { const T = nhGsT(); return T[k] || PANEL_T.en[k] || k; }
  function nhFfCount() { return nhLfFxCount() + (nhLf.filter ? 1 : 0) + (nhLf.who ? 1 : 0); }

  // Numeric dimensions read better as "highest first" than "Z to A".
  function nhFfDirLabel(d, dir) {
    const numeric = ['year', 'added', 'duration', 'rating', 'books', 'progress', 'community'].indexOf(d) >= 0;
    if (dir < 0) return nhFfT(numeric ? 'mfDescNum' : 'mfDesc');
    return nhFfT(numeric ? 'mfAscNum' : 'mfAsc');
  }

  // Every control in the panel funnels through here, so this is the one place the
  // view needs saving from (#17). Clear-all lands here too, with empty state, which
  // is what drops the stored entry.
  function nhFfApply() {
    nhLf.viewSig = '';
    try { nhLfPersist(); } catch (e) {}
    try { nhLibFilter(); } catch (e) {}
    nhFfRender();
  }

  function nhFfClearAll() {
    nhLf.sort = '';
    nhLf.sorts = [];
    nhLf.filter = '';
    nhLf.filters = {};
    nhLf.who = '';
    nhLf.sub = null;
    const nat = nhLfNative();
    if (nat.fb !== 'all') {
      const patch = {};
      patch[nat.fbKey] = 'all';
      try { window.$nuxt.$store.dispatch('user/updateUserSettings', patch); } catch (e) {}
    }
    nhFfApply();
  }

  // ---- the toolbar pill ----
  function nhFfEnsurePill(toolbar) {
    // Park it where the native filter dropdown was, so the toolbar keeps its shape.
    let host = null;
    const fBtn = nhLfDropdowns(toolbar).filter;
    let w = fBtn ? fBtn.parentElement : null;
    while (w && w !== toolbar && !/(^|\s)w-36(\s|$)/.test(String(w.className))) w = w.parentElement;
    if (w && w !== toolbar && w.parentElement) host = w;
    let pill = toolbar.querySelector('#nh-ff-btn');
    if (!pill) {
      pill = document.createElement('button');
      pill.type = 'button';
      pill.id = 'nh-ff-btn';
      pill.setAttribute('aria-haspopup', 'dialog');
      pill.addEventListener('click', (e) => {
        e.preventDefault();
        e.stopPropagation();
        nhFf.open = !nhFf.open;
        if (nhFf.open && nhLf.libId) { nhLfEnsureItems(nhLf.libId); nhRsItems(); nhCmItems(); }
        nhFfRender();
      });
      if (host && host.parentElement) host.parentElement.insertBefore(pill, host);
      else toolbar.appendChild(pill);
    }
    const n = nhFfCount();
    const levels = nhLf.sorts.length;
    pill.setAttribute('aria-expanded', nhFf.open ? 'true' : 'false');
    pill.classList.toggle('nh-ff-live', !!(n || levels));
    const sig = n + '|' + levels + '|' + nhFfT('mfButton');
    if (pill.dataset.sig !== sig) {
      pill.dataset.sig = sig;
      pill.innerHTML = NH_LF_ICONS.filter +
        '<span class="nh-ff-btn-lbl"></span>' +
        (n || levels ? '<span class="nh-ff-pip">' + (n + levels) + '</span>' : '');
      pill.querySelector('.nh-ff-btn-lbl').textContent = nhFfT('mfButton');
    }
    return pill;
  }

  // ---- chips: the active state, always visible, one click to drop ----
  function nhFfEnsureChips(toolbar) {
    let bar = toolbar.querySelector('#nh-ff-chips');
    const parts = [];
    nhLf.sorts.forEach((s, i) => parts.push({
      kind: 'sort', key: s.d,
      label: (i + 1) + '. ' + nhLfDimLabel(nhGsT(), s.d) + ' ' + (s.dir < 0 ? '↓' : '↑'),
    }));
    if (nhLf.filter) parts.push({ kind: 'rating', key: nhLf.filter, label: nhLfFilterLabel(nhGsT()) || nhLf.filter });
    if (nhLf.who) {
      const u = nhRsRaters().filter((x) => x.id === nhLf.who)[0];
      parts.push({ kind: 'who', key: nhLf.who, label: '★ ' + ((u && u.name) || '?') });
    }
    Object.keys(nhLf.filters).forEach((dim) => {
      (nhLf.filters[dim] || []).forEach((v) => parts.push({
        kind: 'dim', key: dim, val: v,
        label: nhLfValueLabel(nhGsT(), dim, v),
      }));
    });
    // ABS's own filter (a tag picked from the search dropdown, a genre from a
    // book page) used to be invisible AND unclearable once the panel hid the
    // native dropdown (#29). It is a chip like any other now.
    const natF = nhLfNative();
    if (natF.fb !== 'all') {
      const fvm = nhLfNativeVm('filter');
      parts.push({ kind: 'native', key: natF.fb, label: 'ABS: ' + ((fvm && fvm.selectedText) || natF.fb) });
    }
    if (!parts.length) { if (bar) bar.remove(); return; }
    if (!bar) {
      bar = document.createElement('div');
      bar.id = 'nh-ff-chips';
      const pill = toolbar.querySelector('#nh-ff-btn');
      if (pill && pill.parentElement) pill.parentElement.insertBefore(bar, pill.nextSibling);
      else toolbar.appendChild(bar);
    }
    // The label is part of the signature: the native chip's text resolves a tick
    // after ABS's filter data lands.
    const sig = parts.map((p) => p.kind + ':' + p.key + ':' + (p.val || '') + ':' + p.label).join('|');
    if (bar.dataset.sig === sig) return;
    bar.dataset.sig = sig;
    bar.textContent = '';
    parts.forEach((p) => {
      const c = document.createElement('button');
      c.type = 'button';
      c.className = 'nh-ff-chip' + (p.kind === 'sort' ? ' nh-ff-chip-sort' : '');
      c.title = nhFfT('mfRemove') + ': ' + p.label;
      const t = document.createElement('span');
      t.textContent = p.label;
      c.appendChild(t);
      const x = document.createElement('i');
      x.textContent = '✕';
      c.appendChild(x);
      c.addEventListener('click', () => {
        if (p.kind === 'sort') {
          const i = nhLf.sorts.findIndex((s) => s.d === p.key);
          if (i >= 0) nhLf.sorts.splice(i, 1);
          nhLfSyncSortSig();
        } else if (p.kind === 'rating') {
          nhLf.filter = '';
        } else if (p.kind === 'who') {
          nhLf.who = '';
        } else if (p.kind === 'native') {
          const patch = {};
          patch[nhLfNative().fbKey] = 'all';
          nhLfNativePatch(patch);
        } else {
          const arr = nhLf.filters[p.key] || [];
          const i = arr.indexOf(p.val);
          if (i >= 0) arr.splice(i, 1);
          if (!arr.length) delete nhLf.filters[p.key];
        }
        nhFfApply();
      });
      bar.appendChild(c);
    });
    const clear = document.createElement('button');
    clear.type = 'button';
    clear.className = 'nh-ff-chip nh-ff-chip-clear';
    clear.textContent = nhFfT('mfClear');
    clear.addEventListener('click', nhFfClearAll);
    bar.appendChild(clear);
  }

  // ---- the panel ----
  function nhFfPopEl() {
    let pop = document.getElementById('nh-ff-pop');
    if (pop) return pop;
    pop = document.createElement('div');
    pop.id = 'nh-ff-pop';
    pop.setAttribute('role', 'dialog');
    pop.innerHTML = '<div class="nh-ff-cols"><div class="nh-ff-col nh-ff-sortcol"></div><div class="nh-ff-col nh-ff-filtercol"></div></div><div class="nh-ff-foot"></div>';
    document.body.appendChild(pop);
    // Click-outside and Escape close it. Registered once, on the element's life.
    document.addEventListener('mousedown', (e) => {
      if (!nhFf.open) return;
      if (pop.contains(e.target)) return;
      if (e.target.closest && e.target.closest('#nh-ff-btn')) return;
      nhFf.open = false;
      nhFfRender();
    }, true);
    document.addEventListener('keydown', (e) => {
      if (nhFf.open && e.key === 'Escape') { nhFf.open = false; nhFfRender(); }
    });
    return pop;
  }

  function nhFfPosition(pop, pill) {
    const r = pill.getBoundingClientRect();
    pop.style.visibility = 'hidden';
    pop.style.display = 'block';
    const pw = pop.offsetWidth || 640;
    // Hangs from the pill's RIGHT edge (the pill lives on the right of the
    // toolbar, so left-aligning it threw the panel across the page), clamped so
    // it can never run off either side of the window.
    let left = Math.min(r.right - pw, window.innerWidth - pw - 12);
    if (left < 12) left = 12;
    pop.style.left = Math.round(left) + 'px';
    pop.style.top = Math.round(r.bottom + 8) + 'px';
    // The panel scrolls internally rather than growing past the viewport, the
    // same lesson as the edit modal (#14).
    pop.style.maxHeight = Math.max(240, window.innerHeight - r.bottom - 24) + 'px';
    pop.style.visibility = '';
  }

  function nhFfSortRows(col) {
    const T = nhGsT();
    col.textContent = '';
    const h = document.createElement('h3');
    h.textContent = nhFfT('mfSort');
    col.appendChild(h);
    const list = document.createElement('div');
    list.className = 'nh-ff-levels';
    const svm = nhLfNativeVm('sort');
    if (!nhLf.sorts.length && !svm) {
      const empty = document.createElement('p');
      empty.className = 'nh-ff-empty';
      empty.textContent = nhFfT('mfNoSort');
      list.appendChild(empty);
    }
    nhLf.sorts.forEach((s, i) => {
      const row = document.createElement('div');
      row.className = 'nh-ff-level';
      const num = document.createElement('span');
      num.className = 'nh-ff-num';
      num.textContent = String(i + 1);
      row.appendChild(num);
      const lbl = document.createElement('span');
      lbl.className = 'nh-ff-lvl-lbl';
      lbl.textContent = nhLfDimLabel(T, s.d);
      row.appendChild(lbl);
      // direction toggle, spelled out, "Z to A" beats an arrow nobody decodes
      const ctl = document.createElement('div');
      ctl.className = 'nh-ff-lvl-ctl';
      const dir = document.createElement('button');
      dir.type = 'button';
      dir.className = 'nh-ff-dir';
      dir.textContent = (s.dir < 0 ? '↓ ' : '↑ ') + nhFfDirLabel(s.d, s.dir);
      dir.addEventListener('click', () => { s.dir = -s.dir; nhLfSyncSortSig(); nhFfApply(); });
      ctl.appendChild(dir);
      // Per-level option, currently only Author (surname order vs first-name order).
      const opts = NH_LF_SORT_OPTS[s.d];
      if (opts && opts.length > 1) {
        const ob = document.createElement('button');
        ob.type = 'button';
        ob.className = 'nh-ff-dir nh-ff-opt';
        ob.textContent = nhLfOptLabel(T, s.d, s.opt);
        ob.addEventListener('click', () => {
          const i2 = opts.findIndex((o) => (o.v || '') === (s.opt || ''));
          s.opt = opts[(i2 + 1) % opts.length].v || undefined;
          nhLfSyncSortSig();
          nhFfApply();
        });
        ctl.appendChild(ob);
      }
      row.appendChild(ctl);
      // precedence: move a level up, because pick order used to be the ONLY way
      const up = document.createElement('button');
      up.type = 'button';
      up.className = 'nh-ff-move';
      up.textContent = '↑';
      up.title = nhFfT('mfSort');
      up.disabled = i === 0;
      up.addEventListener('click', () => {
        if (i === 0) return;
        const tmp = nhLf.sorts[i - 1];
        nhLf.sorts[i - 1] = nhLf.sorts[i];
        nhLf.sorts[i] = tmp;
        nhLfSyncSortSig();
        nhFfApply();
      });
      row.appendChild(up);
      const rm = document.createElement('button');
      rm.type = 'button';
      rm.className = 'nh-ff-rm';
      rm.textContent = '✕';
      rm.title = nhFfT('mfRemove');
      rm.addEventListener('click', () => { nhLf.sorts.splice(i, 1); nhLfSyncSortSig(); nhFfApply(); });
      row.appendChild(rm);
      list.appendChild(row);
    });
    // ABS's own sort, always the LAST level: the composed list is sorted level by
    // level and ties keep the server's order (#29). The panel hides ABS's dropdown,
    // so this row is where that order is seen and changed.
    if (svm) {
      const nat = nhLfNative();
      const row = document.createElement('div');
      row.className = 'nh-ff-level nh-ff-level-nat';
      const num = document.createElement('span');
      num.className = 'nh-ff-num';
      num.textContent = String(nhLf.sorts.length + 1);
      row.appendChild(num);
      const lbl = document.createElement('span');
      lbl.className = 'nh-ff-lvl-lbl';
      lbl.textContent = nhFfT('mfNoSort') + ': ' + (svm.selectedText || nat.ob);
      lbl.title = nhFfT('mfNoSort');
      row.appendChild(lbl);
      const ctl = document.createElement('div');
      ctl.className = 'nh-ff-lvl-ctl';
      const numeric = /year|duration|size|addedat|time|tracks|progress|sequence/i.test(nat.ob);
      const dir = document.createElement('button');
      dir.type = 'button';
      dir.className = 'nh-ff-dir';
      dir.textContent = (nat.od ? '↓ ' : '↑ ') + nhFfT(nat.od ? (numeric ? 'mfDescNum' : 'mfDesc') : (numeric ? 'mfAscNum' : 'mfAsc'));
      dir.addEventListener('click', () => {
        const p = {};
        p[nhLf.mode === 'series' ? 'seriesSortDesc' : 'orderDesc'] = !nat.od;
        nhLfNativePatch(p);
      });
      ctl.appendChild(dir);
      const ch = document.createElement('button');
      ch.type = 'button';
      ch.className = 'nh-ff-dir nh-ff-opt';
      ch.textContent = nhFfT('mfNatChange');
      ch.addEventListener('click', () => { nhFf.natOpen = !nhFf.natOpen; nhFfRender(); });
      ctl.appendChild(ch);
      row.appendChild(ctl);
      list.appendChild(row);
      if (nhFf.natOpen) {
        const menu = document.createElement('div');
        menu.className = 'nh-ff-addmenu';
        (svm.selectItems || svm.items || []).forEach((it) => {
          const b = document.createElement('button');
          b.type = 'button';
          b.textContent = it.text;
          if (it.value === nat.ob) b.classList.add('nh-ff-cur');
          b.addEventListener('click', () => {
            const p = {};
            p[nhLf.mode === 'series' ? 'seriesSortBy' : 'orderBy'] = it.value;
            nhFf.natOpen = false;
            nhLfNativePatch(p);
          });
          menu.appendChild(b);
        });
        list.appendChild(menu);
      }
    }
    col.appendChild(list);

    const unused = nhFfSortDims().filter((d) => !nhLf.sorts.some((s) => s.d === d));
    if (unused.length) {
      const add = document.createElement('button');
      add.type = 'button';
      add.className = 'nh-ff-add';
      add.textContent = '+ ' + nhFfT('mfAddSort');
      add.addEventListener('click', () => { nhFf.addOpen = !nhFf.addOpen; nhFfRender(); });
      col.appendChild(add);
      if (nhFf.addOpen) {
        const menu = document.createElement('div');
        menu.className = 'nh-ff-addmenu';
        unused.forEach((d) => {
          const b = document.createElement('button');
          b.type = 'button';
          b.textContent = nhLfDimLabel(T, d);
          b.addEventListener('click', () => {
            // rating and the other numeric axes start at "highest first"
            const def = ['rating', 'added', 'progress', 'books', 'community'].indexOf(d) >= 0 ? -1 : 1;
            nhLf.sorts.push({ d: d, dir: def });
            nhLfSyncSortSig();
            nhFf.addOpen = false;
            nhFfApply();
          });
          menu.appendChild(b);
        });
        col.appendChild(menu);
      }
    }
  }

  function nhFfFilterCol(col) {
    const T = nhGsT();
    col.textContent = '';
    const head = document.createElement('div');
    head.className = 'nh-ff-fhead';
    const h = document.createElement('h3');
    h.textContent = nhFfT('mfFilter');
    head.appendChild(h);
    const search = document.createElement('input');
    search.type = 'search';
    search.className = 'nh-ff-search';
    search.placeholder = nhFfT('mfSearch');
    search.value = nhFf.q;
    search.addEventListener('input', () => {
      nhFf.q = search.value;
      nhFfFilterCol(col);
      const s2 = col.querySelector('.nh-ff-search');
      if (s2) { s2.focus(); s2.setSelectionRange(s2.value.length, s2.value.length); }
    });
    head.appendChild(search);
    col.appendChild(head);

    const body = document.createElement('div');
    body.className = 'nh-ff-facets';
    col.appendChild(body);

    if (!nhLf.items) {
      const p = document.createElement('p');
      p.className = 'nh-ff-empty';
      p.textContent = nhFfT('mfLoading');
      body.appendChild(p);
      return;
    }

    const q = nhFf.q.trim().toLowerCase();
    const section = (key, title, rows, exclusive) => {
      if (!rows.length) return;
      const wrap = document.createElement('div');
      wrap.className = 'nh-ff-sec';
      const hd = document.createElement('button');
      hd.type = 'button';
      hd.className = 'nh-ff-sec-h';
      // A search collapses nothing: if you typed, you want to see the matches.
      const open = q ? true : (nhFf.openDims[key] !== undefined ? nhFf.openDims[key] : (key === 'rating' || key === 'who' || key === 'progress' || key === 'format' || key === 'community'));
      hd.innerHTML = '<span class="nh-ff-caret">' + (open ? '▾' : '▸') + '</span>';
      const ht = document.createElement('span');
      ht.textContent = title;
      hd.appendChild(ht);
      const chosen = key === 'rating' ? (nhLf.filter ? 1 : 0) : key === 'who' ? (nhLf.who ? 1 : 0) : (nhLf.filters[key] || []).length;
      if (chosen) {
        const b = document.createElement('span');
        b.className = 'nh-ff-secn';
        b.textContent = String(chosen);
        hd.appendChild(b);
      }
      hd.addEventListener('click', () => {
        nhFf.openDims[key] = !open;
        nhFfFilterCol(col);
      });
      wrap.appendChild(hd);
      if (open) {
        const ul = document.createElement('div');
        ul.className = 'nh-ff-vals';
        const cap = nhFf.showAll[key] ? rows.length : 10;
        rows.slice(0, cap).forEach((r) => {
          const lab = document.createElement('label');
          lab.className = 'nh-ff-val';
          const cb = document.createElement('input');
          cb.type = exclusive ? 'radio' : 'checkbox';
          if (exclusive) cb.name = 'nh-ff-' + key;
          cb.checked = r.on;
          cb.addEventListener('change', r.toggle);
          lab.appendChild(cb);
          const t = document.createElement('span');
          t.className = 'nh-ff-vlbl';
          t.textContent = r.label;
          lab.appendChild(t);
          if (r.n !== undefined) {
            const n = document.createElement('span');
            n.className = 'nh-ff-vn';
            n.textContent = String(r.n);
            lab.appendChild(n);
          }
          ul.appendChild(lab);
        });
        if (rows.length > cap) {
          const more = document.createElement('button');
          more.type = 'button';
          more.className = 'nh-ff-more';
          more.textContent = nhFfT('mfMore').replace('{n}', rows.length);
          more.addEventListener('click', () => { nhFf.showAll[key] = true; nhFfFilterCol(col); });
          ul.appendChild(more);
        }
        wrap.appendChild(ul);
      }
      body.appendChild(wrap);
    };

    // Rating is single-choice (the engine models it as one value), so it renders
    // as radios and re-clicking the chosen one clears it.
    const ratingRows = NH_LF_FILTERS.map((k) => ({
      label: k === 'rated' ? (T.lfRated || PANEL_T.en.lfRated) : k === 'unrated' ? (T.lfUnrated || PANEL_T.en.lfUnrated) : k === 'min4' ? '4★+' : '3★+',
      on: nhLf.filter === k,
      toggle: () => { nhLf.filter = nhLf.filter === k ? '' : k; nhFfApply(); },
    })).filter((r) => !q || r.label.toLowerCase().indexOf(q) >= 0);
    section('rating', T.lfSort || PANEL_T.en.lfSort, ratingRows, true);

    // Whose ratings (#23): scopes the rating sort AND the quick-filters above to
    // one person's stars. Only offered once a second rater exists, with a single
    // rater the average IS that person. Counted against THIS library's items so
    // nobody is offered where they have nothing to show.
    let within = null;
    if (nhLf.items) {
      within = new Set();
      nhLf.items.forEach((li) => {
        if (nhLf.mode === 'series') (li.books || []).forEach((bk) => within.add(bk.id || bk.libraryItemId));
        else within.add(li.id);
      });
    }
    const raters = nhRsRaters(within);
    // Offered from ONE rater up (was >= 2): in a library where only one person
    // has rated, the whole section vanished and the person filter read as
    // broken (Pawel's My Audiobooks). Filtering to one person's books is
    // useful even when the averages equal their stars.
    if (raters.length >= 1 || nhLf.who) {
      const whoRows = [{
        label: T.lfEveryone || PANEL_T.en.lfEveryone,
        on: !nhLf.who,
        toggle: () => { nhLf.who = ''; nhFfApply(); },
      }].concat(raters.map((u) => ({
        // admin view: masked raters keep their name but carry the opted-out tag
        label: u.name + (u.anon ? ' · ' + (nhFfT('socOpted')) : ''),
        n: u.n,
        on: nhLf.who === u.id,
        toggle: () => {
          nhLf.who = nhLf.who === u.id ? '' : u.id;
          // Picking a person with no rating filter or sort active would change
          // nothing visible (Pawel hit exactly that), so it implies "Rated":
          // choosing monika means "show me monika's books". Explicit state, the
          // radio above lights up, and any other rating choice is left alone.
          if (nhLf.who && !nhLf.filter && !nhLf.sorts.some((s) => s.d === 'rating')) nhLf.filter = 'rated';
          nhFfApply();
        },
      }))).filter((r) => !q || r.label.toLowerCase().indexOf(q) >= 0);
      section('who', T.lfWho || PANEL_T.en.lfWho, whoRows, true);
    }

    nhFfDims().forEach((dim) => {
      const vals = nhLfValues(dim, T);
      const rows = vals.map((o) => ({
        label: nhLfValueLabel(T, dim, o.v),
        n: o.n,
        on: (nhLf.filters[dim] || []).indexOf(o.v) >= 0,
        toggle: () => {
          const arr = nhLf.filters[dim] = (nhLf.filters[dim] || []);
          const i = arr.indexOf(o.v);
          if (i >= 0) arr.splice(i, 1); else arr.push(o.v);
          if (!arr.length) delete nhLf.filters[dim];
          nhFfApply();
        },
      })).filter((r) => !q || String(r.label).toLowerCase().indexOf(q) >= 0);
      section(dim, nhLfDimLabel(T, dim), rows, false);
    });

    if (!body.children.length) {
      const p = document.createElement('p');
      p.className = 'nh-ff-empty';
      p.textContent = nhFfT('mfNoValues');
      body.appendChild(p);
    }
  }

  // IDEMPOTENT, and it has to stay that way (GitHub #17). This runs on the tick.
  // The first version rebuilt the whole footer every call, which mutated the DOM,
  // which retriggered the mutation-observer tick, which rebuilt it again: the Done
  // button was being replaced ~14 times a second. A click needs mousedown AND
  // mouseup on the SAME element, so a real press (60ms and up) never produced a
  // click and Done looked dead. A synthetic .click() still worked, which is why the
  // probe passed. Build the buttons ONCE, then only touch the count text.
  function nhFfFoot(foot) {
    let info = foot.querySelector(':scope > .nh-ff-info');
    if (!info) {
      foot.textContent = '';
      info = document.createElement('span');
      info.className = 'nh-ff-info';
      foot.appendChild(info);
      const clear = document.createElement('button');
      clear.type = 'button';
      clear.className = 'nh-ff-foot-btn';
      clear.textContent = nhFfT('mfClear');
      clear.addEventListener('click', nhFfClearAll);
      foot.appendChild(clear);
      const done = document.createElement('button');
      done.type = 'button';
      done.className = 'nh-ff-foot-btn nh-ff-done';
      done.textContent = nhFfT('mfDone');
      done.addEventListener('click', () => { nhFf.open = false; nhFfRender(); });
      foot.appendChild(done);
    }
    const txt = (nhLf.view && nhLf.items)
      ? nhFfT('mfShowing').replace('{n}', nhLf.view.length).replace('{t}', nhLf.items.length)
      : '';
    if (info.textContent !== txt) info.textContent = txt;
  }

  // Full rebuild. Only ever from a user action or a real data change, see nhFfSync.
  function nhFfRender() {
    const toolbar = document.getElementById('toolbar');
    if (!toolbar) return;
    const pill = nhFfEnsurePill(toolbar);
    nhFfEnsureChips(toolbar);
    const pop = nhFfPopEl();
    if (!nhFf.open) { pop.style.display = 'none'; return; }
    nhFfSortRows(pop.querySelector('.nh-ff-sortcol'));
    nhFfFilterCol(pop.querySelector('.nh-ff-filtercol'));
    nhFfFoot(pop.querySelector('.nh-ff-foot'));
    nhFfPosition(pop, pill);
  }
  // The TICK path. Keeps the pill and chips current (both no-op unless their
  // signature changed) and re-anchors an open panel, but never touches the panel's
  // contents, rebuilding those 500ms apart would rip focus out of the search box
  // mid-word and reset every collapsed section under the user's hands.
  function nhFfSync() {
    const toolbar = document.getElementById('toolbar');
    if (!toolbar) return;
    const pill = nhFfEnsurePill(toolbar);
    nhFfEnsureChips(toolbar);
    const pop = document.getElementById('nh-ff-pop');
    if (!pop) return;
    if (!nhFf.open) { pop.style.display = 'none'; return; }
    nhFfPosition(pop, pill);
    // Only the footer's live count is cheap enough to refresh every tick, and it
    // holds no focusable state.
    nhFfFoot(pop.querySelector('.nh-ff-foot'));
  }

  // Tear the whole surface down, leaving the route, or switching to classic mode.
  function nhFfTeardown() {
    nhFf.open = false;
    nhFf.addOpen = false;
    nhFf.natOpen = false;
    const pop = document.getElementById('nh-ff-pop');
    if (pop) pop.style.display = 'none';
    document.querySelectorAll('#nh-ff-btn, #nh-ff-chips').forEach((n) => n.remove());
    document.body.classList.remove('nh-ff-active');
    document.querySelectorAll('.nh-ff-nat').forEach((n) => n.classList.remove('nh-ff-nat'));
  }

  // Mark ABS's two dropdown wrappers so CSS can hide them while our panel owns
  // the toolbar. Marking (not removing) keeps Vue's own DOM intact, so classic
  // mode is one class away and nothing has to be rebuilt.
  function nhFfHideNative(toolbar) {
    const dd = nhLfDropdowns(toolbar);
    [dd.filter, dd.sort].forEach((btn) => {
      if (!btn) return;
      let w = btn.parentElement;
      while (w && w !== toolbar && !/(^|\s)w-36(\s|$)/.test(String(w.className))) w = w.parentElement;
      if (w && w !== toolbar) w.classList.add('nh-ff-nat');
    });
    document.body.classList.add('nh-ff-active');
  }

  function nhLibFilter() {
    const m = location.pathname.match(/\/library\/([^/]+)\/bookshelf(\/series)?\/?$/);
    if (!m || nhSettings.showRatings === false || nhRs.dead) {
      nhLfReset();
      // Forget which library we were on, so coming BACK counts as a change and the
      // saved view is restored (#17). Without this, leaving the shelf cleared the
      // state while libId stayed put, so re-entry took the "nothing changed" path
      // and the sort was silently gone. A reload worked, which made it look like
      // persistence was fine.
      nhLf.libId = null;
      nhLf.mode = null;
      return;
    }
    const libId = m[1];
    const mode = m[2] ? 'series' : 'items';
    if (nhLf.libId !== libId || nhLf.mode !== mode) {
      nhLf.libId = libId;
      nhLf.mode = mode;
      nhLf.items = null;
      nhLf.itemsKey = '';
      nhLf.fetching = false;
      nhLfReset();
      nhLfRestore(libId, mode);
    }
    const toolbar = document.getElementById('toolbar');
    const bookshelf = document.getElementById('bookshelf');
    if (!toolbar || !bookshelf) return;
    const T = nhGsT();

    // v2.1: one panel instead of two dropdowns, unless the user asked for the
    // classic menus back. Only ONE of the two surfaces is ever wired up.
    if (nhFfOn()) {
      nhFfHideNative(toolbar);
      // Redraw an OPEN panel only when the data behind it changes (the item list
      // arriving, ratings refreshing). Everything else is the cheap sync path.
      const dataSig = ((nhLf.items || []).length) + '|' + nhRs.sig + '|' + nhCm.sig + '|' + nhLf.mode + '|' + (nhLf.libId || '');
      if (nhFf.open && nhFf.dataSig !== dataSig) {
        nhFf.dataSig = dataSig;
        nhFfRender();
      } else {
        nhFf.dataSig = dataSig;
        nhFfSync();
      }
    } else {
      nhFfTeardown();
      nhLfInjectMenus(toolbar, T);
      nhLfButtonLabels(toolbar, T);
    }

    const active = !!(nhLf.sort || nhLf.filter || nhLfFxSig());
    const vm = bookshelf.__vue__;
    const natNow = nhLfNative();

    // Pawel: after applying a filter the grid sometimes sits with far too much side
    // margin, and applying a second one puts it right, a stale column/padding
    // calculation in ABS's LazyBookshelf, which only recomputes on resize.
    //
    // This used to fire a GLOBAL synthetic window resize 320ms later. That was the
    // "everything shows and then reloads all elements" flicker: 320ms lands AFTER the
    // page mask has lifted, and ABS answers a resize by destroying and re-creating
    // every card and every cover <img> in full view (measured: 36 tagged cards and 36
    // tagged imgs all replaced). On a real server that also re-requests every visible
    // cover. A global resize additionally re-lays the appbar, player and modals.
    //
    // Now: nudge ONLY this shelf, promptly, and behind the shelf mask so the rebuild
    // is never seen. The underlying stale-width cause is separately prevented by
    // `scrollbar-gutter: stable` (core.js), this stays as belt-and-braces for
    // browsers that lack scrollbar-gutter support.
    // natNow exposes `od`, not `desc`, the old `natNow.desc` was permanently
    // undefined, so sort-direction changes never registered here at all.
    const natSig = natNow.fb + '|' + natNow.ob + '|' + (natNow.od ? 1 : 0) + '|' + (nhLf.filter || '') + '|' + (nhLf.sort || '') + '|' + nhLfFxSig();
    if (nhLf.natSig !== undefined && nhLf.natSig !== natSig) {
      clearTimeout(nhLf.relayoutT);
      nhLf.relayoutT = setTimeout(() => {
        try {
          const bs2 = document.getElementById('bookshelf');
          const v = bs2 && bs2.__vue__;
          if (!v) return;
          nhMaskShelfForRebuild();
          if (typeof v.setCardSize === 'function') {
            Promise.resolve(v.setCardSize())
              .then(() => { try { if (v.executeRebuild) v.executeRebuild(); } catch (e) {} })
              .catch(() => {});
          } else if (typeof v.executeRebuild === 'function') {
            v.executeRebuild();
          }
        } catch (e) {}
      }, 60);
    }
    nhLf.natSig = natSig;

    // pill host: directly LEFT of the filter dropdown
    const hostEl = (function () {
      const fBtn2 = nhLfDropdowns(toolbar).filter;
      let host = fBtn2 ? fBtn2.parentElement : null;
      while (host && host !== toolbar && !/(^|\s)w-36(\s|$)/.test(String(host.className))) host = host.parentElement;
      return (host && host !== toolbar && host.parentElement) ? host : null;
    })();

    // clear-all ✕: shown whenever ANY filter is active (rating or native).
    // The new panel carries its own Clear, in the chip row and in the footer, so
    // the toolbar ✕ would just be a third one.
    let clr = toolbar.querySelector('.nh-lf-clearx');
    const showClear = !nhFfOn() && !!(active || natNow.fb !== 'all');
    if (showClear && !clr && hostEl) {
      clr = document.createElement('button');
      clr.type = 'button';
      clr.className = 'nh-lf-clearx';
      clr.textContent = '✕';
      clr.title = T.lfClear || PANEL_T.en.lfClear;
      clr.addEventListener('click', () => {
        nhLf.sort = '';
        nhLf.sorts = [];
        nhLf.filter = '';
        nhLf.filters = {};
        nhLf.sub = null;
        nhLf.viewSig = '';
        const nat2 = nhLfNative();
        if (nat2.fb !== 'all') {
          const patch = {};
          patch[nat2.fbKey] = 'all';
          try { window.$nuxt.$store.dispatch('user/updateUserSettings', patch); } catch (e) {}
        }
        try { nhLfPersist(); } catch (e) {} // clearing is a choice worth remembering too
        nhLibFilter();
      });
      hostEl.parentElement.insertBefore(clr, hostEl);
    } else if (!showClear && clr) {
      clr.remove();
    }

    // count pill shows for RATING filters and for native filters alike (user
    // request, the N / M readout shouldn't disappear for plain native filters)
    let cnt = toolbar.querySelector('.nh-lf-count');
    const showCnt = !!(active || natNow.fb !== 'all');
    if (!showCnt && cnt) { cnt.remove(); cnt = null; }
    if (showCnt && !cnt) {
      cnt = document.createElement('span');
      cnt.className = 'nh-lf-count';
      if (hostEl) hostEl.parentElement.insertBefore(cnt, clr && clr.parentElement ? clr : hostEl);
      else toolbar.appendChild(cnt);
    }
    if (!active) {
      if (cnt) {
        // native-only filter: shelf total / library total
        nhLfEnsureLibTotal(libId);
        const shown = vm && typeof vm.totalEntities === 'number' ? vm.totalEntities : '…';
        cnt.textContent = shown + ' / ' + (nhLf.libTotal === null ? '…' : nhLf.libTotal);
      }
      nhLfRelease(vm);
      return;
    }

    // items list composed WITH the native filter/sort (server-side)
    const key = nhLfEnsureItems(libId).key;
    // Ratings only gate the view when a level or filter needs them (#29): a
    // server whose ratings call keeps failing used to leave the native shelf up
    // for good, which reads as "my sort does nothing, ABS's title order wins".
    const rsReady = nhRsItems();
    const needRs = !!(nhLf.filter || nhLf.who || nhLf.sorts.some((s) => s.d === 'rating'));
    const cmReady = nhCmItems();
    const needCm = !!(nhLf.filters.community || nhLf.sorts.some((s) => s.d === 'community'));
    if (!nhLf.items || (needRs && !rsReady) || (needCm && !cmReady)) { cnt.textContent = '…'; return; } // native shelf stays until ready
    if (!vm || typeof vm.fetchEntites !== 'function' || typeof vm.resetEntities !== 'function') return;

    // Build the composed view (full minified item objects, the same shape the
    // shelf fetches itself, so it can render them natively).
    const viewSig = key + '|' + nhLf.sort + '|' + nhLf.filter + '|' + nhLf.who + '|' + nhLfFxSig() + '|' + nhRs.sig + '|' + nhCm.sig;
    if (nhLf.viewSig !== viewSig) {
      nhLf.viewSig = viewSig;
      // series rating = mean of its rated books' averages (same math as the
      // series header); n = how many of its books are rated
      let list = nhLf.items.map((li) => {
        if (nhLf.mode === 'series') {
          let sum = 0, n = 0;
          (li.books || []).forEach((bk) => {
            const r2 = nhRsAvgFor(bk.id, nhLf.who);
            if (r2) { sum += r2.avg; n++; }
          });
          return { e: li, t: li.name || '', avg: n ? sum / n : -1, n: n };
        }
        const r = nhRsAvgFor(li.id, nhLf.who);
        return { e: li, t: ((li.media && li.media.metadata) || {}).title || '', avg: r ? r.avg : -1, n: r ? r.n : 0 };
      });
      // Stacked dimension filters: OR within a dimension, AND across dimensions
      // (and with the native filter, applied server-side in the items fetch).
      const fxKeys = Object.keys(nhLf.filters).filter((k) => nhLf.filters[k] && nhLf.filters[k].length);
      if (fxKeys.length) {
        const pg = fxKeys.indexOf('progress') >= 0 ? nhLfProgressMap() : null;
        const isSeries = nhLf.mode === 'series';
        list = list.filter((x) => fxKeys.every((k) => {
          const vals = nhLf.filters[k];
          if (isSeries) {
            // The series list only supports what its member books can answer.
            if (k === 'progress') return vals.indexOf(nhLfSeriesProgress(pg, x.e)) >= 0;
            return true;
          }
          const md = nhLfMeta(x.e);
          if (k === 'genre') return (md.genres || []).some((g) => vals.indexOf(g) >= 0);
          if (k === 'tag') return ((x.e.media && x.e.media.tags) || md.tags || []).some((g) => vals.indexOf(g) >= 0);
          if (k === 'author') return String(md.authorName || '').split(', ').some((a) => vals.indexOf(a.trim()) >= 0);
          if (k === 'narrator') return String(md.narratorName || '').split(', ').some((a) => vals.indexOf(a.trim()) >= 0);
          if (k === 'publisher') return vals.indexOf(md.publisher) >= 0;
          if (k === 'series') return String(md.seriesName || '').split(', ').some((s) => vals.indexOf(s.replace(/\s+#[\d.]+$/, '').trim()) >= 0);
          if (k === 'language') return vals.indexOf(md.language) >= 0;
          if (k === 'decade') { const y = parseInt(md.publishedYear, 10); return !!y && vals.indexOf(Math.floor(y / 10) * 10 + 's') >= 0; }
          if (k === 'progress') return vals.indexOf(nhLfProgressOf(pg, x.e.id)) >= 0;
          if (k === 'format') return nhLfFormats(x.e).some((f) => vals.indexOf(f) >= 0);
          if (k === 'community') return vals.indexOf(nhCmBucket(x.e.id)) >= 0;
          return true;
        }));
      }
      if (nhLf.filter === 'rated') list = list.filter((x) => x.n);
      else if (nhLf.filter === 'unrated') list = list.filter((x) => !x.n);
      else if (nhLf.filter === 'min4') list = list.filter((x) => x.avg >= 4);
      else if (nhLf.filter === 'min3') list = list.filter((x) => x.avg >= 3);
      // Multi-level sort: compare level by level in pick order; final tiebreak
      // is title A→Z. Series is a composite level (name, then sequence, with
      // standalones interleaving by title, Plex-style); a book with no value
      // for a level sorts last within it.
      if (nhLf.sorts.length) {
        const pgS = nhLf.mode === 'series' ? nhLfProgressMap() : null;
        list.forEach((x) => {
          if (nhLf.mode === 'series') {
            const ids = (x.e.books || []).map((b) => b.id || b.libraryItemId).filter(Boolean);
            const done = ids.filter((id) => pgS[id] === 'finished').length;
            x.k = {
              title: String(x.t || '').toLowerCase(),
              rating: x.avg,
              books: ids.length,
              // fraction finished, so a half-read series sorts between the two ends
              progress: ids.length ? done / ids.length : 0,
            };
            return;
          }
          const md = nhLfMeta(x.e);
          const sk = nhLfSeriesKey(md);
          x.k = {
            author: String(md.authorNameLF || md.authorName || '').toLowerCase(),
            // "Charles Dickens" rather than "Dickens, Charles", the Author level
            // can be sorted either way.
            authorFirst: String(md.authorName || md.authorNameLF || '').toLowerCase(),
            seriesG: sk.group, seriesQ: sk.seq,
            title: String(md.titleIgnorePrefix || md.title || x.t || '').toLowerCase(),
            year: parseInt(md.publishedYear, 10) || Infinity,
            added: x.e.addedAt || 0,
            duration: (x.e.media && x.e.media.duration) || 0,
            narrator: String(md.narratorName || '').toLowerCase() || '￿',
            rating: x.avg,
            community: nhCmScore(x.e.id),
          };
        });
        // Takes the LEVEL, not just the dimension name, because a level can carry
        // an option (Author by surname vs by first name).
        const one = (a, b, lvl) => {
          const d = lvl.d;
          if (d === 'series') return a.k.seriesG.localeCompare(b.k.seriesG) || (a.k.seriesQ === b.k.seriesQ ? 0 : (a.k.seriesQ < b.k.seriesQ ? -1 : 1)) || a.k.title.localeCompare(b.k.title);
          const key = (d === 'author' && lvl.opt === 'first') ? 'authorFirst' : d;
          if (key === 'author' || key === 'authorFirst' || key === 'narrator' || key === 'title') {
            return String(a.k[key] || '').localeCompare(String(b.k[key] || ''));
          }
          return a.k[key] === b.k[key] ? 0 : (a.k[key] < b.k[key] ? -1 : 1);
        };
        list.sort((a, b) => {
          for (const s2 of nhLf.sorts) {
            const c = one(a, b, s2) * (s2.dir < 0 ? -1 : 1);
            if (c) return c;
          }
          // Stable sort: a tie keeps ABS's own order, the last row in the panel (#29).
          return 0;
        });
      }
      nhLf.view = list.map((x) => x.e);
      nhLf.needReset = true;
    }
    cnt.textContent = nhLf.view.length + ' / ' + nhLf.items.length;

    // NATIVE-SHELF TAKEOVER: patch the shelf's own page fetcher so it renders
    // OUR list, every card is a real LazyBookCard (hover overlay, play/read,
    // edit, multi-select, kebab all native). The patch swaps $axios.$get only
    // for the duration of the original fetch call and only answers the shelf's
    // own items request; everything else passes through. Inactive state falls
    // back to the original fetcher, so clearing = one resetEntities().
    if (!vm.__nhLfPatched) {
      vm.__nhLfPatched = true;
      vm.__nhOrigFetch = vm.fetchEntites;
      vm.fetchEntites = async function (page) {
        if (!((nhLf.sort || nhLf.filter || nhLfFxSig()) && nhLf.view)) return vm.__nhOrigFetch.call(this, page);
        const ax = this.$axios;
        const orig = ax.$get;
        const self = this;
        ax.$get = async function (url) {
          if (String(url).indexOf('/' + self.entityName + '?') !== -1) {
            const bpf = self.booksPerFetch || 36;
            return { results: nhLf.view.slice(page * bpf, page * bpf + bpf), total: nhLf.view.length, page: page };
          }
          return orig.apply(ax, arguments);
        };
        try { return await vm.__nhOrigFetch.call(this, page); } finally { ax.$get = orig; }
      };
    }
    if (nhLf.needReset) {
      nhLf.needReset = false;
      try { vm.resetEntities(); } catch (e) {}
    }
    nhLfEmptyState(nhLf.view.length === 0);
  }

  // A filter that matches nothing is not an empty library. ABS only knows the
  // shelf came back with no rows, so it offers "scan" and "add books", wrong and
  // alarming when you simply filtered to rated books and have not rated any.
  // Ours says what actually happened and offers the one useful action.
  function nhLfEmptyState(on) {
    const bs = document.getElementById('bookshelf');
    document.body.classList.toggle('nh-lf-none', !!on);
    let el = document.getElementById('nh-lf-none');
    if (!on || !bs) { if (el) el.remove(); return; }
    if (el && el.parentElement === bs) return;
    const T = nhGsT();
    el = el || document.createElement('div');
    el.id = 'nh-lf-none';
    el.textContent = '';
    const p = document.createElement('p');
    p.className = 'nh-lf-none-t';
    p.textContent = T.lfNone || PANEL_T.en.lfNone;
    const b = document.createElement('button');
    b.type = 'button';
    b.className = 'nh-lf-none-btn';
    b.textContent = T.lfClear || PANEL_T.en.lfClear;
    // Clear means ALL of it: a native filter (Progress etc.) kept the shelf
    // empty after our rating filter was gone, and the button looked broken.
    b.addEventListener('click', () => {
      try {
        const nat = nhLfNative();
        if (nat.fb !== 'all') {
          const patch = {};
          patch[nat.fbKey] = 'all';
          window.$nuxt.$store.dispatch('user/updateUserSettings', patch);
        }
      } catch (e) {}
      try { nhLfReset(); } catch (e) {}
    });
    el.appendChild(p); el.appendChild(b);
    bs.appendChild(el);
  }

  // ===================== NARRATORS PAGE REDESIGN (A9) =====================
  // The stock narrators page is a bare two-column table. Replace it with a card
  // grid: cover collage from the narrator's books, name, localized book count;
  // click lands on the library pre-filtered to that narrator (the same URL the
  // native table rows link to). Native table stays in the DOM, just hidden,   // zero risk to stock behavior.
  const nhNr = { libId: null, list: null, fetching: false, byName: null, itemsFetching: false, sig: '', q: '', sort: 'books', tints: {}, qTimer: null };

  // average-color tint from the first cover → subtle per-card background
  function nhNrTint(name, img, card) {
    const cached = nhNr.tints[name];
    if (cached) { card.style.background = cached; return; }
    try {
      const cv = document.createElement('canvas');
      cv.width = 4; cv.height = 4;
      const ctx = cv.getContext('2d');
      ctx.drawImage(img, 0, 0, 4, 4);
      const d = ctx.getImageData(0, 0, 4, 4).data;
      let r = 0, g = 0, bl = 0;
      for (let k = 0; k < d.length; k += 4) { r += d[k]; g += d[k + 1]; bl += d[k + 2]; }
      const n2 = d.length / 4;
      r = Math.round(r / n2); g = Math.round(g / n2); bl = Math.round(bl / n2);
      const bg = 'linear-gradient(160deg, rgba(' + r + ',' + g + ',' + bl + ',0.26) 0%, rgba(' + r + ',' + g + ',' + bl + ',0.07) 70%)';
      nhNr.tints[name] = bg;
      card.style.background = bg;
    } catch (e) {}
  }

  function nhNarratorsPage() {
    if (nhSettings.narratorsCards === false) return;
    const m = location.pathname.match(/\/library\/([^/]+)\/narrators\/?$/);
    if (!m) {
      document.body.classList.remove('nh-narrators-page');
      document.querySelectorAll('#nh-narrators, #nh-nr-bar, #toolbar .nh-nr-count').forEach((el) => el.remove());
      return;
    }
    const libId = m[1];
    document.body.classList.add('nh-narrators-page');
    if (nhNr.libId !== libId) {
      nhNr.libId = libId;
      nhNr.list = null;
      nhNr.fetching = false;
      nhNr.byName = null;
      nhNr.itemsFetching = false;
      nhNr.sig = '';
      nhNr.q = '';
    }
    const page = document.querySelector('#app-content .page');
    if (!page) return;
    const T = nhGsT();
    const tok = nhSrToken();
    if (!nhNr.list && !nhNr.fetching && tok) {
      nhNr.fetching = true;
      fetch('/api/libraries/' + libId + '/narrators', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => {
          nhNr.fetching = false;
          if (j && Array.isArray(j.narrators) && nhNr.libId === libId) nhNr.list = j.narrators;
        })
        .catch(() => { nhNr.fetching = false; });
    }
    // ONE items fetch builds every collage (narratorName is on minified items,     // per-narrator requests took 15s+ on big libraries)
    if (!nhNr.byName && !nhNr.itemsFetching && tok) {
      nhNr.itemsFetching = true;
      fetch('/api/libraries/' + libId + '/items?limit=0&sort=media.metadata.title', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => {
          if (!(j && Array.isArray(j.results)) || nhNr.libId !== libId) { nhNr.itemsFetching = false; return; }
          const map = {};
          j.results.forEach((it) => {
            const nn = ((it.media && it.media.metadata && it.media.metadata.narratorName) || '');
            if (!nn) return;
            nn.split(', ').forEach((nm) => {
              if (!map[nm]) map[nm] = [];
              if (map[nm].length < 3) map[nm].push(it.id);
            });
          });
          nhNr.byName = map;
          nhNr.itemsFetching = false;
        })
        .catch(() => { nhNr.itemsFetching = false; });
    }
    if (!nhNr.list) return;

    // toolbar: count as LEFT text (like "156 Serie"), right side gets the name
    // filter + a THEMED sort dropdown, a native <select> pops the white OS
    // menu, nothing like the reskinned dropdowns on the other pages
    const toolbar = document.getElementById('toolbar');
    if (toolbar && !document.getElementById('nh-nr-bar')) {
      const cnt = document.createElement('p');
      cnt.className = 'nh-nr-count';
      toolbar.insertBefore(cnt, toolbar.firstChild);
      const bar = document.createElement('div');
      bar.id = 'nh-nr-bar';
      const inp = document.createElement('input');
      inp.type = 'text';
      inp.className = 'nh-nr-search';
      inp.placeholder = T.nrSearch || PANEL_T.en.nrSearch;
      inp.addEventListener('input', () => {
        clearTimeout(nhNr.qTimer);
        nhNr.qTimer = setTimeout(() => { nhNr.q = inp.value.trim().toLowerCase(); nhNarratorsPage(); }, 150);
      });
      const dd = document.createElement('div');
      dd.className = 'nh-nr-dd';
      const opts = [['books', T.nrSortBooks || PANEL_T.en.nrSortBooks], ['name', T.nrSortName || PANEL_T.en.nrSortName]];
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = 'nh-nr-dd-btn';
      const lbl = document.createElement('span');
      lbl.textContent = (opts.find((o) => o[0] === nhNr.sort) || opts[0])[1];
      const chev = document.createElement('span');
      chev.className = 'nh-nr-dd-chev';
      chev.textContent = '⌄';
      btn.appendChild(lbl);
      btn.appendChild(chev);
      const menu = document.createElement('div');
      menu.className = 'nh-nr-dd-menu';
      opts.forEach((o) => {
        const row = document.createElement('div');
        row.className = 'nh-nr-dd-row';
        row.dataset.val = o[0];
        const t2 = document.createElement('span');
        t2.textContent = o[1];
        const chk = document.createElement('span');
        chk.className = 'nh-nr-dd-check';
        chk.textContent = '✓';
        row.appendChild(t2);
        row.appendChild(chk);
        row.addEventListener('click', (e) => {
          e.stopPropagation();
          nhNr.sort = o[0];
          lbl.textContent = o[1];
          dd.classList.remove('nh-open');
          menu.querySelectorAll('.nh-nr-dd-row').forEach((r2) => r2.classList.toggle('nh-active', r2.dataset.val === nhNr.sort));
          nhNarratorsPage();
        });
        if (o[0] === nhNr.sort) row.classList.add('nh-active');
        menu.appendChild(row);
      });
      btn.addEventListener('click', (e) => { e.stopPropagation(); dd.classList.toggle('nh-open'); });
      document.addEventListener('click', (e) => { if (!dd.contains(e.target)) dd.classList.remove('nh-open'); });
      dd.appendChild(btn);
      dd.appendChild(menu);
      bar.appendChild(inp);
      bar.appendChild(dd);
      toolbar.appendChild(bar);
    }

    let list = nhNr.list;
    if (nhNr.q) list = list.filter((n) => String(n.name || '').toLowerCase().indexOf(nhNr.q) !== -1);
    list = list.slice();
    if (nhNr.sort === 'books') list.sort((a, b) => (b.numBooks - a.numBooks) || String(a.name).localeCompare(String(b.name)));
    else list.sort((a, b) => String(a.name).localeCompare(String(b.name)));
    const cntEl = document.querySelector('#toolbar .nh-nr-count');
    if (cntEl) {
      const nf = T.nrForms || PANEL_T.en.nrForms;
      cntEl.textContent = (nhNr.q ? list.length + ' / ' : '') + nhNr.list.length + ' ' + nhWordForm(nhNr.list.length, nf);
    }

    const sig = libId + '|' + list.length + '|' + nhNr.q + '|' + nhNr.sort + '|' + (nhNr.byName ? 1 : 0);
    if (nhNr.sig === sig && document.getElementById('nh-narrators')) return;
    nhNr.sig = sig;

    let grid = document.getElementById('nh-narrators');
    if (!grid) {
      grid = document.createElement('div');
      grid.id = 'nh-narrators';
      // the (hidden) table lives inside a full-height #bookshelf scroller,       // the grid must render inside it, not below it
      (page.querySelector('#bookshelf') || page).appendChild(grid);
    }
    grid.textContent = '';
    const forms = T.nrBooksForms || PANEL_T.en.nrBooksForms;
    list.forEach((n) => {
      const card = document.createElement('a');
      card.className = 'nh-nr-card';
      card.href = getBaseNH() + '/library/' + libId + '/bookshelf?filter=narrators.' + n.id;
      card.addEventListener('click', (e) => { e.preventDefault(); nhRouterPush('/library/' + libId + '/bookshelf?filter=narrators.' + n.id); });
      if (nhNr.tints[n.name]) card.style.background = nhNr.tints[n.name];
      const cov = document.createElement('div');
      cov.className = 'nh-nr-covers';
      // initials medallion always renders as the base layer, covers stack on
      // top of it, and if they 404 (or there are none) the medallion shows
      const ph = document.createElement('span');
      ph.className = 'nh-nr-ph';
      ph.textContent = (n.name || '?').trim().charAt(0).toUpperCase();
      cov.appendChild(ph);
      const ids = (nhNr.byName && nhNr.byName[n.name]) || [];
      ids.slice(0, 3).forEach((iid, idx) => {
        const box = document.createElement('span');
        box.className = 'nh-nr-cbox nh-nr-c' + idx;
        const img = document.createElement('img');
        img.loading = 'lazy';
        img.crossOrigin = 'anonymous';
        img.src = '/api/items/' + iid + '/cover?width=300';
        img.addEventListener('error', () => box.remove());
        if (idx === 0) img.addEventListener('load', () => nhNrTint(n.name, img, card));
        box.appendChild(img);
        cov.appendChild(box);
      });
      const nm = document.createElement('p');
      nm.className = 'nh-nr-name';
      nm.textContent = n.name;
      const ct = document.createElement('p');
      ct.className = 'nh-nr-count2';
      ct.textContent = n.numBooks + ' ' + nhWordForm(n.numBooks, forms);
      card.appendChild(cov);
      card.appendChild(nm);
      card.appendChild(ct);
      grid.appendChild(card);
    });
  }

  // ===================== AUTHORS PAGE TOOLBAR (A9 companion) =====================
  // Same toolbar treatment as narrators: localized count text on the left and a
  // themed name-filter input on the right. Filtering drives the NATIVE authors
  // shelf through the proven fetcher takeover (authors are virtualized, hiding
  // cards would leave holes in the grid).
  const nhAuF = { libId: null, list: null, fetching: false, q: '', qTimer: null, view: null, needReset: false };

  function nhAuRelease(vm) {
    if (vm && vm.__nhAuPatched) {
      if (vm.__nhAuOrig) vm.fetchEntites = vm.__nhAuOrig;
      vm.__nhAuPatched = false;
      try { vm.resetEntities(); } catch (e) {}
    }
    nhAuF.view = null;
  }

  // Admin tool: authors left behind with no books. Always lists them and asks
  // first, this deletes records in ABS, so it must never be one careless click.
  function nhAuthorTidy(libId) {
    const T = nhGsT();
    const tok = nhSrToken();
    if (!tok || !libId) return;
    const old = document.getElementById('nh-ae-modal'); if (old) old.remove();
    const overlay = document.createElement('div'); overlay.id = 'nh-ae-modal';
    const bg = document.createElement('div'); bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div'); box.className = 'nh-rt-modal-box';
    const close = () => overlay.remove();
    bg.addEventListener('click', close);
    const head = document.createElement('div'); head.className = 'nh-rt-modal-head';
    const ttl = document.createElement('span'); ttl.textContent = T.aeTitle || PANEL_T.en.aeTitle;
    const x = document.createElement('button'); x.type = 'button'; x.className = 'nh-rt-modal-x'; x.textContent = '×';
    x.addEventListener('click', close);
    head.appendChild(ttl); head.appendChild(x); box.appendChild(head);
    const body = document.createElement('div'); body.className = 'nh-ae-body';
    body.textContent = '…';
    box.appendChild(body);
    overlay.appendChild(bg); overlay.appendChild(box); document.body.appendChild(overlay);

    fetch('/api/libraries/' + libId + '/authors', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => {
        const all = (j && (j.authors || j.results)) || [];
        const empty = all.filter((a) => a && a.numBooks === 0);
        body.textContent = '';
        if (!empty.length) {
          const p = document.createElement('p'); p.className = 'nh-sb-empty';
          p.textContent = T.aeNone || PANEL_T.en.aeNone;
          body.appendChild(p);
          return;
        }
        const list = document.createElement('div'); list.className = 'nh-ae-list';
        empty.forEach((a) => {
          const row = document.createElement('div'); row.className = 'nh-ae-row';
          row.textContent = a.name || a.id;
          list.appendChild(row);
        });
        body.appendChild(list);
        const acts = document.createElement('div'); acts.className = 'nh-rt-actions';
        const go = document.createElement('button'); go.type = 'button'; go.className = 'nh-rt-btn';
        go.textContent = (T.aeRemove || PANEL_T.en.aeRemove) + ' (' + empty.length + ')';
        const status = document.createElement('span'); status.className = 'nh-rt-status';
        go.addEventListener('click', () => {
          go.disabled = true;
          status.textContent = T.aeWorking || PANEL_T.en.aeWorking;
          let done = 0;
          Promise.all(empty.map((a) => fetch('/api/authors/' + a.id, { method: 'DELETE', headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
            .then((r) => { if (r.ok) done++; }).catch(() => {})))
            .then(() => {
              status.textContent = (T.aeDone || PANEL_T.en.aeDone) + ' ' + done + '/' + empty.length;
              nhAuF.list = null;              // the cached author list is stale now
              setTimeout(() => { close(); try { location.reload(); } catch (e) {} }, 900);
            });
        });
        acts.appendChild(go); acts.appendChild(status);
        body.appendChild(acts);
      })
      .catch(() => { body.textContent = '✗'; });
  }

  // Admin list of problem reports. Reads through the admin-gated twin route,   // the queue carries other users' names, so it must not be world-readable.
  const NH_RP_LABELS = { missing: 'rpMissing', quality: 'rpQuality', play: 'rpPlay', wrong: 'rpWrong', chapters: 'rpChapters', other: 'rpOther' };
  function nhReportsAdminRender(host) {
    if (!host) return;
    const T = nhGsT();
    const tok = nhSrToken();
    host.textContent = '…';
    if (!tok) { host.textContent = ''; return; }
    fetch('/_nh/api/reports-admin', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => {
        const rows = (j && j.reports) || [];
        nhRb.count = rows.length; nhRb.ts = Date.now(); // keep the appbar badge honest
        host.textContent = '';
        if (!rows.length) {
          const p = document.createElement('p'); p.className = 'nh-hint'; p.style.margin = '0';
          p.textContent = T.rpEmpty || PANEL_T.en.rpEmpty;
          host.appendChild(p);
          return;
        }
        rows.forEach((rp) => {
          const row = document.createElement('div'); row.className = 'nh-rp-row';
          const main = document.createElement('div'); main.className = 'nh-rp-main';
          const t = document.createElement('a'); t.className = 'nh-rp-t';
          t.textContent = rp.title || rp.itemId;
          t.href = getBaseNH() + '/item/' + rp.itemId;
          t.addEventListener('click', (e) => { e.preventDefault(); nhRouterPush('/item/' + rp.itemId); });
          const meta = document.createElement('div'); meta.className = 'nh-rp-meta';
          const key = NH_RP_LABELS[rp.reason] || 'rpOther';
          const when = rp.ts ? new Date(rp.ts).toLocaleDateString(getUserLanguage()) : '';
          meta.textContent = (T[key] || PANEL_T.en[key]) + ' · ' + (rp.user || '?') + (when ? ' · ' + when : '');
          main.appendChild(t); main.appendChild(meta);
          if (rp.note) {
            const n = document.createElement('div'); n.className = 'nh-rp-note-txt'; n.textContent = rp.note;
            main.appendChild(n);
          }
          const done = document.createElement('button');
          done.type = 'button'; done.className = 'nh-rt-link nh-rp-done';
          done.textContent = T.rpResolve || PANEL_T.en.rpResolve;
          done.addEventListener('click', () => {
            done.disabled = true;
            fetch('/_nh/api/reports-admin?id=' + encodeURIComponent(rp.id), { method: 'DELETE', headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
              .then((r) => {
                if (!r.ok) { done.disabled = false; return; }
                row.remove();
                nhRb.count = Math.max(0, (nhRb.count || 1) - 1); nhRb.ts = Date.now();
              })
              .catch(() => { done.disabled = false; });
          });
          row.appendChild(main); row.appendChild(done);
          host.appendChild(row);
        });
      })
      .catch(() => { host.textContent = ''; });
  }

  // Book page: make every author name clickable.
  // ABS only links an author it holds a record for. When a book carries just an
  // `authorName` string, imported without matching author records, which is
  // common, it renders flat text, and that is the "sometimes not clickable"
  // Pawel hit. The item's own metadata says which names have ids: those become
  // real author links, and the rest link to a search for the name so the click
  // still goes somewhere useful.
  const nhBaCache = {};
  function nhBookAuthorLinks() {
    const m = location.pathname.match(/\/item\/([^/?#]+)/);
    const host = document.querySelector('.nh-metadata-container');
    if (!m || !host) return;
    const id = m[1];
    if (nhBaCache[id] === undefined) {
      nhBaCache[id] = null;
      const tok = nhSrToken();
      if (tok) {
        fetch('/api/items/' + id, { headers: { Authorization: 'Bearer ' + tok } })
          .then((r) => (r.ok ? r.json() : null))
          .then((j) => {
            const md = (j && j.media && j.media.metadata) || {};
            nhBaCache[id] = {
              authors: (md.authors || []).filter((a) => a && a.name),
              authorName: md.authorName || '',
              libraryId: (j && j.libraryId) || '',
            };
          })
          .catch(() => { nhBaCache[id] = null; });
      } else { nhBaCache[id] = undefined; }   // no token yet, try again next tick
      return;
    }
    const info = nhBaCache[id];
    if (!info) return;
    const names = info.authors.length ? info.authors.map((a) => a.name) : (info.authorName ? info.authorName.split(',').map((x) => x.trim()) : []);
    if (!names.length) return;
    const joined = names.join(', ');
    // Find the metadata cell whose text IS the author list, language-independent,
    // no reliance on the label wording.
    const cells = host.querySelectorAll(':scope > div > div:last-child, :scope > div');
    for (const cell of cells) {
      const txt = (cell.textContent || '').trim();
      if (txt !== joined || cell.dataset.nhAu === joined) continue;
      if (cell.querySelector('a')) { cell.dataset.nhAu = joined; continue; } // ABS already linked it
      cell.dataset.nhAu = joined;
      cell.textContent = '';
      names.forEach((nm, i) => {
        if (i) cell.appendChild(document.createTextNode(', '));
        const rec = info.authors.find((a) => a.name === nm);
        const a = document.createElement('a');
        a.textContent = nm;
        a.className = 'nh-bd-au';
        if (rec && rec.id) {
          a.href = getBaseNH() + '/author/' + rec.id;
          a.addEventListener('click', (e) => { e.preventDefault(); nhRouterPush('/author/' + rec.id); });
        } else if (info.libraryId) {
          // No author record: fall back to a library search for the name.
          const q = '/library/' + info.libraryId + '/search?q=' + encodeURIComponent(nm);
          a.href = getBaseNH() + q;
          a.title = nm;
          a.addEventListener('click', (e) => { e.preventDefault(); nhRouterPush(q); });
        }
        cell.appendChild(a);
      });
      break;
    }
  }

  function nhAuthorsBar() {
    const m = location.pathname.match(/\/library\/([^/]+)\/bookshelf\/authors\/?$/);
    const bs = document.getElementById('bookshelf');
    if (!m) {
      document.body.classList.remove('nh-authors-list');
      document.querySelectorAll('#nh-au-bar, #toolbar .nh-au-count').forEach((el) => el.remove());
      document.querySelectorAll('#toolbar [data-nh-au-hidden]').forEach((el) => { el.style.display = ''; delete el.dataset.nhAuHidden; });
      if (bs && bs.__vue__ && bs.__vue__.__nhAuPatched) nhAuRelease(bs.__vue__);
      nhAuF.q = '';
      return;
    }
    const libId = m[1];
    // Scopes the author-card skin below. NOT 'nh-authors-page', the author
    // DETAIL page already owns 'nh-author-page' (singular) and a one-character
    // difference would silently kill one of the two rule sets.
    document.body.classList.add('nh-authors-list');
    if (nhAuF.libId !== libId) {
      nhAuF.libId = libId;
      nhAuF.list = null;
      nhAuF.fetching = false;
      nhAuF.q = '';
      nhAuF.view = null;
    }
    const toolbar = document.getElementById('toolbar');
    if (!toolbar || !bs) return;
    const vm = bs.__vue__;
    const T = nhGsT();
    if (!document.getElementById('nh-au-bar')) {
      const cnt = document.createElement('p');
      cnt.className = 'nh-au-count';
      toolbar.insertBefore(cnt, toolbar.firstChild);
      // the authors toolbar has its own count <p>, ours replaces it
      Array.prototype.forEach.call(toolbar.querySelectorAll(':scope > p:not(.nh-au-count)'), (el) => {
        el.dataset.nhAuHidden = '1';
        el.style.display = 'none';
      });
      const bar = document.createElement('div');
      bar.id = 'nh-au-bar';
      const inp = document.createElement('input');
      inp.type = 'text';
      inp.className = 'nh-nr-search';
      inp.placeholder = T.auSearch || PANEL_T.en.auSearch;
      inp.addEventListener('input', () => {
        clearTimeout(nhAuF.qTimer);
        nhAuF.qTimer = setTimeout(() => { nhAuF.q = inp.value.trim().toLowerCase(); nhAuthorsBar(); }, 150);
      });
      bar.appendChild(inp);
      // Admin-only: ABS keeps an author record after its last book is removed or
      // re-tagged, so libraries accumulate authors with nothing behind them.
      if (isUserAdmin()) {
        const tidy = document.createElement('button');
        tidy.type = 'button';
        tidy.className = 'nh-au-tidy';
        tidy.textContent = T.aeBtn || PANEL_T.en.aeBtn;
        tidy.addEventListener('click', () => nhAuthorTidy(libId));
        bar.appendChild(tidy);
      }
      toolbar.appendChild(bar);
    }
    const cntEl = toolbar.querySelector('.nh-au-count');
    const forms = T.auForms || PANEL_T.en.auForms;
    if (!nhAuF.q) {
      if (vm && vm.__nhAuPatched) { nhAuRelease(vm); }
      if (cntEl && vm && typeof vm.totalEntities === 'number') cntEl.textContent = vm.totalEntities + ' ' + nhWordForm(vm.totalEntities, forms);
      return;
    }
    // full list once per library, then serve filtered slices natively
    const tok = nhSrToken();
    if (!nhAuF.list && !nhAuF.fetching && tok) {
      nhAuF.fetching = true;
      fetch('/api/libraries/' + libId + '/authors', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => {
          nhAuF.fetching = false;
          if (j && Array.isArray(j.authors) && nhAuF.libId === libId) nhAuF.list = j.authors;
        })
        .catch(() => { nhAuF.fetching = false; });
    }
    if (!nhAuF.list || !vm || typeof vm.fetchEntites !== 'function' || typeof vm.resetEntities !== 'function') {
      if (cntEl) cntEl.textContent = '…';
      return;
    }
    const view = nhAuF.list.filter((a) => String(a.name || '').toLowerCase().indexOf(nhAuF.q) !== -1);
    const viewIds = view.map((a) => a.id).join(',');
    if (!nhAuF.view || nhAuF.view.ids !== viewIds) {
      nhAuF.view = { ids: viewIds, rows: view };
      nhAuF.needReset = true;
    }
    if (cntEl) cntEl.textContent = view.length + ' / ' + nhAuF.list.length + ' ' + nhWordForm(nhAuF.list.length, forms);
    if (!vm.__nhAuPatched) {
      vm.__nhAuPatched = true;
      vm.__nhAuOrig = vm.fetchEntites;
      vm.fetchEntites = async function (page) {
        if (!(nhAuF.q && nhAuF.view)) return vm.__nhAuOrig.call(this, page);
        const ax = this.$axios;
        const orig = ax.$get;
        const self = this;
        ax.$get = async function (url) {
          if (String(url).indexOf('/' + self.entityName + '?') !== -1) {
            const bpf = self.booksPerFetch || 36;
            return { results: nhAuF.view.rows.slice(page * bpf, page * bpf + bpf), total: nhAuF.view.rows.length, page: page };
          }
          return orig.apply(ax, arguments);
        };
        try { return await vm.__nhAuOrig.call(this, page); } finally { ax.$get = orig; }
      };
    }
    if (nhAuF.needReset) {
      nhAuF.needReset = false;
      try { vm.resetEntities(); } catch (e) {}
    }
  }

  // ===================== COLLECTION DETAIL PAGE (A10) =====================
  // ABS gives collections a native detail page at /collection/<id> (route name
  // collection-id): a two-column skeleton (stacked cover left, content right)
  // whose books render through tables-collection-books-table, which is slow on
  // big collections ("loads forever", Pawel). We (E1) widen the page to the
  // same axis the series page uses, (E2) replace the table with a fast cover
  // grid built client-side from the page vm's own book list, and (E3) give
  // admins an inline description editor. Collections carry a REAL description
  // field in ABS (unlike series), so the edit just PATCHes /api/collections/<id>
  //, no proxy store needed. The page root #page-wrapper is shared with other
  // routes, so every path guards on vm.collectionId + vm.bookItems.
  const nhCol = { sig: '' };

  function nhColOff() {
    document.body.classList.remove('nh-collection-page');
    const g = document.getElementById('nh-col-grid'); if (g) g.remove();
    const er = document.getElementById('nh-col-editrow'); if (er) er.remove();
    document.querySelectorAll('.nh-col-native-hide').forEach((el) => el.classList.remove('nh-col-native-hide'));
    nhCol.sig = '';
  }

  // Change a collection's tile art. Same modal shell as nhColEditDesc.
  function nhColEditArt(col) {
    const T = nhGsT();
    const old = document.getElementById('nh-col-modal'); if (old) old.remove();
    const overlay = document.createElement('div'); overlay.id = 'nh-col-modal';
    const bg = document.createElement('div'); bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div'); box.className = 'nh-rt-modal-box';
    const close = () => overlay.remove();
    bg.addEventListener('click', close);
    const head = document.createElement('div'); head.className = 'nh-rt-modal-head';
    const title = document.createElement('span'); title.textContent = T.ciTitle || PANEL_T.en.ciTitle;
    const x = document.createElement('button'); x.type = 'button'; x.className = 'nh-rt-modal-x'; x.textContent = '×'; x.addEventListener('click', close);
    head.appendChild(title); head.appendChild(x); box.appendChild(head);

    const cur = nhColArt(col.id) || nhColMatch(col.name || '');
    const pick = nhColArtPicker(cur.icon, cur.tint, T);
    box.appendChild(pick.el);

    const row = document.createElement('div'); row.className = 'nh-rt-actions';
    // Once an override exists, renaming the collection no longer changes its icon.
    // This link is what makes that recoverable.
    const reset = document.createElement('button'); reset.type = 'button'; reset.className = 'nh-rt-link';
    reset.textContent = T.ciReset || PANEL_T.en.ciReset;
    const save = document.createElement('button'); save.type = 'button'; save.className = 'nh-rt-btn'; save.textContent = T.sdSave || PANEL_T.en.sdSave;
    const status = document.createElement('span'); status.className = 'nh-rt-status';
    const after = (ok) => {
      if (!ok) { status.textContent = '✗'; return; }
      nhCl.sig = '';                       // force the landing grid to rebuild
      try { delete document.getElementById('bookshelf').dataset.nhColArt; } catch (e) {}
      close();
    };
    reset.addEventListener('click', () => { status.textContent = '…'; nhColArtSave(col.id, null).then(after); });
    save.addEventListener('click', () => { status.textContent = '…'; nhColArtSave(col.id, pick.get()).then(after); });
    row.appendChild(reset); row.appendChild(save); row.appendChild(status); box.appendChild(row);
    overlay.appendChild(bg); overlay.appendChild(box); document.body.appendChild(overlay);
  }

  function nhColEditDesc(col) {
    const T = nhGsT();
    const old = document.getElementById('nh-col-modal'); if (old) old.remove();
    const overlay = document.createElement('div'); overlay.id = 'nh-col-modal';
    const bg = document.createElement('div'); bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div'); box.className = 'nh-rt-modal-box';
    const close = () => overlay.remove();
    bg.addEventListener('click', close);
    const head = document.createElement('div'); head.className = 'nh-rt-modal-head';
    const title = document.createElement('span'); title.textContent = T.colEditDesc || PANEL_T.en.colEditDesc;
    const x = document.createElement('button'); x.type = 'button'; x.className = 'nh-rt-modal-x'; x.textContent = '×'; x.addEventListener('click', close);
    head.appendChild(title); head.appendChild(x); box.appendChild(head);
    const ta = document.createElement('textarea'); ta.className = 'nh-sd-ta'; ta.maxLength = 5000;
    ta.value = (col.description || '').trim();
    box.appendChild(ta);
    const row = document.createElement('div'); row.className = 'nh-rt-actions';
    const save = document.createElement('button'); save.type = 'button'; save.className = 'nh-rt-btn'; save.textContent = T.sdSave || PANEL_T.en.sdSave;
    const status = document.createElement('span'); status.className = 'nh-rt-status';
    save.addEventListener('click', () => {
      status.textContent = '…';
      const tok = window.__NH_TOKEN || getTokenNH();
      fetch('/api/collections/' + col.id, {
        method: 'PATCH',
        headers: { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/json' },
        body: JSON.stringify({ name: col.name, description: ta.value.trim() || null })
      }).then((r) => (r.ok ? r.json() : null)).then((j) => {
        if (!j) { status.textContent = '✗'; return; }
        try { window.$nuxt.$store.commit('libraries/addUpdateCollection', j); } catch (e) {}
        close();
      }).catch(() => { status.textContent = '✗'; });
    });
    row.appendChild(save); row.appendChild(status); box.appendChild(row);
    overlay.appendChild(bg); overlay.appendChild(box); document.body.appendChild(overlay);
    ta.focus();
  }

  // Add-books modal for the collection detail page: search books (and authors,   // clicking an author adds all their books), then POST /batch/add. Books already
  // in the collection are shown ticked and non-selectable.
  function nhColAddBooks(col) {
    const T = nhGsT();
    const libId = col.libraryId;
    const old = document.getElementById('nh-ab-modal'); if (old) old.remove();
    const overlay = document.createElement('div'); overlay.id = 'nh-ab-modal';
    const bg = document.createElement('div'); bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div'); box.className = 'nh-rt-modal-box';
    const close = () => overlay.remove();
    bg.addEventListener('click', close);
    const head = document.createElement('div'); head.className = 'nh-rt-modal-head';
    const title = document.createElement('span'); title.textContent = T.abTitle || PANEL_T.en.abTitle;
    const x = document.createElement('button'); x.type = 'button'; x.className = 'nh-rt-modal-x'; x.textContent = '×'; x.addEventListener('click', close);
    head.appendChild(title); head.appendChild(x); box.appendChild(head);
    const wrap = document.createElement('div'); wrap.className = 'nh-ct-build';
    const existing = {}; (Array.isArray(col.books) ? col.books : []).forEach((b) => { if (b && b.id) existing[b.id] = 1; });
    const selected = {};
    const search = document.createElement('input'); search.type = 'text'; search.className = 'nh-ct-input'; search.placeholder = T.ctSearch || PANEL_T.en.ctSearch;
    wrap.appendChild(search);
    const results = document.createElement('div'); results.className = 'nh-ct-results'; wrap.appendChild(results);
    const chips = document.createElement('div'); chips.className = 'nh-ct-chips'; wrap.appendChild(chips);
    const add = document.createElement('button'); add.type = 'button'; add.className = 'nh-rt-btn'; add.textContent = T.abAdd || PANEL_T.en.abAdd;
    const updateAdd = () => { add.disabled = !Object.keys(selected).length; };
    const renderChips = () => {
      chips.textContent = '';
      Object.keys(selected).forEach((id) => {
        const c = document.createElement('span'); c.className = 'nh-ct-chip'; c.textContent = selected[id];
        const rm = document.createElement('button'); rm.type = 'button'; rm.textContent = '×';
        rm.addEventListener('click', () => { delete selected[id]; renderChips(); updateAdd(); const row = results.querySelector('[data-id="' + id + '"]'); if (row) row.classList.remove('nh-on'); });
        c.appendChild(rm); chips.appendChild(c);
      });
    };
    let seq = 0;
    const runSearch = () => {
      const q = search.value.trim(); if (!q) { results.textContent = ''; return; }
      const my = ++seq; const tok = nhSrToken();
      fetch('/api/libraries/' + libId + '/search?q=' + encodeURIComponent(q) + '&limit=10', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => {
          if (my !== seq) return; results.textContent = '';
          const books = (j && Array.isArray(j.book)) ? j.book : [];
          const auth = (j && Array.isArray(j.authors)) ? j.authors : [];
          if (!books.length && !auth.length) { const e = document.createElement('div'); e.className = 'nh-ct-empty'; e.textContent = T.gsNoResults || PANEL_T.en.gsNoResults; results.appendChild(e); return; }
          const bf = T.colBookForms || PANEL_T.en.colBookForms;
          auth.slice(0, 4).forEach((a) => {
            if (!a.id) return;
            const row = document.createElement('button'); row.type = 'button'; row.className = 'nh-ct-row nh-ct-author';
            const gi = document.createElement('span'); gi.className = 'material-symbols'; gi.textContent = 'person';
            const c2 = document.createElement('span'); c2.className = 'nh-ct-atext';
            const tt = document.createElement('span'); tt.className = 'nh-ct-rt'; tt.textContent = a.name || '';
            const au = document.createElement('span'); au.className = 'nh-ct-ra'; const cnt = (typeof a.numBooks === 'number') ? a.numBooks : null; au.textContent = cnt != null ? cnt + ' ' + nhWordForm(cnt, bf) : (T.ctAddAll || PANEL_T.en.ctAddAll);
            c2.appendChild(tt); c2.appendChild(au); row.appendChild(gi); row.appendChild(c2);
            row.addEventListener('click', () => {
              if (row.dataset.busy) return; row.dataset.busy = '1'; const o = au.textContent; au.textContent = '…'; const tk2 = nhSrToken();
              fetch('/api/libraries/' + libId + '/items?filter=authors.' + encodeURIComponent(btoa(a.id)) + '&limit=0', { headers: { Authorization: 'Bearer ' + tk2 }, credentials: 'include' })
                .then((r2) => (r2.ok ? r2.json() : null))
                .then((jj) => {
                  ((jj && jj.results) || []).forEach((it) => { if (it.id && !existing[it.id]) selected[it.id] = ((it.media && it.media.metadata && it.media.metadata.title) || '(untitled)'); });
                  renderChips(); updateAdd(); au.textContent = o; delete row.dataset.busy;
                  results.querySelectorAll('.nh-ct-row[data-id]').forEach((rr) => { if (selected[rr.dataset.id]) rr.classList.add('nh-on'); });
                })
                .catch(() => { au.textContent = o; delete row.dataset.busy; });
            });
            results.appendChild(row);
          });
          books.forEach((bk) => {
            const it = bk.libraryItem || bk; const md = (it.media && it.media.metadata) || {}; const id = it.id; if (!id) return;
            const row = document.createElement('button'); row.type = 'button'; row.className = 'nh-ct-row' + (existing[id] ? ' nh-ct-have' : ''); row.dataset.id = id;
            if (selected[id]) row.classList.add('nh-on');
            const tt = document.createElement('span'); tt.className = 'nh-ct-rt'; tt.textContent = (md.title || '') + (existing[id] ? ' ✓' : '');
            const au = document.createElement('span'); au.className = 'nh-ct-ra'; au.textContent = md.authorName || '';
            row.appendChild(tt); row.appendChild(au);
            row.addEventListener('click', () => {
              if (existing[id]) return;
              if (selected[id]) { delete selected[id]; row.classList.remove('nh-on'); } else { selected[id] = md.title || '(untitled)'; row.classList.add('nh-on'); }
              renderChips(); updateAdd();
            });
            results.appendChild(row);
          });
        })
        .catch(() => {});
    };
    let tmr = null; search.addEventListener('input', () => { clearTimeout(tmr); tmr = setTimeout(runSearch, 250); });
    const actions = document.createElement('div'); actions.className = 'nh-rt-actions';
    const status = document.createElement('span'); status.className = 'nh-rt-status';
    updateAdd();
    add.addEventListener('click', () => {
      if (add.disabled || add.dataset.busy) return; add.dataset.busy = '1'; status.textContent = '…';
      const tok = window.__NH_TOKEN || getTokenNH();
      fetch('/api/collections/' + col.id + '/batch/add', { method: 'POST', headers: { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/json' }, body: JSON.stringify({ books: Object.keys(selected) }) })
        .then((r) => (r.ok ? r.json() : null))
        .then((c2) => {
          if (!c2) { status.textContent = '✗'; delete add.dataset.busy; return; }
          try { window.$nuxt.$store.commit('libraries/addUpdateCollection', c2); } catch (e) {}
          nhCol.sig = '';
          close();
        })
        .catch(() => { status.textContent = '✗'; delete add.dataset.busy; });
    });
    actions.appendChild(add); actions.appendChild(status); wrap.appendChild(actions);
    box.appendChild(wrap); overlay.appendChild(bg); overlay.appendChild(box); document.body.appendChild(overlay);
    search.focus();
  }

  function nhCollectionPage() {
    if (nhSettings.collectionsPages === false) return;
    const m = location.pathname.match(/(?:^|\/)collection\/([^/?#]+)/);
    const wrap = document.getElementById('page-wrapper');
    // Read the collection from Vuex, NOT #page-wrapper.__vue__: on ABS latest that
    // element's component is the layout, not the page, so vm.collectionId is absent.
    // The store getter is populated by the page's asyncData and works on 2.35.x too.
    const store = window.$nuxt && window.$nuxt.$store;
    const getCol = store && store.getters && store.getters['libraries/getCollection'];
    const col = (m && typeof getCol === 'function') ? getCol(m[1]) : null;
    if (!m || !wrap || !col || !Array.isArray(col.books)) {
      if (document.body.classList.contains('nh-collection-page')) nhColOff();
      return;
    }
    document.body.classList.add('nh-collection-page');

    const books = col.books.filter(Boolean);
    // Content column = the div that holds the <h1> title. Its structure:
    // [title flex-row (h1 + buttons)] [.my-8.max-w-2xl description] [books table].
    const h1 = wrap.querySelector('h1');
    const titleRow = h1 && h1.parentNode;
    const contentCol = titleRow && titleRow.parentNode;
    if (!contentCol) return;
    const descBlock = contentCol.querySelector('.my-8.max-w-2xl');

    // ---- E3: inline admin "edit description" affordance ----
    const canEdit = !!(store.getters['user/getUserCanUpdate'] || store.getters['user/getIsAdminOrUp']);
    if (descBlock && canEdit) {
      let er = document.getElementById('nh-col-editrow');
      if (!er) {
        er = document.createElement('div');
        er.id = 'nh-col-editrow';
        const btn = document.createElement('button');
        btn.type = 'button';
        btn.className = 'nh-col-edit';
        btn.innerHTML = '<span class="material-symbols">edit</span><span class="nh-col-edit-lbl"></span>';
        btn.addEventListener('click', () => { const c = (typeof getCol === 'function') ? getCol(m[1]) : col; if (c) nhColEditDesc(c); });
        er.appendChild(btn);
        // Without this, a renamed or imported collection could never be re-iconed:
        // the art is derived from the name, and once an override exists renaming no
        // longer changes it. Admin-gated because /_nh/data/ is behind admincheck --
        // canEdit alone would give a non-admin a silent 403.
        if (isUserAdmin()) {
          const ib = document.createElement('button');
          ib.type = 'button';
          ib.className = 'nh-col-edit nh-col-icobtn';
          ib.innerHTML = '<span class="material-symbols">palette</span><span class="nh-col-ico-lbl"></span>';
          ib.addEventListener('click', () => { const c = (typeof getCol === 'function') ? getCol(m[1]) : col; if (c) nhColEditArt(c); });
          er.appendChild(ib);
        }
        descBlock.appendChild(er);
      }
      const T = nhGsT();
      const lbl = er.querySelector('.nh-col-edit-lbl');
      if (lbl) lbl.textContent = T.colEditDesc || PANEL_T.en.colEditDesc;
      const ilbl = er.querySelector('.nh-col-ico-lbl');
      if (ilbl) ilbl.textContent = T.colEditIcon || PANEL_T.en.colEditIcon;
    }

    // ---- "Add books" as a title-row action (Pawel: a toolbar button, not a tile) ----
    if (canEdit && titleRow) {
      let ab = titleRow.querySelector('.nh-col-addbtn');
      if (!ab) {
        ab = document.createElement('button');
        ab.type = 'button'; ab.className = 'nh-col-addbtn';
        ab.innerHTML = '<span class="material-symbols">add</span><span class="nh-col-addbtn-lbl"></span>';
        ab.addEventListener('click', () => { const c = (typeof getCol === 'function') ? getCol(m[1]) : col; if (c) nhColAddBooks(c); });
        titleRow.appendChild(ab);
      }
      const albl = ab.querySelector('.nh-col-addbtn-lbl');
      const TT = nhGsT();
      const want = TT.abTitle || PANEL_T.en.abTitle;
      if (albl && albl.textContent !== want) albl.textContent = want;
    }

    // ---- E1b: category emblem over the native two-cover collage (Pawel: show
    // the same tile graphic, not a grid of two book covers). Overlaid absolutely
    // so we never hide/fight the native cover's Vue children. ----
    const twoCol = contentCol.parentElement;
    const coverCol = twoCol && twoCol.firstElementChild;
    if (coverCol && coverCol !== contentCol) {
      const holder = coverCol.querySelector('.relative') || coverCol;
      let em = holder.querySelector('.nh-col-emblem');
      if (!em) {
        em = document.createElement('div');
        em.className = 'nh-cl-emblem nh-col-emblem';
        const wm = document.createElement('span'); wm.className = 'material-symbols nh-cl-wm'; wm.setAttribute('aria-hidden', 'true');
        const ic = document.createElement('span'); ic.className = 'material-symbols nh-cl-ico';
        em.appendChild(wm); em.appendChild(ic);
        holder.insertBefore(em, holder.firstChild);
      }
      // Hide the native two-book cover so only our emblem shows (re-assert each
      // tick, the cover component can re-render). Our emblem is left in flow.
      Array.prototype.forEach.call(holder.children, (c) => { if (c !== em && c.style.display !== 'none') c.style.display = 'none'; });
      const match = nhColArt(col.id) || nhColMatch(col.name);
      // A custom property, NOT the background shorthand: the shorthand resets
      // background-color to transparent, which killed the glass pane's own
      // translucent base and with it the no-blur @supports fallback in core.js.
      const bgv = nhColEmblemBg(match.tint);
      if (em.style.getPropertyValue('--nh-cl-bg') !== bgv) em.style.setProperty('--nh-cl-bg', bgv);
      const wmEl = em.querySelector('.nh-cl-wm'); if (wmEl && wmEl.textContent !== match.icon) wmEl.textContent = match.icon;
      const icEl = em.querySelector('.nh-cl-ico'); if (icEl && icEl.textContent !== match.icon) icEl.textContent = match.icon;
    }

    // ---- E2: fast cover grid (rebuilt only when the book set changes) ----
    const sig = col.id + '|' + books.map((b) => b.id).join(',');
    let grid = document.getElementById('nh-col-grid');
    if (!grid || nhCol.sig !== sig) {
      nhCol.sig = sig;
      if (!grid) { grid = document.createElement('div'); grid.id = 'nh-col-grid'; }
      grid.textContent = '';
      const tk = window.__NH_TOKEN || getTokenNH();
      const tq = tk ? '&token=' + encodeURIComponent(tk) : '';
      // "Add books" now lives in the page's title row (same reasoning as the
      // landing page: an action tile in a grid of books reads as a book).
      books.forEach((b) => {
        const md = (b.media && b.media.metadata) || {};
        const btitle = md.title || '';
        const tile = document.createElement('a');
        tile.className = 'nh-col-tile';
        tile.href = getBaseNH() + '/item/' + b.id;
        tile.addEventListener('click', (e) => { e.preventDefault(); nhRouterPush('/item/' + b.id); });
        const cover = document.createElement('div');
        cover.className = 'nh-col-cover';
        const img = document.createElement('img');
        img.loading = 'lazy';
        img.alt = btitle;
        img.src = '/api/items/' + b.id + '/cover?width=300' + tq;
        img.addEventListener('error', () => {
          img.remove();
          const ph = document.createElement('div'); ph.className = 'nh-col-ph'; ph.textContent = btitle; cover.appendChild(ph);
        });
        cover.appendChild(img);
        // Same treatment as a library card (Pawel: every book tile, everywhere):
        // a hover dim over the cover, and the rating on the author line of the
        // caption, always visible, never over the artwork, never hover-gated.
        const dim = document.createElement('div'); dim.className = 'nh-col-dim'; cover.appendChild(dim);
        const t = document.createElement('div'); t.className = 'nh-col-t'; t.textContent = btitle;
        const arow = document.createElement('div'); arow.className = 'nh-col-arow';
        const a = document.createElement('div'); a.className = 'nh-col-a';
        a.textContent = (md.authors || []).map((x) => x.name).join(', ') || (md.authorName || '');
        // These tiles are narrow and the rating shares the line, so the name
        // often ellipses, keep the full text reachable.
        if (a.textContent) a.title = a.textContent;
        arow.appendChild(a);
        // The badge itself is painted by nhCardRatings on the tick, not here: the
        // grid is built as soon as the collection loads, which is often BEFORE the
        // ratings map arrives, and the grid is not rebuilt afterwards -- so a
        // build-time badge simply never appeared on a cold load.
        tile.appendChild(cover); tile.appendChild(t); tile.appendChild(arow);
        grid.appendChild(tile);
      });
    }
    // Hide the native table and mount our grid in the content column. The table
    // component's root class isn't predictable, so the table is identified as the
    // last element child that is neither the title row, the description, nor us.
    const kids = Array.prototype.filter.call(contentCol.children, (el) => el !== titleRow && el !== descBlock && el.id !== 'nh-col-grid');
    const nativeTable = kids[kids.length - 1] || null;
    if (nativeTable && !nativeTable.classList.contains('nh-col-native-hide')) nativeTable.classList.add('nh-col-native-hide');
    if (grid.parentNode !== contentCol) contentCol.appendChild(grid);
  }

  // ===================== COLLECTIONS LANDING PAGE (A10) =====================
  // The native /bookshelf/collections shelf renders each collection as a wide
  // two-cover card, loading many full covers at once (slow at scale) and reading
  // generically. We hide the native cards (CSS, body.nh-collections-page) and
  // mount our own fast tile grid inside #bookshelf: each collection is an ICON
  // EMBLEM on a category-tinted card (Pawel's pick, no cover images at all, so
  // it's instant) + name + book count, click → the detail page. An admin-only
  // "New" tile opens the curated template picker. Mount pattern from narrators.

  // Curated category templates: drive BOTH the emblem shown for an existing
  // collection (matched by name keyword) AND the starter-collection picker.
  // Icons are material-symbols ligatures (already loaded by the app).
  const NH_COL_TEMPLATES = [
    { name: 'Science Fiction', icon: 'rocket_launch', tint: '#3b5bd9', kw: ['sci-fi', 'science fiction', 'scifi', 'space opera'], desc: "Worlds beyond our own, the far future, distant stars, and the ideas that stretch what's possible." },
    { name: 'Fantasy', icon: 'castle', tint: '#7048e8', kw: ['fantasy', 'epic fantasy', 'dragon', 'magic'], desc: 'Maps with dragons in the margins, myth, magic, and quests worth the long road.' },
    { name: 'Mystery & Thriller', icon: 'search', tint: '#d6336c', kw: ['myster', 'thriller', 'detective', 'suspense', 'noir'], desc: 'Locked rooms, long shadows, and the slow pleasure of piecing it together.' },
    { name: 'True Crime', icon: 'local_police', tint: '#a51111', kw: ['true crime', 'crime'], desc: 'Real cases, real stakes, the investigations that kept a nation reading.' },
    { name: 'Romance', icon: 'favorite', tint: '#e64980', kw: ['romance', 'love story'], desc: 'Hearts colliding, second chances, and the warm ache of a story that ends in an embrace.' },
    { name: 'History', icon: 'account_balance', tint: '#b0855b', kw: ['history', 'histor', 'ancient', 'politic'], desc: 'How we got here, empires, revolutions, and the quiet moments that turned the world.' },
    { name: 'War & Military', icon: 'military_tech', tint: '#5c6670', kw: ['war', 'military', 'army', 'battle'], desc: 'Courage and cost, the front lines, the strategy, and the people who lived it.' },
    { name: 'Science', icon: 'science', tint: '#1098ad', kw: ['science', 'physics', 'biology', 'cosmos', 'nature'], desc: 'The universe, explained, from the very small to the unimaginably vast.' },
    { name: 'Technology & AI', icon: 'smart_toy', tint: '#4263eb', kw: ['technolog', 'artificial intelligence', 'machine learning', 'computer', 'digital', 'robot'], desc: 'Code, machines, and minds, the tools remaking how we live and think.' },
    { name: 'Psychology & Self-Help', icon: 'psychology', tint: '#7950f2', kw: ['psycholog', 'self-help', 'self help', 'mindful', 'habit'], desc: 'The mind at work, habits, happiness, and becoming a little more yourself.' },
    { name: 'Health & Medicine', icon: 'medical_services', tint: '#2f9e44', kw: ['health', 'medic', 'fitness', 'wellness', 'nutrition'], desc: 'Body and wellbeing, the science of living longer and feeling better.' },
    { name: 'Business & Money', icon: 'trending_up', tint: '#e8850c', kw: ['business', 'money', 'finance', 'econom', 'startup', 'invest', 'leadership'], desc: 'Ideas, markets, and money, building things and making them work.' },
    { name: 'Biography & Memoir', icon: 'person', tint: '#9c6b3f', kw: ['biograph', 'memoir', 'lives', 'autobiograph'], desc: 'Real lives, well told, the people worth spending an evening with.' },
    { name: 'Poetry', icon: 'format_quote', tint: '#ae3ec9', kw: ['poet', 'poem', 'verse'], desc: 'Language distilled, verse to read slowly and return to often.' },
    { name: 'Philosophy & Religion', icon: 'self_improvement', tint: '#5f3dc4', kw: ['philosoph', 'religion', 'spiritual', 'faith', 'theolog'], desc: 'The big questions, meaning, ethics, and the search for how to live.' },
    { name: 'Food & Cooking', icon: 'restaurant', tint: '#e8590c', kw: ['food', 'cook', 'recipe', 'culinary', 'kitchen'], desc: 'The pleasures of the table, recipes, kitchens, and the stories behind the meal.' },
    { name: 'Travel & Adventure', icon: 'travel_explore', tint: '#0ca678', kw: ['travel', 'adventure', 'journey', 'explor', 'outdoor'], desc: 'Far horizons, journeys, wild places, and the pull of the open road.' },
    { name: 'Young Adult', icon: 'auto_stories', tint: '#f06595', kw: ['young adult', 'teen', 'coming of age'], desc: 'Coming of age, first loves, hard choices, and finding your feet.' },
    { name: 'Classics & Literature', icon: 'menu_book', tint: '#846358', kw: ['classic', 'literature', 'literary'], desc: 'The books that lasted, timeless stories and the writers who shaped them.' },
    { name: "Children's", icon: 'child_care', tint: '#f59f00', kw: ['children', 'kids', 'picture book', 'bedtime'], desc: 'Big adventures for small listeners, bedtime favourites and first chapter books.' }
  ];

  // Pick an emblem (icon + tint) for a collection by matching its name against
  // the template keywords; fall back to a generic book icon with a stable tint.
  // Curated template names/descriptions per UI language. Keyed by the spine's
  // ICON, which is unique per template, so the English array above needs no edit
  // and no parallel index to drift. A language defines only what it has; anything
  // missing falls back to the English spine, which is correct rather than broken.
  // `kw` here is ADDITIVE: matching unions the keywords of every language,
  // because a Polish user can still have English collection names and vice versa.
  const NH_COL_TPL_T = {
    pl: {
      rocket_launch:    { name: 'Fantastyka naukowa', desc: "Światy inne niż nasz, daleka przyszłość, obce gwiazdy i idee rozciągające wyobraźnię.", kw: ['fantastyka naukowa', 'kosmos'] },
      castle:           { name: 'Fantasy', desc: "Mapy ze smokami na marginesach, mit, magia i wyprawy warte dalekiej drogi.", kw: ['fantastyka', 'magia', 'smoki'] },
      search:           { name: 'Kryminał i thriller', desc: "Zamknięte pokoje, długie cienie i powolna przyjemność składania tropów w całość.", kw: ['kryminal', 'kryminał', 'thriller', 'detektyw', 'sensacja'] },
      local_police:     { name: 'Zbrodnie prawdziwe', desc: "Prawdziwe sprawy, prawdziwa stawka, śledztwa, które trzymały w napięciu cały kraj.", kw: ['zbrodnia', 'true crime'] },
      favorite:         { name: 'Romans', desc: "Zderzenia serc, drugie szanse i ciepły ból historii kończącej się uściskiem.", kw: ['romans', 'milosc', 'miłość'] },
      account_balance:  { name: 'Historia', desc: "Jak tu dotarliśmy, imperia, rewolucje i ciche chwile, które odmieniły świat.", kw: ['historia', 'historyczn'] },
      military_tech:    { name: 'Wojna i wojskowość', desc: "Strategia, odwaga i cena zwycięstwa, relacje z pola bitwy i z jego obrzeży.", kw: ['wojna', 'wojenn', 'wojskow'] },
      science:          { name: 'Nauka', desc: "Jak działa świat, od cząstek elementarnych po skraj obserwowalnego wszechświata.", kw: ['nauka', 'naukow', 'fizyka', 'biologia', 'przyroda'] },
      smart_toy:        { name: 'Technologia i AI', desc: "Maszyny, które uczą się myśleć, i ludzie, którzy je budują.", kw: ['technologi', 'sztuczna inteligencja', 'komputer', 'cyfrow'] },
      psychology:       { name: 'Psychologia i rozwój', desc: "Dlaczego robimy to, co robimy, umysł, nawyki i praca nad sobą.", kw: ['psycholog', 'rozwoj', 'rozwój', 'nawyk'] },
      medical_services: { name: 'Zdrowie i medycyna', desc: "Ciało, umysł i medycyna, od profilaktyki po wielkie odkrycia.", kw: ['zdrowie', 'medycyn', 'dieta', 'fitness'] },
      trending_up:      { name: 'Biznes i finanse', desc: "Rynki, decyzje i pieniądze, jak powstają firmy i jak upadają.", kw: ['biznes', 'finans', 'ekonomi', 'pieniadz', 'pieniądz', 'inwest'] },
      person:           { name: 'Biografie i wspomnienia', desc: "Prawdziwe życiorysy opowiedziane własnym głosem, wspomnienia i biografie.", kw: ['biografi', 'wspomnien', 'pamietnik', 'pamiętnik'] },
      format_quote:     { name: 'Poezja', desc: "Język ściśnięty do granic, wiersze, które zostają w pamięci.", kw: ['poezja', 'wiersz'] },
      self_improvement: { name: 'Filozofia i religia', desc: "Wielkie pytania, sens, wiara i sposoby patrzenia na życie.", kw: ['filozofi', 'religi', 'duchow', 'wiara'] },
      restaurant:       { name: 'Kuchnia', desc: "Przepisy, smaki i historie z kuchni, jedzenie jako opowieść.", kw: ['kuchnia', 'gotowan', 'przepis', 'kulinar'] },
      travel_explore:   { name: 'Podróże i przygoda', desc: "Drogi, mapy i miejsca za horyzontem, podróż jako cel sam w sobie.", kw: ['podroz', 'podróż', 'przygod', 'wyprawa'] },
      auto_stories:     { name: 'Młodzieżowe', desc: "Dorastanie, pierwsze wybory i świat widziany świeżym okiem.", kw: ['mlodziez', 'młodzież', 'nastolat'] },
      menu_book:        { name: 'Klasyka i literatura', desc: "Książki, które przetrwały, kanon i literatura z najwyższej półki.", kw: ['klasyk', 'literatur'] },
      child_care:       { name: 'Dla dzieci', desc: "Opowieści na dobranoc i pierwsze wielkie przygody, dla najmłodszych.", kw: ['dzieci', 'bajk', 'dziecie'] }
    }
  };

  // ---- per-collection icon/accent overrides -------------------------------
  // The tile art is otherwise DERIVED from the collection name on every render,
  // so a picker without a store would lose the choice on the next tick. Kept in
  // its own file, not server-config.json: the settings panel PUTs that one
  // wholesale and would erase anything else living in it. Shape is versioned and
  // leaves room for an "img" key so uploaded artwork can be added later without
  // a migration.
  const NH_COL_ART_URL = '/_nh/collection-art.json';
  let nhColArtMap = null;        // null = not loaded yet, {} = loaded and empty
  let nhColArtPending = false;

  function nhColArtLoad() {
    if (nhColArtMap || nhColArtPending) return;
    nhColArtPending = true;
    fetch(NH_COL_ART_URL, { cache: 'no-store' })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => { nhColArtMap = (j && j.cols && typeof j.cols === 'object') ? j.cols : {}; nhColArtPending = false; })
      .catch(() => { nhColArtMap = {}; nhColArtPending = false; });
  }

  function nhColArt(id) {
    if (!nhColArtMap) { nhColArtLoad(); return null; }
    const a = nhColArtMap[id];
    if (!a || !a.icon) return null;
    return { icon: a.icon, tint: a.tint || '#5c5048', name: a.name };
  }

  // Admin-only: /_nh/data/ is behind auth_request /_nh/admincheck, so a
  // non-admin would get a silent 403 here.
  function nhColArtSave(id, art) {
    if (!nhColArtMap) nhColArtMap = {};
    if (art) nhColArtMap[id] = art; else delete nhColArtMap[id];
    const body = JSON.stringify({ v: 1, cols: nhColArtMap });
    const tok = window.__NH_TOKEN || getTokenNH();
    return fetch('/_nh/data/collection-art.json', {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + tok },
      body: body
    }).then((r) => r.ok).catch(() => false);
  }

  // Icons offered by the picker: the 20 curated template glyphs plus a few
  // neutral ones, so a collection that matches no template still has choices.
  const NH_COL_ICONS = ['rocket_launch', 'castle', 'search', 'local_police', 'favorite', 'account_balance',
    'military_tech', 'science', 'smart_toy', 'psychology', 'medical_services', 'trending_up', 'person',
    'format_quote', 'self_improvement', 'restaurant', 'travel_explore', 'auto_stories', 'menu_book',
    'child_care', 'library_books', 'star', 'bolt', 'landscape', 'theater_comedy', 'sports_esports'];
  const NH_COL_TINTS = ['#3b5bd9', '#7048e8', '#d6336c', '#a51111', '#e64980', '#b0855b', '#5c6670',
    '#1098ad', '#2f9e44', '#e8850c', '#ae3ec9', '#0ca678', '#846358', '#f59f00', '#5c5048', '#455a64'];

  // Icon + accent picker. Returns { el, get() } so both the create dialog and the
  // detail page can mount the same widget without duplicating the markup.
  function nhColArtPicker(icon0, tint0, T) {
    let icon = icon0 || 'library_books';
    let tint = tint0 || '#5c5048';
    const el = document.createElement('div'); el.className = 'nh-ci-wrap';

    const il = document.createElement('div'); il.className = 'nh-ci-lbl';
    il.textContent = (T && T.ctIcon) || PANEL_T.en.ctIcon; el.appendChild(il);
    const ig = document.createElement('div'); ig.className = 'nh-ci-grid'; el.appendChild(ig);
    NH_COL_ICONS.forEach((ic) => {
      const b = document.createElement('button'); b.type = 'button'; b.className = 'nh-ci-opt' + (ic === icon ? ' nh-on' : '');
      b.innerHTML = '<span class="material-symbols">' + ic + '</span>';
      b.addEventListener('click', () => {
        icon = ic;
        ig.querySelectorAll('.nh-ci-opt').forEach((o) => o.classList.remove('nh-on'));
        b.classList.add('nh-on');
        prev.firstChild.textContent = ic;
      });
      ig.appendChild(b);
    });

    const cl = document.createElement('div'); cl.className = 'nh-ci-lbl';
    cl.textContent = (T && T.ctColour) || PANEL_T.en.ctColour; el.appendChild(cl);
    const cg = document.createElement('div'); cg.className = 'nh-ci-grid'; el.appendChild(cg);
    NH_COL_TINTS.forEach((tc) => {
      const b = document.createElement('button'); b.type = 'button'; b.className = 'nh-ci-sw' + (tc === tint ? ' nh-on' : '');
      b.style.setProperty('--nh-sw', tc);
      b.addEventListener('click', () => {
        tint = tc;
        cg.querySelectorAll('.nh-ci-sw').forEach((o) => o.classList.remove('nh-on'));
        b.classList.add('nh-on');
        prev.style.setProperty('--nh-cl-bg', nhColEmblemBg(tint));
      });
      cg.appendChild(b);
    });

    const prev = document.createElement('div'); prev.className = 'nh-cl-emblem nh-ci-prev';
    prev.innerHTML = '<span class="material-symbols nh-cl-ico">' + icon + '</span>';
    prev.style.setProperty('--nh-cl-bg', nhColEmblemBg(tint));
    el.appendChild(prev);

    return { el: el, get: () => ({ icon: icon, tint: tint }) };
  }

  function nhColLang() {
    try { return String(getUserLanguage() || 'en').split('-')[0].toLowerCase(); } catch (e) { return 'en'; }
  }

  // One localised template. Only name/desc are translatable; icon and tint are
  // language-neutral identity.
  function nhColTplOne(t) {
    const loc = (NH_COL_TPL_T[nhColLang()] || {})[t.icon];
    if (!loc) return t;
    return Object.assign({}, t, { name: loc.name || t.name, desc: loc.desc || t.desc });
  }

  function nhColTpl() { return NH_COL_TEMPLATES.map(nhColTplOne); }

  // Normalise every non-letter to a space and match on WORD START. A bare
  // indexOf gave "Award Winners", "Software" and "Beware the Dark" the military
  // medal, because all three contain "war". Prefix keywords such as "histor"
  // still work -- only the START of the word is anchored, not the end.
  function nhColNorm(s) {
    return ' ' + String(s || '').toLowerCase().replace(/[^\p{L}\p{N}]+/gu, ' ').trim() + ' ';
  }

  function nhColMatch(name) {
    const n = nhColNorm(name);
    const langs = Object.keys(NH_COL_TPL_T);
    for (let i = 0; i < NH_COL_TEMPLATES.length; i++) {
      const t = NH_COL_TEMPLATES[i];
      let kws = t.kw;
      for (let L = 0; L < langs.length; L++) {
        const e = NH_COL_TPL_T[langs[L]][t.icon];
        if (e && e.kw) kws = kws.concat(e.kw);
      }
      // Keywords of 4+ characters match as a word-START PREFIX, so the curated
      // stems ("histor", "myster", "poet", "biograph") still catch their whole
      // family. Shorter ones must match a WHOLE word: the only 3-letter keyword
      // in the set is "war", and as a prefix it claimed "Warmth & Comfort".
      if (kws.some((k) => {
        const kn = nhColNorm(k);
        const body = kn.slice(1, -1);
        return body.length < 4 ? n.indexOf(kn) !== -1 : n.indexOf(' ' + body) !== -1;
      })) return nhColTplOne(t);
    }
    let h = 0;
    for (let i = 0; i < n.length; i++) h = (h * 31 + n.charCodeAt(i)) & 0x7fffffff;
    const pal = ['#5c5048', '#4a5568', '#5b4a63', '#4a5b52', '#63564a', '#455a64'];
    return { icon: 'library_books', tint: pal[h % pal.length] };
  }

  function nhHexA(hex, a) {
    const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(String(hex || ''));
    if (!m) return 'rgba(224,194,122,' + a + ')';
    return 'rgba(' + parseInt(m[1], 16) + ',' + parseInt(m[2], 16) + ',' + parseInt(m[3], 16) + ',' + a + ')';
  }

  // Glass, not candy. The genre tint is a restrained corner radial over a
  // translucent pane -- never a solid fill. The old version ended with ', ' + tint,
  // i.e. an OPAQUE colour layer, and an opaque layer reinstates the flat look no
  // matter what is composited above it (and kills the backdrop-filter, which has
  // nothing left to show through). So there is deliberately no opaque layer here:
  // the pane's transparency is what lets the blur in core.js pick up the ambient
  // cinematic background behind the grid.
  function nhColEmblemBg(tint) {
    return 'radial-gradient(120% 100% at 88% 6%, ' + nhHexA(tint, 0.34) + ' 0%, ' +
      nhHexA(tint, 0.10) + ' 42%, rgba(0,0,0,0) 72%), ' +
      'linear-gradient(158deg, rgba(255,255,255,0.10) 0%, rgba(255,255,255,0.03) 42%, rgba(0,0,0,0.24) 100%)';
  }

  // POST a new collection (ABS requires >=1 book, it rejects an empty books
  // array with 400 "No books"), then PATCH the prewritten description (POST may
  // ignore it depending on ABS version, so set it explicitly). Resolves to the id.
  function nhColCreate(libId, name, desc, books) {
    const tok = window.__NH_TOKEN || getTokenNH();
    const H = { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/json' };
    return fetch('/api/collections', { method: 'POST', headers: H, body: JSON.stringify({ libraryId: libId, name: name, books: books || [] }) })
      .then((r) => (r.ok ? r.json() : null))
      .then((col) => {
        if (!col || !col.id) return null;
        try { window.$nuxt.$store.commit('libraries/addUpdateCollection', col); } catch (e) {}
        if (!desc) return col.id;
        return fetch('/api/collections/' + col.id, { method: 'PATCH', headers: H, body: JSON.stringify({ name: name, description: desc }) })
          .then((r2) => (r2.ok ? r2.json() : null))
          .then((c2) => { if (c2) { try { window.$nuxt.$store.commit('libraries/addUpdateCollection', c2); } catch (e) {} } return col.id; })
          .catch(() => col.id);
      })
      .catch(() => null);
  }

  // Template picker modal: a curated set of starter shelves (icon + name +
  // prewritten description) plus a blank option. Picking one creates the
  // collection and opens its (empty) detail page, where books are added.
  function nhColTemplatePicker(libId) {
    const T = nhGsT();
    const old = document.getElementById('nh-ct-modal'); if (old) old.remove();
    const overlay = document.createElement('div'); overlay.id = 'nh-ct-modal';
    const bg = document.createElement('div'); bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div'); box.className = 'nh-rt-modal-box';
    const close = () => overlay.remove();
    bg.addEventListener('click', close);
    const head = document.createElement('div'); head.className = 'nh-rt-modal-head';
    const title = document.createElement('span'); title.textContent = T.ctTitle || PANEL_T.en.ctTitle;
    const x = document.createElement('button'); x.type = 'button'; x.className = 'nh-rt-modal-x'; x.textContent = '×'; x.addEventListener('click', close);
    head.appendChild(title); head.appendChild(x); box.appendChild(head);
    const body = document.createElement('div'); box.appendChild(body);

    // ---- PHASE 1: pick a template (or blank) ----
    function showPick() {
      title.textContent = T.ctTitle || PANEL_T.en.ctTitle;
      body.textContent = '';
      const gridEl = document.createElement('div'); gridEl.className = 'nh-ct-grid';
      const mk = (name, desc, icon, tint) => {
        const tile = document.createElement('button'); tile.type = 'button'; tile.className = 'nh-ct-tile';
        const ico = document.createElement('div'); ico.className = 'nh-ct-ico'; ico.style.background = tint;
        const is = document.createElement('span'); is.className = 'material-symbols'; is.textContent = icon; ico.appendChild(is);
        const nm = document.createElement('div'); nm.className = 'nh-ct-name'; nm.textContent = name;
        tile.appendChild(ico); tile.appendChild(nm);
        if (desc) { const de = document.createElement('div'); de.className = 'nh-ct-desc'; de.textContent = desc; tile.appendChild(de); }
        // Carry the template's icon/tint through, so the picker in phase 2 opens
        // on the choice the user just made rather than re-deriving from the name.
        tile.addEventListener('click', () => showBuild(name, desc, icon, tint));
        return tile;
      };
      gridEl.appendChild(mk(T.ctBlank || PANEL_T.en.ctBlank, '', 'add', 'rgba(224,194,122,0.25)'));
      nhColTpl().forEach((t) => gridEl.appendChild(mk(t.name, t.desc, t.icon, t.tint)));
      body.appendChild(gridEl);
    }

    // ---- PHASE 2: name it, add >=1 book (ABS requires it), create ----
    function showBuild(name, desc, icon0, tint0) {
      title.textContent = name || (T.ctBlank || PANEL_T.en.ctBlank);
      body.textContent = '';
      const selected = {}; // id -> title
      const wrap = document.createElement('div'); wrap.className = 'nh-ct-build';

      const nameIn = document.createElement('input'); nameIn.type = 'text'; nameIn.className = 'nh-ct-input'; nameIn.value = name || ''; nameIn.placeholder = T.ctNamePh || PANEL_T.en.ctNamePh;
      wrap.appendChild(nameIn);
      if (desc) { const dta = document.createElement('textarea'); dta.className = 'nh-sd-ta nh-ct-desc-ta'; dta.value = desc; dta.dataset.role = 'desc'; wrap.appendChild(dta); }

      // Icon + accent, seeded from the template that was picked, or from the
      // name-derived match otherwise, so the default is never wrong. The BLANK
      // tile passes icon 'add' and a non-hex rgba tint for its own appearance --
      // neither is a valid seed, hence the guard rather than a truthiness check.
      const seedOk = icon0 && icon0 !== 'add' && /^#[0-9a-f]{6}$/i.test(String(tint0 || ''));
      const seed = seedOk ? { icon: icon0, tint: tint0 } : nhColMatch(name || '');
      const artPick = nhColArtPicker(seed.icon, seed.tint, T);
      wrap.appendChild(artPick.el);

      const search = document.createElement('input'); search.type = 'text'; search.className = 'nh-ct-input'; search.placeholder = T.ctSearch || PANEL_T.en.ctSearch;
      wrap.appendChild(search);
      const results = document.createElement('div'); results.className = 'nh-ct-results'; wrap.appendChild(results);
      const chips = document.createElement('div'); chips.className = 'nh-ct-chips'; wrap.appendChild(chips);

      const renderChips = () => {
        chips.textContent = '';
        Object.keys(selected).forEach((id) => {
          const c = document.createElement('span'); c.className = 'nh-ct-chip'; c.textContent = selected[id];
          const rm = document.createElement('button'); rm.type = 'button'; rm.textContent = '×';
          rm.addEventListener('click', () => { delete selected[id]; renderChips(); updateCreate(); const row = results.querySelector('[data-id="' + id + '"]'); if (row) row.classList.remove('nh-on'); });
          c.appendChild(rm); chips.appendChild(c);
        });
      };

      let seq = 0;
      const runSearch = () => {
        const q = search.value.trim();
        if (!q) { results.textContent = ''; return; }
        const my = ++seq;
        const tok = nhSrToken();
        fetch('/api/libraries/' + libId + '/search?q=' + encodeURIComponent(q) + '&limit=10', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
          .then((r) => (r.ok ? r.json() : null))
          .then((j) => {
            if (my !== seq) return;
            results.textContent = '';
            const books = (j && Array.isArray(j.book)) ? j.book : [];
            const auth = (j && Array.isArray(j.authors)) ? j.authors : [];
            if (!books.length && !auth.length) { const e = document.createElement('div'); e.className = 'nh-ct-empty'; e.textContent = T.gsNoResults || PANEL_T.en.gsNoResults; results.appendChild(e); return; }
            const bookForms = T.colBookForms || PANEL_T.en.colBookForms;
            // Authors first: ABS book-search doesn't match author names, so we
            // surface author matches separately, clicking one adds ALL their books.
            auth.slice(0, 4).forEach((a) => {
              if (!a.id) return;
              const row = document.createElement('button'); row.type = 'button'; row.className = 'nh-ct-row nh-ct-author';
              const gi = document.createElement('span'); gi.className = 'material-symbols'; gi.textContent = 'person';
              const col2 = document.createElement('span'); col2.className = 'nh-ct-atext';
              const tt = document.createElement('span'); tt.className = 'nh-ct-rt'; tt.textContent = a.name || '';
              const au = document.createElement('span'); au.className = 'nh-ct-ra';
              const cnt = (typeof a.numBooks === 'number') ? a.numBooks : null;
              au.textContent = (cnt != null ? cnt + ' ' + nhWordForm(cnt, bookForms) : (T.ctAddAll || PANEL_T.en.ctAddAll));
              col2.appendChild(tt); col2.appendChild(au);
              row.appendChild(gi); row.appendChild(col2);
              row.addEventListener('click', () => {
                if (row.dataset.busy) return; row.dataset.busy = '1';
                const orig = au.textContent; au.textContent = '…';
                const tk2 = nhSrToken();
                fetch('/api/libraries/' + libId + '/items?filter=authors.' + encodeURIComponent(btoa(a.id)) + '&limit=0', { headers: { Authorization: 'Bearer ' + tk2 }, credentials: 'include' })
                  .then((r2) => (r2.ok ? r2.json() : null))
                  .then((jj) => {
                    ((jj && jj.results) || []).forEach((it) => { if (it.id) selected[it.id] = ((it.media && it.media.metadata && it.media.metadata.title) || '(untitled)'); });
                    renderChips(); updateCreate();
                    au.textContent = orig; delete row.dataset.busy;
                    results.querySelectorAll('.nh-ct-row[data-id]').forEach((rr) => { if (selected[rr.dataset.id]) rr.classList.add('nh-on'); });
                  })
                  .catch(() => { au.textContent = orig; delete row.dataset.busy; });
              });
              results.appendChild(row);
            });
            books.forEach((b) => {
              const it = b.libraryItem || b;
              const md = (it.media && it.media.metadata) || {};
              const id = it.id; if (!id) return;
              const row = document.createElement('button'); row.type = 'button'; row.className = 'nh-ct-row'; row.dataset.id = id;
              if (selected[id]) row.classList.add('nh-on');
              const tt = document.createElement('span'); tt.className = 'nh-ct-rt'; tt.textContent = md.title || '';
              const au = document.createElement('span'); au.className = 'nh-ct-ra'; au.textContent = md.authorName || '';
              row.appendChild(tt); row.appendChild(au);
              row.addEventListener('click', () => {
                if (selected[id]) { delete selected[id]; row.classList.remove('nh-on'); }
                else { selected[id] = md.title || '(untitled)'; row.classList.add('nh-on'); }
                renderChips(); updateCreate();
              });
              results.appendChild(row);
            });
          })
          .catch(() => {});
      };
      let sTimer = null;
      search.addEventListener('input', () => { clearTimeout(sTimer); sTimer = setTimeout(runSearch, 250); });

      const actions = document.createElement('div'); actions.className = 'nh-rt-actions';
      const back = document.createElement('button'); back.type = 'button'; back.className = 'nh-rt-link'; back.textContent = T.ctBack || PANEL_T.en.ctBack;
      back.addEventListener('click', showPick);
      const create = document.createElement('button'); create.type = 'button'; create.className = 'nh-rt-btn'; create.textContent = T.ctCreate || PANEL_T.en.ctCreate;
      const status = document.createElement('span'); status.className = 'nh-rt-status';
      // Say WHY the button is dead. Probed against ABS 2.35.1: it answers
      // 400 "Invalid collection data. No books" both with books: [] and with the
      // key omitted, so a truly empty collection cannot be created through any
      // UI, ours or ABS's. The gate is a server constraint, not our choice.
      const hint = document.createElement('p'); hint.className = 'nh-ct-hint';
      hint.textContent = T.ctNeedBook || PANEL_T.en.ctNeedBook;
      const updateCreate = () => {
        const ok = !!(nameIn.value.trim() && Object.keys(selected).length);
        create.disabled = !ok;
        hint.style.display = Object.keys(selected).length ? 'none' : '';
      };
      nameIn.addEventListener('input', updateCreate);
      updateCreate();
      create.addEventListener('click', () => {
        if (create.disabled || create.dataset.busy) return;
        create.dataset.busy = '1'; status.textContent = '…';
        const dta = wrap.querySelector('textarea[data-role="desc"]');
        nhColCreate(libId, nameIn.value.trim(), dta ? dta.value.trim() : desc, Object.keys(selected)).then((id) => {
          if (!id) { status.textContent = '✗'; delete create.dataset.busy; return; }
          nhCl.list = null; nhCl.sig = '';
          // Persist the chosen art, then navigate. Failure is non-fatal: the tile
          // falls back to the name-derived match, which is what it did before.
          const done = () => { close(); nhRouterPush('/collection/' + id); };
          if (isUserAdmin()) nhColArtSave(id, artPick.get()).then(done, done); else done();
        });
      });
      actions.appendChild(back); actions.appendChild(create); actions.appendChild(status);
      wrap.appendChild(hint);
      wrap.appendChild(actions);
      body.appendChild(wrap);
      nameIn.focus();
    }

    showPick();
    overlay.appendChild(bg); overlay.appendChild(box); document.body.appendChild(overlay);
  }

  const nhCl = { libId: null, list: null, fetching: false, sig: '' };

  // Our sibling grid has to clear the FIXED toolbar the way #bookshelf does with
  // its padding-top. Measured from the toolbar itself (its padding can still be 0
  // on a fresh mount, which is what put the tiles behind it), plus a buffer for
  // the hover lift, and re-applied on every tick.
  function nhColsGridPad(grid, bookshelf) {
    if (!grid) return;
    try {
      const tb = document.getElementById('toolbar');
      const host = grid.parentElement;
      let want = 0;
      if (tb && host) {
        const tr = tb.getBoundingClientRect();
        const hr = host.getBoundingClientRect();
        if (tr.height > 0) want = Math.round(tr.bottom - hr.top + 22);
      }
      if (want <= 0 && bookshelf) {
        const pt = parseInt(getComputedStyle(bookshelf).paddingTop, 10);
        if (pt > 0) want = pt + 22;
      }
      if (want > 0 && grid.style.paddingTop !== want + 'px') grid.style.paddingTop = want + 'px';
    } catch (e) {}
  }
  // "New collection" as a real toolbar action on the collections landing page.
  // The toolbar is Vue's, so the button is re-inserted whenever it is patched
  // away, and removed as soon as we leave the route.
  function nhColNewButton(libId, T) {
    const bar = document.getElementById('toolbar');
    if (!bar) return;
    let btn = bar.querySelector('.nh-cl-newbtn');
    if (!btn) {
      btn = document.createElement('button');
      btn.type = 'button'; btn.className = 'nh-cl-newbtn';
      btn.innerHTML = '<span class="material-symbols">add</span><span class="nh-cl-newbtn-lbl"></span>';
      btn.addEventListener('click', () => nhColTemplatePicker(libId));
      bar.appendChild(btn); // after the .grow spacer -> right-hand side
    }
    const lbl = btn.querySelector('.nh-cl-newbtn-lbl');
    const want = T.ctNew || PANEL_T.en.ctNew;
    if (lbl && lbl.textContent !== want) lbl.textContent = want;
  }
  function nhColNewButtonClear() {
    const b = document.querySelector('#toolbar .nh-cl-newbtn');
    if (b) b.remove();
  }

  function nhCollectionsLanding() {
    if (nhSettings.collectionsPages === false) return;
    const m = location.pathname.match(/\/library\/([^/]+)\/bookshelf\/collections\/?$/);
    if (!m) {
      if (document.body.classList.contains('nh-collections-page')) {
        document.body.classList.remove('nh-collections-page');
        document.querySelectorAll('#nh-cols-grid, #toolbar .nh-cl-count-top, #toolbar .nh-cl-newbtn').forEach((el) => el.remove());
        nhCl.sig = '';
      }
      return;
    }
    const libId = m[1];
    document.body.classList.add('nh-collections-page');
    // Clear the takeover SYNCHRONOUSLY on any navigation away, before the shared
    // #bookshelf re-measures for the next route (collections↔series). A tick-based
    // cleanup lands too late, the series shelf already measured #bookshelf while
    // it was display:none and loaded nothing. Registered once.
    if (!nhCl.hooked) {
      try {
        const rt = window.$nuxt && window.$nuxt.$router;
        if (rt && typeof rt.beforeEach === 'function') {
          nhCl.hooked = true;
          rt.beforeEach((to, from, next) => {
            const toPath = (to && to.path) || '';
            if (!/\/bookshelf\/collections\/?$/.test(toPath)) {
              document.body.classList.remove('nh-collections-page');
              document.querySelectorAll('#nh-cols-grid, #toolbar .nh-cl-count-top, #toolbar .nh-cl-newbtn').forEach((el) => el.remove());
              nhCl.sig = '';
            }
            next();
          });
        }
      } catch (e) {}
    }
    if (nhCl.libId !== libId) { nhCl.libId = libId; nhCl.list = null; nhCl.fetching = false; nhCl.sig = ''; }
    const bookshelf = document.getElementById('bookshelf');
    if (!bookshelf) return;
    const tok = nhSrToken();
    if (!nhCl.list && !nhCl.fetching && tok) {
      nhCl.fetching = true;
      fetch('/api/libraries/' + libId + '/collections?limit=0', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => { nhCl.fetching = false; if (j && Array.isArray(j.results) && nhCl.libId === libId) nhCl.list = j.results; })
        .catch(() => { nhCl.fetching = false; });
    }
    if (!nhCl.list) return;

    const T = nhGsT();
    // Our own count in the toolbar: the native "N Collections" text zeroes out
    // once the native shelf is hidden, so we render (and localize) it ourselves.
    const toolbar = document.getElementById('toolbar');
    if (toolbar) {
      let cnt = toolbar.querySelector('.nh-cl-count-top');
      if (!cnt) { cnt = document.createElement('p'); cnt.className = 'nh-cl-count-top'; toolbar.insertBefore(cnt, toolbar.firstChild); }
      cnt.textContent = nhCl.list.length + ' ' + nhWordForm(nhCl.list.length, T.colForms || PANEL_T.en.colForms);
    }

    let canEdit = false;
    try { const g = window.$nuxt.$store.getters; canEdit = !!(g['user/getUserCanUpdate'] || g['user/getIsAdminOrUp']); } catch (e) {}

    // Mount our grid as a SIBLING of #bookshelf in .page, never inside it (see
    // the CSS note: foreign nodes in the shared LazyBookshelf break series).
    const host = bookshelf.parentElement || bookshelf;
    const sig = libId + '|' + nhCl.list.length + '|' + (canEdit ? 1 : 0);
    const gridNow = document.getElementById('nh-cols-grid');
    // Toolbar clearance is re-asserted on EVERY tick, before the "nothing
    // changed" guard, the geometry it depends on settles after the grid is
    // built, so a one-shot value is what put the tiles behind the toolbar.
    if (gridNow) nhColsGridPad(gridNow, bookshelf);
    if (nhCl.sig === sig && gridNow && gridNow.parentElement === host) return;
    nhCl.sig = sig;

    let grid = gridNow;
    if (!grid) { grid = document.createElement('div'); grid.id = 'nh-cols-grid'; }
    if (grid.parentElement !== host) host.appendChild(grid);
    nhColsGridPad(grid, bookshelf);
    grid.textContent = '';
    const forms = T.colBookForms || PANEL_T.en.colBookForms;

    // "New collection" belongs in the toolbar, not as a tile in the grid
    // (Pawel), an action pretending to be content sorts and reads badly.
    if (canEdit) nhColNewButton(libId, T);

    nhCl.list.forEach((col) => {
      const books = Array.isArray(col.books) ? col.books.filter(Boolean) : [];
      const card = document.createElement('a');
      card.className = 'nh-cl-card';
      card.href = getBaseNH() + '/collection/' + col.id;
      card.addEventListener('click', (e) => { e.preventDefault(); nhRouterPush('/collection/' + col.id); });
      const match = nhColArt(col.id) || nhColMatch(col.name);
      const emb = document.createElement('div'); emb.className = 'nh-cl-emblem';
      emb.style.setProperty('--nh-cl-bg', nhColEmblemBg(match.tint));
      const wm = document.createElement('span'); wm.className = 'material-symbols nh-cl-wm'; wm.textContent = match.icon; wm.setAttribute('aria-hidden', 'true'); emb.appendChild(wm);
      const ic = document.createElement('span'); ic.className = 'material-symbols nh-cl-ico'; ic.textContent = match.icon; emb.appendChild(ic);
      const nm = document.createElement('p'); nm.className = 'nh-cl-name'; nm.textContent = col.name || '';
      const ct = document.createElement('p'); ct.className = 'nh-cl-count'; ct.textContent = books.length + ' ' + nhWordForm(books.length, forms);
      card.appendChild(emb); card.appendChild(nm); card.appendChild(ct);
      grid.appendChild(card);
    });
  }

  // ===================== ADMIN STATS: FAMILY SCOREBOARD + DRILL-DOWN (A11) =====================
  // On /config/stats, admins get a "Family Scoreboard" card at the top: every
  // user ranked by listening time (week/month/year/all), each row opening a
  // per-user drill-down. Data via the admin-authorized /api/users/<id>/
  // listening-stats endpoint (same shape as /api/me/listening-stats).
  const nhSb = { fetching: false, tries: 0, users: null, stats: {}, period: 'week', renderedSig: '' };

  // Big listening totals read better as days + hours than as a four-digit hour
  // count, so anything past a day formats as "5d 3h" and the plain hour figure
  // is kept as a second line underneath (nhSbSub) for comparing users.
  function nhSbFmt(secs) {
    // Round to minutes FIRST and carry upward, rounding each unit separately
    // produced "12h 60m".
    let m = Math.round(Math.max(0, secs || 0) / 60);
    const d = Math.floor(m / 1440); m -= d * 1440;
    const h = Math.floor(m / 60); m -= h * 60;
    if (d > 0) return d + 'd ' + h + 'h';
    if (h > 0) return h + 'h ' + m + 'm';
    return m + 'm';
  }
  function nhSbHours(secs) {
    const h = (secs || 0) / 3600;
    return (h >= 10 ? Math.round(h) : Math.round(h * 10) / 10) + ' h';
  }
  // Second line: the overall hour count, on EVERY value with any time in it.
  // It used to appear only past a full day, which made Week/Month tiles read
  // differently from Year/All time (Pawel: all levels should show hours).
  function nhSbSub(secs) { return (secs || 0) > 0 ? nhSbHours(secs) : ''; }
  function nhSbPeriodTotal(stats, period) {
    if (!stats) return 0;
    if (period === 'all') return stats.totalTime || 0;
    const days = stats.days || {};
    const n = period === 'week' ? 7 : (period === 'month' ? 30 : 365);
    const now = Date.now(); let sum = 0;
    for (let i = 0; i < n; i++) { const ds = new Date(now - i * 86400000).toISOString().slice(0, 10); sum += days[ds] || 0; }
    return sum;
  }

  // Profile photos: served from /data/nh/user-avatars via the proxy; the
  // {userId: ext} map arrives with the series-meta payload (nhSc.avatars).
  // The photo layers OVER the initial-letter avatar, on 404 the img removes
  // itself and the letter simply shows again.
  function nhAvatarUrl(uid) {
    const ext = nhSc.avatars && uid ? nhSc.avatars[uid] : null;
    return ext ? '/_nh/user-avatars/' + uid + '.' + ext + (nhSc.ts ? '?v=' + nhSc.ts : '') : null;
  }
  function nhAvatarInto(el, uid) {
    const url = nhAvatarUrl(uid);
    if (!url) return;
    const img = document.createElement('img');
    img.alt = '';
    img.src = url;
    img.addEventListener('error', () => img.remove());
    el.appendChild(img);
  }
  // Photos are shown in circles, so a non-square upload has to become square
  // somewhere. Doing it in the browser at upload time (rather than leaning on
  // object-fit at every render site) means the stored file is already correct
  // and NOTHING can stretch it later:
  //   • near-square           -> crop, biased up a little so faces survive
  //   • clearly non-square    -> the whole photo, centred on a blurred copy of
  //                              itself, so no part of it is cut away
  // Output is capped at 640px (the largest circle we draw is 62px, even at 4x
  // DPR that is plenty) which also keeps every upload well under the 2MB limit.
  const NH_AV_SIDE = 640;
  const NH_AV_CROP_RATIO = 1.35; // beyond this, padding beats cropping
  function nhAvatarNormalize(file, cb) {
    if (!file || !/^image\//.test(file.type || '') || /svg/.test(file.type || '')) { cb(file); return; }
    const url = URL.createObjectURL(file);
    const img = new Image();
    const done = (out) => { URL.revokeObjectURL(url); cb(out || file); };
    img.onerror = () => done(null);
    img.onload = function () {
      try {
        const w = img.naturalWidth, h = img.naturalHeight;
        if (!w || !h) { done(null); return; }
        const ratio = Math.max(w, h) / Math.min(w, h);
        if (ratio <= 1.02 && Math.max(w, h) <= NH_AV_SIDE) { done(null); return; } // already fine, keep the original bytes
        const side = Math.min(NH_AV_SIDE, Math.max(w, h));
        const c = document.createElement('canvas');
        c.width = side; c.height = side;
        const x = c.getContext('2d');
        if (ratio <= NH_AV_CROP_RATIO) {
          const s = Math.min(w, h);
          // Portraits: take the crop from 30% down rather than dead centre,           // heads sit in the top half of most photos.
          const sx = (w - s) / 2;
          const sy = h > w ? (h - s) * 0.3 : (h - s) / 2;
          x.drawImage(img, sx, sy, s, s, 0, 0, side, side);
        } else {
          const cov = Math.max(side / w, side / h);
          try { x.filter = 'blur(18px)'; } catch (e) {}
          x.drawImage(img, (side - w * cov) / 2, (side - h * cov) / 2, w * cov, h * cov);
          try { x.filter = 'none'; } catch (e) {}
          x.fillStyle = 'rgba(20,17,13,0.45)';
          x.fillRect(0, 0, side, side);
          const con = Math.min(side / w, side / h);
          x.drawImage(img, (side - w * con) / 2, (side - h * con) / 2, w * con, h * con);
        }
        const png = /png/i.test(file.type);
        c.toBlob((b) => done(b), png ? 'image/png' : 'image/jpeg', 0.92);
      } catch (e) { done(null); }
    };
    img.src = url;
  }

  // Upload/remove a photo through the njs endpoint (raw bytes; server sniffs
  // the type). forUser only works for admins, the server enforces it.
  function nhAvatarSave(rawFile, uid, cb) {
    if (!rawFile) return;
    nhAvatarNormalize(rawFile, (file) => nhAvatarPut(file, uid, cb));
  }
  function nhAvatarPut(file, uid, cb) {
    if (!file) { cb(false); return; }
    if (file.size > 2 * 1024 * 1024) { cb(false, 'size'); return; }
    const tok = window.__NH_TOKEN || getTokenNH();
    fetch('/_nh/api/avatar-admin?forUser=' + encodeURIComponent(uid), {
      method: 'POST', headers: { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/octet-stream' }, body: file
    })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => {
        if (!j || !j.ext) { cb(false); return; }
        if (!nhSc.avatars) nhSc.avatars = {};
        nhSc.avatars[uid] = j.ext;
        nhScBump();
        nhSb.renderedSig = '';
        cb(true);
      })
      .catch(() => cb(false));
  }
  function nhAvatarClear(uid, cb) {
    const tok = window.__NH_TOKEN || getTokenNH();
    fetch('/_nh/api/avatar-admin?forUser=' + encodeURIComponent(uid), { method: 'DELETE', headers: { Authorization: 'Bearer ' + tok } })
      .then((r) => {
        if (!r.ok) { cb(false); return; }
        if (nhSc.avatars) delete nhSc.avatars[uid];
        nhScBump();
        nhSb.renderedSig = '';
        cb(true);
      })
      .catch(() => cb(false));
  }

  function nhStatsUserModal(user) {
    const stats = nhSb.stats[user.id];
    const T = nhGsT();
    const old = document.getElementById('nh-us-modal'); if (old) old.remove();
    const overlay = document.createElement('div'); overlay.id = 'nh-us-modal';
    const bg = document.createElement('div'); bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div'); box.className = 'nh-rt-modal-box';
    const close = () => overlay.remove();
    bg.addEventListener('click', close);
    const head = document.createElement('div'); head.className = 'nh-rt-modal-head';
    const hleft = document.createElement('span'); hleft.className = 'nh-us-hleft';
    const hav = document.createElement('span'); hav.className = 'nh-av-prev nh-us-hav'; hav.textContent = (user.username || '?').charAt(0).toUpperCase();
    nhAvatarInto(hav, user.id);
    const title = document.createElement('span'); title.textContent = user.username;
    hleft.appendChild(hav); hleft.appendChild(title);
    const x = document.createElement('button'); x.type = 'button'; x.className = 'nh-rt-modal-x'; x.textContent = '×'; x.addEventListener('click', close);
    head.appendChild(hleft); head.appendChild(x); box.appendChild(head);

    if (!stats) {
      const p = document.createElement('div'); p.className = 'nh-sb-loading'; p.textContent = '…'; box.appendChild(p);
    } else {
      const tiles = document.createElement('div'); tiles.className = 'nh-us-tiles';
      const mk = (val, lab, sub) => {
        const t = document.createElement('div'); t.className = 'nh-us-tile';
        const v = document.createElement('div'); v.className = 'nh-us-val'; v.textContent = val; t.appendChild(v);
        const s = document.createElement('div'); s.className = 'nh-us-sub'; s.textContent = sub || ' '; t.appendChild(s);
        const l = document.createElement('div'); l.className = 'nh-us-lab'; l.textContent = lab; t.appendChild(l); return t;
      };
      const wk = nhSbPeriodTotal(stats, 'week'), mo = nhSbPeriodTotal(stats, 'month');
      tiles.appendChild(mk(nhSbFmt(stats.totalTime), T.sbAll || PANEL_T.en.sbAll, nhSbSub(stats.totalTime)));
      tiles.appendChild(mk(nhSbFmt(wk), T.sbWeek || PANEL_T.en.sbWeek, nhSbSub(wk)));
      tiles.appendChild(mk(nhSbFmt(mo), T.sbMonth || PANEL_T.en.sbMonth, nhSbSub(mo)));
      tiles.appendChild(mk(String(Object.keys(stats.items || {}).length), T.sbBooks || PANEL_T.en.sbBooks, ''));
      box.appendChild(tiles);
      const items = Object.keys(stats.items || {}).map((k) => stats.items[k]).sort((a, b) => (b.timeListening || 0) - (a.timeListening || 0)).slice(0, 6);
      if (items.length) {
        const th = document.createElement('div'); th.className = 'nh-us-sec'; th.textContent = T.sbTopBooks || PANEL_T.en.sbTopBooks; box.appendChild(th);
        const ul = document.createElement('div'); ul.className = 'nh-us-books';
        items.forEach((it) => {
          const row = document.createElement('div'); row.className = 'nh-us-book';
          const nm = document.createElement('span'); nm.className = 'nh-us-bt'; nm.textContent = (it.mediaMetadata && it.mediaMetadata.title) || '-';
          const tm = document.createElement('span'); tm.className = 'nh-us-btime'; tm.textContent = nhSbFmt(it.timeListening);
          row.appendChild(nm); row.appendChild(tm); ul.appendChild(row);
        });
        box.appendChild(ul);
      }
    }
    overlay.appendChild(bg); overlay.appendChild(box); document.body.appendChild(overlay);
  }

  // The year heatmap on "Your Stats" ships a fixed GitHub-green ramp written as an
  // INLINE background-color on every cell, so it stayed green on an amber (or any
  // other) accent and read as a transplant from another app. CSS cannot outrank an
  // inline style without knowing the value, so the cells are repainted here:
  // each known ramp step maps to the accent at a matching alpha, and the empty
  // cells to a faint neutral. Matching is done on the COMPUTED colour, which the
  // browser normalises to "rgb(r, g, b)", the raw style attribute is serialised
  // differently by different engines and would work in Chrome only.
  const NH_HEAT_RAMP = {
    'rgb(45, 45, 45)': 'rgba(255,255,255,0.05)',   // no listening
    'rgb(14, 68, 41)': 0.30,                        // ramp 1 → accent alpha
    'rgb(0, 109, 50)': 0.52,
    'rgb(38, 166, 65)': 0.76,
    'rgb(57, 211, 83)': 1,
  };
  let nhHeatRgb = null;
  function nhAccentRgb() {
    const raw = (getComputedStyle(document.documentElement).getPropertyValue('--nh-amber') || '').trim();
    if (nhHeatRgb && nhHeatRgb.src === raw) return nhHeatRgb.v;
    let v = '224,194,122';
    const m = raw.match(/^#?([0-9a-f]{3}|[0-9a-f]{6})$/i);
    if (m) {
      let h = m[1];
      if (h.length === 3) h = h[0] + h[0] + h[1] + h[1] + h[2] + h[2];
      v = parseInt(h.slice(0, 2), 16) + ',' + parseInt(h.slice(2, 4), 16) + ',' + parseInt(h.slice(4, 6), 16);
    } else {
      const r = raw.match(/(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/);
      if (r) v = r[1] + ',' + r[2] + ',' + r[3];
    }
    nhHeatRgb = { src: raw, v: v };
    return v;
  }
  // ===================== AUTOPLAY NEXT IN SERIES =====================
  // ABS stops at the end of a book. When this is on, the next volume of the same
  // series starts by itself.
  // The trigger is deliberately narrow: playback STOPPED and the book that was
  // playing is now marked finished. Listening to the <audio> "ended" event would
  // be wrong, that fires between every track of a multi-file book, and reacting
  // to isFinished alone would hijack the speakers when someone merely ticks a
  // book off by hand. `done` makes it fire at most once per book per session.
  const nhAp = { last: null, done: {}, busy: false };

  function nhApSeriesOf(md) {
    // /api/items returns series as an object on the item, as an array in the
    // listening-stats metadata. Accept either.
    const s = md && md.series;
    if (!s) return null;
    const one = Array.isArray(s) ? s[0] : s;
    return one && one.id ? one : null;
  }
  function nhApSeq(v) {
    const n = parseFloat(String(v == null ? '' : v).replace(',', '.'));
    return isFinite(n) ? n : null;
  }

  async function nhApPlayNext(finishedId) {
    const tok = nhSrToken();
    if (!tok) return;
    const H = { Authorization: 'Bearer ' + tok };
    const item = await fetch('/api/items/' + finishedId, { headers: H }).then((r) => (r.ok ? r.json() : null)).catch(() => null);
    const md = item && item.media && item.media.metadata;
    const ser = nhApSeriesOf(md);
    if (!ser || !item.libraryId) return;
    const here = nhApSeq(ser.sequence);
    // ABS filters a library by series with a base64 (unpadded) series id.
    let key = '';
    try { key = btoa(ser.id).replace(/=/g, ''); } catch (e) { return; }
    const list = await fetch('/api/libraries/' + item.libraryId + '/items?filter=series.' + key + '&limit=300', { headers: H })
      .then((r) => (r.ok ? r.json() : null)).catch(() => null);
    const rows = (list && list.results) || [];
    if (!rows.length) return;
    let prog = [];
    try { prog = window.$nuxt.$store.state.user.user.mediaProgress || []; } catch (e) {}
    const finished = {};
    prog.forEach((x) => { if (x && x.isFinished) finished[x.libraryItemId] = 1; });
    const all = rows.map((it) => ({
      id: it.id,
      title: (it.media && it.media.metadata && it.media.metadata.title) || '',
      seq: nhApSeq(((it.media && it.media.metadata && it.media.metadata.series) || {}).sequence),
      audio: !!(it.media && (it.media.numTracks || (it.media.audioFiles || []).length)),
    }));
    // Plenty of libraries have series with no sequence numbers at all. Order by
    // sequence when there is one, otherwise keep the order ABS returned, which
    // is the series order it shows everywhere else. "Next" is then simply the
    // first playable, unfinished book after this one, either way.
    const numbered = all.filter((x) => x.seq !== null);
    const ordered = numbered.length ? numbered.slice().sort((a, b) => a.seq - b.seq) : all;
    let after;
    const idx = ordered.findIndex((x) => x.id === finishedId);
    if (idx >= 0) after = ordered.slice(idx + 1);
    else if (here !== null) after = ordered.filter((x) => x.seq !== null && x.seq > here);
    else after = ordered;
    const next = after.find((x) => x.audio && !finished[x.id]);
    if (!next) return;
    try { window.$nuxt.$eventBus.$emit('play-item', { libraryItemId: next.id }); } catch (e) { return; }
    const T = nhGsT();
    nhApToast((T.apNext || PANEL_T.en.apNext) + ' · ' + next.title);
  }

  // Autoplay without a word is unnerving, so say what just started.
  function nhApToast(msg) {
    const old = document.getElementById('nh-ap-toast');
    if (old) old.remove();
    const t = document.createElement('div');
    t.id = 'nh-ap-toast';
    t.textContent = msg;
    document.body.appendChild(t);
    setTimeout(() => { t.classList.add('nh-ap-out'); }, 4200);
    setTimeout(() => { t.remove(); }, 5000);
  }

  function nhAutoplaySeries() {
    if (nhSettings.autoplaySeries !== true) return;
    let st = null;
    try { st = window.$nuxt && window.$nuxt.$store; } catch (e) {}
    if (!st) return;
    // ABS does NOT clear streamLibraryItem when a book ends, it leaves the
    // finished book loaded and paused on its last second (measured). So the
    // signal is the item that is STILL current having run out, not the player
    // going empty.
    const cur = st.state.streamLibraryItem ? st.state.streamLibraryItem.id : null;
    if (!cur || nhAp.done[cur] || nhAp.busy) return;
    const a = document.querySelector('audio');
    // Both halves matter. "Audio ended" alone fires between the tracks of a
    // multi-file book; "marked finished" alone would grab the speakers when
    // someone simply ticks a book off by hand. Together they mean: this book
    // just played out.
    const atEnd = !!a && (a.ended || (a.duration && a.currentTime >= a.duration - 1.5));
    if (!atEnd) return;
    let prog = [];
    try { prog = st.state.user.user.mediaProgress || []; } catch (e) {}
    const p = prog.find((x) => x && x.libraryItemId === cur);
    if (!p || !p.isFinished) return;
    nhAp.done[cur] = 1;
    nhAp.busy = true;
    nhApPlayNext(cur).catch(() => {}).then(() => { nhAp.busy = false; });
  }

  // While one of our overlays is up, the page underneath must not scroll, its
  // scrollbar sat right beside the sheet's own (Pawel: "two scrolls" on Year in
  // review). Driven from the tick rather than from every open/close site: the
  // overlays are created and removed in a dozen places, and a missed call there
  // would leave the app permanently unscrollable.
  // How much every scroller must keep free at the bottom so its last line is not
  // hidden behind the floating player. MEASURED, because the player is taller on
  // phones than on the desktop, the old flat 190px cleared it on a desktop and
  // still buried ~18-76px of content on a phone.
  function nhPlayerPad() {
    const root = document.documentElement;
    const pl = document.getElementById('mediaPlayerContainer');
    if (!pl) { if (root.style.getPropertyValue('--nh-player-pad')) root.style.removeProperty('--nh-player-pad'); return; }
    const r = pl.getBoundingClientRect();
    if (!r.height) return;
    // Phones need a deeper lip: ABS paints a card's caption OUTSIDE the card box
    // (measured 35px of title/author hanging below the last row), so padding sized
    // to the shelf alone still left the text under the player.
    const lip = window.innerWidth <= 640 ? 48 : 16;
    const want = Math.ceil(window.innerHeight - r.top) + lip;
    const cur = parseFloat(root.style.getPropertyValue('--nh-player-pad')) || 0;
    if (Math.abs(want - cur) >= 4) root.style.setProperty('--nh-player-pad', want + 'px');
  }

  const NH_MODAL_IDS = '#nh-yir-modal, #nh-sd-modal, #nh-col-modal, #nh-ct-modal, #nh-ab-modal, #nh-us-modal, #nh-ae-modal, #nh-rf-sheet, #nh-rt-modal, #nh-imp-modal, #nh-hx-modal, #nh-rd-modal, #nh-amf-confirm';
  function nhModalLock() {
    document.body.classList.toggle('nh-modal-open', !!document.querySelector(NH_MODAL_IDS));
  }

  // ===================== THEMED DATE PICKER =====================
  // The popup a native <input type=date> opens is browser chrome, no page CSS
  // reaches it, so it kept appearing as the stock white-on-grey calendar next to
  // our themed fields (Pawel's screenshot). The inputs stay in the DOM exactly
  // as they were (value + change event, so every save handler is untouched) but
  // are readOnly, and this popover is the whole editing surface.
  // Exposed as window.__nhDatePicker so book-details.js (loaded after this file)
  // uses the same one.
  const nhDp = { input: null, view: null, path: '' };

  // A real click closes the popover via pointerdown-outside before any nav, but
  // keyboard or programmatic navigation has no pointerdown, without this the
  // calendar floats over the NEXT page (seen live in the r10 probe).
  function nhDpRouteGuard() {
    if (nhDp.input && nhDp.path !== location.pathname && document.getElementById('nh-dp')) nhDpClose();
  }

  function nhDpIso(d) {
    return d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0') + '-' + String(d.getDate()).padStart(2, '0');
  }

  function nhDpClose() {
    const el = document.getElementById('nh-dp');
    if (el) el.remove();
    nhDp.input = null;
    document.removeEventListener('keydown', nhDpKey, true);
    document.removeEventListener('pointerdown', nhDpOutside, true);
    window.removeEventListener('scroll', nhDpClose, true);
  }
  function nhDpKey(e) { if (e.key === 'Escape') { e.stopPropagation(); nhDpClose(); } }
  function nhDpOutside(e) {
    const el = document.getElementById('nh-dp');
    if (el && !el.contains(e.target) && e.target !== nhDp.input) nhDpClose();
  }

  function nhDpRender() {
    const el = document.getElementById('nh-dp');
    if (!el || !nhDp.input) return;
    const lang = getUserLanguage();
    const view = nhDp.view;
    const selIso = nhDp.input.value || '';
    const todayIso = nhDpIso(new Date());
    el.textContent = '';

    const head = document.createElement('div'); head.className = 'nh-dp-head';
    const mk = (txt, delta) => {
      const b = document.createElement('button');
      b.type = 'button'; b.className = 'nh-dp-nav'; b.textContent = txt;
      b.addEventListener('click', (e) => { e.stopPropagation(); nhDp.view = new Date(view.getFullYear(), view.getMonth() + delta, 1); nhDpRender(); });
      return b;
    };
    const lab = document.createElement('span'); lab.className = 'nh-dp-month';
    lab.textContent = view.toLocaleDateString(lang, { month: 'long', year: 'numeric' });
    head.appendChild(mk('‹', -1)); head.appendChild(lab); head.appendChild(mk('›', 1));
    el.appendChild(head);

    const grid = document.createElement('div'); grid.className = 'nh-dp-grid';
    // 2026-01-05 is a Monday, locale weekday names without shipping a table.
    for (let i = 0; i < 7; i++) {
      const w = document.createElement('span'); w.className = 'nh-dp-w';
      try { w.textContent = new Date(Date.UTC(2026, 0, 5 + i)).toLocaleDateString(lang, { weekday: 'short', timeZone: 'UTC' }).slice(0, 2); } catch (e) { w.textContent = 'MTWTFSS'[i]; }
      grid.appendChild(w);
    }
    // Monday-first grid; 42 cells covers every month shape.
    const first = new Date(view.getFullYear(), view.getMonth(), 1);
    const lead = (first.getDay() + 6) % 7;
    for (let c = 0; c < 42; c++) {
      const d = new Date(view.getFullYear(), view.getMonth(), 1 - lead + c);
      const iso = nhDpIso(d);
      const b = document.createElement('button');
      b.type = 'button';
      b.className = 'nh-dp-day' + (d.getMonth() !== view.getMonth() ? ' nh-dp-out' : '') +
        (iso === selIso ? ' nh-dp-sel' : '') + (iso === todayIso ? ' nh-dp-today' : '');
      b.textContent = String(d.getDate());
      b.addEventListener('click', (e) => {
        e.stopPropagation();
        const inp = nhDp.input;
        nhDpClose();
        if (!inp) return;
        inp.value = iso;
        inp.dispatchEvent(new Event('change', { bubbles: true }));
      });
      grid.appendChild(b);
    }
    el.appendChild(grid);
  }

  function nhDpOpen(input) {
    if (nhDp.input === input && document.getElementById('nh-dp')) { nhDpClose(); return; }
    nhDpClose();
    nhDp.input = input;
    nhDp.path = location.pathname;
    const parts = (input.value || '').split('-');
    nhDp.view = parts.length === 3 ? new Date(+parts[0], +parts[1] - 1, 1) : new Date();
    if (isNaN(nhDp.view.getTime())) nhDp.view = new Date();
    const el = document.createElement('div');
    el.id = 'nh-dp';
    document.body.appendChild(el);
    nhDpRender();
    // Below the field, flipped above when there is no room, clamped to the viewport.
    const r = input.getBoundingClientRect();
    const w = el.offsetWidth, h = el.offsetHeight;
    let top = r.bottom + 6;
    if (top + h > innerHeight - 8) top = Math.max(8, r.top - h - 6);
    let left = Math.min(Math.max(8, r.left), innerWidth - w - 8);
    el.style.top = Math.round(top) + 'px';
    el.style.left = Math.round(left) + 'px';
    document.addEventListener('keydown', nhDpKey, true);
    document.addEventListener('pointerdown', nhDpOutside, true);
    window.addEventListener('scroll', nhDpClose, true);
  }
  window.__nhDatePicker = nhDpOpen;

  // One delegated opener for every themed date field, current and future.
  document.addEventListener('click', (e) => {
    const inp = e.target.closest && e.target.closest('input.nh-ys-date, input.nh-bd-dt-inp');
    if (!inp) return;
    e.preventDefault();
    nhDpOpen(inp);
  }, true);
  document.addEventListener('keydown', (e) => {
    if (e.key !== 'Enter' && e.key !== ' ') return;
    const inp = e.target && e.target.matches && e.target.matches('input.nh-ys-date, input.nh-bd-dt-inp') ? e.target : null;
    if (!inp) return;
    e.preventDefault();
    nhDpOpen(inp);
  }, true);

  // ===================== "YOUR LISTENING" INSIGHTS =====================
  // ABS's Your Stats page shows four numbers, a 7-day chart and a year heatmap,
  // and stops there, while /api/me/listening-stats already carries a per-item
  // breakdown (title, authors, narrators) and a dayOfWeek map it never renders.
  // This panel spends that data: streaks, which weekday you actually listen on,
  // the week-over-week trend, and the books/authors/narrators you spend time
  // with. It also fills the width the fixed-width native columns left empty.
  const nhYs = { data: null, fetching: false, tried: 0, sig: '' };

  function nhYsFetch() {
    if (nhYs.data || nhYs.fetching || nhYs.tried > 3) return;
    const tok = nhSrToken();
    if (!tok) return;
    nhYs.fetching = true; nhYs.tried++;
    fetch('/api/me/listening-stats', { headers: { Authorization: 'Bearer ' + tok } })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => { nhYs.data = j || null; nhYs.fetching = false; })
      .catch(() => { nhYs.fetching = false; });
  }

  // Date maths in UTC on the Y-M-D parts: the keys are plain local dates, and
  // doing this with real Date arithmetic makes a streak break on a DST boundary.
  function nhYsDayNum(key) {
    const m = /^(\d{4})-(\d{2})-(\d{2})$/.exec(key);
    return m ? Date.UTC(+m[1], +m[2] - 1, +m[3]) / 86400000 : null;
  }
  function nhYsTodayNum() {
    const d = new Date();
    return Date.UTC(d.getFullYear(), d.getMonth(), d.getDate()) / 86400000;
  }

  function nhYsDigest(st) {
    const raw = (st && st.days) || {};
    const days = {};
    Object.keys(raw).forEach((k) => { const n = nhYsDayNum(k); if (n !== null && raw[k] > 0) days[n] = raw[k]; });
    const nums = Object.keys(days).map(Number).sort((a, b) => a - b);
    if (!nums.length) return null;

    let longest = 0, run = 0, prev = null;
    nums.forEach((n) => { run = (prev !== null && n - prev === 1) ? run + 1 : 1; if (run > longest) longest = run; prev = n; });

    // Today with nothing played yet must not read as a broken streak, so start
    // from today when it counts and from yesterday otherwise.
    const today = nhYsTodayNum();
    let cur = 0, at = days[today] ? today : today - 1;
    while (days[at]) { cur++; at--; }

    const span = (from, to) => { let s = 0; for (let n = from; n <= to; n++) s += days[n] || 0; return s; };
    const week = span(today - 6, today);
    const prevWeek = span(today - 13, today - 7);
    const totalSecs = nums.reduce((s, n) => s + days[n], 0);
    const best = nums.reduce((b, n) => (days[n] > (days[b] || 0) ? n : b), nums[0]);

    const items = (st && st.items) || {};
    const books = Object.keys(items).map((k) => items[k])
      .filter((it) => it && it.timeListening > 0)
      .sort((a, b) => b.timeListening - a.timeListening);
    const tally = (pick) => {
      const acc = {};
      books.forEach((it) => {
        const md = it.mediaMetadata || {};
        (pick(md) || []).forEach((name) => { if (name) acc[name] = (acc[name] || 0) + it.timeListening; });
      });
      return Object.keys(acc).map((k) => ({ name: k, secs: acc[k] })).sort((a, b) => b.secs - a.secs).slice(0, 5);
    };
    return {
      cur: cur, longest: longest, daysCount: nums.length,
      week: week, prevWeek: prevWeek,
      perDay: totalSecs / nums.length,
      best: days[best],
      dow: (st && st.dayOfWeek) || null,
      books: books.slice(0, 5),
      authors: tally((md) => (md.authors || []).map((a) => a.name)),
      narrators: tally((md) => md.narrators || []),
    };
  }

  function nhYsBarRow(name, secs, max, thumb) {
    const row = document.createElement('div'); row.className = 'nh-ys-row';
    if (thumb) { const c = document.createElement('span'); c.className = 'nh-ys-thumb'; c.style.backgroundImage = 'url("' + thumb + '")'; row.appendChild(c); }
    const mid = document.createElement('span'); mid.className = 'nh-ys-mid';
    const nm = document.createElement('span'); nm.className = 'nh-ys-name'; nm.textContent = name; nm.title = name;
    const bar = document.createElement('span'); bar.className = 'nh-ys-bar';
    const fill = document.createElement('span'); fill.className = 'nh-ys-fill';
    fill.style.width = Math.max(3, Math.round((secs / (max || 1)) * 100)) + '%';
    bar.appendChild(fill);
    mid.appendChild(nm); mid.appendChild(bar);
    const tm = document.createElement('span'); tm.className = 'nh-ys-time'; tm.textContent = nhSbFmt(secs);
    row.appendChild(mid); row.appendChild(tm);
    return row;
  }

  // ---- finished-book tools -------------------------------------------------
  // Two things ABS has the data for but never surfaces: what you finished lately
  // (with a wrong date you can correct, ABS stamps finishedAt when you tick the
  // box, not when you actually finished), and the books sitting at 97-99% that
  // never got marked because the file has credits or silence at the end.
  // "Almost done" criteria (Pawel, v2.5.0): a book counts as abandoned-near-the-
  // end when it is 94%+ in, has not been touched for three days, AND something
  // ELSE was played after it, you moved on without finishing. The last clause
  // keeps a book you only paused yesterday-equivalent (nothing played since)
  // out of everyone's face.
  const NH_ALMOST_MIN = 0.94;
  const NH_ALMOST_IDLE = 3 * 24 * 3600 * 1000;

  function nhAmfList() {
    let prog = [];
    try { prog = window.$nuxt.$store.state.user.user.mediaProgress || []; } catch (e) {}
    const now = Date.now();
    return prog.filter((x) => {
      if (!x || x.isFinished || !x.libraryItemId || x.episodeId) return false;
      const p = Math.max(x.progress || 0, x.ebookProgress || 0);
      if (p < NH_ALMOST_MIN || p >= 1) return false;
      const lu = x.lastUpdate || 0;
      if (now - lu < NH_ALMOST_IDLE) return false;
      return prog.some((o) => o && o !== x && o.libraryItemId && (o.lastUpdate || 0) > lu);
    }).sort((a, b) => (b.lastUpdate || 0) - (a.lastUpdate || 0));
  }

  function nhFdRows() {
    let prog = [];
    try { prog = window.$nuxt.$store.state.user.user.mediaProgress || []; } catch (e) {}
    const done = prog.filter((x) => x && x.isFinished && x.libraryItemId)
      .sort((a, b) => (b.finishedAt || b.lastUpdate || 0) - (a.finishedAt || a.lastUpdate || 0)).slice(0, 6);
    const almost = nhAmfList().slice(0, 6);
    return { done: done, almost: almost };
  }

  // Titles are not in mediaProgress; fetch once and reuse the rate-row cache.
  function nhFdTitle(id, el) {
    if (nhRfTitles[id]) { el.textContent = nhRfTitles[id]; return; }
    el.textContent = '…';
    const tk = nhSrToken();
    if (!tk) return;
    fetch('/api/items/' + id, { headers: { Authorization: 'Bearer ' + tk } })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => {
        const nm = j && j.media && j.media.metadata && j.media.metadata.title;
        if (nm) { nhRfTitles[id] = nm; el.textContent = nm; }
      }).catch(() => {});
  }

  function nhFdPatch(id, body, onOk) {
    const tk = nhSrToken();
    if (!tk) return;
    fetch('/api/me/progress/' + id, { method: 'PATCH', headers: { Authorization: 'Bearer ' + tk, 'Content-Type': 'application/json' }, body: JSON.stringify(body) })
      .then((r) => { if (r.ok && onOk) onOk(); })
      .catch(() => {});
  }

  function nhFdIsoDay(ms) {
    if (!ms) return '';
    const d = new Date(ms);
    return d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0') + '-' + String(d.getDate()).padStart(2, '0');
  }

  function nhFdCards(wrap, t) {
    const rows = nhFdRows();
    const tk = window.__NH_TOKEN || getTokenNH();
    const tq = tk ? '&token=' + encodeURIComponent(tk) : '';
    // Both lists present: side by side in two columns (Pawel) instead of a wide
    // card stacked over a narrow one. Alone, each keeps its old footprint.
    const both = rows.done.length && rows.almost.length;
    let host = wrap;
    if (both) {
      host = document.createElement('div');
      host.className = 'nh-fd-cols';
      wrap.appendChild(host);
    }
    if (rows.done.length) {
      // Full width: a cover, a title and a whole date do not fit one column of
      // the insights grid, and the date field was being cut to "26 / 07 / 202".
      const card = document.createElement('div'); card.className = 'nh-ys-card' + (both ? '' : ' nh-ys-wide');
      const h = document.createElement('div'); h.className = 'nh-ys-h'; h.textContent = t('ysFinished'); card.appendChild(h);
      const list = document.createElement('div'); list.className = 'nh-ys-fgrid'; card.appendChild(list);
      rows.done.forEach((p) => {
        const row = document.createElement('div'); row.className = 'nh-ys-frow';
        const th = document.createElement('span'); th.className = 'nh-ys-thumb';
        th.style.backgroundImage = 'url("/api/items/' + p.libraryItemId + '/cover?width=80' + tq + '")';
        row.appendChild(th);
        const nm = document.createElement('span'); nm.className = 'nh-ys-name'; nhFdTitle(p.libraryItemId, nm);
        // A native date input is the whole editor: keyboard accessible, and the
        // OS picker is better than anything hand-rolled at this size.
        const date = document.createElement('input');
        date.type = 'date'; date.className = 'nh-ys-date'; date.title = t('ysEditDate');
        date.readOnly = true;   // readOnly suppresses the native popup; ours opens instead
        date.value = nhFdIsoDay(p.finishedAt);
        // readOnly also drops the browser's calendar glyph, so the field read as
        // plain text, the wrap overlays our own icon (says "clickable").
        const dWrap = document.createElement('span'); dWrap.className = 'nh-date-wrap';
        dWrap.appendChild(date);
        const dIco = document.createElement('span'); dIco.className = 'nh-date-ico';
        dWrap.appendChild(dIco);
        date.addEventListener('change', () => {
          if (!date.value) return;
          const parts = date.value.split('-');
          const ms = new Date(+parts[0], +parts[1] - 1, +parts[2], 12, 0, 0).getTime();
          date.classList.add('nh-ys-saving');
          nhFdPatch(p.libraryItemId, { finishedAt: ms }, () => {
            date.classList.remove('nh-ys-saving');
            try { window.$nuxt.$store.state.user.user.mediaProgress.forEach((x) => { if (x.libraryItemId === p.libraryItemId) x.finishedAt = ms; }); } catch (e) {}
            nhYs.bump = (nhYs.bump || 0) + 1;
          });
        });
        row.appendChild(nm); row.appendChild(dWrap);
        list.appendChild(row);
      });
      host.appendChild(card);
    }
    if (rows.almost.length) {
      const card = document.createElement('div'); card.className = 'nh-ys-card';
      card.id = 'nh-ys-almost'; // the avatar badge deep-links here
      const h = document.createElement('div'); h.className = 'nh-ys-h'; h.textContent = t('ysAlmost'); card.appendChild(h);
      rows.almost.forEach((p) => {
        const row = document.createElement('div'); row.className = 'nh-ys-frow';
        const nm = document.createElement('span'); nm.className = 'nh-ys-name'; nhFdTitle(p.libraryItemId, nm);
        const pct = document.createElement('span'); pct.className = 'nh-ys-pct';
        pct.textContent = Math.floor((p.progress || 0) * 100) + '%';
        const btn = document.createElement('button');
        btn.type = 'button'; btn.className = 'nh-ys-done'; btn.title = t('ysMarkDone');
        btn.setAttribute('aria-label', t('ysMarkDone'));
        btn.innerHTML = '<span class="material-symbols">check_circle</span>';
        btn.addEventListener('click', () => {
          nhAmfConfirm(nm.textContent, () => {
            btn.disabled = true;
            nhFdPatch(p.libraryItemId, { isFinished: true }, () => {
              row.classList.add('nh-ys-gone');
              try {
                const mp = window.$nuxt.$store.state.user.user.mediaProgress;
                mp.forEach((x) => { if (x.libraryItemId === p.libraryItemId) { x.isFinished = true; x.progress = 1; x.finishedAt = Date.now(); } });
              } catch (e) {}
              setTimeout(() => { nhYs.bump = (nhYs.bump || 0) + 1; }, 260);
            });
          });
        });
        row.appendChild(nm); row.appendChild(pct); row.appendChild(btn);
        card.appendChild(row);
      });
      host.appendChild(card);
    }
  }

  function nhYsList(title, rows, thumbFor) {
    const card = document.createElement('div'); card.className = 'nh-ys-card';
    const h = document.createElement('div'); h.className = 'nh-ys-h'; h.textContent = title; card.appendChild(h);
    const max = rows.reduce((m, r) => Math.max(m, r.secs), 0);
    rows.forEach((r) => card.appendChild(nhYsBarRow(r.name, r.secs, max, thumbFor ? thumbFor(r) : null)));
    return card;
  }

  function nhYourStatsExtras() {
    const grid = document.getElementById('nh-stats-grid');
    const heat = document.getElementById('heatmap');
    if (!grid || !heat || !heat.parentElement) { return; }
    nhYsFetch();
    const d = nhYs.data ? nhYsDigest(nhYs.data) : null;
    if (!d) return;
    // Home is the chart/sessions ROW, not the card: the chart is stuck at 384px
    // and the session list only wants ~500, so on a wide card the rest of that
    // row was empty. As a third flex child the panel takes exactly that surplus
    // and wraps underneath when there isn't any. Falls back to sitting above the
    // heatmap if ABS ever restructures the row.
    const row = heat.parentElement.querySelector(':scope > div[class*="flex-row"]');
    const host = row || heat.parentElement;
    // The finished/almost lists come from the live store, so they join the
    // signature, otherwise ticking a book off would not redraw the panel.
    const fd = nhSettings.finishedTools === false ? { done: [], almost: [] } : nhFdRows();
    const sig = [d.cur, d.longest, d.daysCount, d.week, d.prevWeek, Math.round(d.perDay), d.best,
      d.books.map((b) => b.id + ':' + b.timeListening).join(','), d.authors.length, d.narrators.length,
      fd.done.map((x) => x.libraryItemId + ':' + (x.finishedAt || 0)).join(','),
      fd.almost.map((x) => x.libraryItemId + ':' + Math.round((x.progress || 0) * 1000)).join(','),
      nhYs.bump || 0].join('|');
    const existing = document.getElementById('nh-ys');
    if (existing && existing.dataset.sig === sig && existing.parentElement === host) return;

    const T = nhGsT();
    const t = (k) => T[k] || PANEL_T.en[k];
    const panel = existing || document.createElement('div');
    panel.id = 'nh-ys';
    panel.dataset.sig = sig;
    panel.textContent = '';

    const head = document.createElement('h2'); head.className = 'nh-ys-title'; head.textContent = t('ysTitle');
    panel.appendChild(head);
    const wrap = document.createElement('div'); wrap.className = 'nh-ys-grid'; panel.appendChild(wrap);

    // --- streak ---
    const sc = document.createElement('div'); sc.className = 'nh-ys-card nh-ys-figs';
    const sh = document.createElement('div'); sh.className = 'nh-ys-h'; sh.textContent = t('ysStreak'); sc.appendChild(sh);
    const mkFig = (val, lab) => {
      const f = document.createElement('div'); f.className = 'nh-ys-fig';
      const v = document.createElement('div'); v.className = 'nh-ys-val'; v.textContent = val;
      const l = document.createElement('div'); l.className = 'nh-ys-lab'; l.textContent = lab;
      f.appendChild(v); f.appendChild(l); return f;
    };
    const figs = document.createElement('div'); figs.className = 'nh-ys-figrow';
    figs.appendChild(mkFig(String(d.cur), t('ysCurrent')));
    figs.appendChild(mkFig(String(d.longest), t('ysLongest')));
    figs.appendChild(mkFig(String(d.daysCount), t('ysDaysTotal')));
    sc.appendChild(figs);
    wrap.appendChild(sc);

    // --- pace: this week against the one before it ---
    const pc = document.createElement('div'); pc.className = 'nh-ys-card nh-ys-figs';
    const ph = document.createElement('div'); ph.className = 'nh-ys-h'; ph.textContent = t('ysPace'); pc.appendChild(ph);
    const prow = document.createElement('div'); prow.className = 'nh-ys-figrow';
    const wk = mkFig(nhSbFmt(d.week), t('ysThisWeek'));
    if (d.prevWeek > 0) {
      const pct = Math.round(((d.week - d.prevWeek) / d.prevWeek) * 100);
      const tag = document.createElement('span');
      tag.className = 'nh-ys-delta ' + (pct >= 0 ? 'up' : 'down');
      tag.textContent = (pct >= 0 ? '▲ ' : '▼ ') + Math.abs(pct) + '%';
      wk.querySelector('.nh-ys-lab').appendChild(tag);
    }
    prow.appendChild(wk);
    prow.appendChild(mkFig(nhSbFmt(d.perDay), t('ysPerDay')));
    prow.appendChild(mkFig(nhSbFmt(d.best), t('ysBestDay')));
    pc.appendChild(prow);
    wrap.appendChild(pc);

    // --- which weekday, from the dayOfWeek map ABS computes but never shows ---
    if (d.dow) {
      const order = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'];
      const max = order.reduce((m, k) => Math.max(m, d.dow[k] || 0), 0);
      if (max > 0) {
        const wc = document.createElement('div'); wc.className = 'nh-ys-card';
        const wh = document.createElement('div'); wh.className = 'nh-ys-h'; wh.textContent = t('ysWhen'); wc.appendChild(wh);
        const cols = document.createElement('div'); cols.className = 'nh-ys-week';
        const lang = getUserLanguage();
        order.forEach((k, idx) => {
          const secs = d.dow[k] || 0;
          const col = document.createElement('div'); col.className = 'nh-ys-day' + (secs === max ? ' nh-ys-top' : '');
          col.title = nhSbFmt(secs);
          const track = document.createElement('span'); track.className = 'nh-ys-dtrack';
          const f = document.createElement('span'); f.className = 'nh-ys-dfill';
          f.style.height = Math.max(3, Math.round((secs / max) * 100)) + '%';
          track.appendChild(f);
          const lab = document.createElement('span'); lab.className = 'nh-ys-dlab';
          // 2026-01-05 is a Monday, a fixed anchor so the short weekday names
          // come from the browser's locale instead of a table we have to ship.
          let nm = k.slice(0, 3);
          try { nm = new Date(Date.UTC(2026, 0, 5 + idx)).toLocaleDateString(lang, { weekday: 'short', timeZone: 'UTC' }); } catch (e) {}
          lab.textContent = nm;
          col.appendChild(track); col.appendChild(lab);
          cols.appendChild(col);
        });
        wc.appendChild(cols);
        wrap.appendChild(wc);
      }
    }

    const tk = window.__NH_TOKEN || getTokenNH();
    const tq = tk ? '&token=' + encodeURIComponent(tk) : '';
    if (d.books.length) {
      wrap.appendChild(nhYsList(t('ysTopBooks'),
        d.books.map((b) => ({ name: (b.mediaMetadata && b.mediaMetadata.title) || '-', secs: b.timeListening, id: b.id })),
        (r) => '/api/items/' + r.id + '/cover?width=80' + tq));
    }
    if (d.authors.length) wrap.appendChild(nhYsList(t('ysTopAuthors'), d.authors));
    if (d.narrators.length) wrap.appendChild(nhYsList(t('ysTopNarrators'), d.narrators));
    if (nhSettings.finishedTools !== false) nhFdCards(wrap, t);

    if (panel.parentElement !== host) host.appendChild(panel);
  }

  // Two blocks on Your Stats have a fixed intrinsic width: the 7-day chart (its
  // bars are absolutely positioned against a 384px box) and the year heatmap
  // (ABS sizes it once on mount and overshoots its container by ~24px, which is
  // what put a scrollbar under it on a phone). Neither can simply be told to be
  // narrower, so each is SCALED to the room it has.
  // The negative margins are the point: a scale never shrinks the layout box, so
  // without them the block keeps its full-size footprint and leaves exactly the
  // scaled-away strip as dead space, which is what ABS's own scale-75 did below
  // lg, under-filling the width AND leaving a gap after it.
  function nhFitInto(el, avail, naturalW, maxScale) {
    if (!el || !avail) return;
    const h = el.offsetHeight; // layout metrics ignore scale
    const w = naturalW || el.offsetWidth;
    if (!w || !h) return;
    const cap = maxScale || 1;
    const sig = Math.round(avail) + ':' + Math.round(w) + ':' + h + ':' + cap;
    if (el.dataset.nhFit === sig) return;
    el.dataset.nhFit = sig;
    // Scales UP as well as down when a cap above 1 is given: these blocks are
    // DOM boxes, not bitmaps, so enlarging them stays crisp and they finally use
    // the room they are given instead of sitting small in the middle of it.
    const s = Math.min(cap, avail / w);
    // Only skip when the scale really is 1. The old guard was >= 0.999, written
    // when this could only ever shrink, with a cap above 1 it swallowed every
    // enlargement and stripped the transform straight back off.
    if (Math.abs(s - 1) < 0.01) {
      el.style.removeProperty('transform');
      el.style.removeProperty('margin-right');
      el.style.removeProperty('margin-bottom');
      return;
    }
    el.style.setProperty('transform-origin', 'top left');
    el.style.setProperty('transform', 'scale(' + s.toFixed(4) + ')');
    // Signed, not a literal minus: scaling UP needs POSITIVE margins to make
    // room for the extra size, scaling down needs negative ones to reclaim it.
    el.style.setProperty('margin-right', Math.round(w * (s - 1)) + 'px');
    el.style.setProperty('margin-bottom', Math.round(h * (s - 1)) + 'px');
  }

  // ABS's caption always says "N days listened in the last year", but it sizes
  // the grid to its container and on a phone only draws the last few months,   // so the sentence claimed a year while the chart showed five months. Name the
  // window that is actually on screen when it is short of a full year.
  // The month row is found by geometry, not by matching month names: the labels
  // are localised, but the months are the ones sharing the topmost row (weekday
  // labels sit further down the left edge).
  function nhHeatmapCaption() {
    const hm = document.getElementById('heatmap');
    const cap = hm && hm.querySelector('p');
    if (!cap) return;
    const labels = [...hm.querySelectorAll('div')].filter((d) => !d.children.length && (d.textContent || '').trim() && /text-gray-300/.test(d.className || ''));
    if (labels.length < 4) return;
    const boxes = labels.map((d) => ({ t: d.getBoundingClientRect().top, l: d.getBoundingClientRect().left, x: d.textContent.trim() }));
    const top = Math.min.apply(null, boxes.map((b) => b.t));
    const months = boxes.filter((b) => b.t - top < 4).sort((a, b) => a.l - b.l);
    if (!months.length) return;
    if (!cap.dataset.nhOrig) cap.dataset.nhOrig = cap.textContent.trim();
    const base = cap.dataset.nhOrig;
    const T = nhGsT();
    const range = months.length >= 12 ? '' : ' · ' + (T.hmShowing || PANEL_T.en.hmShowing) + ' ' +
      months[0].x + (months.length > 1 ? '–' + months[months.length - 1].x : '');
    const want = base + range;
    if (cap.textContent !== want) cap.textContent = want;
  }

  function nhStatsFit() {
    const grid = document.getElementById('nh-stats-grid');
    if (!grid) return;
    const col = grid.querySelector('.configContent div[class*="scale-75"]');
    // The row is the flex container; its content box is the room the column has.
    if (col && col.parentElement) {
      const par = col.parentElement;
      const cs = getComputedStyle(par);
      let avail = par.clientWidth - parseFloat(cs.paddingLeft || 0) - parseFloat(cs.paddingRight || 0);
      // Side by side (md+) the column is one of several children, so it may only
      // have part of the row, but it is also flex:0 0 auto there and the row
      // wraps, so its own offsetWidth is the honest answer whenever it fits.
      if (avail > col.offsetWidth) avail = col.offsetWidth;
      nhFitInto(col, avail);
    }
    // Fit the heatmap's OUTER wrapper (#heatmap > div.mx-auto), not the bordered
    // box inside it. That wrapper is shrink-to-fit around the grid, so it is the
    // element that actually overhangs, scaling its child left the wrapper at its
    // full width and the container still reported the overflow.
    const hm = document.getElementById('heatmap');
    const wrap = hm && hm.firstElementChild;
    if (wrap && hm.clientWidth) {
      const inner = hm.querySelector('[class*="border-white/25"]');
      if (inner && inner.style.transform) { // clear an earlier attempt at the wrong element
        inner.style.removeProperty('transform');
        inner.style.removeProperty('margin-right');
        inner.style.removeProperty('margin-bottom');
        delete inner.dataset.nhFit;
      }
      nhFitInto(wrap, hm.clientWidth, 0, 1.6);
    }
  }

  // ===================== OUR OWN 7-DAY CHART =====================
  // Replaces ABS's Minutes-Listening block wholesale. That one is a 384px box of
  // absolutely-positioned bars at scale(.75): it cannot stretch, cannot centre,
  // and two rounds of transform surgery destabilised the card, so it is hidden
  // (CSS) and this block, built from the same /api/me/listening-stats days map,
  // takes its place: fluid width, theme type, accent on the heaviest day.
  function nhWeekChart() {
    const grid = document.getElementById('nh-stats-grid');
    const heat = document.getElementById('heatmap');
    if (!grid || !heat || !heat.parentElement) return;
    const row = heat.parentElement.querySelector(':scope > div[class*="flex-row"]');
    if (!row) return;
    nhYsFetch();
    const st = nhYs.data;
    if (!st) return;
    const T = nhGsT();
    const t = (k) => T[k] || PANEL_T.en[k];
    const lang = getUserLanguage();
    const now = new Date();
    const days = [];
    for (let k = 6; k >= 0; k--) {
      const d = new Date(now.getFullYear(), now.getMonth(), now.getDate() - k);
      const key = nhDpIso(d);
      days.push({ key: key, lab: d.toLocaleDateString(lang, { weekday: 'short' }), secs: (st.days && st.days[key]) || 0 });
    }
    const max = Math.max.apply(null, days.map((d) => d.secs));
    const total = days.reduce((s, d) => s + d.secs, 0);
    const dig = nhYsDigest(st);
    const sig = days.map((d) => d.key + ':' + d.secs).join(',') + '|' + (dig ? dig.cur : 0);
    let el = document.getElementById('nh-wk');
    if (el && el.dataset.sig === sig && el.parentElement === row && row.firstElementChild === el) return;
    if (!el) { el = document.createElement('div'); el.id = 'nh-wk'; }
    el.dataset.sig = sig;
    el.textContent = '';

    const h = document.createElement('h2'); h.className = 'nh-wk-title'; h.textContent = t('wkTitle');
    el.appendChild(h);

    // The summary figures come FIRST, as tiles (Pawel: numbers before shape,     // the flat row under the bars was easy to miss). Scoreboard tile classes so
    // the whole stats page keeps one tile look.
    const tiles = document.createElement('div'); tiles.className = 'nh-sb-tiles nh-wk-tiles';
    const mkTile = (val, lab2, sub) => {
      const x = document.createElement('div'); x.className = 'nh-sb-tile';
      const v = document.createElement('div'); v.className = 'nh-sb-tval'; v.textContent = val; x.appendChild(v);
      const s2 = document.createElement('div'); s2.className = 'nh-sb-tsub'; s2.textContent = sub || ' '; x.appendChild(s2);
      const l = document.createElement('div'); l.className = 'nh-sb-tlab'; l.textContent = lab2; x.appendChild(l);
      return x;
    };
    tiles.appendChild(mkTile(nhSbFmt(total), t('ysThisWeek'), nhSbSub(total)));
    tiles.appendChild(mkTile(nhSbFmt(total / 7), t('wkAvg'), ''));
    tiles.appendChild(mkTile(nhSbFmt(max), t('ysBestDay'), ''));
    tiles.appendChild(mkTile(String(dig ? dig.cur : 0), t('wkRow'), ''));
    el.appendChild(tiles);

    const bars = document.createElement('div'); bars.className = 'nh-wk-bars';
    days.forEach((d) => {
      const col = document.createElement('div');
      col.className = 'nh-wk-day' + (max > 0 && d.secs === max ? ' nh-wk-top' : '');
      col.title = nhSbFmt(d.secs);
      const v = document.createElement('span'); v.className = 'nh-wk-v';
      v.textContent = d.secs > 0 ? nhSbFmt(d.secs) : '';
      const track = document.createElement('span'); track.className = 'nh-wk-track';
      const fill = document.createElement('span'); fill.className = 'nh-wk-fill';
      fill.style.height = max > 0 ? Math.max(3, Math.round((d.secs / max) * 100)) + '%' : '3%';
      track.appendChild(fill);
      const lab = document.createElement('span'); lab.className = 'nh-wk-lab'; lab.textContent = d.lab;
      col.appendChild(v); col.appendChild(track); col.appendChild(lab);
      bars.appendChild(col);
    });
    el.appendChild(bars);

    if (row.firstElementChild !== el) row.insertBefore(el, row.firstChild);
  }

  // ===================== SERVER STATISTICS (admin, /config/library-stats) =====
  // ABS's Library Stats page counts files in one library. This replaces it with
  // what an admin actually asks: what does the whole SERVER listen to, most
  // listened books, best rated, top genres, top authors, across every user,
  // with a per-library filter. Built from the same admin listening-stats calls
  // the ranking uses plus the ratings store; genres and authors come from the
  // metadata each listened item already carries, so no library scan is needed
  // until a specific library filter asks for one.
  const nhLs = { users: null, stats: {}, fetching: false, tries: 0, ratings: null, libs: null, libSel: 'all', libItems: {}, libFetching: {}, genres: {}, titles: {}, sig: '' };

  function nhLsAgg() {
    const filt = nhLs.libSel !== 'all' ? nhLs.libItems[nhLs.libSel] : null;
    const inLib = (id) => !filt || filt[id];
    const perItem = {}, perAuthor = {}, perGenre = {}, perUser = {};
    let total = 0;
    (nhLs.users || []).forEach((u) => {
      const st = nhLs.stats[u.id];
      if (!st || !st.items) return;
      let mine = 0;
      Object.keys(st.items).forEach((k) => {
        const it = st.items[k];
        const id = it.id || k;
        if (!it || !(it.timeListening > 0) || !inLib(id)) return;
        const secs = it.timeListening;
        mine += secs;
        const md = it.mediaMetadata || {};
        if (!perItem[id]) perItem[id] = { secs: 0, n: 0, title: md.title || '' };
        perItem[id].secs += secs; perItem[id].n++;
        if (!perItem[id].title && md.title) perItem[id].title = md.title;
        (md.authors || []).forEach((a) => { if (a && a.name) perAuthor[a.name] = (perAuthor[a.name] || 0) + secs; });
        // Live library genres first (#31): the session snapshot keeps whatever
        // genres a book had when it was played, so a cleaned-up library kept
        // surfacing its old Audible ladder ("Literature & Fiction"...).
        (nhLs.genres[id] || md.genres || []).forEach((g) => { if (g) perGenre[g] = (perGenre[g] || 0) + secs; });
      });
      if (mine > 0) { total += mine; perUser[u.id] = mine; }
    });
    const top = (map) => Object.keys(map).map((k) => ({ name: k, secs: map[k] })).sort((a, b) => b.secs - a.secs);
    return {
      total: total,
      listeners: Object.keys(perUser).length,
      played: Object.keys(perItem).length,
      books: Object.keys(perItem).map((id) => ({ id: id, title: perItem[id].title, secs: perItem[id].secs, n: perItem[id].n })).sort((a, b) => b.secs - a.secs).slice(0, 10),
      authors: top(perAuthor).slice(0, 10),
      genres: top(perGenre).slice(0, 5),
    };
  }

  function nhLsBestRated(filt) {
    const items = (nhLs.ratings && nhLs.ratings.items) || {};
    const out = [];
    Object.keys(items).forEach((id) => {
      if (id.indexOf('series:') === 0) return;
      if (filt && !filt[id]) return;
      const rs = items[id];
      let sum = 0, n = 0;
      Object.keys(rs).forEach((uid) => { const v = rs[uid] && rs[uid].stars; if (typeof v === 'number') { sum += v; n++; } });
      if (n) out.push({ id: id, avg: sum / n, n: n });
    });
    return out.sort((a, b) => (b.avg - a.avg) || (b.n - a.n)).slice(0, 5);
  }

  function nhLsTitle(id) {
    if (nhLs.titles[id]) return nhLs.titles[id];
    for (const uid of Object.keys(nhLs.stats)) {
      const it = nhLs.stats[uid] && nhLs.stats[uid].items && nhLs.stats[uid].items[id];
      const nm = it && it.mediaMetadata && it.mediaMetadata.title;
      if (nm) { nhLs.titles[id] = nm; return nm; }
    }
    if (nhRfTitles[id]) return nhRfTitles[id];
    const tok = nhSrToken();
    if (tok && nhLs.titles[id] === undefined) {
      nhLs.titles[id] = null;   // in flight; '' after a failed fetch stops retries
      fetch('/api/items/' + id, { headers: { Authorization: 'Bearer ' + tok } })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => {
          const nm = j && j.media && j.media.metadata && j.media.metadata.title;
          nhLs.titles[id] = nm || '';
          if (nm) nhLs.sig = '';
        }).catch(() => { nhLs.titles[id] = ''; });
    }
    return '';
  }

  function nhLsFetchLibItems(libId) {
    if (nhLs.libItems[libId] || nhLs.libFetching[libId]) return;
    nhLs.libFetching[libId] = true;
    const tok = nhSrToken();
    const map = {};
    // Paged to the end (20k cap). The id map is the library filter; the genres
    // per item are the LIVE source for the genre card (#31), the play-session
    // snapshots are only the fallback for items that are gone.
    const page = (n) => fetch('/api/libraries/' + libId + '/items?limit=500&page=' + n, { headers: { Authorization: 'Bearer ' + tok } })
      .then((r) => (r.ok ? r.json() : null))
      .then((j) => {
        const rows = (j && j.results) || [];
        rows.forEach((it) => {
          map[it.id] = 1;
          const md = (it.media && it.media.metadata) || {};
          if (Array.isArray(md.genres)) nhLs.genres[it.id] = md.genres;
        });
        if (rows.length === 500 && n < 39) return page(n + 1);
        if (rows.length === 500) console.warn('[NanoHive] server stats: library scan capped at 20000 items');
      });
    page(0).catch(() => {}).then(() => { nhLs.libItems[libId] = map; nhLs.libFetching[libId] = false; nhLs.sig = ''; });
  }

  // Library numbers (#33), fetched once per library and summed for "all".
  const nhLsG = { stats: {}, series: {}, fetching: {} };
  function nhLsGlanceFetch(libId) {
    if (nhLsG.stats[libId] !== undefined || nhLsG.fetching[libId]) return;
    nhLsG.fetching[libId] = true;
    const H = { headers: { Authorization: 'Bearer ' + nhSrToken() } };
    Promise.all([
      fetch('/api/libraries/' + libId + '/stats', H).then((r) => (r.ok ? r.json() : null)),
      fetch('/api/libraries/' + libId + '/series?limit=1', H).then((r) => (r.ok ? r.json() : null)),
    ]).then((res) => {
      nhLsG.stats[libId] = res[0] || null;
      nhLsG.series[libId] = (res[1] && typeof res[1].total === 'number') ? res[1].total : 0;
      nhLsG.fetching[libId] = false;
      nhLs.sig = '';
    }).catch(() => { nhLsG.stats[libId] = null; nhLsG.fetching[libId] = false; });
  }
  function nhLsGlance() {
    const libs = (nhLs.libs || []).filter((l) => l.mediaType === 'book' && (nhLs.libSel === 'all' || l.id === nhLs.libSel));
    if (!libs.length) return null;
    libs.forEach((l) => nhLsGlanceFetch(l.id));
    const have = libs.filter((l) => nhLsG.stats[l.id]);
    if (!have.length) return null;
    const out = { items: 0, authors: 0, series: 0, genres: 0, duration: 0, size: 0, longest: [] };
    const genreSet = {};
    have.forEach((l) => {
      const s = nhLsG.stats[l.id];
      out.items += s.totalItems || 0;
      out.authors += s.totalAuthors || 0;
      out.series += nhLsG.series[l.id] || 0;
      out.duration += s.totalDuration || 0;
      out.size += s.totalSize || 0;
      (s.genresWithCount || []).forEach((g) => { genreSet[g.genre] = 1; });
      (s.longestItems || []).forEach((b) => out.longest.push(b));
    });
    out.genres = Object.keys(genreSet).length;
    out.longest.sort((a, b) => (b.duration || 0) - (a.duration || 0));
    out.longest = out.longest.slice(0, 8);
    return out;
  }
  function nhLsFmtSize(bytes) {
    const gb = bytes / 1073741824;
    if (gb >= 100) return Math.round(gb) + ' GB';
    if (gb >= 1) return (Math.round(gb * 10) / 10) + ' GB';
    return Math.round(bytes / 1048576) + ' MB';
  }

  function nhLibraryStats() {
    // The sidebar's "Library Stats" goes to /library/<id>/stats, that is the
    // page this replaces for admins. (/config/library-stats is NOT a route in
    // ABS; round 9 mounted only there, which is why Pawel never saw the page.
    // The config match stays for builds that might grow one.)
    const onCfg = /\/config\/library-stats\/?$/.test(location.pathname);
    const onLib = /\/library\/[^/]+\/stats\/?$/.test(location.pathname);
    const on = (onCfg || onLib) && isUserAdmin();
    const old = document.getElementById('nh-libstats');
    if (!on) {
      if (document.body.classList.contains('nh-libstats')) {
        document.body.classList.remove('nh-libstats');
        document.body.classList.remove('nh-libstats-cfg');
        // give the native page back exactly what we hid
        document.querySelectorAll('[data-nh-ls-hid]').forEach((c) => { c.style.display = c.dataset.nhLsHid; delete c.dataset.nhLsHid; });
        if (old) old.remove();
        nhLs.sig = '';
      }
      return;
    }
    // On the library route the stats page's own scroller is (confusingly) named
    // #bookshelf, a plain div here, NOT the virtual shelf. Mounting inside it
    // inherits the toolbar clearance and native scrolling; its siblings (the
    // library nav strip) stay untouched.
    const container = onLib
      ? document.querySelector('#app-content .page > #bookshelf')
      : (document.querySelector('#page-wrapper .page') || document.getElementById('page-wrapper'));
    if (!container) return;
    document.body.classList.add('nh-libstats');
    document.body.classList.toggle('nh-libstats-cfg', onCfg);
    // Anything the /config/stats dashboard left behind is removed, EXCEPT the
    // ranking board, which nhStatsScoreboard now legitimately mounts at the
    // bottom of this page (round 14). The grid may still hold the Vue-owned
    // .configContent, hand it back BEFORE removing (the reuse trap).
    document.querySelectorAll('#nh-stats-grid').forEach((g) => {
      const cc = g.querySelector('.configContent');
      if (cc && g.parentElement) g.parentElement.insertBefore(cc, g);
      g.remove();
    });
    document.querySelectorAll('#nh-wk, #nh-ys').forEach((el) => { el.remove(); });
    document.querySelectorAll('#nh-libstats').forEach((el) => { if (el !== old) el.remove(); });
    // Hide the native page IN PLACE, nothing is moved, so Vue's reuse of these
    // nodes across config routes (the trap the stats grid hit) cannot bite.
    Array.prototype.forEach.call(container.children, (c) => {
      if (c.id !== 'nh-libstats' && !c.dataset.nhLsHid) { c.dataset.nhLsHid = c.style.display || ''; c.style.display = 'none'; }
    });

    const tok = nhSrToken();
    if (!nhLs.libs && tok) {
      nhLs.libs = [];
      fetch('/api/libraries', { headers: { Authorization: 'Bearer ' + tok } })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => { nhLs.libs = (j && j.libraries) || []; nhLs.sig = ''; }).catch(() => {});
    }
    if (!nhLs.users && !nhLs.fetching && tok && nhLs.tries < 5) {
      nhLs.fetching = true; nhLs.tries++;
      Promise.all([
        fetch('/api/users', { headers: { Authorization: 'Bearer ' + tok } }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
        fetch('/_nh/api/ratings', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
      ]).then((res) => {
        nhLs.ratings = res[1] || { items: {} };
        const us = (res[0] && (res[0].users || res[0])) || [];
        nhLs.users = us.filter((u) => u.isActive !== false).map((u) => ({ id: u.id, username: u.username }));
        nhLs.fetching = false;
        nhLs.users.forEach((u) => {
          fetch('/api/users/' + u.id + '/listening-stats', { headers: { Authorization: 'Bearer ' + tok } })
            .then((r) => (r.ok ? r.json() : null)).then((st) => { if (st) { nhLs.stats[u.id] = st; nhLs.sig = ''; } }).catch(() => {});
        });
      }).catch(() => { nhLs.fetching = false; });
    }
    if (!nhLs.users) return;
    // Every library is scanned once for live genres (#31); the selected one also
    // serves as the item filter.
    (nhLs.libs || []).forEach((l) => nhLsFetchLibItems(l.id));
    if (nhLs.libSel !== 'all' && !nhLs.libItems[nhLs.libSel]) { nhLsFetchLibItems(nhLs.libSel); }

    const T = nhGsT();
    const t = (k) => T[k] || PANEL_T.en[k];
    const agg = nhLsAgg();
    const best = nhLsBestRated(nhLs.libSel !== 'all' ? nhLs.libItems[nhLs.libSel] : null);
    const loaded = nhLs.users.filter((u) => nhLs.stats[u.id]).length;
    const sig = [nhLs.libSel, loaded, agg.total, agg.played, best.map((b) => b.id + b.avg).join(','), (nhLs.libs || []).length].join('|');
    // The guard must read nhLs.sig, async arrivals (best-rated TITLES, library
    // filter maps) blank it to force a redraw, and the computed signature alone
    // does not change when they land (r10 probe: a column of "…" that never
    // resolved).
    let el = old;
    if (el && nhLs.sig === sig && el.parentElement === container) return;
    if (!el) { el = document.createElement('div'); el.id = 'nh-libstats'; container.appendChild(el); }
    nhLs.sig = sig;
    el.textContent = '';

    const head = document.createElement('div'); head.className = 'nh-sb-head';
    const h2 = document.createElement('h2'); h2.className = 'nh-sb-title'; h2.textContent = t('lsTitle');
    const sel = document.createElement('select'); sel.className = 'nh-ls-sel';
    const optAll = document.createElement('option'); optAll.value = 'all'; optAll.textContent = t('lsAll');
    sel.appendChild(optAll);
    (nhLs.libs || []).forEach((l) => {
      const o = document.createElement('option'); o.value = l.id; o.textContent = l.name;
      if (nhLs.libSel === l.id) o.selected = true;
      sel.appendChild(o);
    });
    sel.addEventListener('change', () => { nhLs.libSel = sel.value; nhLs.sig = ''; el.dataset.sig = ''; });
    head.appendChild(h2); head.appendChild(sel);
    nhSelectify(sel, true);
    el.appendChild(head);

    const tiles = document.createElement('div'); tiles.className = 'nh-sb-tiles';
    const mkTile = (val, lab, sub) => {
      const x = document.createElement('div'); x.className = 'nh-sb-tile';
      const v = document.createElement('div'); v.className = 'nh-sb-tval'; v.textContent = val; x.appendChild(v);
      const s2 = document.createElement('div'); s2.className = 'nh-sb-tsub'; s2.textContent = sub || ' '; x.appendChild(s2);
      const l = document.createElement('div'); l.className = 'nh-sb-tlab'; l.textContent = lab; x.appendChild(l);
      return x;
    };
    tiles.appendChild(mkTile(nhSbFmt(agg.total), t('sbTotal'), nhSbSub(agg.total)));
    tiles.appendChild(mkTile(String(agg.listeners), t('sbListeners'), ''));
    tiles.appendChild(mkTile(String(agg.played), t('lsPlayed'), ''));
    el.appendChild(tiles);

    // Library at a glance (#33): the plain numbers ABS's own stats page had
    // (our page replaced it). Per library from /api/libraries/:id/stats, or
    // summed over every book library for "all".
    const gl = nhLsGlance();
    if (gl) {
      const gh = document.createElement('div'); gh.className = 'nh-ys-h'; gh.style.margin = '14px 0 8px'; gh.textContent = t('lsGlance');
      el.appendChild(gh);
      const gt = document.createElement('div'); gt.className = 'nh-sb-tiles';
      gt.appendChild(mkTile(String(gl.items), t('lsBooks'), ''));
      gt.appendChild(mkTile(String(gl.authors), t('lsAuthorsN'), ''));
      gt.appendChild(mkTile(String(gl.series), t('lsSeriesN'), ''));
      gt.appendChild(mkTile(String(gl.genres), t('lsGenresN'), ''));
      gt.appendChild(mkTile(nhSbFmt(gl.duration), t('lsDuration'), nhSbSub(gl.duration)));
      gt.appendChild(mkTile(nhLsFmtSize(gl.size), t('lsSize'), ''));
      el.appendChild(gt);
    }

    const wrap = document.createElement('div'); wrap.className = 'nh-ys-grid nh-ls-grid';
    const tk = window.__NH_TOKEN || getTokenNH();
    const tq = tk ? '&token=' + encodeURIComponent(tk) : '';
    if (agg.books.length) {
      wrap.appendChild(nhYsList(t('ysTopBooks'),
        agg.books.map((b) => ({ name: b.title || nhLsTitle(b.id) || '…', secs: b.secs, id: b.id })),
        (r) => '/api/items/' + r.id + '/cover?width=80' + tq));
    }
    if (best.length) {
      const card = document.createElement('div'); card.className = 'nh-ys-card';
      const bh = document.createElement('div'); bh.className = 'nh-ys-h'; bh.textContent = t('lsBest'); card.appendChild(bh);
      best.forEach((b) => {
        const rowEl = document.createElement('div'); rowEl.className = 'nh-ys-row';
        const th = document.createElement('span'); th.className = 'nh-ys-thumb';
        th.style.backgroundImage = 'url("/api/items/' + b.id + '/cover?width=80' + tq + '")';
        const mid = document.createElement('span'); mid.className = 'nh-ys-mid';
        const nm = document.createElement('span'); nm.className = 'nh-ys-name';
        nm.textContent = nhLsTitle(b.id) || '…';
        mid.appendChild(nm);
        mid.appendChild(nhCrStarsEl({ avg: b.avg, n: b.n }, '', true));
        rowEl.appendChild(th); rowEl.appendChild(mid);
        card.appendChild(rowEl);
      });
      wrap.appendChild(card);
    }
    if (agg.genres.length) wrap.appendChild(nhYsList(t('lsGenres') + ' (' + t('lsByTime') + ')', agg.genres));
    if (gl && gl.longest.length) wrap.appendChild(nhYsList(t('lsLongest'), gl.longest.map((b) => ({ name: b.title, secs: b.duration, id: b.id })), (r) => r.id ? '/api/items/' + r.id + '/cover?width=80' + tq : null));
    if (agg.authors.length) wrap.appendChild(nhYsList(t('ysTopAuthors'), agg.authors));
    if (!agg.books.length && !best.length) {
      const none = document.createElement('p'); none.className = 'nh-sb-empty'; none.textContent = t('yirNone');
      wrap.appendChild(none);
    }
    el.appendChild(wrap);
  }

  // The settings sidebar entry that opens the page above still said "Library
  // Stats" (Pawel: "in settings I still see Library Stats"). For admins the
  // link opens Server Statistics, so the label says so. Hide-and-sibling, a
  // textContent write on a Vue-bound node orphans its text from the vdom.
  function nhLsNavLabel() {
    const links = document.querySelectorAll('div.w-44 a');
    if (!links.length) return;
    const admin = isUserAdmin();
    const T = nhGsT();
    const txt = T.lsTitle || PANEL_T.en.lsTitle;
    links.forEach((a) => {
      const href = a.getAttribute('href') || '';
      if (!/\/library\/[^/]+\/stats\/?$/.test(href)) return;
      const native = a.querySelector('p:not(.nh-ls-navlbl)');
      let ours = a.querySelector('.nh-ls-navlbl');
      if (!admin) {
        if (ours) ours.remove();
        if (native && native.style.display === 'none') native.style.display = '';
        return;
      }
      if (!native) return; // Vue mid-patch; next tick
      if (native.style.display !== 'none') native.style.display = 'none';
      if (!ours) {
        ours = document.createElement('p');
        ours.className = (native.className ? native.className + ' ' : '') + 'nh-ls-navlbl';
        native.parentNode.insertBefore(ours, native.nextSibling);
      }
      if (ours.textContent !== txt) ours.textContent = txt;
    });
  }

  // ===================== ADMIN BADGE: OPEN PROBLEM REPORTS ====================
  // A count on the appbar's account button so an admin learns about new reports
  // without opening the settings panel. Read-only, the queue itself lives in
  // the panel's Administration card. Refreshed every 5 minutes; the panel's
  // list/resolve handlers push fresher counts into nhRb when they touch the API.
  const nhRb = { count: null, ts: 0, fetching: false };
  const NH_RB_EVERY = 2 * 60 * 1000; // Pawel: badge + queue refresh every 2 minutes

  function nhReportsBadge() {
    const link = document.querySelector('#appbar a[href$="/account"]');
    if (!link) return;
    let b = link.querySelector('.nh-rp-badge');
    if (!isUserAdmin()) { if (b) b.remove(); return; }
    const tok = nhSrToken();
    if (tok && !nhRb.fetching && (nhRb.count === null || Date.now() - nhRb.ts > NH_RB_EVERY)) {
      nhRb.fetching = true;
      fetch('/_nh/api/reports-admin', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => { nhRb.count = j && Array.isArray(j.reports) ? j.reports.length : 0; })
        .catch(() => { if (nhRb.count === null) nhRb.count = 0; })
        .then(() => { nhRb.ts = Date.now(); nhRb.fetching = false; });
    }
    if (!nhRb.count) { if (b) b.remove(); return; }
    const T = nhGsT();
    const shown = nhRb.count > 9 ? '9+' : String(nhRb.count);
    const label = nhRb.count + ' · ' + (T.rpBadge || PANEL_T.en.rpBadge);
    if (!b) { b = document.createElement('span'); b.className = 'nh-rp-badge'; link.appendChild(b); }
    if (b.textContent !== shown) b.textContent = shown;
    if (b.title !== label) b.title = label;
  }

  // The queue itself, at the bottom of the ACCOUNT page (Pawel: the badge must
  // lead somewhere reviewable, and admin plumbing does not belong in the
  // customization panel). The badge sits on the account button, so its click
  // already lands here. Vue owns the column, presence is re-checked per tick.
  const NH_ACC_RP_EVERY = 2 * 60 * 1000;
  function nhAccountReports() {
    const on = /\/account\/?$/.test(location.pathname) && isUserAdmin();
    const old = document.getElementById('nh-acc-reports');
    if (!on) { if (old) old.remove(); return; }
    // ABS 2.36.0 renamed this column max-w-xl -> max-w-2xl (it grew an auth
    // sessions table). Match either, and only then fall back to the first child
    //, the fallback works but is blind to any wrapper ABS adds later.
    const col = document.querySelector('#page-wrapper .max-w-xl, #page-wrapper .max-w-2xl') || document.querySelector('#page-wrapper > div');
    if (!col) return;
    if (old && old.parentElement === col) {
      // keep the queue fresh while the page stays open (Pawel: 2 minutes)
      if (Date.now() - (+old.dataset.ts || 0) > NH_ACC_RP_EVERY) {
        old.dataset.ts = String(Date.now());
        nhReportsAdminRender(old.querySelector('.nh-rp-list'));
      }
      return;
    }
    if (old) old.remove();
    const T = nhGsT();
    const card = document.createElement('div');
    card.id = 'nh-acc-reports';
    card.dataset.ts = String(Date.now());
    const h = document.createElement('h2'); h.className = 'nh-acc-rp-t'; h.textContent = T.rpCard || PANEL_T.en.rpCard;
    const hint = document.createElement('p'); hint.className = 'nh-acc-rp-hint'; hint.textContent = T.rpHint || PANEL_T.en.rpHint;
    const list = document.createElement('div'); list.className = 'nh-rp-list';
    card.appendChild(h); card.appendChild(hint); card.appendChild(list);
    // at the TOP of the account column (Pawel), right under the page heading
    const h1 = col.querySelector(':scope > h1');
    if (h1 && h1.nextSibling) col.insertBefore(card, h1.nextSibling);
    else col.insertBefore(card, col.firstChild);
    nhReportsAdminRender(list);
  }

  function nhHeatmapSkin() {
    const hm = document.getElementById('heatmap');
    if (!hm) return;
    // data-nh-heat is the guard, not the colour: once repainted a cell no longer
    // matches the ramp, and Vue re-rendering one hands back a fresh untagged node.
    const cells = hm.querySelectorAll('div:not([data-nh-heat])');
    if (!cells.length) return;
    const rgb = nhAccentRgb();
    cells.forEach((c) => {
      const bg = getComputedStyle(c).backgroundColor;
      const step = NH_HEAT_RAMP[bg];
      if (step === undefined) return;
      c.dataset.nhHeat = '1';
      c.style.setProperty('background-color', typeof step === 'number' ? 'rgba(' + rgb + ',' + step + ')' : step, 'important');
      c.style.setProperty('outline', 'none', 'important');
    });
  }

  function nhStatsScoreboard() {
    if (nhSettings.statsRanking === false) return;
    const onCfg = /\/config\/stats\/?$/.test(location.pathname);
    // The board ALSO renders under the Server statistics content (Pawel:
    // "put Server ranking under server statistics, so it's visible there too").
    // Mount target there is #nh-libstats itself, it only exists for admins.
    const lsHost = /\/library\/[^/]+\/stats\/?$/.test(location.pathname) ? document.getElementById('nh-libstats') : null;
    if (!onCfg && !lsHost) {
      if (document.body.classList.contains('nh-stats-dash') || document.getElementById('nh-scoreboard')) {
        document.body.classList.remove('nh-stats-dash');
        const g = document.getElementById('nh-stats-grid');
        if (g) {
          // CRITICAL: Vue reuses ONE .configContent element across ALL config
          // pages (class flips to page-Users etc. in place). It's still inside
          // our grid, hand it back to its original parent BEFORE removing the
          // grid, or the next config page's content is destroyed with it.
          const cc = g.querySelector('.configContent');
          if (cc && g.parentElement) g.parentElement.insertBefore(cc, g);
          g.remove();
        }
        const c = document.getElementById('nh-scoreboard'); if (c) c.remove();
        nhSb.renderedSig = '';
      }
      return;
    }
    let admin = false;
    try { admin = !!(window.$nuxt.$store.getters['user/getIsAdminOrUp']); } catch (e) {}
    nhSb.mode = admin ? 'admin' : 'shared';
    let wrap;
    if (onCfg) {
      const container = document.querySelector('#page-wrapper .page.overflow-y-auto') || document.querySelector('#page-wrapper .page') || document.querySelector('#page-wrapper');
      if (!container) return;
      document.body.classList.add('nh-stats-dash');
      // Lay "Your Stats" (native .configContent) and our ranking side by side in
      // a responsive grid, Your Stats first (top on narrow / left on wide). The
      // card is Vue's; moving it is safe here because the stats page renders
      // once and config navigation destroys #page-wrapper wholesale.
      wrap = document.getElementById('nh-stats-grid');
      if (!wrap) { wrap = document.createElement('div'); wrap.id = 'nh-stats-grid'; }
      if (wrap.parentElement !== container) container.insertBefore(wrap, container.firstChild);
      const yourStats = container.querySelector(':scope > .configContent') || document.querySelector('#page-wrapper .configContent');
      if (yourStats && yourStats.parentElement !== wrap) wrap.appendChild(yourStats);
    } else {
      // Server statistics page: the board goes at the BOTTOM of #nh-libstats.
      // No grid, no configContent adoption, no body class (that class carries
      // the config-page padding).
      wrap = lsHost;
    }
    nhScFetch(); // loads the avatars map (shared series-meta payload)
    const tok = nhSrToken();
    if (!nhSb.users && !nhSb.fetching && tok && nhSb.tries < 5) {
      nhSb.fetching = true; nhSb.tries++;
      if (admin) {
        // Admins read the real thing for the NUMBERS (every user's sessions, no
        // opt-in needed) but still honour the opt-OUT: "Share my listening stats"
        // off means gone from the board and out of the totals, for everyone
        // including admins (Pawel). Absence from the shared store cannot mean
        // that, a user who has simply not opened the app since is absent too,         // so the filter is the explicit tombstone the DELETE leaves behind.
        Promise.all([
          fetch('/api/users', { headers: { Authorization: 'Bearer ' + tok } }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
          fetch('/_nh/api/stats', { headers: { Authorization: 'Bearer ' + tok } }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
        ]).then((res) => {
          const j = res[0];
          const shared = (res[1] && res[1].users) || {};
          const out = {};
          Object.keys(shared).forEach((id) => { if (shared[id] && shared[id].out) out[id] = 1; });
          const us = (j && (j.users || j)) || [];
          // Admins see EVERYONE (Pawel); an opt-out only earns the row a marker
          // instead of removing it from the board.
          nhSb.users = us.filter((u) => u.isActive !== false).map((u) => ({ id: u.id, username: u.username, type: u.type, opted: !!out[u.id] }));
          nhSb.fetching = false;
          nhSb.users.forEach((u) => {
            fetch('/api/users/' + u.id + '/listening-stats', { headers: { Authorization: 'Bearer ' + tok } })
              .then((r) => (r.ok ? r.json() : null)).then((st) => { if (st) nhSb.stats[u.id] = st; }).catch(() => {});
          });
        }).catch(() => { nhSb.fetching = false; });
      } else {
        // Everyone else sees the OPT-IN shared store (A5), each participant's own
        // browser posts their summary there. Normalised into the same shape the
        // admin path produces, so the board below is one code path.
        fetch('/_nh/api/stats', { headers: { Authorization: 'Bearer ' + tok } })
          .then((r) => (r.ok ? r.json() : null))
          .then((j) => {
            nhSb.fetching = false;
            const recs = (j && j.users) || {};
            // Tombstones (opted out) carry no figures, skip them outright.
            nhSb.users = Object.keys(recs).filter((id) => recs[id] && !recs[id].out).map((id) => ({ id: id, username: recs[id].user || '?' }));
            nhSb.users.forEach((u) => {
              const rec = recs[u.id];
              const items = {};
              (rec.books || []).forEach((b, n) => { items['b' + n] = { timeListening: b.s, mediaMetadata: { title: b.t } }; });
              nhSb.stats[u.id] = { totalTime: rec.total || 0, days: rec.days || {}, items: items };
            });
          })
          .catch(() => { nhSb.fetching = false; nhSb.users = []; });
      }
    }
    if (!nhSb.users) return;

    const rows = nhSb.users.map((u) => ({ u: u, secs: nhSbPeriodTotal(nhSb.stats[u.id], nhSb.period) })).sort((a, b) => b.secs - a.secs);
    const loaded = nhSb.users.filter((u) => nhSb.stats[u.id]).length;
    const avSig = nhSc.avatars ? Object.keys(nhSc.avatars).sort().join('.') + ':' + (nhSc.ts || 0) : 'x';
    const sig = nhSb.mode + '|' + nhSb.period + '|' + loaded + '|' + avSig + '|' + rows.map((r) => r.u.id + ':' + r.secs + (r.u.opted ? '!' : '')).join(',') + '|' + (nhSettings.familyStats ? 1 : 0);
    let card = document.getElementById('nh-scoreboard');
    // Belt and braces (Pawel saw a doubled ranking on staging): if any stray
    // copy of the board exists, an interrupted cleanup, a re-parented grid,     // keep only the one getElementById returned and drop the rest.
    document.querySelectorAll('#nh-scoreboard').forEach((el) => { if (el !== card) el.remove(); });
    if (card && card.parentElement === wrap && nhSb.renderedSig === sig) return;
    nhSb.renderedSig = sig;
    const T = nhGsT();
    if (!card) { card = document.createElement('div'); card.id = 'nh-scoreboard'; }
    if (card.parentElement !== wrap) wrap.appendChild(card);
    card.textContent = '';

    const head = document.createElement('div'); head.className = 'nh-sb-head';
    const h2 = document.createElement('h2'); h2.className = 'nh-sb-title';
    // One name in every language and every mode (Pawel): the shared-mode board
    // called itself "Family listening", which read as a different feature.
    h2.textContent = T.sbTitle || PANEL_T.en.sbTitle;
    const headRight = document.createElement('div'); headRight.className = 'nh-sb-headright';
    // Year in Review is the caller's OWN year, so it needs no shared data and is
    // offered to every user, admin or not.
    const yir = document.createElement('button');
    yir.type = 'button'; yir.className = 'nh-sb-yir';
    yir.textContent = T.yirBtn || PANEL_T.en.yirBtn;
    yir.addEventListener('click', () => nhYearInReview());
    if (rows.length) {
      const toggle = document.createElement('div'); toggle.className = 'nh-sb-toggle';
      [['week', T.sbWeek || PANEL_T.en.sbWeek], ['month', T.sbMonth || PANEL_T.en.sbMonth], ['year', T.sbYear || PANEL_T.en.sbYear], ['all', T.sbAll || PANEL_T.en.sbAll]].forEach((o) => {
        const btn = document.createElement('button'); btn.type = 'button'; btn.className = 'nh-sb-pill' + (nhSb.period === o[0] ? ' nh-on' : ''); btn.textContent = o[1];
        btn.addEventListener('click', () => { nhSb.period = o[0]; nhSb.renderedSig = ''; nhStatsScoreboard(); });
        toggle.appendChild(btn);
      });
      headRight.appendChild(toggle);
    }
    headRight.appendChild(yir);
    head.appendChild(h2); head.appendChild(headRight); card.appendChild(head);

    // Shared board with nobody in it yet: explain how to join instead of showing
    // a wall of zeroes.
    if (nhSb.mode !== 'admin' && !rows.length) {
      const empty = document.createElement('div'); empty.className = 'nh-sb-empty';
      empty.textContent = nhSettings.familyStats
        ? (T.fsWaiting || PANEL_T.en.fsWaiting)
        : (T.fsJoin || PANEL_T.en.fsJoin);
      card.appendChild(empty);
      return;
    }

    // Summary tiles (period-aware): server total, active listeners, average each.
    const totalSecs = rows.reduce((s, r) => s + r.secs, 0);
    const active = rows.filter((r) => r.secs > 0).length;
    const avg = active ? totalSecs / active : 0;
    const tiles = document.createElement('div'); tiles.className = 'nh-sb-tiles';
    const mkTile = (val, lab, sub) => {
      const t = document.createElement('div'); t.className = 'nh-sb-tile';
      const v = document.createElement('div'); v.className = 'nh-sb-tval'; v.textContent = val;
      t.appendChild(v);
      // always present, even when empty, so every tile's label sits on one line
      const s = document.createElement('div'); s.className = 'nh-sb-tsub'; s.textContent = sub || ' '; t.appendChild(s);
      const l = document.createElement('div'); l.className = 'nh-sb-tlab'; l.textContent = lab;
      t.appendChild(l); return t;
    };
    tiles.appendChild(mkTile(nhSbFmt(totalSecs), T.sbTotal || PANEL_T.en.sbTotal, nhSbSub(totalSecs)));
    tiles.appendChild(mkTile(String(active), T.sbListeners || PANEL_T.en.sbListeners, ''));
    tiles.appendChild(mkTile(nhSbFmt(avg), T.sbAvg || PANEL_T.en.sbAvg, nhSbSub(avg)));
    card.appendChild(tiles);

    // Podium, top 3 (arranged 2 · 1 · 3; #1 gold and tallest). Click → drill-down.
    const top = rows.slice(0, 3);
    if (top.length) {
      const podium = document.createElement('div'); podium.className = 'nh-sb-podium';
      // A family board can have one or two participants; keep the podium centred
      // and sized to what it holds instead of leaving two empty thirds.
      if (top.length === 1) podium.style.gridTemplateColumns = 'minmax(200px, 340px)';
      else if (top.length === 2) podium.style.gridTemplateColumns = 'repeat(2, minmax(180px, 300px))';
      const order = top.length >= 3 ? [1, 0, 2] : (top.length === 2 ? [1, 0] : [0]);
      order.forEach((idx) => {
        const r = top[idx]; if (!r) return;
        const pod = document.createElement('button'); pod.type = 'button'; pod.className = 'nh-sb-pod p' + (idx + 1);
        pod.addEventListener('click', () => nhStatsUserModal(r.u));
        // The rank used to be an emoji medal, which every OS draws in its own
        // style, on Windows a blue-ribboned sticker that belonged to no part of
        // this theme. It is now a numbered chip pinned to the portrait, coloured
        // by place, so the podium is ours end to end.
        const av = document.createElement('div'); av.className = 'nh-sb-pod-av'; av.textContent = (r.u.username || '?').charAt(0).toUpperCase();
        nhAvatarInto(av, r.u.id);
        const avw = document.createElement('div'); avw.className = 'nh-sb-pod-avw';
        avw.appendChild(av);
        const badge = document.createElement('span'); badge.className = 'nh-sb-pod-rank'; badge.textContent = String(idx + 1);
        avw.appendChild(badge);
        const nm = document.createElement('div'); nm.className = 'nh-sb-pod-name'; nm.textContent = r.u.username;
        if (r.u.opted) nm.appendChild(nhSbOptedIcon());
        const tm = document.createElement('div'); tm.className = 'nh-sb-pod-time'; tm.textContent = nhSbFmt(r.secs);
        pod.appendChild(avw); pod.appendChild(nm); pod.appendChild(tm);
        const s = document.createElement('div'); s.className = 'nh-sb-pod-sub'; s.textContent = nhSbSub(r.secs) || ' '; pod.appendChild(s);
        podium.appendChild(pod);
      });
      card.appendChild(podium);
    }

    // The rest (rank 4+) as TILES rather than a bar list, a column of bars
    // scaled to an arbitrary maximum said little and read as filler; a photo,
    // a name and a time per tile is the same information, legible at a glance.
    const rest = rows.slice(3);
    if (rest.length) {
      const grid = document.createElement('div'); grid.className = 'nh-sb-grid';
      rest.forEach((r, i) => {
        const cardEl = document.createElement('button'); cardEl.type = 'button'; cardEl.className = 'nh-sb-card' + (r.secs > 0 ? '' : ' nh-sb-idle');
        cardEl.addEventListener('click', () => nhStatsUserModal(r.u));
        const rank = document.createElement('span'); rank.className = 'nh-sb-crank'; rank.textContent = '#' + (i + 4);
        const av = document.createElement('span'); av.className = 'nh-sb-av nh-sb-cav'; av.textContent = (r.u.username || '?').charAt(0).toUpperCase();
        nhAvatarInto(av, r.u.id);
        const nm = document.createElement('span'); nm.className = 'nh-sb-cname'; nm.textContent = r.u.username;
        if (r.u.opted) nm.appendChild(nhSbOptedIcon());
        const tm = document.createElement('span'); tm.className = 'nh-sb-ctime'; tm.textContent = nhSbFmt(r.secs);
        cardEl.appendChild(rank); cardEl.appendChild(av); cardEl.appendChild(nm); cardEl.appendChild(tm);
        const s = document.createElement('span'); s.className = 'nh-sb-csub'; s.textContent = nhSbSub(r.secs) || ' '; cardEl.appendChild(s);
        grid.appendChild(cardEl);
      });
      card.appendChild(grid);
    }
    if (loaded < nhSb.users.length) { const ld = document.createElement('div'); ld.className = 'nh-sb-loading'; ld.textContent = '…'; card.appendChild(ld); }
  }

  // ===================== A5: SHARED LISTENING SUMMARIES + YEAR IN REVIEW =====================
  // Opt-in. When it is on, this browser posts a SUMMARY of its own
  // /api/me/listening-stats to the proxy's store every few hours; the family
  // board on /config/stats reads that store, which is how non-admins can see a
  // leaderboard without anyone handing out admin rights. Turning it off deletes
  // the record, the flag alone would leave stale data on the server.
  const nhFs = { busy: false, tried: 0 };

  // Membership changed, so the board's cached roster is stale. Clearing users
  // (not just the render signature) is what makes it refetch: the fetch guard is
  // `if (!nhSb.users ...)`, so a signature reset alone would redraw the very same
  // list and the person who just opted out would sit there until a reload.
  // Small marker for an admin's board: this user opted out, others cannot see
  // them, the admin still can (Pawel).
  function nhSbOptedIcon() {
    const T = nhGsT();
    const s = document.createElement('span');
    s.className = 'material-symbols nh-sb-opted';
    s.textContent = 'visibility_off';
    s.title = T.socOpted || PANEL_T.en.socOpted;
    return s;
  }

  function nhSbInvalidate() {
    nhSb.users = null;
    nhSb.stats = {};
    nhSb.tries = 0;
    nhSb.fetching = false;
    nhSb.renderedSig = '';
  }
  const NH_FS_EVERY = 6 * 3600 * 1000;
  const NH_FS_DAYS = 400;

  // Admins fill the shared store for EVERYONE (round 11: a regular user opened
  // the ranking and saw two names, the store only held the browsers that had
  // synced themselves; "on by default" has to mean the board is full without
  // each person logging in). Once per NH_FS_EVERY per admin browser: one
  // summary per active user through the admin-gated twin. Opt-outs are skipped
  // here AND refused server-side, so a tombstone survives an admin sweep.
  const nhFsa = { busy: false, tried: 0 };

  function nhFsAdminSummary(st) {
    const days = {};
    const src = st.days || {};
    Object.keys(src).sort().slice(-NH_FS_DAYS).forEach((k) => { if (src[k] > 0) days[k] = Math.round(src[k]); });
    const items = st.items || {};
    const books = Object.keys(items)
      .map((k) => { const it = items[k] || {}; return { t: (it.mediaMetadata && it.mediaMetadata.title) || '', s: it.timeListening || 0 }; })
      .filter((b) => b.t && b.s > 0)
      .sort((a, b) => b.s - a.s).slice(0, 8)
      .map((b) => ({ t: b.t, s: Math.round(b.s) }));
    return { total: Math.round(st.totalTime || 0), days: days, books: books };
  }

  function nhFsAdminSeed() {
    if (!isUserAdmin()) return;
    // #25: gated on the SERVER's social flags, not on the admin's personal
    // toggle, this browser publishes other people's summaries.
    const socA = nhSocEff();
    if (!socA || !socA.time) return;
    const tok = nhSrToken();
    if (!tok || nhFsa.busy || nhFsa.tried > 2) return;
    let last = 0;
    try { last = parseInt(localStorage.getItem('nh-fsa-ts'), 10) || 0; } catch (e) {}
    if (Date.now() - last < NH_FS_EVERY) return;
    nhFsa.busy = true; nhFsa.tried++;
    const H = { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/json' };
    Promise.all([
      fetch('/api/users', { headers: H }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
      fetch('/_nh/api/stats', { headers: H, credentials: 'include' }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
    ]).then(async (res) => {
      const us = ((res[0] && (res[0].users || res[0])) || []).filter((u) => u && u.isActive !== false);
      if (!us.length) { nhFsa.busy = false; return; } // /api/users failed, not an admin session after all
      const shared = (res[1] && res[1].users) || {};
      for (const u of us) {
        if (shared[u.id] && shared[u.id].out) continue;
        try {
          const st = await fetch('/api/users/' + u.id + '/listening-stats', { headers: H }).then((r) => (r.ok ? r.json() : null));
          if (!st) continue;
          const sum = nhFsAdminSummary(st);
          // The seeder NEVER sends titles: it cannot know another user's
          // shareReading consent, so content only ever arrives from the user's
          // own browser (and njs drops titles on the forUser path anyway).
          sum.books = [];
          sum.user = u.username;
          await fetch('/_nh/api/stats-admin?forUser=' + encodeURIComponent(u.id), { method: 'POST', headers: H, credentials: 'include', body: JSON.stringify(sum) });
        } catch (e) {}
      }
      try { localStorage.setItem('nh-fsa-ts', String(Date.now())); } catch (e) {}
      nhFsa.busy = false;
    }).catch(() => { nhFsa.busy = false; });
  }

  function nhFamilyStatsSync() {
    const tok = nhSrToken();
    if (!tok) return;
    const on = nhSettings.familyStats === true;
    if (!on) {
      // #25: unconditional once per load, same reasoning as the progress sync,       // the joined-flag guard left never-opted-in users without any tombstone.
      if (!nhFs.cleared && !nhFs.busy) {
        nhFs.busy = true;
        fetch('/_nh/api/stats', { method: 'DELETE', headers: { Authorization: 'Bearer ' + tok } })
          .then((r) => {
            if (r && r.ok) nhFs.cleared = true;
            try { localStorage.removeItem('nh-fs-on'); localStorage.removeItem('nh-fs-ts'); } catch (e) {}
            nhSbInvalidate(); // drop off the board now, not on the next reload
          })
          .catch(() => {})
          .then(() => { nhFs.busy = false; });
      }
      return;
    }
    nhFs.cleared = false;
    const soc = nhSocEff();
    // Same as the progress sync: the user's own post always goes out so a stale
    // tombstone can heal; the server serves nothing while its flag is off.
    if (!soc) return; // still resolving
    if (nhFs.busy || nhFs.tried > 3) return;
    let last = 0;
    try { last = parseInt(localStorage.getItem('nh-fs-ts'), 10) || 0; } catch (e) {}
    if (Date.now() - last < NH_FS_EVERY) return;
    nhFs.busy = true; nhFs.tried++;
    fetch('/api/me/listening-stats', { headers: { Authorization: 'Bearer ' + tok } })
      .then((r) => (r.ok ? r.json() : null))
      .then((st) => {
        if (!st) throw new Error('no stats');
        const days = {};
        const src = st.days || {};
        Object.keys(src).sort().slice(-NH_FS_DAYS).forEach((k) => { if (src[k] > 0) days[k] = Math.round(src[k]); });
        const items = st.items || {};
        const books = Object.keys(items).map((k) => ({
          t: (items[k].mediaMetadata && items[k].mediaMetadata.title) || '',
          s: Math.round(items[k].timeListening || 0),
        })).filter((b) => b.t && b.s > 0).sort((a, b) => b.s - a.s).slice(0, 10);
        // Titles ride along only with BOTH consents: the user's own shareReading
        // toggle (opt-in, #Pawel) and the server's content flag.
        const withTitles = nhSettings.shareReading === true && soc.content;
        return fetch('/_nh/api/stats', {
          method: 'POST',
          headers: { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/json' },
          body: JSON.stringify({ total: Math.round(st.totalTime || 0), days: days, books: withTitles ? books : [] }),
        });
      })
      .then((r) => {
        if (r && r.ok) {
          try { localStorage.setItem('nh-fs-on', '1'); localStorage.setItem('nh-fs-ts', String(Date.now())); } catch (e) {}
          nhSbInvalidate(); // the board includes us again
        }
      })
      .catch(() => {})
      .then(() => { nhFs.busy = false; });
  }

  // ---- Social switches (#25) ----
  // What this server shares between users, resolved SERVER-SIDE (admin file >
  // NH_SOCIAL env > built-ins) and fetched once per load. null while loading,   // callers treat that as "not yet", never as "on". A 404 means an older proxy
  // without the endpoint: behave as everything-on, which is what that proxy
  // enforces anyway.
  const nhSoc = { eff: null, fetching: false, tries: 0 };
  function nhSocEff() {
    if (!nhSoc.eff && !nhSoc.fetching && nhSoc.tries < 5) {
      const tok = nhSrToken();
      if (tok) {
        nhSoc.fetching = true;
        nhSoc.tries++;
        fetch('/_nh/api/social', { headers: { Authorization: 'Bearer ' + tok }, credentials: 'include' })
          .then((r) => {
            if (r.status === 404 || r.status === 405) return { eff: { time: true, content: true, whoReading: true } };
            return r.ok ? r.json() : null;
          })
          .then((j) => {
            nhSoc.fetching = false;
            if (j && j.eff) nhSoc.eff = j.eff;
          })
          .catch(() => { nhSoc.fetching = false; });
      }
    }
    return nhSoc.eff;
  }

  // ---- Shared book progress (#23): posting side ----
  // Same shape as the A5 stats sync above and gated on the SAME familyStats
  // switch, "share my listening stats" honestly covers which books you are in
  // the middle of. The summary comes from the vuex user (ABS keeps mediaProgress
  // there live), so posting costs one request and no /api/me fetch. Podcast
  // episodes are skipped: the book page is the only consumer.
  const NH_PG_EVERY = 30 * 60 * 1000; // progress moves faster than stats: 30 min, not 6 h
  const nhPg = { busy: false, tried: 0 };
  const nhPga = { busy: false, tried: 0 };

  function nhPgSummary(mediaProgress) {
    const reading = [];
    const done = [];
    (mediaProgress || []).forEach((e) => {
      if (!e || !e.libraryItemId || e.episodeId) return;
      const p = Math.max(Number(e.progress) || 0, Number(e.ebookProgress) || 0);
      const ts = Number(e.finishedAt) || Number(e.lastUpdate) || 0;
      if (e.isFinished) done.push({ i: e.libraryItemId, ts: ts });
      else if (p > 0.001 && p < 1) reading.push({ i: e.libraryItemId, p: Math.round(p * 1000) / 1000, ts: ts });
    });
    reading.sort((a, b) => b.ts - a.ts);
    done.sort((a, b) => b.ts - a.ts);
    return { reading: reading.slice(0, 40), done: done.slice(0, 600) };
  }

  function nhFamilyProgressSync() {
    const tok = nhSrToken();
    if (!tok) return;
    // Governed by the OPT-IN shareReading toggle (Pawel split the consent):
    // which books you read is a different disclosure from how long you listen.
    const on = nhSettings.shareReading === true;
    if (!on) {
      // #25: the tombstone DELETE is UNCONDITIONAL (once per page load), not
      // gated on having joined from this browser. The old joined-flag guard
      // meant a user who never opted in, or who opted out on another device,       // never wrote a tombstone, and the admin seeder then published them.
      // The server no-ops when the tombstone already exists, so this is cheap.
      if (!nhPg.cleared && !nhPg.busy) {
        nhPg.busy = true;
        fetch('/_nh/api/progress', { method: 'DELETE', headers: { Authorization: 'Bearer ' + tok } })
          .then((r) => {
            if (r && r.ok) nhPg.cleared = true;
            try { localStorage.removeItem('nh-pg-on'); localStorage.removeItem('nh-pg-ts'); } catch (e) {}
          })
          .catch(() => {})
          .then(() => { nhPg.busy = false; });
      }
      return;
    }
    nhPg.cleared = false;
    const soc = nhSocEff();
    // Post whenever the USER's toggle is on, even while the server flag is off:
    // the record stays dark until an admin enables the feature, but the post
    // lifts any stale plain tombstone (andrzej case) so consent state is true.
    if (!soc) return; // still resolving
    if (nhPg.busy || nhPg.tried > 3) return;
    let last = 0;
    try { last = parseInt(localStorage.getItem('nh-pg-ts'), 10) || 0; } catch (e) {}
    if (Date.now() - last < NH_PG_EVERY) return;
    let mp = null;
    try { mp = window.$nuxt.$store.state.user.user.mediaProgress; } catch (e) {}
    if (!mp) return;
    nhPg.busy = true; nhPg.tried++;
    fetch('/_nh/api/progress', {
      method: 'POST',
      headers: { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/json' },
      body: JSON.stringify(nhPgSummary(mp)),
    })
      .then((r) => {
        if (r && r.ok) {
          try { localStorage.setItem('nh-pg-on', '1'); localStorage.setItem('nh-pg-ts', String(Date.now())); } catch (e) {}
        }
      })
      .catch(() => {})
      .then(() => { nhPg.busy = false; });
  }

  // Admin seeding twin, the nhFsAdminSeed shape: the book page is complete even
  // for people who never open the web app. Tombstones are respected here and
  // enforced again server-side.
  function nhPgAdminSeed() {
    if (!isUserAdmin()) return;
    // #25: the server switch decides, not the admin's own setting
    const socA = nhSocEff();
    if (!socA || !socA.whoReading) return;
    const tok = nhSrToken();
    if (!tok || nhPga.busy || nhPga.tried > 2) return;
    let last = 0;
    try { last = parseInt(localStorage.getItem('nh-pga-ts'), 10) || 0; } catch (e) {}
    if (Date.now() - last < NH_FS_EVERY) return;
    nhPga.busy = true; nhPga.tried++;
    const H = { Authorization: 'Bearer ' + tok, 'Content-Type': 'application/json' };
    Promise.all([
      fetch('/api/users', { headers: H }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
      fetch('/_nh/api/progress', { headers: H, credentials: 'include' }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
    ]).then(async (res) => {
      const us = ((res[0] && (res[0].users || res[0])) || []).filter((u) => u && u.isActive !== false);
      if (!us.length) { nhPga.busy = false; return; } // /api/users failed, not an admin session after all
      const shared = (res[1] && res[1].users) || {};
      for (const u of us) {
        // shareReading is ON by default now, so absence is not a refusal and
        // absent users are seedable. Tombstones (the explicit "no") still are.
        if (shared[u.id] && shared[u.id].out) continue;
        try {
          const full = await fetch('/api/users/' + u.id, { headers: H }).then((r) => (r.ok ? r.json() : null));
          if (!full || !Array.isArray(full.mediaProgress)) continue;
          const sum = nhPgSummary(full.mediaProgress);
          sum.user = u.username;
          await fetch('/_nh/api/progress-admin?forUser=' + encodeURIComponent(u.id), { method: 'POST', headers: H, credentials: 'include', body: JSON.stringify(sum) });
        } catch (e) {}
      }
      try { localStorage.setItem('nh-pga-ts', String(Date.now())); } catch (e) {}
      nhPga.busy = false;
    }).catch(() => { nhPga.busy = false; });
  }

  // ---- Year in Review: the caller's own last 12 months ----
  function nhYirMonthKey(d) { return d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0'); }

  function nhYearInReview() {
    const T = nhGsT();
    const old = document.getElementById('nh-yir-modal'); if (old) old.remove();
    const overlay = document.createElement('div'); overlay.id = 'nh-yir-modal';
    const bg = document.createElement('div'); bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div'); box.className = 'nh-rt-modal-box';
    const close = () => overlay.remove();
    bg.addEventListener('click', close);
    const head = document.createElement('div'); head.className = 'nh-rt-modal-head';
    const title = document.createElement('span'); title.textContent = T.yirTitle || PANEL_T.en.yirTitle;
    const x = document.createElement('button'); x.type = 'button'; x.className = 'nh-rt-modal-x'; x.textContent = '×';
    x.addEventListener('click', close);
    head.appendChild(title); head.appendChild(x); box.appendChild(head);
    const body = document.createElement('div'); body.className = 'nh-yir-body';
    const loading = document.createElement('div'); loading.className = 'nh-sb-loading'; loading.textContent = '…';
    body.appendChild(loading);
    box.appendChild(body);
    overlay.appendChild(bg); overlay.appendChild(box); document.body.appendChild(overlay);

    const tok = nhSrToken();
    fetch('/api/me/listening-stats', { headers: tok ? { Authorization: 'Bearer ' + tok } : {} })
      .then((r) => (r.ok ? r.json() : null))
      .then((st) => {
        body.textContent = '';
        if (!st) { const e = document.createElement('div'); e.className = 'nh-sb-empty'; e.textContent = T.yirNone || PANEL_T.en.yirNone; body.appendChild(e); return; }
        const days = st.days || {};
        const now = new Date();
        // last 12 months, oldest first
        const months = [];
        for (let i = 11; i >= 0; i--) {
          const d = new Date(now.getFullYear(), now.getMonth() - i, 1);
          months.push({ key: nhYirMonthKey(d), label: new Intl.DateTimeFormat(getUserLanguage(), { month: 'short' }).format(d), secs: 0 });
        }
        const byMonth = {};
        months.forEach((m) => { byMonth[m.key] = m; });
        let yearSecs = 0, activeDays = 0, best = { d: '', s: 0 };
        const cutoff = new Date(now.getFullYear(), now.getMonth() - 11, 1).toISOString().slice(0, 10);
        Object.keys(days).forEach((k) => {
          const s = days[k] || 0;
          if (s <= 0 || k < cutoff) return;
          const m = byMonth[k.slice(0, 7)];
          if (m) { m.secs += s; yearSecs += s; activeDays++; if (s > best.s) best = { d: k, s: s }; }
        });

        const tiles = document.createElement('div'); tiles.className = 'nh-us-tiles';
        const mk = (val, lab, sub) => {
          const t = document.createElement('div'); t.className = 'nh-us-tile';
          const v = document.createElement('div'); v.className = 'nh-us-val'; v.textContent = val; t.appendChild(v);
          const s = document.createElement('div'); s.className = 'nh-us-sub'; s.textContent = sub || ' '; t.appendChild(s);
          const l = document.createElement('div'); l.className = 'nh-us-lab'; l.textContent = lab; t.appendChild(l);
          return t;
        };
        tiles.appendChild(mk(nhSbFmt(yearSecs), T.yirYear || PANEL_T.en.yirYear, nhSbSub(yearSecs)));
        tiles.appendChild(mk(String(activeDays), T.yirDays || PANEL_T.en.yirDays, ''));
        tiles.appendChild(mk(best.s ? nhSbFmt(best.s) : '-', T.yirBest || PANEL_T.en.yirBest, best.d || ''));
        tiles.appendChild(mk(nhSbFmt(st.totalTime || 0), T.sbAll || PANEL_T.en.sbAll, nhSbSub(st.totalTime || 0)));
        body.appendChild(tiles);

        const secTitle = document.createElement('div'); secTitle.className = 'nh-us-sec'; secTitle.textContent = T.yirMonths || PANEL_T.en.yirMonths;
        body.appendChild(secTitle);
        const chart = document.createElement('div'); chart.className = 'nh-yir-chart';
        const max = Math.max.apply(null, months.map((m) => m.secs).concat([1]));
        months.forEach((m) => {
          const col = document.createElement('div'); col.className = 'nh-yir-col';
          const barWrap = document.createElement('div'); barWrap.className = 'nh-yir-barwrap';
          const bar = document.createElement('div'); bar.className = 'nh-yir-bar';
          bar.style.height = Math.max(2, Math.round((m.secs / max) * 100)) + '%';
          bar.title = nhSbFmt(m.secs);
          barWrap.appendChild(bar);
          const val = document.createElement('div'); val.className = 'nh-yir-val'; val.textContent = m.secs ? nhSbFmt(m.secs) : '';
          const lab = document.createElement('div'); lab.className = 'nh-yir-lab'; lab.textContent = m.label;
          col.appendChild(val); col.appendChild(barWrap); col.appendChild(lab);
          chart.appendChild(col);
        });
        body.appendChild(chart);

        const items = st.items || {};
        const top = Object.keys(items).map((k) => items[k])
          .sort((a, b) => (b.timeListening || 0) - (a.timeListening || 0)).slice(0, 6);
        if (top.length) {
          const th = document.createElement('div'); th.className = 'nh-us-sec'; th.textContent = T.sbTopBooks || PANEL_T.en.sbTopBooks;
          body.appendChild(th);
          const ul = document.createElement('div'); ul.className = 'nh-us-books';
          top.forEach((it) => {
            const row = document.createElement('div'); row.className = 'nh-us-book';
            const nm = document.createElement('span'); nm.className = 'nh-us-bt'; nm.textContent = (it.mediaMetadata && it.mediaMetadata.title) || '-';
            const tm = document.createElement('span'); tm.className = 'nh-us-btime'; tm.textContent = nhSbFmt(it.timeListening);
            row.appendChild(nm); row.appendChild(tm); ul.appendChild(row);
          });
          body.appendChild(ul);
        }
      })
      .catch(() => { body.textContent = ''; const e = document.createElement('div'); e.className = 'nh-sb-empty'; e.textContent = T.yirNone || PANEL_T.en.yirNone; body.appendChild(e); });
  }

  // ===================== USERS PAGE: CARD GRID + PROFILE PHOTOS (admin) =====================
  // Settings → Users is a dense table; as the place where photos are managed it
  // should show people, not rows. We keep ABS's table in the DOM (hidden) as the
  // source of truth and re-present it as cards: photo, name, account type, last
  // seen, and how much they have actually listened. Every action clicks the
  // native control underneath, so ABS keeps owning the behaviour.
  // Photos are admin-only (Pawel: users don't set their own).
  const nhUav = { list: null, fetching: false, tries: 0, stats: {}, statTried: {}, sig: '' };

  // One file picker for every entry point (card, table row, edit modal).
  function nhAvatarPick(uid, onDone) {
    const fi = document.createElement('input');
    fi.type = 'file'; fi.accept = 'image/*'; fi.style.display = 'none';
    document.body.appendChild(fi);
    fi.addEventListener('change', () => {
      const f = fi.files && fi.files[0];
      fi.remove();
      if (!f) { onDone(false); return; }
      nhAvatarSave(f, uid, (ok) => onDone(ok));
    });
    fi.click();
  }

  // Give the card grid the width ABS's 900px panel doesn't have, measured, not
  // guessed. The settings sub-rail is position:fixed on top of a full-width
  // wrapper, so a wider box centred in that wrapper hides its own left edge (and
  // the page heading) behind the rail. Anchor to the rail's right edge instead.
  function nhUsersWiden(cc) {
    const parent = cc && cc.parentElement;
    if (!parent) return;
    const pr = parent.getBoundingClientRect();
    if (pr.width < 1200) { nhUsersUnwiden(); return; } // narrow: ABS's own layout is fine
    let railRight = 0;
    document.querySelectorAll('[class*="w-44"], [class*="w-48"]').forEach(function (el) {
      const r = el.getBoundingClientRect();
      // the sub-rail: tall, hugging the left edge of the wrapper
      if (r.height > 200 && r.left <= pr.left + 4 && r.right > railRight) railRight = r.right;
    });
    const left = Math.max(24, Math.round(railRight - pr.left) + 28);
    const width = Math.min(2200, Math.round(pr.width - left - 32));
    if (width < 700) { nhUsersUnwiden(); return; }
    const stamp = left + 'x' + width;
    if (cc.dataset.nhWide === stamp) return; // no layout thrash on every tick
    cc.dataset.nhWide = stamp;
    cc.style.setProperty('margin-left', left + 'px', 'important');
    cc.style.setProperty('margin-right', 'auto', 'important');
    cc.style.setProperty('width', width + 'px', 'important');
    cc.style.setProperty('max-width', 'none', 'important');
  }
  function nhUsersUnwiden() {
    const cc = document.querySelector('.configContent[data-nh-wide]');
    if (!cc) return;
    ['margin-left', 'margin-right', 'width', 'max-width'].forEach((k) => cc.style.removeProperty(k));
    delete cc.dataset.nhWide;
  }

  function nhUsersRows() {
    return Array.from(document.querySelectorAll('.configContent tr.cursor-pointer'));
  }
  function nhUsersRowFor(username) {
    return nhUsersRows().find((tr) => {
      const p = tr.querySelector('td p.truncate');
      return p && p.textContent.trim() === username;
    }) || null;
  }
  // ABS's row actions are icon buttons whose text is the ligature name.
  function nhUsersRowBtn(tr, name) {
    return Array.from(tr.querySelectorAll('button')).find((b) => b.textContent.trim() === name) || null;
  }

  // Listening totals per user (admin endpoint, same shape as /api/me).
  function nhUsersFetchStats(uid, tok) {
    if (!tok || nhUav.statTried[uid]) return;
    nhUav.statTried[uid] = true;
    fetch('/api/users/' + uid + '/listening-stats', { headers: { Authorization: 'Bearer ' + tok } })
      .then((r) => (r.ok ? r.json() : null))
      .then((st) => { if (st) { nhUav.stats[uid] = st; nhUav.sig = ''; } })
      .catch(() => {});
  }

  function nhUsersPage() {
    const onUsers = /\/config\/users\/?$/.test(location.pathname);
    if (!onUsers) {
      // The grid is ours, but .configContent is Vue's and is REUSED across every
      // /config/* page, only ever remove our own node.
      if (document.body.classList.contains('nh-users-grid')) {
        document.body.classList.remove('nh-users-grid');
        const g = document.getElementById('nh-ug-grid'); if (g) g.remove();
        const gb = document.getElementById('nh-ug-bar'); if (gb) gb.remove();
        nhUsersUnwiden();
        nhUav.sig = '';
      }
      return;
    }
    if (!isUserAdmin()) return;
    if (nhSettings.usersCards === false) { nhUsersUnwiden(); return; }
    nhScFetch(); // avatars map
    const tok = nhSrToken();
    if (!nhUav.list && !nhUav.fetching && tok && nhUav.tries < 5) {
      nhUav.fetching = true; nhUav.tries++;
      fetch('/api/users', { headers: { Authorization: 'Bearer ' + tok } })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => {
          nhUav.fetching = false;
          const us = (j && (j.users || j)) || [];
          nhUav.list = {};
          us.forEach((u) => { nhUav.list[u.username] = u.id; });
          nhUav.sig = '';
        })
        .catch(() => { nhUav.fetching = false; });
    }
    // Photos are a bonus: if the avatars endpoint is unavailable the grid still
    // renders, just with initials.
    if (!nhUav.list) return;
    const avatars = nhSc.avatars || {};

    const rows = nhUsersRows();
    if (!rows.length) return;
    const table = document.querySelector('.configContent table');
    if (!table || !table.parentElement) return;

    // Read everything off the native row so ABS keeps owning the formatting
    // (relative dates, account type, current activity) and our card stays a
    // presentation layer.
    const people = rows.map((tr) => {
      const nameP = tr.querySelector('td p.truncate');
      const name = nameP ? nameP.textContent.trim() : '';
      const tds = Array.from(tr.children);
      const cellText = (i) => (tds[i] ? tds[i].textContent.trim().replace(/\s+/g, ' ') : '');
      return {
        name: name,
        uid: nhUav.list[name] || null,
        type: cellText(1),
        activity: cellText(2),
        seen: cellText(3),
        created: cellText(4),
        online: !!tr.querySelector('td .text-success'), // ABS marks a connected user with a text-success dot
      };
    }).filter((p) => p.name);
    if (!people.length) return;

    people.forEach((p) => { if (p.uid) nhUsersFetchStats(p.uid, tok); });

    const T = nhGsT();
    const sort = nhSettings.usersSort || 'time';
    const secsOf = (p) => (p.uid && nhUav.stats[p.uid] ? (nhUav.stats[p.uid].totalTime || 0) : -1);
    const collator = (function () {
      try { return new Intl.Collator(getUserLanguage(), { sensitivity: 'base' }); } catch (e) { return null; }
    })();
    const byName = (a, b) => (collator ? collator.compare(a.name, b.name) : a.name.localeCompare(b.name));
    people.sort(sort === 'name' ? byName : (a, b) => (secsOf(b) - secsOf(a)) || byName(a, b));

    const sig = sort + '#' + people.map((p) => [p.name, p.type, p.seen, p.activity,
      (p.uid && avatars[p.uid]) || '', (p.uid && nhUav.stats[p.uid] ? Math.round(nhUav.stats[p.uid].totalTime || 0) : '-')].join('~')).join('|') + '#' + (nhSc.ts || 0);
    document.body.classList.add('nh-users-grid');
    nhUsersWiden(table.closest('.configContent')); // re-measured each tick (resize, rail changes)
    let bar = document.getElementById('nh-ug-bar');
    let grid = document.getElementById('nh-ug-grid');
    if (grid && grid.isConnected && nhUav.sig === sig) return;
    nhUav.sig = sig;
    if (!bar) { bar = document.createElement('div'); bar.id = 'nh-ug-bar'; }
    if (!grid) { grid = document.createElement('div'); grid.id = 'nh-ug-grid'; }
    if (bar.previousElementSibling !== table || bar.parentElement !== table.parentElement) {
      table.parentElement.insertBefore(bar, table.nextSibling);
    }
    if (grid.previousElementSibling !== bar || grid.parentElement !== table.parentElement) {
      table.parentElement.insertBefore(grid, bar.nextSibling);
    }
    bar.textContent = '';
    grid.textContent = '';

    // Sort control, the choice sticks per browser, like the other panel settings.
    const toggle = document.createElement('div'); toggle.className = 'nh-ug-sort';
    [['time', T.ugSortTime || PANEL_T.en.ugSortTime], ['name', T.ugSortName || PANEL_T.en.ugSortName]].forEach((o) => {
      const b = document.createElement('button');
      b.type = 'button';
      b.className = 'nh-ug-spill' + (sort === o[0] ? ' nh-on' : '');
      b.textContent = o[1];
      b.addEventListener('click', () => {
        if ((nhSettings.usersSort || 'time') === o[0]) return;
        nhSettings.usersSort = o[0];
        saveSettings();
        nhUav.sig = '';
        nhUsersPage();
      });
      toggle.appendChild(b);
    });
    bar.appendChild(toggle);

    people.forEach((p) => {
      const cardEl = document.createElement('div');
      cardEl.className = 'nh-ug-card';
      cardEl.tabIndex = 0;
      cardEl.addEventListener('click', () => {
        const tr = nhUsersRowFor(p.name); // resolve late: Vue re-renders rows
        if (tr) tr.click();
      });
      cardEl.addEventListener('keydown', (e) => { if (e.key === 'Enter') cardEl.click(); });

      const avWrap = document.createElement('div'); avWrap.className = 'nh-ug-avwrap';
      const av = document.createElement('button');
      av.type = 'button';
      av.className = 'nh-ug-av' + (p.uid && avatars[p.uid] ? ' nh-has' : '');
      av.textContent = (p.name || '?').charAt(0).toUpperCase();
      av.title = (p.uid && avatars[p.uid]) ? (T.ugChange || PANEL_T.en.ugChange) : (T.ugAdd || PANEL_T.en.ugAdd);
      if (p.uid) nhAvatarInto(av, p.uid);
      av.addEventListener('click', (e) => {
        e.stopPropagation();
        if (!p.uid) return;
        av.classList.add('nh-busy');
        nhAvatarPick(p.uid, () => { av.classList.remove('nh-busy'); nhUav.sig = ''; nhUsersPage(); });
      });
      const cam = document.createElement('span'); cam.className = 'nh-ug-cam material-symbols'; cam.textContent = 'photo_camera';
      av.appendChild(cam);
      avWrap.appendChild(av);
      if (p.online) { const dot = document.createElement('span'); dot.className = 'nh-ug-online'; avWrap.appendChild(dot); }
      if (p.uid && avatars[p.uid]) {
        const rm = document.createElement('button');
        rm.type = 'button'; rm.className = 'nh-ug-rm'; rm.textContent = '×';
        rm.title = T.avRemove || PANEL_T.en.avRemove;
        rm.addEventListener('click', (e) => {
          e.stopPropagation();
          nhAvatarClear(p.uid, () => { nhUav.sig = ''; nhUsersPage(); });
        });
        avWrap.appendChild(rm);
      }
      cardEl.appendChild(avWrap);

      const main = document.createElement('div'); main.className = 'nh-ug-main';
      const nameRow = document.createElement('div'); nameRow.className = 'nh-ug-namerow';
      const nm = document.createElement('span'); nm.className = 'nh-ug-name'; nm.textContent = p.name;
      nameRow.appendChild(nm);
      if (p.type) { const ty = document.createElement('span'); ty.className = 'nh-ug-type t-' + p.type.toLowerCase(); ty.textContent = p.type; nameRow.appendChild(ty); }
      main.appendChild(nameRow);

      const st = p.uid ? nhUav.stats[p.uid] : null;
      const secs = st ? (st.totalTime || 0) : 0;
      const timeRow = document.createElement('div'); timeRow.className = 'nh-ug-timerow';
      const big = document.createElement('span'); big.className = 'nh-ug-time';
      big.textContent = st ? nhSbFmt(secs) : '·';
      timeRow.appendChild(big);
      const lab = document.createElement('span'); lab.className = 'nh-ug-tlab';
      lab.textContent = (T.ugListened || PANEL_T.en.ugListened) + (st && nhSbSub(secs) ? ' · ' + nhSbSub(secs) : '');
      timeRow.appendChild(lab);
      main.appendChild(timeRow);

      const meta = document.createElement('div'); meta.className = 'nh-ug-meta';
      meta.textContent = (T.ugSeen || PANEL_T.en.ugSeen) + ': ' + (p.seen || (T.ugNever || PANEL_T.en.ugNever));
      main.appendChild(meta);
      if (p.activity) { const ac = document.createElement('div'); ac.className = 'nh-ug-act'; ac.textContent = p.activity; main.appendChild(ac); }
      cardEl.appendChild(main);

      const acts = document.createElement('div'); acts.className = 'nh-ug-acts';
      [['edit', T.ugEdit || PANEL_T.en.ugEdit], ['delete', T.ugDelete || PANEL_T.en.ugDelete]].forEach((a) => {
        const tr0 = nhUsersRowFor(p.name);
        if (!tr0 || !nhUsersRowBtn(tr0, a[0])) return; // root has no delete
        const b = document.createElement('button');
        b.type = 'button'; b.className = 'nh-ug-act-btn' + (a[0] === 'delete' ? ' nh-del' : '');
        b.title = a[1];
        const ic = document.createElement('span'); ic.className = 'material-symbols'; ic.textContent = a[0];
        b.appendChild(ic);
        b.addEventListener('click', (e) => {
          e.stopPropagation();
          const tr = nhUsersRowFor(p.name);
          const nb = tr && nhUsersRowBtn(tr, a[0]);
          if (nb) nb.click();
        });
        acts.appendChild(b);
      });
      cardEl.appendChild(acts);
      grid.appendChild(cardEl);
    });
  }

  // ---- "Update Account" modal: a profile-picture control next to the fields ----
  // ABS's modal is a generic shell, so the user is identified by the username
  // input's value (survives Vue re-renders; the Add-User modal has none yet, and
  // we simply don't inject there, there is no account to attach a photo to).
  // Goodreads line in ABS's Edit details window (#34): admins fix matches book
  // after book with the Next arrow. The window is the .modal named edit-book;
  // its owner component knows the item id. book-details.js paints the line.
  function nhEditModalGoodreads() {
    if (typeof window.__nhCmMount !== 'function') return;
    const mods = document.querySelectorAll('.modal');
    for (let i = 0; i < mods.length; i++) {
      const m = mods[i];
      const vm = m.__vue__;
      if (!vm || !vm.$props || vm.$props.name !== 'edit-book') continue;
      if (getComputedStyle(m).display === 'none') { const old = m.querySelector('#nh-em-cm'); if (old) old.remove(); continue; }
      let owner = vm.$parent;
      while (owner && !(owner.$options && /item-edit-modal/.test(owner.$options.name || owner.$options._componentTag || ''))) owner = owner.$parent;
      const itemId = owner && owner.selectedLibraryItemId;
      if (!itemId) continue;
      // Only on the Tools tab, as one more tool card under "Embed metadata"
      // (Pawel). The tab's root is the scrolling pane inside the tabpanel.
      const pane = m.querySelector('[role="tabpanel"] > div.overflow-y-auto');
      const isTools = owner.selectedTab === 'tools' && pane && pane.querySelector('.border.border-black-200, p.text-lg');
      let host = m.querySelector('#nh-em-cm');
      if (!isTools) { if (host) host.remove(); continue; }
      if (!host) {
        host = document.createElement('div');
        host.id = 'nh-em-cm';
        host.className = 'w-full border border-black-200 p-4 my-8';
        const T = nhGsT();
        host.innerHTML = '<div class="flex flex-wrap items-center gap-4"><div><p class="text-lg">' + (T.cmTitle || PANEL_T.en.cmTitle) + '</p>' +
          '<p class="max-w-sm text-sm pt-2 text-gray-300">' + (T.cmToolHint || PANEL_T.en.cmToolHint) + '</p></div><div class="grow"></div><div class="nh-em-line" style="position:relative;"></div></div><div class="nh-em-panel"></div>';
        const cards = pane.querySelectorAll('.border.border-black-200');
        const last = cards[cards.length - 1];
        if (last) last.insertAdjacentElement('afterend', host); else pane.appendChild(host);
      }
      window.__nhCmMount(host.querySelector('.nh-em-line'), itemId, host.querySelector('.nh-em-panel'));
    }
  }

  function nhUserEditModal() {
    if (!isUserAdmin() || !nhUav.list || !nhSc.avatars) return;
    const toggle = document.getElementById('user-enabled-toggle');
    if (!toggle) return;
    let modal = toggle;
    for (let i = 0; i < 12 && modal; i++) {
      if (getComputedStyle(modal).position === 'fixed') break;
      modal = modal.parentElement;
    }
    // NOT offsetParent: it is null for every position:fixed element, open or
    // closed. Measure the anchor instead, a closed modal has no box.
    if (!modal || toggle.getBoundingClientRect().height <= 0) return;
    const holder = modal.querySelector('form .w-full.p-8') || modal.querySelector('form > div > div');
    if (!holder) return;
    const nameInput = modal.querySelector('input[type="text"]');
    const username = nameInput ? String(nameInput.value).trim() : '';
    const uid = username ? nhUav.list[username] : null;
    const T = nhGsT();
    const sig = (uid || '') + ':' + ((uid && nhSc.avatars[uid]) || '') + ':' + (nhSc.ts || 0);
    const existing = holder.querySelector('#nh-um-photo');
    if (!uid) { if (existing) existing.remove(); return; }
    // Vue patches modal contents in place, confirm the node is still there,
    // never trust the stamp alone.
    if (existing && existing.dataset.sig === sig) return;
    if (existing) existing.remove();

    const row = document.createElement('div');
    row.id = 'nh-um-photo'; row.dataset.sig = sig;
    const prev = document.createElement('div'); prev.className = 'nh-um-prev';
    prev.textContent = (username || '?').charAt(0).toUpperCase();
    nhAvatarInto(prev, uid);
    const txt = document.createElement('div'); txt.className = 'nh-um-txt';
    const lab = document.createElement('div'); lab.className = 'nh-um-lab'; lab.textContent = T.ugPhoto || PANEL_T.en.ugPhoto;
    const btns = document.createElement('div'); btns.className = 'nh-um-btns';
    const set = document.createElement('button');
    set.type = 'button'; set.className = 'nh-um-btn';
    set.textContent = nhSc.avatars[uid] ? (T.ugChange || PANEL_T.en.ugChange) : (T.ugAdd || PANEL_T.en.ugAdd);
    set.addEventListener('click', (e) => {
      e.preventDefault(); e.stopPropagation();
      set.disabled = true;
      nhAvatarPick(uid, () => { set.disabled = false; row.dataset.sig = ''; nhUserEditModal(); nhUav.sig = ''; });
    });
    btns.appendChild(set);
    if (nhSc.avatars[uid]) {
      const rm = document.createElement('button');
      rm.type = 'button'; rm.className = 'nh-um-btn nh-um-rm'; rm.textContent = T.avRemove || PANEL_T.en.avRemove;
      rm.addEventListener('click', (e) => {
        e.preventDefault(); e.stopPropagation();
        nhAvatarClear(uid, () => { row.dataset.sig = ''; nhUserEditModal(); nhUav.sig = ''; });
      });
      btns.appendChild(rm);
    }
    txt.appendChild(lab); txt.appendChild(btns);
    row.appendChild(prev); row.appendChild(txt);
    holder.insertBefore(row, holder.firstChild);
  }

  // ===================== PER-USER START PAGE (A4) =====================
  // Lands the browser on a chosen library view on APP ENTRY only. Deep links and
  // SPA navigation are never hijacked: the decision keys off the URL captured at
  // script parse time (before ABS's own default-library redirect can move it),
  // runs once per page load, and a sessionStorage guard keeps a tab that already
  // got its start page this session from redirecting again on reload.
  const nhSp = { entry: location.pathname, tried: false, hold: false };
  // Veil hold: when this load WILL likely redirect (setting present, tab hasn't
  // redirected yet, entry looks like a neutral app entry), keep the boot veil up
  // until the decision lands, otherwise the default home paints for a few frames
  // and then jumps to the start page (user: "very jittery"). The template's 4s
  // nh-ready timeout is the unconditional failsafe, so a wrong guess here can
  // never trap the veil. The loose regex tolerates a router base prefix; the
  // strict base-stripped check still decides the actual redirect below.
  try {
    nhSp.hold = !!(nhSettings.startPage && !sessionStorage.getItem('nh-sp-done') &&
      (/(?:^|\/)(?:login\/?)?$/.test(nhSp.entry) || /\/library\/[^/]+\/?$/.test(nhSp.entry)));
  } catch (e) {}
  function nhStartPage() {
    if (nhSp.tried) return;
    // Never veil the login form: an unauthenticated entry lands there and the
    // user has to see it. The post-login redirect is covered by the normal
    // masked page transition instead.
    if (nhSp.hold && /\/login(?:\/|$)/.test(location.pathname)) nhSp.hold = false;
    const nuxt = window.$nuxt;
    if (!nuxt || !nuxt.$router) return; // app not up yet
    const target = String(nhSettings.startPage || '');
    if (!target) { nhSp.tried = true; nhSp.hold = false; return; }
    if (!(nuxt.$store && nuxt.$store.state.user && nuxt.$store.state.user.user)) return; // wait for login
    nhSp.tried = true;
    // Release the veil hold only AFTER the redirect has actually landed (router
    // promise), releasing at push time still unveiled the interim default home
    // for a couple of frames. Every declined path releases immediately.
    const unhold = () => { nhSp.hold = false; };
    if (sessionStorage.getItem('nh-sp-done')) return unhold();
    sessionStorage.setItem('nh-sp-done', '1');
    // Strip the router base (e.g. /audiobookshelf/) off the captured entry URL.
    let entry = nhSp.entry;
    const base = (nuxt.$router.options && nuxt.$router.options.base) || '/';
    if (base !== '/' && entry.indexOf(base) === 0) entry = '/' + entry.slice(base.length);
    entry = entry.replace(/\/+$/, '') || '/';
    if (!(entry === '/' || entry === '/login' || /^\/library\/[^/]+$/.test(entry))) return unhold();
    const cut = target.indexOf(':');
    const view = target.slice(0, cut), lib = target.slice(cut + 1);
    if (!lib) return unhold();
    // authors lives UNDER /bookshelf, a bare /authors route 404s (ABS 2.35)
    const paths = { home: '', bookshelf: '/bookshelf', series: '/bookshelf/series', collections: '/bookshelf/collections', authors: '/bookshelf/authors' };
    if (paths[view] === undefined) return unhold();
    const dest = '/library/' + lib + paths[view];
    const cur = ((nuxt.$route && nuxt.$route.path) || '').replace(/\/+$/, '');
    if (cur !== dest) {
      try { Promise.resolve(nuxt.$router.push(dest)).catch(function () {}).then(unhold); }
      catch (e) { unhold(); }
    } else {
      unhold();
    }
  }

  // ===================== EREADER CUSTOMIZATION =====================
  // Extends ABS's epub reader (settings modal + rendition). Facts from ABS source:
  // #viewer hosts the epub.js rendition (EpubReader.vue); ABS applies its colour theme
  // via contents.addStylesheetRules and RE-APPLIES on every settings change, so our
  // rules must land after, applyTheme is patched to chain our pass. Fonts are set via
  // rendition.themes.font(), and the font files must be loaded INSIDE the iframe, so a
  // content hook injects a Google Fonts stylesheet into each rendered chapter.
  const NH_ER_FONTS_SERIF = ['Literata', 'Merriweather', 'Lora', 'EB Garamond', 'Crimson Pro'];
  const NH_ER_FONTS_SANS = ['Atkinson Hyperlegible', 'Inter', 'Source Sans 3', 'Nunito Sans', 'Lexend'];
  const NH_ER_FONTS_DYS = ['OpenDyslexic'];
  // Google Fonts only, OpenDyslexic loads from fontsource (not on Google Fonts)
  const NH_EREADER_FONTS = NH_ER_FONTS_SERIF.concat(NH_ER_FONTS_SANS);
  const NH_DYS_CSS = 'https://cdn.jsdelivr.net/npm/@fontsource/opendyslexic@5/index.css';
  function nhFontGeneric(f) { return NH_ER_FONTS_SERIF.indexOf(f) !== -1 ? 'serif' : 'sans-serif'; }
  // ABS's own Dark/Sepia/Light are folded in as presets (its Theme row is hidden by the
  // modal builder) so there is ONE colour control. They're implemented as overlay rules
  // with the exact ABS colours, so the underlying ABS theme state never fights us.
  // 'dark' is the neutral state and stores as '' (no overlay at all).
  const NH_EREADER_PAGES = {
    dark:     { label: 'Dark',     fg: '#ffffff', bg: function () { return '#232323'; } },
    sepia:    { label: 'Sepia',    fg: '#5b4636', bg: function () { return '#f4ecd8'; } },
    light:    { label: 'Light',    fg: '#000000', bg: function () { return '#ffffff'; } },
    nanohive: { label: 'Theme', fg: function () { try { const v = getComputedStyle(document.documentElement).getPropertyValue('--nh-text-1').trim(); return v || '#e8e0d2'; } catch (e) { return '#e8e0d2'; } }, bg: function () { try { const v = getComputedStyle(document.documentElement).getPropertyValue('--nh-canvas').trim(); return v || '#181512'; } catch (e) { return '#181512'; } } },
    black:    { label: 'Black',    fg: '#c9c2b6', bg: function () { return '#000000'; } },
    paper:    { label: 'Paper',    fg: '#2e2a24', bg: function () { return '#f7f3ea'; } }
  };

  // Reader preferences live in the same store, so they are per-user too (#12).
  function nhEreaderGet(k) { try { return (JSON.parse(localStorage.getItem(nhSettingsKey(nhSetUid)) || '{}') || {})[k] || ''; } catch (e) { return ''; } }
  function nhEreaderSet(k, v) {
    try {
      const key = nhSettingsKey(nhSetUid);
      const s = JSON.parse(localStorage.getItem(key) || '{}') || {};
      s[k] = v; localStorage.setItem(key, JSON.stringify(s));
      nhPrefsPush(s);
    } catch (e) {}
  }

  function nhFindEpubVm() {
    let el = document.getElementById('viewer');
    while (el) { if (el.__vue__ && el.__vue__.rendition) return el.__vue__; el = el.parentElement; }
    return null;
  }

  function nhPresetFg(p) { return typeof p.fg === 'function' ? p.fg() : p.fg; }
  function nhEreaderEff() {
    // Unset = follow the active NanoHive theme (item: theme colours are the ereader default)
    const preset = NH_EREADER_PAGES[nhEreaderGet('ereaderPage') || 'nanohive'];
    return {
      font: nhEreaderGet('ereaderFont'),
      fg: nhEreaderGet('ereaderFg') || (preset ? nhPresetFg(preset) : ''),
      bg: nhEreaderGet('ereaderBg') || (preset ? preset.bg() : '')
    };
  }

  function nhDecorateContents(c) {
    const eff = nhEreaderEff();
    try { if (eff.font) c.addStylesheet(eff.font === 'OpenDyslexic' ? NH_DYS_CSS : 'https://fonts.googleapis.com/css2?family=' + eff.font.replace(/ /g, '+') + ':ital,wght@0,400;0,700;1,400&display=swap'); } catch (e) {}
    if (eff.fg || eff.bg) {
      const all = {}, links = {};
      if (eff.fg) { all.color = eff.fg + '!important'; links.color = eff.fg + '!important'; }
      if (eff.bg) { all['background-color'] = eff.bg + '!important'; }
      try { c.addStylesheetRules({ '*': all, a: links }); } catch (e) {}
    }
  }

  // The reader shell (#reader) around the pages carries ABS's data-theme background;
  // painted to match the effective page colour so the surround never mismatches.
  function nhPaintShell() {
    const r = document.getElementById('reader');
    if (!r) return;
    const eff = nhEreaderEff();
    if (eff.bg) {
      r.style.setProperty('background-color', eff.bg, 'important');
      if (eff.fg) r.style.setProperty('color', eff.fg, 'important');
    } else {
      r.style.removeProperty('background-color');
      r.style.removeProperty('color');
    }
  }

  function nhApplyEreader() {
    const vm = nhFindEpubVm();
    if (!vm || !vm.rendition) return;
    const rend = vm.rendition;
    if (!rend.__nhHook) {
      rend.__nhHook = true;
      try { rend.hooks.content.register(function (c) { nhDecorateContents(c); }); } catch (e) {}
    }
    if (!vm.__nhPatched && typeof vm.applyTheme === 'function') {
      vm.__nhPatched = true;
      const orig = vm.applyTheme.bind(vm);
      // ABS re-applies its theme on EVERY settings change (incl. font-size/spacing),
      // which resets rendition.themes.font() back to ABS's font. Re-assert ours after
      // orig() so a size change no longer wipes the reader's chosen NanoHive font.
      vm.applyTheme = function () {
        orig();
        try { const e2 = nhEreaderEff(); if (e2.font) rend.themes.font('"' + e2.font + '", ' + nhFontGeneric(e2.font)); } catch (e) {}
        try { rend.getContents().forEach(nhDecorateContents); } catch (e) {}
      };
    }
    const eff = nhEreaderEff();
    try {
      if (eff.font) rend.themes.font('"' + eff.font + '", ' + nhFontGeneric(eff.font));
      else if (vm.ereaderSettings && vm.ereaderSettings.font) rend.themes.font(vm.ereaderSettings.font);
    } catch (e) {}
    // ABS pass first (restores stock when everything is Default), ours chained after
    try { vm.applyTheme(); } catch (e) {}
    nhPaintShell();
  }

  function nhEreaderFontsLink() {
    if (document.getElementById('nh-er-fonts')) return;
    const l = document.createElement('link');
    l.id = 'nh-er-fonts'; l.rel = 'stylesheet';
    l.href = 'https://fonts.googleapis.com/css2?' + NH_EREADER_FONTS.map(function (f) { return 'family=' + f.replace(/ /g, '+') + ':wght@400;600'; }).join('&') + '&display=swap';
    if (!document.getElementById('nh-er-dys-css')) {
      const d = document.createElement('link');
      d.id = 'nh-er-dys-css'; d.rel = 'stylesheet'; d.href = NH_DYS_CSS;
      document.head.appendChild(d);
    }
    document.head.appendChild(l);
  }

  const NH_ER_FG_SWATCHES = ['#ffffff', '#e8e0d2', '#c9c2b6', '#5b4636', '#2e2a24', '#000000'];
  const NH_ER_BG_SWATCHES = ['#232323', '#181512', '#0b1618', '#000000', '#f4ecd8', '#f7f3ea', '#ffffff'];
  const NH_ER_SAMPLE = 'The quick brown fox jumps over the lazy dog. Pack my box with five dozen liquor jugs \u2014 0123456789. She said, \u201cReading should feel effortless.\u201d';

  // Keeps the preview live against ABS's own sliders (scale, spacing, boldness),   // called every mutation cycle, cheap no-op when the preview isn't on screen.
  function nhEreaderPreviewSync() {
    const prev = document.getElementById('nh-er-preview');
    if (!prev) return;
    const vm = nhFindEpubVm();
    const s = (vm && vm.ereaderSettings) || {};
    const eff = nhEreaderEff();
    const fg = eff.fg || '#ffffff';
    prev.style.fontFamily = eff.font ? '"' + eff.font + '", ' + nhFontGeneric(eff.font) : ((s.font === 'sans') ? 'ui-sans-serif, system-ui, sans-serif' : 'Georgia, serif');
    prev.style.color = fg;
    prev.style.background = eff.bg || '#232323';
    const fs = (s.fontScale || 100) / 100;
    const ls = (s.lineSpacing || 115) / 100;
    const ts = (s.textStroke || 0) / 100;
    prev.style.fontSize = (fs * 16) + 'px';
    prev.style.lineHeight = (ls * fs) + 'rem';
    prev.style.webkitTextStroke = ts + 'px ' + fg;
    nhPaintShell();
  }

  function nhEreaderModal() {
    // Anchor on ABS's Sans/Serif toggle buttons, they're hardcoded untranslated in
    // ABS source, so this works in every UI language (the old 'Theme:' text anchor
    // broke the whole extension on non-English UIs).
    const fontBtn = Array.prototype.find.call(document.querySelectorAll('button.toggle-btn'), function (b) { return b.textContent.trim() === 'Serif'; });
    if (!fontBtn) return;
    const card = fontBtn.closest('div.bg-bg') || fontBtn.closest('.rounded-lg');
    if (!card || card.dataset.nhExt) return;
    card.dataset.nhExt = '1';
    nhEreaderFontsLink();

    // ABS's Theme row (first row) is superseded by our Page theme presets, and its
    // Sans/Serif toggle by our full typeface dropdown, one control each, not two.
    const absRows = card.querySelectorAll('.flex.items-center');
    if (absRows[0]) absRows[0].style.display = 'none';
    const absFontRow = fontBtn.closest('.flex.items-center');
    if (absFontRow) absFontRow.style.display = 'none';

    const rebuild = function () {
      delete card.dataset.nhExt;
      const p0 = document.getElementById('nh-er-preview'); if (p0) p0.remove();
      const s0 = card.querySelector('.nh-er'); if (s0) s0.remove();
      nhEreaderModal();
      nhApplyEreader();
    };
    const pick = function (key, val) { nhEreaderSet(key, val); rebuild(); };

    // Live sample at the very top of the modal
    const prev = document.createElement('div');
    prev.id = 'nh-er-preview';
    prev.className = 'nh-er-preview';
    prev.innerHTML = '<span class="nh-er-aa">Aa</span>' + NH_ER_SAMPLE;
    card.insertBefore(prev, card.firstChild);

    const sec = document.createElement('div');
    sec.className = 'nh-er';
    sec.appendChild(Object.assign(document.createElement('div'), { className: 'nh-er-sep' }));
    const title = document.createElement('p');
    title.className = 'nh-er-title'; title.textContent = 'NanoHive';
    sec.appendChild(title);

    const row = function (labelText, ctlChildren) {
      const r = document.createElement('div');
      r.className = 'nh-er-row';
      const lab = document.createElement('div');
      lab.className = 'nh-er-lab';
      lab.innerHTML = '<p class="text-lg">' + labelText + '</p>';
      const ctl = document.createElement('div');
      ctl.className = 'nh-er-ctl';
      ctlChildren.forEach(function (ch) { ctl.appendChild(ch); });
      r.appendChild(lab); r.appendChild(ctl);
      return r;
    };
    const chip = function (text, sel, onClick, styler) {
      const b = document.createElement('button');
      b.type = 'button';
      b.className = 'nh-er-chip' + (sel ? ' sel' : '');
      b.textContent = text;
      if (styler) styler(b);
      b.addEventListener('click', onClick);
      return b;
    };
    const swatch = function (color, sel, onClick) {
      const b = document.createElement('button');
      b.type = 'button';
      b.className = 'nh-er-swatch' + (sel ? ' sel' : '');
      b.style.background = color;
      b.title = color;
      b.addEventListener('click', onClick);
      return b;
    };
    const colorInput = function (cur, onPick) {
      const i = document.createElement('input');
      i.type = 'color'; i.className = 'nh-er-color';
      i.value = /^#[0-9a-f]{6}$/i.test(cur) ? cur : '#e0c27a';
      i.addEventListener('change', function () { onPick(i.value); });
      return i;
    };

    const curFont = nhEreaderGet('ereaderFont');
    const fontSel = document.createElement('select');
    fontSel.className = 'nh-er-select';
    const defOpt = document.createElement('option');
    defOpt.value = ''; defOpt.textContent = (nhGsT().erDefault || PANEL_T.en.erDefault);
    fontSel.appendChild(defOpt);
    [['Serif', NH_ER_FONTS_SERIF], ['Sans serif', NH_ER_FONTS_SANS], ['Dyslexia friendly', NH_ER_FONTS_DYS]].forEach(function (grp) {
      const og = document.createElement('optgroup');
      og.label = grp[0];
      grp[1].forEach(function (f) {
        const o = document.createElement('option');
        o.value = f; o.textContent = f;
        o.style.fontFamily = '"' + f + '", ' + nhFontGeneric(f);
        og.appendChild(o);
      });
      fontSel.appendChild(og);
    });
    fontSel.value = curFont || '';
    if (curFont) fontSel.style.fontFamily = '"' + curFont + '", ' + nhFontGeneric(curFont);
    fontSel.addEventListener('change', function () { pick('ereaderFont', fontSel.value); });
    sec.appendChild(row(nhGsT().erTypeface || PANEL_T.en.erTypeface, [fontSel]));
    nhSelectify(fontSel);

    const curPage = nhEreaderGet('ereaderPage');
    const hasCustom = !!(nhEreaderGet('ereaderFg') || nhEreaderGet('ereaderBg'));
    sec.appendChild(row(nhGsT().erPage || PANEL_T.en.erPage, Object.keys(NH_EREADER_PAGES).map(function (k) {
      const p = NH_EREADER_PAGES[k];
      const sel = !hasCustom && (curPage === k || (!curPage && k === 'nanohive'));
      return chip(p.label, sel, function () {
        nhEreaderSet('ereaderPage', k === 'nanohive' ? '' : k);
        nhEreaderSet('ereaderFg', ''); nhEreaderSet('ereaderBg', '');
        rebuild();
      }, function (b) { b.classList.add('nh-er-tile'); b.style.background = p.bg(); b.style.color = nhPresetFg(p); b.style.borderColor = 'rgba(255,255,255,0.35)'; });
    })));

    const curFg = nhEreaderGet('ereaderFg');
    sec.appendChild(row(nhGsT().erText || PANEL_T.en.erText, [chip(nhGsT().erAuto || PANEL_T.en.erAuto, !curFg, function () { pick('ereaderFg', ''); })].concat(
      NH_ER_FG_SWATCHES.map(function (cc) { return swatch(cc, curFg.toLowerCase() === cc, function () { pick('ereaderFg', cc); }); }),
      [colorInput(curFg, function (v) { pick('ereaderFg', v); })]
    )));
    const curBg = nhEreaderGet('ereaderBg');
    sec.appendChild(row(nhGsT().erBg || PANEL_T.en.erBg, [chip(nhGsT().erAuto || PANEL_T.en.erAuto, !curBg, function () { pick('ereaderBg', ''); })].concat(
      NH_ER_BG_SWATCHES.map(function (cc) { return swatch(cc, curBg.toLowerCase() === cc, function () { pick('ereaderBg', cc); }); }),
      [colorInput(curBg, function (v) { pick('ereaderBg', v); })]
    )));

    card.appendChild(sec);
    nhEreaderPreviewSync();
  }

  function nhEreader() {
    const vm = nhFindEpubVm();
    if (vm && vm.rendition && !vm.rendition.__nhInit) { vm.rendition.__nhInit = true; nhApplyEreader(); }
    nhEreaderModal();
  }

  // Version footer at the bottom of the library sidebar rail (and mobile drawer).
  // ABS 2.35+ moved its version link into a hidden changelog modal, so users lost the
  // at-a-glance "what am I running" readout. Restore it and add the theme version.
  // Bump NH_THEME_VERSION on each release (the composite THEME_VERSION from NH_CONFIG is
  // shown on hover for exact per-file versions).
  const NH_THEME_VERSION = 'v2.7.2';
  // The RELEASES LIST, not this version's own tag. Linking to
  // /releases/tag/<version> looked tidier, but it 404s for any build running
  // ahead of its release, which is every staging build, and any nightly. The
  // list always exists, opens on the newest release with its changelog, and
  // still lets you scroll to the version you are actually running. It is also
  // exactly the page the original request pointed at.
  const NH_RELEASE_URL = 'https://github.com/rodzalendo/nanohive-abs-theme/releases';
  function nhAbsVersion() {
    try {
      const v = window.$nuxt && window.$nuxt.$store && window.$nuxt.$store.state.serverSettings && window.$nuxt.$store.state.serverSettings.version;
      if (v) return String(v).replace(/^v?/, 'v');
    } catch (e) {}
    const a = document.querySelector('a[href*="/releases/tag/v"]');
    if (a) { const m = a.textContent.trim().match(/\d+\.\d+\.\d+/); if (m) return 'v' + m[0]; }
    return '';
  }
  function nhVersionFooterMarkup(f) {
    const abs = nhAbsVersion();
    const composite = (window.NH_CONFIG && window.NH_CONFIG.themeVersion) || '';
    f.title = composite ? 'NanoHive theme build: ' + composite : '';
    if (f.dataset.v === abs) return;
    f.dataset.v = abs;
    const T = panelT();
    // The NanoHive line is a link to this exact version's release notes. The
    // desktop footer is pointer-events:none (it overlays the rail and must not
    // eat clicks meant for it), so the anchor re-enables them for itself only.
    // Both strings are build constants, never user input.
    f.innerHTML = `<div style="opacity:.85;">Audiobookshelf${abs ? ' ' + abs : ''}</div>` +
                  `<a class="nh-vf-link" href="${NH_RELEASE_URL}" target="_blank" rel="noopener noreferrer"` +
                  ` title="${escapeHtmlNH(T.vfRelease || PANEL_T.en.vfRelease)}">NanoHive ${NH_THEME_VERSION}</a>`;
  }
  function nhVersionFooter() {
    // Desktop rail: append inside it and pin to the viewport's bottom-left. Because it
    // lives inside the rail (display:none on mobile via `hidden md:block`), it auto-hides
    // on mobile, where we instead drop a copy into the slide-out drawer (below).
    const rail = document.querySelector('[aria-label="Library Sidebar"]');
    if (rail) {
      let f = rail.querySelector('#nh-version-footer');
      if (!f) {
        f = document.createElement('div');
        f.id = 'nh-version-footer';
        f.style.cssText = 'position:fixed;bottom:0;left:0;width:80px;box-sizing:border-box;padding:10px 4px;text-align:center;font-family:system-ui,-apple-system,"Segoe UI",sans-serif;font-size:0.6rem;line-height:1.35;color:var(--nh-muted-2);pointer-events:none;z-index:50;';
        rail.appendChild(f);
      }
      nhVersionFooterMarkup(f);
    }
    // Mobile drawer (#nh-mobile-drawer is a flex column), pin a footer to its bottom.
    const drawer = document.getElementById('nh-mobile-drawer');
    if (drawer) {
      let df = drawer.querySelector('#nh-version-footer-mobile');
      if (!df) {
        df = document.createElement('div');
        df.id = 'nh-version-footer-mobile';
        df.style.cssText = 'margin-top:auto;padding:16px 8px 6px;text-align:center;font-family:system-ui,-apple-system,"Segoe UI",sans-serif;font-size:0.7rem;line-height:1.4;color:var(--nh-muted-2);';
        drawer.appendChild(df);
      }
      nhVersionFooterMarkup(df);
    }
  }

  // The theme began life as a Tampermonkey userscript. If a browser still has that
  // old userscript installed, it injects a SECOND (outdated) copy of the theme on
  // top of the proxy's inlined one, the two fight over the DOM every tick, which
  // presents as flicker, scroll jumps, a self-emptying settings panel, mangled
  // series cards, and stale CSS. Detect the duplicate injection and say so loudly.
  function nhDetectDuplicateTheme() {
    if (document.getElementById('nh-dup-banner')) return;
    const dupCore = document.querySelectorAll('style#nanohive-abs-theme').length > 1;
    const dupBook = document.querySelectorAll('style#nanohive-abs-details-theme').length > 1;
    if (!dupCore && !dupBook) return;
    try { console.error('[NanoHive] DUPLICATE theme injection detected, an old NanoHive userscript (Tampermonkey/Greasemonkey) is still installed in this browser. Remove it; the server now themes every browser by itself.'); } catch (e) {}
    const b = document.createElement('div');
    b.id = 'nh-dup-banner';
    b.style.cssText = 'position:fixed;top:0;left:0;right:0;z-index:99999;background:#7a1f1f;color:#fff;font:600 14px/1.4 system-ui,sans-serif;padding:10px 44px 10px 16px;text-align:center;box-shadow:0 4px 16px rgba(0,0,0,.5);';
    b.textContent = 'NanoHive: this browser still runs the OLD NanoHive userscript (Tampermonkey), which conflicts with the server theme. Please remove/disable it and reload.';
    const x = document.createElement('button');
    x.textContent = '×';
    x.style.cssText = 'position:absolute;right:10px;top:6px;background:none;border:none;color:#fff;font-size:20px;cursor:pointer;';
    x.addEventListener('click', () => b.remove());
    b.appendChild(x);
    (document.body || document.documentElement).appendChild(b);
  }

  // Series-page mask. The 500ms tick is too late to stop the staged reveal the user
  // sees on series pages (wide books -> column squeezes them -> cards re-measure ->
  // finished badges pop). So: (1) an SPA route hook masks the page the INSTANT the
  // navigation happens; (2) nhSeriesReveal fades everything in one tick AFTER the
  // two-column structure is applied and the shelf re-measured, by then the cards
  // and their finished badges are painted, so the page appears fully formed.
  let nhMaskT0 = 0;
  let nhLastPath = '';
  let nhPgArm = '';
  let nhEnterT = null;
  // The eased-card-entrance window is re-armed by the reveal, not only by the
  // route change, see nhPageShown.
  function nhEnterWindow(ms) {
    clearTimeout(nhEnterT);
    nhEnterT = setTimeout(() => { try { document.body.classList.remove('nh-entering'); } catch (e) {} }, ms);
  }
  function nhRouteMask() {
    if (nhSettings.pageTransitions === false) return;
    if (!document.body) return;
    nhRememberQuery(); // learn whatever query ABS settles on for this route
    const path = location.pathname;
    if (path === nhLastPath) { nhRequeryMask(); return; } // filter/sort: shelf-only mask
    nhLastPath = path;
    // A real navigation supersedes any in-flight requery mask, and re-baselines the
    // query so landing on a page with ?filter=... does not instantly re-mask.
    nhRqQuery = location.search;
    nhRqArm = null;
    document.body.classList.remove('nh-requery', 'nh-requery-ready');
    // Entrance window (B5): cards that mount within ~2s of a navigation fade in
    // instead of popping, kills the "trickle" on slow shelf pages (series list
    // especially). Cards mounted later (scroll) appear instantly as before.
    document.body.classList.add('nh-entering');
    nhEnterWindow(2200);
    const onSeries = /\/library\/[^/]+\/series\/[^/?#]+/.test(path);
    document.body.classList.remove('nh-series-ready');
    if (onSeries) document.body.classList.add('nh-series-page');
    else document.body.classList.remove('nh-series-page');
    // General mask for EVERY app page (series detail has its own system above; the
    // login page is handled by the boot veil): hidden from the instant of
    // navigation until nhPageReveal decides the layout has settled.
    const maskable = !onSeries && !/\/login\/?$/.test(path);
    document.body.classList.remove('nh-page-ready');
    document.body.classList.toggle('nh-page-loading', maskable);
    nhMaskT0 = Date.now();
    nhPgArm = '';
    nhCoverT0 = 0;
    // Mark the OUTGOING page's bookshelf as stale: during a route transition it
    // can survive a tick or two, and its cards satisfied the reveal conditions
    // for the NEW page, the mask lifted before the new grid ever rendered
    // (user: "easing-in for the library page doesn't work"). The remounted
    // shelf arrives without the marker.
    const staleBs = document.getElementById('bookshelf');
    if (staleBs) staleBs.dataset.nhStale = '1';
  }

  // The bookshelf for reveal checks: rejects the OUTGOING page's still-mounted
  // shelf during the first beat of a transition, but un-stales it if it survives
  // longer, Vue reuses the same component (and element) for home→home library
  // switches and series→series, and blocking those forever would force the
  // 3.5s failsafe on every such navigation.
  function nhShelfFresh() {
    const bs = document.getElementById('bookshelf');
    if (!bs) return null;
    if (bs.dataset.nhStale) {
      if (Date.now() - nhMaskT0 < 600) return null;
      delete bs.dataset.nhStale;
    }
    return bs;
  }

  // Is the grid's geometry stable? Compares the first card's box against the
  // previous tick; two matching samples (~2 ticks) mean ABS has finished
  // re-laying the shelf. Capped by the caller's failsafe, and by its own timeout
  // so a shelf that never settles can't hold the page hostage.
  const nhGridS = { key: '', since: 0 };
  function nhGridSettled(bs) {
    const card = bs.querySelector('[id^="book-card-"], [cy-id="card"], [id^="series-card-"], [id^="collection-card-"], [id^="author-card-"], [id^="narrator-card-"]');
    if (!card) return true;
    const r = card.getBoundingClientRect();
    // The card's box alone is not enough: on the series grid ABS re-computed
    // entitiesPerShelf (10 -> 6) and its left margin ~200ms AFTER the reveal,
    // while the first card sat still, so watch the shelf's own layout inputs too.
    const vm = bs.__vue__ || {};
    const key = Math.round(r.left) + 'x' + Math.round(r.top) + 'x' + Math.round(r.width) +
      '|' + vm.entitiesPerShelf + '|' + Math.round(vm.bookshelfMarginLeft || 0) +
      '|' + vm.bookWidth + '|' + Math.round(vm.bookshelfWidth || 0);
    const now = Date.now();
    if (key !== nhGridS.key) { nhGridS.key = key; nhGridS.since = now; return false; }
    // 220ms ≈ three ticks: a two-sample window still let a late ABS
    // re-position (~7px on the series grid) slip in just after the reveal.
    return (now - nhGridS.since >= 220) || (now - nhMaskT0 > 2600);
  }

  // Filter / sort / search change the QUERY, not the path, so the page mask above
  // deliberately skips them, masking the whole page there would blink the toolbar
  // control you just clicked. But ABS tears the virtual shelf down to ZERO cards and
  // rebuilds it (measured in Firefox: 37 -> 0 -> 36 within ~100ms), so the grid
  // blanks and snaps back. That is the "it flickers once when I filter or sort"
  // report. Cover the SHELF ONLY for that rebuild, then fade it back.
  let nhRqT0 = 0;
  let nhRqQuery = '';
  // null, NOT '', a bare nav link's query IS '', so an '' sentinel collided with a
  // real value and made the reveal guard below short-circuit before it ever armed,
  // leaving the shelf hidden until the next navigation ("click Library again and all
  // tiles disappear"). Never give this a value a real location.search can take.
  let nhRqArm = null;
  function nhRequeryMask() {
    if (nhSettings.pageTransitions === false) return;
    if (!document.body) return;
    const q = location.search;
    if (q === nhRqQuery) return;
    const wasQueried = !!nhRqQuery;
    nhRqQuery = q;
    // Nothing to cover on pages with no virtual shelf (settings, book detail...).
    if (!document.getElementById('bookshelf')) return;
    // Clicking the CURRENT page's nav link just strips the query (one replaceState,
    // no refetch, measured: the shelf keeps all its cards). There is no teardown to
    // hide, so masking it only made the grid blink for no reason.
    if (!q && wasQueried) return;
    nhRqT0 = Date.now();
    nhRqArm = null;
    document.body.classList.remove('nh-requery-ready');
    document.body.classList.add('nh-requery');
  }

  // Hide the shelf for a rebuild that is NOT driven by a query change (our own
  // filter/sort chips). Reuses the requery mask's reveal conditions, cards back and
  // geometry settled, so the rebuild happens behind opacity 0 instead of in view.
  function nhMaskShelfForRebuild() {
    if (nhSettings.pageTransitions === false) return;
    if (!document.body || !document.getElementById('bookshelf')) return;
    nhRqT0 = Date.now();
    nhRqArm = null;
    document.body.classList.remove('nh-requery-ready');
    document.body.classList.add('nh-requery');
  }

  function nhRequeryReveal() {
    const body = document.body;
    if (!body.classList.contains('nh-requery') || body.classList.contains('nh-requery-ready')) return;
    // Shorter failsafe than the page mask: a filter matching NOTHING is a legitimately
    // empty shelf and must never sit hidden waiting for a card that is not coming.
    const overdue = Date.now() - nhRqT0 > 1500;
    if (!overdue) {
      const bs = document.getElementById('bookshelf');
      if (!bs) return;
      if (!bs.querySelector('[cy-id="card"], [id^="book-card-"], [id^="cover-area-"], [id^="series-card-"], [id^="collection-card-"], [id^="author-card-"], [id^="narrator-card-"]')) return;
      if (!nhGridSettled(bs)) return;
    }
    if (nhRqArm === nhRqQuery) return;
    nhRqArm = nhRqQuery;
    const q = nhRqQuery;
    requestAnimationFrame(() => requestAnimationFrame(() => {
      if (location.search === q) body.classList.add('nh-requery-ready');
    }));
  }

  // ---- "Rate finished" home row -------------------------------------------
  // Books you have finished but never rated. Uses the same community-ratings
  // store as the book page, so a rating left anywhere makes the book drop out.
  let nhRf = { key: '', loading: false, data: null, rates: null, ratesAt: 0 };

  function nhRfHome() {
    if (nhSettings.showRateFinished === false) return null;
    const m = location.pathname.match(/\/library\/([^/]+)\/?$/);
    if (!m) return null;
    // A library that does not do ratings does not get the row that asks for them.
    return nhRatingsLibOn(m[1]) ? m[1] : null;
  }

  // Finished items belonging to ONE library. mediaProgress is server-wide and
  // carries no library id, so the row used to offer every finished item on the
  // server no matter which library you were looking at. ABS's own progress filter
  // answers per library, and the same response carries title + author, which
  // replaced a per-item fetch each. Cached for a minute, like the ratings map.
  // Which library does an item belong to? Answered per item, in one batch call.
  //
  // The obvious route, /api/libraries/:id/items?filter=progress.<base64
  // "finished">, CANNOT be trusted, and this row is the worst possible caller
  // for it. Measured against ABS 2.36.0 shortly after marking twelve books
  // finished: the profile listed 19 finished items while that query returned 3,
  // and its answer varied with the page size asked for (limit=0 -> 18, 200 -> 6,
  // 500 -> 2) before settling on 18 for every limit some minutes later. So it
  // lags progress writes, exactly the moment this row cares about, since you
  // reach it right after finishing something. Gating on it dropped most of a
  // reader's finished books, and often emptied the row completely.
  //
  // mediaProgress is authoritative for WHAT you finished and WHEN (it is the
  // user's own record), and /api/items/batch/get answers WHICH LIBRARY for the
  // handful of ids that matter, a direct item read with no filter layer in
  // front of it, in a single request, and carries the titles too.
  const nhRfLibOf = {};       // itemId -> libraryId ('' = gone / unknowable)
  const nhRfResolving = {};   // itemId -> true while a lookup is in flight
  function nhRfResolve(ids) {
    const want = ids.filter((id) => nhRfLibOf[id] === undefined && !nhRfResolving[id]);
    if (!want.length) return ids.every((id) => nhRfLibOf[id] !== undefined);
    want.forEach((id) => { nhRfResolving[id] = true; });
    const tk = window.__NH_TOKEN || getTokenNH();
    const done = (list) => {
      // Anything asked for but not returned is gone (deleted item); record it as
      // unknowable so the row never waits on it forever.
      const seen = {};
      list.forEach((it) => {
        if (!it || !it.id) return;
        seen[it.id] = true;
        nhRfLibOf[it.id] = it.libraryId || '';
        const md = (it.media && it.media.metadata) || {};
        if (md.title) nhRfTitles[it.id] = md.title;
        nhRfAuthors[it.id] = md.authorName || (md.authors || []).map((x) => x.name).join(', ') || '';
      });
      want.forEach((id) => {
        if (!seen[id]) nhRfLibOf[id] = '';
        delete nhRfResolving[id];
      });
    };
    fetch('/api/items/batch/get', {
      method: 'POST', credentials: 'include',
      headers: { Authorization: 'Bearer ' + tk, 'Content-Type': 'application/json' },
      body: JSON.stringify({ libraryItemIds: want }),
    })
      .then((r) => (r.ok ? r.json() : Promise.reject(new Error('HTTP ' + r.status))))
      .then((j) => { done((j && (j.libraryItems || j.results)) || []); })
      .catch((e) => {
        try { console.warn('[NanoHive] batch item lookup failed, falling back to one-by-one:', e && e.message); } catch (e2) {}
        // Older/odd deployments: ask for them individually rather than give up.
        Promise.all(want.map((id) => fetch('/api/items/' + id, { headers: { Authorization: 'Bearer ' + tk }, credentials: 'include' })
          .then((r) => (r.ok ? r.json() : null))
          .catch(() => null))).then((list) => done(list.filter(Boolean)));
      });
    return false;
  }

  function nhRateFinished() {
    const libId = nhRfHome();
    const existing = document.getElementById('nh-rate-finished-row');
    if (!libId) { if (existing) existing.remove(); return; }

    // Ratings map: { items: { <itemId>: { <userId>: {...} } } }. Refreshed at most
    // once a minute -- a rating left on the book page should remove the card here
    // without a reload, but this must not become a per-tick request.
    const now = Date.now();
    if (!nhRf.loading && (!nhRf.rates || now - nhRf.ratesAt > 60000)) {
      nhRf.loading = true;
      fetch('/_nh/api/ratings', { headers: { Authorization: 'Bearer ' + (window.__NH_TOKEN || getTokenNH()) }, credentials: 'include' })
        .then((r) => (r.ok ? r.json() : null))
        .then((j) => { nhRf.rates = (j && j.items) || {}; nhRf.ratesAt = Date.now(); nhRf.loading = false; })
        .catch(() => { nhRf.rates = {}; nhRf.ratesAt = Date.now(); nhRf.loading = false; });
    }
    if (!nhRf.rates) return;

    let me = null, prog = [];
    try { me = window.$nuxt.$store.state.user.user; prog = (me && me.mediaProgress) || []; } catch (e) {}
    if (!me) return;

    // Ordering comes from the profile (mediaProgress is the only thing that knows
    // when you finished something), library membership from the batch lookup.
    // Only a window of the most recent finishes is resolved, a reader with a
    // thousand finished books should not trigger a thousand lookups to fill a
    // fourteen-card row.
    const recent = prog.filter((x) => x && x.isFinished && x.libraryItemId)
      .sort((a, b) => (b.finishedAt || b.lastUpdate || 0) - (a.finishedAt || a.lastUpdate || 0))
      .map((x) => x.libraryItemId)
      .filter((id) => !(nhRf.rates[id] && nhRf.rates[id][me.id]))
      .slice(0, 60);

    if (!recent.length) { if (existing) existing.remove(); nhRf.key = libId + '|'; return; }
    // Hold the row until membership is known, rather than showing another
    // library's books for a beat and yanking them back out.
    if (!nhRfResolve(recent)) return;

    const ids = recent.filter((id) => nhRfLibOf[id] === libId).slice(0, 14);

    const key = libId + '|' + ids.join(',');
    if (!ids.length) { if (existing) existing.remove(); nhRf.key = key; return; }
    if (existing && existing.dataset.sig === key) return;

    // Titles/covers come from the store's loaded items where possible; anything
    // missing is fetched once and memoised on the row itself.
    const T = nhGsT();
    const row = existing || document.createElement('div');
    row.id = 'nh-rate-finished-row';
    row.dataset.sig = key;
    row.innerHTML = '';
    const h = document.createElement('h2');
    h.className = 'nh-rf-heading';
    h.textContent = T.rfTitle || PANEL_T.en.rfTitle;
    row.appendChild(h);
    const scroll = document.createElement('div');
    scroll.className = 'nh-rf-scroll';
    row.appendChild(scroll);

    const tk = window.__NH_TOKEN || getTokenNH();
    // Titles and authors already arrived with the batch lookup that established
    // library membership, so there is nothing left to fetch per card.
    ids.forEach((id) => {
      const a = document.createElement('a');
      a.className = 'nh-rf-card';
      a.href = getBaseNH() + '/item/' + id;
      a.dataset.id = id;
      const coverUrl = '/api/items/' + id + '/cover?width=400' + (tk ? '&token=' + encodeURIComponent(tk) : '');
      const cov = document.createElement('div');
      cov.className = 'nh-rf-cover';
      cov.style.backgroundImage = 'url("' + coverUrl + '")';
      a.appendChild(cov);
      const t = document.createElement('p');
      t.className = 'nh-rf-title';
      t.textContent = nhRfTitles[id] || '';
      a.appendChild(t);
      // The tile says what it is instead of only revealing stars under a cursor
      // that a phone does not have. Tapping anywhere on the tile opens the sheet.
      const cta = document.createElement('p');
      cta.className = 'nh-rf-cta';
      const ghost = document.createElement('span');
      ghost.className = 'nh-rt-stars';
      ghost.textContent = '★★★★★';
      cta.appendChild(ghost);
      const ctaLbl = document.createElement('span');
      ctaLbl.textContent = T.rfRate || PANEL_T.en.rfRate;
      cta.appendChild(ctaLbl);
      a.appendChild(cta);
      // Still a real <a> (middle-click / long-press / "open in new tab" all keep
      // working), but a plain click rates rather than navigates -- the sheet
      // carries its own link to the book page.
      a.addEventListener('click', (ev) => {
        if (ev.metaKey || ev.ctrlKey || ev.shiftKey || ev.button > 0) return;
        ev.preventDefault();
        nhRfSheet(id, nhRfTitles[id] || t.textContent, nhRfAuthors[id] || '', coverUrl, () => {
          // Force the ratings map to refetch so the row's own filter agrees on
          // the next pass, and collapse the card out rather than blinking it away.
          nhRf.ratesAt = 0;
          a.classList.add('nh-rf-done');
          setTimeout(() => { a.remove(); }, 260);
        });
      });
      scroll.appendChild(a);
    });

    if (!row.parentNode) {
      const host = nhHomeRowHost();
      if (host) host.appendChild(row); else return;
    }
    try {
      const sib = nhShelfRows().find((r) => r.id !== 'nh-rate-finished-row' && r.id !== 'nh-recent-series-row');
      const pad = nhShelfIndent(sib);
      if (pad) { row.style.paddingLeft = pad.left; row.style.paddingRight = pad.right; }
    } catch (e) {}
    // Scroll arrows, the ones ABS gives its own shelves (#21). After the cards are
    // in, so the first overflow measurement is against real content.
    try { nhShelfArrows(row, '.nh-rf-heading', '.nh-rf-scroll'); } catch (e) {}
    nhRfSize();
    nhRf.key = key;
  }
  const nhRfTitles = {};
  const nhRfAuthors = {};

  // Tile size for the rate row. Same approach as applyRecentSeriesSize: measure a
  // real, visible ABS cover and title rather than guessing, so the row follows the
  // cover-size button. It used to be hardcoded at 154px/0.82rem, which is why this
  // one row ignored the button and sat at its own size next to everything else.
  function nhRfSize() {
    // Sizes BOTH theme rows (rate-finished and mark-finished) off one measure.
    ['nh-rate-finished-row', 'nh-mark-finished-row'].forEach((rid) => nhRfSizeOne(document.getElementById(rid)));
  }
  function nhRfSizeOne(row) {
    if (!row) return;
    let w = 0;
    const els = document.querySelectorAll('#bookshelf [id^="cover-area-"]');
    for (const el of els) {
      if (el.closest('#nh-hero-container') || el.closest('#nh-recent-series-row') || el.closest('#nh-rate-finished-row') || el.closest('#nh-mark-finished-row')) continue;
      const r = el.getBoundingClientRect();
      if (r.width > 40) { w = r.width; break; }
    }
    if (!w) { try { w = (window.$nuxt && window.$nuxt.$store && window.$nuxt.$store.state.globals && window.$nuxt.$store.state.globals.bookshelfBookWidth) || 0; } catch (e) {} }
    if (w > 40) {
      const v = Math.round(w) + 'px';
      if (row.style.getPropertyValue('--nh-rf-cw') !== v) row.style.setProperty('--nh-rf-cw', v);
    }
    const titles = document.querySelectorAll('#bookshelf [cy-id="title"]');
    for (const el of titles) {
      if (el.closest('#nh-hero-container') || el.closest('#nh-recent-series-row')) continue;
      if (el.getBoundingClientRect().width > 10) {
        const fs = getComputedStyle(el).fontSize;
        if (row.style.getPropertyValue('--nh-rf-fs') !== fs) row.style.setProperty('--nh-rf-fs', fs);
        break;
      }
    }
  }

  // Confirmation before any mark-as-finished write (Pawel: a one-tap write was
  // too easy to fire by accident). One dialog for the home row and the stats
  // card; the strings reuse ysMarkDone/impCancel, which every language carries.
  function nhAmfConfirm(title, onYes) {
    const T = nhGsT();
    const old = document.getElementById('nh-amf-confirm'); if (old) old.remove();
    const overlay = document.createElement('div'); overlay.id = 'nh-amf-confirm';
    const bg = document.createElement('div'); bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div'); box.className = 'nh-rt-modal-box';
    const close = () => overlay.remove();
    bg.addEventListener('click', close);
    const head = document.createElement('div'); head.className = 'nh-rt-modal-head';
    const ht = document.createElement('span'); ht.textContent = T.ysMarkDone || PANEL_T.en.ysMarkDone;
    const x = document.createElement('button'); x.type = 'button'; x.className = 'nh-rt-modal-x'; x.textContent = '×';
    x.addEventListener('click', close);
    head.appendChild(ht); head.appendChild(x);
    const bt = document.createElement('p'); bt.className = 'nh-amf-q-title'; bt.textContent = title || '';
    const q = document.createElement('p'); q.className = 'nh-amf-q'; q.textContent = T.amfConfirm || PANEL_T.en.amfConfirm;
    const actions = document.createElement('div'); actions.className = 'nh-amf-actions';
    const yes = document.createElement('button'); yes.type = 'button'; yes.className = 'nh-amf-yes';
    yes.textContent = T.ysMarkDone || PANEL_T.en.ysMarkDone;
    yes.addEventListener('click', () => { close(); onYes(); });
    const no = document.createElement('button'); no.type = 'button'; no.className = 'nh-amf-no';
    no.textContent = T.impCancel || PANEL_T.en.impCancel;
    no.addEventListener('click', close);
    actions.appendChild(yes); actions.appendChild(no);
    box.appendChild(head); box.appendChild(bt); box.appendChild(q); box.appendChild(actions);
    overlay.appendChild(bg); overlay.appendChild(box);
    document.body.appendChild(overlay);
  }

  // ===================== MARK AS FINISHED ROW (home) =====================
  // The almost-done books (94%+, idle three days, something else played since)
  // as a home shelf: one tap marks the book finished and the card collapses
  // away. Same visual language as the rate row, so it reuses its classes,
  // resolver caches and sizing.
  const nhAmf = { key: '' };

  function nhAmfHome() {
    if (nhSettings.showMarkFinished === false) return null;
    const m = location.pathname.match(/\/library\/([^/]+)\/?$/);
    return m ? m[1] : null;
  }

  function nhMarkFinishedRow() {
    const libId = nhAmfHome();
    const existing = document.getElementById('nh-mark-finished-row');
    if (!libId) { if (existing) existing.remove(); return; }
    const list = nhAmfList();
    const cand = list.map((x) => x.libraryItemId).slice(0, 60);
    if (!cand.length) { if (existing) existing.remove(); nhAmf.key = libId + '|'; return; }
    if (!nhRfResolve(cand)) return;
    const ids = cand.filter((id) => nhRfLibOf[id] === libId).slice(0, 14);
    const key = libId + '|' + ids.join(',');
    if (!ids.length) { if (existing) existing.remove(); nhAmf.key = key; return; }
    if (existing && existing.dataset.sig === key) return;

    const T = nhGsT();
    const pmap = {};
    list.forEach((x) => { pmap[x.libraryItemId] = Math.max(x.progress || 0, x.ebookProgress || 0); });
    const row = existing || document.createElement('div');
    row.id = 'nh-mark-finished-row';
    row.dataset.sig = key;
    row.innerHTML = '';
    const h = document.createElement('h2');
    h.className = 'nh-rf-heading';
    h.textContent = T.ysMarkDone || PANEL_T.en.ysMarkDone;
    row.appendChild(h);
    const scroll = document.createElement('div');
    scroll.className = 'nh-rf-scroll';
    row.appendChild(scroll);

    const tk = window.__NH_TOKEN || getTokenNH();
    ids.forEach((id) => {
      const a = document.createElement('a');
      a.className = 'nh-rf-card';
      a.href = getBaseNH() + '/item/' + id;
      a.dataset.id = id;
      const coverUrl = '/api/items/' + id + '/cover?width=400' + (tk ? '&token=' + encodeURIComponent(tk) : '');
      const cov = document.createElement('div');
      cov.className = 'nh-rf-cover';
      cov.style.backgroundImage = 'url("' + coverUrl + '")';
      a.appendChild(cov);
      const t = document.createElement('p');
      t.className = 'nh-rf-title';
      t.textContent = nhRfTitles[id] || '';
      a.appendChild(t);
      const cta = document.createElement('p');
      cta.className = 'nh-rf-cta';
      const chk = document.createElement('span');
      chk.className = 'material-symbols nh-amf-chk';
      chk.textContent = 'check_circle';
      cta.appendChild(chk);
      const ctaLbl = document.createElement('span');
      ctaLbl.textContent = (T.ysMarkDone || PANEL_T.en.ysMarkDone) + (pmap[id] ? ' · ' + Math.floor(pmap[id] * 100) + '%' : '');
      cta.appendChild(ctaLbl);
      a.appendChild(cta);
      // Real <a> for middle-click; a plain click asks, then marks finished.
      a.addEventListener('click', (ev) => {
        if (ev.metaKey || ev.ctrlKey || ev.shiftKey || ev.button > 0) return;
        ev.preventDefault();
        nhAmfConfirm(nhRfTitles[id] || t.textContent, () => {
          nhFdPatch(id, { isFinished: true }, () => {
            try {
              const mp = window.$nuxt.$store.state.user.user.mediaProgress;
              mp.forEach((x) => { if (x.libraryItemId === id) { x.isFinished = true; x.progress = 1; x.finishedAt = Date.now(); } });
            } catch (e) {}
            a.classList.add('nh-rf-done');
            setTimeout(() => { a.remove(); if (!scroll.children.length) row.remove(); }, 260);
          });
        });
      });
      scroll.appendChild(a);
    });

    if (!row.parentNode) {
      const host = nhHomeRowHost();
      if (host) host.appendChild(row); else return;
    }
    try {
      const sib = nhShelfRows().find((r) => r.id !== 'nh-mark-finished-row' && r.id !== 'nh-rate-finished-row' && r.id !== 'nh-recent-series-row');
      const pad = nhShelfIndent(sib);
      if (pad) { row.style.paddingLeft = pad.left; row.style.paddingRight = pad.right; }
    } catch (e) {}
    try { nhShelfArrows(row, '.nh-rf-heading', '.nh-rf-scroll'); } catch (e) {}
    nhRfSize();
    nhAmf.key = key;
  }

  // Avatar badge: how many books are waiting to be marked finished. Click leads
  // to the Almost done card on the stats page (the badge intercepts, the
  // account link underneath keeps working for normal clicks elsewhere).
  function nhAmfBadge() {
    const link = document.querySelector('#appbar a[href$="/account"]');
    if (!link) return;
    let b = link.querySelector('.nh-amf-badge');
    const on = nhSettings.showMarkFinished !== false && nhSettings.finishedTools !== false;
    const n = on ? nhAmfList().length : 0;
    if (!n) { if (b) b.remove(); return; }
    const T = nhGsT();
    const shown = n > 9 ? '9+' : String(n);
    const label = n + ' · ' + (T.ysAlmost || PANEL_T.en.ysAlmost);
    if (!b) {
      b = document.createElement('span');
      b.className = 'nh-amf-badge';
      b.addEventListener('click', (ev) => {
        ev.preventDefault();
        ev.stopPropagation();
        // the Almost done card lives on Your Stats (/config/stats), NOT the
        // library stats page (that one is the server ranking)
        try { sessionStorage.setItem('nh-amf-goto', '1'); } catch (e) {}
        try { window.$nuxt.$router.push('/config/stats'); } catch (e) {}
      });
      link.appendChild(b);
    }
    if (b.textContent !== shown) b.textContent = shown;
    if (b.title !== label) b.title = label;
  }

  // The badge's landing: once the stats page has painted the Almost done card,
  // scroll it into view and flash it, then forget the errand.
  function nhAmfGoto() {
    let want = null;
    try { want = sessionStorage.getItem('nh-amf-goto'); } catch (e) {}
    if (!want) return;
    const card = document.getElementById('nh-ys-almost');
    if (!card) return;
    try { sessionStorage.removeItem('nh-amf-goto'); } catch (e) {}
    card.scrollIntoView({ behavior: 'smooth', block: 'center' });
    card.classList.add('nh-amf-flash');
    setTimeout(() => { card.classList.remove('nh-amf-flash'); }, 2600);
  }

  // Rate sheet: one book, one oversized star row, an explicit Save. Replaces
  // rating straight off the tile, that needed hover to even appear, and where it
  // did appear on touch a stray tap wrote a rating with no confirmation.
  function nhRfSheet(id, title, author, coverUrl, onSaved) {
    const T = nhGsT();
    const old = document.getElementById('nh-rf-sheet');
    if (old) old.remove();
    let value = 0;

    const overlay = document.createElement('div');
    overlay.id = 'nh-rf-sheet';
    const bg = document.createElement('div');
    bg.className = 'nh-rt-modal-bg';
    const box = document.createElement('div');
    box.className = 'nh-rt-modal-box';
    const close = () => { overlay.remove(); document.removeEventListener('keydown', onKey); };
    const onKey = (e) => { if (e.key === 'Escape') close(); };
    document.addEventListener('keydown', onKey);
    bg.addEventListener('click', close);

    const head = document.createElement('div');
    head.className = 'nh-rt-modal-head';
    const htxt = document.createElement('span');
    htxt.textContent = T.rfSheet || PANEL_T.en.rfSheet;
    const x = document.createElement('button');
    x.type = 'button';
    x.className = 'nh-rt-modal-x';
    x.textContent = '×';
    x.addEventListener('click', close);
    head.appendChild(htxt); head.appendChild(x);
    box.appendChild(head);

    const top = document.createElement('div');
    top.className = 'nh-rf-sheet-top';
    const cv = document.createElement('div');
    cv.className = 'nh-rf-sheet-cover';
    cv.style.backgroundImage = 'url("' + coverUrl + '")';
    const meta = document.createElement('div');
    meta.className = 'nh-rf-sheet-meta';
    const mt = document.createElement('p');
    mt.className = 'nh-rf-sheet-t';
    mt.textContent = title || '';
    const ma = document.createElement('p');
    ma.className = 'nh-rf-sheet-a';
    ma.textContent = author || '';
    meta.appendChild(mt); meta.appendChild(ma);
    top.appendChild(cv); top.appendChild(meta);
    box.appendChild(top);

    // Same two-layer widget as the book page (grey base + amber overlay clipped
    // by width), which gives half-stars for free.
    const pick = document.createElement('div');
    pick.className = 'nh-rf-pick';
    const stars = document.createElement('span');
    stars.className = 'nh-rt-stars';
    const base = document.createElement('span');
    base.textContent = '★★★★★';
    const fill = document.createElement('span');
    fill.className = 'nh-rt-fill';
    fill.textContent = '★★★★★';
    fill.style.width = '0%';
    stars.appendChild(base); stars.appendChild(fill);
    pick.appendChild(stars);
    box.appendChild(pick);

    // Empty until a rating is picked: the old "Pick a rating" line only restated
    // the row of stars right above it. The element stays so its reserved height
    // keeps the buttons from jumping when the value appears.
    const val = document.createElement('p');
    val.className = 'nh-rf-val';
    box.appendChild(val);

    // Pointer events, not mouse events: this has to preview under a dragging
    // thumb as well as a hovering cursor.
    const valueAt = (clientX) => {
      const r = stars.getBoundingClientRect();
      if (!r.width) return 0;
      // Quantise to the chosen step rather than to a hardcoded half star; the
      // smallest pickable rating is one step, never zero.
      const st = nhStarStep();
      const v = Math.ceil(((clientX - r.left) / r.width) * (5 / st)) * st;
      return Math.max(st, Math.min(5, v));
    };
    // The readout follows the preview, not just the commit: at quarter-star
    // precision the stars alone cannot tell you whether you are on 4.25 or 4.5.
    const showVal = (v) => { val.textContent = v ? nhStarText(v) + ' / 5' : ''; };
    const preview = (v) => { nhStarFill(stars, fill, v); showVal(v); };
    const commit = (v) => { value = v; nhStarFill(stars, fill, v); showVal(v); save.disabled = false; };
    stars.addEventListener('pointermove', (ev) => { if (ev.pointerType === 'mouse' && !ev.buttons) preview(valueAt(ev.clientX)); else if (ev.buttons) commit(valueAt(ev.clientX)); });
    // Back to whatever is actually picked, which is nothing until the first tap,
    // and the line then goes empty again rather than keeping a hovered value.
    stars.addEventListener('pointerleave', () => preview(value));
    stars.addEventListener('pointerdown', (ev) => { ev.preventDefault(); commit(valueAt(ev.clientX)); });

    const acts = document.createElement('div');
    acts.className = 'nh-rt-actions';
    const save = document.createElement('button');
    save.type = 'button';
    save.className = 'nh-rt-btn';
    save.textContent = T.sdSave || PANEL_T.en.sdSave;
    save.disabled = true;
    // A real button, matching Save's shape in a quieter fill, an underlined link
    // beside a solid button read as two different kinds of control.
    const open = document.createElement('button');
    open.type = 'button';
    open.className = 'nh-rt-btn nh-rf-btn2';
    open.textContent = T.rfOpen || PANEL_T.en.rfOpen;
    open.addEventListener('click', () => { close(); nhRouterPush('/item/' + id); });
    const status = document.createElement('span');
    status.className = 'nh-rt-status';
    save.addEventListener('click', () => {
      if (!value || save.disabled) return;
      save.disabled = true;
      status.textContent = '…';
      fetch('/_nh/api/ratings', {
        method: 'POST',
        headers: { Authorization: 'Bearer ' + (window.__NH_TOKEN || getTokenNH()), 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify({ itemId: id, stars: value, review: '' })
      }).then((r) => {
        if (!r.ok) { save.disabled = false; status.textContent = '✗'; return; }
        status.textContent = '';
        close();
        if (onSaved) onSaved();
      }).catch(() => { save.disabled = false; status.textContent = '✗'; });
    });
    // Open on the left, Save on the right (Pawel).
    acts.appendChild(open); acts.appendChild(save); acts.appendChild(status);
    box.appendChild(acts);

    overlay.appendChild(bg);
    overlay.appendChild(box);
    document.body.appendChild(overlay);
  }

  // The single element that holds every home shelf row. All rows share one parent
  // (measured), so this is where our injected rows belong too.
  function nhHomeRowHost() {
    const r = document.querySelector('#bookshelf .bookshelf-row');
    if (!r) return null;
    let row = r;
    if (!r.querySelector('h2')) {
      let q = r.parentElement, hop = 0;
      while (q && hop < 4) { if (q.querySelector('h2')) { row = q; break; } q = q.parentElement; hop++; }
    }
    return row.parentElement;
  }

  // ---- home section order --------------------------------------------------
  // Keys are ABS's own shelfIds plus our injected rows. Labels come from each
  // row's live <h2>, so the reorder list is automatically in the UI language
  // without translating ABS's shelf names ourselves.
  // A shelf the theme has hidden is still in the DOM (applySettings sets
  // display:none on it), so it must not appear in the reorder list -- the native
  // "Recent Series" shelf is hidden whenever our custom row replaces it, and
  // listing both showed "Recent Series" twice with identical labels.
  function nhSectionHidden(el) {
    if (!el) return true;
    if (getComputedStyle(el).display === 'none') return true;
    // A shelf HOSTING our hero carousel is not a hidden shelf. The carousel replaces
    // that shelf's strip, so the strip is display:none while the section itself is the
    // biggest thing on the page. In STANDARD view the strip is a CHILD of the section,
    // so the inner-row test below condemned the whole section: it dropped out of the
    // reorder list, never received an order value, and an item with no `order` in a flex
    // container sorts to the very top -- Continue Listening stayed pinned first no
    // matter what order the user chose. In DETAIL view the section IS the strip, which
    // is why it only ever misbehaved in one of the two views.
    if (el.dataset && el.dataset.heroInjected === 'true') return false;
    if (el.querySelector('#nh-hero-container')) return false;
    const inner = el.querySelector('.bookshelf-row');
    return !!(inner && getComputedStyle(inner).display === 'none');
  }

  function nhHomeSections() {
    const host = nhHomeRowHost();
    if (!host) return [];
    const out = [];
    Array.prototype.forEach.call(host.children, (el) => {
      if (!(el instanceof HTMLElement)) return;
      if (nhSectionHidden(el)) return;
      let key = el.id || null;
      if (!key) {
        const vm = el.__vue__ || {};
        key = vm.shelfId || (vm.shelf && vm.shelf.id) || null;
        if (!key) {
          const inner = el.querySelector('.bookshelf-row');
          const ivm = inner && inner.__vue__;
          key = (ivm && (ivm.shelfId || (ivm.shelf && ivm.shelf.id))) || null;
        }
      }
      if (!key) return;
      const h2 = el.querySelector('h2');
      out.push({ key: key, label: (h2 && h2.textContent.trim()) || key, el: el });
    });
    return out;
  }

  function nhHomeOrderApply() {
    if (!nhRfHome() && !/\/library\/[^/]+\/?$/.test(location.pathname)) return;
    const secs = nhHomeSections();
    if (secs.length < 2) return;
    // Remember what exists so the settings panel can offer a list even when the
    // user is not standing on the home page. Written BEFORE the early return for
    // "no order set yet" -- otherwise the panel can never show a list until an
    // order exists, and an order can never be set without a list.
    try { localStorage.setItem('nh-home-sections', JSON.stringify(secs.map((s) => ({ key: s.key, label: s.label })))); } catch (e) {}
    const host = nhHomeRowHost();
    if (!host) return;
    const order = Array.isArray(nhSettings.homeOrder) ? nhSettings.homeOrder : [];
    if (!order.length) {
      // Reset ("use ABS order") has to undo both halves, or the page keeps the last
      // arrangement until a reload.
      if (host.classList.contains('nh-home-flexed')) {
        host.classList.remove('nh-home-flexed');
        secs.forEach((s) => { if (s.el.style.order) s.el.style.order = ''; });
      }
      return;
    }
    const rank = {};
    order.forEach((k, i) => { rank[k] = i; });
    const want = secs.slice().sort((a, b) => {
      const ra = rank[a.key], rb = rank[b.key];
      if (ra === undefined && rb === undefined) return 0;   // both unknown: keep ABS order
      if (ra === undefined) return 1;                        // unknown sinks below ordered ones
      if (rb === undefined) return -1;
      return ra - rb;
    });
    // CSS order, NOT appendChild. Moving ABS's own shelf nodes desynchronised
    // Vue's virtual DOM: it re-inserted them in its own order, we moved them
    // back, and the shelves re-mounted on every pass -- which is what made every
    // book tile on the home page flicker continuously. Setting `order` never
    // touches the tree, so Vue has nothing to react to.
    //
    // The flex context is marked on the HOST -- the element whose children carry
    // those order values -- and marked HERE rather than guessed at in CSS. The old
    // rule (`body.nh-home-ordered #bookshelf div:has(> .bookshelf-row)`) styled
    // whichever div directly contains a shelf strip, which is the host only in
    // ABS's DETAIL home view. In its STANDARD (skeuomorphic) view that is each
    // shelf's OWN wrapper, so the host stayed display:block, `order` does nothing
    // outside a flex/grid parent, and reordering saved but never moved anything
    // (reported from the field). Marking the resolved element cannot drift.
    host.classList.add('nh-home-flexed');
    want.forEach((s, i) => {
      const v = String(i + 1);
      if (s.el.style.order !== v) s.el.style.order = v;
    });
  }

  // Reorder UI in the settings panel. Sections and their labels come from the
  // last home page that was rendered (cached), so the list reads in whatever
  // language ABS is showing rather than needing its own translations.
  function nhHomeOrderUI(host) {
    const T = nhGsT();
    const wrap = document.createElement('div');
    wrap.className = 'nh-ho-wrap';
    const lbl = document.createElement('div');
    lbl.className = 'nh-ho-lbl';
    lbl.textContent = T.hoTitle || PANEL_T.en.hoTitle;
    wrap.appendChild(lbl);
    const list = document.createElement('div');
    list.className = 'nh-ho-list';
    wrap.appendChild(list);

    const render = () => {
      let known = [];
      try { known = JSON.parse(localStorage.getItem('nh-home-sections') || '[]') || []; } catch (e) {}
      if (!known.length) {
        list.innerHTML = '';
        const p = document.createElement('p');
        p.className = 'nh-ho-empty';
        p.textContent = T.hoEmpty || PANEL_T.en.hoEmpty;
        list.appendChild(p);
        return;
      }
      const saved = Array.isArray(nhSettings.homeOrder) ? nhSettings.homeOrder : [];
      const rank = {}; saved.forEach((k, i) => { rank[k] = i; });
      const rows = known.slice().sort((a, b) => {
        const ra = rank[a.key], rb = rank[b.key];
        if (ra === undefined && rb === undefined) return 0;
        if (ra === undefined) return 1;
        if (rb === undefined) return -1;
        return ra - rb;
      });
      const commit = () => {
        nhSettings.homeOrder = rows.map((r) => r.key);
        saveSettings();
        render();
        try { nhHomeOrderApply(); } catch (e) {}
      };
      list.innerHTML = '';
      rows.forEach((r, i) => {
        const item = document.createElement('div');
        item.className = 'nh-ho-item';
        const nm = document.createElement('span');
        nm.className = 'nh-ho-name';
        nm.textContent = r.label || r.key;
        const up = document.createElement('button');
        up.type = 'button'; up.className = 'nh-ho-btn';
        up.innerHTML = '<span class="material-symbols">arrow_upward</span>';
        up.disabled = i === 0;
        up.addEventListener('click', () => { rows.splice(i - 1, 0, rows.splice(i, 1)[0]); commit(); });
        const dn = document.createElement('button');
        dn.type = 'button'; dn.className = 'nh-ho-btn';
        dn.innerHTML = '<span class="material-symbols">arrow_downward</span>';
        dn.disabled = i === rows.length - 1;
        dn.addEventListener('click', () => { rows.splice(i + 1, 0, rows.splice(i, 1)[0]); commit(); });
        item.appendChild(nm); item.appendChild(up); item.appendChild(dn);
        list.appendChild(item);
      });
      const reset = document.createElement('button');
      reset.type = 'button'; reset.className = 'nh-ho-reset';
      reset.textContent = T.hoReset || PANEL_T.en.hoReset;
      reset.addEventListener('click', () => { nhSettings.homeOrder = []; saveSettings(); render(); });
      list.appendChild(reset);
    };
    render.wrap = wrap;
    nhHoRender = render;
    nhHoSig = nhHoSnapshotSig();
    render();
    host.appendChild(wrap);
  }

  // Hiding a shelf changes what this list should contain, but the list was built
  // once when the panel opened, so a hidden section sat there until you closed
  // the panel and reopened it. The underlying snapshot is already correct within a
  // tick (nhHomeOrderApply re-records it from the live home page, which is right
  // behind the settings modal), so all that was missing was a re-render.
  let nhHoRender = null;
  let nhHoSig = '';
  function nhHoSnapshotSig() {
    let raw = '';
    try { raw = localStorage.getItem('nh-home-sections') || ''; } catch (e) {}
    return raw + '|' + JSON.stringify(nhSettings.homeOrder || []);
  }
  function nhHomeOrderSync() {
    // A rebuilt panel leaves the previous closure pointing at a detached node.
    if (!nhHoRender || !nhHoRender.wrap || !nhHoRender.wrap.isConnected) { nhHoRender = null; nhHoSig = ''; return; }
    const sig = nhHoSnapshotSig();
    if (sig === nhHoSig) return;
    nhHoSig = sig;
    nhHoRender();
  }

  // Author cards carry NO id on ABS 2.35 (an [id^="author-card-"] selector matches
  // nothing), so the only stable hooks are cy-id plus the /author/ href. Photo-less
  // cards get an initial medallion drawn by CSS from this attribute, mirroring the
  // narrators page. Runs globally rather than from nhAuthorsBar, because the same
  // card appears in the home page's "Newest Authors" shelf, which that function
  // never sees. The shelf is virtualized and recycles nodes, so this re-runs every
  // tick; it only writes when the value actually changed, so the observer does not
  // thrash.
  function nhAuthorCardInitials() {
    document.querySelectorAll('#bookshelf a[href*="/author/"] [cy-id="imageArea"]').forEach((ia) => {
      if (ia.querySelector('img')) { if (ia.hasAttribute('data-nh-ini')) ia.removeAttribute('data-nh-ini'); return; }
      const p = ia.parentElement && ia.parentElement.querySelector('[cy-id="textInline"] p');
      const ch = (((p && p.textContent.trim().charAt(0)) || '?')).toUpperCase();
      if (ia.getAttribute('data-nh-ini') !== ch) ia.setAttribute('data-nh-ini', ch);
    });
  }

  // Multi-select puts ABS into a mode where a fixed bar covers the appbar. Flag
  // it on <body> so the theme's own appbar additions can step out of the way --
  // the bar and the appbar are both z-index 60, so which one wins is decided by
  // DOM order and varies between ABS builds.
  function nhSelectionClass() {
    let on = false;
    try {
      const g = window.$nuxt.$store.state.globals;
      on = !!(g && Array.isArray(g.selectedMediaItems) && g.selectedMediaItems.length);
    } catch (e) {}
    document.body.classList.toggle('nh-selecting', on);
  }

  // Cover-art gate. A settled layout is NOT a finished page: the grid revealed
  // fully laid out with empty cover boxes and the art popped in a beat later
  // (user: "trickle of elements, I see stuff loading"). Wait for the covers that
  // are actually in the first viewport, rows below the fold can stream in unseen
  //, and only on a budget that starts when the STRUCTURE settled, so a slow link
  // degrades to the old behaviour instead of sitting on the 3.5s failsafe.
  let nhCoverT0 = 0;
  function nhCoversReady(scope) {
    if (!scope) return true;
    if (!nhCoverT0) nhCoverT0 = Date.now();
    if (Date.now() - nhCoverT0 > 1200) return true;
    const vh = window.innerHeight || 0;
    const imgs = scope.querySelectorAll('img');
    // Cap the scan: a 120-card shelf must not cost a full layout pass per tick.
    // getBoundingClientRect (not offsetParent), the page is opacity:0 here, which
    // keeps layout, and fixed-position ancestors have no offsetParent.
    for (let i = 0, seen = 0; i < imgs.length && seen < 40; i++) {
      const img = imgs[i];
      // No src yet = ABS has not resolved this cover; that is a structural wait,
      // already covered by the branch gates. Only judge images actually fetching.
      if (!img.getAttribute('src')) continue;
      const r = img.getBoundingClientRect();
      if (!r.width || r.bottom <= 0 || r.top >= vh) continue;
      seen++;
      if (!img.complete) return false;
    }
    return true;
  }

  // Covers that miss the reveal gate must EASE in, not snap. ABS resizes covers on
  // demand, so a first visit to a shelf can take seconds per thumbnail, no reveal
  // gate can wait that out without leaving the user on a blank canvas. Fading each
  // cover as it decodes is what turns the leftover "stuff loading" into something
  // that reads as intentional. ONE delegated capturing listener per scroller (load
  // does not bubble, but it does capture), a per-image handler on a 120-card shelf
  // would cost more than it saves.
  function nhCoverFadeIn() {
    ['bookshelf', 'item-page-wrapper'].forEach(function (id) {
      const el = document.getElementById(id);
      if (!el || el.dataset.nhImgFade === '1') return;
      el.dataset.nhImgFade = '1';
      el.addEventListener('load', function (e) {
        const t = e.target;
        if (t && t.tagName === 'IMG') t.classList.add('nh-img-in');
      }, true);
    });
  }

  // Reveal + re-arm the entrance window. The eased card entrance has to outlive the
  // REVEAL, not the route change: the fixed 2.2s window from navigation had already
  // expired by the time a slow library page appeared, so every card that mounted
  // afterwards popped in instead of fading.
  function nhPageShown(body) {
    body.classList.add('nh-page-ready');
    nhEnterWindow(1200);
  }

  // ABS syncs the user's saved sort into the URL a beat AFTER the shelf first
  // mounts (search "" -> "?sort=media.metadata.title&desc=0"), and that re-query
  // tears the shelf down to zero cards and refetches it. Whether it lands before or
  // after our reveal is a RACE decided by library size and link speed: on the dev
  // sandbox it lands first and the mask covers it, on Pawel's server it lands second
  // so the page appeared and then visibly "reloaded all elements". Hold the reveal
  // until the sync has landed. Bounded, builds that never sync (or libraries with
  // no saved sort) must not sit masked waiting for a param that is not coming.
  // Pre-seed the route query so ABS's post-mount sync is a NO-OP.
  //
  // Verified against an UNTHEMED ABS on :13378, stock Audiobookshelf pushes the bare
  // path and then replaceStates the user's saved sort onto it, which re-queries and
  // rebuilds the whole shelf. We cannot drop that without losing sort persistence and
  // deep links. Instead we arrive with the query already applied, via a router guard
  // that runs BEFORE the page component mounts, so ABS finds nothing to change.
  //
  // The params are LEARNED from ABS, never reconstructed: whatever it settles on for a
  // route is what we replay next time. So we can't drift from its logic (the books grid
  // and the series list use different param sets), and if it ever changes its mind it
  // simply replaceStates once more and we re-learn.
  // localStorage, not session: a first visit in a fresh session would otherwise still
  // pay the double fetch. If the saved sort was changed elsewhere the replayed query
  // is stale for exactly one navigation, ABS replaceStates its own value and we
  // re-learn it, so this is self-correcting rather than something that can go wrong.
  const NH_Q_KEY = 'nh-route-q';
  let nhQMap = {};
  try { nhQMap = JSON.parse(localStorage.getItem(NH_Q_KEY) || '{}') || {}; } catch (e) {}
  const NH_Q_ROUTES = /\/bookshelf(\/series)?\/?$/;

  // Router-relative path. location.pathname carries the deployment prefix (ABS is
  // commonly served under /audiobookshelf) but the router's to.path never does, so
  // keying the map on the raw pathname made every lookup miss.
  function nhRelPath() {
    const base = getBaseNH();
    const p = location.pathname;
    return (base && p.indexOf(base) === 0 ? p.slice(base.length) : p) || '/';
  }

  function nhRememberQuery() {
    const p = nhRelPath();
    if (!NH_Q_ROUTES.test(p) || !location.search) return;
    if (nhQMap[p] === location.search) return;
    nhQMap[p] = location.search;
    try { localStorage.setItem(NH_Q_KEY, JSON.stringify(nhQMap)); } catch (e) {}
  }

  let nhRouterHooked = false;
  function nhHookRouter() {
    if (nhRouterHooked) return;
    const r = window.$nuxt && window.$nuxt.$router;
    if (!r || typeof r.beforeEach !== 'function') return;
    nhRouterHooked = true;
    r.beforeEach(function (to, from, next) {
      try {
        if (nhSettings.pageTransitions === false) return next();
        // Only bare navigations: an explicit query (a filter link, a deep link, the
        // user's own sort click) is the user's intent and must pass through untouched.
        if (to.query && Object.keys(to.query).length) return next();
        if (!NH_Q_ROUTES.test(to.path)) return next();
        const remembered = nhQMap[to.path];
        if (!remembered) return next();
        const query = {};
        remembered.replace(/^\?/, '').split('&').forEach(function (kv) {
          if (!kv) return;
          const i = kv.indexOf('=');
          const k = decodeURIComponent(i < 0 ? kv : kv.slice(0, i));
          query[k] = i < 0 ? '' : decodeURIComponent(kv.slice(i + 1));
        });
        // replace, not push, so this never adds a back-button step. The redirected
        // navigation re-enters this guard with a non-empty query and passes straight
        // through, so there is no loop.
        return next({ path: to.path, query: query, replace: true });
      } catch (e) {}
      next();
    });
  }

  function nhSortSynced(bs) {
    // ONLY the books grid and the series list get a sort param. Collections and
    // playlists share the same #bookshelf component, so vm.orderBy is stale from the
    // last books grid, but never sync one, and gating them just burned the full
    // bound (measured: collections reveal 1740ms instead of ~400ms).
    if (!/\/bookshelf(\/series)?\/?$/.test(location.pathname)) return true;
    if (Date.now() - nhMaskT0 > 1400) return true;
    const vm = bs && bs.__vue__;
    if (!vm || !vm.orderBy) return true;
    return /[?&]sort=/.test(location.search);
  }

  function nhPageReveal() {
    const body = document.body;
    if (!body.classList.contains('nh-page-loading') || body.classList.contains('nh-page-ready')) return;
    const overdue = Date.now() - nhMaskT0 > 3500; // failsafe: never stay hidden
    if (!overdue) {
      const path = location.pathname;
      const page = document.querySelector('#app-content .page');
      // Some routes render NO page container at all (the sandbox's ABS build
      // serves a blank authors list, upstream quirk), don't sit masked on
      // nothing until the 3.5s failsafe.
      if (!page) {
        if (Date.now() - nhMaskT0 > 1200) nhPageShown(body);
        return;
      }
      if (/\/library\/[^/]+\/?$/.test(path)) {
        // HOME: wait for shelves, plus the hero (container or its skeleton, both
        // appear the instant the Continue Listening shelf is identified) and the
        // custom Recent Series row (or certainty that either is not coming).
        const bs = nhShelfFresh();
        if (!bs) return;
        const shelves = nhShelfRows();
        if (!shelves.length) return;
        const clExists = shelves.some(r => nhShelfId(r) === 'continue-listening' ||
          /continue|kontynuuj|weiter|continu/.test(((r.querySelector('h2') || {}).textContent || '').toLowerCase()));
        // The REAL carousel, not its skeleton, with the first-slide-first build it
        // arrives one item-fetch after the shelf, and revealing the skeleton only to
        // swap it a beat later was the last visible home-page flicker.
        const heroSettled = nhSettings.showHeroCarousel === false || !clExists ||
          document.getElementById('nh-hero-container');
        const rsOn = nhSettings.showCustomRecentSeries && nhSettings.customSeriesCards !== false && !nhSettings.hideHomeRecentSeries;
        const rsSettled = !rsOn || document.getElementById('nh-recent-series-row') ||
          (nhRecentSeries.key && !nhRecentSeries.loading && (!nhRecentSeries.data || !nhRecentSeries.data.length));
        if (!heroSettled || !rsSettled) return;
        if (!nhCoversReady(bs)) return;
      } else if (/\/item\/[^/?#]+/.test(path)) {
        // BOOK DETAIL: the redesign has landed once the metadata grid is classed
        // and the HD cover swap has run (book-details.js does both in its tick).
        const ipw = document.getElementById('item-page-wrapper');
        if (!ipw) return;
        if (!ipw.querySelector('.nh-metadata-container')) return;
        // The HD cover swap is polish, not structure, wait briefly, never hostage.
        if (!ipw.querySelector('[data-hd-fixed="true"]') && Date.now() - nhMaskT0 < 1200) return;
        if (!nhCoversReady(ipw)) return;
      } else if (document.body.classList.contains('nh-collections-page')) {
        // Collections hides #bookshelf entirely and renders its own grid, so the
        // card check below can never pass and the page sat on the 1500ms escape
        // hatch (measured: 383ms when a stale card happened to linger, 1595ms
        // otherwise). Gate on the grid we actually draw.
        const g = document.getElementById('nh-cols-grid');
        if (!g || !g.querySelector('.nh-cl-card')) {
          if (Date.now() - nhMaskT0 < 1500) return;
        }
      } else if (/\/library\/[^/]+\/(bookshelf|narrators|authors)/.test(path)) {
        // Grid pages: at least one card rendered, on the FRESH shelf, not the
        // outgoing page's still-mounted one. Two escape hatches: some builds
        // render authors/narrators with NO #bookshelf at all (blank-page ABS
        // quirk), and legitimately empty grids (filter with 0 results) never
        // get a card, neither may hold the mask to the 3.5s failsafe.
        const bs = nhShelfFresh();
        if (!bs) {
          if (Date.now() - nhMaskT0 < 1200) return;
        } else if (!bs.querySelector('[cy-id="card"], [id^="book-card-"], [id^="cover-area-"], [id^="series-card-"], [id^="collection-card-"], [id^="author-card-"], [id^="narrator-card-"]')) {
          if (Date.now() - nhMaskT0 < 1500) return;
        } else if (!nhSortSynced(bs)) {
          // Checked BEFORE the geometry/cover gates: these cards are about to be
          // destroyed by the sync, so measuring or waiting on them is wasted budget.
          return;
        } else if (!nhGridSettled(bs)) {
          // Pawel's priority: no trickle-in. ABS lays the shelf out with the
          // DEFAULT cover size and re-lays it once the user's saved size arrives,
          // which moves every card and changes the side margins a beat after the
          // reveal. Hold the mask until two consecutive samples agree.
          return;
        } else if (!nhCoversReady(bs)) {
          // ...and the boxes must have their art in them, not just their place.
          return;
        }
      } else if (path.includes('/config')) {
        if (!document.querySelector('.configContent')) return;
      } else {
        // Collection/author/playlist details and anything else: content mounted.
        if (!page.firstElementChild) return;
      }
      // Conditions met: reveal two animation frames from now, so everything this
      // tick injected/restyled has actually painted underneath the mask first.
      if (nhPgArm !== path) {
        nhPgArm = path;
        requestAnimationFrame(() => requestAnimationFrame(() => {
          if (location.pathname === path) nhPageShown(body);
        }));
      }
      return;
    }
    nhPageShown(body);
  }
  try {
    const origPush = history.pushState;
    history.pushState = function () { const r = origPush.apply(this, arguments); try { nhRouteMask(); nhQueueTick(); } catch (e) {} return r; };
    const origReplace = history.replaceState;
    history.replaceState = function () { const r = origReplace.apply(this, arguments); try { nhRouteMask(); nhQueueTick(); } catch (e) {} return r; };
    window.addEventListener('popstate', function () { try { nhRouteMask(); nhQueueTick(); } catch (e) {} });
    nhRouteMask();
  } catch (e) {}

  function nhSeriesReveal() {
    const body = document.body;
    if (!body.classList.contains('nh-series-page') || body.classList.contains('nh-series-ready')) return;
    const bookshelf = document.getElementById('bookshelf');
    const h = document.getElementById('nh-series-header');
    const born = h ? (parseInt(h.dataset.born, 10) || 0) : 0;
    const overdue = born && Date.now() - born > 4000; // failsafe: never stay hidden
    if (!overdue) {
      if (!bookshelf || !h || !bookshelf.classList.contains('nh-with-series-header')) return;
      if (bookshelf.dataset.nhRebuilt !== location.pathname) return;
      // structure + badges are in: reveal two frames from now (post-paint)
      if (bookshelf.dataset.nhRevealArm !== location.pathname) {
        bookshelf.dataset.nhRevealArm = location.pathname;
        const path = location.pathname;
        requestAnimationFrame(() => requestAnimationFrame(() => {
          if (location.pathname === path) body.classList.add('nh-series-ready');
        }));
      }
      return;
    }
    body.classList.add('nh-series-ready');
  }

  // Veil lift. The head-injected FOUC blocker now paints a full-screen canvas-
  // coloured veil (+spinner) over <body> instead of hiding it, so Vue boots and
  // renders UNDERNEATH, users never see the stock logo / "audiobookshelf" name
  // flash before the theme re-brands them (user report). Reveal on the first tick
  // where the app shell exists: applySettings runs EARLIER in the same tick, so by
  // now the logo/name are already swapped. The 4s head timeout stays as failsafe.
  function nhLiftVeil() {
    const html = document.documentElement;
    if (html.classList.contains('nh-ready')) return;
    if (nhSp.hold) return; // start-page redirect still pending, stay veiled (A4)
    if (!document.querySelector('#appbar a[href$="/"] img') &&
        !document.querySelector('#page-wrapper img[alt="Audiobookshelf Logo"]')) return;
    html.classList.add('nh-ready');
  }

  const nhWarned = {};
  function runMutations() {
    // One failure must not block the rest, but it must not be invisible either:
    // log the FIRST failure per subsystem so field issues are diagnosable from F12.
    const safe = (fn) => {
      try { fn(); } catch (e) {
        if (!nhWarned[fn.name]) {
          nhWarned[fn.name] = 1;
          try { console.warn('[NanoHive] ' + fn.name + ' failed:', e); } catch (e2) {}
        }
      }
    };
    safe(nhDetectDuplicateTheme);
    safe(nhSettingsUserWatch);
    safe(nhVersionFooter);
    safe(nhSeriesScale);
    safe(nhCoverModeClass);
    safe(nhSeriesHeader);
    safe(nhSeriesCardCovers);
    safe(nhSeriesProgress);
    safe(nhShelfArrowsTick);
    safe(nhGlobalSearch);
    safe(nhStartPage);
    safe(nhTagFinished);
    safe(nhCardRatings);
    safe(nhCardPlayIcon);
    safe(nhLibFilter);
    safe(nhNarratorsPage);
    safe(nhAuthorsBar);
    safe(nhCollectionPage);
    safe(nhCollectionsLanding);
    safe(nhStatsScoreboard);
    safe(nhWeekChart);
    safe(nhYourStatsExtras);
    safe(nhLibraryStats);
    safe(nhLsNavLabel);
    safe(nhReportsBadge);
    safe(nhAmfBadge);
    safe(nhAmfGoto);
    safe(nhAccountReports);
    safe(nhDpRouteGuard);
    safe(nhSelGuard);
    safe(nhModalLock);
    safe(nhPlayerPad);
    safe(nhBookAuthorLinks);
    safe(nhHeatmapSkin);
    safe(nhStatsFit);
    safe(nhHeatmapCaption);
    safe(nhAutoplaySeries);
    safe(nhFamilyStatsSync);
    safe(nhFsAdminSeed);
    safe(nhFamilyProgressSync);
    safe(nhPgAdminSeed);
    safe(nhUsersPage);
    safe(nhUserEditModal);
    safe(nhEditModalGoodreads);
    safe(nhCmSyncTick);
    safe(nhSeriesReveal);
    safe(nhEreader);
    safe(nhEreaderPreviewSync);
    safe(applySettings);
    safe(nhLiftVeil);
    safe(injectSettingsPanel);
    safe(sweepFooters);
    safe(swapStatsLinks);
    safe(remapRailIcons);
    safe(nhApplyTabIdentity); // Nuxt re-renders <head> on navigation
    safe(localizeRail);
    safe(nhAccountAvatar);
    safe(injectMobileMenuButton);
    safe(nhSyncMobileDrawer);
    safe(hideMobileUploadButton);
    safe(injectGearButton);
    safe(injectHeroBanner);
    safe(manageRecentSeries);
    safe(applyRecentSeriesSize);
    safe(nhRfSize);
    safe(nhShelfNudge);
    // core.js owns the toolbar layout classes; drive them from the reactive tick too
    // so a remounted #toolbar is re-classed within a frame, not on the 200ms poll.
    safe(function nhLayout() { if (window.__nhManageLayout) window.__nhManageLayout(); });
    safe(nhAuthorCardInitials);
    safe(nhRateFinished);
    safe(nhMarkFinishedRow);
    safe(nhForceDetailView);
    safe(nhStdShelfTitles);
    safe(nhHomeOrderApply);
    safe(nhHomeOrderSync);
    safe(nhSelectionClass);
    safe(nhHookRouter);
    safe(nhCoverFadeIn);
    safe(nhPageReveal);
    safe(nhRequeryReveal);
    safe(manageCinematic);
    safe(injectBookSites);
    safe(nhWhoReading);
  }

  // Reactive scheduler: run a tick within ~80ms of any DOM change instead of only
  // every 500ms, masks reveal (and injections land) as soon as content actually
  // mounts, cutting up to ~1s of perceived latency per navigation. Safe against
  // feedback loops because every function in runMutations is idempotent (checks
  // before writing): once the page settles, a tick makes no mutations and the
  // observer goes quiet. The interval stays as a heartbeat for non-DOM state.
  let nhTickQueued = false;
  let nhLastTickAt = 0;
  function nhQueueTick() {
    if (nhTickQueued) return;
    nhTickQueued = true;
    requestAnimationFrame(() => {
      const wait = Math.max(0, 80 - (Date.now() - nhLastTickAt));
      setTimeout(() => {
        nhTickQueued = false;
        nhLastTickAt = Date.now();
        runMutations();
      }, wait);
    });
  }
  try {
    new MutationObserver(nhQueueTick).observe(document.documentElement, { childList: true, subtree: true });
  } catch (e) {}
  setInterval(runMutations, 500);
})();
